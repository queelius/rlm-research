# Main acceptance review: source cue timing48

Main read the approved design, own native CPU feasibility and CPU decision; all
five frozen Python source/tests, four documents, and relevant inherited run/owned
seams. READYbbc209707a096912fc5240b096dcc02cac9cd6683b83ab2a2a64f940ab350ad4;
SPEC3064b9d04607734f0ec9db01f826c9a86d2827fefc28b9105bfdf0114efcca8c.
Fresh main nine focused tests PASS in1.59seconds; owned verify exit0, confirming
exact fixed c32de/config/base closure. No model/GPU calls during main verification.

Exactly two exposed64-record contexts per task, TREC and SST, two fresh seeds,
three output-tag rules and two field orders produce48 calls. All use the first
historical input presentation, disjoint q source numerals and historical c32de
weights. No new data, trained seed, crop, outcome selection or whole-RLM claim.
Only ordered decoder schema differs within a pair: messages, seeds, full physical
input IDs and every non-schema body field are identical. Both instructions use
neutral 'tag and label'. Actual CPU XGrammar acceptance/rejection is required
rather than assuming JSON schema insertion order controls all backends.

The preparation exactly reproduces all48 parent feasibility request/schema/token
identities,24 identical-input pairs,16 ordered schemas, max2838+3072=5910.
Ordered body/schema hashes and actual outgoing bytes are checked separately from
sorted-key digests, which erase property order. A focused test demonstrates that
the wire check rejects properties-only reordering even when dict/canonical digest
equality survives. Raw output must have assigned key order, exact cardinality,
duplicate-free objects, correct prescribed tags and canonical labels before the
unchanged original-content semantic scorer is applied. There is no output repair.

Primary is displayed-record matching-minus-ordinal within order and their order
interaction, reported separately by task/context/seed. Constant outputs and class
count errors are secondary. Previous-record agreement at positions2..64 is an
unrepaired diagnostic with first item separate and prediction/gold multiset chance.
Repeated labels cannot identify an attention mechanism. Two clusters per task are
not3072 independent observations. Generic field-order sensitivity is prior work.

The owned wrapper reuses one qualified single-adapter service and disappearance
observer; private adaptations change only study names/calls/clocks. Four workers,
collection600/work780/owned900/outer930 with120 cleanup reserve. Pre-main imports
are covered by outer cap; small builtin summary precedes release and any measurable
CPU tail remains cost. Detailed independent audit follows release. First request
error stops new dispatch without retry; prior completed outputs persist, errors
and unrun are null, completed malformed/order-invalid output strict0. Attempted
HTTP is not proven sampling; all usage/missingness and raw token IDs are retained.

Exact waiting predecessor adaptive3368987/start1074998031, conservative bound
1788969475.588377 (2026-09-09T15:57:55.588Z). No actual adaptive
launch is invented. Cue plus all acceptance/GPUclear/job/cleanup margins fits
through2026-09-09T16:25:25.588Z, before allocation end19:07:17.
A thin adapter changes only the already qualified coordinator ROOT. Two missing-
file tests were observed RED before implementation; both PASS in0.02seconds and
read-only coordinator path verification exits0 before acceptance. A separate
reviewer preparing the outcome method also inspected the ordered-hash/scoring
seams and reported no material flaw; that bounded check is not exhaustive.
