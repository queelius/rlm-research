# Parent review: typed helper child36

September9,2026, before this run's outcomes. Main read the new matcher,
request-local hook, experiment/driver, results, native qualifier, preparation,
both test modules and runbook. Main also read the adopted private-image wrapper,
short-alias isolation and inherited adapter/build; existing sources remain pinned.

The question is whether structurally constrained helper replies improve whole-RLM
answers. Twelve paired task coordinates use fixed historical weights and new seeds;
T minus restored-raw is primary, familiar unchanged is calibration. All six contexts
are exposed. No selection on prior helper uptake or success. Same seeds are not
deterministic counterfactuals. Grammar changes child action space, not just wording.

Qualification002 proves actual owned native R/T root and ordinary-child requests
match after removal of only the eligible child's grammar; eight synthetic provider
calls, four real ancestry-linked turns per arm, no actual model calls. The first
fixture's local report attribute failure remains intact. Synthetic likelihoods are
not training evidence. Eleven implementer tests passed; main handoff tests started
RED before the wrapper/plan were created, and are verified before acceptance.

Completed empty output is an ordinary zero under the inherited scorer; infrastructure
nulls remain separate. Exact public six-label contracts plus trusted role metadata
choose immutable per-invocation decisions. No root constraint, semantic validation,
repair, new retry, output overwrite or accepted predecessor mutation is introduced.

Cue48 has exited0 with empty GPU. Use the unchanged coordinator for exclusive launch,
2430-second outer cap and exact-owned-process cleanup. Credential is read privately
from the completed owned cue service configuration; never print it or serialize it
into this operation. Scientific analysis occurs after GPU release.

The GPU became idle after cue's10:37:18 exit while this new implementation was being
prepared. This was avoidable queue exhaustion and must remain in utilization accounting.
No audit or documentation should hold up this now-ready exploratory comparison.
