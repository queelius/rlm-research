# Main exploratory acceptance review

September9,2026. Read all new study/driver/owned/seal/tests/plan/runbook/report and
approved scientific design; the inherited identity96 collector/lifecycle was
already read and successfully exercised in the completed96-call run. Only pinned,
count-checked private namespace/cardinality/cap adapters change collection.

Fresh main verification: seven focused native CPU tests PASS in3.32s and
published owned.py --verify exits0, fixed c32de adapter/config binding authenticated.
Agent qualification covers all384 typed prompt requests and72 exact schemas.
Reviewed disjoint random stable IDs, prefix swap, primary displayed alignment,
posthoc-inspired but prospectively frozen numeric-source diagnostic restricted to
overlap, nulls and exposed context boundaries. One selected random ID3000 shares
suffix000 with constant padding, not with an ordinal; this disclosed secondary-arm
caveat is not grounds to revise outcome-unseen frozen IDs. No mechanistic or
end-to-end claim follows from this component study.

Main accepts launching the ready exploratory comparison now: broad16 stopped
before its first optimizer update on an exporter rejection. Its24 rollouts and
baseline remain preserved; agents diagnose and independently audit while GPU runs.
Do not reroll broad data or loosen training-credit rules as part of this launch.
The predecessor exit1 is not relabeled success. All scientific inputs are frozen;
no model retries, one2400s owned envelope/2430s outer, four workers, per-call saves.
Allocation end19:07:17UTC leaves ample time for this bounded job.

The thin operation reuses the exact pinned coordinator, shared GPU ownership lock,
UID/start/PGID cleanup, no predecessor signaling, and acceptance closure checks.
Two operation tests captured missing-file RED before this loader/plan existed;
the same focused tests must pass before launch. CPU analysis runs after release.
