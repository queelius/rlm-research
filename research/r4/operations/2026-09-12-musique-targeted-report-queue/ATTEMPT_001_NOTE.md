# Zero-query startup failure and GPU opportunity cost

The owner ran42.09 seconds (49.40 outer seconds) and released its processes.
It asked zero of the132 planned model questions. The first-dispatch receipt was
absent because the Prime-RL serving entrypoint overwrote the configured worker
extension with its filesystem default. The CPU fixture had tested configuration
construction, not the later server argument override. The original attempt and
READY are preserved; a narrow additive repair is being prepared.

The failed stage exited at18:20:13.028UTC; the next accepted RL evaluation stage
started18:22:20.599UTC. This127.57-second inter-stage interval is recorded as a
research-operations cost while the independent held-evaluator review and actual
owner verification finished. It is not productive GPU training. Startup/model
loading occurs additionally inside the new owner. Do not count a memory-resident
service as a measured utilization percentage on this MIG device.

One further metadata clarification from the CPU preparer: static input bodies
were tokenized before public JSON was saved with sorted keys. The actual runtime
deterministically reconstructs the sealed public object, preserving all text,
partitions, seeds and policies, but using a different key order. The static36
bodies are therefore preflight budget checks, not exact runtime-wire receipts.
The reconstructed static maximum is5708 rather than5726 tokens including output;
each runtime request is still independently bounded and saved. No responses or
scientific outcomes were used to make this clarification.
