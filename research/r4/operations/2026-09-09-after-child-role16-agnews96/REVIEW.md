# Main acceptance review: AG News96

Main read the complete approved design and feasibility, all five new Python
files, four sidecar documents, the inherited owned/run seams and the independent
source review. The frozen READY is8c4e037ab93e406246c0198b27d5102b04d72c1f6eb741696addeb333e0333e8;
SPEC052926cdb4b64cb38863241565bce8cf70f312fdf8922d383017075d3c1fc876.
No material blocker was found in the reviewed comparison/lifecycle scope.

Fresh main tests:8PASS in4.62seconds and owned.py --verify exit0 with the exact
two adapter/config identities and GPUcalls0. Final preparation separately passed
8tests in4.14seconds. The independent reviewd740cb6f28030a84747c2d73291ff6768ad603383e26d7946640df3fc874c6d4
checked all96 stored body/token crosswalks,48 alias-only pairs, displayed records,
source transformations and actual dual-service binding. These are CPU checks,
not live inference or observed task accuracy.

The study is exactly four64-record AG News contexts × two seeds × two prefixes ×
two fixed weights × three output-tag rules. It preserves complete news text,
first256 normalized-group selection, disjoint source numerals and displayed-label
alignment. Source-ID output is an instruction/grammar package. Original857a and
oldc32de share a qualified base/dual-adapter service; both actual endpoint
descriptors, live model paths and response aliases must agree before scoring.
All96 input proofs match the approved candidate. Max4707+3072=7779fits8192.
Twelve schemas compile; there is no crop, repair, retry or selected-output rerun.

The failed fresh-SST proposal remains unchanged. This replacement is new-task
transfer across two existing weights, not fresh sentiment replication, an
independent training seed or a whole-RLM result. Four contexts are the clusters,
not6144 independent labels. Data license and base-pretraining exposure remain
unknown; public-test provenance does not resolve them. Class-count cancellation
and unknown/provider/unrun outcomes remain separate from item correctness.

One1080-second work clock includes verification/startup/collection after main
entry, with collection≤900seconds and120seconds cleanup reserve in the1200-second
owned envelope. Main imports precede that inner clock and remain covered by the
1230-second outer process cap. The small built-in summary precedes release;
independent audit follows release and any measurable idle tail is real cost.
HTTP500 stops new dispatch without retry and leaves unknown/unrun outcomes null;
already completed outcomes stay observed. model_called means attempted HTTP,
not proven server receipt. Physical raw records govern measured cost.

The automatic predecessor is the exact waiting child-role16 parent3000046,
start1074613864. No child-role command start is invented: the conservative bound
14:27:55.588UTC includes its complete predecessor/start/owned/cleanup margins.
AG News plus all scheduling/cleanup margins fits by15:00:25.588UTC, before the
19:07:17UTC allocation end. Earlier predecessor completion or failure permits
dispatch once owned GPU work is clear. No predecessor is signaled or retried.

The thin launcher only changes the root directory of the already pinned
coordinator. Two missing-file tests were observed RED before implementation;
both passed in0.02seconds for the exact predecessor, READY argv, shared lock and
allocation-bound calculation. The prior draft of the orchestration tool call
had a JavaScript syntax error before any file/process action; the corrected call
created these new files once. No scientific source or accepted predecessor changed.
