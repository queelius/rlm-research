# Corrective reduction SFT: feasible design, no implementation

Design: `ideas/2026-09-09-corrective-reduction-sft-design.md`.

Recommend32-example Option A:8 training-only16-record contexts ×2 query types ×2 actually executed states (full map versus four separate root actions printing batches/overwriting the map variable). Real c32 produces all labels; authored next Python action reconstructs only visible map data, selects requested IDs from actual records.user and executes the scoped count. An authentic strict final is captured but has zero training weight. This avoids assuming teacher-only labels/all_labels variables exist in sampled states and preserves the multi-observation history. Eight preassigned genuine metadata-error cases add repair context. Compare unchanged low66c, correction-only SFT and matched first-producer-action-only SFT from the same new corpus; all later producer turns remain masked.

Before full capture/training, probe the first8 fixed examples: native prefix/action/scalar authenticity, remaining teacher-forced correction loss, separate scope/merge/reduction versus copied-map payload losses, token limits and measured projected time. Declared practical floors reject a saturated target; they are not validated scientific thresholds. All prior actions/child/error/observation/scalar tokens are masked; only the selected current authored Python action receives CE. Same qualified base/LoRA/fresh Adam machinery, four updates, checkpoint each complete update; no epoch extension or evaluation-trace training.

Option B samples training-only prefixes until a predeclared map/error boundary, then executes the authored correction in the same live REPL. It better matches reached states but needs internal pause/override, higher variable cost and state-eligibility accounting. Recommend it if Option A improves held-out controlled continuations but fails to transfer to free roots; no automatic reroll of ineligible prefixes.

Data feasibility is concrete but not a frozen split: existing eight root-interface training contexts contain384 record groups. Select128 training-only raw groups without labels after excluding every readout/reserved group. Existing64-group complete-SFT/supplied-map/visibility panel stays readout-only; freeze new u02 and u00/u02 compositions plus existing-query regressions. Sources are research-exposed, helper exposure persists, and no global freshness claim is made. Do not borrow evaluated traces or groups if the filtered128-group pool is too small.

Planned primary free readout32 endpoints/root; separate16 controlled native continuations/root identify reached-state mismatch. Actual executed map use, scope/scalar boundaries, semantic errors, strict format/NULL and extra child costs remain distinct. The already-preparing example/map-visibility diagnostic is not duplicated or altered.

≤2h envelope: outer7200/work6900/cleanup180/parent120; capture1800, objective probe300, training2×600, free readout3×450, controlled readout3×300, service readiness900 aggregate, leaving450s work margin. All stages share the deadline. Estimates are extrapolated from measured prior328s/48-child capture and49s/16-example action training; first8 actual timings decide feasibility before the remainder.

Important code seams: current capture.py assumes exactly producer→scalar-final and must become one-or-four producer decisions→corrective-code→authentic-final (three or six authored root requests); current learning.update hard-codes action/terminal and0.1 terminal coefficient and must select explicit current-action roles for the two arms. Do not reuse those contracts accidentally. New pinned sidecar/corpus/READY, output ownership and focused mask/native-prefix checks are required before launch; none was created here. This task only read the sealed audit, current capture/training/loss code and immediate source-pool metadata, then wrote these two design documents. No GPU, services, downloads, source edits or queue actions occurred.
