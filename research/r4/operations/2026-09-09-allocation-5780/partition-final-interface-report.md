# Partition final-interface backup: design and implementation handoff

## Current launch successor: additive wrong-route scoring V2

Before acceptance MAIN identified a material endpoint-accounting issue: an authenticated tool-route response should be observed invalid0, not infrastructure NULL. Original READY and all its members remain unchanged. New READY_V2 SHA256 is `99cc501ecd84b10917045fa2b38a34cf8181ab8c3cf3c9b35c6a5d452493fafe`, identity `f4d1f94bb83ea81efd45a5424cbf8dcdb6d91ba564d8a393c9578080ab3c3ed0`. Fresh `owner_v2.py verify` exited0. MAIN should use READY_V2's native-Python `owner_v2.py run --output <same-sidecar>/outputs/attempt-001` argv, not original owner.py.

Only additive files were written: scoring_v2.py, counted collect_v2.py/owner_v2.py adapters, test_scoring_v2.py, amend.py, SCORING_AMENDMENT.md, CPU_TESTS_V2 and READY_V2. V2 verifies bound model, unique branch, exact prompt/completion IDs and usage, then reconstructs parsed tool function names/arguments and surrounding text from the generated native envelope, following the existing free-ID audit details.py method. Known EOS is removed only when present. Fully authenticated wrong-route responses become available invalid0 with an explicit wrong_route flag and no execution; inconsistent envelope/token/text identity still fails to NULL. Ordinary stop/length semantics are unchanged. All original messages, request bodies/grammar, seeds,24 source endpoints,900s cap and output namespace are unchanged.

The two narrow amendment regressions passed: verified native tool route0 versus corrupted arguments rejected/NULL, and exact V2 owner→collector path composition rejecting the original collector path. No additional GPU/service/lock/model call or broad checks were introduced. The original nine-test qualification and protocol record remain below as history.

## Final status: CPU_READY, accepted design implemented

MAIN approved the design below and900s envelope before any implementation. New sidecar `/project/alex_phd/runs/rlm-research-r4/sidecars/root-partition-final-interface-v1` is now sealed, not launched by this agent.

READY SHA256: `9c799679477311fe0dde2543fdf3609aece787e3fc80d4943b8aed86cc8dbb95`; canonical identity `b09800c8054022757c3fcdf4ffa2d91c9f1da957d36700281aba7e0faf01b66b`. CPU_TESTS SHA256: `7419d0b92168f6e4917b3027153fcdebc95d7d7f43fe4c840e67420b757d5014`.

Fresh `PYTHONDONTWRITEBYTECODE=1 CUDA_VISIBLE_DEVICES='' /project/alex_phd/envs/rlm/bin/python owner.py verify` exited0 and returned that identity. All149 manifest members and qualified base-weight stat identities verified; all tested Python source hashes match. outputs/attempt-001 was absent. No new model/GPU/service/container/lock call, child acquisition, download, live queue edit or old-source mutation occurred. Corrective SFT remains sealed and untouched.

Changes are confined to this report and the new sidecar: `study.py` composes immutable source/model/lifecycle identities; `protocol.py` defines exact paired bodies, syntax/domain-only schema and no-repair scoring; `prepare.py` freezes all12 original message objects and both historical/native token arrays, source-call/acquisition hashes, fresh seeds,24 paired requests, scoring-only host gold and provenance; `collect.py` records exact ordered HTTP bodies, actual bound model/token/text/usage identity, finished or capped strict scoring, NULL failures, per-source reused costs and world-clustered pairs; `owner.py` supplies the exact new CLI/output binding and900s owned lifecycle; `RUNBOOK.md` freezes limitations and acceptance; two test modules retain the focused regressions.

The released-base service is reused unchanged: original free-ID `service_wrapper_v3.py`, `service.py`, `lifecycle_adapter_v3.py` and qualified suite. The wrapper continues validating its original exact base-model/WEIGHTS binding, never a fabricated LoRA; this new owner overrides only its own endpoint preflight and collector argv. Actual wrapper source/hash identity remains symmetric in service start/claim/release. The qualified580.159.04 environment is unchanged. No new service wrapper or HF grammar was invented.

Nine focused CPU tests passed in7.304s under the existing native environment with CUDA_VISIBLE_DEVICES empty. Tests cover all12 actual historical/native prompt identities,24 pair bodies differing only in schema, real completion-token/text/usage checks, length outputs scoring strict0/1 rather than NULL, no parser reordering/deduplication/prose extraction, native vLLM request-schema validation, installed xgrammar compilation accepting empty/singleton/duplicate/unsorted/all-customer arrays and rejecting c13, actual base WEIGHTS schema, and exact owner→collector CLI namespace with rejection of old outputs. Only existing SWIG/PyTorch deprecation and no-CUDA-runtime warnings appeared; no weights/GPU inference loaded. Initial missing-function tests failed before implementation; native grammar qualification is CPU-only, not a claim that future model calls succeeded.

Important MAIN scoring clarification was implemented: an actually returned verified `finish_reason=length` is observed. A complete strict answer can score1, malformed text0, while the length flag remains explicit. NULL is reserved for transport/missing/unverified native endpoint cases. No automatic answer extraction or repair.

Exact owner argv appears in READY: native Python, new owner.py run, new outputs/attempt-001.900s outer =780 shared work,90 cleanup,30 parent margin; readiness≤180, collection≤600, four workers and120s individual request timeout. Main privately binds the provider credential before launch; no secret value is serialized into request records or argv. There are24 new final calls,24 unique historical extractions,48 hypothetical acquisition calls across16 full endpoints and no acquisition cost for direct. Source inputs expose four worlds, not12 independent worlds; original records remain visible in full-report prompts. New free/exact comparison shares one vLLM backend; HF→vLLM/history/cap differences are not identified separately. Actual provider sampling and any native failures remain launch-time evidence.

## Original pre-approval design record (preserved below)

2026-09-09. CPU-only source inspection completed; awaiting MAIN design approval before implementation. No service/GPU/container/lock action, new child acquisition, old-pilot mutation or corrective-SFT mutation. Proposed ownership: new `sidecars/root-partition-final-interface-v1` plus this report.

## Evidence and question

Verified every member of the partition audit FINAL_MANIFEST source closure. Its REPORT.md SHA256 is `6d72da758a4d69561445b1799a7d78662550c948c92da5f687745f9016c7206a`. Independently read retained calls: eight unique full-report native prompts (1260 tokens each), eight direct-expanded calls but four unique direct prompt arrays (695 tokens), and24 actual extraction calls. Historical extraction cost is7056 input tokens,5448 output tokens,193.9091135840863 summed request-seconds.

The audit found all24 extractions exact, all8 full parents truncated at256, and all4 unique direct2560 outputs naturally stopped but prose-invalid; diagnostic embedded arrays were correct for only2/4 worlds. The question is whether syntax/customer-domain-constrained final decoding reveals correct joins supported by those full reports/direct records, at sufficient equal output cap. Full-report roots still see the original records: this is not a blind communication or autonomous-RLM test.

## Proposed24-call comparison

Use all8 actual parent-full message objects unchanged and four original direct-expanded messages deduplicated by native prompt IDs. Preserve actual extraction contents, report ordering/serialization, source records and question. No outcome-based subset or regenerated child. Pair each of these12 inputs under free decoding versus native vLLM `structured_outputs` JSON schema:

`{"type":"array","items":{"type":"string","enum":["c01","c02","c03","c04","c05","c06","c07","c08","c09","c10","c11","c12"]}}`

The grammar constrains syntax and the public customer domain only. No min/max cardinality, uniqueItems, sorting, gold set, repair or fallback. The original ascending/distinct final-array contract remains post-hoc scoring, so duplicate or unsorted domain-valid arrays still score invalid0. Both conditions retain the identical original prompt, max2560, temperature0.6, top_p0.95, disabled top_k, repetition_penalty1.0 and one fresh seed per input. Freeze paired seeds and balanced decoder order before new sampling; no search over seeds.

Prefer the qualified free-ID released-base vLLM service path (`service_wrapper_v3.py`, corresponding lifecycle identity adapter and current an27 driver environment), no research LoRA, released Qwen3-4B-Instruct-2507 cdbee checkpoint. Reuse configuration/ownership machinery, not that study's96-call collector/data. CPU tests must confirm new exact namespace argv, source pins, actual-model descriptor, native tokenizer equivalence to these original prompts, request equality except grammar, and schema accepts every public-domain array regardless of gold. No HF grammar implementation or new downloads.

## Endpoints and cost

Primary: strict available correct/invalid with paired wins/losses/ties separately for8 full and4 direct inputs; report source-world clustering, since the two full partitions share each of four worlds. Require actual bound model, returned prompt IDs equal frozen native IDs, valid completion token accounting, terminal assistant text and no tool calls. Provider errors, missing/unrun calls, native mismatch or unavailable final are NULL; actually returned malformed/length text is available invalid0 under the frozen parser, not repaired. Retain raw wire, response/token IDs, exact finish reason, usage and latency. Any embedded-array diagnostic is explicitly non-primary and cannot supply a replacement answer.

There are24 new physical final calls and zero new extraction calls. Every full endpoint's hypothetical standalone pipeline receives all three relevant historical extraction calls; across16 full endpoints this repeats48 acquisition calls by design, distinct from the24 historical physical acquisitions. Direct endpoints receive no acquisition cost. Record new-only and standalone costs separately; do not sum hypothetical rows as physical compute. Do not infer unknown cache/dollar usage.

Proposed hard envelope:900s outer,780 work (startup≤180, collection≤600, shared deadline),90 owned cleanup and30 parent margin, one MAIN-owned GPU, four request workers, per-call timeout120s. Expected shape is modest batched4B inference; max token allowance is24×2560=61440 output tokens, not promised realized compute. Preserve NULLs if cap expires, no retry or selected continuation.

## Interpretation and approval boundary

A within-pair grammar gain isolates decoder-interface restriction under this new common vLLM backend/cap. It does not separately identify cap versus HF-to-vLLM differences relative to the historical pilot. Domain-valid but semantically wrong joins motivate state-use/presentation tests; high direct and low full performance motivates report interference tests. Both failing semantically argues against a communication-loss conclusion. Four engineered worlds are not broad benchmark evidence.

MAIN received this concise design before any implementation. The brainstorming skill is holding the code boundary pending approval. Corrective SFT remains sealed and untouched.
