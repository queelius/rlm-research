# Task3 diagnostic preparation report

CPU preparation is complete and sealed for MAIN acceptance. No GPU, model service,
native container, scheduler, queue, lifecycle authority, environment mutation,
subagent, or old-source modification was used. The new study remains unrun.

Sidecar: `/project/alex_phd/runs/rlm-research-r4/sidecars/root-supplied-map-reducer-v1`.
READY SHA256 `5aca1018840158985491f12f3cb9e39d6956b8b014db3c6dd04a3f1efc5b9b98`.
Identity `108e7398ca7cc094963c8141d0a7bf45d8da44129d82d41727de2bcb7bfdb407`.

## MAIN ruling and scientific comparison

MAIN read the September9 approval and explicitly ruled: preserve the original
design—fixed last bounded-RL4 root2286be, actual native c32 maps versus privileged
dataset maps, ordinary Python versus pure count_labels;32 root endpoints plus
four acquisitions;1350s outer/1200s work/120s cleanup. MAIN explicitly superseded
the brief's erroneous low66c/raw-context wording. That ruling is in DESIGN.md.
No complete-demonstration SFT outcomes were consumed to construct this diagnostic.

The exact four16-record contexts reserved by complete-demonstration DATA_READY
are retained: two helper-training and two helper-validation contexts,64 groups,
no relabeling/filtering/reordering. Single-user u00 and union u00/u01 questions
retain alternating NUM/HUM targets. New paired seeds981334201..981334208 cover
eight context/query blocks, four treatments each,32 endpoints. Acquisition seeds
981334101..104 and master981334001 were fixed after a scoped pre-creation search
found no981334xxx collision in sidecars/ideas excluding outputs/qualification.
Treatment order is label-independent hash order within each paired block.

One authored wrapper per context asks the actual native typed c32 child for its
full16-record map. It prints the actual reply and ends with a dummy authored
terminal. All physical requests, raw native graphs, supplied maps and costs are
retained. The wrapper's synthetic likelihood placeholders are explicitly NOT
sampled behavior, RL, training data or endpoint root performance. No reroll or
gold-aware acceptance occurs. A malformed/unavailable acquisition leaves its four
native-map endpoints NULL; the planned oracle endpoints remain. All acquisitions
are frozen in MAPS_READY before root readout.

Each root has original public records/context and labels.json. Oracle treatment
is explicitly identified as privileged frozen dataset labels. Helper treatment
also supplies the documented pure function count_labels(labels, requested_ids,
target); root selects IDs using public user metadata and submits its own final.
The helper checks categories/unique IDs/coverage; it does not access gold, query,
metadata or filesystem, log, repair labels or automatically submit. Optional
additional child access remains available and is charged per root endpoint.
This is an assistance-package diagnostic, not normal end-to-end performance.

## Exact inputs and implementations

All paths below are relative to the sidecar; full inherited and new closure is
in READY.source_sha256 and READY.input_sha256.

| Input | SHA256 |
|---|---|
| inputs/PLAN.json | 2f54dcb62f5823dacec2c5c144825e568ece1e954f2843ef74bfc162f27520f0 |
| inputs/PROMPTS.json | 586a025a81f31f43dcbfbb1a961e35e6bf79e738fb865e3d6b2e63575a4a28d9 |
| inputs/PUBLIC.json | a505f22913412c0805d2e357667a9b62ea342b614ddc1706fafa998c2018f2bf |
| inputs/HOST_GOLD.json | 22242395b4561052c25c0d675a7559473b0f564f128c1fc29b7b7abb5af1912e |
| inputs/BINDING.json | 7d58ecea33ad12ae11331e20db8911187435e6de717cc1646fed0bfb5e85613e |
| CPU_TESTS.json | 6bbe29e2ba0e98627580cedb64354a5c04aa81f9e9388dc61be0f5ce1025d197 |

Root adapter SHA256 is
`2286be3f7c0c9cc0e22c8ef8e3473b7d8eb6ec4b7a789ca380a0af9d3b944c71`.
Child adapter SHA256 is
`c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3`.
The inherited root alias still contains the old bridge name; actual paths,
adapter/config checksums and live model-card checks define the policy identity.

The sidecar contains the native collector, pure helper, metrics, outcome-independent
protocol, pinned study/task binding, runtime binding, CPU preparation/qualification,
two focused test files, DESIGN, RUNBOOK, PREPARED, READY, SOURCE_PATH_MAP and six
input JSON files. Maximum prepared prompt is1230 tokens; all32 remain within the
native8192 prompt-plus2048 generation budget. No labels or task-specific answers
are inserted into normal prompts; oracle labels appear only through its explicitly
privileged supplied file.

## Verification and preparation corrections

Nine focused CPU tests passed in14.19s. Two upstream SWIG deprecation warnings
were emitted, with no test failure. Checks cover strict helper/key/value behavior,
malformed/duplicate/incomplete maps, NULL versus empty/wrong final accounting,
paired treatment construction, scalar agreement versus executed reduction,
native flat tool-call parsing, missing cached-token usage, exact reconstruction
of all32 native prompt IDs, new-context task metadata, and actual sidecar file
injection at a recording runtime-write boundary. All10 Python files parsed.

Initial tests failed as expected for missing implementation. Two added regressions
then caught native flat tool-call parsing and missing usage-accounting behavior;
both were fixed and pass. No generated model program was executed on the host.

The first seal failed on one inherited historical node-local supervisor.py path.
Its SHA2561b9a1b303b7c2a389d8981d0c7ea9d3e5603b49f8a420f354220c6a28c9cc78e
matches the preserved image source and the exact an27 destination in MAIN's
qualified COPY_MANIFEST. SOURCE_PATH_MAP records all three paths and the copy
manifest SHA. New READY uses the byte-identical image source; runtime_binding
verifies the exact new destination before use. No old path/cache was recreated
or edited. A subsequent seal and fresh full checksum verify exited0.

The collector reuses runtime-an27-5780-v1's pinned study_wrapper.adapt_stack
function after its existing runtime verification. Its separate model service and
driver adaptation remain MAIN-owned. No new native container qualification was
performed; this is an exploratory handoff over the already qualified stack.

## Endpoint reporting and launch handoff

Primary: strict exact Answer: integer and frozen dataset correctness. Available
empty/malformed/wrong finals score0. Missing native finals, native execution errors
without finals, incomplete/unrun endpoints and unavailable native-map acquisitions
remain NULL. PLANNED_NULL_ENDPOINTS is written before acquisitions; observed row
files explicitly supersede those immutable placeholders. Per-completed-episode
raw graphs/rows are preserved incrementally; terminal and output inventory are
written after collection. RUNBOOK names every expected artifact and partial-run
interpretation.

Separate automatic evidence includes supplied/scoped coverage, final consistency
with the supplied map, actual integer tool observations, valid emitted Python,
literal count_labels use, and bare-variable zip risk. The zip marker is a screen,
not proof of dictionary corruption. `actual_map_consistent_reduction` deliberately
remains NULL pending manual inspection of actual code and native observations;
a host-computed target or coincident matching displayed scalar is not execution
proof. The pure helper performs no runtime logging. This manual audit is required
for the corresponding secondary result, not a prerequisite to launching exploration.

Full native audits and per-field known/missing usage counts preserve cost
availability. c32 acquisition costs are shared across each context's four
native-map endpoints and reported separately. Additional root-time child calls
are not hidden. Logical token sums/request latencies are not GPU time.

MAIN can use READY.collector_argv with its authenticated endpoint descriptor and
absolute shared deadline. MAIN must retain the1350/1200/120 lifecycle envelope,
the fixed inputs/BINDING.json, and the accepted service-driver wrapper. Collector
starts no model service and assumes no lifecycle/queue authority. Cap ceilings:
four sequential acquisitions up to120s each, then four work-conserving native
root workers with180s individual caps, all clipped to the same1200s deadline.
No reroll, continuation under an old attempt ID, root selection, training or
extra evaluation work is implied by this handoff.

Interpretation remains limited to four source clusters, helper-exposed TREC data,
unknown pretraining overlap, unresolved dataset license and one fixed root policy.
No source-label disagreement is independently adjudicated semantic error.

## Additive owned launcher requested by MAIN

MAIN subsequently requested a launchable successor using the existing qualified
suite lifecycle. Added only owner.py, test_owner.py, OWNER_CPU_TESTS.json and
OWNER_READY.json; the original scientific READY and every pinned scientific source
are unchanged. OWNER_READY SHA256 is
`88a81502a520a6e33cc0d6c0f8a901c344b67837f9068834fafc50ec3843cf77`, identity
`732ca3dbecf3c07eb55d50757ecb4b9365b00e36c6a225d2f74d1d0efa1c709f`.

The exact launch argv is:

    /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-supplied-map-reducer-v1/owner.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-supplied-map-reducer-v1/outputs/attempt-001

MAIN independently owns the GPU and shared lock and privately supplies
STRICT_RLM_CALIBRATION_API_KEY. The wrapper requires that nonempty credential before
any output creation or service/process work and never serializes/returns its value.
It requires one assigned CUDA device and the exact nonexistent attempt directory.
The owner uses outputs/attempt-001; service artifacts use owned-service/; the
unchanged collector uses rollout/. This additive output nesting is in OWNER_READY
and changes no task, prompt, seed, model or endpoint semantics.

The one work clock starts before verification/startup:1200s for service readiness
(individually capped180s) and all four acquisitions plus32 native endpoints.
Collector receives the same absolute work deadline. Owned deadline is1320s including
120s cleanup; MAIN's outer cap is1350s. The wrapper uses the existing qualified
leaf-post-sft-suite start_service, command and release_service with release in
finally. Existing root-interface lifecycle composition retains absent-process handling,
stable PID/start/UID/PGID ownership and memoized static amendment verification.
runtime-an27-5780-v1/lifecycle_adapter.install(suite) binds both launcher identity
checks and actual startup to service_wrapper_v2.py. No speculative lifecycle or
cleanup implementation was added.

Three focused CPU lifecycle tests passed: credential failure creates no output,
the shared deadline/exact collector command is passed on success, and release runs
after collector failure. Actual dependency composition was also loaded read-only:
suite.SERVE and suite.life.ALLOCATION_SERVICE both resolved to the qualified
service_wrapper_v2.py, with zero service launchers. Fresh owner verify passed and
also verified unchanged original READY. No GPU or model/container/service launch
occurred during this additive preparation.
