# Fixed-output visible-reference48: CPU READY for MAIN review

New isolated sidecar: `sidecars/leaf-mnli-fixed-output-visible-reference-v1`. Approval was recorded before any new-alien48 outcome reads. No accepted ancestor source, output or independent audit seal was changed. No GPU/model/service/queue action was taken.

READY SHA `22c5636206757747bb4f147904a4fd49c32ca418960bfced1c59054e97fd39b6`; identity `563e007c5f0360645340ec30385f6b8a2e28b001d6d0f9f7f0c73a2551710d37`. Fresh actual `owner.py verify` returned this identity. READY pins737 source files and14 input/qualification JSON receipts; three exact superseded READYs are explicitly historical receipts rather than active executable closures. The first failed recursive seal and its narrow resolution are retained in CPU_PREPARATION_HISTORY.md; no READY existed at that failure.

All16 contexts/768 MNLI rows are byte-identical copies of the queued new-alien panel. Existing shift17 requested tags and entire48-object grammar remain fixed across aligned/wrong/unrelated arms; only visible `id` fields differ. Every rendered native prefix is token-length-identical across the three arms within its context, range3464–4325; max prompt+3072 output=7397<8192. All768 new unrelated IDs are unique, length-matched and absent from the frozen named collision inventory. Private gold mutation leaves every request unchanged; all three label alternatives compile under the exact grammar. No output/label selection or retries.

Primary aligned−wrong; wrong−unrelated secondary with repetition caveat. All48 planned NULLs preinventory,16 actual context clusters in collector metadata, native authenticated strict score0 for malformed vs NULL for unavailable, no repair. Six arm permutations are cycled over hash-ordered contexts; first-position counts A6/W6/U4 (not perfectly balanced across16). Fixed forced tag subsequences do not mean identical free labels or full sampled histories. Source aliases and record exposure are explicit; this is mechanism work on a panel exposed by the pending predecessor, not fresh confirmation or whole-RLM transfer.

Qualification: CPU_TESTS_003 has10 passing tests in7.671s (8.631s subprocess wall); all48 actual composed HTTP/native-token fixtures plus owner→service wrapper→intercepted inference launch, collector namespace, visible-ID-only manipulation,16-cluster inventory, wrong-named oracle, malformed/NULL scoring and active source closure. Initial CPU input preparation took12.223s. Existing tokenizer deprecation warnings were benign; no scientific generated code executed. Verification/test-first skills motivated these focused checks, not broad repository tests.

Exact MAIN-only argv:

```text
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-mnli-fixed-output-visible-reference-v1/owner.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-mnli-fixed-output-visible-reference-v1/outputs/attempt-001
```

Qualified released Qwen3-4B no-LoRA service, no tools, thinkingoff,T.5/top_p1,3072/8192,90s requests/four workers. 1200outer=1080work+90cleanup+30margin; startup≤180 is inside work. Physical requests/returns/known-unknown native usage retained; billing unknown. MAIN must independently review/accept and decide scheduling conditional on pending replication and queue. No automatic launch implied by READY.

Review files: APPROVAL.md, DESIGN.md, PLAN.md, CPU_PREPARATION_HISTORY.md; study.py, protocol.py, scoring.py, collect.py, owner.py, service_wrapper.py, prepare.py; test_protocol.py, test_scoring.py, test_owner.py, test_collect.py; immutable DATA/PUBLIC/PLAN/REQUESTS/ORDERED_REQUESTS/PROMPT_IDS/UNRELATED_IDS/PROVENANCE/COLLISION_AUDIT/CPU_NATIVE/PLANNED_NULL_ENDPOINTS and CPU_TESTS receipts.
