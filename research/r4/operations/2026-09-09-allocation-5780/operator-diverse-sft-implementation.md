# Operator-diverse SFT72: CPU-ready handoff

New isolated sidecar: `/project/alex_phd/runs/rlm-research-r4/sidecars/root-operator-diverse-sft-v1`.

READY SHA256: `9cf0f37b5295064e486ca25caed7cb3ee6eb916921722170ca7e5ed7ce5a594e`.
Canonical identity: `8287f45b6730831ac78eed3e20ddb307108371f50a210dfa2b51d4fe56c710b8`.
Closure:1167 source pins and25 input pins. Actual composed `od_owner.py verify` exited0 and printed this identity after sealing.

Exact MAIN-only launch argv:

```
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-operator-diverse-sft-v1/od_owner.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-operator-diverse-sft-v1/outputs/attempt-001
```

Outer8400 seconds. MAIN supplies its one-GPU ownership/lock and approved credential privately. This preparation launched no GPU/model service, acquired no lock, edited no queue and created no scientific attempt.

## Implementation and fixed science

`od_protocol.py`/`prepare.py`:72 label-blind trajectories, all12 QSR training contexts/192 groups, supported count/distinct-user/visible-weight operators, widths4/16. Exactly180 genuine c32 acquisitions and324 authored root actions if structurally complete. Actual file-only four-field records; cumulative labels stay in the same REPL. Authored reduction reads live variables/users/weights and terminal action copies only the genuinely printed scalar. Wrong child labels and resulting wrong scalars are retained. No historical maps, host-gold targets, retries or successful-only admission. The fixed gate members are count/single and distinct/union on training00, weight/all on training01, each width. Full72 capture must complete before any training admission.

`od_study.py`/`od_binding.py`/`bind_start.py`: explicit unique module names and exact-path pinned dependencies; actual question/setup/native prefix equality; exact supported operator scorer. MAIN-approved start is the actual served857a7ce6 key-converted released-reference adapter, confige6828a7c, conversion428f0241, with base manifest and original source receipt. It is not an optimizer checkpoint and no state.json is fabricated. Fresh Adam is explicit. The disclosed exploratory calibration is reference2/8, success8 3/8, interface4 2/8, all zero successes/no child; selection is least-assumption lineage, not superiority or bare-base equivalence. Calibration00–03 excludes readout04–11.

`od_collect.py`: counted transforms of unchanged qualified joint collector sourcef2e0d2ad. Exact adaptations are namespace imports, operator target/scoring, producer code/literal span capture, physical-attempt accounting,72-corpus admission/receipt, and capture/free-only CLI. Actual root prefixes are re-rendered against native graph history; all authored actions execute through native IPython/typed c32. Root2048/context8192 and optional typed child stay unchanged. Physical attempts are marked before transport and retained on failure. The inherited field `paid_model_call` is a legacy name, not a billing claim.

`od_learning.py`/`od_train.py`: unchanged qualified current-action numerical objective and checkpoint writer. Masses .45 equally across producers/.50reduce/.05stop; fresh AdamW lr1e-4, weight_decay0, clip1, FP32 rank8 LoRA. Code mechanism spans exclude both strings and numeric literals, never a copied map. Six fixed forward-only examples require4/6 corrective mechanism means above .1nat and a measured cost projection fitting the remaining training stage. Gate and training share one model load; no gradient before gate. Six complete shuffled72-example updates/checkpoints1–6 include optimizer/RNG/delta/corpus/ancestor state. No checkpoint5 substitute for fixedSFT6. No automatic resume.

`od_owner.py`: prewrites72 capture slots and48 NULL final endpoints. Exact baseline-first then fixedSFT6 24/policy; same inputs/seeds/common role, eight held-out-composition contexts04–11. Ordinary capture/gate/learning failure does not bypass baseline or fixed6 availability checks. Missing6 remains24 planned unavailable treatment endpoints. Actual final authentication/strict parsing is distinct from orchestrator completion; malformed returned finals strict0, missing native finals NULL. Physical attempts, returned tokens/unknown outputs/cache and authored transports are separately retained in the ledger.

## Exact lifecycle changes relative to qualified joint owner

No service-wrapper, config-provider, inference runtime, port allocator or cleanup primitive changed. Qualified an27 service_wrapper_v2 and lifecycle adapter are reused.

The owner changes study namespace, fixed starting binding, corpus/update/readout counts, training-failure continuation, and inclusive budgets. Outer8400 =work8100 +cleanup180 +margin120; owned8280. Work allocations are capture1800, gate/train3600, unchanged1200, SFT6 1200, finalize300. Each service stage includes startup up to180 and release up to90; collection ends90 seconds before its stage cap. Stage end is min(now+cap, work-deadline minus later reservations), so early completion advances immediately rather than waiting. SIGINT/SIGTERM stops later work; failed release leaves explicit active ownership and stops subsequent services. No repeat cleanup or hidden rerun.

## Focused verification

Initial aggregate14 passed25.93s. Already-started retained qualification repeat:14 passed26.64s, two existing SWIG dependency deprecation warnings. JUnit and102 CPU fixture files are retained under the sidecar; CPU_REPORT SHA256 `8f18a233a7648c89656217072389ec8f07238d78e3eafbda52b83bc02057fd34` pins them.

Coverage:72/24 plans,180/324 forecasts, split and host-label invariance; truthful actual file setup/native prefix and typed q+12hex IDs; live wrong-prediction operator reductions; string/numeric/code span partition; true authored CPU native capture with6root/4fakechild calls and real observations; current-action-only masks and root2048; equal trajectory mass/tiny actual Adam delta and expired no-op; exact owner→collector argv; early caps and planned48 NULLs; actual composed owner→service_wrapper_v2→config/descriptor→intercepted inference Popen; missing6 and training-failure→both final policy attempts. Qualified checkpoint persistence/ancestry is reused; no new GPU checkpoint or efficacy is claimed. Namespace981451 source/prepared-plan scan found only this sidecar.

## Limits and decision

Capture1305s and training1232s are length-scaled historical proxies, not guarantees. Real six-example forward timings decide spending; final caps can still leave NULLs. Equal trajectory counts are not equal tokens/FLOPs. Data are prior research/child exposed, not pristine. Template/name schedules inspect context/width rather than operator/gold, but supported scope correlates with context parity: no fully independent template×width×scope factorial or causal breadth claim. MAIN accepted this limitation without input redesign. Baseline-first versus trained-second retains time-order confounding. Scientific value is held-out free acquisition→reduction→stop reachability and actual mechanism/cost, not CE decrease or nominal update count.

Sources are frozen after READY; MAIN acceptance and launch remain separate.
