# Operator-diverse SFT original-attempt audit

The original attempt did not test SFT: its 72/72 authentic teacher captures completed, but the
trainer inherited a launcher that hid CUDA and failed before model load, gate evaluation, or any Adam
step. All 24 SFT6 endpoints are therefore infrastructure NULLs, not zeros.

The frozen unchanged readout is 2/24 planned, 22/24 available (conditional 2/22; missing-data bounds
2/24–4/24). Both successes were nonzero and authentic, but used direct file/keyword logic rather than
child acquisition and live accumulation. No paired treatment estimate exists.

Capture integrity is strong: 72/72 trajectories passed the independent structural/dataflow recount,
with 180/180 child responses. Real errors were retained (82/1,152 child labels wrong; executed scalar
different from host gold in 14/72), so the corpus was not gold-repaired. Total retained physical cost
was 259 attempts/258 returns, 314,898 known input tokens and 38,624 known output tokens; cache use and
provider billing are unknown.

The approved recovery is a separate, narrow infrastructure completion: immutable corpus, same 857
start/seed/gate/six passes/Adam 1e-4, and only the original 24 SFT6 coordinates. The original baseline
and NULL inventory must remain untouched. Full evidence and qualifications are in
`analyses/root-operator-diverse-sft-live-2026-09-10/REPORT.md`.
