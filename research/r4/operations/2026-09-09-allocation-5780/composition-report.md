# Composition pilot preparation complete

Executable CPU_READY is frozen at
`/project/alex_phd/runs/rlm-research-r4/sidecars/root-partition-report-pilot-v1/READY.json`.
MAIN approved the88-call proposal and owns launch acceptance, device assignment,
queue and1800s external parent. No model, GPU, service or queue action occurred
in this preparation.

READY SHA256 `e87abba449fa1be385c52a3575cc40e5b683bcdd31c706119c3636a59a54cc7a`.
PLAN SHA256 `2885f12ea9d20e71b4fada985cc9d52e632d7ed06f86be58020cf983cb407d6c`.
RECIPE SHA256 `1d982a1ecbb6c140f48a361fcf074a1a69ca612f66f6b269dc3de950d9d7badc`.

The new sidecar contains a deterministic purchase-join generator, exact CPU
solver, raw extraction projections/scorer, fixed native HF collector, immutable
freeze, design, runbook and focused tests. Four worlds each have12 customers,
48 records and distinct global answer sets of sizes3/5/7/9. Every world has
balanced co-located and cross-boundary3×16 partitions preserving exactly the
same48 records. The cross construction splits every qualifying A/B pair;
all local-winner reports are empty by construction. The full-input-visible
parent can still recover independently. This engineered negative control does
not establish ordinary child reporting failure before actual model outcomes.

Eight coordinates each use3 actual extraction calls,3 ordinary unconstrained
global-query reports,3 report-conditioned parents and2 direct baselines. The
structured full/lossy pair reuses identical actual extraction calls; each
hypothetical pipeline is charged its full3-child acquisition cost. Both direct
conditions have identical input; expanded direct has a2560-token ceiling versus
256 short and parent tokens and768 child tokens. It is explicitly not equal
realized compute. All original records remain visible to parents. CPU_ORACLE
adds exact nonrecursive answers and exact-extraction symbolic report implications
without additional model calls.

Generator seeds981409091–094 and sampling seeds981409101/111 (child chunk
offsets0/1/2) are frozen. One sampling realization/world is paired across
partitions and report arms; no within-world seed replication claim. Source
catalog scan is stored with its complete named path list in SEED_AUDIT.
No outcome-conditioned reroll or label-dependent model selection occurs.

Every role uses the cached released Qwen3-4B-Instruct-2507 revision
cdbee75f17c01a7cc42f958dc650907174af0554, no adapters, native in-process
HF BF16 SDPA sampling. Authenticated prior shard hashes and current
size/mtime/inode identities are preserved; nonweight source/model auxiliary
hashes are freshly verified. Exact library source, versions, model manifest,
native static prompt IDs and dynamic rendering implementation are bound.
This is a fixed one-level decomposition diagnostic, not autonomous RLM
planning, learned interface choice, production Responses transport or training.

Primary outcomes are paired full-versus-ordinary exact set correctness on8
coordinates and worlds correct under both partitions, denominator4. Native
calls and episode output expose precision/recall, exact extraction, ordinary
reports, symbolic implications, malformed/unavailable answers, paired scores,
pipeline/physical token cost and call latency. Report-use, length and exact
nonrecursive alternatives remain live confounds. No statistical or novelty
claim is warranted from four engineered worlds.

The exact launch command is READY.launch_argv: the existing training Python
plus this sidecar's `run.py --output <sidecar>/outputs/attempt-001`. Work cap
1650s includes import and cold load, with120s cleanup allowance under1800s
outer cap. Each request is saved before inference, every actual call after
completion/interruption, every11-call episode and final full-denominator
OUTCOMES/STATUS. Unknown interrupted token counts remain null. Existing output
directories are refused; no automatic retry or resume. Preserve partial work
and prepare a separately frozen missing-call continuation if needed.

Fresh verification: exact `run.py --verify` exited0;9 focused CPU tests passed
in4.20s in the pinned training environment, including real tokenizer rendering
and88-call collector integration with the inference boundary replaced. Targeted
Ruff F/E9 checks pass. Formatting was applied before freeze; full style lint
still has long-line diagnostics and is not claimed clean. No model-generation
qualification was performed, appropriate to this exploratory successor.

Preparation observed and fixed one concrete API mismatch: transformers5.15
returns BatchEncoding by default from apply_chat_template. Both preparation
and execution now explicitly request return_dict=False through one shared
renderer, qualified against the real cached tokenizer. The unsuccessful
preparation's partial PLAN is preserved as
PREPARATION_FAILED_BATCHENCODING.json.partial, not mistaken for frozen inputs.

Read in scope: user new-ideas/ideas.md in full (unchanged), factory
rq:sufficient-interface, transfer HANDOFF priorities, bridge RESUME_CHECKPOINT,
complete allocation audit-report, and neighboring structured-decomposition
STATUS/scientific design as conceptual context only. Brainstorming was used to
present the bounded design for MAIN's explicit ruling; verification and narrow
debugging instructions shaped the CPU checks. No third-party numeric results
were borrowed, no broad literature investigation or framework edits occurred.
