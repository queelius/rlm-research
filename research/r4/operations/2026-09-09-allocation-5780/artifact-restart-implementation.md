# Restart48 CPU implementation handoff

New sidecar: `/project/alex_phd/runs/rlm-research-r4/sidecars/root-artifact-restart-v1`.

READY SHA `9568ab66b89269da8670b855e66e9db0c1122b8ddc196f5759afa931613f5b42`; identity `d1bc56846273e085a818e4821956b541c913959f5bbd381d2937b82d76535464`. CPU_TESTS SHA `8594804f13f8e8c9f0eb59b6fc68d3e931dc68f1f7e51240a696bccb7a367df5`. MAIN retains acceptance, GPU, lock and queue; no service/GPU/container/lock was launched during implementation. Existing SFT, controlled completion, clarity, partition and leaf sources/outcomes are untouched.

Exact MAIN launch argv:

```
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-artifact-restart-v1/owner.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-artifact-restart-v1/outputs/attempt-001
```

Fresh verification substitutes `verify` for the run/output arguments. Exact attempt is absent at handoff; no retry or overwrite permitted. READY pins the complete inherited native/model/lifecycle closure plus new sources, actual source-state provenance, prompt/files, CPU evidence and approved feasibility memo.

## Scientific implementation

All16 original controlled-source states, not selected by correctness:4 already exposed contexts ×2 query compositions ×width16/4.48 new native root sessions, unchanged low66c root SHA66cce4009628ff7d8e047f08e6fb3a29febdbf8fb3ed8303871f69860f1151d5 and c32 child SHA c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3. The existing source binding retains its unchanged-root alias; actual checkpoint bytes, not alias wording, define the policy. No new training or rescue selection from failed corrective free endpoints.

Q quotes content-preserving historical root-visible JSON messages in its actual NEW user prompt. A supplies common artifact references. M adds inline public metadata already present in the common files. All use fresh native sessions/REPLs with no producer replay or old variables. Identical query.txt contains the common original accurate-field goal, independently of arm-specific data.prompt. Identical records/context/helper, history archive, actual map pieces and metadata are written and byte-attested during setup. A/M may retrieve Q's same archived history; resulting model/tool/elapsed costs count. This is a state-representation/restart pilot, NOT exact native-history continuation. Original uninterrupted controlled outcomes are separately labeled descriptive references only.

`protocol.py` cuts root ancestry strictly before source correction, excluding child-private nodes and all future teacher actions/observations/scalar/final. `prepare.py` checks all16 cuts against recorded prompt IDs, native re-rendering and physical source-prefix capture. Every map piece matches an actual paid child's returned token decoding; wrong predictions remain. Packages contain no host gold, source TEACHER files, masks/targets, sampled old readout or hidden correction helper. Host scoring labels and provenance stay outside mounted files. The native task's placeholder answer0 is not primary reward; the collector's explicit scoring-only host count plus genuine final verification defines retained reward.

Fresh paired seeds981359101–116; hash-ranked source triples, cyclic6/5/5 arm positions. T0.5/top_p1.0,2048 native per-call cap,8192 context cap,180s per endpoint and4 workers. Fresh prompt lengths Q2474–3077, A1206–1232, M1503–1581; no truncation. All48 planned NULL slots are written before service startup, and collector per-call typed/role records, graph episodes, setup attestations and rows checkpoint independently. Qualified strict native branch/token/text matching is reused. Missing/unreturned/unverified final remains NULL; actual malformed/capped final is observed0 unless strict-correct.

Data-flow fields remain explicitly unaudited until actual code+tool-return inspection. Programs, returned observations, native tool timing and raw model usage are preserved; filename/AST presence or source-map consistency is not automatically counted as retrieval/reduction. Current row costs include all new root/child calls and full per-source historical acquisition. The standalone source acquisition is not amortized.

## Lifecycle difference from qualified machinery

No edit to an27 service/runtime/lifecycle code. New61-line owner calls the existing qualified launcher dependencies and installs the existing lifecycle adapter. Only new exact owner/collector output namespaces, fixed source binding,48-endpoint collector command and budget differ. Private credential preflight precedes output/process creation; no key appears in argv or printed metadata. MAIN supplies one GPU under its independent lock.

One clock starts at owner entry: work2100s, owned2280, outer2400. Startup≤300s and collector≤1800s share work;180s cleanup plus120s outer margin. `suite.command` owns the collector child; `release_service` executes in finally on success/failure and records release errors without claiming clean completion. No automatic retries, alternate checkpoints or old output cleanup. Owner→collector argv is consumed by the actual collector parser in tests; both actual CLI `--help` entrypoints execute successfully.

## Costs, checks and limits

40 unique historical child calls already paid:37340 input/2430 output/34528 cached tokens,28.243s summed physical child time. Across48 hypothetical standalone endpoints, source cost is120 calls/112020 input/7290 output/103584 cached, plus new work. Source gross capture wall280.985s includes later authored correction/final and teardown and is explicitly labeled gross; separate first-producer-to-pre-correction observed time excludes setup. Successful CPU export/tokenization took60.092s and is recorded separately. Failed CPU boundary investigations and qualification add preparation overhead; they are not represented as GPU or model time.

Eleven focused tests passed in13.13s; only upstream SWIG deprecation warnings. Tests exercise private/future cutoff, omitted root-parent serialization, native generation-header boundary, wrong-label/full-coverage retention, deterministic pairing, actual owner/collector CLI namespace, planned NULL behavior, exact attempt rejection, real task/setup with byte equality for both widths×3 arms,16 source native prefix rerenders and48 fresh prompt rerenders. Native setup uses a filesystem-only test double, not a container launch. Source-map/raw-file SHA checks and seed-namespace source/plan scan passed. Environment metadata: torch2.13.0+cu130, transformers5.6.2, tokenizers0.22.2, vLLM0.28.0, verifiers0.3.2.dev36.

Two narrow source-boundary assumptions were corrected before freezing: native root nodes may omit `parent`; the next assistant node carries a three-token generation header belonging to the pre-correction prompt, but no future completion. A failing regression preceded each correction. Setup attestation was extended to include the common batch_contract bytes after its focused fixture exposed the omission. Raw package hashes are SHA256 of actual UTF-8 file bytes. No old scientific source changed.

CPU readiness does not claim successful actual GPU/runtime execution, fresh heldout generalization, long-history compression, learned-state sufficiency or adaptive decomposition. Q also exposes authored action examples as quoted data; an effect cannot be attributed exclusively to map availability. Four context clusters and8 query compositions remain the inference units beneath16 source states/48 correlated trajectories. MAIN review/acceptance is the next action.
