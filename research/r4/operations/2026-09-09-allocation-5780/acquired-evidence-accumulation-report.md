# Acquired-evidence accumulation32 — CPU handoff

CPU_READY, MAIN acceptance/launch required. Authored by bridge_audit; any later author analysis must disclose that role. No GPU/model/service/lock/queue actions or accepted ancestor edits.

Sidecar: `sidecars/root-acquired-evidence-accumulation-v1`.
READY SHA256 `ca0210997b73190f010ea56d7c40361ba6360e605f00d97b75f573364eb040a8`.
Identity `d7727c26572295084d5f01a8374c1ee09da41fe79be97d2fa88ddcada38bf74e`.
Sealed source includes DESIGN/PLAN, ae_study.py, ae_protocol.py, decoder_tail.py, ae_collect.py, ae_owner.py, prepare.py, seal.py and four focused test files. Exact executable argv and1392 source/3966 input hashes are in READY. No source changes after this seal.

Verify: `/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-acquired-evidence-accumulation-v1/ae_owner.py verify`.
MAIN-only run: same interpreter and owner with `run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-acquired-evidence-accumulation-v1/outputs/attempt-001`.

## Frozen scientific boundary

All16 original scale large blocks: four exposed parent clusters ×128/256 ×count/weight; two paired contemporaneous arms=32, fixed24 root94022838…/c32 child. Source records, context/query bytes, order, IDs and original oracle answers unchanged. Historical `split=root_new` survives as a source-coordinate field, but these contexts are now explicitly scale-exposed; no new-root or unseen claim. All1024 distinct source IDs are reused. Sixteen fresh paired seeds, collision-free against62 actual prepared plan inventories; balanced8/8 arm-first dispatch. Golds are all nonzero; zero baseline0/16, best constant40=2/16 per arm, no resampling.

B returns only current validated batch; C returns a fresh ordinary dict of all successfully decoded inputs, latest valid input wins. Current-batch JSON/ID/category validation remains the original code. Both arms share conventional private state and unadvertised JSONL bookkeeping; only return-scope flag and truthful prompt description differ. Host decoder never adds labels, query, gold, reducer, calls or recovery. Valid fabricated/literal input remains accepted as in B and is never presumed acquired. Genuine child provenance requires independent raw-return/program/decoder-input/time joins; common bookkeeping alone is not state-use proof.

The ledger file is harvested through the qualified post-rollout task.finalize runtime.read seam into trace.info. It is not printed or advertised. It remains runtime-writable/introspectable; actual reload/tampering/ledger use are mechanism observations, not prevented. On failed/timed-out runtimes missing ledger harvest remains unknown. Native requests/returns and all physical JSON files remain available independently. Source validators and host gold never repair sampled outputs.

Return-scope compatibility risk: summing returned dict.values() into a batch-wise running total can double-count old records in C. The prompt explicitly discloses cumulative scope. Such errors, expanded cumulative printing/context growth, and abandonment remain policy/package outcomes; no code repair, compression or increased caps.

## Qualification and caps

Final10 focused tests passed52.13s; known SWIG deprecation warnings only. RED receipts preserved. The real ACP/IPython fixture ran B and C with two intercepted authored child completions, repeated assignment and intervening snapshot mutation, checked the actual next native request observations, authenticated native final, and harvested two ledger entries after rollout without adding ledger tokens to the model prefix. These are synthetic qualification examples, not sampled scientific acquisition. Actual owner→qualified service wrapper→intercepted inference Popen and collector argv/aliases passed; no real service started. Invalid/duplicate/missing/invalid-category/dict-input/truncated cases preserve errors and atomic state; fabricated valid JSON explicitly remains unauthenticated.

All32 gold-independent first prefixes were rendered and pinned, maximum1145 tokens; actual outgoing2048 root-action cap checked. Context8192, temperature0.5/top-p1, four workers/180s episode. Outer1800; work1650; owned1770; startup≤180, collection≤1440 with30 reserved, release≤120 and final harvest≤30 under inclusive owned deadline,30 outer margin. Early advance, no reroll. Model selection and all planned NULL slots exist before service. Missing RESULT is NULL; attempted physical/failure paths are distinguished from unstarted slots and all physical records enter cost union. Rejected requests are attempts, not proven GPU completions; missing usage and billing remain unknown.

Skill influence: external isolation followed MAIN's namespace preference; inline TDD established failing decoder/entry cases before implementation; final verification ran actual focused fixtures and fresh pin checks. MAIN performs independent source acceptance. No broader framework, novel memory claim, oracle-child arm or live campaign change.
