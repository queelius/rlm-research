# Record-interface24 CPU handoff

2026-09-09; runtime_port. Ready for MAIN review/acceptance only. No GPU or model service started, no lock or live queue changes, and no scientific attempt directory exists. All files in the new sidecar are frozen after READY. Old nativejoin sources, worlds, acquisitions and exact-source gates remain unchanged.

Sidecar: `/project/alex_phd/runs/rlm-research-r4/sidecars/root-record-interface-v1`.

## Frozen result and invocation

- READY SHA256: `72182ad8962dd34670d5febd57c8d7b09297f0c4c16b4b7df72dd3c14a57a2c4`.
- Identity: `a95cba4e50f6dac7b84dc65c80639aee5b5038fbaf17e82c0be9c30726990c7e`; 408 pinned files including reused source closure and retained CPU evidence.
- Actual composed `owner.py verify` exited zero in 0.211 seconds after sealing.
- Exact MAIN-owned command:

```text
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-record-interface-v1/owner.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-record-interface-v1/outputs/attempt-001
```

MAIN must privately bind the qualified credential and single owned GPU and apply the 1,200-second outer deadline. No credential values appear in artifacts or argv.

## Changes and exact lifecycle difference

`protocol.py` loads all four hash-pinned exposed nativejoin worlds. It provides T sentence/sentence, F sentence/typed-file, I typed-inline/typed-file interfaces, always using the same full record order and facts. `evidence.dat` and inline formats are accurately described separately. Typed objects have exactly the string keys record_id/customer_id/product. The eight paired seeds are 981397101–981397108; six arm-order permutations with the first two repeated and shuffled blocks are fixed before outcomes. No extraction, algorithm, gold-conditioned source conversion or forced tool use.

`native.py` explicitly loads the pinned original native tool/root/child implementation with the new study/protocol namespaces. `study.py`, service wrapper and taskset retain the existing qualified released-base runtime. No shared runtime or installed source changes. Same Qwen3-4B-Instruct-2507 cdbee75f17c01a7cc42f958dc650907174af0554, no LoRA, actual optional IPython/native-child inventory and free final decoder.

`collect.py` is the qualified nativejoin collector with its entire extraction stage removed, source packages generated directly from the immutable full facts, 24 rather than48 roots, three rather than six cells, and zero acquisition costs. It retains exact first native prefix and actual final-branch authentication, physical-request/usage checkpoints, raw native episodes, programs and observations. It does not equate episode completion with a returned final or automatically call a program a successful join. Independent execution/data-flow audit remains required. Provider billing is explicitly unknown.

`owner.py`: work1650→1080; owned1770→1170; outer1800→1200; cleanup reserve120→90; startup ceiling180 unchanged; collector finalization reserve30 unchanged; four workers and120/episode unchanged. Owner/collector paths bind only to the new unused attempt. Owner writes all24 planned NULL coordinates before service startup; collector also writes its own inventory before transport. Existing lifecycle start/preflight/owned release logic is reused, with the new explicit service wrapper identity. No retry/resume or automatic launch authority added.

`prepare.py` freezes source worlds, plan, all12 interface packages, all24 exact native prefixes, ordered tool JSON, host-only gold, preplanned NULL inventory and provenance. Original facts are unmodified; maximum initial prefix is1,650 tokens, initial+output4,210 below8,192. Host serialization/rendering took4.540 seconds and is recorded separately, not treated as billed inference.

## Focused verification

Ten final unit tests passed (8.008 test seconds;9.227 command seconds): all-source fact/order/format equivalence,24 paired slots/seeds, strict final vs episode completion/capped answer/mismatched text, exact owner→collector namespace, actual owned wrapper identity, owner startup-failure all24 inventory/shared1080/1170 deadlines/cleanup, actual service entry→binding/config/descriptor→intercepted Popen, sorted READY identity, actual collector all24 failure inventory with zero acquisition POSTs, and shared-deadline attempt flushing.

Actual three-arm CPU-native qualification passed seven authored transport calls in34.738 seconds: three fresh native REPLs, byte-level file assertions and genuine observations, three exact actual first native prefixes, identical ordered tool inventory, one optional native child and returned child observation, and three authenticated full final branches. These authored CPU answers/likelihoods are fixtures, not model measurements or successful science joins. No sampled source code was executed by the analyst.

The first fixture's post-run checker failed because it rebuilt tools from key-sorted JSON rather than the retained ordered tool string; actual messages matched. That qualifier-only mismatch is corrected. The scientific collector already used ordered tool JSON. Both fixture directories remain; QUALIFICATION_HISTORY.md explains the failure and the shorter hash-based authored file checks.

Pins: DESIGN `21bcf1cb08917a2a64ff7fc013a78d19f614c817f956895fd94e2fb09facef5d`; PLAN `f0605fa3e3f6127d65861738bec778f1f70183583b74e038497bafc224a853ec`; PACKAGES `94f037858018e8bb9acb0381ab730359ffc1d3bdc5398852b62a6ccecda0dd62`; NATIVE_PREFIXES `3b42fe72632e1d4e46e8841c44f9313a0218a266997f00be94582614a5df935f`; CPU_TESTS `2ce505f03c89f809e87412a98414a95607967699a716d83635f0e7acb56890df`; qualification-002/RESULT `a13ea42f139cc236def2c5cc9df37e64ba0cca6f17949ae5c2b23a088de7f520`.

## Interpretation limits

F−T estimates a truthful typed-file offer; I−F estimates immediate typed visibility. Tools and accurate descriptions are held common, not separate factorial estimates. Representation-specific wording and token lengths differ. A file load, matching final scalar/set, or apparent join AST is not proof of executed faithful aggregation. All four worlds are previously exposed; two seeds are nested within each world. This is an adaptive mechanism pilot, not new-data generalization or a novel join algorithm. Historical report NULLs are not repaired or rescored. Original runtime author provenance is disclosed; independent mechanism and endpoint review remains separate.
