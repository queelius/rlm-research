# Canonical-loader48 CPU implementation

New isolated sidecar: `sidecars/root-canonical-source-loader-v1`. DESIGN.md and PLAN.md implement MAIN's approved source-access-only ablation. New source files: cl_study.py, cl_protocol.py, cl_native.py, cl_collect.py, cl_owner.py, prepare.py, qualify_native.py and seal.py. Six focused test files accompany them. No original experiment, installed runtime, active environment, GPU/service/lock or queue was changed. This author must not label its later outcome audit independent.

Exactly48 new roots: all eight exposed semantic training00–07 contexts ×three queries ×FILE/LOADER. Paired fresh seeds981631101–981631124;12 blocks per arm-first order. The old FILE prompt is unchanged; treatment appends the explicit optional interface description. Common records.json/context.txt/query.txt/labels.json bytes are equal. Only treatment adds source_state.py. Thus it is module-availability plus description, not pure prompt-only or persistence.

`source_state.load()` returns an ordinary dict with exactly records:list[dict] and predictions:dict[str,str]. Each call decodes original private JSON strings into fresh independent objects. No query/gold/selection/reducer, automatic call/restore/retry or custom immutable container. It is conventional Python, not a sandbox preventing deliberate module tampering. Unlike supplied-map helper32, it teaches no aggregation algorithm.

All eight frozen original acquisition requests/native prefixes/suffixes authenticate; returned maps and13 semantic errors remain exact. No new source acquisition is scheduled. Their complete request/response/usage/lineage records are copied to reused-acquisitions, explicitly historical. New total cost includes only actual root/optional-child attempts and returned completions; historical8 once and per-endpoint hypothetical pipeline costs stay separate. Provider billing unknown.

Fixed857 reference/c32, unchanged qualified native coding role and optional typed-child transport,2048 action/8192 context,0.5/1.0 sampling, four workers/120s. Full1800 envelope =1650 work+120 owned cleanup+30 margin; owned1770; startup180/root1440/closure30 inclusive. All48 NULL slots precede service. Strict dataset primary, separate actual source/loader use, faithful reduction, map consistency, abandonment/recovery, NULL bounds and full costs. No checkpoint choice or dependency on current SFT outcomes.

Qualification: all48 actual-source first requests frozen, maximum1,227 prefix tokens. Eight focused tests passed in25.38s (upstream SWIG deprecation warnings only). Real owner→qualified service wrapper→intercepted inference Popen and actual owner→collector parser/protocol aliases passed. Authored collector fixture dispatched all48 slots, made zero source POSTs, retained all result files and separated historical cost. Actual ACP/IPython fixture completed two episodes/seven authored native transport responses (five root/two child),43.340s, no model/GPU work. Treatment mutated returned source objects, raised a retained NameError, then voluntarily loaded original copies and printed exact4; both arms returned authenticated Answer:4. Complete requests/responses/traces/observations remain in qualification-native-001. This is qualification, not scientific performance.

READY.json is the executable immutable source/input/qualification closure; `cl_owner.py verify` rechecks it. Exact command: qualified prime-rl Python, cl_owner.py run --output this sidecar's outputs/attempt-001. MAIN must independently review/accept and owns actual launch. No output attempt exists at handoff.

Skills informed bounded planning, test-first failures, external isolation, focused verification and review handoff. MAIN's explicit no-delegation/no-repository-edits scope supersedes generic branch/subagent workflows. No broad tests, refactoring, production hardening or automatic recovery were introduced.
