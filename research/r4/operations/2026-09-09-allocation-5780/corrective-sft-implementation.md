# Corrective reduction SFT implementation handoff

Status: CPU_READY_FOR_MAIN_ACCEPTANCE, 2026-09-09. No GPU, model service, runtime container, download, lock, queue edit, or attempt launch performed by this agent. MAIN retains all launch/GPU authority. The new attempt directory did not exist at final verification.

Sidecar: `/project/alex_phd/runs/rlm-research-r4/sidecars/root-corrective-reduction-sft-v1`.

READY SHA256: `90f5df76eca02dbdfd47bb3e3ab0afb391e96f8acde95ee042731911ac7f0999`.

READY canonical identity: `231e07c47f072656281f5a90d1a2a5a377fef1c431fac5b8e9d1755d833152d1`.

CPU_TESTS SHA256: `985b76b433aac9efa4f686b2ebca80d802824ec6823da982dc0b619cd4f13fd5`.

Fresh `PYTHONDONTWRITEBYTECODE=1 CUDA_VISIBLE_DEVICES='' /project/alex_phd/envs/rlm/bin/python owner.py verify` exited0 and returned that identity. READY closes1007 inherited/new source paths and2483 inherited/new input paths, using the already-qualified archived supervisor path mapping. All CPU_TESTS source hashes still match the sealed Python sources. No source changed after seal.

## Implemented scope

I authored this additive implementation of MAIN-approved Option A and the earlier runtime plumbing; I did not author the historical scientist complete-SFT driver or claim its results as my own. Approved design/history and all old artifacts remain untouched. Ownership writes were limited to the named new sidecar and this report.

- `study.py`: exact immutable source composition, low66c/c32 constants, runtime adapter application, native catalog IDs, complete-corpus hash validation and deterministic arm orders. Restores this sidecar at the front of Python's search path after inherited native imports; a native CPU test exposed and qualified this otherwise silent binding-module collision.
- `prepare.py` and `inputs/`: immutable128-group training membership from384 eligible historical training groups after explicit exclusions,32 fixed training coordinates,32 free and16 controlled coordinates per policy, actual native templates/token IDs, provenance, prospective common accurate prompt amendment, CPU qualification and READY sealing. Historical generated PROMPTS.json is retained; PROMPTS_ACCURATE.json alone is operative.
- `protocol.py`: valid one/four producer actions, separate known-error action, map parsing from actual visible observations without semantic gold repair, fresh-name corrective program using real `user`, strict authentic scalar, native contiguous suffix masks, offset-aligned mechanism/map-payload/copied-string/other spans, whole-request replay equality, physical token-ID cost accounting.
- `collect.py`: actual same-REPL native capture and exact graph-to-wire prefix verification; forwards actual c32 calls, clearly marks authored transport as non-policy; controlled replay reconstructs genuine source state and validates the first sampled prefix. Four readout workers share one installed native hook/environment scope, avoiding concurrent global-hook replacement. Actual final scoring reuses pinned sibling `metrics.final_capture`/`score`, not last HTTP status or trace flags. All raw graph, physical and role/typed audits survive for independent executed-action audit.
- `learning.py`, `train.py`: initial fixed8 forward-only objective/cost gate and separate matched four-step current-action SFT arms. Qualified BF16 base/rank8 FP32 LoRA, fresh AdamW1e-4, zero weight decay, clip1 and per-complete-update adapter/config/Adam/RNG/cursor/ancestry checkpoints. Fixed checkpoint4 only. Terminal is diagnostic-only, training weight0; no gradients-share claims.
- `binding.py`: real unchanged low66c and fixed4 selected checkpoint lineage, immutable c32 child, actual endpoint binding validation.
- `owner.py`: private credential-before-output preflight; exact fresh output namespace; serial inference/gate/train/readout ownership; shared deadlines, aggregate capture/readiness budgets, qualified process stop/release; all144 prospective endpoint slots retained in final inventory with unavailable/unrun NULL.
- `RUNBOOK.md`, `DESIGN_AMENDMENT.md`: frozen protocol, mechanisms, budgets, limitations and precise launch authority. Four focused test modules retain the checks below.

## Material scientific amendments requested during preparation

All arms now receive accurate `id`, `user`, `text` descriptions. Records/context files have no category labels; displayed child maps are predictions. The sibling visibility result motivates a common-contract alternative explanation and potential high unchanged baseline; this study does not claim intrinsic root inability. No membership, query, seed or target was changed by that amendment.

Each of eight deliberate metadata failures is now a separate authored root action after all producers and masked in both loss arms. Exact original membership truth is preserved: index%4==3 made all eight error cases width4, so original first-producer targets were already clean; no observed contaminated-control claim is made. The separate action provides an unambiguous error-history role. Counts:32 training episodes,80 actual c32 calls,152 authored root transport calls including8 error probes. Error and batching are confounded, not a factorial comparison.

Corrective mechanism NLL excludes copied map payload AND Python string literals (including user/category and dictionary-field strings), placing boundary-crossing tokens in other. The prospective gate requires≥4/8 mechanism means≥0.1 nat/token and median whole corrective mean≥0.05. These are practical spend floors only. Final scalar NLL is measured separately with zero training weight.

Controlled source acquisition is evaluation-only and physical once:16 states/40 c32 calls. Every policy reexecutes the same authored producers in its actual REPL; every child request must equal the saved request in its entirety, including sampling/caps/cache salt, before replaying its real response. No exception fields are currently allowed. First sampled root prefix equals the source genuine correction prefix. Source map semantic errors/scalars are retained. New post-boundary calls are paid normally. Per-row reports separate new physical cost, relevant shared acquisition and hypothetical standalone total; owner reports physical40 versus hypothetical40 per policy/120 for three. Cache-token usage is explicitly unknown rather than invented0.

## Verification

Retained CPU_TESTS contains full argv, stdout/stderr, timing, return codes and matching source hashes. Both commands used CUDA_VISIBLE_DEVICES empty and PYTHONDONTWRITEBYTECODE=1:

1. Native environment `python -m unittest -v test_focused test_native_cpu test_owner_cpu`:13 tests,7.420s, exit0. Covers exact80 native prompt renderings; real tokenizer suffix offsets/span partition; actual low66c weight hash; current suffix masking/history rejection; mechanism-only gate; fixed split/early8 crossing; separate error/action counts; observed wrong map preservation; exact whole replay request; shared native environment boundary; credential stop-before-output; native final text/tool strictness and malformed0 versus NULL. Only inherited SWIG deprecation warnings.
2. Qualified training environment `python -m unittest -v test_learning_cpu`:2 tests,0.941s, exit0. Tiny CPU tensors exercise real shifted CE, equal example mass with different target lengths, one real Adam step and deadline-no-step behavior. No model weights or GPU were loaded for these tests.

Source syntax was parsed on CPU. Native tests caught the inherited Python module-search collision; a narrower span test failed when category literals remained in mechanism NLL, then passed after explicit string-span separation. Separate replay sampling regression rejects a changed seed even with identical model/prompt tokens.

These are CPU qualifications, not a claim that the new native multi-action corpus or GPU optimizer already succeeded. The real first8 native capture/prefix/cost gate is intentionally the first experiment stage, with failed coordinates preserved and no retry.

## Exact lifecycle/authority boundary

No runtime or lifecycle file changed in this task. Owner composes the previously qualified row-mean lifecycle dependency loader and existing an27 `lifecycle_adapter.install`; native collectors apply existing `study_wrapper.adapt_stack`. Actual `service_wrapper_v2.py` identity remains symmetric for service claim/start/release. The actual580.159.04 driver library environment is inherited; no historical compatibility path was introduced. Services release before GPU gate/training subprocesses. MAIN privately binds the provider credential and single assigned GPU; the wrapper does not print credential values or include them in argv.

READY's exact argv is native Python + new `owner.py run --output <sidecar>/outputs/attempt-001`. Hard outer7200s, owner7080s, work6900s, cleanup180s, parent margin120s. Stage ceilings: capture1800 total, gate300, train600 per arm, free450 and controlled300 per policy, readiness900 aggregate/180 each, all clipped to shared clock. Owned subcommands retain PID identity, command/log/exit records and complete checkpoints. Failed or unrun endpoints remain NULL; no old namespace reuse, checkpoint substitution, resampling, or epoch extension.

## Remaining limitations and informative outcomes

The held-out64 record-group panel is already research exposed; training raw groups were training-exposed. New user-set compositions are prospective within that panel, not an untouched benchmark.32 training examples share eight source contexts, so they are not32 independent units. Matched updates/example mass are not matched token counts/FLOPs. Gate training-cost projection is a declared3× measured forward-time proxy, excluding startup/checkpoint overhead, and must fit both per-arm and remaining inclusive budgets.

Actual scoped reduction remains NULL in collector summaries until native branch/data-flow audit; it is not inferred from prompt or AST presence. Raw final branches, observations, shared-map consistency, physical requests and costs enable that audit. A controlled-only improvement indicates state-use learning without free-root reachability transfer; high unchanged accuracy after common prompt clarification would limit the need for training. Either is informative and does not justify automatic epoch extension. No extra experiment or live queue change was prepared by this handoff.
