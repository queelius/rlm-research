# Warm-start reference24: CPU-ready handoff

Prepared under MAIN's explicit default24 approval; no GPU/model service, lock, launcher, queue or historical source changes. Attempt001 does not exist. All implementation is isolated in `sidecars/root-warmstart-reference-v1`. Writing-plans/TDD/executing-plans and verification-before-completion guided the narrow test-first work; MAIN source review and acceptance remain required, not replaced by these tests.

READY SHA `3b1cbbe962262114b7931034f5d5dc98e21ca3b050f108a66fdbb4d14bb328f1`; canonical identity `25de35614384ba6640f8c978ccd5318d71a379540ef94f908ac4ecc9d971b1a2`. Fresh actual `owner.py verify` exited0 with that identity after sealing. READY carries complete source/input closure and the exact MAIN-only argv; neither command nor service was launched.

Implemented24 = released-weight reference through zero-effect857 / interface-SFT fixed4 efab / success-SFT fixed8 low66c × four specified exposed QSR readout contexts × two selected questions. No alternate32. Hash-fixed phase order `success8 → released_reference → interface4`; eight paired seeds981456101–108, same hash-fixed block order. Existing native role, optional Python/children and typed child exact-ID grammar remain common. No new basebinding, root grammar, source acquisition/map, role override, algorithm example or aggregation helper. Only the approved common all-scope clarification changes original question wording; source record/file/gold bytes remain pinned. Phase order remains a nuisance, and inference through zero-effect LoRA is not claimed bitwise equivalent to bare-base transport.

The exact eight-block answer histogram is0:4,2:2,3:1,5:1. Zero/bestconstant both4/8 per root. PLAN SHA `14d42158521848a5c76f2c3f736f395ca65bbedf2260d41702426b8396d3c1c8`; baseline distribution SHA `dec1c28e22759c053d05d7dc2c9db71d146d6861311a71372dc391226fc509b2`. No resampling followed this finding. Independent analysis must separate the four nonzero blocks, report paired four-context effects/NULL bounds and not promote zero-only improvements as mechanism learning. `EXPOSURE.md` records MAIN's later QSR final aggregate without changing this design or independently auditing CE.

Clocks:1800outer/1650work/1770owned; each phase≤480 including startup≤180 and collection≤300; release≤90. Nominal3×570=1710,60s left to owned1770,30s outer margin. All collection respects1650; final release/harvest can use owned cleanup. Collector15s closure reserve is subtracted within its assigned deadline. All24 slots are NULL before service; interruption/unrun remains NULL, never rerolled. MAIN termination prevents later phases; failed release is not retried and reports active ownership. Strict native final and planned-denominator operational success are separate. Cost preserves attempted transports, raw returned completions, parsed completions, pretransport rejections, usage unknowns and unknown billing.

Qualification:

- Nine focused tests passed13.93s,16.52s including process overhead. `CPU_TESTS.json` SHA `f726256d0f8e9e3fb8692fb4512bb52d872f594582b716f9bdaaf10e974b3294`; only existing SWIG deprecation warnings. Tests exercise exact three bindings/24paired plans/strict scalar scoring, phase clock/path/termination, returned-raw versus parse failure accounting, current-role2048 request configuration, actual owner→unchanged an27 wrapper up to intercepted inference launch, and actual owner-generated collector CLI through eight retained NULLs with native execution intercepted.
- One authored actual ACP/IPython fixture completed in22.56s:3native calls (2reference root,1typed c32 child), actual files/first native prefix/root2048/typed grammar/full final branch. Fixture RESULT SHA `74b2d1615b77b3c4c9e6c89b636936bce1634e48bd72e4cd9638a1a177ee4584`. Synthetic fixture code/predictions/logprobs are qualification only, never model outcomes or training. All24 CPU-rendered first prefixes are paired-identical; maximum1085+2048<8192.
- Initial missing-module test failures preceded implementation. The native-config fixture initially lacked its explicit fake private credential and was corrected locally; a targeted cost test exposed undercounting raw returned-but-unparsed completions and now passes. No production run or old source was repaired.

Source review list: `DESIGN.md`, `PLAN.md`, `EXPOSURE.md`; `study.py`, `protocol.py`, `native.py`, `collect.py`, `owner.py`, `prepare.py`, `qualify_native.py`; six `test_*.py` files;12 immutable input JSON files; native fixture and CPU test receipts. No source changes after READY. The checklist's final handoff item remains unchecked in its sealed text because report delivery followed sealing; this report completes that handoff without rewriting the seal.
