# Semantic-map externalization48: CPU-ready handoff

Sidecar: `/project/alex_phd/runs/rlm-research-r4/sidecars/root-semantic-map-externalization-v1`.
READY SHA256: `953eaf07242127050415bc922c3b08b9a4fa94f03f5f124ea08f9ff3ec73c7c5`.
Canonical identity: `dffe33f5e216fb553ffdd7d85c07360e5a71b607c96b957006311b63cf2a5f10`.
Closure1492 source+21 input pins. Actual `sm_owner.py verify` exited0 with that identity. Sources frozen for MAIN acceptance; no scientific attempt exists.

Exact MAIN-only argv:

```
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-semantic-map-externalization-v1/sm_owner.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-semantic-map-externalization-v1/outputs/attempt-001
```

Outer2400s, work2220, owned2370, cleanup150, outer margin30. MAIN supplies one GPU/lock and privately inherited approved credential. No GPU/model service, lock, queue, current-SFT edit or environment mutation occurred in this preparation. Native rollout interpreter only; no training/PEFT path is involved.

## Exact study

Fixed actually served reference857a7ce6 and c32, not current SFT weights or bare-base equivalence. Eight exposed QSR training00–07 contexts ×three supported operator/scope queries ×INLINE/FILE =48 roots/24 paired seeds; eight context clusters. Raw records are file-only in both arms. records.json/context.txt/query.txt are byte-identical within each pair; query.txt is the base question, never the arm-specific map-bearing prompt. Both get labels.json containing the identical actual returned map bytes. INLINE appends those same bytes only to task.data.prompt. Current native coding system role and optional Python/child inventory are unchanged.

Eight actual full16-ID c32 acquisitions, one/context, are made once. MAIN explicitly approved exact public-ID/canonical-category JSON grammar with1536 cap; no semantic/gold restriction. This differs from historical CE free-JSON acquisition, and no historical source-validity effect is claimed. Missing/authentication/structural failures gate all six associated roots to source-NULL; canonical wrong labels remain untouched. No retries, replacement, gold repair, semantic filtering, prior solution/history/program, algorithm/example or scalar hint. Optional root reacquisition is allowed and charged separately.

Primary strict authentic dataset final and co-primary **actual supplied-map-consistent executed scoped reduction** are separate. The collector emits dataset score and scalar map-consistency separately, preserving code/observation traces for subsequent manual execution/dataflow audit. `actual_scoped_reduction=None` deliberately avoids treating AST markers, literal presence or scalar agreement as execution proof. Malformed returned finals strict0; genuinely missing native/source finals NULL, with planned operational sensitivity/bounds. No model-program reexecution in audits.

## Changes/reuse

sm_study/protocol: exact isolated aliases, fixed857/c32 receipt, deterministic48/8 plans, common repaired all-scope wording, original host gold used only for scoring. Fresh seeds981512101–124 for roots and981512201–208 for sources; source/prepared-plan scan found no other use.

sm_native: small actual task wrapper writes labels.json and attests four common files, explicitly preserves plain_query, appends inline only to data.prompt. No sitecustomize/compact-role override is installed. Qualified CE native wire capture/current2048 root cap/first messages-tools-token and final-branch authentication remain in use; optional typed child transport is unchanged.

sm_collect: counted immutable CE collector adaptation for unique imports/owner path,48 roots/8 sources,300-source/1500-root caps and120 closure reserve, new setup attestation/co-primary audit marker. Exact qualified public catalog/schema supplies the source grammar. The eight source raw records and their seal precede native task preparation; actual map-bearing native prefixes are frozen after acquisition and before their root runs. CPU maximum-canonical-label fixture prefix1470, comfortably below8192 with2048 output; actual prefixes are still checked.

sm_owner: qualified an27 dual-adapter service_wrapper_v2/config/lifecycle reused without edits. Prewrites all48 NULL endpoints and8 acquisition slots, exact fresh output namespace. Startup≤180 preserves later source/root/closure reserves; source stage≤300, root stage≤1500 and work−120. Stages advance early, not timed waits. Release failures remain explicit active ownership. Harvest uses unique sm_collect import, not ambiguous generic collect. Complete owner status is not a claim of returned scientific finals.

Physical ledgers retain dispatched/returned/pretransport-rejected requests, raw token usage and unknowns; provider billing unknown. Shared acquisition physical count is8, not48. Per-attempt hypothetical standalone costs add that endpoint's source to its native work; unstarted slots remain planned NULL and their source allocation remains reconstructible from the fixed source/endpoint plan. Do not sum repeated hypothetical source charges into physical work. Optional child reacquisition is independently counted in root rows.

## Focused qualification

Seven focused tests passed23.58s, with two existing SWIG dependency deprecations. Tests cover48/8 pairing, host-label mutation invariance, exact common file/query/map bytes, only-inline prompt difference, actual typed source schema/public ID+text requests, strict0/NULL, exact owner→collector namespace, actual collector eight-source fixture with one bad source gating precisely six roots and42 remaining native attempts, and real owner→service_wrapper_v2→config/descriptor→intercepted inference Popen.

Separate actual ACP/IPython fixture passed35.24s: two representations, six authenticated native transports, two optional child transports, four root transports. Authored programs actually read records/labels/query files, exercise typed optional reacquisition, compute/print the scoped scalar, and finish through actual native final branches. Both setup attestations have identical four-file hashes; first prompts/tokens, current native role, root2048 and final branches match. This is CPU authored qualification, not six scientific model calls or a capability result.

JUnit, native traces/wire captures, fake service config/descriptor fixtures and hashes are retained under the new sidecar. No broad suite, old-source mutation or live-model validation was added. Budget caps can still leave NULLs; semantic accuracy remains limited by c32 predictions. The data are prior research/child/current-SFT exposed. INLINE/FILE is a matched representation intervention here, not a general memory or decomposition claim.
