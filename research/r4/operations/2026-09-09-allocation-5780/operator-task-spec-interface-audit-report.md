# Task-specification interface 72: independent audit

The sealed detailed report is [`analyses/root-operator-task-spec-interface-live-2026-09-10/REPORT.md`](../../analyses/root-operator-task-spec-interface-live-2026-09-10/REPORT.md).

Strict planned-denominator scores were `U=2/24`, `P=4/24`, and `J=4/24`, with only 16, 17, and 15 native finals available. Pairwise jointly available comparisons were small: `J−P` 2 wins/1 loss/7 ties (`n=10`), `P−U` 1/1/9 (`n=11`), and `J−U` 1/1/7 (`n=9`).

The mechanism result is clearer than the scalar result: across every executed final-branch program in all 65 retained episode graphs, there were zero reads or references to `task.txt` and zero to `task.json`. Manual review of all ten strict successes found zero faithful requested operator-and-scope executions. Six nonzero matches used simplified wrong operators; four zero matches were coincidences. Scores are retained unchanged.

Native body/token/physical-response joins validated all 65 episodes and all 48 admitted finals decoded to their recorded reply. There were 24 NULLs: seven timeout failures, sixteen null-content completions, and one outstanding tool route. Disk-union cost was 1,019 physical attempts (753 root, 266 child), 1,012 returned choice completions, 3,059,757 known input tokens, and 95,782 known output tokens; billing was not measured.

Interpretation: this optional file-plus-prompt package did not instantiate metadata use. The result does not test whether correctly retrieved metadata helps. The next useful comparison is a small reachability-first interface test with directly rendered or explicitly returned task metadata before another full task or training run.
