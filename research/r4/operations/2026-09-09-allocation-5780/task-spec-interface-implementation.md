# Task-spec interface72 CPU handoff

CPU READY; exact owner verification exited0. MAIN acceptance/launch only. All new files are under `/project/alex_phd/runs/rlm-research-r4/sidecars/root-operator-task-spec-interface-v1`; no active/ancestor sources, GPU services, models or queues changed. Sources/inputs are now frozen; attempt-001 is unused.

READY SHA256 **b90d595b0fe43ecd29d5e27160fe1ebc82ad0cb235374dae669dfc50bb809d0b**. Identity **a7810890c2096c38347426b9ef9a085016f9c9c3b78cbdec8c844ba4d5524169**. Seal and actual `ts_owner.py verify` checked1388source+3938input pins. Owner source SHA256 `93af4112e4eb9502007e6774378cfb7818b71a806e68b73eecba761eb533bbfe`.

Exact command:

```text
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-operator-task-spec-interface-v1/ts_owner.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-operator-task-spec-interface-v1/outputs/attempt-001
```

MAIN must provide the ordinary private credential and exactly one inherited GPU as for the qualified scale owner. This handoff did not launch that command. CPU verify used an empty CUDA_VISIBLE_DEVICES and PYTHONDONTWRITEBYTECODE=1.

## Frozen study and lifecycle

All24 current composed tasks, eight exposed contexts, U/P/J=72 endpoints, fixed SFT24root94022838… and c32de129… child. No training, primitive efficacy claim, new source groups, outcome-conditioned selection or resampling. English questions and original records.json/context.txt/query.txt/batch_contract.py bytes identical across arms; U actual initial native prompts also match their original composition prefixes. P adds task.txt explicit prose; J adds task.json equivalent fields; one matched discoverability sentence is the only prompt addition.

Metadata exposes the existing operator/parameter specification and changes redundancy/access. It supplies no algorithm/formula/glossary/example/map/gold/batching constraint. J−P still bundles file format, extension/discoverability and token differences. P−U and J−U have explicit wording/file-affordance differences. Metadata retrieval and faithful executed parameter use must be audited separately; no keyword-only interpretation.

3900outer/3870owned/3720work:180startup+3450collection+90harvest,150release/emergency+30margin. Stages advance early; no completion guarantee. Four workers,180-second episode caps,8192 context/2048 actions. One shared fixed24/c32 service, balanced cyclic arm order within hash-ordered paired task blocks. Exact seeds2026091901–24 are paired across arms; refreshed60named prepared plan receipts showed no collision. Original fixed policy selection does not depend on concurrent scale/warm outcomes.

371new Python lines: ts_protocol (public serialization), ts_study (task setup/binding), prepare (immutable fixed-panel/spec/native input production), ts_collect (exact original native collector with explicit namespace aliases), ts_owner (bounded single policy), seal, test_task and test_runtime. Owner reuses corrected ledger/harvest functions by hash-bound path, not copied endpoint cost snapshots. Existing RESULT remains original primary; missing RESULT staysNULL; all on-disk physical calls counted independently, including partial/late costs. No scientific retry, alternate checkpoint or primary completion salvage.

## Qualification

**6 focused tests passed35.12s**, two inherited SWIG deprecation warnings. CPU_REPORT SHA256 `f554da9d93070f4398bb9ff10432e572c79da16c2ad2db0ec70b0d9d5a6e8bca`. Actual owner→installed lifecycle→service wrapper→inference config/Popen was intercepted before model launch, verifying selected root/c32 descriptor and8192/two-LoRA config; exact owner→collector main parses72 and resolves this study/binding. Missing RESULT plus existing RESULT/late partial physical receipts retains72 inventory and known/unknown costs.

Authored native fixture genuinely reads task.json and records, receives one exact16-ID typed c32 map, executes a positive per-user maximum using visible spec/category, prints scalar and returns authenticated final. Three authored root requests/one authored child response; no GPU/model inference. This fixture's code is not present in scientific task setup. All72 initial prefixes and public setup files are frozen; gold/label changes cannot alter them. Prefix ranges U1064–1076, P1077–1089, J1076–1088, all fit cap. Prose reconstructed from each JSON object equals exact task.txt bytes. U source files/native prefixes unchanged.

Preserved failures: initial two test-first assertions failed on missing modules then passed. First complete fixture run:5passed/1failed before any model request because native keyword injection supplies `runtime` while wrapper argument was named `environment`. Added explicit keyword regression reproduced the failure; changed only parameter name and CPU preparation invocation to the actual keyword contract. Scientific input bytes were not regenerated or selected. Corrected full6-test/native fixture passed in a fresh owned qualification directory; failed receipt/directory are retained. No accepted source was edited.

FREE_PLAN SHA256 `59c7141bafe38f57c91851d4b49eef9220d37808559c00d7f034c4fff1505df9`. ANTI_COINCIDENCE SHA256 `c033a428a9c3947149cd0c4e4c1205ed0035357030f2851c53b965a4caffeaab`. All4zero/20nonzero pairs retained. Host-only alternative-answer equality diagnostics are frozen, not supplied to roots and not used for selection. They show why strict correctness is insufficient: threshold gold equals threshold-on-pooled-weight6/8; maximum gold equals pooledA sum2/8; conditional gold equals pooledB sum3/8. Report noncoincidence/mechanism diagnostics without dropping these tasks.

Approved design and PLAN/DESIGN receipt are pinned within READY. The original plan's checkboxes remain a prospective record; this handoff records completed implementation. MAIN may review all stable files now. No additional framework/hardening, acquisition or GPU work was performed. The writing-plans/test-first/debugging/verification workflow caught the actual setup boundary issue before launch; no broader test suite was used.
