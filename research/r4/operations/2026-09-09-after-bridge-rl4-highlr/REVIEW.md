# MAIN fixed higher-rate SFT8 bridge acceptance

September9,2026. Actual root adapter0ba42364 replaces low66c, with all32 original
coordinates/seeds/prompts/c32 and array/map broker projection unchanged. This is
the third prespecified policy package, not an independent-seed replication or
randomized comparison of training algorithms. The selected fixed final8 is not
the validation best. Original native ownership/runtime qualification is reused.

MAIN read all new files by full prior-view plus exact diffs, original collector
input seams and actual binding checks. Fresh closure/driver verification and
focused parent authority test are required. Two role-routing/import fixtures
passed in preparation. No full requalification or broad refactoring.

One frozen free-text READY description accidentally retains RL4/2286be text;
METADATA_CORRECTION is bound separately. Executable NEW_SHA, checkpoint/config,
SPEC, fixed selection, actual role route and complete hashes all bind0ba42364.
The stale low8 transport alias is likewise not a weight identity. Neither typo
changes sampled input or scientific treatment. Original files stay unchanged.

All32 planned outcomes incl NULLs, checksum8pairs primary,count8 separate; actual
native child/next-root evidence, not root-writable logs, determines delivery.
Root-readable bridge config is not blinded.1800 outer,1650 work,1500 collection
clipped,1770 recorded envelope,120 cleanup; exclusive MIG and allocation19:07:17.
