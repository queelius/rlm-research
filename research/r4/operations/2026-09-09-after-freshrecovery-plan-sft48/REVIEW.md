# MAIN acceptance: matched authored-plan SFT48

September9,2026 ~16:01UTC. Compare operator-authored classify-all-then-filter with filter-before-classify for user-specific queries. Eight global targets are byte/token-identical; eight user targets differ only at selected = records versus selected = relevant.16 initial actions/arm, same384 training groups disjoint from320 readout groups and192 unused validation groups. Targets2472/pass/arm; four fixed complete passes9888targets each. The training text is explicitly authored, not sampled behavior or a successful-teacher claim.

Common fixed low-SFT8 66cce starts both arms with fresh Adam, rank8FP32 adapter/BF16base/dropout0/WD0/clip1. LR1e-4 applies symmetrically by prospective outcome-informed MAIN amendment; no high-LR winning-checkpoint substitution. Hash-fixed orders, canonical then filter training; canonical/filter/unchanged readouts,16 paired query+length coordinates each. Primary filter minus canonical; no evaluated checkpoint selection, answer repair or forced plan at inference.

MAIN fully read all original production/preparation/qualification/tests/DESIGN/CPU_AMENDMENT/PREPARATION/RUNBOOK and flat corrected readout/launch/seal/test/amendment sources. Independent reviewer reconstructed all32 target rows,16 paired physical prefixes and exact split exclusions, with no numeric/data-contract blocker. Original actual top-level readout import failed because inherited PLAN literal occurs twice, not once. Its frozen READY/source and failure remain. New flat readout_v2/launch_v2 preserve inner native collection and scientific identity, replacing only three paths/readiness/actual executable selection. READY_V2 is the sole accepted executable version; original READY remains the scientific checkpoint identity.

Fresh full2497-unique-file verification matches READY_V2 source/input hashes. The parent uses a compact **content-addressed transitive closure**: it pins original/corrected READY plus30 direct source/input/contract files, rather than copying thousands of historical qualification paths into another parent JSON. The actual launch_v2 verify_corrected authenticates original READY, invokes original full source/input verification, verifies additive hashes and exact unchanged scientific input map, before any work; train and readout subprocesses independently repeat their declared verification. This changes parent manifest representation, not the required scientific hashes. No full-closure-check claim relies on truncated tool output.

Author12 focused tests passed; MAIN reruns actual corrected-entrypoint/import/flat-AST/workflow4 tests and verify0. Owned CPU qualification002 completed7 synthetic-provider calls, canonical32 versus filter8IDs,2 versus1helperbatches, both printed2. These are transport/program tests only; no fake likelihoods/observations/rewards enter training. Original qualification001180s failure and API-spelling corrections remain preserved.

2430outer/2400owned alarm/2280work; each training360, each collection420, service/release consumes shared clock. Maxima do not all guarantee fit. Failed fixed4/partial collection leaves missing planned cellsNULL; no fallback/retry. Wait for exact fresh-recovery parent4138703/start1077269993, require emptyGPU/sharedlease. Allocation ends19:07:17UTC. Real model behavior still needs measurement; no success/general-planning claim from preparation.
