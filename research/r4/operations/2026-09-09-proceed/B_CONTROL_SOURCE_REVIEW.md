# B80: independent bounded source review

Reviewed 2026-09-09, approximately04:32–04:35 UTC. Verdict: no Critical or Important
blocking mismatch found within the requested crosswalk, prompt/schema, fixed-weight,
strict-scoring and single-alias lifecycle scope. Parent alone supplies acceptance, exact
operation argv, empty-GPU/shared-lock ordering and launch authority.

Read the complete B_CONTROL_BRIEF.md, new driver.py/owned.py/DESIGN.md/README.md and focused
test source. Inspected the reused grammar owned loader and fixed-final authenticator;
the underlying suite/serve paths were completely or narrowly inspected during the preceding
padding review. No tests rerun, model/GPU calls, signals, frozen edits or new readout outcome
inspection. The root96 operation result remained absent, so this did not displace its audit.

Independent read-only verification passed:

- All134 SPEC source/input hashes authenticated. Exact B fixed epoch2/step204 weight,
  configuration, RESULT/SELECTION, curriculum identity and required optimizer/RNG/state
  members passed the inherited semantic authenticator. This is not a newly selected weight.
- Eighty B coordinates cross-reference all160 corresponding old/indexed parent coordinates.
  For both matches, serialized request bodies differ only in model. Full saved prompt-ID
  arrays hash to the exact parent typed-prompt hashes. The original schema/tool/message
  ordering, input IDs, labels, seeds and3,072 cap are retained, not reconstructed or sampled.
- The old-weight subsequence preserves its parent relative dispatch order. The later B
  service is explicitly a phase/time nuisance, not an interleaved three-weight comparison.
  The post-HF-result choice of this follow-up is disclosed rather than retroactively frozen.
- Scoring uses the unchanged grammar/anchor callbacks, with only a private make_request
  replacement. Exact-cardinality/duplicate-ID/canonical-label handling, unavailable semantic
  alignment for invalid outputs, infrastructure/unrun nulls, physical-token checks and cache
  missingness remain inherited. No output repair or generated-code execution is introduced.

The model placeholder exists only in prepared request templates. Binding writes a new immutable
bound spec, authenticates actual B descriptor/disk identity, substitutes only its truthful model
alias and re-verifies the full crosswalk. Live version/model cards and physical prompt IDs are
checked during collection. No original/old-model claim is attached to endpoint-original.json:
its B-only descriptor semantics are explicit.

Owned execution uses one B alias for the single binding map; the inspected suite/serve loop
supports this path. The wrapper requires an explicitly supplied operation root, a directory
beneath it, exact accepted argv, accepted source/READY/WEIGHTS/SPEC paths and inherited
environment. The main's intended after-grammar-followups/B80-owned placement is compatible;
the README example is not a hardcoded execution destination. The1800s clock starts in execute
after verification, with1680s work/120s cleanup; parent1830s envelope includes verification.
This disclosed timing convention is not an unrecorded claim that verification costs zero.
Finally releases only the authenticated owned service/descendants. No live single-adapter
service was exercised in this review, as intended.

Reviewed identities (SHA256):

| Artifact | SHA256 |
| --- | --- |
| driver.py | ff03d7cd540ef41c9a2b7b6b6eec42a4025b7232d92f5b33610c64dce76748fa |
| owned.py | a4084e7419b48690fc6cb704682da7d2c3b22e8f308247ac4452d172d9619565 |
| SPEC.json | 80c814648d896249cd15ef672a2b8d769f992f0ed5683a719f8d13f4e4676b02 |
| WEIGHTS.json | b7e6317802d9d6ad9c9e89028859e25e540099eebd84e191e930712fe47f3032 |
| PROMPT_IDS.json | 1f4ffbba058f3104cfe8796bff9c6677707124e382a5e9dc02cb89b2db5a8aef |
| B adapter | 59ad854293142161f6b5e613dd5d1e3630fed600637106d5eae2cbcd764e9200 |
| B config | cb56eb273b63376fa54da323982f99cf54dfee8af939eb1a94195636a980923a |

The B output directory was absent at review. No new safeguard, source change or launch is
requested by this verdict. Returning to the terminal-only root96 audit.
