# Root96 automatic successor

Parent-approved CPU preparation, 2026-09-09. This operation contains only the already accepted
root-return-contract96 study. Grammar160 will be a separate operation and cannot block this one.

Reuse the frozen queued-successors coordinator by importing it and assigning only ROOT to this
new operation directory. Its shared campaign LOCK, exact predecessor polling, parent acceptance,
empty-GPU checks, owned child process group handling and no-retry behavior remain unchanged.
No server is started and no predecessor is signaled during preparation.

Predecessor: coordinator PID1988752, start_ticks1072428288. Its recorded indexed-SFT child
COMMAND starts1788922102.2401078 and has5430s outer cap; the predecessor deadline is their sum,
plus the inherited120s cleanup grace before declaring a still-live predecessor blocked.
The parent's approximate1788922102.2124615 timestamp differs by0.028s; the actual immutable
COMMAND timestamp is authoritative here. Original START, child COMMAND/OWNER, and training
overall-clock bytes are pinned. PID reuse is not mistaken for the original process.

One successor: root-return-contract-factorial-v1/driver.py run, internal3600s budget and3720s
owned-process envelope including emergency cleanup and post-release CPU analysis. Nonzero exit
is retained, not retried. Completion markers include TERMINAL, ANALYSIS, and both service-release
records. The coordinator requires an empty GPU before launch, again while holding the unchanged
shared lock, and records GPU PIDs after exit. It never kills the predecessor or unrelated GPU users.

The parent alone may publish ACCEPTANCE.json with approved=true and launch `handoff.py run`.
This preparation writes only ACCEPTANCE.template.json with approved=false. All exact commands,
model/source/input hashes and required environment strings are bound before acceptance. API-key
value is inherited but never recorded. Waiting can begin while the predecessor is still live;
acceptance must arrive within300s after it exits. Poll every5s; occupied GPU wait cap120s.

Focused CPU checks cover changed ROOT with unchanged LOCK, exact predecessor identity and missing
acceptance/no launch. No service, GPU, model call, speculative install, retry or source modification.
