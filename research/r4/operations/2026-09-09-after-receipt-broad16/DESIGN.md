# Automatic broad16 successor

Main operational decision, September9,2026 at approximately06:54UTC. Execute the
approved broader16-update campaign after the exact owned receipt72 parent exits.
This operation adds no scientific decision, scheduler, retry, or live-source edit.
The pinned queued-successors coordinator keeps its shared GPU lock, predecessor
PID/start identity checks, parent acceptance, empty-device checks and bounded
owned-child cleanup. Rebind only its operation ROOT with a tiny loader.

Receipt predecessor PID2656566/start1074001564 is recorded in its START.json.
Use the actual receipt child COMMAND.started_epoch plus2430s outer cap,120s owned
cleanup and180s scheduling margin as the predecessor deadline. Never derive this
from the earlier waiter startup and never signal the predecessor. Retain exact
START/COMMAND/PLAN/ACCEPTANCE/source hashes as operation evidence.

One accepted job: frozen root-broad-curriculum-v1 campaign.py run, new
outputs/attempt-001. Its work18000s/inclusive18120s/outer18150s envelopes fit the
remaining allocation, whose inherited SLURM_JOB_END_TIME is1788980837 (19:07:17UTC).
There is ample room after the receipt job's worst declared endpoint. If runtime
state changes, no duplicate or unrelated GPU process is removed to force launch.

Main reviews scientific READY/source/qualification, publishes exact acceptance,
then starts this waiter with the owned predecessor's allowlisted CUDA/LD/credential
environment. No credential value appears in an artifact. Source identity includes
the additive qualification-only metadata resume, not only original PREPARED.
Checkpoint every update; partial failures remain partial and are not retried here.
Read-only result audits and next-job preparation proceed without GPU authority.

Two narrow tests are written first for the wrapper's actual pinned-source import,
ROOT/shared-lock identity and source-drift refusal. Existing qualified coordinator
behavior is not rewritten or exhaustively retested. No Git/environment mutation.
