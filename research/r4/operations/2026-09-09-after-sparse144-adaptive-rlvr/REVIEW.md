# MAIN exploratory launch decision

The frozen scientific nine-source implementation, seven tests, design and runbook were read completely. Fresh scientific verify exited0 and21 focused tests passed before this acceptance; implementer reports25 native tests and a real tiny two-generation PEFT/Adam recovery fixture. The shared coordinator is unchanged and hash-pinned; only this thin wrapper and declarative plan are new. Two parent tests first failed on missing wrapper/plan, as expected.

Start from exact SFT final4 efab2913, fresh Adam/RNG. The independent early audit shows real helper uptake in24/24 versus0/24, but only2/24 versus1/24 correct and poorer final formatting. This is a mechanistic training target, not a best-validation or SFT-benefit claim. Child weights remain fixed; only authenticated causal root tokens receive terminal reward. Fixed final8, save every update, no retries/rerolls to obtain reward diversity, observed inference/graph failures null, no invented gradients. Preserve all partial/STOP artifacts.

Inputs: frozen896 context-group allocation,128 training rollout schedule plus56 validation/transfer readouts. Work7080/inclusive7200/parent7230 seconds inside allocation deadline1788980837; four rollout workers. Parent owns only its child group and waits for exact predecessor identity; no predecessor signals. Recipe inherited declared_at_utc is historical template metadata; PREPARED epoch1788955081.6540992 and campaign/start bindings are authoritative current provenance.

This is exploratory research-first acceptance, not confirmatory qualification. No scientific source edits after preparation. Queue-exhaustion idle interval after sparse completion must be recorded separately.

