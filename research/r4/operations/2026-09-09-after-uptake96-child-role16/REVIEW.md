# Main accepts the16-episode child-role suffix pilot

Main read the complete approved design, implementation decision, every new Python
source/test, preparation report, design/plan/runbook, real native qualification
summary and frozen READY. Runtime660ed907…/experiment55e6492a…/qualifierf2208989…
are unchanged since their full read and eight-call real rootless CPU proof.
The three remaining production files are owned70f75fca…/resultsbb2636a7…/
prepareec34b2b1…; driver15e28a71… is a small verifier/collector/readout entrypoint.

Fresh main verification: seven focused tests PASS4.53s and owned.py --verify
exit0/planned16/GPUcalls0. Agent's separate final seven tests PASS7.21s. The
two-arm CPU fixture uses actual concurrent private runtimes/native graphs, with
identical root physical inputs and exactly47 added child tokens at both initial
and continuation calls. This verifies intervention delivery, not model obedience.

Fixed original root857a7ce6…/childc32de129…, four exposed task groups, two seeds,
unchanged root wrapper and child user requests. Three groups are failure-enriched;
the fourth is a moderate-cost comparator. Keep tools available and suffix only
at trusted depth1 before conversation/graph/rendered-prefix construction. No
training, source hot edits, payload repair, added retries or forced helper calls.

Main review required distinct pretransport allowed entries versus proved provider
receipt/sampling, and preservation of earlier completed answers after a later
global stop. Metadata and the saved-native-row test cover these points. Per-call
request-only tails, prevented attempts, recovered errors, empty/malformed replies
and incomplete/censored episodes remain separate. The built-in readout is not
a substitute for independent physical-request and strict-answer analysis.
Lexical suffix-conflict flags require review; suppressed useful answers are not
an efficiency win. No general causal/novelty claim from eight selected pairs.

READY497830d8…/SPECb2e55dbe… binds224 source paths and33 qualification/artifact
paths. The1800s inclusive clock retains1500s collection and120s cleanup reserve;
2048 allowed request-hook entries, four pair workers, inherited error stops.
Parent outer1830s plus120s emergency cleanup is recorded separately. Owned
cleanup runs in finally and uses the already qualified process-absence observer.
No CPU final readout runs under GPU scheduling authority.

Automatic predecessor is the exact already-waiting uptake96 parent2846988,
start1074421106. Its launch COMMAND does not exist yet; do not invent one. The
conservative13:45:25.588UTC predecessor bound includes its own predecessor wait,
acceptance/GPU-clear allowance,3630s job,120s cleanup and180s margin. This pilot
plus scheduling/cleanup/margin remains before14:27:55.588UTC, well within the
actual19:07:17UTC allocation. Earlier predecessor completion/failure permits
dispatch once the GPU is clear; no predecessor is signaled and no job is retried.

Two missing-file thin-handoff tests were observed RED before implementation.
Fresh PASS for the final plan and pinned coordinator is required before launch.
Only the operation root changes in the existing coordinator; its exact identity,
source acceptance, shared lock, nonzero-exit preservation and owned cleanup remain.
