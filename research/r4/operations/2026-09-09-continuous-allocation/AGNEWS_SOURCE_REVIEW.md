# Independent AG News96 source review

September 9, 2026. Verdict: **no material blocker found within this bounded review** of the frozen implementation against the approved AG News design. This is not launch acceptance, live endpoint qualification or a model result. Main retains all launch/lifecycle authority.

Bound freeze: READY `8c4e037ab93e406246c0198b27d5102b04d72c1f6eb741696addeb333e0333e8`; SPEC `052926cdb4b64cb38863241565bce8cf70f312fdf8922d383017075d3c1fc876`. Full new study/driver/owned sources, both tests and four documents were read, together with the material inherited request, scoring, service and lifecycle seams. Exact reviewed file and artifact hashes are in `AGNEWS_SOURCE_REVIEW_INPUTS.json`. No tests, model calls, service calls, source edits or outcome reads were performed by this reviewer. The retained preparation evidence reports eight tests passed in 4.14 seconds and twelve compiled grammars; those tests were inspected, not independently rerun here.

## Material checks

- **Data and gold separation.** The builder rejects a changed Parquet, duplicate/conflict census or selected-group digest rather than substituting records. Its 256 sorted normalized hashes and four display/numeral maps match the approved feasibility. Whole original news text is preserved. Independent checks of all 96 frozen payloads found exactly the displayed ID→text mapping, in displayed order, without per-record gold or source metadata. Canonical labels are an allowed vocabulary, not per-record answers. Public test provenance and unknown data license/pretraining exposure remain explicit.
- **Physical inputs and ordering.** All 96 stored request bodies and full typed prompt-ID hashes independently match the approved feasibility crosswalk. All 48 adjacent weight pairs have identical bodies except model alias and identical prompt IDs. Maximum reserved length is 4,707+3,072=7,779 of 8,192. The fixed loop implements the declared condition rotation/reversal and opposite weight-first order across seeds. Four asynchronous workers do not guarantee completion order, identical cache exposure or simultaneous pairs; the design does not claim those guarantees.
- **Scoring and estimand.** The exact ordered-tag scorer changes expected tag values without changing displayed-record label alignment. Wrong cardinality, duplicate object fields, unexpected tags/noncanonical labels remain invalid; no numeric-source rescue exists. Complete invalid outputs receive strict zero with unavailable semantic alignment. Provider failures and unrun coordinates remain null. Meaningful−ordinal and old−original interactions are computed per context/seed/prefix, with four context clusters and separate weights; class-count cancellation remains secondary.
- **Actual weights and service.** Both exact original857a and oldc32de adapters/configs are authenticated before service work. The new binding contains both truthful aliases; inherited service writes original/selected descriptors. The collector checks each descriptor against its frozen adapter/config/base, requires one service address, checks live version and both `/models` alias paths/base, and rejects response aliases differing from the requested model. This is the existing Chat Completions component path, not a TrainClient RLM trajectory. Physical response IDs are checked against the typed input proof; no live response is claimed here.
- **Source lineage.** The four private run substitutions and five owned-source substitutions have explicit count guards. Their reconstructed executed hashes match SOURCE_ADAPTERS: run `99b19fc4067160d849a86aa51fca4efb8b11dfed61168af43306371ad0d76d5e`, owned `362bff0e9aea775bc34cc5a584eb4adad8e58cb8b39e88f0b18dee706487949e`. The READY binds 134 direct paths. The reviewed service source is also authenticated transitively through the pinned suite MANIFEST and unchanged `suite.verify`; absence from the flat scientific map is not a missing runtime binding. This review checked those links and selected source hashes, not a redundant rehash of the entire model/history closure.
- **Clock, stop and release.** One `started` epoch feeds service startup, the 1,080-second work deadline and the collector's remaining budget; collection is at most 900 seconds and its command allowance is at most 930 seconds within that work clock. Release remains in `finally`, using the qualified process-identity/descendant lifecycle and narrow vanished-process observer. An HTTP500 preserves a raw failure/null, stops subsequent dispatch without retry, permits already in-flight work to settle, and leaves unrun coordinates null. Complete earlier outcomes are not erased by a later stop. Actual error/setup/timeout and owned release markers must be reconciled at terminal.

## Retained limits, not blockers

The owned clock starts inside `main`, after module import; the parent's 1,230-second outer process envelope is the relevant accounting for pre-main work. A small built-in `analysis.json` summary executes before service release. Independent analysis must follow release, and any measurable GPU-idle CPU tail should be reported rather than described as rollout time. This is not a reason to mutate the sealed runner.

`model_called=True` is assigned before the HTTP call, not proof of server receipt or sampled work. Use actual wire/HTTP/response records for cost and failure accounting; missing usage is unknown even when convenience sums are zero. Full typed prompt matching is a component input check, not native causal-graph verification. Freshness is exact normalized-group selection in this new AG task, not an assurance of distinct news events or absent pretraining contamination.

Reviewed primary source hashes:

- study: `c6cf251f5fc6de0f7d6aed22bae3927971880db2fa2378c654c4c16ba9d9e6f9`
- driver: `01811553694c348d21a4b4300c0013184c651d2a3f41b271cfa4205307d3ed36`
- owned: `d27951d0d8b220c7e847e17a135c5c79084400af90334c65574a01e679f8d1c4`

No correction or broader refactor is requested. Uptake96 and child-role16 frozen methods and all queued/live artifacts remain untouched.
