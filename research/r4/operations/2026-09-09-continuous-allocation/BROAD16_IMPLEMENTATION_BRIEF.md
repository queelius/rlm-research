# Broader root curriculum: execute a bounded16-update exploratory campaign

Main decision September9,2026 at approximately06:33UTC. Read the frozen data/design
proposal ideas/2026-09-09-broader-root-curriculum.md and data README before acting.
Only one broad campaign is authorized for CPU preparation here; main later owns
GPU acceptance/launch. A narrow matched campaign is a follow-up, not autoauthorized.

## Scientific choice and fixed scope

Use option1 and the already frozen root-curriculum-data-v1 data. It broadens source
documents, lengths and requested categories while retaining the count operation.
It is exploratory and NOT a breadth-only causal comparison: more updates, different
tasks and additional rollouts change together. No current-seed transfer outcome was
used for this choice. Do not wait for a positive receipt or seed result to prepare.

New implementation namespace: sidecars/root-broad-curriculum-v1. Original root
857a7ce6907c759a8c1b478eb53029d3b700b81c3094d8edb3394e52def9fcb6,
empty initial Adam/RNG, fixed old childc32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3,
same pinned4B base, native sampler and root-only TIS/PPO loss. Do not initialize
from a selected prior root or leak test outcomes into this starting choice.

Use24 training contexts:8each at16/32/64 records. The frozen16-slot schedule has
3contexts/slot (one per length), each visited twice with different targets. Use
8fresh rollouts/context:24/round,384training episodes. Training targetsHUM/NUM/ENTY/LOC;
DESC/ABBRare reserved requested categories, not unseen semantic labels. Other
class records may occur in training contexts. Report zero-count target prevalence.

Training master981268001; derive per-phase/task/round/repeat seeds by namespace
hash, audit collisions before freeze. Do not reuse rollout streams from earlier
campaigns. The eight validation contexts use one balanced training-category target
per context, two fixed seeds =16episodes at steps0/4/8/12/16 (80total). Fix target
assignment by context index, independent of answers. Validation is developmental.

Primary final policy is fixed-final update16, NOT validation-selected. Record the
whole validation curve and earliest-max validation checkpoint descriptively, but
do not evaluate a different checkpoint in place of final16. A stopped checkpoint
is partial; no relabeling as final16. Retain every adapter/config/Adam/RNG/state.

Paired final panel: original versus fixed-final16, same48coordinates/seeds/inputs:
- Composition6×64, one trained and one reserved target×2seeds =24pairs.
- Length4×128+2×256, one trained target×2seeds =12pairs; lengths reported separately.
- Child-test-exposed3×32, two trained targets×2seeds =12pairs; never call it wholly
  unexposed or pool it with child-training-supported results.
Total96final-panel episodes. Defer optional child-validation/SST strata to separate
evaluation once useful weights exist; no SST definitions/runtime adapter in this run.
Both transfer stages are fixed before training. Preserve their serial order as a
limitation, not an interleaved-weight or deterministic replay claim.

Metric: strict final aggregate answer plus complete failure/coverage accounting;
per-context gains/losses/ties, actual root/child calls/tokens/cache, sizes of requests,
first-action syntax, and recorded intermediate correspondence when observable.
Keep unknown/censored/infra outcomes distinct from semantic errors. No full-label
coverage requirement, extra process reward, trajectory repair or recursive-shape filter.

## Runtime implementation and bounded qualification

Prefer private authenticated adapters over root-rlvr-independent-seed-v1 and its
original coordinator/native/trainer, as already done for other sidecars. Do not edit
accepted source files or install/upgrade active environments. All new sources/data
and actual environment versions belong in the new frozen closure.

Main inspected these material fixed-size seams:
- Original campaign_common.check_generation bounds generation1..8. Change only
  the declared upper bound to16 while preserving digest, previous policy, optimizer
  cursor, fixed child and contiguous commit checks.
- Original campaign.run_campaign hardcodes8 in loop/terminal/final policy, validation
  list0/2/4/6/8, planned validation8, and validation-based selection. Adapt to16,
  0/4/8/12/16, actual16validation count, fixed-final16; keep the state machine,
  checkpoint commitments, no-retry behavior and owned cleanup.
- Original campaign_train.authenticate_group hardcodes32planned/recorded. Require
  the frozen plan's24and exact coordinate set, not an unconstrained arbitrary count.
- Native make_tasks currently pulls original pilot tasks plus fixed TRANSFER files.
  Build only tasks from the candidate PUBLIC and HOST_GOLD using the same strict
  task wrapper and public definition/example instruction. Never pass HOST_GOLD or
  labels/source membership metadata into the runtime. Context bytes and public
  task questions must match the frozen candidate data exactly.
- Existing known-child-overflow exclusion remains qualified; no new automatic
  negative-reward admission. No mixed fresh reward group may stop this attempt;
  retain it as an observed feasibility failure, not silent rerolling until success.

A small checked private source/AST transformation is acceptable if replacements
are exact/count-checked and focused tests exercise real resulting behavior. Do not
create a general scheduler or rewrite the native math. Give generation8→9→16,
24-row admission, unchanged root-only masks and primary final16 selection focused
tests. Use the observed narrow process-exit fix from root-seed-lifecycle-continuation-v1
for service observations, with exact source attribution; no other lifecycle rewrite.

CPU-qualify actual task/prompt reconstruction, all source-group partitions, no gold
or unrelated SST definitions in model-visible inputs, template/tokenizer config,
seed/plan cardinalities and actual imported native interfaces. Reuse trusted fake
fixtures/rootless qualification where informative. Do not fabricate current-policy
logprobs or claim a fresh native training capture before the actual first rollout.

## Compute envelope and ownership

OneA100,24rollouts/round; max8concurrent episodes and unchanged native service limits.
T0.5/fullsupport, root2048sample cap/max8192causal tokens; retain actual child and
environment deadlines. Keep native recorded physical IDs/logprobs, no retokenized
behavior likelihoods. LR5e-5, AdamWweightdecay0,clip1, frozenBF16base/FP32LoRA,
TIScap2/PPOepsilon0.2and existing guards/weighting remain unchanged.

Work cap18000s (5h), inclusive exception18120s with120scleanup reserve, outer18150s
plus ordinary owned cleanup. Planning estimate3–4h, not a claim all of it is GPU
optimization. Collection1800s/round, optimization600s, service-ready180s,
validation900s, transfer3600s. Checkpoints every actual update. Main will compare
the launch time to remaining allocation; do not launch without sufficient window.

Prepare CPU-ready within~35–45minutes, report material findings early. While this
is being prepared, current training, identity96 and receipt72 occupy the GPU.
The user explicitly authorizes rapid scoped research preparation, so no blocking
approval question, broad test suite, Git integration or documentation polish gate.
READY binds exact argv/source/input/model identities, caps, source exclusions,
failure policy and actual preparation qualifications. Main accepts and launches.
