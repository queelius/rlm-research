# Child-role suffix pilot: CPU implementation approved

September 9, 2026, 08:03 UTC. Main read the complete design and inspected the
pinned nano `_start` seam and collector `run_slot` call. This approves bounded
CPU preparation, not GPU launch and not changes to the active BROAD16 lineage.

Design: `ideas/2026-09-09-child-role-suffix-design.md`, SHA-256
`d1665da58c22172c921944b463af1847983d7f28a7afb5302749b5412d3ac708`.

Implement the specified16-episode suffix-only comparison in a new owned sidecar.
Keep the original root857a7ce6… and fixed childc32de129…, four exposed groups,
two frozen paired seeds, full root prompt, child user requests, tools, sampling
and native error rules unchanged. Three groups are failure-enriched; the fourth
is explicitly a moderate-cost comparator. This is a small diagnostic experiment,
not an unselected prevalence estimate or a causal estimate of general efficiency.

Use the host-only ContextVar solely to select the private episode runtime overlay.
Qualify its concurrent propagation and reset, and insert the exact suffix at the
trusted depth1 `_start` seam before graph/message/rendered-prefix construction.
Prove unchanged initial root physical tokens and child user/tool schemas on matched
fake paths; prove the child system change actually reaches initial and continuation
provider inputs. Do not edit shared clones or any historical/active source. If the
ContextVar does not reach setup, report the observed boundary before substituting
a different arm-delivery mechanism.

Retain1800s inclusive job,1500s collection and2048 actual native dispatch ceiling.
The numerical guard must distinguish prevented dispatch from sampled/provider
failure. Record censoring and exposure by arm/order; do not turn incomplete controls
into cheap wrong answers. No per-child tool/turn prohibition is added. Directly
inspectable call/token reductions and strict final answers are the primary readout.

The suffix may conflict with a root child request that asks for code or an aggregate,
rather than item labels. Preserve such requests verbatim and mark that mismatch in
analysis; do not silently rewrite them or interpret all treatment nonuse as defiance.
An instruction that reduces calls by suppressing useful answers is not a success.

Use focused synthetic and owned rootless qualification, not broad production
hardening. Main will review source, qualification and immutable launch contract,
then queue this pilot behind the already accepted helper-uptake experiment. No
GPU work, new environment or long-running polling is authorized to the preparer.
