# Accept composed CPU evidence for exploratory typed operator24

September9,2026,11:03UTC. Main explicitly chooses not to block a useful new
GPU experiment on another expensive duplicate CPU-runtime fixture.

The new all16 case passed actual owned operator→broker→native typed-child
dispatch, exact schema/alias, valid answer reduction and zero sampled root
ancestry. The new filter16 case did NOT complete: its owned runtime setup
overlay exceeded the160-second fixture cap before any provider request.
The first fixture had also timed out at inherited setup45 before inference.
Both failed/partial attempts remain intact and must be bound in READY.

For this exploratory launch, combine the actual all16 native proof with the
unchanged previously qualified filter operator program and exact request/schema
checks covering all36 distinct real batch requests and136 planned sessions.
The typed native hook is unchanged apart from its exact public matcher/setup
binding. Main read every new source/test module and the inherited driver/setup
seam. This is sufficient risk-proportionate evidence to run, not a claim that
the fresh typed-filter native fixture passed. Record its uncompleted path.

The preparation gate must authenticate the actual partial result and original
filter qualification, not manufacture a two-case PASS. Keep symmetric setup120
in the new study, collection1440/work1680/owned1800/outer1830. No original study
is edited, no model outcome repeated, no failed episode repaired and no cap reset.
The real24-episode experiment will test filter execution; preserve its failures.

Shared private VFS/container startup contention is an observed resource cost,
not a scientific or provider failure. Investigate a separately owned local
ephemeral runtime-image cache for future jobs after this source is READY; do
not change any live image, store configuration, accepted source or environment.

Main separately binds and accepts the exact READY launch through its coordinator.
