# Implement the36-episode contract-matched child comparison

September 9, 2026. Main read the complete typed-helper-child design
90f8195e5fcbdf4889101be78f3568ed5c85c3411c8778543be80bb031b67e60,
the completed uptake audit84a853ca369cca5b36ea8762a8296492d96ba237ccc33dbb179817edde746831,
and the full private-image completion report/CPU_READY. Authorize CPU implementation
and qualification now, not a GPU launch. Use new sidecar typed-helper-child-v1.
Do not change any accepted source, run, runtime, checkpoint or old analysis.

Question: does an explicitly constrained, source-bound child response contract
improve complete RLM answers when root prompts and weights remain unchanged?
Use36 episodes: all six existing64-record contexts, both count queries, fixed
historical step8 root473210b1… and childc32de129…, U/R/T as designed, twelve new
coordinate seeds from981291600+1…12 after collision check. Balanced six arm orders
twice, deterministic order seed981291620, four concurrent triple workers. U is
calibration; T−R is the primary contrast. No selection by prior helper uptake.

Select immutable preinstalled image8cfe5976…/OCIa4c710e9… symmetrically in all
three arms, using its qualified private wrapper/store. Main is approving this
environment choice for the new study only. It is not environment-identical to old
campaigns or a proven throughput win. Record actual startup/setup/compile clocks.
Preserve the existing image and private artifacts; no rebuild or shared writer.

Implement the smallest study-owned native hook and exact public request matcher.
Authenticate trusted depth/invocation/alias/kind, entire helper-built request bytes,
unique known IDs and matching public texts, exact six-label vocabulary. Bind one
decision per invocation before dispatch and preserve it. Root and all unmatched
children are untouched, with explicit nonmatch reasons. Do not attribute Python
helper origin from root-writable logs. Schema comes only from requested IDs and
fixed labels, never host gold. Use a fresh per-request SamplingConfig; no mutation
or cross-invocation leakage. Record actual nested native wire grammar, canonical
schema digest AND ordered schema bytes, requested IDs, role, call and invocation.

The constrained child action space suppresses tool-call output and enforces key
order; document both, rather than describing this as a wording-only intervention.
Root probabilities/masks stay native and unconstrained. Child grammar probabilities
are evidence only, never root training credit. No optimization, hidden answer
repair, automatic free-output fallback, new retries, forced uptake or score changes.

Before READY, focused tests must fail then pass for root lookalikes, unknown/changed
records, duplicate IDs, altered vocabularies, ambiguous request parsing, trusted
metadata failure and two concurrent invocations with different schemas. Use a
bounded real owned CPU-native fake-provider fixture for the actual hook, not just
an isolated matcher: exact R/T root wires, exact child wires except schema, normal
graph ancestry, correctly parsed typed output, and untouched nonmatching child.
Qualification responses/logprobs are synthetic and never scientific model data.
Preserve failed CPU attempts; local fixture fixes may proceed inside one declared
preparation envelope without repeated parent permission. No model episode rerolls.

Reuse the existing strict final-nonblank-line numeric task scorer and observable/
graph-valid accounting, including scored completed-empty policy failures; do not
replace it with the adaptive pilot's different unanswered-null contract. Publish
all planned36 coordinates and raw statuses/costs, with actual counts and unknowns.
Record pre-treatment root-token equality: same seeds do not guarantee identical
sampled actions, as role16 showed. No causal effect claim from an early divergence.

Service ready180s, cumulative collection1800s, work2280s, owned2400s, outer2430s;
all startup/compilation/collection/export/cleanup share the absolute work envelope,
with120s owned cleanup reserved and no phase reset. Main will review the source,
CPU proofs, immutable inputs and actual sampler/wrapper before accepting an
automatic successor. Publish READY plus a concise handoff; prioritize a runnable
comparison over broad test suites or a generalized framework.
