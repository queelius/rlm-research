# Accept the disjoint-number and prefix control for CPU preparation

Main decision September9,2026 at07:05UTC. Read the full proposal
ideas/2026-09-09-identity-factorial-design.md, SHA
d2d48f25ae6850501f5d9a3395e0aca6e840883f63f9940a0304113e933bb21f.
Approve its384-call prospective factorial in new sidecars/leaf-identity-factorial-v1.
No GPU authority is delegated. Broad16 is already active and remains next priority;
this will be a ready successor/contingency, not an interruption.

Rationale: post-hoc numeric alignment explains much of ordinal error on all32
completed calls. That makes a disjoint-number control more informative than a
prefix swap alone. Do not turn the explanatory rescore into successful task
performance or silently relabel the old ordinal condition as a neutral control.

Preserve eight contexts, two existing permutations, fixedc32de child, two freshly
derived seeds, and the full prefix×numeric-overlap×tag-rule grid. Use labels only
for host scoring, never ID assignment. Primary displayed-position scores and the
overlap-only numeric-alignment diagnostic must remain separate. Disjoint numeric
diagnostic is unavailable; do not introduce a hidden rank mapping to rescue it.

Caps1800s collection/2400s inclusive owned/2430s outer,120s cleanup reserve, four
workers. Freeze actual source/code/model/data/request/grammar/tokenizer/seed/dispatch
identities before launch. Use existing isolated environments. Qualify new token
lengths rather than claiming equal realized compute. No free-output filler.

Implementation is bounded to the new sidecar and a preparation report. Reuse
pinned existing HTTP collector/lifecycle where possible; count-check any literal
cardinality/cap adapter. Focus tests on data namespaces, actual384 coordinate
dispatch, strict primary versus diagnostic alignment, and full prompt/grammar
construction. Publish READY only after final CPU qualification; main reviews,
accepts and installs the automatic handoff while broad16 runs.
