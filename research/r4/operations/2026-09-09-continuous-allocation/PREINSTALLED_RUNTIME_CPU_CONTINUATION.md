# Bounded correction of the private runtime preflight

September 9, 2026. Main has read the complete failed report, adapter,
probe and raw ISOLATION.json. The failure was an unsupported global CLI option,
before Podman storage initialization, not a test of container isolation.

Preserve the first source and attempt unchanged. Authorize an additive corrected
attempt in a new versioned directory and new task-owned builder storage. Remove
the unsupported global option after checking the installed CLI/config semantics;
use only private configuration paths. All original ownership, HOME-preservation,
exact-base, immutable accepted-job and no-shared-writer constraints remain.

Use one new 900-second aggregate CPU preparation envelope and at most two fresh
180-second native fixtures. Within the preparation envelope, up to three recorded
CLI/configuration preflights may repair local adapter mistakes without returning
for another parent approval. They must not reset the clock, rerun a scientific
model episode, hide earlier failures, broaden shared-state authority, or implicitly
repeat a failed image build. Only one real installation/build is authorized.
If isolation itself cannot be established, or the deadline is exhausted, preserve
the evidence and return the actual limit. Keep two CPU workers and no GPU calls.

This is a local tooling correction, not a changed scientific treatment. The
current uptake, child-role, AG News, adaptive and cue-order jobs remain frozen.
An image is not qualified until full native setup/PEP723 and the fresh-container
offline comparison pass. Report filesystem free bytes as filesystem-level only;
they are not project quota headroom. Record the actual project quota if available.
