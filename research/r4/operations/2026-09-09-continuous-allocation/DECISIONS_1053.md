# Adaptive decisions at10:53UTC

Main made these decisions while the accepted typed36 run executes. They are
exploratory research choices, not promotion of preliminary effects.

1. Adopt the typed-adaptive operator designfa9d51c2217c80de6d26f810315103d60b60df34ad5871950061c0239e4b2c97
   for CPU implementation. Same four exposed contexts, two fresh seeds,
   user-all/user-filter/shared-global:24physical/32logical references. Keep
   childc32de, public batch-contract bytes and zero sampled root credit.
   Native grammar is a child action-space intervention, not a semantic validator.
   Work1680/owned1800/outer1830. No new retries, answer repair, data reselection
   or active source/image mutation. Main separately accepts exact READY.
2. Revise proposed root interface-SFT32 examples to include final-answer teaching:
   16 authored native helper-call first actions plus16 final-answer actions after
   actual public-metadata observations. Two epochs/four updates from473210,
   fresh optimizer. The original32 proposal had no final-answer targets and
   cannot honestly claim final-output competence. Metadata targets vary and
   contain no TREC semantic gold or fabricated child answers. All preceding
   prompts/actions/observations and renderer scaffolding are masked appropriately.
   These are newly authored SFT targets, never reclassified native fixture
   likelihoods. No complete semantic filtering plan is taught. Freeze split,
   query/length readouts before inference;48 original/final episodes proposed.
3. Child-interface compatibility is explicit: typed36 matches receipt_api;
   adaptive/SFT requests use batch_contract. The new compatible typed operator
   matcher is required before claiming those calls are constrained. Evaluation
   uses the same chosen child interface for original and final root models.
4. AG96 strengthens the ID-package research direction, including original weights.
   Let the pending cue-order readout choose a small mechanism follow-up. Do not
   attribute the AG gain to SFT or promote a whole-RLM gain from leaf accuracy.
5. No role-suffix promotion: exact score unchanged and control did not reproduce
   child loops. No repeated determinism investigation: preserve observed
   pretreatment sampling divergence and use appropriate paired interpretations.

Prepare data/code while GPU runs. No broad suite, exhaustive review, literature
survey or report polishing should block the next scoped, capped, useful run.
