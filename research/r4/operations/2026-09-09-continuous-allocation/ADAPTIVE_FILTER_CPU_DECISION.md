# Query-sensitive planning: bounded CPU implementation approval

Main decision, September 9, 2026. I read the complete design
`ideas/2026-09-09-adaptive-filter-pilot-design.md`
(SHA256 ca35e4ff864a91d5b54a95bc4c28bed1f97c5c6a435b8520647efceedd958578),
the source-group feasibility and the actual pinned engine/supervisor seams.
This is CPU preparation authority only; no GPU launch, changed active run or
interactive approval checkpoint follows.

The useful question is whether a coordinating model can change its work when
the question changes. Counting all records and counting records belonging to
one user have different efficient plans. Compare a frozen all-record operator,
a frozen query-aware filtering operator and a free coordinating model. This is
a whole-policy comparison, not a claim to have isolated learned filtering from
all prompt/interface differences. The proposed common request-builder/parser
module is a new public interface; disclose it rather than call the free arm the
unchanged historical harness.

First prove the small, consequential feasibility question in a new private
`sidecars/adaptive-filter-pilot-v1` directory: can the owned pinned nano/Prime
runtime execute an operator root, call a real brokered child with a deterministic
CPU fixture provider, and return the computed answer through ACP without inventing
a sampled root request? Keep ordinary child generation and routing unchanged.
Use the depth0-only `_run_loop` seam after ordinary `_start`, actual IPython,
`open_scope(invocation_id, request_id=None)` and ordinary cleanup. No host model
code execution, fake root completion, synthetic root likelihood or repaired
root-training export. Fixture outputs are qualification evidence, never research
observations or future training data.

Require an ordinary free-root fixture alongside that operator, zero root provider
calls for the operator, real child ancestry and exact full initial child prompt
token IDs when ordered inputs and seeds coincide. Record an honest operator
transcript separately from sampled assistant turns. Strictly score the computed
terminal against host gold; retain child failures and cancellation. A root
training exporter requiring root actions is not the right validator for an
operator baseline. No new general-purpose scheduler/framework is justified.

If this bounded seam proof passes, proceed without another interactive checkpoint
to implement the exact40 physical/48 logical design, source512 membership, public
files, host gold, two seeds, dispatch table and focused tests. Do not execute
identical global baselines twice or count shared outcomes as independent. No crop,
label quota, answer-conditioned selection, hidden oracle or new source reservation.
The named-history exclusion is not an all-OOLONG or base-pretraining holdout.
Keep the prescribed historical step8 root and c32de child fixed, not a checkpoint
chosen after the current broader validation results.

Proposed work2580/owned2700/outer2730 seconds with collection≤2280, one absolute
clock and cleanup120. Confirm actual lifecycle fields during CPU qualification;
report any material deviation before READY. These are tentative allocation caps,
not measured speed promises. Null/incomplete answers remain distinct from observed
wrong/malformed answers, and operator interface failures do not become fabricated
counts or automatic infrastructure exclusions.

After freeze, main will independently read the implementation and accept an exact
successor operation. If the narrow seam fails, report the saved evidence promptly
and stop expanding the implementation; do not let a general framework rewrite
displace the ready GPU queue. A successful pilot can motivate small query-varied
RLVR only if there is observable reward variation and a real cost/accuracy gap.
No adaptive training or cost-shaped reward is already approved by this document.
