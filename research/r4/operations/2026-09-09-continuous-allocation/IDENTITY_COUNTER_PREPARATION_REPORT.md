# Identity-versus-counter96 is CPU-prepared

New sidecar: `/project/alex_phd/runs/rlm-research-r4/sidecars/leaf-identity-counter-v1`.
No model requests, GPU actions, service signals, acceptance, training or historical
source changes. READY.json is the final publication marker, not launch authority.

The exact eight exposed padding128 contexts are retained. New source-ID ranks are
assigned by a namespace/master981266001 hash of source context/group identity,
independent of labels and display order. Each record retains that randomized q-ID
through two independent hash-derived presentation permutations. Parent IDs and
record provenance remain in DATA.json. Three ordered-array tag formats classify
the same displayed record: matching source ID, displayed ordinal p0001..p0064,
and constant p0000. Canonical-label enums never include gold-specific constraints.

The96 calls use seeds227909818 and1362606574, derived from the new namespace/master;
the retained non-output SPEC/READY/RECIPE collision audit found no match. Six
three-arm orders appear5 or6 times across32 paired coordinates. All arms retain
identical ID-bearing source text/order, system and tools within each coordinate;
only truthful instructions and exact tag grammar differ. Four source groups per
task, presentation permutations and sampled seeds are nested, not independent data.

Five desired tests captured RED before study.py existed, then passed in both the
default and native CPU interpreters. Checks cover stable IDs across orders,
label-independent construction,96-grid/order balance, paired input equality,
strict tag/cardinality/unknown alignment, three real collector calls against a fake
HTTP provider, deadline refusal and release-after-startup-error. No broad tests.

Native qualification compiled20 distinct schemas and rendered all96 requests;
full prompt IDs are retained. Maximum2838 prompt tokens plus3072 output=5910,
below8192. Both q/p prefixes encode as one token, and all65 corresponding full
q/p tags have equal token lengths. Therefore p was retained before inference;
there was no alternate-prefix or outcome-based choice. This is tokenization
qualification, not a claim that realized generated output cost will be identical.

Private frozen padding/grammar helpers supply the native renderer, exact scoring,
first-response collector, raw-wire capture and endpoint authentication. The
scorer adapter substitutes only the expected output tags in a temporary gold
view: label scoring remains aligned to the displayed record, not a dictionary
lookup by an incorrect ID. Per-call class-count vectors and actual object key order
are retained alongside strict/semantic/null scores and paired contrasts.

The owned wrapper is a scoped copy of the qualified padding wrapper with namespace
and96/cap changes; its imported suite/lifecycle code is unchanged. Exact existing
old-child c32de adapter/config/selection and base closure are authenticated.
No new model choice or gradient step occurs. The wrapper uses the one-alias
endpoint-original descriptor truthfully and releases only owned processes in finally.

Proposed parent argv:

```
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-identity-counter-v1/owned.py --directory /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-identity-counter-v1/owned/attempt-001
```

Four workers,120sHTTP,zero retries,T0.5/full support,3072output/8192context;
600s collection,1200s owned from main entry,120s cleanup reserve,1230s parent cap.
Inherit the parent's actual MIG UUID and LD environment; never substitute CUDA=0.
The parent owns serialization, GPU-empty checks and acceptance. Inputs are frozen
before any call; no decoder repair, prefix credit or generated code execution.

SPEC SHA256: `c6ab4f82525424b5aef77a21be1dc7fe46c333bf54575a48070b27a850a5565b`.
Prepared identity: `369ad053023f19e4970aa514a1f5d749857f8a6c22d23a6bc3e9fdbd51a5f0ef`.
READY records all exact source, data, qualification, weight and command hashes.

If ordinal matches matching-ID and beats constant, revise toward positional
bookkeeping. A matching-ID advantage across groups motivates source correspondence
beyond an ordinal cue. Neither outcome proves an attention mechanism; instructions,
prefixes and forced token content differ. These are exposed developmental component
readouts, not a new untouched test or an end-to-end RLM improvement.
