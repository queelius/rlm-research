# Smallest useful refill-RL adaptation in the remaining allocation

Design refinement only; no implementation, GPU activity or new high-LR/readout outcome inspection. The high-LR audit method was sealed first (ae4b36b98189cf39f86a572715be62d87d40c4c949e8a652edb1baf1194f858a). I read the complete bounded-refill options and the executed adaptive coordinator/export/trainer/start-binding sources.

**Recommend a two-window feasibility pilot of the memo's adaptive2→4-group procedure, not a promise of four successful updates.** Maximum64 training attempts +48 paired before/after readouts =112 episodes; at most two actual optimizer steps. This retains the key new question—does a bounded refill supply mixed support when the initial two prompts do not?—and reduces the original176-episode/45-minute proposal's completion risk. It can fit as one bounded follow-on within the remaining four hours without consuming the entire allocation. Actual completion is still conditional on the shared cap.

The competing smallest implementation is four fixed two-group windows/no-op (64 training attempts, at most four updates). That is simpler and may yield more updates, but it does **not** test refill. Keep the original four-window adaptive maximum128-training/176-total option for a separately budgeted follow-up if the two-window pilot demonstrates usable support. Do not extend a live pilot after favorable results.

## Fixed experiment proposed for approval

Start exact existing low-LR success-SFT fixed8 adapter66cce4009628ff7d8e047f08e6fb3a29febdbf8fb3ed8303871f69860f1151d5, selected for availability. Fresh RL Adam0, fixed c32 child, unchanged native typed interface and root-only capped-token TIS/PPO loss/guards. Do not import SFT Adam or substitute high-LR/other weights based on its unfolding evaluation. No reward, grammar, temperature, admission or task changes.

Use the existing source-disjoint896 allocation, train partition only. Prospectively hash-order the four32-record and four64-record training contexts and select two from each: each window has one32 and one64 context, both global and single-user questions. Mandatory first pair is length-balanced; alternate family assignment across windows, with the complementary pair in a fixed refill order. Eight distinct context×family groups total, each visited at most once. Freeze all64 candidate rollouts before sampling. Skipped groups stay skipped; no retry/replacement or reward-selected new prompt.

Within an unchanged policy, collect two eight-episode groups. If fewer than two are mixed among admitted binary outcomes, collect group3 and, if still needed, group4. At the bound, train on all admitted episodes from all mixed groups, even if only one group is mixed. With no mixed group, record a genuine no-op and advance the window. No Adam step, moment decay, checkpoint fabrication or repeat of that window. A group with infrastructure NULLs may still be mixed among its admitted episodes under the unchanged rule; no new minimum-admitted threshold. A capped/incomplete or integrity-failed group stops this window/procedure rather than inviting more samples or partial-window training.

Use all24 existing development readout coordinates before and after, fresh paired seeds, with8 validation/8 query/8 length and context-group reporting. The endpoint is the last committed policy after exactly two candidate windows, actual step0–2; never label it promised step2 or validation-selected. A no-update run can measure trajectory variability but cannot claim learning. Report support-yield rescue, actual update/weight/gradient evidence and costs before paired accuracy. The first-two-versus-refilled support comparison is descriptive, not a causal comparison of trained algorithms.

Proposed namespace981316001 master,981316002 optimizer,981316101–164 candidate rollouts,981316501–524 paired readouts. No matching integers in a bounded78-file SPEC/RECIPE/CAMPAIGN/named-plan/seed metadata scan; freeze the exact scan at preparation. The memo's proposed981312001 is **already used by accepted coverage48**, so it must not be reused.

Retain2700 owned/2730 outer, one immutable start clock;120 cleanup and final660s service/readout reservation. Stop starting training work by elapsed1920s; clip every collection/update to that same absolute cutoff and reserve checkpoint/release time. Initial24 readout remains capped within660s. Do not promise all maxima; preserve unrun final cells as NULL if the budget expires. An explicit larger budget is a different MAIN decision, not a timer extension.

## Exact seams that need new code, and what does not

1. `study.authenticate_start` currently supports only historical473210 or interface-SFTfinal4. Add an exact fixed-success-SFT8 RESULT/SELECTION/state/member closure and fresh-policy wrapper—no generic different-weights bypass.
2. `coordinator.stage` currently raises on no mixed group, and its loop/schedule uses the actual optimizer step as the round index. Introduce separate immutable candidate-window and committed-update cursors, no-op records, and consumed-prefix metadata. Otherwise a no-op would repeat or stall a window. Output checkpoint naming/Adam continuity must remain based on actual updates.
3. `train.authenticate_group` hardcodes16 attempts/two prompts after adapting the campaign's32 check. Authenticate a consumed prefix of16/24/32 predeclared same-policy attempts from two/three/four complete groups. The generation identity must bind the entire candidate window plus immutable consumed-prefix exports, while actual optimizer index stays step+1. Do not concatenate pre-normalized gradients or reuse prior-policy episodes.
4. Reuse `export.outcome`, strict native reconstruction and mixed-group advantages, with a small union-manifest verifier that authenticates each complete subgroup once. Keep native-integrity failures fatal. Retain equal-episode→equal-root-turn→mean-action-token weighting: varying admitted group size changes effective group mass and must be reported.

The qualified model loading, actual loss, correction guards, Adam/save/RNG code, rootless native collector and owned service lifecycle need no redesign. The minimal credible qualification is a two-window CPU fixture with first-window no-op and second-window refill/update, plus stale-generation/double-step rejection and variable16/24/32 admission checks. Synthetic fixture likelihood is never scientific RL data. This is a small new coordinator/admission adapter, not an unchanged invocation; MAIN approval is required before implementation.

Read-only source fingerprints: adaptive coordinator c60bd59c5982bac21f37bcee6ce403cbcf69b0145356479fe43334ced659a949; train a8a0b04e4326d1fc79827eea77d77162e7866f2518a503d9981622fcf078ebae; export ba8e9d6e763238ee287db699428155d3e9ded6070cf2daeceda749bc0694d551; study df4e519f7e2fc932e2ae136fdcdada5896391330b67d9d4fc46f0e71d22822a6; common42ed9ccad6580a5f421609fcc24677778974abbe5e616fccc6f59a9f395652b1. Existing approved studies and methods remain unchanged.
