# Coverage-first summaries × root training: bounded next comparison

Status: design proposal for MAIN approval; no implementation or GPU action. The ledger16 independent report is sealed (REPORT b79913417b991037aa9fc4c09515601aa67682f2d2edd11400cd7fe02cf3dd36). It found3/8→0/8 strict success, identical first requests/actions in all eight pairs, and eight ledger runs ending after four records. Six strict answers equaled the visibly partial total. This motivates a prospective presentation/plan test, not a claim that a coverage-first policy already works.

## Options and recommendation

| Option | Episodes | Information / limitation |
|---|---:|---|
| **Recommended: two roots × three summary policies** |48|Tests partial-count visibility and whether complete-success imitation changes the response, with a contemporaneous map-only control for both roots. Two ordinary service phases; no new routing framework.|
| efab only × three policies |24|Fastest direct presentation probe, but leaves the training interaction unresolved and likely needs another run.|
| Two roots × always-counts/coverage-first only |32|Tests the main histogram contrast, but cannot distinguish helpful coverage metadata from harm caused by counts relative to ordinary map-only behavior under the new root.|

Use the48-episode option. It is a narrow collector/summary projection adaptation of root-accumulation-ledger-v1, not a new general framework, training run, answer repair or completion gate.

## Frozen factorial

Reuse **exactly** ledger16's four exposed global contexts and task/environment bytes: validation-00 NUM32, validation-01 HUM32, validation-02 ENTY64, validation-03 LOC64. These are already inspected, leaf-training-supported source groups, not new task evidence. Copy/authenticate the old public/task/host-gold inputs unchanged; gold stays host-side. Two new seeds per context give eight matched coordinates per cell,48 episodes total; four context clusters are the analysis units.

Fixed roots: (1) efab interface-SFT final4, SHA efab2913e7fe9f5f9b381ae6eb67bb56071070f86b237e6145f98654816aad64; (2) **fixed final8** complete-success SFT, SHA66cce4009628ff7d8e047f08e6fb3a29febdbf8fb3ed8303871f69860f1151d5. The latter was chosen by completed-checkpoint availability, not readout performance. Only training identity/state/member hashes were inspected for this decision; no new SFT readout outcomes were read. Both use the identical frozen c32 child and base/model/native renderer.

All three arms share the exact source-bound delivered-map tracker, unique-ID deduplication and permanent conflict handling:

- `map`: unchanged original tool observation; no appended summary.
- `always_counts`: the existing ledger suffix, including per-class histogram after any eligible delivered map.
- `coverage_first`: the same header and state, but omit the entire `counts` field while any source ID is unqueried/unresolved or conflicting. Show only unique_seen/resolved/source_records/unqueried/conflicts/partial. Once every source ID is resolved without conflicts, expose the **byte-identical always-counts summary**. The omission applies only to the added harness summary: do not redact original child maps, model-produced counts or other original tool output.

No class totals, zero-valued class bins, target category, gold or expected answer enter the partial coverage-only projection. No forced further call, termination prohibition, response fallback, warning-policy paragraph or reward change. Preserve the existing20,000-byte original-tool truncation **before** a separately bounded≤2048-character suffix; do not reserve suffix space by extra truncation. All arms can still stop incorrectly at any time.

## Seeds, order and clocks

Proposed master981312001; sampling981312011/981312021, identical across all six arms for a context/repeat. A bounded scan of81 existing top-level SPEC/RECIPE/CAMPAIGN and named inputs/prepared PLAN/SEED_AUDIT JSON files found zero matching integer values. This is a named metadata-namespace check, not a claim of exhaustive historical uniqueness; freeze its exact file/hash manifest with preparation.

Two qualified sequential root-weight service phases, fixed by SHA256(`981312001:<root-name>`): `success_sft8` then `efab`. Do not implement new multi-root interleaving. Service/order effects remain a confound for between-weight contrasts. Within each phase, each context×seed dispatches a sequential three-arm block; rotate the arm order by block index, yielding2–3 occurrences in each position, and use the same fixed block/rotation order across roots. Four block workers. Freeze all48 coordinates and first native prompt IDs before inference; check within-weight arm first-request equality and report any pretreatment sampled-action divergence as scheduling/randomness nuisance.

Preserve inherited per-episode/runtime budgets, T.5/full support, native Train Qwen3 thinking renderer, max2048 completion per call, typed child-only grammar, no retries and actual root/child aliases. Proposed caps:900s collection per root phase,2550s shared work,2670s owned including cleanup,2700s parent process envelope. Start the shared clock before verification/startup; each phase deadline is min(shared work deadline, collection start+900). The first release consumes shared work time; reserve120s globally for final cleanup. Record failures/unrun/missing separately and keep already complete outcomes. No silent grace, rollover or automatic retry. Typical ledger16 timing suggests a few minutes per phase, but richer root plans may cost more; the caps, not that estimate, bind execution.

## Primary question and audit

Primary: paired strict Answer:N **coverage_first minus always_counts within each root**, preserving all8 planned coordinates/cell and NULL separately. Report gains/losses and four context summaries. Main interaction: difference of those within-root contrasts; exploratory because root phases are ordered services. Map contrasts anchor whether counts harm or coverage-only metadata helps relative to the unchanged observation. A trained-root advantage is a training-package comparison, not equal-compute or objective-only attribution.

Secondary: source/native-corroborated delivered coverage; continuation beyond the first helper map; correct global count versus partial-total final; format; logical/cached/action tokens and all captured attempts. Verify actual appended suffix bytes on official physical branches and unsampled observation masks. Do not trust writable audit logs alone or infer Python state use from visibility. Preserve conflicting/ambiguous map and semantic diagnostics as null where unsupported. No repair or posthoc exclusion of bad plans.

Falsifiable outcomes: if coverage-first improves completion and strict success relative to always-counts, especially from identical initial behavior, partial-total presentation becomes a stronger explanation. If it merely produces more calls without correct answers, or both summary arms fail like map-only, retire the simple fix claim. If root training changes the contrast, describe policy–presentation interaction, not a universal arithmetic or attention mechanism.

## Small implementation boundary after approval

New owned sidecar only. Reuse authenticated native collector, rootless overlay, broker seam, source-bound tracker and owned service lifecycle. Add a narrowly tested summary projection/three-arm validator and two fixed weight bindings; old files/images/outputs unchanged. Reuse the sealed ledger audit's source-order correction rather than its erroneous V1 suffix projection. Focused checks: count suppression until full conflict-free coverage, identical full summary bytes, duplicate/conflict persistence, all48 task/seed/arm crosswalks, first-prefix identity, and one tiny fake-provider/rootless observation proof—never synthetic likelihood as training evidence. Publish READY last for MAIN source review/launch.

Fixed SFT8 closure checked on CPU: training/RESULT d3279d748b618e0b50103e066c2680e1d3b59c1a13a37c36243d58812611a4a0; checkpoint-0008/state506fc355e19b3201822ee1f6fb88eca1edd23e04219787b1d84484598f9531f2; adapter_config118bb737297c35d96a194b642026b1e58c72f945d55ae1616eb716cfc4601728. Path: sidecars/root-success-trajectory-sft-v1/outputs/attempt-001/training/checkpoint-0008. Final checkpoint rule is fixed8; do not substitute another checkpoint based on ongoing analysis.
