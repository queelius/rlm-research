# Add a fixed higher-rate SFT policy to the bridge panel

MAIN, September9,2026, before either new policy bridge launches. The original
low-SFT bridge's independently projected checksum result is0/8 in both formats;
count1/8 in both. Its full audit is ongoing. This motivates a policy-dependence
check with stronger available controllers, not a claim that IDs improve RLMs.

In addition to the already approved last boundedRL4 policy, authorize an additive
bridge with the fixed higher-rate SFT8 adapter
0ba42364183a311a8f5b67e4bac4e9294924c1dfb73f152a209811df09b78773.
Keep all32 original coordinates/seeds, c32 child, array/map treatment, actual
root interface, metrics and1800outer/1650work/1500collection/120cleanup bounds.
Both new checkpoint choices are frozen before their bridge outcomes. This forms
an exploratory three-policy by two-child-format panel on four exposed contexts,
not independent seeds or a randomized comparison of training algorithms.

Use additive tiny authenticated loaders and immutable outputs, identical native
role routing checks, and report actual model bindings despite stale aliases.
The higher-rate policy job is a useful ready backup while the new-panel complete
demonstration SFT prepares; it must not delay an already ready more informative
training comparison. MAIN owns exact launch acceptance and scheduling.
