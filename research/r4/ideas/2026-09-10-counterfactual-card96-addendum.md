# Additive proposal: counterfactual × procedural-card96

**Design only; implementation/launch require MAIN approval.** Preserve the original counterfactual48 proposal and completed card48 seals. Card48's full practical promotion gate failed because availability fell16 versus18; four faithful correct and two faithful wrong computations motivate this new diagnostic, not a confirmed net gain.

**Offline gate passed.** Frozen [GATE.json](../../../ARTIFACTS.md#unpublished-files "Not published: ../analyses/root-counterfactual-operator-gate-2026-09-10/GATE.json"), SHA `ce10e939b210bcc11c14d47cdf27a2ab57799146f9c1670f503bb006b9c16880`, evaluates all528 predetermined candidates across all24 blocks. All24 selected counterfactuals—and therefore all24 two-answer signatures—differ from all four fixed confusers; requirement was≥16. Originals separated all four on7/24. Independently expressed host formulas checked all24 selected answers. Weights are label-blind hashes; query selection is explicitly gold-conditioned. Six counterfactual zeros versus four originals remain. Sixteen blocks retain original A/B categories. This is an exposed, targeted diagnostic; no model outcomes entered candidate ranking.

**Exact comparison.** Eight existing contexts ×three composed questions ×original/counterfactual ×U/P =96 new native endpoints. Use exact fixed24/c32, not RL-trained weights. All four cells are contemporaneous; old outcomes are motivation, never new controls. P appends the same183-token card; U remains the original native contract. A single fresh paired sampling seed per block is shared across four cells. Hash-fixed block order and balanced four-cell rotations; no rerolls or card tuning.

Freeze the chosen GATE rows without reselection. Original tasks preserve all ancestor query/file bytes. Counterfactual tasks consistently update weights in both records.json and mirrored context.txt, and update only category/threshold parameters in the same three unambiguous question templates. U/P have identical files and query within each variant. Texts, public IDs, owners/order and all-scope stay fixed. No gold map, helper, algorithm beyond the P card, forced batching/children, root grammar or process feedback.

**Primary readout.** Strict native correctness/all24 per cell; card P−U within each variant, plus both-original-and-counterfactual-correct/all24 signatures per card arm. Report the interaction descriptively, with eight-context means, availability/NULL bounds, zero/nonzero strata and physical usage. Zero/best-constant references: original4/24 and6/24; counterfactual6/24 and6/24; best fixed constant passing both tasks1/24 (ties0,2,6,9). A finite signature rejects specified substitutes, not all wrong algorithms. Actual child predictions may differ; faithfulness and child-semantic errors require separate manual review of every available final, not repaired primary scores.

**Resource/decision.** One A100,3600s outer/3450 work/3570 owned:180 startup,3240 collection,30 harvest,120 cleanup,30 margin;4 workers,180s/episode,2048/8192,.5/top_p1, no retries. Expected20–35min from the684s card48 run, not guaranteed;96 worst-case timeouts exceed collection cap, so all planned NULLs/costs remain. No per-cell early-success stopping. A useful follow-up signal requires P's positive counterfactual strict difference and ≥4 additional verified requested computations across≥3 contexts without availability loss; otherwise no practical promotion. Retain weaker observational findings separately. No primitive-preservation, unseen-transfer or learned-composition claim. Pair-reward RLVR remains a separate unapproved proposal; no changes to warm/card/accumulation studies.
