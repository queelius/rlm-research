# Root warm-start reference: bounded feasibility, 2026-09-09

Decision requested: select a matched checkpoint screen before more SFT or larger weights. No implementation, READY, service, GPU, queue change or new acquisition occurred. The accompanying JSON records exact read depth, source/artifact hashes and the proposed 64 source group IDs. Brainstorming was used as a design-only feasibility spike.

## Finding: use the existing zero-effect reference

The exact `857a7ce6…` adapter at `sidecars/single-gpu-self-sft-control-v1/inputs/step0-peft-key-conversion-v2` is a **released-weight reference served through a zero-effect adapter**, not a claimed bare-base transport experiment. Independent CPU inspection of every tensor found 252 finite FP32 A matrices and 252 finite FP32 B matrices; all 8,847,360 B elements are exactly zero. All 504 keys are the expected seven projection pairs across 36 layers: no bias, saved-module, embedding, magnitude-vector or other parameter tensors. Configuration has rank8, alpha16 (finite scale2), dropout0, bias `none`, modules_to_save null; no DoRA or rsLoRA. The installed vLLM PEFTHelper defaults both variants false and uses alpha/r. Thus its mathematical inference update is `(16/8) BA = 0`; casting zero B to BF16 preserves zero. This does not promise bitwise equality between different inference kernel paths.

The configuration and qualified service name the same released Qwen3-4B-Instruct-2507 revision `cdbee75f…`, manifest `19619b44…`; the native renderer uses that same base directory/tokenizer. The conversion receipt traces `857…` to Prime broadcast step0 `e5be32…` by key-prefix conversion only, with tensor values unchanged. No adapter tokenizer or extra weights are substituted. Native role/typed-child overlays alter the common harness, not these weights. Keep these exact common overlays, sampling and runtime for every arm. No GPU base-alias comparison is necessary for this labeled weight reference; a bare-base transport claim would require a separately charged qualification and is not proposed.

## Actual ancestry and what can be identified

`released base + zero-effect857 → root RL8 473210 → interface SFT4 efab → success SFT8 66c`.

The RL8 ancestor is the original root-rlvr campaign's continued checkpoint, including the explicit recovered-child exclusion amendment; it is not a released-weight checkpoint. Actual RESULT identifies efab's start as `473210b1…`, four fixed updates, fresh Adam, 64 row exposures and 3,206 target-token exposures. Its 32 authored examples teach a first acquisition/interface action and metadata-derived terminal format; they do not teach complete semantic reduction. Actual RESULT identifies 66c's start as `efab2913…`, eight fixed full-corpus updates, fresh Adam, 216 episode/912 root-turn/122,048 target-token exposures. Its immutable corpus is 27 successful count trajectories from adaptive-RLVR training rounds1–5: 114 turns, 15,256 targets, seven contexts/eight context-query pairs (10 global,17 single-user), LR2e-5, mean episode→turn→action-token CE. The child was neither loaded nor updated in either SFT stage.

Crucially, adaptive RL trajectories are a **data source**, not an additional optimizer ancestry edge from adaptive RL7 into 66c. Neither the later high-LR success-SFT checkpoint nor bounded RL/joint SFT is QSR's frozen initial66c. Efab→66c identifies the last success-distillation package; reference→efab bundles the earlier RL plus authored interface SFT. None isolates interface SFT alone.

## Default: three roots, 24 fresh native endpoints

Run exact zero-effect857, efab fixed4 and low66c fixed8, each on eight paired blocks: QSR `readout-00`–`readout-03`, count-union on all four, plus distinct-all on even context indices and weight-single on odd indices. One new paired sampling seed/block, three roots =24. This covers all three held-out operator/scope families and four target categories, but only four context clusters; count receives four blocks, other operators two each. Do not select blocks by gold, old success, answer size or CE outcome.

Use the current QSR native role, **not compact role**, prospectively. Exact file-only public records, IDs (`q`+12hex), users, texts, weights, categories, optional batch_contract, child c32 and optional native tools remain common. No supplied map, new algorithm example, forced batching, root grammar, aggregation helper or mandatory child call. Preserve the typed exact-ID grammar only for recognized optional child batches. For every all-scope question replace only “Among records belonging to all four users,” with “Across all records, regardless of which of u0, u1, u2, or u3 owns the record,”. Other query bytes and gold remain unchanged. This common wording amendment precludes treating historical QSR outcomes as matched controls.

Suggested fresh namespace `root-warmstart-reference-v1`, master981456001, paired seeds981456101–108, subject to bounded collision check before freezing. Freeze phase order by hash and record it as a serial-service confound; use the same predeclared balanced block order and exact first native prefix across roots. Four workers, 120s/episode, explicit2048/action,8192context,.5 temperature/full-support QSR sampling. Outer1800/work1650/owned1770,120 final cleanup/30 margin. Three serial phases each have480s inclusive startup+collection (startup≤180, collection≤300 and remaining phase); allow≤90s per release within shared clock. The worst nominated sequence is1710s. All24 slots exist as NULL before startup; no reroll or deadline extension. Runtime sufficiency is a forecast, not guaranteed availability.

Source boundary: these 64 groups are unique and disjoint from QSR's pinned prior-root exclusion list; all are c32-training-exposed. They are already QSR readout/prepared and adaptively exposed to this research, not new confirmatory data. Pin PUBLIC/QUERIES/GROUPS/PROVENANCE/HOST_GOLD/PLANS and exact records/query bytes; the JSON carries those hashes and group IDs. Gold stays host-only. MAIN's reported unchanged2/48 (46 available, zero child tokens; one zero answer and one ambiguous-all success) motivates the question but is not pooled, rescored or used to infer a QSR RL effect. Trained final was unavailable when this task was assigned; I inspected no new final responses or CE outcomes.

Alternative: two roots857/66c × all four original queries in each same four contexts =32 endpoints, one paired seed/block. This gives better operator/support breadth but cannot localize the final success-SFT increment. Two inclusive690s startup+collection phases plus90s releases fit the same envelope. Prefer24 because efab is the decisive intermediate, not another selected checkpoint.

## Exact reusable seams and decision rule

Use unchanged `leaf-role-routing-v1/source/serve.py`, an27 `service_wrapper_v2.py`/lifecycle_adapter/private credential preflight and its standard two-LoRA root+c32 binding. No base-or-adapter protocol is needed. Bind each root as an ordinary existing adapter; preserve routing.py's trusted depth metadata/model-only rewrite and QSR typed hook. Reuse `qsr_native.py` task/files/native prefix, `qsr_collect.py` composed collector/dispatch and native authentication, with isolated names and a small explicit fixed-policy binding/spec/plan owner. Do not import campaign `binding_for`/policy authentication as though arbitrary fixed roots were new RL generations. CPU qualification must cover actual composed entry, descriptor/live aliases, unchanged root/child request bodies, explicit sampling, null inventory and all three first-prefix fixtures. No installed/public source edits.

Primary: strict authenticated whole `Answer: N`, operational planned-denominator successes, separate final availability and NULL bounds; paired gains/losses and mean context differences. Secondary: actual file access, child calls/coverage, executed scoped reduction and observed result→final agreement, wrong constant/format/loop failures. No code-mention mechanism credit or sampled-code reexecution. Charge all physical attempts, returned completions, known/unknown usage and wall time; billing unknown.

If efab exceeds66c across at least three contexts with at least two net paired successes and verified nontrivial state use, prioritize retiring/revising narrow success distillation. If reference exceeds both under the same criterion, test accumulated adaptation/initialization before buying weights. These are exploratory promotion triggers, not significance thresholds. Uniform failure does not establish a 4B ceiling: use the independent CE evidence to separate role/state availability from operator failure. Role changes or additional SFT remain separate prospective experiments.
