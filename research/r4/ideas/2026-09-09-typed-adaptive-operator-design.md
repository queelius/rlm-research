# Typed operator planning pilot:24 executions,32 references

2026-09-09. Parent authorized this new CPU preparation while typed36 runs. No changes to frozen studies; main alone accepts/launches. Question: after constraining the child response contract, can the existing metadata-first operator plan reduce child calls/tokens while retaining final count accuracy?

Reuse exactly adaptive-filter-pilot-v1's four now-exposed128-record contexts, synthetic users, global/user queries, record order, definitions, strict decoder, operator program and owned operator→IPython→broker→native child seam. No free-root arm and no newly generated contexts. Two fresh seeds981292801/981292802, checked before inference. Three executions per context/seed: user_all(all16), user_filter(filter16), global_shared(all16). Thus24 physical executions,32 logical method references: each of eight global executions supports both all16 and filter16 because the global query makes their deterministic actions identical. Never count those as independent executions or charge cost twice. Alternate six three-job orders plus two prespecified extra orders; record the small8-block imbalance.

All16 selects all128 records and classifies8 batches16; filter16 selects the query user's8 records and classifies one batch8. Both then aggregate the same requested user subset. Global selects all128 and classifies8 batches16. If complete, planned child sessions are64 user-all +8 user-filter +64 shared-global =136, with zero root model calls. Source IDs and batch order are observable and fixed. Matching batches have identical request bytes, native renderer/sampling/child weights, but seeded concurrent sampling need not be deterministic. All-versus-filter semantic inputs legitimately differ. This is an operator-policy/cost comparison, not a learned planning claim or isolated routing mechanism claim.

Apply exact ordered ID→six-label JSON grammar only to trusted depth1 child sessions whose initial user request round-trips through unchanged `batch_contract.request_for` against the actual public catalog. Derive schema from requested IDs and fixed labels only, no gold. Reuse typed36's qualified native sampling hook privately, substituting only this public contract matcher and the already qualified adaptive operator setup overlay. Bind one immutable decision per invocation before dispatch. Root remains unsampled operator execution, never synthetic model actions/logprobs; child data receive no training credit. Ordinary tool schemas remain present, but the grammar constrains effective child action space, suppressing tool-call responses and enforcing requested key order. Syntax constraints do not guarantee semantic correctness or eliminate caps/nulls.

Same fixed c32de child; historical step8 root alias remains bound only for unchanged service/role plumbing, with expected zero root model requests. Same qualified private image8cfe… symmetrically for every operator. No rebuild, shared-store mutation, new retries, replacement episodes, answer repair, fallback grammar or outcome-selected work. Preserve inherited transient retries and raw malformed/truncated/error evidence. Exact dependency pins and launch argv will be in the new sidecar READY.

Primary comparison: user_filter minus user_all strict final accuracy, with paired task/context/seed outcomes and planned/null denominators. Cost comparison reports actual child native input/completion tokens, sessions/model turns/retries and whole execution clocks, separately for paired-complete successes and all planned work. An early-failing cheap run is not an efficiency win. Global is a shared calibration, not a second independent estimate. Preserve original operator transcript, source selection/query parsing, returned-order/map validity, counts, physical requests/aliases/token IDs and ancestry. Incomplete/unanswered/operator-protocol failures retain the adaptive pilot's null contract; no conversion into typed36's different scored-empty task contract.

Known caveats: user answers2/1/2/2 mean constant2 gets6/8 per repeated-seed user method; global16/18/22/20 provides variation. No reselection. The Python operator loads the entire records.json including all texts before metadata filtering: this saves **child classification input/calls**, not host file reads or payload-loading work. All runtime inputs are public, gold host-only. Proposed bounds: startup180, cumulative collection1440, work1680, inclusive owned1800, outer1830,120 cleanup reserve; four triple workers, one shared absolute clock with no phase reset.

CPU scope: focused exact matcher/schema/ordered plan/null tests and one bounded real owned operator→broker→native fake-provider fixture for all/filter, demonstrating zero root calls, changed selected IDs, actual nested grammar and valid aggregate. Parent requested rapid readiness; reuse prior cancellation/lifecycle and source qualification, no broad suite or framework. A protocol/accuracy improvement supports further co-adaptation study; cost reduction without semantic accuracy or with large null loss does not establish useful planning.
