# Decision proposal: exact20-context question-sensitive SFT72

Design only, 2026-09-10. No allocation, generation, implementation, model calls or changes to live studies. Extends the fully read training-options note SHA `444ad44d9f6cf020893e9ba5252daf24b385848fe9b7ef7a3016a89b8b65f945`. Recommend its default new72 complete-transition curriculum from exact fixed24, conditional on MAIN's warm-readout decision; no best warm checkpoint substitution. The current counterfactual96 method is separately frozen and its outcomes remain unread.

## Exact allocation rule, not an allocation receipt

After approval, refresh the named actual/accepted root selections against the scale study's pinned2000-group pool, using its source loader and positive-group inventory, including every newly accepted PUBLIC/GROUPS/TASKS manifest. Broad candidate/pre-render presence alone is not root execution, but selected/prepared root tasks are excluded. Retain original named exposure exclusions and actual broad-task selections. The prior note's976 remaining is a feasibility count, not freshly verified here. Require at least320 eligible normalized groups, no duplicate/overlapping group, and record every admitted/excluded group plus source revision/path/line/license and inventory SHA. Do not allocate now.

Proposed namespace `question-sensitive-sft72-20260910-v1`, collision-checked before allocation. Define H as SHA256 of UTF-8 compact JSON (`sort_keys=True`, separators comma/colon). Rank eligible groups by `(H([namespace,"source",group_id]),group_id)`, take exactly first320 once. Rank positions0–127 form train00–07 in consecutive16 blocks;128–191 form dev00–03;192–319 form protected00–07. Within each block order by `(H([namespace,"order",split,j,group_id]),group_id)`. Record i gets user `u{i%4}`, weight `1+int(H([namespace,"weight",group_id])[:8],16)%7`, public ID `q` plus first12 hex of H([namespace,"record",group_id]). Preserve full source text. Check IDs/native-context IDs against named prepared inventory; any collision or prefix-budget failure stops for amendment, not substitution/cropping/reranking. Freeze generated facts/specs before any child/model call or gold-baseline review. All320 are TREC train/c32-training/catalog exposed: protected is new-process withholding, not whole-system unseen text.

## Exact parameter/operator/scope table

Let label cycle L be `[abbreviation, description and abstract concept, entity, human being, location, numeric value]`; indices modulo6. For local context j (train/protected0–7, dev0–3), r=j mod6. Scopes: ALL={u0,u1,u2,u3}; EVEN={u0,u2}; ODD={u1,u3}; ADJ={u0,u1}. Categories/weights/assignment are independent of actual labels. The eight-context cycle repeats r0/r1 twice; disclose this fixed unequal category frequency, never rebalance using outcomes.

| Slot | Requested operator | Training parameters | Protected parameters |
|---|---|---|---|
| T1 | Count scoped users whose A-weight total >t | A=L[r], ALL, t=2 | A=L[r], ALL, t=4 |
| T2 | Same | A=L[r], ALL, t=6 | A=L[r], ALL, t=8 |
| M1 | Maximum scoped per-user A-weight total | A=L[r+1], EVEN | A=L[r+1], ALL |
| M2 | Same | A=L[r+1], ODD | A=L[r+1], EVEN |
| J1 | B-weight for scoped users with ≥1 A record | A=L[r+2], B=L[r+3], EVEN | A=L[r+3], B=L[r+2], ALL |
| J2 | Same | A=L[r+2], B=L[r+3], ODD | A=L[r+3], B=L[r+2], EVEN |
| P1 | Count A records in scope | A=L[r+4], ADJ | Same |
| P2 | Distinct scoped users with ≥1 A | A=L[r+5], ODD | Same |
| P3 | Sum scoped A-record weights | A=L[r], ALL | Same |

T1/T2 alter threshold only, M1/M2 scope only, J1/J2 scope only; records/order stay identical within context. Protected max-EVEN is a seen operator/scope control on new inputs; max-ALL is withheld scope recombination. Conditional training pairs are clockwise adjacent labels; protected pairs reverse them (never a training pair), with EVEN preserving trained scope and ALL adding withheld scope. Threshold4/8 is withheld numerical interpolation/extrapolation within the familiar comparison operator; not an unseen operator. Every category and all four scopes occur elsewhere in training. Predeclare this exact withheld-cell inventory; do not claim it isolates all individual factors—conditional ALL jointly changes pair and scope relative to training.

Use truthful native questions, not a procedural card: state category, user scope and quantity explicitly. ALL wording is “across all records, regardless of which of u0, u1, u2, or u3 owns the record.” For subsets: “consider only records owned by [literal user list].” Threshold is strictly greater than t; maximum includes zero for a scoped user with no A; conditional B records count once, with both A qualification and B sum restricted to the named users. No operator algorithm, map, formula or solution is added to the task. Public files remain records.json/context.txt/query.txt/batch_contract.py. Code-layout/variable-name choices use a separately hashed style key, independent of parameter-table selection, with style contingencies reported rather than claimed perfectly balanced.

Development constructs the same nine training-column specs on its four disjoint contexts, but only M1/M2 are executed after training:4×2×2policies=16 endpoints. This narrow interpolation/stopping diagnostic is not a checkpoint selector or a composition-wide validation set. All protected9/context are mandatory:8×9×2policies=144 endpoints, one fresh paired seed/task, with exact fixed24 and fixed new-six. Freeze all160 slots and proposed seed namespace `[namespace,"readout",split,j,slot]` (collision-checked integer mapping before READY); no allocation/policy selection using dev outcomes. Train72=8×9 with one genuine acquisition trajectory each. No extra dev tasks quietly enter the cost denominator.

## Learning, measurement and cap

Keep note's fixed24 SHA94022838… starting adapter, frozen c32, fresh Adam0, six global updates, unchanged lr1e-4/clip1, .45 producer/.50 reduction/.05 stop masses; save all504 Adam states/RNG after each update. These are six *additional* passes over a new corpus, not a continuation of old Adam or a matched-dose causal comparison. Capture72 separate actual c32 maps through native contexts; authored acquisition/reduction/terminal uses returned labels and actual observed scalar, never gold or repaired labels. Structural capture failure stops admission with all attempts retained, no retries/success filtering. Root action-only loss excludes prompts/observations/children; nonliteral diagnostics additionally exclude category/user/threshold/output literals. Predetermined forward gate trajectories: train00/T1, train01/M1, train02/J1, train03/P1, train04/P2, train05/P3. This is cost/validity screening, not efficacy selection.

Independent oracle fixtures must cover > versus >=, absent target/max0, repeated A and B counting once, scope exclusion and reversed roles. Freeze gold baselines for all specs including zeros and shortcut coincidences; do not improve separation by resampling. Native strict primary planned denominators, NULL/operational bounds and eight protected context clusters; primitive24/policy versus composed48/policy reported separately. Actual executed operation/scope on observed predictions is mandatory for all available outcomes, not just scalar agreement. Paired threshold/scope signatures are secondary targeted diagnostics, with unequal requested-versus-confuser gold strata disclosed. Co-primary process evidence is not privileged input/reward.

Keep proposed8100outer/8070owned/7920work: capture1200, load/gate/train2100, dev300, protected3900, finalization420; cleanup150, margin30. All stage startup/release overhead is charged, stages advance early, protected reserve cannot be borrowed for extra gradients. No fixed5 substitute if6 incomplete. This is new-curriculum package versus unchanged24; primitive anchors permit retention measurement, not guaranteed retention. A positive strict/faithful composition difference across ≥3 protected contexts with no native-availability loss and no negative primitive operational difference supports a later matched-curriculum ablation; failure of those practical gates is not promotion. Faithful reductions dominated by c32 errors motivate a separate leaf question; continued wrong-operation output despite teacher fit motivates distribution/recovery work, not blind identical extra passes.

Read depth: full444ad44d note; full current scale prepare.py and ss_study.py; scale PROVENANCE SHA3fa3b5d6517994cf7140befd8aa899dfe71a400e5ec824be982f6fcd13a03d1a verified. No new source allocation or broad inventory refresh was performed. This proposal imports no new primary-paper claim. Brainstorming skill keeps the concrete table at MAIN's approval boundary.
