# Sparse cue order96 — bounded proposed preparation plan

Design-only, awaiting MAIN decision. Proposed namespace `sidecars/leaf-sparse-cue-order-v1`; no directory, code, inputs, or READY has been created there.

1. Import pinned sparse study and cue-order logic privately. Freeze the exact sparse12-context data/record order and fixed c32de/base hashes. Remove cadence variation by retaining only4. Add assigned field order to each coordinate. Keep all historical source files unchanged.
2. Build all96 coordinates/requests before model outcomes, with master981304001 and sampling seeds981304011/021 after bounded collision recheck. Four conditions A=matching/tag-first, B=constant/tag-first, C=matching/label-first, D=constant/label-first. Use a master-seeded shuffle of the12 context work units; within each context run both seed units, reversing seed order by shuffled-context parity. Within a context/seed quadruple use one of four balanced sequences: A-B-D-C, B-C-A-D, C-D-B-A, D-A-C-B. Assign sequence index `(original_context_index + 2*repeat) mod4`. Each task consequently has each sequence twice across its eight quadruples, each condition twice in each serial position, and balanced directed adjacent condition pairs. Freeze actual dispatch order, not merely randomization intent.
3. Preserve one quadruple as a bounded dispatch work unit with sequential calls inside it and at most four concurrent units; use the qualified collector/native request path. If inherited grouping is literally two rows, a pinned/count-asserted private grouping-only adapter is acceptable to propose at implementation, with exact dispatched coverage/order test. Do not build a new scheduler or change episode retry policy.
4. Common order-neutral prompt phrase in all cells; exact grammar property/required insertion order alone assigns field order. Qualify96 schemas, at most48 unique schemas after seed deduplication, with existing CPU xgrammar/typed renderer. Prove complete schema accepts assigned raw order and rejects reversed order, duplicate/missing/extra keys, wrong tag, noncanonical labels, cardinality errors, and a nonanchor object. Preserve observed grammar/compiler failures rather than silently weakening constraints.
5. Prove all48 order pairs have byte-identical messages/tools/sampling and full typed prompt IDs; every matching/constant pair has identical public input bytes, source order and sampling. Canonical and insertion-order request hashes are separate; freeze actual serialized-body hash. All full prompt-plus-output caps must fit8192 context. Keep labels host-side except the declared public canonical vocabulary.
6. Implement independent-of-stored-score projection of prospective primary `[(M−C)d1−(M−C)d0]label-first−[...]tag-first`, all0..3 profiles, context/seed/dispatch strata, and planned/null/strict/joint-valid denominators. Preserve first-anchor boundary diagnostic, whole64 success, count L1/confusion, all native token/cache usage, and raw failures. No realignment or alternative best outcome selection.
7. Reuse verified owned service lifecycle with collection600/shared work780/owned900 including cleanup120/outer930s; four workers, request120s, max_output3072, calls96 exactly, no rerolls. Source/test/input/weights/grammar-renderer/command/cap closure and failures travel in READY. Publish READY last only after focused tests and CPU validation. MAIN alone reads/accepts/launches; this plan grants no GPU action.
8. Before outcomes, another analyst or MAIN seals audit method. After terminal and owned release, compute primary independently from raw wire, maintaining all eight planned task quadruples and four context clusters. Report outcomes promptly; no broad hardening should displace a ready GPU experiment.

Expected preparation is a narrow source adapter plus focused tests, not a new framework. A material native grammar/order capability failure should be reported without switching format, increasing scope, or launching a different comparison.
