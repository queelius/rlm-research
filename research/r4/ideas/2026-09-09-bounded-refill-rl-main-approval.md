# MAIN decision: bounded-refill RL after successful-trajectory SFT

September9,2026, prospective design approval. MAIN read the complete bounded-refill options and the three primary-literature notes. The preferred2→4group windows test whether bounded extra sampling obtains reward variation without stopping the entire run at the first uniform window. Fixed four groups or fixed two/no-op remain sensible later controls; this pilot measures support yield, not a causal refill-versus-no-refill policy effect.

Approve a new isolated root-bounded-refill-rl-v1, starting exactly fixed low-LR success-SFT8 adapter66cce400… with fresh RL Adam. This choice is fixed now, not contingent on the running highLR readout. MAIN already knows the prior success-SFT72 5→9/24 signal, so this is exploratory development. Do not use highLR outputs or evaluation trajectories as teachers. Fixed c32 child/native runtime/reward/admission/likelihood guards remain unchanged. Root-only current-action credit, binary strict terminal reward, equal episode/turn/token loss; homogeneous groups yield no invented gradient.

Freeze16 context×family prompt groups from the existing eight training contexts into four windows, each32+64 records and both families. Mandatory first pair length-balanced/family alternating; fixed third/fourth refill order. Eight fresh candidates/group, max128train attempts. Within a window one unchanged policy; collect third then fourth only while fewer than two admitted mixed groups. After at mostfour groups, select all admitted episodes of every mixed group; one true optimizer update if support exists, otherwise explicit no-op with no Adam mutation. Distinct candidate-window and Adam cursors. No incomplete/capped-window update. Save full optimizer/RNG/source provenance each actual update; final is last committed policy0–4, never best validation or substitute fixed4 claim.

Fresh master981316001 (proposed, require collision scan), separate explicit rollout/training/readout namespaces. All128 potential train coordinates and fixed24 baseline/final readouts frozen before first sample. Exposed eight readout contexts, not independent holdout. All48 readout denominator retained with strict0 versus NULL. No intermediate evaluation.

MAIN changes the options' proposed45min envelope prospectively to60min:3630outer/3600owned/3480sharedwork, reserve final660work seconds for180service+480collection and120cleanup. Thus stop starting training collection/update work at elapsed2820s. Baseline also max660 inclusive, no clock reset. Candidate group and update caps must be explicit and clipped to remaining work; leave genuine save/release reserve. Up to four updates is a maximum, not a completion promise. Do not spend all final-readout reserve chasing support. Fresh service warmup/verify/model switching/failed or excluded calls count against total cost. No unsafe predecessor cleanup.

Acceptance requires small real-admission/cursor/no-op fixtures and actual dispatch/global/cardinality smoke, not a large framework rewrite. Pin old sources; implement variable-group validation transparently. Training support diagnostic compares mandatoryfirst2 eligibility to finalwindow eligibility under same policy, never counterfactual learned accuracy. Retire or revise if added groups mostly stay uniform or cost dominates; promising support plus directional endpoint gain justifies another separately seeded comparison. Do not declare a new RL algorithm or broad adaptive planning from this pilot.
