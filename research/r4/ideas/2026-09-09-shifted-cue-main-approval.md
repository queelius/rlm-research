# MAIN approval: contradictory source-cue mechanism screen

September9,2026 ~16:11UTC; prospective design approval before preparation/model
calls. Read option2 of correspondence-to-rlm-next-options.md (SHA4d4e6f3e…).
Implement one new sidecar leaf-shifted-cue-v1; no accepted-source changes or GPU
authority delegated. This is the next short mechanism discriminator, independent
of the still-running RL/fresh96/paired-plan results.

Use the same12 existing64-record contexts (4TREC/4SST/4AG), fixed c32 helper,
two fresh seeds981327011/021 and matching/constant/shifted dense tag-label output
schemas:72 calls. Recheck proposed master981327001 against bounded named metadata
and record scope. Keep input order, shared positional-label instruction and
physical initial prompt fixed. Shifted tags name the source record at
(i+17)mod64; matching and shifted contain exactly the same ID multiset. Validate
canonical labels, cardinality and actual token budgets before freeze.

Primary correctness is always the label at the displayed input position.
Separately freeze named-record label alignment and its difference from positional
accuracy, task/context profiles, histogram error, validity and all usage/cost.
Constant tags have no named-record correspondence. Never repair primary scoring
by rematching after outcomes. Interpret stronger shifted named-record alignment
as behavioral following of a contradictory source cue, not attention proof or
freely learned ID retrieval. The grammar supplies the cue. Different schemas
change constrained generation; literal/token costs must be measured.

All72 planned slots are retained. Observed malformed/truncated outputs score0;
unavailable/provider/canceled/unrun outcomes are NULL. No best-subset selection,
rerolls, changed data after failed qualification, or model checkpoint choice.
New external900s cap, owned870/shared750/collection600, single exclusiveA100,
four concurrent calls; raw calls checkpointed individually. Reuse the qualified
model/runtime/renderer and minimal flat entrypoints. Check actual executable
imports, whole72 cardinality and actual launcher identity to avoid old nested
__file__ provenance errors. Source/input/environment hashes, READY and runbook
before MAIN acceptance.

Expected promotion: directional cue-following in multiple context groups, with
valid outputs and measured costs, motivates a bounded mechanistic behavioral
claim or same-map-API RLM bridge. Flat/mixed results retire a simple source-pointer
account, not prove equivalence. Generic indexing itself is established prior art.

Do not interrupt active GPU work. CPU preparation can pause briefly to execute
already-ready independent terminal audits as runs finish. MAIN owns launch,
allocation checks and exact service cleanup; existing two waiters remain intact.
