# Allocation-end root learning and mechanism follow-ups

MAIN decision, September 9, 2026, after the bridge32 and local replay parents
reported exit zero. This is exploratory authorization, not a result claim.
Allocation ends 19:07:17 UTC. Preserve prior sources and outputs unchanged.

## Immediate ready-run priority

Prepare an additive version of the qualified child-representation bridge32 using
the fixed last bounded-RL4 root checkpoint (adapter SHA
2286be3f7c0c9cc0e22c8ef8e3473b7d8eb6ec4b7a789ca380a0af9d3b944c71)
instead of the low66c root. Keep the same c32 child, 32 coordinates, prompts,
sampling seeds, public records, array/map treatment and strict metrics. The
question is whether the child-format effect depends on the root policy. This is
an exposed-context, paired policy-by-format exploratory comparison, not a fresh
replication or a selected best checkpoint. Reuse existing seed coordinates for
pairing; no new independent-seed claim. Cap1800 outer,1650 shared work,1500
collection,120 cleanup as qualified. No science output or accepted source edits.

## New-root-source panel

Leaf owns a new immutable panel, selected before any readout, using the nine
named exclusion inputs and remaining-set identities in the post-refill
feasibility memo. Use master981330001 only after checking collisions. Select
32 helper-training and32 helper-validation groups by label-independent hash
rank; make two16-record contexts in each stratum. Do not use labels to balance,
reject, reorder or obtain nonzero targets. Preserve lineage, representative
selection and helper/pretraining exposure caveats. This reserves64 groups only
for readout, not training. Four synthetic users u00..u03 are assigned round-robin
in the frozen record order. Define single-user u00 and union u00/u01 questions,
with NUM/HUM target category alternating by context index. Publish DATA_READY
with complete checksums and original membership before another agent consumes
it. These are short new-root-source tasks, not long-context transfer or globally
fresh data. Dataset licensing remains unresolved.

## Matched complete-demonstration SFT

Proceed with option2 of the post-refill memo, modified precisely as follows.
Reuse the16 canonical authored actions from the384 training-only groups and
the fixed low66c start. Capture one actual owned-runtime execution with the
actual c32 child per action; no invented child maps, observations or policy
likelihoods. Authored targets are SFT, never native-policy samples or RL data.
Terminal targets use the actual computed scalar, even if dataset gold differs.
If any execution fails to supply an authentic scalar, preserve the partial
corpus and stop this attempt; do not reroll, omit or manufacture examples.

Train two fresh-Adam four-update arms from identical initial weights/order and
LR1e-4. Preserve the original per-example token-mean action coefficient in both:
each action token has weight1/(16 times that action's target-token count).
Use L_action versus L_action + 0.1 L_terminal, with terminal loss also the mean
of16 per-example target-token means. Mask observations, prior actions and child
tokens. Record exact target counts, loss terms, gradient/delta norms and all
checkpoint/optimizer states. This adds supervision, not equal gradient or token
compute. Actual terminal native prefixes must be causally aligned and frozen
from capture before optimization. Do not reuse the older action-only result as
the new matched objective control.

Predeclare unchanged start plus both fixed four-update arms on all four new
contexts, both query types and two fresh seeds:16 readouts per arm,48 total.
Hash-fix condition order and reuse coordinate seeds for paired comparison.
Outer cap3600s, shared work3450s, cleanup120s, with phase-specific bounds clipped
to that deadline. Save every capture, update and readout incrementally; all
unavailable/unrun coordinates remain NULL. No validation-selected checkpoint,
answer repair, follow-on epochs or evaluation-label filtering. If expected
remaining allocation cannot fit this job, preserve the prepared study for the
next allocation and run a shorter informative ready comparison instead.

Main endpoints are strict task accuracy and terminal format, with supplied-map
consistency, scope/coverage, real child cost and failure availability separately.
If only format improves, claim only format learning. If neither improves, retire
this small recipe rather than extending it by default.

## Subsequent map/reducer diagnostic

Option1 remains next-ranked after the ready bridge-policy comparison and CPU
preparation of the learning job: same new panel, fixed last bounded-RL4 root,
actual native c32 maps versus explicitly privileged dataset maps, ordinary
Python versus a pure documented count_labels helper.32 root episodes plus four
map acquisitions,1350 outer/1200 work/120 cleanup. Oracle maps are never training
data or normal end-to-end performance. No generated host-code execution,
invented ancestry, automatic submission or gold-aware repair. Prepare only if
it does not delay ready GPU work. MAIN will accept an exact READY before launch.
