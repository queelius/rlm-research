# Fixed24 at natural acquisition scale: proposed24-endpoint assay

Design/CPU feasibility only; no implementation, allocation READY, GPU or launch. Composition96's sealed audit takes priority on terminal relay. This revises MAIN's16/64/128 suggestion **prospectively to16/128/256**, for the specific evidence below. No scale outcomes were read or used to select sources.

## Question and why this is not a repeat

Can exact operator-SFT24 retain and combine evidence when its usual single typed full-map acquisition no longer fits, and then finish a count or weight-sum task? Correctness alone is insufficient: distinguish genuine combination across actual acquisitions, literal reconstruction from returned observations, persistent map accumulation, and delegation of the entire reduction to one child.

Completed scale work exists. BROAD16 had old-root length1282/8→3/8,2560/4→0/4, and flat64-record “composition”5/24; it did not test this now-procedurally competent fixed24 checkpoint or this four-field weighted native interface. The completed child-representation bridge used exposed32/128-record contexts with low66c and c32, mostly width4; it found a concrete128-record final-batch-only reduction despite a correct full map. The new dose audit finds real acquisition and faithful reductions at fixed24 but mostly one16/32-record child call; its successful four-call example recovered through literal inspection rather than demonstrated live-map merging. Thus **fixed24's new-input, multi-acquisition scale transfer remains unanswered**. Do not call record scale or result handles novel ideas.

## Smallest informative comparison

One fixed policy: exact24 adapter `94022838a64a530e1abc8daf6cd43502d8aec7d549b6bfec10dd4928b0ca9006`; unchanged c32 `c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3`, base cdbee, same qualified two-LoRA native lifecycle and coding role. No root training/comparator checkpoint selection, new helper, map, hint, output grammar, mandatory recursion or prescribed batch width.

Four disjoint parent contexts ×nested first16/128/256 records ×count/weight-sum =24 endpoints. Use the **exact count and weight-sum question strings from `ct_protocol.specs(cluster)[0/2]`**, including all-users scope and existing native final contract. A cycles human being/entity/location/numeric value by parent cluster. The same IDs/text/users/weights and order survive nesting; only file contents/declared size and resulting truth change. Count and weight queries use the same target. One fresh seed per parent/operator reused across its three sizes; sizes therefore have matched sampling seeds, not identical prompts or difficulty. This is four source clusters, not24 independent replicates.

Why omit64? CPU reconstruction under the actual tokenizer/native child prefix finds full64 requests only2,570–2,666 input tokens, and compact constant-label maps918–1,129 output tokens. That comfortably fits current8192 context/2048 child output;64 may remain a one-call problem. In the recommended four-parent preview:

| Records | Full typed child input | Input + requested2048 | Compact canonical constant-label maps, incl EOS |
|---:|---:|---:|---:|
|16|1,239–1,261|3,287–3,309|224–285|
|128|4,420–4,623|6,468–6,671|1,854–2,241|
|256|8,111–8,367|10,159–10,415|3,681–4,489|

These are source-only measurements: exact native child prefix from the retained authored CPU fixture, with only the public `request_for(records)` text replaced; original decode/re-encode round-trip equals actual token IDs. Constant-label maps are serialization illustrations across all six allowed labels, **not gold predictions, actual completions, or a proof of the globally shortest encoding**. The256 full-map request exceeds declared input+output budget in all four parents; two exceed input context alone. Record actual provider rejection/capping rather than assume its error route.128 is a useful near-output-cap bridge.

This creates pressure on the *single typed full-map route*, not a theorem that every valid solution requires multiple calls. A root can reason directly, ask a child a shorter/custom question, select subsets, or delegate a scalar reduction; those strategies remain legal and are separately measured. If no trajectory actually uses several relevant acquisitions, the study is an initiation/budget-response assay and cannot establish multi-batch retention.

## Label-blind source rule and admission

Use composition's pinned refreshed2,128-group eligible pool minus its newly accepted128 groups: **2,000 available**, already excluding dose64 and older named root exposure. Sort by SHA256(canonical compact JSON `[2026091700,"source",group_id]`); take first1,024; split into four consecutive256 blocks. For parent `scale-state-{ci:02d}`, hash-order its block by `[master,"order",parent,group_id]`. Assign `user=u{position%4}`, weight `1+int(hash([master,"weight",group_id])[:8],16)%7`, and ID `q`+first12 hex of hash(`[master,"record",parent,group_id]`). Nested sizes take prefixes with no reordering. No labels, gold cardinalities, text length or sampled behavior enter selection. These sources remain c32-training/catalog exposed, not globally/pretraining unseen. This spends1,024 distinct source groups, leaving976 under this inventory; that source-pool cost is a reason to keep only four parents.

The exploratory CPU preview is **not a scientific input allocation**: no sidecar/input files were created. Pool sorted-list digest `bf77c583123f7dce3ff99826de24265f3af6ed0d902afc0021896ad46b3660e9`; recommended selected sequence digest `d52903c9a0f72fc9caa5ff1c83302bc30e1241da9bbad9bee697eb861f594e3d`. Earlier16/64/128 preview used512 groups (digest `9ce45f75d0574560c8e5e8d99a5c71b13782b38defe5355e4451f766ff5abf1e`); it was revised for structural informativeness, without model outcomes or gold-based replacement. If source availability changes before approval, request an explicit new allocation amendment; do not silently substitute groups.

All24 preview gold values are nonzero, **by realization rather than filtering**. Count/weight pairs for16→128→256:

- Parent0: (3,18)→(35,123)→(63,240).
- Parent1: (5,21)→(26,103)→(57,230).
- Parent2: (4,20)→(19,78)→(40,170).
- Parent3: (2,6)→(22,89)→(40,156).

Zero baseline0/24; pooled best constant40 gets2/24. Publish family/size histograms and all tied constants before launch; never rerank zeros. Natural size changes labels, gold magnitude, semantic error exposure and difficulty. Neither a size effect nor nested pairing isolates memory causally.

Separate admission: all **initial root** prefixes must equal the real native renderer, and prefix+2048≤8192 (preview1,034–1,042). All source files/host-label mutation invariance and typed arbitrary-ID matching are CPU-qualified. Do not demand that a full256 child call fit as an endpoint-admission condition: its lack of fit is the prospective pressure being tested. Later root context overflow after printing records/maps is an observed operational failure, not an input reroll or automatic context expansion. Preserve all24 planned slots; missing per-episode RESULT stays primary NULL, authenticated malformed scalar finals strict0.

## Budget, observations and conditional next intervention

One A10040GB, one fixed24+c32 service, root/child2048, context8192, temperature.5/top-p1, four workers,180s/episode, no retry. Proposed2400 outer /2220 work /2370 owned: startup≤180, collection≤1950, harvest90;150 release/emergency reserve and30 outer margin. Stages advance early. About20–35min is a planning estimate, not a guaranteed completion time. All calls, successful/failed attempted work, known/unknown usage/cache, actual widths/coverage, context-cap errors and wall time are retained; billing unknown. Episode results and raw physical records checkpoint per coordinate, with complete planned NULL inventory; no resume/reroll authority.

Primary: strict dataset success/8 by size, by operator and nested four-parent contrasts with NULL bounds; report nonzero and constant baselines. Mechanisms, each with explicit denominator: actual child acquisition; ≥2 acquisitions contributing different relevant records; observed-label coverage and complete-state map-consistent result; faithfully executed combined reduction; persistent live-map merge versus literal reconstruction/manual arithmetic; final stopping. A root that overwrites its map then correctly reasons from printed history has evidence reuse, **not live-map accumulation**. Multiple calls, variable names, or host union correctness alone do not prove use. Reconstruct exact actually consumed state, preserve conflicting/wrong child labels, never union convenient batches or execute sampled programs. Correct child-derived reduction with wrong dataset labels is a leaf error, not root forgetting.

No handle arm now: the existing evidence has too little successful multi-batch exposure to justify it. Only if new traces show repeated loss/overwrite of *sufficient genuine acquired evidence* across at least two parent contexts should MAIN consider an independently approved paired intervention. Minimal candidate: append-only raw-answer files plus a read-only index of actual request IDs and requested-ID sets, populated only after each real child return; preserve `child.answer` bytes and ordinary observations, compute no map union/selection/scalar, add no gold/repair/fallback. Advertise only file discovery. This would test availability of retained on-policy results, unlike prior preloaded-map/canonical-loader studies; it would still bundle persistence/discoverability and require exact native integration qualification. If child errors, one-shot delegation or cap rejection dominate instead, do not label the next experiment a memory repair.

## Read depth and exact provenance

Read fully: MAIN options note; completed BROAD16 report; child-representation bridge report; fixed24 dose report plus accounting erratum. Inspected current composition protocol/study/input provenance/group allocation (already independently CPU-reviewed), original public request/strict-map helper, actual authored child request/caps and real tokenizer rendering. No new literature/source downloads; these are local prospective design conclusions, not novelty claims.

Pins: options `1a5f9162275293742cf364d8e15f1ce6d92ce885da957d22ab47a20531fcb067`; composition `inputs/PROVENANCE.json` `c14c313b1b3e5e9a75edbac354fb8a97b9613b337a353f17fad34c2cf0a76823`, `inputs/GROUPS.json` `1f7aae71113c96920b210fa8edc4997656c55ff56c9cae8a7dc7125ea37e58e1`; BROAD16 report `8c2883690f8d9c5d00f708eadba291b7f14a90741d1ab24532517d7b9bb6bfa6`; bridge report `b07584134aeac387328f557c2dd976b25a1e3d9e345363dee8397ca2df64f206`; dose report `c67c2b7430b69a890d9a51f0be14d3084fc45439210284fca1454708b84a749e`, erratum `949d6c5b74c6eb5286dff38b2155200ec825b3b51fb43a07b371b77506083090`. Helper `sidecars/adaptive-filter-pilot-v1/batch_contract.py` `d2c7f8df62a4190930dbf3e14f7e68cfb27cc573898bbc343117e8d97190bd88`; tokenizer JSON `aeb13307a71acd8fe81861d94ad54ab689df773318809eed3cbe794b4492dae4`; retained authored child physical fixture `/tmp/composition-independent-native-20260910/test_authored_native_file_chil0/native/physical/0002.json` `dd60b6fc9d9f49ab150cdcd2e8e285fa7028ca69718d1184fd7991094e09ac02` (CPU-only, not model evidence).
