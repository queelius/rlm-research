---
title: Conditional fixed checkpoint-2 protected readout
date: 2026-09-10
status: prospective_before_sparse_recovery_outcome
gpu_authorized: false
---

# Question

If and only if the sparse-head recovery authenticates the exact failed group and commits Adam step
2, does fixed checkpoint 2 change protected free behavior relative to the already-collected start and
checkpoint-1 policies?

# Fixed comparison

Run the exact 72 protected coordinates from the terminal-RLVR campaign once under checkpoint 2.
The source questions, public records, files, c32 child, native role, decoding, and original seeds are
unchanged. Existing start72 and checkpoint1-72 are never rerun. Checkpoint 2 is fixed by optimizer
ordinal before its outcome exists; there is no best-checkpoint selection.

The owner first authenticates the sparse recovery terminal, clean release, checkpoint state, exact
11-episode/154-turn/18,517-action-token failed group lineage, adapter/config/optimizer/RNG files,
and step 1 to step 2 cursor. If any gate is absent or false, it writes a zero-request SKIP and exits;
there is no substitute checkpoint or GPU service. A malformed claim of success is fatal rather than
a skip.

All 72 planned slots remain visible. Authenticated completed malformed/empty finals are observed
zeros; missing or unverified native finals are NULL with bounds. Primary reporting is checkpoint 2
versus the immutable checkpoint-1 readout, paired on all 72 planned coordinates; start is a fixed
secondary reference. Report strict correctness, actual child acquisition, faithful requested
operator/scope/threshold execution, state use, stopping, zero-answer coincidences, paired
wins/losses/ties/NULLs, context clustering, all physical requests, and known/unknown token usage.
No model-generated code is reexecuted in audit.

# Budget

One A100, one root service, four collector workers, 180-second endpoint cap inherited from the
qualified collector, 2,400 seconds work, 2,670 seconds owned, and 2,700 seconds inclusive outer
cap. The larger envelope avoids making missingness differ from the earlier readouts; actual elapsed
and physical costs remain the comparison. This readout establishes behavior at one additional dose,
not a smooth learning curve or independent-context replication.
