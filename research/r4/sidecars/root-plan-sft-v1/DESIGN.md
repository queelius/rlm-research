# Authored query-sensitive plan teaching

Implements optionA only from `ideas/2026-09-09-context-sensitive-root-next-options.md` and MAIN's `ideas/2026-09-09-root-plan-sft-main-approval.md`, both read fully. Common start is fixed66cce400 success-SFT8, not a pending high-LR/coverage-selected policy. Two fresh-Adam fixed4 roots: canonical classify-all-then-filter and filter_first. Sixteen initial-action targets per arm from the original eight training contexts×single-user/global. Global code/targets equal byte-for-byte; single-user code differs only in `selected = records` versus `selected = relevant`. Public user/category literals only, batch16, strict returned maps accumulated without repair, count printed. No ledger or worked-example removal.

These are explicitly operator-authored CE actions, not sampled native trajectories, success-filtered demonstrations or RL data. No behavior probabilities/reward/advantages or fake child observations enter training. Actual native first-prefix rendering and authored tool-action serialization define the boundary; only authored current-action tokens (including declared151645 terminator) receive loss. A separate owned CPU fake-provider fixture may prove execution/transport, but its probabilities/observations never enter SFT rows.

Both arms train four full16-example passes: equal example then mean action-token CE, LR1e-4/WD0/clip1/BF16 base/FP32rank8/dropout0. MAIN's separately bound prospective LR amendment supersedes the original2e-5 only: this is explicitly outcome-informed exploratory teaching, with unchanged fixed low66cce start and no high-LR winner substitution. Shared training seed981320002 and per-pass orders; fresh Adam each arm. Save adapter/optimizer/RNG/cursor each update; fixed4 only. No token padding or equal-FLOP/gradient-share claim. Three readouts include unchanged66cce400 and both fixed4,16 transfer coordinates each (query+length only),48 planned. Primary filter_first−canonical, calibration contrasts secondary. Correct direct solutions valid, NULL distinct from completed wrong/empty0. Existing320 readout source groups and192 unused validation groups excluded from384 training groups.

Fresh master981320001; query seeds981320201–208, length981320301–308; CPU-only fixture901. Hash-fixed training and readout phase orders. Same c32 child/native typed interface/local immutable image in all arms. New study is explanatory/exploratory on exposed developmental contexts, not generic planning or self-improvement evidence. No coverage/high-LR outcomes were consulted in selecting the strategy/data design; MAIN subsequently disclosed the high-LR aggregate and approved the prospective symmetric LR amendment. No evaluation trajectories or child answers enter targets.
