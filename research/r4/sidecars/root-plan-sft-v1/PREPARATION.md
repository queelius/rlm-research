# Preparation evidence and review scope

CPU-only preparation, no learned model/service/GPU calls or accepted-source mutation. Three initial contract tests were observed RED (missing study module), then passed after implementation. Focused final suite has8 tests: public-only minimal strategy difference and actual API signature, authored masks/no fake behavior fields, exact16 transferred coordinates/seeds, actual private collector16 dispatch/cancellation/terminal cardinality, actual top-level two-training/three-readout48 flow with fake external boundaries, four real tiny-model Adam steps with optimizer serialization/frozen base, trusted fixture context ID, and actual corrected native fixture observation/action/grammar checks. The tiny model is a CPU optimizer/CE check, not the research checkpoint or an assertion GPU training has occurred. Checkpoint saving itself reuses the unchanged qualified helper.

Qualification001:180s timeout,0provider requests, preserved. Source-derived missing context_window_id explanation and separate caught public API mistakes are in CPU_AMENDMENT. Qualification002:PASS57.201s,7physical synthetic-provider requests,4root/3child. Actual native first prefixes and authored sampled action token suffixes match prepared-v2. Both actual unsampled tool messages are exactly2 newline. Synthetic completion logprobs exist only in qualification raw graphs, never authored rows.

The corrected targets contain2472 action tokens per complete pass in each arm;16examples/arm,9888 target exposures after4 passes, max combined prefix+target1232tokens. Global target bytes equal; each user target differs only in selected=records versus selected=relevant. Four shared16-example orders are frozen.16 readout native prefixes match historical source bytes/token IDs. No training group overlaps the320 transfer groups or192 held-out validation groups. Fresh981320 namespace scan has no external collision. Selection is hash-label-independent, inherited contexts unchanged; no model outputs were used to construct authored targets.

MAIN decisions read fully: optionA approval, then prospective symmetric LR1e-4 amendment before READY. Fixedstart66cce unchanged. Coverage audit ran after preparation began and was independently sealed during a pause; none of its outcomes changed this study's inputs/code/recipe. Root code remains free at readout; authored plans are CE targets, not forced inference actions. No behavior-likelihood, child-credit or successful-trajectory claim.

Native interfaces, typed source-matcher/grammar, local immutable image and owned release are source-pinned inherited components. New private substitutions are counted and tested: readout prepared-v2 locations (two), composer24→16 (one), collector planned24→16 (one) and terminal cardinality24→16 (two). Training objective helper is reused without its genuine-native data validator, through a new explicit authored CE contract. No artificial behavior probabilities bridge that boundary.

READY is written last after fresh focused tests and source/input/starting-state authentication. MAIN's source review/acceptance is distinct from these implementer checks; no acceptance or launch is inferred. Full tensor execution remains a real-run risk under the bounded experiment. Superseded local CPU inputs/source/failure remain in the closure as evidence, never active data.
