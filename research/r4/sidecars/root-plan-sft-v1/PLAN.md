# Root plan SFT implementation plan

> Execute inline using executing-plans in this explicitly requested isolated external namespace. MAIN already approved implementation; no repository branch, install, GPU call, source hot edit or additional experiment.

Goal: prepare two authenticated authored-plan SFT4 arms and48 transferred readouts.
Architecture: new authored-data validation/paired plan builder; reuse qualified native renderer, current-action CE, checkpoint helpers, typed child collector and owned lifecycle through source-pinned private imports. Never fabricate a native-only trainer's probability fields.
Stack: existing Prime native Python, existing GPU trainer Python, native Qwen3 renderer, immutable local rootless image, PyTorch/PEFT.
Spec: DESIGN.md and the separately bound MAIN approval.

## Task1: authored input contract

- [ ] Write tests for exact global action equality, user assignment-only difference, public literals, no behavior-likelihood/reward fields and exact current-action masks; observe RED.
- [ ] Implement study.py `program(context,family,arm)`, `authored_row(prefix,target,...)`, `build_plan(old24)` and source-pinned loaders.
- [ ] prepare.py reuses immutable PUBLIC/HOST/TEMPLATE and native renderers, creates32 authored rows,16 new readout coordinates, exact group-split/prefix/seed/order proof. No host execution of target code.

## Task2: training

- [ ] Test tiny in-memory LoRA full-pass update and four real Adam cursors/checkpoints; prompt/history/observation positions receive no CE.
- [ ] train.py validates16 authored rows for one arm, calls unchanged qualified episode/turn/action CE with one turn per example, performs four fresh-Adam full passes, binds arm/corpus/source identity and fixed4 selection. No old probabilities or native-success adapter.
- [ ] Deadline360 includes load/verification/save, no partial-pass or repeated checkpoint. Finite gradient/weights and exactly incrementing Adam required.

## Task3: native/readout/lifecycle

- [ ] Test actual qualified collector16-row dispatch/terminal metadata and cancellation writes; exact three-phase48 owned flow with fake process/service boundaries.
- [ ] binding.py authenticates unchanged8 and each new arm4 separately; readout.py uses private24→16 composer/terminal substitutions only, actual root/child descriptor/cards and native prefix checks unchanged.
- [ ] launch.py trains both arms serially then reads three hash-fixed phases under one2280 shared/2400 owned clock;360 each train/420 each readout/startup+release consume shared clock/120 final cleanup; parent2430 outer.
- [ ] qualify.py one bounded owned CPU test executes vetted authored canonical and filter programs against synthetic child maps, checks native initial/action boundaries and correct user-scope count; fixture data never supplies training observations/probabilities.

## Task4: freeze

- [ ] Run focused tests/compile and native qualification; preserve failed local attempts separately. Authenticate immutable source/input/start-weight closures once.
- [ ] Write RECIPE/INPUTS/RUNBOOK/preparation evidence; READY last with exact argv and caps. MAIN independently reviews and alone accepts/launches. Coverage terminal audit preempts preparation when triggered.
