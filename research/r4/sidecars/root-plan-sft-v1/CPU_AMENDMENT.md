# Pre-READY CPU corrections, no model observations

The first native qualification had0 provider calls and timed out at its180-second envelope. Its fixture coordinate omitted context_window_id, required by the source-pinned typed native hook before dispatch; this explains why capture/provider records were never created. No learned model ran. All original prepared/, INPUTS.json, RECIPE.json, qualification-001 and superseded-cpu-001 source snapshots are retained, bound as superseded evidence, never training inputs.

Source inspection also found public API spellings in the initial authored proposal that had not yet executed: the actual task supplies records.json (a list), not records.jsonl; rlm is imported from rlm.api; its result exposes .answer; strict_map takes raw answer plus an ID list, not record objects. Corrected **both** strategy arms identically before any training/READY. Global targets remain byte-identical; single-user programs still differ only at selected=records versus selected=relevant. New active inputs are prepared-v2/, INPUTS_V2.json and RECIPE_V2.json. This is a preparation/API correction, not outcome-based task or target selection.

Qualification002 supplies the trusted public context_window_id and executes these corrected authored programs only inside owned rootless runtimes.7 physical fake-provider calls completed in57.201s:4 root turns,3 source-bound typed children. Canonical inspects32IDs in two16-record batches; filter-first inspects the8 records of the public requested user in one batch. Both actual tool messages are exactly `2\n` for plainly synthetic ID-based child labels. The initial token prefix and authored action suffix equal the prepared training row; tool observation masks/logprobs have no training credit. No fake likelihood/reward/observation enters either16-row corpus.

No new study episode retries, training resumes or checkpoint substitutions. The inherited nano runtime retains transient API retry capability under the same total cap; max agent/SDK retries0 does not disable that layer. The first fixture's no-dispatch failure is preserved rather than reported as a model attempt. New study model work has not started.

Private collector adapter: the qualified row-mean composer checks16 rather than24 supplied prompts; the inherited individual-row collector changes planned24→16 and both terminal len(records)==24 tests→16, each with exact source hash/count assertion. It keeps individual rows/four workers, native capture, parser, timeout and cancellation-finally writes. It is not a new pair/quad scheduler. The top-level readout changes only its new prepared-v2 plan/prompt locations and exact lineage binding. Focused tests execute the actual adapted collector16 terminal plus cancellation and the new two-training/three-readout48 owned flow with fake external process boundaries.
