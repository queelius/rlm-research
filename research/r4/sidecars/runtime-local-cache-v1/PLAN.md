# Future-only ephemeral runtime cache

Parent-authorized900s aggregate CPU preparation; one180s native fixture; no GPU/model calls, image rebuild or accepted source changes. Own `/tmp/rlmc.0m4242` only, UID verified, CPUs34,35 within allocation32-35,96-99. Hard resource target≤12GiB store plus fixture work, leaving headroom inside64GiB allocation. Source immutable image8cfe5976b347e0201e52035256537a7cc90fca5004a0bf48cbd42b282498838c and six-layer chain from frozen preinstalled manifest. Copy only selected immutable paths; regenerate private image/layer metadata from frozen manifest, never copy a live DB. Hash source and destination files, record symlinks and old/new mapping. Check exact private image identity before one fixture.

Use unchanged native root-child-root fixture and offline installer canary, with only isolated cache/image/path/CPU rebinding. Compare complete token IDs, aliases and sampling to historical fixture, not model outcomes. Keep one failure without retry; record copy/startup/total time and footprint. No general speedup claim from one observation. Publish CPU_READY only after evidence; parent chooses future symmetric adoption. Store is ephemeral and not resumable after deletion/node loss; no automatic reconstruction or changed-source fallback.
