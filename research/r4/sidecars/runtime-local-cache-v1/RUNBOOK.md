# Qualified future runtime cache, not a live-job migration

The owned ephemeral store is `/tmp/rlmc.0m4242`, UID1523821556. Wrapper is this sidecar's `bin/docker`; runtime root is this sidecar directory. It points only at the local private VFS root/runroot/tmp/runtime and pins CPUs34,35, within allocation32-35,96-99. No HOME reassignment. Staged Podman/crun/conmon binaries remain pinned inherited external assets. Image8cfe is byte-identical to the qualified preinstalled image, with six selected immutable layer directories, exact per-file source/destination hashes and symlink mapping in COPY_MANIFEST. Private images/layers JSON is derived from frozen metadata, not copied from a live DB. No rebuild, tag mutation, image load or shared writer was used.

CPU fixture is ONE inherited root→child→root execution with a fake token provider, real broker/ACP/native renderer and full wire token/sampling/alias comparison. Offline installer scope uses the inherited UV_OFFLINE/PIP_NO_INDEX/command-denial canary; it is not a general network sandbox. Synthetic logprobs are fixtures, never training evidence. Captured setup timing and total fixture timing describe this observation only: storage, CPU affinity, load and run order are not randomized, so no general causal speedup follows.

For a separately frozen future study, symmetrically set `capture.q.ROOTLESS` to this sidecar, prepend its `bin` to PATH, select exact image8cfe, and use a separately owned VERIFIERS_CACHE_DIR (or a declared directory under this owned cache). Every runtime must use this wrapper throughout its lifetime. Preserve scientific prompts/sampling/roles/hooks unchanged. Do not change accepted/live24 or36. Main controls acceptance and GPU launch.

Current proof reserves≤12GiB for copy plus one small fixture under64GiB allocation; it does not qualify arbitrary concurrency. Source apparent bytes1,684,878,918; conservative twice-source+2GiB estimate5,517,241,484. A future job must include its maximum concurrent VFS copies and mutable cache growth in its memory budget; limit concurrency/cost or use the prior persistent store if not affordable. `/tmp` free space is whole-node capacity, not this allocation's entitlement. Stored image files consume tmpfs memory and are not automatically reclaimed by this sidecar.

Ephemeral limits: path validity/UID/image identity must be rechecked at acceptance; node loss, cleanup or missing directory invalidates readiness. There is no automatic recopy, retry, fallback or resume. Reconstruction requires a new bounded recorded copy/qualification; original persistent image and durable hash map remain. No deletion is authorized here; main must resolve all live consumers before later removing this exact store. Do not remove `/tmp`, other caches or any broader path.

Preparation uses an external namespace with a narrow red→green isolation test and one native fixture. All failures must remain, no repeated fixture. CPU_READY publication authenticates fresh result/comparison and clean private container inventory only; it grants no job launch authority.
