# Broader root-only RLVR: sixteen scheduled learning windows

2026-09-09. MAIN approved this core design before implementation. New external sidecar only; MAIN retains GPU admission, locks and live queue. No current SFT, old checkpoint, source, data manifest or output is edited. This is a substantial exploratory learning run, not a breadth-causal comparison or a general adaptive-decomposition claim.

## Question and fixed starting policy

Can terminal-reward RLVR from the qualified low66c root improve actual native aggregation and finishing on new source-group compositions, requested categories and longer documents, after sixteen diverse rollout windows? The corrective/complete-demo studies showed decreasing teacher CE and conditional answer gains without reliable free acquisition→reduction→stop. Therefore neither nominal optimizer count, teacher targets nor code in prompts is a success measure here. Roots choose their own actions, child batches and stopping; reward is only the authentic strict task answer.

Start from `/project/alex_phd/runs/rlm-research-r4/sidecars/root-success-trajectory-sft-v1/outputs/attempt-001/training/checkpoint-0008`, adapter SHA256 `66cce4009628ff7d8e047f08e6fb3a29febdbf8fb3ed8303871f69860f1151d5`, config `118bb737297c35d96a194b642026b1e58c72f945d55ae1616eb716cfc4601728`, state `506fc355e19b3201822ee1f6fb88eca1edd23e04219787b1d84484598f9531f2`. This is the prior fixed low-LR success-SFT8 warm start, not an untrained original root or a checkpoint selected from the live joint-SFT result. Begin fresh RL Adam0; never load its SFT optimizer. Pin the whole starting closure. Child c32 (`c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3`) stays fixed and is never loaded into the trainable root model. Base is the qualified local Qwen3-4B-Instruct-2507 cdbee revision, BF16 base/rank8 FP32 LoRA.

## Immutable data, task semantics and exposure

Reuse `root-curriculum-data-v1` candidate-data identity `75e4fcd76174bf52762523bcde24b23580ef9a7d89c6c49eb60285df14d40154`; no outcome-conditioned reallocation. Its24 training contexts have eight each of16/32/64 records and896 disjoint normalized source-question groups. Use its exact16-row candidate schedule: one context per size per window, each context visited twice with a different target; HUM/NUM/ENTY/LOC each supply12 of48 task groups. Eight fresh native rollouts per task gives24 planned episodes/window and384 training slots. Three fixed groups are always planned: no adaptive refill, reroll, successful-only substitution, extra attempts for homogeneous rewards or borrowed earlier-policy group.

Validation: eight32/64 contexts,384 different groups. Final composition/query: six64 contexts,384 different groups. Length: four128 plus two256 contexts,1024 different groups. Child-test-exposed: three32 contexts,96 groups. The used47 contexts contain2784 unique groups. The remaining child-validation-exposed and SST candidate strata are **not executed** in this run. No TREC definitions are ever applied to an SST task. All candidate58 contexts remain disjoint; the named old root training384-group pool that supplied the low-SFT study has zero intersection with every candidate split (read-only set comparison). This is a scoped exposure check, not all-history/paraphrase/pretraining exclusion.

Training/validation/composition/length questions were in child supervised training; child-test-exposed questions were absent from child gradient training but previously used in leaf evaluations. Prepared data exclude named old root/transfer compositions; broad unknown historical/OOLONG/pretraining exposure remains disclosed. Validation is development data, never an untouched confirmatory panel. New composition documents genuinely rearrange distinct, source-disjoint questions into new contexts. Reserved DESC/ABBR **requested targets** are category substitution, not unseen semantic labels or a new operation. Single/global filtering, unions, new-domain training and SFT curriculum mixtures are outside this fixed whole-context-count experiment.

Preserve original context text and selected task question bytes. Parse only the public `Date: … || User: … || Instance: …` lines to produce compatibility `records.json`: IDs `q0001`… in original text order, original user string and original Instance text. No label or source-record field is taken from HOST_GOLD. Dates remain in unchanged context.txt; records.json has exactly `id`, `user`, `text`. Public prompts state those exact fields, explain positional IDs and that maps are predictions, provide all six fixed category definitions and the actual selected task question. `query.txt` contains that plain original question; data.prompt adds only common truthful interface instructions. No forced recursion, complete-map requirement, corrective action, expected scalar or hidden teacher state. The optional pure batch request/decoder helper is identical across baseline and trained roots, and cannot call a model or choose a batch itself. Target/category conditioning must be tested in actual native task construction, not only host answers or prompt string markers.

HOST_GOLD, GROUPS, split names and all answer/count/source metadata stay host-only. Task hash/provenance may bind host answers, but setup files and actual first native request must be reconstructed from the model-public allowlist. CPU tests change host labels/answers and prove that model-visible task/setup content does not change. Root receives no dataset category labels or automatic answer fallback.

## Objective and native admission

Retain the proven bounded-RL numerical objective and masking: equal admitted episode, then equal current root turn, then mean native action token; root-depth0 only. Prior root text, child calls/responses and observations appear only as masked context. Actual current-generation processed native logprobs—not authored likelihoods—provide the behavior side of the existing capped token correction. Persistent AdamW learning rate5e-5, decay0, clip norm1; PPO clip.2, TIS cap2, no batch IS normalization, population-std within-task advantages and original discrepancy/ESS/removed-mass guards. The HF proximal value is current.detach only until the sole full-batch step, as in the qualified source; do not claim exact trajectory or off-policy-as-on-policy correction. No added KL, length penalty, recursion bonus, intermediate scalar reward or reward repair.

Keep **endpoint availability and training admission distinct**. A returned native final whose physical token branch/model/prompt identity and decoded content are authenticated is observable. Strict `Answer: N` with the actual host aggregate gives1; wrong/empty/malformed observable final gives0. Returned length-capped strict finals are scored under the same contract and flagged; a final-less length/tool loop is not an invented answer. Missing transport, unreturned/censored endpoint or unverified branch is NULL. Recovered provider errors do not automatically erase an otherwise authenticated endpoint from the endpoint readout.

Training remains deliberately stricter: require the complete native trace/action-mask/likelihood export and retain the old conservative provider/runtime error exclusions, separately recording their reason. Integrity failure prevents that window's update. A collection stage that did not finish its declared24 coordinates cannot supply a partial-window update. All eligible episodes from every within-task mixed1/0 group in the completed window enter one update; homogeneous groups contribute no manufactured advantage. A complete window with no mixed admitted group is a no-op, recorded with the policy/Adam cursor unchanged. Admission-only losses are not all-planned-population learning. Terminal metrics, admitted reward metrics and the conservative operational success/planned fraction are reported separately; NULLs are never silently zero-imputed into the primary conditional comparison or advantages.

## Schedule, checkpoints and failure/resume semantics

Window cursor1…16 and actual Adam cursor0…16 are different. “Fixed16” means the policy after scheduled window16, annotated with its actual optimizer cursor; it never implies sixteen gradients. Save immutable per-call, per-episode and complete-window exports before training, then adapter/Adam/RNG/generation/input-identity checkpoint after each actual full update, plus a committed window ledger even on no-op. Publish current policy only from a verified committed checkpoint, never a partial state. Validation at window0/4/8/12/16 uses16 fixed coordinates each, records the actual optimizer cursor and never chooses a checkpoint or schedule. A skipped validation retains planned NULLs and cannot consume final reserve.

On cutoff or training-stage/numerical failure, stop further training, preserve all artifacts and evaluate unchanged versus the last committed valid policy if service execution remains feasible. Fewer than12 informative updates is inadequate substantial-learning exposure; report it and propose a separate follow-up, never refill this run. Completing16 windows with12 updates is window16/Adam12, not final-update16. No automatic resume. Any separately MAIN-authorized resumed process must use the same immutable input/cursor/generation closure and cumulative compute debit in a new owned namespace; a committed generation is recovered without a second update. An interrupted uncommitted window is retained, not rerolled under its old identity. No implicit new six-hour clock or predecessor cleanup.

## Mandatory final transfer comparison

Run **all96 transfer endpoints after training**, regardless of learning signal: unchanged48 and last-committed48, exact shared inputs and fresh paired seeds, with predeclared policy order. This protects both halves under the final reserve and does not expose transfer scores during learning. Each policy gets:

- Composition/query24: six64 contexts × one trained target (alternate HUM/NUM) and one reserved requested target (alternate DESC/ABBR) × two seeds.
- Length12: six128/256 contexts × one trained target × two seeds; report128 and256 separately.
- Child-test-exposed12: three32 contexts × two trained targets × two seeds; keep this stratum separate from child-training-supported tasks.

Together with384 training and80 monitored validation coordinates, the owner plans560 episode slots before launch. A genuinely unrun checkpoint/endpoint remains explicitly unrun/NULL, not an attempted policy failure; report scheduled versus attempted counts. All context/target/seed variants are nested within context clusters. Report per-context paired changes, observable/strict success and format, actual executed acquisition/reduction/finish mechanism, repeated producers, batch sizes, root/child/total physical tokens/calls, and cap/transport/execution failures separately. Static AST/prompt presence or final agreement alone cannot authenticate state use. Zero-count target frequencies and the always-zero benchmark are host-side descriptive controls, never a changed target allocation.

A useful exploratory signal is at least+6/24 composition successes with gains in≥4/6 contexts, positive length change and no coverage collapse, after≥12 valid updates. These spending/research thresholds are not significance tests. A positive result motivates independent training-seed replication and a narrow16-window matched-attempt comparison; this single broad run cannot attribute change specifically to breadth versus added experience. Flat transfer with meaningful updates retires this recipe, not RLVR. Finishing improvement without faithful reduction warrants a narrower mechanism claim.

## Six-hour budget and minimal implementation

Exact proposed nested clocks: MAIN outer21600s; owner work21180s; owner alarm21480s, leaving300s owned cleanup and120s outer margin. Training and intermediate validation stop at elapsed16980s, leaving4200s of work for both final transfer services/96 endpoints. Final services each get at most180s readiness +1800s collection; their release and remaining slack also lie inside4200s. Service/core failures may prevent actual responses, but never remove the planned final slots. Checkpoint validation and no-op windows cannot borrow that reserve. All stage caps intersect the appropriate absolute deadline; maxima are not an additive runtime promise.

Training service readiness≤180s; complete24-slot collection≤600s with four workers and180s individual endpoint timeout; optimizer/load/forward/backward≤240s plus60s save/release allowance. Intermediate16-slot validation≤480s and is attempted only within the training cutoff. Sampling remains temperature.5, top_p1, native2048 completion and8192 causal context, unrestricted root and unchanged request-local typed child behavior. Long input documents stay in files, not a larger root prefill;128/256 transfer stress actual retrieval/working context. The first two completed windows supply an honest timing/admission projection, not permission to change the fixed schedule or manufacture updates.

Reuse bounded-refill's authenticated low66c start, root-only native action export and campaign_train numerical/checkpoint implementation; replace only its four-window adaptive consumed-prefix logic with fixed16×3×8 windows and the new data/task adapter. Reuse the working an27 local runtime/dual-LoRA owner/service path, with unique local module names and actual owner→service→config/descriptor→interceptedPopen CPU qualification. Focused tests cover public/host isolation, target-specific native prefixes/setup, planned-window/no-op/commit semantics, strict endpoint versus conservative training admission, source replay/role masks, persistent optimizer cursor, exact composed CLIs and shared final reserve. No new framework, download, generic memory infrastructure or broad test suite.

Prepared data and code are CPU-qualified before READY; true rollout yield, numerical guards and GPU throughput remain live experimental checks. MAIN reviews/accepts the sealed closure and owns all launch/cleanup authority.
