# Fresh purchase joins with optional native Python

MAIN approved CPU implementation on 2026-09-09. This supersedes IDEA.md's one-seed recommendation. No GPU/service/queue authority is delegated. This sidecar alone is writable; old science stays immutable.

Question: can an unchanged released Qwen3-4B-Instruct-2507 root turn complete purchase evidence into an exact cross-partition join when native Python is available? Is an informative triple representation helpful with facts fixed? No training, forced recursion, algorithm demonstration, host answer in prompt, report repair or answer extraction.

48 preplanned root endpoints: four newly generated isomorphic worlds × two paired sampling seeds × three representations (direct originals, co-located full reports, cross-partition full reports) × Python present/absent. Direct originals are not duplicated by partition. Each world contains 12 customers and 48 purchases, balanced three-chunk partitions, queried product pair and distractors; match sizes 3/5/7/9. Fresh namespace 981391: generator seeds 981391011–014; sampling seeds 981391101–108, one per world/repeat block; acquisition seeds 981391201–224; order seed 981391001. A pre-source search of prior PLAN/SPEC/READY/SEED/protocol/study files found no 981391 namespace. Customer/record IDs, product symbols, their mapping and order are randomized before outcomes. This is exploratory reuse of an exposed task design, four world clusters, not 48 independent replications.

Acquire 24 genuine native extraction responses (three per world/partition), preserve raw response bodies, native tokens, exact triple arrays and errors immutably. One acquisition serves both seeds and tool arms. Shape-invalid, missing or incomplete/non-exact extraction makes that report condition unavailable; do not repair, retry, replace with host gold or select another world. Direct conditions still run. Exactness is an acquisition availability gate declared before outcomes, and gate failures stay in planned-denominator scores. Original-record and actual-report host reducers are separately reported diagnostic ceilings, never model solutions and never visible to a root.

The acquisition role is separately pinned as a final-only purchase-record extraction assistant, with no tools and no grammar, 768 output tokens, enable_thinking=False. This is not the root coding/tool interface. Exactness-gated report availability is a conditional complete-evidence capacity screen, not expected performance on arbitrary generated reports; failures remain in full-pipeline planned denominators and costs.

Both tool conditions receive byte-identical complete representation-specific evidence inline, goal, field definitions, customer domain and system-role text. Both fresh native runtimes contain the same evidence files. Direct sees original records only; report roots see three actual unmerged reports only, not original records or computed local winners. Do not label partitions cross/co-located in the prompt. Common text says supplied files duplicate visible evidence and use of any advertised tool is optional; no code or algorithm hints. Only tool inventory changes. Same released base, no adapter, sampling .6/top_p .95, free decoding, 2560 tokens/action, 8192-token context; no grammar constrains actions or final answers.

The installed native harness normally fixes IPython and rejects disabled_tools. Use a process-owned private override, never installed-source mutation, to remove tool inventory and prevent execution in the absent condition while retaining the same native harness. This is an experimental harness modification, not a claim about current public RLM support. An authenticated attempt at an unadvertised tool is an observed policy failure. If no native final follows, preserve final=NULL, operational success=0, and explicit disabled-tool-attempt cause; do not fabricate a final or label the attempt as exogenous infrastructure failure. A pre-model setup failure remains infrastructure/no-model-work.

Python-present arms may choose manual answers, Python, or ordinary native child calls; no forced decomposition. All model identities remain the released base. A tool-availability gain is a native-tool package effect; Python-specific mechanism requires observed actual aggregation without subordinate assistance. Do not inherit TREC classification helpers, typed-child grammars or trained root/child bindings from the reusable restart wrapper.

Primary strict final contract: the complete authenticated terminal assistant content is a JSON list of distinct in-domain customer IDs in ascending order, exactly equal to host gold. No prose/fence extraction, sort/dedup repair, or substituted printed answer. Returned malformed/capped finals are observed wrong, not NULL. Native-final availability, available-only accuracy, missing-final bounds and planned-denominator operational success are separate. All unrun and report-acquisition-gated coordinates remain planned NULL rows with operational failure. Report-consistent correctness and independent set precision/recall are diagnostics.

Trace audit records actual requests/responses, tools inventory, files, generated programs, execution observations, child calls, first-prefix token identity, final branch, stopping/cap/context errors, and usage. Actual aggregation requires recorded input-dependent across-record/chunk computation and observed output; file loading alone or printing a literal is not aggregation. Generated code is audited as text, never rerun by the auditor. Host reducers run only trusted implementation code outside the model runtime.

Four workers, 120 seconds/episode, no retries. Inclusive 1650-second work budget: startup at most 180 seconds, acquisition at most 210 seconds, roots share all remaining time (nominal 1260 seconds); 1770-second owned deadline including 120-second cleanup, 1800-second outer deadline. Twelve 120-second root waves would exceed the remaining budget if all loop; the work cap deliberately retains unstarted/interrupted NULLs rather than extending time. Expected feasibility relies on normal short joins; this is a bounded exploratory48, not a promise of48 available results. Balanced interleaving prevents entire tool/representation cells being scheduled last.

Physical ledger: 24 new acquisition calls once plus every actual root/child physical attempt and returned completion usage. Report endpoints each carry three acquisition calls, 32 endpoints ×3=96 hypothetical acquisition charges, distinct from24 physical acquisitions. Report source failures retain attempted cost. Missing usage/billing stay unknown; cache, elapsed inference, owner cleanup and predecessor waiting remain separate.

Qualification before READY: independent world/property and strict-no-repair tests; actual native present/absent request and setup-byte equality; native authored fixture proving Python observation/return and disabled execution; actual collector planning/cancellation/NULL inventory; exact owner→service namespace and entry through a launch stub. No GPU/services, broad tests, environment mutation, or model-generated-code execution during CPU preparation.

Implementation detail: the private override loads an authenticated engine module into the RLM subprocess's memory through a task-local sitecustomize file; the ACP bridge itself does not receive that PYTHONPATH. Identical runtime-support bytes exist in both arms, and a private environment flag selects inventory. No installed package file is edited. CPU qualification uses a temporary local authored-response transport and actual rootless native processes, not a model service. It verifies an actual optional child call as well as Python execution, terminal full-branch authentication and an unadvertised native tool attempt that retains final=NULL and operational0.
