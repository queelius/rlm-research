# Additive service-binding correction

The first READY is preserved and must not be launched. Pre-acceptance review found that the new
outer service wrapper supplied the free-ID study too early when importing the already-qualified
MNLI wrapper. The inherited wrapper expects the pinned MNLI study and performs the free-ID/service
aliasing itself. V2 binds that exact study explicitly and checks the imported module identity in a
focused CPU test. Scientific contexts, plan, prompts, seeds, scorer and output namespace are
unchanged. `READY_V2.json` is authoritative.
