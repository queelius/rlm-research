# Fresh12-cell vector/singleton replication — MAIN only

Question: does the first screen's fixed singleton advantage survive new stages, applied-change histories and check revisions? This is within-family replication, not a new dataset or learned routing. Original176 audit found10/12 versus4/12 whole-list/vector, six wins at three contexts; all nine matched-vector candidate errors improved except the shared residual low-quality candidate. No old cases or models are selected again.

Exactly12 supported cells in lexicographic `(width,history,checks)` order: widths6/12/20 ×history1/3 ×check revisions1/3. Stage index=i%3; gen202609490000+i; repeat0/1 rootseed202609500000+2i+repeat; scalarseed=rootseed+1000+public candidate index. Width32 is outside the pinned generator's supported range and is deferred with no validator/generator edits. All152 distinct candidates retained;304 scalar calls and24 whole-vector calls=328, producing24 complete stage/seed tasks per arm. Six width/history/check combinations are not causally matched versions of identical data; the cells use different generated roots.

New roots/IDs exclude all existing B05 public manifests, including original176. Public freeze precedes host gold; two solvers agree. No selection by outcome, class balance or check flips. CHECK_REVISION_PROVENANCE records actual contradictions across public revisions without using them to choose data. Model inputs are the unchanged normalization, so check-revision transfer also tests preprocessing; it does not establish that the model chooses latest revisions itself.

Same released Qwen3-4B-Instruct-2507 base, T=.5, four workers, actual total context bound8192, scalar cap16/vector384. Same literal scalar/vector contracts and complete union, no eligibility repair/filter. Every scalar required; missing unknown and malformed-known distinct. Both arms always run regardless of score. No optimizer, checkpoint selection, or adaptive helper policy. Checkpoint calls and raw responses, not weights.

Natural physical input/output/token/latency costs, including repeated policies, are not matched compute. Scalar maximum aggregate output at width20 is320≤384, but different prompts/calls remain a bundled fixed decomposition intervention. Use matched-valid confusions/BA/recall, all24 complete-set denominators, widths and cells; do not pool with original176 or treat repeats/candidates as independent datasets.

Science900/owner1000/external1100. Startup can shorten effective science because the owner retains90 seconds for cleanup. Source reuse patches are exact-hash-bound, count-checked in memory; they alter only two-arm counts/seeds/cells, not parsing, credit, rewards or service math. Inherited raw response hash field is canonical JSON; actual raw files remain saved. Runtime final real dispatch and owned-process release are unchanged.

CPU:

```sh
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/b05-singleton-decomposition-replica-v1/owner.py verify
```

MAIN, inside shared GPU flock/private credential scope and external1100 timeout:

```sh
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/b05-singleton-decomposition-replica-v1/owner.py run --outer-seconds 1000
```

Output `outputs/attempt-001`, refuse reuse. No automatic continuation or retry. Interpretation/promotion requires actual completed fresh outcomes and full costs; no second optimizer or routing experiment is implied.
