# Fresh12-cell singleton replication plan

Approved CPU-only external sidecar; MAIN alone launches. Preserve singleton176 and all sealed sources. Reuse its native transport, scalar parser/projection, semantic metrics and exact V3 lifecycle; only new data/schedule, two-arm accounting and source bindings.

Fixed cells: widths6/12/20 ×history1/3 ×checks1/3, lexicographic order, stage index=i%3. Generation202609490000..11; root decode202609500000+2*i+repeat; scalar=rootseed+1000+candidateindex. Two repeats. Exactly304 scalar+24 vector=328 physical calls,24 stage/seed outputs per arm. No list arm or outcome selection. Width32 unsupported and deferred; no generator changes.

Same released4B/T.5/four workers, scalar16/vector384, total context8192. Science900/owner1000/external1100. All conditions regardless score. Host gold only after public freeze; independent solver agreement. Exact source/ID exclusions, full effective-candidate preservation and checks/history provenance. This is within-family distribution replication, not new-dataset transfer or learned revision selection.

1. Write and run focused RED fixture for actual12-cell schedule/scalar-parent mapping and native HTTP paths; implement thin new bindings/freeze, then GREEN.
2. Freeze actual requests/token audits/CPU receipts/READY; run actual owner.verify; hand off exact hashes/argv to MAIN. No GPU, optimizer, model call, or polling.
