# BA18 selection-only one-step pilot

CPU-only preparation. MAIN alone supplies one GPU, shared lock and1200s external timeout. Run `owner.py run` with the training Python; owner1100s, science900s, one model, no inference service concurrent with training. `owner.py verify` reaches actual imported preflight without GPU. Output is `outputs/attempt-001`, never overwritten.

QUESTION.md freezes the approved scientific recipe and72-reply downstream plan. Train18 is the original flat9×2 released-base native batch; no cp32/SFT weight is loaded. Present-class balanced accuracy avoids rewarding every-ID enumeration solely because eligibility is common. Four pairs/8 actions have nonzero G2 RLOO advantages;10 actions are skipped exactly while denominator18 remains. Binary exact has zero contrast and is not run. No example selection by reward.

The sampled array is the selection action: native tokens intersecting eligible_ids list value, including ID strings/commas/closing bracket, are credited. Structural characters sharing a boundary token are unavoidable and explicitly inventoried; the mask is not gold-dependent. Full native tokens/logps and full HF action vectors are qualified, with exact causal prefixes; only the loss uses the selection mask. The mask credits1845 tokens across the8 nonzero replies. EOS is excluded. This partial-policy/shared-weight objective can incidentally affect ordering or other tasks; it is not an unbiased full-policy gradient. Capped token-TIS2 is the same explicitly biased route, not sequence-IS. Finite support and exact HF gradient replay gates remain; diagnostics do not become accuracy claims.

Fresh seeded rank8/alpha16 zero-dropout LoRA is initialized from configuration only. All B matrices must be exactly zero; A nonzero. Actual pre-step enabled-versus-disabled HF vectors must be bitwise equal before native/HF diagnostics. Expected first-step localization is A-gradient0/B-gradient>0, checked and saved. All native/HF initial probabilities for18 actions are saved; all8 active gradient replays must pass1e-5 token/1e-4 sequence bounds before any Adam step. Clip1, fresh AdamW1e-4/weight_decay0; save initial tensors/RNG, gradients, checkpoint tensors/Adam/RNG, state/binding/commit. Failure preserves zero-step/partial state, never qualifies an incomplete endpoint.

Nine new held stages3/width are already frozen and never queried; exact prefix+384≤6347. New data use stock generator and do not inspect outcomes or alter policies. `EVAL_TASKS.json` freezes36 tasks per model (train18+held18), paired base/cp1, all72 regardless score. The learned-adapter native endpoint must use the already exercised LoRA service path; do not patch the no-LoRA B05 owner. Separate conditional evaluator source/READY is required before MAIN launches readout. `checkpoint.endpoint()` authenticates actual source/initial/gradient/replay/Adam/state/commit/checkpoint, not accuracy.

Two focused fixtures cover actual native array-mask boundaries using the training tokenizer, and a tiny real Qwen3+PEFT zero-B initialization followed by selected-logp loss, exact replay, B-only gradients, fresh Adam and RNG restoration. During CPU prep the width interface required scoped import and the training environment lacked native renderers; both are handled explicitly without installs or source mutation. The tiny model has no actual released weights; the full model's initialization equivalence is therefore a runtime gate, not a prelaunch claim.

CPU caps:20–30min preparation target, no broad compatibility work or training sweep. Stop at the first qualified one-step result and fixed readout; no automatic second update.
