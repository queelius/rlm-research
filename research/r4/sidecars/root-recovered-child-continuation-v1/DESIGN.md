# Recovered child rejection: exclusion-only continuation

Approved parent decision, 2026-09-09. New additive namespace; no V2 source, STOP, export, prior checkpoint, or episode is changed. CPU preparation only; the parent owns any GPU launch.

## Question and smallest treatment

Can the remaining originally planned root-only updates complete when a fully observed unsampled child context-length rejection is treated as an explicit training exclusion instead of identity corruption? This is not a new reward rule admitting recovered trajectories.

Qualifying failure: a completed observable episode contains a request-local, uniquely identified depth1 call to the exact fixed c32de child. Its trace/audit/wire aliases and bound weight SHA agree; an HTTP400 explicitly rejects an overlong decoder prompt against the frozen8192 limit; actual input-token length agrees with the error; no sampled node, success response, completion or action probabilities exists for the failed attempt. All remaining successful calls must pass the original role/native-token/physical-branch checks on a diagnostic projection that removes only unsampled failed calls and no graph nodes. Unknown/ambiguous errors and any missing identity, wrong role/hash/alias or token inconsistency remain fatal.

The whole qualifying episode remains training-null. Its observable strict endpoint reward is retained separately, independent of training admission. In the fixed round04 data, exactly29 previously admitted episodes remain admitted; `ca1caf…` stays null for training and0 at the endpoint, while two unobservable episodes stay null for both. Original exported row fields remain byte-canonically identical; only an `admission_metadata` field is added. A new export and GROUP bind the amendment/source hashes. No round04 reroll or extra seed is authorized.

## Continuation and immutable references

Reference exact committed policies0–3, prior validation00/02, original32 round04 records and their V2 provenance via a frozen path/hash map. Copy/link only immutable prepared export artifacts into the NEW run directory when launched; never place writable links to original stages or alter old source/outputs. Original policy/generation identities, task/seed plans, fixed child, optimizer recipe and selection rule remain unchanged; the outer continuation identity is additional and explicit.

Continue at step4 with the preserved step3 Adam moments, RNG and FP32 adapter. Then run original fresh collection/update steps5–8; validate4/6/8 and combine with fixed prior0/2; choose the earliest maximum strict successes over the same fixed8 validation coordinates; run the original fresh original/selected transfer coordinates. Each actual step commits adapter/optimizer/RNG/state before it can advance. Completed stages may be verified/recovered but no failed or partial rollout is silently retried.

Reuse the original lifecycleV2 owned-process claim/release checks and original loss/optimizer implementation. Explicit wrapper entry points replace only export admission classification/native verification routing and the continuation coordinator envelope. The original scientific recipe, caps for individual stages, sampling, root-only mask validation, TIS guards, exact model loading and optimizer restore stay intact.

New compute budget:10800 seconds (3h) from the future `run` launch, not from preparation. The trainer retains its existing600s load/update cap and immediate completed-step serialization policy. Four/eight/etc worker settings remain the frozen campaign values; no independent concurrency or model-budget change. No GPU launch during prepare/verify.

## Verification and decision boundaries

First reproduce the old round04 fatal classification, then verify the new export's29 admitted IDs and all pre-existing row fields are unchanged, with distinct endpoint0 versus unobservable null accounting. Reject mutations to failed-call identity/status/body/length/completion and to successful native tokens. Authenticate every prior state and the step3 policy; exercise pending-stage planning, native verifier dispatch, no-reroll round04 staging, fresh3h deadline and checkpoint recovery using CPU fixtures/qualified helpers. Hash only actual input/source/checkpoint files, not archival trees. Publish READY last with exact source/spec hashes and parent launch command.

Statistical limitation: the overlong child request is policy-induced; excluding it can censor difficult behavior. Report planned denominators, admitted denominators and endpoint outcomes separately. A later ranked amendment could train on authenticated recovered root actions as strict0, but it is NOT part of this continuation. This run cannot be represented as an uninterrupted unchangedV2 campaign or an uncensored on-policy objective.
