# Frozen executable joint-state pilot

Implements the approved `ideas/2026-09-09-joint-acquisition-reduction-sft-design.md` (SHA326cf25dadcb6bc3afc7f472a8d7f97d31b640d333d308c5cc4e04f946dc6e7d), with the following exact prospective choices. CPU preparation only; MAIN separately accepts and launches. No old SFT/restart/source artifact is edited.

Sixteen same-selected-training-context trajectories contain40 productive root actions/40 real unchanged-c32 child calls,16 actual same-REPL live-accumulator/user-scope reduction actions and16 authentic scalar-observation finals. Four accumulation names vary across trajectories; every width4 later action updates rather than reinitializes the accumulator. Reduction targets contain no map payload; all user/category strings are excluded from mechanism-token NLL. No metadata-error probes. Wrong actual labels/scalars are retained, without rerolls or host gold in teacher construction. A failed/malformed capture stops the complete-corpus path; no selected-subset training.

The first4 fixed training members cover all width×scope cells; the full16 balance width within target and scope. All16 training captures and8 heldout controlled-source captures are made in one unchanged teacher-service phase, before the first4 forward gate. This avoids an extra teardown/startup. The gate is before every gradient, not before the remaining12 source captures; failed-gate acquisition work remains charged. Training4-state diagnostics reuse their actual training source states and are never heldout, never selected by correctness, and never fed back into learning.

Joint loss is .45 mean-of-producer-turn token means +.50 reduction token mean +.05 authentic terminal token mean. Reduction/stop uses .95 reduction +.05 terminal on the same true histories. Trajectories have equal mass regardless of batch count. Four full16-example updates, identical starting low66c, fresh AdamW1e-4/zero decay/clip1 and original qualified rank8 FP32-LoRA/BF16-base load, optimizer state and per-update checkpoint machinery. Child remains c32, unloaded by training. Fixed checkpoint4 selection. Histories masked, only the selected current native suffix/EOS receives CE; synthetic capture likelihoods are not RL behavior probabilities. Token/FLOP exposures and reduction weight differ between arms, explicitly not compute matched. Each gate mechanism NLL is actual executed-reduction-code token NLL, not gradient attribution.

Gate: fixed4, at least3 mechanism means strictly>.1; sum of forward×exposure×3 projected training ≤1200s and each arm≤600s. Load/checkpoint overhead is excluded from that proxy but included in actual bounded stages. Failure is scientific stop, never an epoch extension. After this gate, a remaining-time test reserves2520s for three480+240+120 readout phases and540s for their three service starts. Actual stage delays/releases consume the shared work clock.

Three policies (unchanged, joint4, reduction-stop4) each receive16 free heldout,8 controlled heldout and4 explicitly training-state diagnostic endpoints:72 heldout+12 diagnostic=84. New compositions are single u03 and union u01/u03; operators remain familiar. Eight controlled live-state captures use two accumulator aliases unseen in training, with20 actual child acquisitions. Whole scientific native child requests are compared exactly for source replay; source pre-reduction native root prefixes must match exactly. No old sampled readout/teacher future correction appears in continued prefixes. Root is not forced to reduce or stop. Controlled maps are predictions; semantic gold is opened only for scoring. Same accurate field/map-visibility prompt and unchanged native harness in every policy. Four heldout contexts/64 raw groups remain disjoint from128 train groups but have prior research exposure; do not claim a fresh confirmatory data panel.

Costs:60 physical authored-source acquisition calls (40 train+20 controlled), plus actual sampled free/continued root/child work. Controlled standalone accounting adds each original source acquisition to each policy endpoint:60 hypothetical child calls across24 controlled heldout endpoints. The first4 training-state sources contain10 acquisitions already included in the40; their12 diagnostics add30 hypothetical standalone calls, not30 new physical acquisitions. Every source capture gross time includes its teacher reduction/final; no excluded work is presented as free. Readout physical-call files, native typed/role records and per-episode checkpoints remain retained, including failures. Static code presence and answer/map agreement are not execution-use findings; actual scoped reduction stays explicitly unaudited pending trace review.

Exact owner clock: start at entry, work deadline+5100s, owned deadline+5280s, MAIN outer deadline+5400s;180s cleanup reserve after work and120s outer margin. These are nested, not additive to stage caps. Initial capture collectors share900s, gate180s, each training600s within shared1200s, each readiness180s, each policy free480/controlled240/diagnostic120s; all intersect work. Releases between phases consume the same shared clock. At work exhaustion no next work starts; all84 rows already exist as planned NULL before startup. Owner success denotes orchestration, not84 authenticated model finals. Actual native final-branch token IDs, returned content, route and strict Answer contract determine availability/reward; no last-HTTP200 or trace-is-completed shortcut. Returned malformed final is observed0; unavailable branches/endpoints remain NULL with failure context and separate operational sensitivity.

Lifecycle is the unchanged working an27 `service_wrapper_v2.py`/`lifecycle_adapter.py` path, including actual-wrapper identity, current driver and private credential preflight. Unique local `joint_*` module names avoid ambiguous service/study-provider imports. Exact owner→suite→actual wrapper→real InferenceConfig/config→descriptor generation is CPU-exercised with only process/network/cleanup boundaries intercepted. No GPU, inference service, container, lock, download, sampled-code execution or live queue modification occurs during preparation. MAIN owns admission and final release verification.
