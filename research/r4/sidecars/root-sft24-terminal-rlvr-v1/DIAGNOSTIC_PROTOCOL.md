# Frozen behavioral diagnostic

Endpoint reward and availability remain the collector's strict authenticated
native-final values.  Offline diagnostic review never repairs or rescores them.

For every available correct endpoint, and every available endpoint that acquires
child output, retain coordinate/native/raw hashes and classify separately:

1. genuine requested child request and authenticated returned map;
2. state lineage (complete, partial, overwritten, literal recovery, fabricated);
3. executed operator, target category and user scope evidenced by code plus its
   actual observation—not keywords or a matching scalar elsewhere in the trace;
4. displayed scalar linked to the genuine native final;
5. dataset correctness, observed-map agreement, and task-faithful execution;
6. zero-gold/best-constant-compatible or other coincidental correctness;
7. acquisition, reduction, stopping/error recovery and all physical costs.

Sampled code is not reexecuted.  Host labels may evaluate an authenticated
observed map only as a labeled diagnostic and never enter prompts, reward, or
training.  Multiple maps are not conveniently unioned unless the executed root
state actually retains that union.  NULL, HTTP/provider failure, incomplete tool
branch, malformed final, strict zero and unstarted work remain distinct.

Report paired unchanged/RL results on all 48 planned tasks and by context,
operator/scope, primitive/composed, zero/nonzero, availability, faithful execution
and coincidence.  This protocol is prospective to the new campaign outputs but
outcome-informed by the sealed dose retrospective and composition audit.
