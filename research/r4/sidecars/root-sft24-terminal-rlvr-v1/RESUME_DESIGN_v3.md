# V3 bounded non-reroll continuation

MAIN approved CPU implementation on2026-09-10 after V2's release-only failure. No GPU/launch authority is granted to the author. V1/V2 source/seals and attempt-002 remain immutable; V3 writes only attempt-003. runtime_port now authors V3, so question_cards/MAIN independently review it; earlier independent V1/V2 review and prospective audit methods are retained with this authorship change disclosed.

V2 collected exactly24 original window1 slots, exported a clean complete inventory and13 mixed admitted episodes, then timed out during release before any trainer command or checkpoint existed. Actual native replay succeeded: GROUP SHA `0d63be9c9ab89a05d55332f536bd17819c1eebfa4416c486af9e11d51e8fa431`, MANIFEST `a38d0fd5da7c3adecde393c64972d1830e02a7f662b0f0dd031c15ede1db9176`. V3 authenticates and reuses these exact read-only source paths plus GENERATION; no copy, reacquisition, changed reward/admission, reseed or success-only selection. First action is the qualified first RL update from the original fresh-Adam0/SFT24 state, followed by original windows2–8 as time permits. Window1 consumes its original cursor after update/noop. Original192 denominator includes24 reused physical slots and at most168 newly attempted; original96 paired final tasks/seeds may run only after proving V2 has no readout dispatch evidence. Last actual committed checkpoint, including zero, remains the selection rule.

Exact original SFT24/c32/base, current native interface, terminal reward, TIS/PPO numerical code, masks, optimizer recipe, freshAdam0 and checkpoint/RNG semantics remain unchanged. No automatic further resume is authorized. A checkpoint committed before a later failure is retained and consumes its window once; no replayed update. Primary V2 inventory and its unrun96 final NULLs remain historical artifacts, while V3 is a linked continuation/completion view rather than overwriting them.

Combined active execution cap is10800 seconds: charge ceil(original parent380.0108866)=381; V3 outer10419/work10119/owned10299. Training-side5019 and protected final5100; startup≤180, collection≤600, training process≤480 with unchanged internal240 load/optimization deadline, release≤90. A new collection window requires≥1380 remaining training seconds (180+600+90+480+30). Two final policy blocks remain2550 each, including startup and90 release; collection is limited by block-end minus90, never steals the other final block. Each stage advances early. Stopover/idle time is excluded from this active-execution cap but explicitly recorded as total calendar gap/cost, not hidden.

Retain the qualified authenticated stop sequence: launcher SIGINT10/TERM5/KILL5, service SIGINT30/TERM20/KILL10. A90-second inclusive release allowance permits that sequence plus overhead. Do not shorten grace silently or bypass process identity/port checks. The owner retains the original active-service pointer until release succeeds. On failure it must not start another service or trainer; bounded cleanup may retry the same owned service only within owned reserve, preserving errors. No unrelated processes are signaled. MAIN termination halts subsequent phases.

V2's original COST_LEDGER erroneously searched physical folders absent from QSR capture. V3 adds role-audit embedded native-wire accounting keyed by stage+requestID. Request/result pairs and typed mirrors are one call, not multiple. A recorded HTTP response establishes a physical attempt; a routed request alone is unresolved dispatch, not a returned completion. Native completion counts require HTTP200, choice/native token+logprob and usage agreement. Preserve known usage and per-field unknown counts, all raw hashes and failures. Combined science ledger references immutable original window1 and new V3 records without double-counting. Original V2 zero-cost receipt is preserved and explicitly identified as defective.

CPU qualification is narrow: replay original complete24/export and actual TRAIN --preflight (no model load or gradients), remaining-budget/source/namespace/no-earlier-readout guards, real qualified stop escalation under fake clock/process signals, prevention of next-service/training after release failure, role-wire accounting, and unchanged owner→collector/trainer argv. No full recapture/resampling fixture or GPU execution.
