# Literal-input inspection: one generic root instruction

Approved question: can the fixed cp32 root inspect actual request strings before exact matching, instead of reconstructing wording from the final question? Append only: “Before exact matching, inspect the actual user-request strings in the supplied document. Use the observed spelling and grammar rather than reconstructing request wording from the final question.” No email-specific hint, example, gold, precomputed request catalog/index, selected source list or question-prefix change.

All8 existing fresh contexts × the same4 seed integers202609250000..202609250031. Same original documents, checkpoint32 adapter, zero-LoRA child, T0.5/top-p1/top-k-1/min-p0,2048 tokens/action, six TOTAL root+child actions and observation limits. Same narrow terminal-strip-disabled hooks; not a generally lossless parser. Four question workers; science900/owner1100/external1200 seconds. No retries, alternate wording, source selection, checkpoint selection or training. MAIN alone launches.

Preparation authenticates the completed fresh8 control and its independent32-episode audit. All cached control scores remain7/32 raw exact. Every new root task is old prompt plus twoLF and the single instruction. Only task/coordinate name metadata changes in addition; all record/ordinal/seed/sampling/source fields are paired unchanged. Re-rendered old prefixes must equal the actual frozen control prefixes before new expected prefixes are sealed. The actual environment fixture checks all32 tasks and one full native HTTP-double/parser/ACP final against the new expected prefix, including preservation of genuine terminal spaces. No model inference or generated tool code is executed in this fixture.

Same qualified shallow collector/owner/role/checkpoint source is imported with explicit new study bindings. The defining study module—not only exported attributes—uses the new inputs, taskset and schedule. Native learning evidence, every tool program/observation, raw final, causal mapping, root/child costs, truncation/errors and first-prefix checks are retained unchanged. Cached source/control artifacts are read-only. Public model inputs contain no new source catalog, answer or gold; host gold is used only for existing scoring after rollout.

Primary is raw exact and all8 G4 vectors, with unknown separate from wrong. The post-terminal inert mechanism audit must determine whether an earlier tool observation actually exposed literal user request strings, whether later matching uses those strings, whether target stdout is clean, and whether final copying remains exact. Do not infer actual inspection merely from a different code constant or a correct answer. Retain all32, including already-successful retrieval controls. Preserve raw code/observations for ambiguous cases. No generated code is executed by the analyzer.

32 new trajectories, variable physical root/child calls within unchanged caps. Inspection may consume another turn; record actual cost rather than claim token/call matching. A retrieval gain without final exactness is not a copying gain. If the root repeats the guessed template, preserve that failure and do not sweep prompts over these exposed8. This is a task-interrogation/harness intervention with shared MRCR framing, not a new recursive algorithm or confirmatory generalization claim.

CPU verification after READY: run the scoped native Python on owner.py verify. READY.fixed_argv is for MAIN’s externally capped queue only. Do not launch while preparing or reviewing.
