"""Unchanged exact cp32 adapter/Adam lineage qualifier."""
import study
source=study.load('literal_inspection_checkpoint',study.PARENT/'checkpoint.py')
RECEIPT=source.RECEIPT
verify_checkpoint=source.verify_checkpoint
binding=source.binding
