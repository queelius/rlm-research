# Preserved CPU preparation evidence

Three initial ledger tests and one dispatch test failed because their implementation modules did not yet exist. A test's event index was corrected to select the child-result event rather than the new initialized event. A wrong-working-directory unittest command failed discovery; the corrected discovery command passed all4 tests. No scientific episode or model request occurred.

qualification-001 completed successfully in46.226 seconds with12 synthetic native requests. Before freezing, an additional focused assertion found that an unmatched-only delivered child would expose a zero-coverage summary. The intended first eligibility is a source-bound strict returned map, so observe now requires an eligible delivered event. This makes unmatched-only children observation-identical to control. The failed test is preserved here; no outcome selection occurred. qualification-002 reruns the same small scoped native fixture after this narrow fix and also explicitly compares every ledger raw answer byte-for-byte to native response content. Both fixture slots are retained, with no GPU/model calls, image rebuild or shared-store configuration mutation.
