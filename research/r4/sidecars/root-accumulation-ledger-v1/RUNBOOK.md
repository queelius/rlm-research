# Global16 ledger screen

CPU preparation only. MAIN must independently accept the sealed READY and provide one GPU and an owned native-service credential. This sidecar does not authorize a launch.

From this directory, exact future argv:

`/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-accumulation-ledger-v1/driver.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-accumulation-ledger-v1/outputs/attempt-001`

Outer operation cap1800 seconds; shared work1650; owned1770 including120 normal owned cleanup. Service startup at most300 within shared work, collection1200 within shared work, no resets. Four pair units concurrently, sequential map/ledger within each pair; no study retry or replacement episode. Inherited nano transient transport retries remain possible and count as physical cost.

Fixed final4 root efab and learned child c32de. All16 planned episodes on four exposed validation global contexts, two fresh paired seeds. No user filtering or new selection. Both arms have the exact same root task/prompt, ordinary tools, public request builder, source-bound typed child grammar and symmetric immutable local image. Matched seeds do not guarantee deterministic counterfactual first actions. Full policy/harness comparison; the extra tool observation can alter later planning, code and child calls.

The supervisor snapshots exact public ID/text records before sampled root actions and keeps the ledger in its own memory. Successful broker delivery commits source-bound strict maps; raw answers and broker payload schemas are unchanged. Same-label repeated IDs count once; conflicting labels are permanently excluded from the six-label histogram. Counts are child predictions, not truth. No target lookup, gold, automatic final answer or per-user table exists in ledger state. Partial coverage/unqueried/conflict counts are always explicit.

The root observation first undergoes the unchanged original truncation. Treatment appends at most2048 characters after that; both arms log the actual observation, retaining original raw content in a separate audit. A summary appears only after an eligible delivered map. Root-only suffix is part of physical native input; it is not a hidden response edit, child return replacement, sampled model action or action with a behavior likelihood. No training occurs.

Runtime config and audit files are root-writable, so this is not an adversarial integrity guarantee. Input config is snapshotted before root execution; auditor must bind child/parent invocation, exact source request and native completion, then verify actual root exposure via graph/native input. Audit files alone do not establish consumption. Shadow audits exist in both arms. No deterministic operator program/config is provided to the free root; only ledger-config/public records/request builder plus ordinary inherited task files.

The local cache under /tmp/rlmc.0m4242 is ephemeral, owned by the current allocation, and validated against frozen runtime-local-cache-v1. Missing store is a failure, no fallback. New runtime/cache namespace is owned by this study. Image8cfe and existing stores/configuration are not rebuilt or edited.

After terminal and owned release, audit all16 planned strict whole-ASCII Answer endpoints, completed empty/wrong0 versus unavailable NULL, raw observable versus graph-valid/finalization errors. Report8 seed-pairs within4 contexts, helper delivery/coverage/conflicts/physical summary exposure and conditional final-to-child-count fidelity separately from host-gold accuracy. Count every physical root/child attempt, including errors/retries; decode raw wire response JSON-string bodies for cached tokens. Do not replace missing results, require helper use for reward, or select a different root. Independent primary review remains required.
