---
question: Does the fixed 32-update continuation make the unchanged procedural task executable, and does it transfer to the already-declared held16 panel?
status: CPU_PREPARATION
gpu_launch_authority: MAIN_ONLY
---

# Fixed checkpoint-0032 procedural-SFT readout

This is a dose continuation, not checkpoint selection. It reuses the exact procedural teacher corpus,
objective, prompt, learning rate, absolute-step ordering, and the original evaluator's immutable train32
and held16×2 coordinates. The trained arm is exactly committed checkpoint-0032 after resuming checkpoint-0004
with Adam and RNG state. The base arm remains the same mathematically zero LoRA transport and both arms retain
the same released child.

The train32 readout is first. Held evaluation opens only if all32 train endpoints are scientifically available,
at least8 are raw exact, and exact answers span at least4 source contexts. The held comparison is then fixed base
versus checkpoint32 on the same 32 coordinates and paired seeds. No intermediate checkpoint is queried and no
failed checkpoint4 artifact is overwritten. These held contexts were already exposed to the separate procedural
SFT evaluation design, so this is an exploratory transfer readout rather than pristine confirmation.

