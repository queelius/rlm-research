# Verified wrong-route scoring amendment

MAIN review requested this additive change before acceptance. Original READY9c799679477311fe0dde2543fdf3609aece787e3fc80d4943b8aed86cc8dbb95 and every original member remain unchanged. All prompts, native IDs, seeds,24 planned calls, source data,900s cap and fresh outputs/attempt-001 namespace remain identical.

An actually returned assistant tool route can be a model mistake even though this task advertises no tools. V2 validates actual model, unique response branch, exact prompt IDs, completion IDs/counts and usage first. Parsed function name/arguments and surrounding assistant text must reconstruct the decoded native tool envelope, following the existing free-ID audit boundary. Native EOS is removed only when its known ID is actually present. A fully authenticated wrong route becomes observed available invalid0, with wrong_route explicitly recorded and no execution. Inconsistent tool envelope/token/text/model identity remains NULL through the unchanged collector error boundary. Ordinary stop/length scoring stays unchanged: capped valid answers can score1, capped malformed outputs0.

owner_v2.py and collect_v2.py apply counted transformations to the pinned original owner/collector and substitute scoring_v2. The amended owner and collector both accept only collect_v2.py and the same exact new rollout path. READY_V2 binds this amendment plus original READY; the old READY is preserved, not overwritten. Two focused tests cover native wrong-route0 versus mismatched-envelope rejection/NULL, and owner→collector V2 path composition rejecting V1 collector path. No GPU/service/lock/model call was used.
