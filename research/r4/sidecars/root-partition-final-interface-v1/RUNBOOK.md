# Partition final-interface v1

MAIN approved all12 inputs×2decoders and900s outer cap before implementation. This is a CPU-prepared24-call backup, not launched by its author. The original partition pilot, its sealed audit and corrective-SFT sources remain untouched.

Question: does syntax/public-customer-domain-constrained final decoding reveal correct joins on complete-evidence/direct source inputs after the pilot's cap/prose failure? Eight actual parent-full messages retain original records AND unchanged actual extracted reports; four direct messages are deduplicated by exact historical native prompt identity. This is not a blind communication, autonomous recursion or training experiment. All four already exposed engineered worlds are included, not just previously successful outputs.

Both paired calls share exact original system/user content, fresh seed981360001..981360012, max2560, temperature0.6, top_p0.95, disabled top_k, repetition_penalty1.0, and common released cdbee Qwen3-4B/noLoRA vLLM path. Bodies differ ONLY in structured_outputs. Native schema is array of string enums c01..c12, with no cardinality, uniqueness, sorted order, gold or repair constraint. CPU qualification checks schema acceptance of empty, singleton, duplicate, unsorted and all-customer arrays. The original strict ascending/distinct whole-JSON parser remains scoring-only. Actual historical and prospective native prompt token arrays are retained; every actual service response must return the matching prompt IDs plus valid completion IDs/text/usage and bound model.

An actually returned verified length-capped output is observed, NOT infrastructure NULL: score a complete strict answer normally and malformed text0, retaining finish_reason/length. Missing/failed transport, unverified actual native identity/final branch or unreturned endpoints are NULL. No embedded-array extraction, sorting, deduplication, answer fallback or retries. Raw wire bodies, response text, model token IDs, usage and latency are retained. No model-generated program is executed.

There are24 new final calls and zero new child acquisitions. Exactly24 historical extraction calls are reused once across8 full source states. Each full endpoint's hypothetical standalone pipeline receives all3 relevant acquisition calls, giving48 hypothetical acquisitions across16 full endpoints. Direct gets none. Physical new, historical unique and hypothetical repeated acquisition cost remain separate. Summary reports strict cells and paired outcomes with four source-world clusters; eight full states are not eight independent worlds. HF→vLLM and increased parent cap relative to history are descriptive differences, not independently isolated causal cap effects.

MAIN owns GPU/lock/acceptance. Exact owner argv is in READY: native Python, new owner.py run, new outputs/attempt-001 only. Private STRICT_RLM_CALIBRATION_API_KEY preflight precedes output creation; credential values are never argv or reported. Existing free-ID service_wrapper_v3.py, lifecycle_adapter_v3.py and base config are reused unchanged, keeping actual wrapper/source identity symmetric for claim/start/release and current580.159.04 libraries. No inference runtime container is needed for these fixed one-turn requests.

900s outer =780 work +90 cleanup +30 parent margin. Service readiness≤180; collection≤600 clipped to the shared clock, four request workers,120s per-request timeout. No reroll, extra child, silent continuation or old-output reuse. Partials/NULLs are evidence. CPU_READY certifies inputs, native tokenizer/grammar, request/scoring/namespace/source contracts only; actual provider calls remain pending.
