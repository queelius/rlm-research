# Fixed-dose readout implementation

Implements only the already approved evaluation in immutable `root-operator-dose-continuation-v1/inputs/EVALUATION_PLAN.json`; no coordinate, seed, source, query, policy order, checkpoint selection, or cap changes. Training READY76f1462596352a423546fe3f338d78247960d7a298871681244e04325b496191 remains immutable.

- [ ] Reuse exact free native collector with explicit module aliases and new input directory. Each of two serial root+c32 service phases reads48 full native episodes, same4workers/180s/2048action/8192context/.5temperature/1top-p. Keep original and root-new strata separate, all NULLs and actual physical attempts/completions.
- [ ] Validate fixed6 directly against training-ready pins and fixed24 against all18 committed checkpoint links and final RESULT. No partial24 substitution. Freeze current code before checkpoint24 outcomes; actual future24 artifact pin receipt is generated at owner entry after authenticated completion.
- [ ] First-action probes use pinned original first native teacher request bodies, changing only selected model alias and fresh planned seed. Twelve per policy, no tool/program execution or synthesized answer. Preserve raw response, native tokens/logprobs, parser statuses and syntactic producer intent as diagnostics, never label this actual acquisition. Full free traces separately establish executed acquisition/reduction.
- [ ] Readout6480outer:6300work,150owned cleanup,30margin. Two phases at most3000 each including180startup,240first-action probes,2490fullcollection,90release;300harvest. Advance early; no phase/time extensions or rerolls. Combined training4320+readout6480=10800 maximum reserved seconds. Policy order fixed6 then24 (explicit temporal confound).
- [ ] CPU actual owner→qualified service/interceptedPopen and collector entry tests; authored free native file-read→child→reduction→final fixture; direct first-action native-token fixture, malformed-token rejection, and no generated-code execution in probes. Validate all prepared task files/prefixes equal across policies and old exposed prompt bytes unchanged.
- [ ] Separate READY source closure and input seal. MAIN alone accepts/launches. No source mutation after publication; no training source or old seals changed.

Source metadata clarification only: prior qsr task metadata calls records root-catalog-new, which is not true for this new prepared-catalog-exposed pool. New readout metadata will describe the exact exposed/root-unexecuted scope, without changing any frozen model prompt/file bytes. All actual task behavior remains the approved common package.
