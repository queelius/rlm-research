# Terminal-strip-disabled: one matched held32 cp32 arm

Question: do exact model-emitted final answers survive the reward-facing parser and ACP root boundary when only their outer whitespace trims are disabled?

The condition changes exactly `parse_qwen3` final `content=text.strip()` to `content=text`, and successful ACP root storage to the untrimmed typed `ACPTurn.reply`. It is installed only inside this collector process and restored when its context exits. Trusted dependency bytes are hash-pinned. No shared clone, environment, original owner, or original score is changed. Reasoning extraction (including `after.strip("\n")`), stop/tool boundaries, tool JSON validity, historical rendering, marker text, and scorer are unchanged. This is **not a general lossless parser**. Extra model whitespace and altered punctuation remain incorrect when they differ from gold.

Use the authentic fixed resumed cp32 with the identical unchanged zero-LoRA child binding from the completed held arm. Reuse all16 held contexts, both original seeds per context (2026091700–2026091731), exact physical initial prefixes, temperature0.5, max2048tokens, six total root+child turns, four workers, no retries, and the same engine/service source. One additional32episodearm only; science600s, owner900s, external1000s. Retain all episodes and unknowns. No SFT, optimizer updates, checkpoint selection, or gold-dependent output repair. The old train24/32 and held17/32 raw-exact scores remain frozen.

CPU preparation verifies the previous full authentic cp4→32 Adam/RNG lineage once, pins that receipt and the current cp32 commit bytes, and authenticates the completed baseline's exact binding, coordinates, and all32 initial native prefixes. The execution owner and collector recheck immutable closure and current fixed commit bytes without repeatedly walking28 heavy prior checkpoint states.

The full-native fixture uses the actual inherited environment, model response parser, typed train client, native ACP agent, host ACP root storage, and unchanged raw collector. Only the token-producing HTTP model endpoint is faked. The fixed authored ordinary IPython call prints `CPU_TOOL_OK`; no generated code is executed. Cases cover exact spaces/newlines/curly apostrophe, genuinely extra final linefeeds, apostrophe corruption, and malformed tool JSON. The unmodified-path failure is retained under `cpu-red-001`; final passing evidence is under `cpu-green-002`. The reasoning/tool unit fixture and context-restoration regression qualify the deliberately narrow condition.

Run CPU seal only after `CPU_TESTS.json` exists. `CPU_READY.json` records the exact launch argv. MAIN admits the new arm after an independent focused review. Afterward compare every coordinate, initial prefix, native action-token array, final token text, parsed content, and scored root against the frozen old held arm. Matching seeds do not guarantee matching samples; report actual token/program differences. Do not replace the old primary metric with the diagnostic25/32 bare-token exact inventory.

The companion grouped collector may reuse `hooks.installed()` only after authenticating this sidecar's hook/source hashes and CPU fixture receipt, and must label the condition `terminal-strip-disabled` rather than generally whitespace-preserving.
