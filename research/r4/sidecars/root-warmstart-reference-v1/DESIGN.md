# Three-root warm-start reference24 — approved CPU implementation

Implements MAIN's approved `../../ideas/2026-09-09-root-warmstart-reference-feasibility.md` SHA cd287fea5b69376e9ca7e73406a0edfd88db89c90dcac07b6f255b4aebd3f102, with its exact source/group JSON SHA ec149095c7b00af6ce8a5d73c17fbba2770b0152dbb76f2c26fafc4d3dfaf368. No new base/adapter protocol: all roots use the unchanged qualified two-LoRA service/routing with c32 child.857 is mathematically zero-effect (all252 B tensors zero, no extra tensors/bias/modules), accurately labeled released-weight reference through zero-effect adapter, not bitwise bare-base transport.

Policies are exact857/efab fixed4/low66c fixed8, not selected by this panel. Existing64 records across readout00–03 remain byte-identical and file-only; all are child-SFT-exposed and already QSR/research-exposed. Eight paired blocks: count-union each context plus distinct-all in00/02 and weight-single in01/03. One fresh paired seed/block981456101–108, master981456001. Root phase and common block order are hash-fixed before readout; serial service order is a nuisance, not independently replicated. Four context clusters, not24 independent outcomes. Alternative32 is not implemented.

Common role is the unchanged current native coding system. Same record IDs/users/text/weight, optional tools, unchanged typed grammar for recognized native child requests; root has no grammar, extra map, algorithm example, counting helper, forced batching or child call. The only task wording amendment is replacing leading `Among records belonging to all four users,` with `Across all records, regardless of which of u0, u1, u2, or u3 owns the record,`; other question bytes and host-only gold remain fixed. All native task files, first messages/tools/tokens and actual root2048 action cap are authenticated;8192context,.5temperature/full-support inherited QSR sampling. Zero/bestconstant distribution is computed prospectively over the frozen eight blocks, never used to select/resample.

Exactly24 planned NULL slots precede startup. Four workers,120s/episode. Shared work1650/owned1770/outer1800 seconds from execute entry. Each of three phases has≤480s inclusive startup+collection, with startup≤180 and collection≤300 and both bounded by phase/work remaining. Each release≤90, bounded by owned; no hidden timeout extension. Nominal3×(480+90)=1710, leaving60s to owned1770;1800−1770=30s outer margin. All collection stops by1650; final release/harvest may consume remaining owned cleanup. The collector reserves15s from its already-inclusive collection deadline for closure. Setup/verification/load/services/collection/releases/harvest are charged. Missing or interrupted slots remain NULL, no reroll. MAIN termination stops later phases; failed release stops successors and reports active ownership. Phase failure may leave its slots NULL while later preplanned policies are still attempted if released and clock permits.

Primary strict whole native `Answer: N`: completed wrong/malformed/empty0; unauthenticated/missing native finalNULL; operational success0 for any nonreturn. Full branch and raw native transport remain retained. Independent audit must report context-paired differences, availability/bounds, nonzero-answer results and actual file/child/acquisition/reduction/observed-result→final flow, not code mentions. Physical attempted transports, returned raw completions, parsed completions and pretransport rejection are distinct; unknown usage/billing stays unknown. No supplied-source acquisition or training cost is charged as new work.

Reuse pinned QSR task/renderer/native interface and metrics full-final-branch matcher via isolated names; an27 owner/service/lifecycle/private credential remains unchanged. CPU qualification only: focused input/binding/score/cost/clock/termination/actual owner→wrapper→collector checks and one authored actual-native3-call fixture. Fixture code/maps/logprobs are explicitly synthetic qualification, never science/training. No GPU/service/queue authority is delegated to implementation. MAIN reviews READY and alone accepts/launches. Audit record-interface immediately when MAIN relays it.
