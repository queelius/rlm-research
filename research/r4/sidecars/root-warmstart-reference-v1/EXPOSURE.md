# Preparation exposure and zero floor

The eight blocks were fixed by MAIN before this sidecar's implementation. Input preparation found gold histogram0:4,2:2,3:1,5:1; always-zero and bestconstant both4/8 per root. No source selection or resampling followed. Report the four nonzero blocks separately; zero-only gains are not evidence of acquisition/reduction.

During CPU qualification MAIN relayed sealed QSR trained4/48 versus unchanged2/48, all four trained successes zero-gold and available36 versus46. This was aggregate exposure only, not an independent audit by this implementer. It caused no change to roots, blocks, role, question wording, seeds, clocks, scorer or source contracts. CE outcomes were not inspected. The initial idea's timing statement that trained final was unavailable at task assignment remains historically correct.

No fresh-data or globally-unseen claim: sources are the four specified exposed QSR readout contexts. Fresh seeds are sampling repeats, not independent source replication. Only four context clusters support descriptive paired comparisons. Baseline857 is zero-effect inference LoRA, not bitwise proof of bare-base transport equivalence.
