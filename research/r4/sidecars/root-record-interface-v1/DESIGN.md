# Accurate record interface24

MAIN approved implementation 2026-09-09 from `ideas/2026-09-09-native-join-record-interface-design.md`, SHA256 `2b001983464cbbe0f1c73902b033c3afc6212030e5ca7f149976cc01854d8830`. Earlier design-only authority is superseded for CPU preparation only; MAIN retains GPU/service/lock/queue and launch authority.

24 new roots: all four exposed nativejoin worlds × two fresh paired seeds × T/F/I. T: sentence inline and sentence file. F: sentence inline and JSON object file. I: JSON objects inline and file. All original facts, order, domain and query identical within each block. Formats described accurately. No extraction, omitted facts, query-conditioned serialization, algorithm, initialized answer-bearing variables, output grammar, forced tool use or historical source-gate repair. Same physical tool inventory in every arm.

Released Qwen3-4B-Instruct-2507 cdbee75f17c01a7cc42f958dc650907174af0554, no LoRA; optional children same base, qualified native/free-ID vLLM runtime. Root thinking enabled; temperature .6/top-p .95; 2560 tokens/action; 8192 context; unchanged native optional-child behavior. Exposed four-world mechanism panel, not held-out competence or 24 independent world replicates. Fresh paired seeds and balanced six-permutation order; no rerolls.

Primary F−T: typed-file offer. Secondary I−F: immediate typed visibility. Format accuracy and tool availability held constant, not independently estimated. Truthful format metadata and token lengths necessarily differ; no byte-only or equal-FLOP claim. No direct causal comparison with prior inaccurate/ambiguous interface percentages.

Budget: 1080 work inclusive startup≤180; roots/collection get remaining shared clock (root dispatch reserves30 for final collection). 1170 owned includes90 cleanup;1200 outer includes30 margin. Four workers;120/episode. No acquisitions. All24 planned NULL slots retained. Actual native final identity/branch required; returned malformed completion0, unverified/missing finalNULL, capped strict complete answer scored and length flagged. Episode completion does not establish model final availability.

Costs mean actual physical requests, observed tokens/time and separately host serialization/setup. Provider billing is unknown. File load, typed decode, faithful source literals, executed grouping/intersection, intermediate result and final concordance require independent code-plus-observation audit; no AST-only mechanism success. Every program and observation retained without executing sampled code in analysis.

Implementation reuses immutable nativejoin sources with narrow pinned namespace/count/budget adaptations. Focused CPU checks cover all fact/order bijections, actual native three-arm files/prompts and optional child routing, final authentication, all-NULL failure inventory, and actual composed owner/service/config/intercepted-launch CLI. No new framework or mutation of old sources.
