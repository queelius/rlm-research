# CPU preparation and GPU handoff

1. Add focused failing tests for trusted depth routing after global override,
   per-request isolation under concurrency, actual recorded alias equality, and
   root/leaf prompt preservation. Implement only these seams and pinned nano overlay.
2. Add a twelve-episode driver using identical definitions prompts, paired seeds,
   explicit immutable role map, old rootless task/runtime behavior, and atomic records.
3. Qualify the new rootless installation and forwarded root/child exchanges with a
   deterministic CPU fake provider, no GPU and no untrusted host execution.
4. Provide source hashes, prepared design/coordinates, exact service config changes
   and launch command. A trained adapter may be bound at launch only by already-frozen
   validation selection; record binding separately rather than modifying a used spec.
5. Parent starts dual-adapter inference only after SFT exits and GPU ownership is clear.
   Launch this queued comparison immediately after readiness. Analyze while the
   independent48-episode composition comparison uses the same service.

All work is within this sidecar or a new external source/cache directory with provenance.
No GPU request or service launch during CPU preparation, no unrelated tests/refactors.
