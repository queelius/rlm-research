# Indexed-output mixed-size child SFT

Approved bounded exploratory intervention,2026-09-09. The question is whether training the child with an explicit input-ID/output-label map improves correspondence under that interface, while preserving anonymous small-batch skill. The completed indexed inference control405/512 versus anonymous181/512 motivates this job; it does not establish this training effect or a full-RLM gain.

Use the exact authenticated B curriculum allocation:5,065 training groups once per epoch, two epochs, sizes1/5/16/64 with the same bucket quotas, group order, batch order and residuals. There are1,621/1,620 arrays and204actual optimizer increments if complete. Start from original converted LoRA857a7ce6907c759a8c1b478eb53029d3b700b81c3094d8edb3394e52def9fcb6, not old SFT or B. Frozen BF16 base, original FP32 adapters; LR1e-4, AdamW weight decay0, gradient clip1, microbatch2, accumulation up to16arrays, token-normalized assistant suffix loss including native terminator. No truncation, schema-constrained training or child/root RL.

The only intended training intervention is representation and its necessary instruction: each input is a JSON map of batch-local q0001…qNNNN to question; target maps each ID to the authoritative canonical label. IDs reset per batch, so they are not stable source-question keys that could be memorized. Use the completed indexed control's wording. Remove the contradictory array instruction completely; retain native generic system, tools, definitions and exact prior-training tool serialization. The model must emit IDs itself; there is no grammar during SFT.

Reuse the pinned existing mixed train loop in a private import. Only owned prepared-input binding and representation-aware evaluation callbacks are supplied. Its actual loss, optimizer, resume cursor, adapter tensor audit and checkpoint writes are unchanged. No frozen source files or live service modules are edited. Checkpoint every16steps and epoch, with adapter/config, Adam, RNG, exact cursor, source/input identity and token/gradient metrics. Fixed final epoch2, never validation-selected; both representations of the same300 validation groups are monitored each epoch.

Final readout: both anonymous and indexed fixed5 versions of the same489 test groups, plus six reused64-record test compositions in both representations. Three weights—old c32de, fixed-final B59ad8542, and new fixed-final indexed—receive identical prepared prompt IDs within each cell. Natural HF greedy output only: cap256 at fixed5 and common3072 at64, batch2. No grammar, repair, alias fallback or partial-prefix primary score. Indexed grading rejects duplicate, missing or extra IDs/noncanonical labels and maps valid values by exact ID; object key order is measured but not confused with semantic correspondence. Report strict complete-array yield, item/class precision/recall/F1, quartile correctness, missing coverage and truncation separately. Comparison to historical anonymous/native calls is not pooled; this is a fresh matched rendering comparison.

The204 training steps see10,130 record exposures but more ID/target/prompt tokens than B. Actual target tokens, padded/unpadded input, update count, adapter delta and precision are retained; this is not compute-matched training. The60-minute accumulated optimization cap is separate from the90-minute overall attempt cap, which includes load, validation, final readout, comparison-weight loading and checkpoint time. The outer launcher owns only its newly spawned training process group, preserves partial outputs, and allows30seconds cleanup grace. Resume keeps the original overall deadline, not a new90-minute allowance. Parent alone assigns and launches the A100.

Preparation uses no GPU/model requests and no data acquisition. Frozen source partitions and all loaded source/model/checkpoint hashes are authenticated. Existing public TREC licensing, historical exposure, repeated-test-composition and pretraining limitations remain. The broader fresh-permutation/SST correspondence test is owned separately; this job does not claim that its decision gates already passed.
