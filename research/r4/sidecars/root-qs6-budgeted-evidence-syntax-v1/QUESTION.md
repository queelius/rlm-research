---
title: One syntax example for the budgeted evidence interface
date: 2026-09-12
status: frozen_prelaunch_question
---

# Does one syntax-only example repair protocol cold start?

Compare the unchanged budgeted `classify`/`finish` prompt with the same prompt plus one generic
syntax-only call/return illustration. The illustration uses placeholder variables explicitly marked
as not being task IDs. It supplies no real record ID, label, subset, operator, answer, stopping rule,
or host truth.

Both arms use the same 24 research-exposed training-context tasks, authenticated saved batch-4 maps
(including wrong labels), fresh paired seeds 202609123000--202609123023, root checkpoint, action set,
four-call/16-label limit, generation settings, and runtime caps. This is an interface-compliance
screen, not generalization, helper acceleration, or architecture promotion.

Primary outcomes frozen before GPU use are finish-consistent strict `Answer:N` finals and rejected
API actions. Wrong strict finals must remain visible. Accuracy, logical labels acquired, physical root
calls/tokens, unknown usage, and static bypass flags are secondary. The modified prompt's rendered
token prefix is frozen and checked against the actual first native request; the inherited exporter is
not allowed to substitute an older prompt truth.

