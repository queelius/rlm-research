# Prospective common local-runtime amendment

Parent authorized this separate variant after original SFT READY397f3ccc was sealed. Original sources,32 rows/four updates/fresh Adam/final4 selection and48 paired tasks remain unchanged. Both future readout weights use exact image8cfe and the qualified local VFS wrapper5c4a8d…/CPUs34,35, with separate stage cache paths under/tmp/rlmc.0m4242. Trainer command and device placement are untouched. Inherited serving, clocks, cleanup, workerqueue, typed matcher and scoring are reused without scientific deltas. No new model calls, fixture or data allocation.

`adapter.py launch` privately loads the frozen launcher and rewrites only its evaluator argv. `collect` privately replaces the typed interface runtime callback while retaining its hooks/plan/catalog construction. Actual new argv and per-stage LOCAL_RUNTIME records make the amendment observable; original RUN/INPUTS also retain their correct original scientific identity. Missing store/UID/image is fatal, not an implicit persistent-store fallback.

Qualification is explicitly composed: local runtime proof has3/3 exact prompt tokens/aliases/sampling versus the original image; original SFT typed-newcatalog proof has2root+1child calls, exact native root prefix and child schema. Neither is behavioral training evidence. No general speedup established by the single69.24→22.30s fixture.

Four root slots remain unchanged. Conservative VFS estimate1.34GB base+4×1.685GB writable copies+2GiB scratch≈10.2GB within the64GiB allocation; actual recursive sessions share the runtime. This estimate is not a memory guarantee, and local cache growth/failure remains observable. The existing12GiB one-fixture proof is not claimed as a concurrent performance qualification. No store deletion or recopy is authorized.

Caps unchanged:600s training,900s each readout,3300s owned including120cleanup,3330s parent outer. Parent owns acceptance/launch. Three small tests cover eval-only argv substitution, no missing-store fallback, and distinct per-stage cache paths; no broad tests or new native fixture.
