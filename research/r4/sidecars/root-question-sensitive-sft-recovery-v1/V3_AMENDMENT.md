# V3 cross-process identity, readout clock, and usage amendment

V1/V2 were prelaunch CPU artifacts and remain immutable. V2's parent process temporarily replaced `recovery_study.verify`, but subprocesses imported V1 directly. A combined-corpus receipt written with the V2 identity would therefore fail the trainer's V1 identity comparison. V3 supplies one study facade and entry for capture, training, readout, binding, and owner; combined-corpus construction temporarily routes the inherited V1 function through V3 verification. The actual combined72 fixture uses 65 frozen teachers plus seven fixture recovery teachers, writes a V3 receipt, reloads it through the actual qualified trainer CLI, and reaches the actual loader boundary without monkeypatching verification. It does not load a model or provide scientific evidence.

V3 also restores the original readout budget exactly: protected deadline is `min(stage_end, stage_started + 1950 + actual_dev_elapsed - 90)`. Dev exceptions are retained and protected collection still runs. No unused dev/startup allowance is borrowed. Finally, all four physical-file unions use the already qualified per-field usage function, retaining separate known/unknown input, output, and cache counts rather than treating absent fields as zero.

Science, capture boundary, corpus, seeds, masks, optimizer, six updates, readout inventory, 5400-second outer cap, and exact output namespace are unchanged.

