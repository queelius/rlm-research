# CPU preparation result

Thirteen focused tests pass: eleven native/window/dispatch/coordinator tests (2.11s), two real tiny-CPU optimizer tests (5.07s). The actual frozen three-call native fixture reconstructs two root turns and one grammar-constrained, uncredited child turn; scientific admission rejects its synthetic provider likelihood. No model, GPU, live endpoint or service was invoked during preparation.

The numerical trainer is the exact frozen campaign_train.py (38fe3087…), including FP32 adapter load audit, BF16 base, same-forward old policy, capped native/HF correction, root-only masked loss, Adam restoration and checkpoint commit/recovery. Only its authenticate_group callback is replaced: it authenticates a legal16/24/32 consumed prefix against the predeclared full32 candidate window and independently replays its real raw native graphs. Each selected episode contributes1/Nepisodes, each root turn1/Nturns within its episode, then mean physical action-token loss. All admitted members of all mixed groups are retained; NULLs and uniform groups are excluded without synthetic rewards.

The collector is the unchanged actual native collection/dispatch function with private frozen phase/spec callbacks. Current policy, fixed child, base, physical sampling, typed schemas and initial task tokens are still authenticated. First-stage input prompts/task hashes are copied unchanged from the accepted adaptive896 allocation; maximum initial prompt1073 +2048 generation is below8192, with no crop. Candidate scheduling changes to serial eight-slot batches to permit the declared refill decision; this scheduling difference is part of the package.

The new coordinator keeps candidate-window and actual Adam cursors separate. Its real control-flow fixture runs four homogeneous windows/128 candidate dispatches and both24-coordinate readouts while never calling a trainer. A separate real tiny-CPU test commits Adam step1, traverses candidate-window2 as a no-op with bitwise-identical optimizer tensors and unchanged Python/Torch RNG, then commits Adam step2 in candidate-window3. Inherited stale-generation, double-step, correction-capture/recovery, child-gradient and observation-mask checks pass.

Fresh namespace screen covers154 distinct master/optimizer/train/readout seeds against named inherited and six recent accepted metadata inventories. It is not a global collision claim. There are16 fixed training prompts with answer histogram0:1,1:1,2:3,3:2,4:1,6:2,9:2,10:1,11:1,14:1,18:1; no zero-count filtering or label-based selection. Same896 public source allocation and source-disjoint split decisions are inherited; the readout composition is exposed and leaf-training-supported.

One3600s owned clock includes3480 work and120 cleanup. Training cutoff2820 retains660 final service/readout reserve. No automatic resume, retry, incomplete-window update or false four-update promise. Failure and raw physical cost remain observable; final cells can be unrun/NULL on a real stage failure. Fixed initial low-LR SFT8 is MAIN-selected before high-LR outcomes, not validation-selected. The former SFT Adam is authenticated as source provenance but never restored into fresh RL step0.
