# Implementation plan

1. Test legal2/3/4-group prefixes, admitted mixed-group support, true no-op cursor, stale/double-step rejection, and exact128/24 plans before runtime implementation.
2. Pin the adaptive source stack and campaign math. Bind fixed66cce RESULT/SELECTION/state/member closure to MAIN's frozen approval, no generic start bypass.
3. Collect immutable eight-episode subgroups under one full-window generation identity. Export native groups with unchanged admission; union only a legal consumed prefix and recompute one combined mixed batch.
4. Separate window manifests from contiguous actual updates. Keep a service warm across homogeneous no-op windows; release before every actual optimizer step. Persistent Adam/checkpoint/RNG use actual update indices only.
5. Qualify real generic admission plus actual eight/24-slot dispatch/global/cardinality and no-op/step fixtures; preserve original native proof. Freeze all128 potential training/24 paired readout coordinates, seed/corpus/source closure; publish READY last.
