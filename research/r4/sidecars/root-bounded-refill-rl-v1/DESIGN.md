# MAIN-approved bounded refill RL

Implements ideas/2026-09-09-bounded-refill-rl-main-approval.md, not the analyst's smaller two-window alternative. Four candidate windows, each four predeclared context×family groups of eight episodes. Collect the first two; refill group3 then4 only while fewer than two mixed admitted groups. Use every admitted episode of every mixed group at the legal stopping prefix. One real update if any mixed group; otherwise a genuine no-op. Maximum128 training attempts/four actual updates; last committed step0–4 is the endpoint.

Exact availability-fixed low-LR success-SFT8 adapter66cce400, fresh RL Adam0, fixedc32 child. Unchanged source-bound native reward/admission, root-only current-action likelihood, TIS/PPO numerical implementation/guards and equal episode→root turn→action-token weighting. No data/reward repair, high-LR checkpoint substitution, manufactured advantage or partial-window update. Window and Adam cursors are distinct. Every candidate coordinate is frozen before sampling; no prior-policy group reuse.

Read the same24 source-disjoint development coordinates before and after, eight each validation/query/length, fresh paired seeds. Eight exposed source context clusters, not new holdout evidence. Mandatory-first-two versus consumed-window support is a descriptive yield screen, not a counterfactual policy-effect control. Homogeneous successes are not proof of mastery; no-update readout changes cannot be learning.

One immutable3600s owned clock,3480 shared work,3630 parent envelope. Baseline max180 service+480 collection. Training cutoff at elapsed2820; reserve660 final service/readout plus120 cleanup. Candidate group cap300s; only begin a group with300s remaining save/update/release allowance after its clipped cap. Optimizer load/forward/backward cap240s, plus60s checkpoint/release allowance; no optimizer is launched without120s remaining. Caps are maxima clipped to the unchanged cutoff. Incomplete/capped/integrity-failed stages are retained and never trained; an actual stage failure stops the run, leaving later readout cells unrun/NULL. Reaching the cutoff between completed windows selects the last committed policy and starts final readout. No timer reset, automatic resume, reroll or predecessor cleanup.

Reuse existing qualified owned lifecycle/native collector/rootless interface and pure numerical trainer. New code is a window-prefix manifest/admission/state machine and exact fixed8 start binding. Main approves READY and launches; preparation has no GPU authority. Internal tests use explicitly synthetic fixtures only, never scientific behavior likelihood.
