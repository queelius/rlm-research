# Exact qualified collector grouping seam

The frozen `fixed-leaf-composition-v1/driver.py` collector (SHA9afd6c5d5219a6705c79839e99a87ca45e50c84fc7bdff197c97482f6cd8387a) dispatches individual rows from a shared iterator; it does not dispatch two-row pairs. No historical two-row claim is made here.

The private AST-extracted function changes three counted one-occurrence expressions: the pending iterator now iterates validated quadruples, the next item is called unit, and the exhausted-iterator check is on unit. The exactly located request body through worker end is indented under a sequential `for row in unit`, checking the existing stop event before each row. The actual HTTP call, raw response capture, alias check, usage, exception handling, cancellation finally writes, coordinate writes, absolute timeout and gather remain unchanged. Request rebuilding uses the new exact grammar contract; source callbacks are private to this process. No new scheduling implementation, retry, episode replacement, or output repair is added.

Fake HTTP runs the real adapted collector over all96 requests and checks the actual task-wise sequence is exactly the24 declared quadruples, exactly four workers participate, maximum active units4, and no unit overlaps itself. A timeout fixture leaves four calls in flight; all four CancelledError raw records exist when collection returns, and remain stable after another event-loop turn. The unchanged driver's finally reads calls only after await collect_calls returns; the tested gather/timeout boundary completes the raw cancellation writes before STATUS. This avoids counting a partial late-write snapshot without introducing a new capture path.
