# Sparse cue-order96

CPU preparation only. Exact design/plan and approval are pinned through study.py/APPROVAL.md. Scientific question: does label-first order shift matching-minus-constant benefit from distance0 to distance1 at cadence4? Positive primary is `[(M−C)d1−(M−C)d0]label-first − [...]tag-first`, tasks separate, seeds averaged within each of four exposed context clusters. All four cells and full0..3/first-anchor profiles are retained, no old outcomes pooled.

The12 old contexts and c32de adapter are fixed; seeds981304011/021 are fresh within the recorded bounded manifest scan. A/B/C/D mean matching-tag-first, constant-tag-first, matching-label-first, constant-label-first. Each task sees each of the four Williams sequences twice. Requests differ across field order only in ordered grammar; common neutral messages and full typed input vectors match. Matching versus constant retains its instruction/tag/grammar confound, and all interventions alter generated prefixes. No attention mechanism or full-RLM claim.

Prepare (new namespace only):

`CUDA_VISIBLE_DEVICES= PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 taskset -c 96,97 /project/alex_phd/envs/prime-rl-5990b1b/bin/python driver.py prepare`

Parent read-only verification:

`/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-sparse-cue-order-v1/owned.py --verify`

Exact launch, only after MAIN acceptance and exclusive GPU assignment:

`/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-sparse-cue-order-v1/owned.py --directory /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-sparse-cue-order-v1/owned/attempt-001`

Use cwd this sidecar, the inherited real API-key environment and LD_LIBRARY_PATH/assigned exclusive CUDA identifier supplied by MAIN. Never print or embed credentials in docs/source; no hardcoded CUDA0. The parent applies930s outer cap. Wrapper shared work780s from entry, collection600s capped by remaining work,120s cleanup reserve inside900s inclusive; collection subprocess cap min630s/remaining work. No clock reset, API retry, reroll, fallback, new image/runtime, or modified accepted source. Native first-response collector keeps ordinary tool descriptions but executes no tools; this is a constrained child action space, not recursive execution.

Outputs are outputs/attempt-001; owned records are owned/attempt-001. Preserve incomplete/failed attempts. On one call error inherited stop prevents new calls; in-flight calls complete/cancel under the same bounds, then STATUS counts final raw files. No substitute run without a new MAIN decision.

analysis.json is an implementer-owned raw-derived projection, not an independent outcome audit. Complete malformed output is strict0, unavailable/infrastructure/unrun NULL. Interaction is NULL if any constituent is unavailable; jointly-valid secondary remains separate. No partial schema prefix is counted valid. Costs come from all actual physical requests, raw usage including cached tokens; output-token savings are not equal-compute or equal-accuracy proof. All96 planned calls and8 planned quadruples/task remain visible. Independent audit begins after owned release/operation terminal, from raw wire and source closure.
