# Approved CPU implementation

Implement the frozen main design and decision without changing historical sources.

1. Test desired raw-order scoring, displayed alignment, ordered request identity,
   paired dispatch, null accounting and bounded ownership before implementation.
2. Reuse authenticated factorial inputs and single-adapter collector/lifecycle;
   private adapters only for 48 calls, clocks and order-sensitive wire capture.
3. Reproduce all 48 native tokenizer/XGrammar feasibility checks; verify 24
   identical-input order pairs, exact opposite-order rejection and scoped seeds.
4. Freeze inputs/source/weights and focused CPU evidence; publish READY last.

No model calls, GPU actions, environment changes or scheduler. Parent review and
acceptance remain separate. Work is isolated in this external sidecar, not Git.
