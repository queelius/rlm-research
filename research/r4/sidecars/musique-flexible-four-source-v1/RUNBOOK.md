# Fixed2+2 versus flexible4 allocation

All12 original exposed MuSiQue contexts and their unchanged question-blind halves are retained. Two shared selectors each return exactly four ranked, distinct IDs from their own half, most useful first.128 output tokens each; no summaries or answers. The fixed arm takes each list's first two. The flexible planner sees only the eight candidate original paragraphs with half/rank/ID and chooses exactly four distinct candidate IDs using128 tokens. It sees no gold, annotations, or unselected source paragraphs.

Both finals see four exact original paragraphs sorted by original ID, through identical prompt schema/instruction, model, T=.5 and1024-token cap. No planner text, unselected candidates, rank/half metadata or arm labels enter final prompts. Fixed-index parity balances branch order. Seeds202609230000+16×index with offsets0/1 selectors,2 planner,3 both finals.

Invalid shared selector schema/IDs—including duplicate, wrong length, Boolean or out-of-half IDs—produce a recorded model error and identical empty-evidence/error packets for both finals. The planner still receives an explicit empty pool/error and can return an empty list; its dependent result is not treated as a fresh planner model error. Invalid planner schema/IDs affects only the flexible final, which gets explicit empty evidence/error; the valid fixed arm remains intact. No replacement, partial or cross-half fallback. Transport failures remain unavailable for their natural dependent policies; known model errors retain observed final scores rather than becoming unknown. All five calls remain planned, subject to transport/context/deadline bounds.

This is a fresh fixed2+2 arm. The previous score is not reused because asking for four ranked candidates changes selector behavior. Physical60 calls,24 finals,12 paired units. Natural deployed fixed cost is3 calls/question (36 total); flexible is4 (48 total), charging the additional planner. Final paragraph count is matched at four only on valid rows, but token length and natural call cost are not matched. Record observed tokens and unknown usage separately.

Primary answer EM/F1 and support EM/F1 are paired fixed→flexible. Candidate and selected gold-support coverage and half allocations are host-only diagnostics after collection/cleanup, never available to routing. Coverage alone does not prove faithful use, and answer-string matches may hide answer-form or relation errors. All contexts and both arms remain in the readout; no outcome filter or promotion gate.

Reuse exact authenticated V3 base4B native service, eager/batch-invariant dispatch attestation and owned-process release. Four question workers;8192 actual prefix+requested-output boundary; caps science700,owner950,external1050 seconds. Every prompt, native request/response and call receipt is saved. No generated code is executed. MAIN alone launches after CPU_READY review; preparation never starts a GPU service. This is a diagnostic allocation mechanism, not learned routing, recursion or a novel method claim.
