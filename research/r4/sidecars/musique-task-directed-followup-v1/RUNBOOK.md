# Frozen task-directed report follow-up screen

MAIN alone may launch after reading READY and the source closure. No GPU was used in preparation. Run the exact READY argv under the shared research `flock` and an external1800-second timeout; the owner requires1700 seconds, has a1320-second collection deadline after service setup, checkpoints every physical start/request/response/result and always releases its owned service. Attempt: `outputs/attempt-001`; no automatic resume/retry or overwriting.

```text
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python owner.py verify
# MAIN owned GPU/private environment only, after shared lock:
/project/alex_phd/envs/prime-rl-5990b1b/bin/python owner.py run --outer-seconds 1700
```

Sources: `study.py` owns prompts/policy/schema/seed/source paths; `prepare.py` already froze twelve questions and132 logical coordinates; `collect.py` executes the fixed graph with four-question concurrency; `metrics.py` gives all48 terminal slots and physical/policy costs; `owner.py` manages the existing native service lifecycle. `service_wrapper.py` adds only an eager worker extension to the existing released-base launcher. `report_worker.py` calls the original `matmul_persistent` unchanged and writes one scalar-metadata receipt after its first CUDA return. It does not change weights, compute, selection, prompts, temperatures or output limits. An optional Torch warning is not a gate.

Inputs: `inputs/MANIFEST.json` contains source identifiers/hashes/hops/exclusions and original-index partitions; `inputs/public/` contains only question and original paragraphs; `inputs/static/` freezes the initial and full-source tokenized requests; `inputs/SCHEDULE.json` freezes132 roles/seeds/caps. `inputs/host/HOST_GOLD.json` is0600 and never enters prompts. Static largest prefix-plus-output is5726; every dynamic request is independently checked against8192 and refused rather than truncated. Ordinary512-token reports may validly stop by length; finals and plans use ordered JSON grammar. Output prefix budgets are not external-file size budgets.

All model calls use the same cached Qwen3-4B-Instruct-2507 cdbee75 weights, no research adapter, T.5/top-p1/top-k-1. Final and targeted/broad seed pairing is fixed, not selected for outcomes. Twelve branch rotations balance which terminal policy runs first. The collector saves original message JSON, exact native wire request/response bytes and SHA, token IDs, finish reason, schema inventory, native request identity and usage. Secrets stay inherited only; no request headers/credentials are saved. Returned unknown stops, duplicate identities, HTTP errors and start-only records remain unavailable, not wrong. An invalid/empty upstream report or malformed planner prevents dependent decisions without fabricating content; full-source still runs. If the cap produces a partial tail, report it and do not promote.

`RESULT.json` is a fixed12-per-arm result with official score fields, per-hop counts, paired effects, policy costs4/6/6/1 and deduplicated physical132 inventory. `questions/` preserves the shared planning output and prefix hash, while `calls/`, `starts/`, `prompts/` and `native/` retain checkpointable provenance. `RUNTIME.json`, `BINDING.json`, `ENGINE_ATTESTATION.json`, `OWNER_RUN.json` and `OWNER_TERMINAL.json` authenticate runtime and release. Completion is distinct from passing the exploratory promotion screen. Malformed finals are known finite-output failures; unavailable responses are never silently assigned zero reward.

CPU evidence comprises two focused tests, including the actual serialized eleven-call graph on a synthetic native wire, the real tokenizer/JSON/scoring seam,4/6/6/1 cost accounting, unknown usage, prefix bound, and actual prospective wrapper/environment configuration. The telemetry wrapper is exercised with clearly named CPU sentinels and flag-disabled refusal; this is **not** actual CUDA qualification. No inference entrypoint, model load or GPU kernel is executed by tests. The genuine dispatch gate must pass in MAIN's future run.

The full-source control may solve these short contexts directly; do not claim long-context necessity or learned recursion. Compare semantic targeting to ordinary additional reports, not to an intentionally crippled report. This one first screen is the complete implemented scope. MAIN may request a fresh replication if the frozen screen passes; no selector-training or automatic follow-on is authorized here.
