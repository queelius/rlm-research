# Exact engine argv in startup and release

Attempt002 failed in3.383815 owner seconds before0/132 scientific requests. `suite.start_service → observe_service → life.claim_service` rejected the saved `python engine_entry_v2.py @ inference.json` because its inherited expected command remained `bin/inference @ inference.json`. The wrapper path/hash, bindings and GPU fields already agreed. The same claim check in `release_service → stop_service` raised again. A detached engine subsequently loaded: the actual dispatch receipt identifies owned PID577315 and demonstrates that V2's worker registry fix reached a CUDA batch-invariant kernel. This does not retrospectively qualify an experiment with no scientific answers.

MAIN authenticated recorded leader577193 (start ticks1104196454, UID1523821556, PGID577193), direct child577315 and the owned group, then sent SIGTERM. The receipt at `operations/2026-09-12-musique-attempt002-cleanup/SIGTERM.json` preserves that action; MAIN separately confirmed all exited. No files were removed. The original failed owner remains `released:false`; an earlier empty GPU check was not evidence of eventual release.

V3 adds only an exact engine-command substitution to the pinned inherited `claim_service`, retaining all existing binding, wrapper SHA, GPU, actual-command/known-Prime-title, timestamp, PID/UID/PGID/start-tick and descendant checks. Both start and release use that same installed function. Engine entrypoint, service wrapper, telemetry, math, all inputs/prompts/seeds, collectors and scoring are unchanged from V2. Final real-dispatch receipt and clean release remain mandatory. The additive owner additionally saves sanitized exception messages and tracebacks without frame locals, redacting credential-valued environment variables.

CPU evidence: the actual inherited `suite.start_service`, `observe_service`, `claim_service`, `release_service` and `stop_service` run against temporary files and an explicit CPU launcher/process/network double. Unrepaired V2 fails with the observed binding ValueError; V3 writes an authenticated ownership receipt and a complete stop receipt, and rejects a different engine entrypoint. A second test checks useful exception detail and credential redaction. Separately, the actual saved attempt002 receipts reproduce the old rejection and pass V3's command/binding boundary with no live PID and no mutation of the failed directory. Process signals in the fixture are stubbed: no claim of live GPU cleanup testing.

MAIN-only command: native Python `owner_v3.py run --outer-seconds 1700`, output `outputs/attempt-003`, under shared flock and external1800 seconds. Science cap1320,132 planned calls,48 terminals,12 questions, four controls, no model-answer retries or selection. V1/V2 source and outputs are immutable. No agent GPU launch occurred. Missing future dispatch/ownership evidence remains a real failure, not an optional warning.
