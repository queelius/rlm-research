# Attempt002: correct Prime's actual worker-extension registry

Attempt001 is preserved:42.090819 seconds, zero science calls, clean release. Its owner required a startup dispatch receipt and failed before collection. The receipt module was never imported: Prime's actual `server(config)` unconditionally assigns `args.worker_extension_cls = WORKER_EXTENSION_CLS[config.weight_broadcast.type]`. Thus saved `inference.json` named `report_worker.ReportWorker`, but actual API arguments and EngineCore injection named the original `FileSystemWeightUpdateWorker`. This is an entrypoint/config-translation bug, not evidence that batch invariance was disabled or that warmup cannot hit the kernel.

Additive repair only: `engine_entry_v2.py` retains the original CLI and environment-setup order, then changes the process-local filesystem registry to the same telemetry subclass before the original `server(config)` resolves it. `service_wrapper_v2.py` reuses the V1 wrapper/config and original pinned launcher, replacing only its child argv with the exact same native Python plus this entrypoint. Pre-exec command/PID/source attestation reflects that actual argv and new wrapper. Shared Prime/vLLM files, mathematical kernels, model weights and existing sidecars are untouched.

`owner_v2.py` points to the additive service/READY and unused `outputs/attempt-002`; it removes only the pre-science warmup-receipt assertion. Actual source/flag/mode/CUDA/PID dispatch qualification remains mandatory after science and clean release. No optional-warning substitute is introduced, and absent final telemetry still prevents a qualified primary. `report_worker.py`, collector, scorer, prompts, all12 questions, all132 coordinates/seeds and four controls are unchanged. Caps remain1320 science,1700 owner,1800 external. No retry within an attempt; attempt001 had no model answers to select or repeat.

The targeted CPU regression calls the **real installed Prime `server(config)`**, namespace construction and validation twice, replacing only the terminal GPU-serving coroutine with an in-process recorder. Before repair, the actual output argument is the original filesystem class despite the config. After the same registry installation used by the new entrypoint, it is `report_worker.ReportWorker`. The second fixture checks actual additive owner/launcher seams, unchanged schedules/metrics and deferred dispatch qualification. No server, socket, model load or GPU kernel was executed. This diagnoses and tests the observed translation boundary, not device telemetry; the first actual CUDA dispatch is still a future runtime gate.

Additive input-metadata clarification: `inputs/static/` was a token preflight computed before public JSON was written with sorted keys, not the exact request-byte inventory consumed by the collector. Reconstructing the sealed collector's loaded-public serialization gives0/36 identical initial token bodies, each6–20 prompt tokens shorter, largest prefix+requested output5708 versus the preflight5726. All original text, partitions, per-arm conventions and frozen runtime request functions are unchanged; each actual request is still captured byte-for-byte and checked≤8192. This does not modify public inputs or quietly rewrite the V1 seal.

MAIN alone may run the exact `READY_V2.json` command under the shared lock and external1800-second cap. V1 source/READY/output stay immutable. The repair adds no new experiment, task selection, metric, claim or framework.
