# FinQA two-example interface calibration

MAIN only launches `owner.py run --outer-seconds700` using the exact spaced argv in CPU_READY. External cap800s; owner700s; science600s. Native base Qwen3-4B-Instruct-2507, no adapter,8192 total context, T=.5,384 output, four question workers.32 calls on the same16 official-dev page contexts and original paired seeds202609290000..0015. No GPU/model calls during preparation.

Only intervention: two synthetic facts/questions with arm-specific worked output. Both arms see identical facts/questions. Direct sees `answer` objects; DSL sees `program` objects with concrete #0/#1 references. Synthetic arithmetic computes5 and.25; percent string25% maps to.25 under the original parser. No FinQA targets/programs become demonstrations. Public evidence, question, call order, model, seed, decoding budget, interpreter, exact-target metric and all16 denominator remain unchanged. New input prefixes intentionally differ and are frozen in TOKEN_AUDIT.

Cached zero-shot control stays0/16 direct and0/16 DSL, with all16 DSL invalid. This additional32-call condition tests acquisition of a usable arithmetic interface before an efficacy claim. Natural cost remains one call/answer per arm, but neither input nor output token costs are matched. All unknowns and invalid outputs remain explicit. Numeric target equality is not semantic truth; original annotation/source-question mismatches remain diagnostic, never filters or repairs. This is exposed-panel adaptive few-shot calibration, not SFT/RL or recursive-method novelty.

Implementation plan approved by MAIN: (1) test exact synthetic fact/representation and native paired seam; (2) reuse existing FinQA owner/collector/metric with process-local study binding; (3) freeze all32 input requests and run one CPU HTTP double; (4) seal sources, baseline result, data provenance, actual fixture and READY for MAIN review. All files are in this new external sidecar; no shared or live source changes. The native fake sends two synthetic replies and retains30 unknown slots, testing current owner/collector binding and exact request IDs/prefix/canonical-response semantics.

Read order: interface.py; study.py; collect.py/metrics.py/owner.py thin bindings; test_interface.py; prepare.py; CPU_READY. Primary metric and ten-operation bounded interpreter are the unchanged parent science.py and metrics.py in the closure. There is no Python eval/exec of model programs; inherited trusted infrastructure compilation is distinct. Owner retains mandatory real-dispatch qualification and release checks. Prepare is write-once and must not be rerun over sealed artifacts.
