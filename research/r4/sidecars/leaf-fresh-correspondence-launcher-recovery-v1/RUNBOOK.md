# Launcher-only fresh96 recovery

This is an explicitly MAIN-approved new attempt, not an automatic retry or timer reset. The original failed attempt, frozen scientific READY09e30718…, all96 bodies/seeds/models, physical token expectations and scoring remain unchanged. Original output paths are unused and retained; setup failed before any scientific client command. The new owner and launcher live here, independently of the frozen scientific root.

MAIN first authenticates recovery READY and the original source closure, checks the exact predecessor and empty exclusively assigned GPU, and acquires the existing shared operation lock. MAIN already handled the previously recorded orphan; this runner cannot clean up that old service. Parent envelope1800s, unchanged1770 owned/1650 shared work,300 per startup and600 per-model collection, with no reset. Preparation has no GPU/service authority.

```sh
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-fresh-correspondence-launcher-recovery-v1/owned.py --verify
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-fresh-correspondence-launcher-recovery-v1/owned.py --directory /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-fresh-correspondence-launcher-recovery-v1/owned/attempt-001
```

Existing API credentials and exactly one CUDA device are supplied by MAIN; never put their values into the command or runbook. New ownership output: owned/attempt-001/{ATTEMPT,TERMINAL}.json and qwen3/qwen35 service records. The adjacent owned/attempt-001-RECOVERY.json binds recovery/original READY and the separate budget. Scientific output remains leaf-fresh-correspondence-v1/outputs/{qwen3,qwen35}/{calls,wire,STATUS.json,analysis.json}. Any existing scientific output or owned attempt prevents launch. No model-call retries or record overwrites.

The only launcher behavior amendment is inherited.__file__ = actual new wrapper before the unchanged main records SERVER_START. Both suite.SERVE and suite.c.ROLE reference this exact new source/serve.py. All existing V2 checks remain active: command and launcher hash, binding/config, same PID/start/UID/PGID, known Prime title, and observed descendant release. Existing BASE/model/sampling/renderer/config code is reused without substitutions.

Focused CPU evidence executes the real main and config validator with only process/environment/readiness boundaries faked. The old wrapper fails because it records5d6aab04… instead of076e65b7…; the new wrapper records its actual hash. No model or service starts during that test. Actual private lifecycle-root propagation and refusal to reuse existing outputs are also checked.
