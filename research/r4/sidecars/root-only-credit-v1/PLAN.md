# Bounded native capture preparation

1. Freeze deterministic train/validation contexts and40coordinates from authenticated
   source groups; test disjointness, gold separation and source-order independence.
2. Add focused red/green tests for trusted mixed-role native routing and true root-only
   causal credit. Reuse authenticated source helpers without modifying them.
3. Qualify actual native renderer/wire serialization and owned rootless root-child-root
   trace with a deterministic CPU fake provider. No GPU or speculative package change.
4. Freeze source/spec/data and exact launch command tied to current immutable role
   binding. Retain per-call role audit and processed-logprob evidence. Publish READY
   only after source is stable. Parent immediately queues the informative capture.
5. Export all40 episodes with explicit root-only admission and mixed-group counts.
   GPU training is a subsequent bounded implementation, not implicit in preparation.

The main agent owns GPU orchestration and Phase2 choice. Source/outputs are external
research sidecars; broad repo tests, refactors, worktree/merge ceremony are out of scope.
