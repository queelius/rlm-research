# Root interface/output competence: CPU preparation

This is32 authored root-action SFT examples,64 exposures and exactly four optimizer updates, followed by48 paired semantic episodes. It is not autonomous-planning training or RL on operator traces. Start473210… with fresh Adam; fixed capable childc32de… never loaded into the trainer. Final update4 is used without validation selection.

The reconstructed896 groups exactly match the outcome-independent earlier feasibility allocation:384 train,192 validation,64 query transfer and256 length transfer. All are child-training-supported and excluded from the named prior root union plus adaptive512, not globally guaranteed unseen. Underlying TREC license/pretraining overlap remain unknown. Public runtime files contain text/IDs/synthetic users, never per-record gold.

Sixteen first helper-call actions teach real native ipython envelopes/imports/calls/strict parse/print. Sixteen terminal targets follow real owned metadata executions, with answers16,1,10,2,8,4,6,3,10,8,9,7,8,8,7,8. Only the current action is supervised; metadata tool actions/observations are masked. Per epoch1603 target tokens (1520 helper,83 final) and30888 causal input tokens; maximum1163. Thus terminal supervision is only5.2% of supervised tokens, an important limitation. Two epochs are3206 target/61776 causal tokens, not a claim of balanced target weight or adequate general training.

Native preparation initially failed the exact physical-prefix check because saved trace JSON had reordered and unnested native tools. The failed `prepared-v1` remains untouched. `prepared-v2` passes using actual TrainClient tool-to-wire conversion and qualified schema insertion order;16 real metadata executions plus one helper-only CPU proof passed. All provider tokens/logprobs are authored qualification scaffolding, never behavioral RL likelihood. Fake child observations from the helper proof are excluded from SFT.

After parent review, evaluation task metadata uses deterministic truthful new context/source IDs and actual token lengths. The completed metadata training traces retain original fixture bookkeeping IDs; their physical content is unchanged and final training rows bind exact new source-group provenance explicitly. The final new-catalog/free-root/typed-child native proof checks that corrected IDs leave the physical prefix unchanged. Raw traces are not rewritten.

Both root readouts use the parent's prospectively chosen **same compatible typed batch child contract**: exact source-bound `batch_contract.request_for` matcher, trusted depth/invocation and host coordinate headers, nested native grammar only on eligible child calls. No root grammar, root-prompt repair or answer fallback. This choice controls the known native-child contract bottleneck; it does not prove child semantic accuracy. Sources and the new-catalog proof are pinned in READY. Initial/final readouts are separate serving periods; paired tasks/seeds/first physical root IDs are held fixed but adaptive trajectories are not deterministic.

Readout uses a work-conserving four-worker queue under an absolute cap. Completed empty/malformed replies are strict failures0; incomplete/unavailable episodes remain null. This new study intentionally differs from adaptive40's empty-null score. Typed wire capture retains actual aliases, sampling and physical IDs, and each available first-root wire prefix must match the frozen prompt. Completed rows are checkpointed immediately; cap cancellation does not erase earlier outcomes.

Focused tests: desired contract/trainer tests first failed with missing modules, then passed. `CPU_TESTS.json` records final commands/stdout:3 data/mask/scoring tests,1 work-conserving/cancellation test and1 four-update real tiny-PEFT test covering frozen base, finite gradient/delta, adapter/Adam/RNG restore and step/cursor rejection. Native proof artifacts are separate from unit tests. No GPU/model calls during preparation.

Launch: exact argv in READY.json, inherited single-device/MIG and LD environment.600s training cap;900s per collection;3300s owned cap including120s cleanup;3330s parent outer cap. Reuses frozen suite serving and owner-qualified release plus the previously observed process-exit race fix. Native collector workers have CUDA hidden; training alone receives the parent-assigned device. No retries/relaunch/resume are added; completed checkpoints/raw partial stages survive failure. Parent owns acceptance, launch and subsequent interpretation.
