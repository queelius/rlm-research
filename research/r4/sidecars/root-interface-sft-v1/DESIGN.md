# Root interface/output competence SFT: fixed four updates

Parent approved September9 after uptake96/adaptive40. This is authored supervised training, not RLVR, learned filtering, or a decomposition novelty claim. CPU preparation only; parent owns GPU acceptance/launch.

## Question and isolated intervention

Does32-example interface/output SFT improve execution and strict final answers on new root-disjoint semantic count tasks, starting from the exact historical step8 root473210b18c4be163cd46a894614cb14934a7908e45bd97f96720928a7770ccdd? Fixed childc32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3. Same evaluation root prompt/tool/child contract for starting and fixed-final root. No validation checkpoint selection.

## Data and targets

Reconstruct the unreserved `adaptive-root-training-candidate-v1` allocation from the existing feasibility memo, preserving its exact group hashes:384 groups in8 train contexts (4×32,4×64);192 in4 validation contexts (2×32,2×64);64 in2 query-transfer contexts (2×32);256 in2 length-transfer contexts (2×128). All896 are unique, leaf-training-supported, excluded from the named prior root union plus adaptive512.46 remain unallocated. Do not alter allocation based on new model outcomes. Query categories cycle NUM/HUM/ENTY/LOC by context index. Gold is host-only.

Exactly32 SFT rows:

1. Sixteen native first-root-action examples:8 training contexts×single-user/global semantic question. Authored action teaches correct ipython function-envelope serialization, imports, records.json access, ordinary `await rlm(request_for(records[:4]))`, strict map parsing and printing the returned mapping. It stops at this action; no child answer/semantic gold is supplied as an observation or supervised target. It does not implement complete semantic filtering/reduction.
2. Sixteen final-answer microtasks on the same8 training contexts: one public ID-divisibility count and one distinct-user-prefix count per context. Divisor=2+context index; inspected prefix length=1+3×context index. Public metadata only, labels absent. A real owned CPU tool execution supplies the observed number. Only the subsequent exact `Answer: N` continuation is supervised; prior authored action, tool observation and all scaffolding are masked. These varied numeric answers avoid a constant8/128 format lesson.

All targets are explicitly operator-authored SFT demonstrations. Native CPU provider responses/logprobs are qualification scaffolding, never behavioral likelihood or RL credit. Historical fixture outcomes are not training examples. Native Qwen3 enable_thinking=True, actual system/tools/message construction, true end-of-turn token; trailing renderer newline is not a target. No causal truncation, missing-token repair or fabricated child observation.

## Optimizer and immutable state

BF16 frozen Qwen3-4B-Instruct-2507 base; exact starting FP32 LoRA, dropout0. Fresh AdamW LR1e-4, weight decay0, clip1; no historical Adam/RNG restoration. Two deterministic epochs, effective batch16, microbatch1, token-sum CE normalized over current effective-batch target tokens, four optimizer updates. Save adapter/config/Adam/RNG/data cursor after every update. Fixed final checkpoint4; no evaluation-driven selection. Record token exposures, losses, finite gradients/deltas, actual precision/versions and separate clocks. Training cap600s; total owned3300s including120cleanup; outer3330s. No claim of compute matching to leaf SFT or adequate general training.

## Paired evaluation

48 semantic episodes: initial and fixed-final root each run24 coordinates. Validation:4 contexts×2 scopes×one seed=8. Query transfer:2 contexts×single-user/two-user union×2seeds=8. Length transfer:2 contexts×single-user/global×2seeds=8. All heldout groups remain disjoint from SFT groups;128 is beyond adaptive-SFT max64. Two transfer clusters per family make this exploratory only.

Sampling remains native temperature.5/top_p1/top_k−1/min_p0/max2048/depth1/max8192, ordinary root tools. Proposed namespace seed981284001, training shuffle981284002; validation seeds981284101–108, query981284201–208, length981284301–308, with bounded named-manifest collision audit before freeze. All24 initial/final coordinates share task/seed and require exact first-root physical IDs; later trajectories may diverge.

The child-contract selection is a separate parent-frozen READOUT_INTERFACE binding before evaluation, common to both weights. Existing typed36 matches receipt_api, not this batch_contract; use only the compatible frozen typed-adaptive hook if accepted after its control. Otherwise explicitly use the existing native child. No mismatched or hidden grammar substitution. Training target API remains ordinary rlm(request_for(batch)) + strict_map.

Primary strict ASCII whole reply `Answer: N`; completed wrong/malformed/empty0 (empty separately classified), incomplete/setup/provider-unavailable null, inherited raw scores separate. This NEW study deliberately counts completed empty outputs as competence failures, unlike adaptive40's unanswered-null convention; no historical scores change. Report coverage and paired success changes across8 heldout context clusters, helper/native map use, metadata filtering evidence, root/child work, final formatting and semantic errors. No off-line answer repair, old-logprob invention or null replacement. Promote only to replication if heldout success/coverage improves beyond formatting without query/length regressions; flat/negative outcomes revise the bottleneck, not automatically increase scale.

## Minimal implementation and qualification

Own data/native example renderer, narrow SFT runner reusing authenticated CE/collate/adapter-audit/checkpoint utilities, and paired free-root evaluation adapter over existing native collector/lifecycle. No frozen-source edits or general scheduler. Focused tests: masks/terminator/prefix identity; real metadata observations and no labels; tiny real PEFT finite update/frozen base/save-restore/four-step cursor; paired plan/source disjointness and binding. One actual owned-native CPU helper seam plus metadata executions authenticate the training prefixes. Parent independently reviews critical path and launches.
