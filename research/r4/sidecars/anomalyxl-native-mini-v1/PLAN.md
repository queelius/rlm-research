# AnomalyXL native mini implementation plan

Goal: prepare ten frozen metadata-selected cases, direct versus current Python executor, twenty episodes under MAIN-only GPU authority. CPU implementation stops/defer by2026-09-12T10:43Z.

Approved design: ideas/2026-09-12-anomalyxl-native-mini-pilot.md, amended by MAIN: the final-only call receives the remaining aggregate2048 output tokens, each of at most3 inspections gets at most512; official score_precise parsing remains unchanged and whole-response strict JSON is a separate metric.

New files: mini.py (identity, public preparation, native request/decode, scorer and executor seams), native_service.py (single exact-model local service lifecycle), owner.py (bounded two engineering calls and20episodes), seal.py (source/data/model closure and CPU receipts), test_mini.py (two focused fixtures). No shared source, environment or prior experiment is edited. No Chat Completions/verifiers adapter, children, images, extra training, dataset expansion or resume framework.

Task1: write failing fixtures; implement only these seams and freeze public/gold-separated inputs; test real credential-scrubbed current executor, token budget/strict transport/context behavior and exact official scorer/public selection inventory. Inspect official scorer completely before importing. Native Qwen3.5 support is an unqualified first-GPU-run condition, not a CPU success claim.

Task2: seal full sources/model/config/tokenizer/data/provenance and exact run command; report READY to MAIN for independent review/launch. Owner1100/external1200 seconds,20episodes,≤50research calls+2engineering calls,45seconds/episode,5seconds/Python inspection,2048chars/observation. Any unbounded provider adaptation or missed10:43CPUdeadline means DEFERRED, not a wider framework.
