# Bounded CPU preparation plan

1. Capture desired behavior RED: eighty exact parent crosswalks, no non-model body
   change, strict alignment/null semantics, fake-HTTP capture and owned cleanup.
2. Add a small private adapter over frozen grammar scorer/collector and qualified
   suite lifecycle. No changes to accepted sources or scheduling machinery.
3. Authenticate fixed-final B and source closure; compile schemas and compare all
   native typed prompt IDs against frozen parent hashes with GPUs hidden.
4. Run the focused CPU checks, freeze sources/spec/weights/qualification and publish
   READY last. Write the parent preparation report beside B_CONTROL_BRIEF.md.

No model requests, service launches, GPU operations, commits or live queue edits.
