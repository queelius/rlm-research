# Add the mixed-size baseline to the fixed grammar comparison

This is the approved, post-HF-result B-only companion to grammar160, not a new
sample or a change to that accepted study. The motivating matched HF readout was
B373/384 versus indexed-final374/384: extra indexed-target SFT benefit is unproven.

Use exactly the parent study's ten contexts, two seeds, anonymous/indexed output
and free/exact grammar. Select its eighty old-weight coordinates in their existing
relative counterbalanced order; cross-reference both old and indexed parent IDs.
Copy request bodies without reconstruction; only the model alias changes. Preserve
all input IDs, schema ordering, tools, sampler and generation cap. Native CPU
qualification must reproduce the parent's typed prompt hashes and retain full IDs.

Authenticate B's fixed epoch2/step204 checkpoint, Adam/RNG, source recipe, RESULT
and SELECTION. It is not validation-selected. No training occurs. A private import
of frozen grammar helpers supplies strict scoring and the real HTTP collector.
The unchanged qualified suite owns one B alias; root/child descriptor labels both
refer truthfully to the same adapter. No generated code is executed.

Eighty calls, four workers,120s HTTP timeout, T0.5/full support,3072 output tokens,
8192 context. Collection900s; owned operation1800s including startup and120s cleanup
reserve. No retries, repair, prefix credit or semantic labels for unaligned output.
Preserve partial calls, errors, usage/cache missingness and unrun nulls.

This later service stage is not a contemporaneously interleaved three-weight
comparison. Six TREC groups are exposed; four SST groups were fresh when parent
inputs froze. Public pretraining exposure is unknown. Compare B with indexed-final
within format/grammar; a task- or decoding-specific difference needs replication,
not a general orchestration claim. Parent alone accepts and launches.
