# Additive completion of operator72×6

Approved by MAIN after direct diagnosis of the original failed training launch. This is an infrastructure recovery, not a new recipe or outcome-selected rerun. The original accepted attempt, all 72 genuine capture trajectories, 180 child acquisitions, 324 authored root actions, 24 baseline endpoints and original 24 unavailable SFT6 endpoints remain immutable.

The accepted owner incorrectly used the installed evaluation `suite.command`, which always empties CUDA_VISIBLE_DEVICES. Its actual COMMAND receipt reports false GPU visibility; the training process failed before model load/gate/optimizer. A CPU reproduction through the actual owner, installed runtime and intercepted training Popen confirmed the empty environment. The qualified joint owner uses a separate GPU-preserving training command. Interpreter repair alone did not cover this boundary.

Run exactly the original six-example nonliteral objective/cost gate and six full72 passes, fresh Adam1e-4, original released-reference857 start, seed and .45/.50/.05 objective. Invoke the original unmodified od_train.py in its qualified TRAIN interpreter with only a fresh output directory and absolute deadline. Its original corpus/source identity remains the checkpoint identity, while this completion owner has a separate READY identity. No capture, baseline, resampling, checkpoint selection, partial-SFT6 substitution or automatic resume. Missing fixed6 leaves all24 completion endpoints NULL.

Then collect only the original24 fixed-SFT6 coordinates, tasks, seeds and native interface, paired with the untouched original24 baseline outcomes. Bind the selected checkpoint from the fresh training directory by one counted source adaptation of the original binding selector. Preserve the original corpus path/hash in checkpoint validation. The unchanged collector is reused through explicit-path modules and scoped aliases. This time-separated completion is reported additively, never as an originally successful48 run. Historical acquisition and baseline physical costs are charged once as shared actual work; standalone recipe costs include original capture and baseline plus new training/readout. Provider billing remains unknown.

Actual original baseline inventory has24 planned coordinates but23 recorded RESULT files. Its unrecorded coordinate74658bc6… remains NULL, alongside any recorded native-unavailable response. Pairing never implies24 complete observed pairs. The recovery does not fill that baseline endpoint.

Inclusive envelope:5400 outer,5220 work,5370 owned,150 cleanup and30 outer margin. Training≤3600, new SFT6 readout≤1200 inclusive startup/release, finalization≤300,120 slack. Stages advance immediately; no waiting to spend a cap. Training deadline also reserves the1200 final stage and300 finalization. Failed gate/training does not create or substitute a partial treatment. Collector remains GPU-hidden; only the explicit training subprocess inherits MAIN's exact assigned GPU/MIG identifier. Service ownership/release uses the existing accepted an27 lifecycle. No GPU/service/lock/queue actions are part of CPU preparation.

Focused qualification: actual owner→GPU command→intercepted training Popen preserves a nonnumeric MIG-shaped value; actual installed collector command empties GPU. Exact owner→collector CLI and binding/output namespace; original72 corpus closure and original24 baseline paths; six-step selection/ancestry; stage reserve, missing6 and no-reroll. CPU tests do not establish a successful4B load, gate or training result.
