# AG News source-matching output across two fixed weights

Implements the [approved design](../../ideas/2026-09-09-agnews-identity-weight-screen.md), SHA `d42483488566928501f22d8a899165ecb902a41b9a05c08411ad1cfda5bca6c0`, and [implementation decision](../../operations/2026-09-09-continuous-allocation/AGNEWS_IMPLEMENTATION_DECISION.md). No new research cells or training.

Exactly96 component calls: four64-record AG News contexts × two seeds × q/p source prefixes × original/old-SFT weights × meaningful/ordinal/constant tags. All calls use the same exact-cardinality ordered-object grammar and canonical World/Sports/Business/Sci/Tech labels. The tag instruction plus grammar is the intervention package, not an instruction-only or hidden-reasoning claim. System/tools are inherited unchanged; user topic contract is AG News, not TREC or sentiment.

Data is the first256 lexical normalized-question hashes from all7,600 rows of pinned fancyzhx/ag_news test revision `eb185aade064a813bc0b7f42de02595523103ca4`. NFKC→casefold→whitespace collapse; zero duplicate/conflicting groups. Full original text and source-row indices retained; no length/label/outcome filtering. Label-free randomized display and disjoint numeral maps reproduce the approved feasibility artifact exactly. DATA.json contains host gold; model payloads contain only source-ID→full-news-text records, never Parquet, indices or labels.

The four contexts are the sampling clusters. Each of12 cells has eight calls/512 planned assignments. Primary: meaningful-minus-ordinal displayed-record accuracy separately by weight/prefix; then old-minus-original interaction at each context/seed/prefix. Constant contrasts, whole64 correctness, per-call class-count errors/cancellation, actual tokens/cache and wall costs are secondary. No numeric-source diagnostic, permutation rescue, decoder repair or pooled6,144-item independence assumption.

Original857a7ce6 and historical validation-selected childc32de129 are fixed before AG outputs. They share one qualified dual-LoRA service. This is a new topic-classification task and fixed-weight comparison, not a fresh SST replication, randomized training effect, independent SFT seed or whole-RLM gain. Base-model pretraining/historical exposure is unknown. Underlying dataset license remains unknown; the card's research-use description is not converted to a permissive license. No remote dataset loader/code was executed.

Master981275401; sampling seeds981275411/981275421. Exact dispatch rotates/reverses six prefix/tag conditions and counterbalances adjacent weight-first order across the two seeds. Four asynchronous workers preserve queued start order, not completion order or identical cache state. Caps: collection900s, shared work1080s, owned inclusive1200s with120s cleanup reserve, outer1230s. One request per coordinate, timeout120s, temperature0.5/full support, max3072 output. Inherited collector stops on the first request/protocol error without retry; partial/unrun/infra remain null, complete malformed output is strict zero with alignment unavailable.
