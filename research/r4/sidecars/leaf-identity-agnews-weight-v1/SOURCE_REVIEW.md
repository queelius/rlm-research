# AG96 preparation and source-seam report

This is the implementer's bounded CPU source/qualification report, not an independent code review or result. No model/GPU call, new environment, scheduler, acceptance/launch change, dataset reselection or shared/frozen source edit occurred. Main/storage own independent review and launch authority.

## What is prepared

- `study.py`: authenticates data/design inputs, reconstructs exact first256/full-text/disjoint-ID/display mapping, builds the96 ordered calls and delegates request grammar/strict scorer/collector to pinned identity helpers. New summary separates both weights/prefixes and computes the interaction per context/seed/prefix.
- `driver.py`: privately reuses the qualified96 run function. Deltas validate both actual descriptors/live aliases and retain both hashes, raise collection600→900s, and substitute the prepared study callbacks. Source edits are counted and their executed hash is recorded. The inherited stop-on-first-error behavior is unchanged.
- `owned.py`: privately reuses the qualified1200s owned wrapper; adds the second exact adapter,900s collection metadata/930s command allowance, namespace labels and the established process-absence observer. Shared1080s work and120s cleanup reservation remain. No generic service layer was built.
- Two test files cover eight material checks, including the real collector with twelve successful synthetic weight/cell requests and a separate synthetic500→null/early STOP, thirteen fake HTTP calls total. No live endpoint was contacted.

## CPU evidence

Initial desired tests failed because the study/owned contracts were absent. Fixture correction supplied actual started/ended/model_called fields; a fake500 demonstrated the inherited collector stops immediately rather than finishing remaining cells. That behavior was retained and the test now verifies it instead of silently adding retries/continuation.

The pre-freeze run passed all eight tests. Preparation repeats them and records stdout/stderr/exit status in CPU_TESTS.json. Source mutation is rejected before data/model execution. Exact intended64 tags with displayed gold score64; wrong tags, labels, duplicate fields and short arrays fail strict validation. Unrun and infra scores remain null while complete invalid output is strict zero/unavailable alignment.

Full CPU physical proof reproduces **all96 approved feasibility body hashes and typed prompt-ID hashes**. All48 adjacent weight pairs differ only in model alias. All32 within-weight prefix/context/seed triples retain identical complete ID-bearing inputs/system/tools. Twelve ordered grammars compile. Prompt lengths4474–4707; max4707+3072=7779≤8192, leaving413 tokens. No crop, limit extension or alternate data was needed. Ordinary upstream Torch enum/CUDA-runtime warnings during CPU protocol imports do not represent GPU execution and are not hidden as clean model qualification.

The actual original857a7ce6907c759a8c1b478eb53029d3b700b81c3094d8edb3394e52def9fcb6 and oldc32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3 disk/config/source bindings verified. Both are already supported by the frozen service; no model download or current-service reuse is assumed. Actual runtime descriptor, /models and physical-response checks still run at launch.

## Provenance and scientific boundaries

Pinned fancyzhx/ag_news test revision eb185aade064a813bc0b7f42de02595523103ca4: Parquet SHA71de87ec66bc5737752a2502204dfa6d7fe9856ade3ea444dc6317789a4f13fb; acquisition SHAbba780a2e3cdc34b6f7d9ee48a86cd613a8dda9a500b8fc623bea11f58622f54. All7,600 normalized groups unique; selected-group-list SHA1b7b1c675bac3b7e8521ff2534a681f8e6dc3274234e978610c21ac2d4f4f88c. DATA retains source row indices and full texts; only ID→text mappings enter model-facing input. Public test/pretraining/historical exposure and near-duplicate news-event overlap are not resolved. The dataset card says license unknown; source research-use language is not a permissive data license, and no loader/software license is substituted.

Seed freshness is a repeated bounded named-plan audit, not global reservation. The failed fresh-SST proposal is preserved; AG News is a new topic task, not an unseen-SST replication. The intervention is instruction+grammar representation. Four contexts are the sampling clusters, not6,144 independent labels. Class-count cancellation is reported per call and cannot rescue displayed correspondence. Differences between original and old weights are training-associated comparisons, not a randomized training treatment or independent seed estimate. No whole-RLM effect is claimed.

READY is the final publication, binding complete source/data/request/weight/proof identities and exact argv/caps. A parent-reviewed acceptance and predecessor wait are separate operations. Frozen source will not be edited after READY.
