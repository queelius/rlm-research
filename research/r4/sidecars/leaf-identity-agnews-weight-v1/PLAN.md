# Bounded CPU preparation

1. Authenticate approved design/feasibility, dataset acquisition/Parquet and frozen identity collector/scorer sources. Reproduce first256 group membership and all source/display/numeral maps, without reselection.
2. Test first:96 coordinates and counterbalanced weight pairs, displayed semantic alignment, exact schema failures, null versus malformed, preserved full news text, weight-only body differences and immutable source rejection.
3. Reuse the qualified ordered tag grammar and collector via private callbacks. Adapt only task opening/vocabulary, the fixed dual-weight binding and900s collection cap. Preserve exact wire capture, response alias checks, stop/error rules and no repair.
4. Qualify all96 requests with actual vLLM ChatCompletionRequest tool serialization, pinned native HF template and XGrammar compilation. Compare full typed prompt-ID hashes and bodies against the approved feasibility; require48 weight-only matches and32 within-weight tag-input triples.
5. Reuse unchanged suite/V2 owned lifecycle and the previously qualified process-absence observer. Verify exact original/old disk identities and descriptor/alias/path/base bindings. Parent alone supplies the GPU and acceptance.
6. Freeze DATA/SEEDS/WEIGHTS/PROMPT_IDS/SCHEMAS/DISPATCH/source transformations, repeat focused tests, verify closure, publish READY last. No scheduler, model call, new environment or frozen-source edit.

After collection, preserve per-call and per-coordinate records and paired/cell/interaction summaries. Analyze component evidence only, with four context clusters and explicit censoring/cost uncertainty.
