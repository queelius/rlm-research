# Parent-owned equal-row control

This READY is CPU preparation, not GPU/launch acceptance. MAIN supplies the exact predecessor, empty sole GPU and shared operation lock. No auto-waiter, installs, independent lease, retry or old-output mutation is part of this sidecar.

Verify on CPU:

```sh
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-interface-sft-row-mean-v1/launch.py verify
```

After MAIN acceptance, with its one GPU assignment, existing inference API-key environment and qualified library paths:

```sh
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-interface-sft-row-mean-v1/launch.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-interface-sft-row-mean-v1/outputs/attempt-001
```

Parent process cap3330s. One work3180s/inclusive3300s clock covers new training then equal_row and global_target_token readouts, with120s cleanup reserve. Training uses the fixed existing training interpreter,600s optimization deadline/900s subprocess envelope; each native collection900s, readiness180s, four slots. Both inference services expose only the current root and fixed c32de child and are released in finally. The inherited vanished-process and Prime-title ownership handling is reused. No CPU outcome analysis holds the service after collection; the final terminal document is small.

Only treatment R is trained, from historical473210 with new empty Adam and the intentionally reused981284002 training RNG/order. Control T is the exact existing efab2913 final4. R must finish all four updates and authenticate its new READY identity/checkpoint state before either readout starts. A failed training run keeps its partial checkpoints and does not substitute another policy. R's actual bytes are resolved from its fixed-final4 RESULT/state, never a placeholder or validation-selected checkpoint. T's entire training/result/state/member closure is frozen in INPUTS.

The training wrapper imports the exact original train.py run/load/Adam/checkpoint code and replaces only its update callable plus provenance identity after original verification. Scalars in each row_losses ledger are the executed FP32 coefficient, original summed causal CE and weighted contribution; row_mass_sum is the actual finite-precision sum, nominal_row_mass_sum its mathematical counterpart. nll/weighted_ce_sum mean the new equal-row objective; token_nll remains a separate token-weighted diagnostic. All32 row bytes,64 exposures,3206 target exposures, four effective-batch IDs, seed/LR/clipping/dtypes remain fixed.

The readout wrapper keeps the original task/prompt/scorer/native collector. Its private data view replaces only the old PLAN/PROMPTS lookups with the frozen new-seed crosswalk and identical prompt token IDs. Both weight bindings are authenticated against actual model/config/base/child identities and live /models before any episode. Local77df23b8/image8cfe storage/hooks and ordered typed-child semantics are unchanged. First live physical prefix/alias checks remain in the original collector. Raw sampled token/logprob/finish-reason/usage evidence is retained; no likelihood here is reused for RL training.

Outputs: training/{LOAD_AUDIT,STEP-1..4,checkpoint-0001..0004,RESULT,SELECTION}; equal_row and global_target_token each contain BINDING, service ownership/release, READOUT_BINDING, and rollout/{INPUTS,LOCAL_RUNTIME,episodes,rows,role-audit,typed-audit,TERMINAL}; overall RUN/TRAIN_COMMAND/TRAIN_PROCESS/TRAIN_EXIT and TERMINAL. MAIN's enclosing operation records exceptional setup/exit failures even if a pre-episode validation prevents a rollout directory. No automatic resume/reroll is provided; new outputs require a separate decision.

Completed available wrong/malformed/empty replies retain the original0 convention; incomplete/unavailable remain null. A later cap does not erase a recorded completed result. Preserve finalize/setup/episode.ok separately from endpoint score, and retain unrun slots as unrun. Actual graph/physical validation and raw cached usage need the later independent one-pass audit, not a claim inferred from source-only preparation.

Limits: the exact same eight exposed context groups and fixed row→token service order; one training seed; no contemporaneous retraining of the already-authenticated token control; conditional format/coverage diagnostics, not causal mediation or a complete solution curriculum. Training weight effects include Adam/clipping responses. No GPU/model test was run during preparation. SWIG deprecation and tiny PEFT missing-base-config warnings are recorded in CPU_TESTS, not suppressed.
