# Implementation plan

1. Parse and hash-order all 500 official test source lines; preserve raw-train-overlap and prior-exposure strata.
2. Render exact native requests for base/c32 × wide100/small16 × two paired seeds.
3. Rebind the already qualified four-worker collector and owned dual-model lifecycle to the new immutable namespace.
4. Authenticate full native identity and strict maps, retaining malformed outputs as zero and unavailable endpoints as NULL.
5. Report per-width adapter effects, their interaction, paired seeds, coarse classes, exposure strata, and physical cost. MAIN alone may accept or launch.
