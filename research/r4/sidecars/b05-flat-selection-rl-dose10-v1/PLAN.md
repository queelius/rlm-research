# One fixed BA18 dose contrast

Question: does exactly10× learning rate strengthen actual local ID selection or overshoot, and does the fixed held readout change? Original BA18 had five semantic train changes and five FP removals but no held gain. This is not an LR sweep or a claim that dose explains failed transfer.

1. Reuse all reviewed BA18 math/source/native actions with exact original zero-B initialization, original seeds, G2/all18 denominator, eight credited/ten zero replies, array masks, TIS2 and clip1. Change LR1e-4→1e-3 only plus output/alias metadata. Fresh AdamW and one step, not cp1 continuation. CPU test actual fresh-Adam delta scaling and unchanged initialization/input bindings; actual entry must reach CUDA-hidden guard after full verification.
2. MAIN review/admission; only after qualified cp1 prepare fixed fresh base/cp1 train18+held18 (72 calls), both splits regardless outcome. Do not add optimizer steps, resample, change gates, or use held answers for training. Existing sidecars remain immutable.

Caps unchanged:900 science,1100 owner,1200 external seconds; expected original training48s. Preserve all intermediate initialization/logps/replay/gradient/Adam/RNG/commit receipts and exact adapter binding. Stop without update on an initialization, probability, replay or gradient failure. No GPU authority granted to this CPU preparer.
