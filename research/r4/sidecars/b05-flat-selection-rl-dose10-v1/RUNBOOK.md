# BA18 one fixed10x-dose arm

CPU-prepared, MAIN-only admission. Same original released-base weights and exact seeded zero-B LoRA tensors, same18 native actions/seed/array masks, eight credited/ten zero G2 replies, denominator18, biased token-TIS2, clip1 and fresh AdamW. Sole recipe change: LR1e-4→1e-3. Never initialize from trainedcp1. Exact actual initial tensor equality is a mandatory pre-update gate; mismatch is a legitimate no-update failure.

The source BA18 trainer, scorer, likelihood/replay tolerances, checkpoint/Adam/RNG and finite owner remain unchanged. The tiny local facade changes output/alias/LR and points the recorded held-source hash back to the unchanged frozen original. Qualifier retains all source/probability/replay/tensor/Adam checks, substitutes the fixed LR in its three arithmetic/config checks, and verifies actual original-initial tensors again. `INITIAL_PAIRED.json` is additive and checkpoint qualification binds its hash.

Run only READY argv under MAIN's shared flock and1200-second external timeout. Science900/owner1100 seconds; original trainer48 seconds is an estimate, not a promise. Output `outputs/attempt-001`, one checkpoint only. No score-dependent selection, new trajectories, second optimizer, learning-rate sweep or continuation. All intermediate raw-probability/replay/gradients/Adam/RNG receipts remain recoverable; failure never implies reward zero.

After actual checkpoint qualification, a separate fixed72-call fresh base/newcp1 readout uses the same train18 and held18 requests/seeds/current native runtime regardless training score. It compares dose-induced local fit and possible transfer/overshoot. Existing LR1e-4 endpoint and its readout remain immutable references; no broad learning or learned-depth claim. Held is already research-exposed and has nine material cases. Readout preparation/admission is separate and does not authorize further training.

CPU evidence: actual fresh-Adam one-step scaling test, initial identity acceptance/rejection, source/input/seed equality, unmocked trainer entry through all source checks to CUDA-hidden guard. No GPU calls during preparation.
