# Additive proposal timing correction

The immutable proposal `ideas/2026-09-12-musique-semantic-multihop.md` incorrectly attributes 32 episodes / 67 native calls / 368.39 seconds to short32 V2. That receipt belongs to V7 on one long context with repeated rollouts, not 32 distinct short contexts. It only demonstrates an accepted native interface and a conservative throughput reference. MuSiQue completion within its unchanged 30-minute external cap is uncertain; an incomplete run is reported as such and cannot pass the promotion screen.
