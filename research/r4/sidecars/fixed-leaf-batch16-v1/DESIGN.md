# Larger delegated batches

Exploratory followup frozen before its GPU calls,2026-09-08 22:24 UTC.
The fixed five-question operator completed with strict counts1/24 original and15/24
SFT. The free-root system was4/24 and6/24. Test the next simple harness parameter:
does increasing each delegated batch from5 to16 preserve useful accuracy while
reducing calls? The SFT examples used five-item arrays; sixteen tests batch-shape
transfer without any new weight update or prompt definition changes.

Same six64-record contexts,384 source questions,48 parent task/arm/seed coordinates,
immutable original/selected adapters, native first-response leaf contract/definitions,
T0.5,256token cap,4HTTPworkers. Four16-item batches per coordinate:192calls instead
of624. Context order and paired seeds unchanged; batching necessarily changes each
prompt and sample trajectory. No root model, grammar, repair, retry or dropped record.
Keep strict whole-coordinate contract outcomes and itemwise semantic accuracy separate.

Cap900seconds; atomic raw call and coordinate checkpoints. Selected test questions
are already inspected exploratory data; no untouched-source or generic planning claim.
All actual serving inputs and checks are reused from the frozen fixed-leaf comparison.
