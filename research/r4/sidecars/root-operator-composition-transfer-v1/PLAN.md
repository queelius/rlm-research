# Composition transfer96 implementation plan

> Inline scoped execution under MAIN approval; no subagents or Git/source edits. Writing-plans/TDD apply in this explicitly isolated external sidecar.

**Goal:** A launchable, source-pinned96-endpoint fixed6/24 comparison separating primitive transfer from three new compositions.

**Architecture:** Reuse qualified dose readout study/native collector/lifecycle through explicit local aliases. New protocol supplies only question specs, host oracle and immutable input allocation; native policy behavior stays free. Serial two-LoRA service phases change root checkpoint only.

**Tech stack:** Existing native Python/Prime environment, frozen token renderer/native RLM, existing c32, standard JSON and existing qualified owner service seam; no new dependency.

**Spec:** Immutable approved MD/YAML named in APPROVAL.md. No48-endpoint alternative.

## Constraints

96=8contexts×6questions×2policies; one paired seed/question.2048action/8192context,T.5/top_p1,4workers180s episodes.6480outer/6300work/6450owned:2×3000 phases (180startup+2730collection+90release),300harvest,150emergency cleanup,30outer margin. Early advancement, no retry/extension. Strict native finals and planned NULLs, whole physical ledger. No training/map/helper/example/forced child/width/root grammar. Existing interface and typed optional-child behavior unchanged.

## Task1 — Scientific protocol and oracles

Create ct_protocol.py, test_protocol.py. Write literal fixtures first for noA/noB, sum5/sum6, tiedmax, duplicate eligible users and A/B separation. Verify RED, implement independent primary and enumeration-check oracles plus exact unambiguous six-query text, verify GREEN. The root receives no oracle or algorithm. Record label-blind assignment and exact all-scope semantics.

## Task2 — Refreshed input provenance and native rendering

Create ct_study.py and prepare.py. Reuse pinned data loader and2192 inventory, subtract recent64 and additive named actual/accepted root groups. Preserve source receipt and failure preparation. Hash-select128, produce8 PUBLIC/GROUPS contexts,48 common FREE_PLAN,96 EVALUATION_PLAN, HOST_GOLD, QUERIES, per-family/stratum BASELINES, native prompts/IDs and unchanged NATIVE_TEMPLATE. Proposed master2026091500/seeds2026091501–1548 require collision check. Independently compare both oracles and verify exact source membership, no overlap/hidden labels, gold-mutation prompt invariance, root cap and actual native first request. Do not select on answer distributions.

## Task3 — Qualified collector and bounded owner

Create ct_collect.py and ct_owner.py. Explicitly alias only this study/protocol/binding over the qualified free path; exact six-operator host scoring reaches ct_protocol.answer. Retain modefree only/start0/stop48 and immutable source sampling. Owner routes exact fixed6/24+c32, preinventories96, removes old first-action probes, uses approved phase clocks, and reconciles all physical records even when endpoint snapshots are partial. Preserve native NULLs, raw trajectories and policy errors. No adopted historical billing terminology becomes a confirmed-GPU or paid-generation claim.

## Task4 — CPU qualification and READY

Focused tests: actual composed owner→service wrapper→intercepted Popen with correct twoLoRA binding/GPU visibility; owner→collector argv/alias→six-query oracle; real native first-prefix fixture and unchanged interface/optional-child inventory; protocol/source/baseline determinism and no hidden label dependency. No live services or model execution. Build source/input/checkpoint closure and actual entry argv in READY only after tests and CPU verify; ask MAIN to accept. Do not polish broad frameworks. Preserve failed preparations/tests and original source hashes. Pause implementation for published8B outcome audit upon MAIN relay.
