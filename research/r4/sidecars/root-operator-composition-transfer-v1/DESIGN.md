# Fixed-checkpoint primitive/composition transfer96

MAIN approved the complete immutable proposal at `../../ideas/2026-09-10-operator-dose-fresh-composition96.md` and its YAML (hashes in APPROVAL.md). That proposal specifies the estimands, all six literal query families, inclusion rules, oracle, caps and exploratory decisions. This implementation document is additive, not a replacement or historical correction.

Eight new16-record TREC train contexts, three primitive and three composed queries each, exact6 versus exact24, one paired seed/question:96 planned native endpoints. Both roots use the identical current native coding role, original truthful file-only task interface, optional Python/child tools and recognized optional-child exact-ID grammar. The existing acquisition API assets remain unchanged; no new algorithm/helper, canonical loader, map, example, forced width/child use, or root grammar is introduced. Scientific task question changes are the sole prompt changes relative to the qualified free runtime. Both conditions may acquire labels or solve manually. Primary is strict authenticated complete native `Answer: N`, separately for24 primitive and24 composition slots per policy, eight context clusters. Conditional available accuracy, planned NULL bounds and operational failure are separate. Actual source acquisition/faithful executed computation and error taxonomy are observational mechanism measures, never requirements silently added to primary correctness.

## Frozen selection and exposure

The original2192-group root eligibility receipt is pinned, refreshed against named current root PUBLIC/GROUPS/TASKS manifests, and excludes all64 recent dose-new groups even if any endpoint did not finish. The remaining2128 groups are hash-ranked once to select128. No labels, answer values or model outputs drive ranking, record order, user assignment or weights. All groups are c32-training-exposed and broad-catalog-prepared; newness means only root-unexecuted/unselected under this named inventory, not globally unseen or a new generator. Eight contexts are disjoint but six tasks within each are dependent. All four users have four records. The category cycle, threshold strictly>5, maximum includingzero, and cross-category sum counted once per B record follow the approved protocol. Both independent host oracles agree on48 truths.

Frozen realized zeros are7/48 per policy:3/24 primitives and4/24 compositions; corresponding best constants score6/24 and6/24. Full per-family histograms reside in inputs/BASELINES.json. No cases were rerolled. Inputs/PROVENANCE.json names source and seed inventories; CPU_COLLISION_RECEIPT.json adds exact128 q-ID and8native-context uniqueness/disjointness checks without changing allocation. Native prefixes are at most1076 tokens, leaving the approved2048 action allowance within8192. Gold mutation leaves actual native task files and first prefixes identical.

## Runtime and accounting

The isolated ct namespace reuses qualified dr/od native collector and two-LoRA service sources through explicit aliases, with only host query/oracle substitution. Model bindings are exact6 adapterefe7efc1… / exact24 adapter94022838… plus fixedc32; released base/tokenizer remain pinned. Checkpoints are fixed, not selected on this panel. The hash-fixed serial policy order is24 then6, a timing confound. Eight context blocks have hash-fixed dispatch and cyclic query rotations; family-position counts are1or2, not exact balance because8 is not divisible by6. Identical order and seeds are used for both policies.

6480s outer =6300work+150owned cleanup+30outer margin. Work reserves two3000s phases (≤180startup+≤2730collection+≤90release) and300harvest. Four workers,≤180s per episode,2048root action/8192context,T.5/top_p1. Phases advance early but never extend beyond their caps or borrow the next phase's reservation. All96 are recorded before service; no probes, training, retries or context resizing. Historical optimizer work and source acquisition are not new evaluation cost.

Every physical request file is reconciled, including requests under endpoints lacking RESULT. A physical attempt is not proof of returned or billed generation. Missing usage stays unknown; billing is unmeasured. Missing RESULT with physical/FAILURE evidence is attempted exception/interruption NULL; no-artifact slots are separately not started. Native malformed/no-final remains NULL; operational success0 is known. No answer salvage, generated-code reexecution or reward repair. Owner completion/release does not imply native availability. Authored synthetic qualification programs are clearly marked and are not science outcomes.

Promotion/retirement criteria remain the approved exploratory rules; report every family/zero stratum and paired context sign, not a selected favorable subset. Threshold/max share per-user accumulation and are not independent operator replications. This tests composition on new root inputs, not robust multibatch accumulation or whole-system unseen generalization.
