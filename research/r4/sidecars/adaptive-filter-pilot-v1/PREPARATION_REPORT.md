# CPU preparation complete — not launch acceptance

Sealed September9,2026; fresh read-only driver verification exits0 at09:22UTC.
No GPU/model calls, active-source edits, research process signals or launch.
Preparation took approximately32minutes after the checkpoint-8 audit handoff.

READY.json SHA256:
`582f90b85e67e47ff3ca2398d30db9bff79d117763d6f95465d1caa631bcda86`

SPEC.json SHA256:
`1d4ee02d1356998d7705faac4c52d81282d1c25aa7c0c3f2694f1d56e0a443c3`

driver.py SHA256:
`b129d5133acd2358dd21e6fe1d3e08360a0e7c40ab353308fee191afe26e72c0`

The source closure has353 exact file hashes, and READY binds34 additional
SPEC/test/final-qualification artifacts. Scientific source is now frozen; this
post-seal preparation receipt does not change it. Historical root473210… and
childc32de… are fixed, not selected from the current BROAD run.

## What is ready

Four new128-record contexts,512 unique leaf-train-supported source groups,
two fresh declared seeds,40 physical executions and48 logical cells. The eight
identical global fixed outcomes have explicit shared references. Canonical
requests, exact public package hashes, source membership, host-only gold, named
exclusions and a seed-collision audit are frozen before model observations.

One privately adapted inherited collector: queue groups2→5 and intersection
with the absolute collection deadline, no new scheduler. One owned service
wrapper uses the pinned lifecycle and disappearance observer. Caps are
startup180, collection≤2280, work2580, inclusive2700, outer2730 seconds;
per-episode45/300/15/15 setup/rollout/finalize/scoring. No episode/API retries.

Eight focused tests pass (4.124seconds including process startup); all Python
sources compile, including the operator program with top-level-await parsing
only. driver.py verify exits0 after READY, with40 planned and zero GPU calls.
Final qualification005 passes four real owned-rootless CPU fixture episodes:
operator success, ordinary free success, invalid child map and user filter.
Six fixture provider requests total, zero model calls. It proves zero operator
root requests, real child ancestry with no spawning model request, correct ACP
computed terminal, and exact initial child token/sampling identity for matched
inputs. Separate owned cancellation passes, retaining one child-native attempt
and no invented root terminal.

Main review caught the initial free-arm exposure of the complete operator
program. The failing setup-surface regression reproduced it; the corrected free
runtime receives only context/records/query/batch_contract, and the final real
free fixture asserts both operator files absent. Prior CPU proofs are retained
as superseded evidence, not relabeled as final qualification.

Other CPU preparation issues are retained in QUALIFICATION_NOTES: wrong initial
timeout API placement; remaining-set digest serialization convention; later
child-role catalogue resolution; and typed validation mutating the JSON spec.
The final freeze validates a deep copy and reused already-frozen request inputs
only on exact equality. No input membership or answer-conditioned choice changed.

## Interpretation limits to retain

User gold2/1/2/2 means constant2 hits6/8 repeated-seed user episodes; global
gold16/18/22/20 varies. Accuracy alone or zero child calls cannot establish
adaptation. Correct direct-root semantic work remains valid. This is a whole-
policy comparison with a new public builder/parser interface, not unchanged
historical-root evaluation or isolated routing causality.

Native initial child inputs match when ordered rows/seed coincide; full tool-
visible filesystems do not, because only operator runtimes carry operator code.
Generic children retain ordinary tools and can inspect accessible public files.
Requested IDs are not a semantic-inspection guarantee. Runtime-writable operator
transcripts remain diagnostics, corroborable by raw native prompts and answers.

The inherited raw scorer's zero for an empty result is preserved separately;
the declared primary endpoint remains null for unanswered/incomplete episodes.
An invalid child map is a policy-interface failure, not automatically an
infrastructure exclusion. Child-only operator traces are not root-training
exports, regardless of inherited raw agent.trainable metadata.

## Exact proposed argv

```text
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/adaptive-filter-pilot-v1/driver.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/adaptive-filter-pilot-v1/outputs/attempt-001
```

Main alone supplies exclusive GPU/credential and accepts the exact successor.
No launch follows from this report. Detailed CPU analysis is explicit after
owned service release; fixed shared-global dependencies and nulls must survive it.
