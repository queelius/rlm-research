# Main-only launch handoff

Do not launch from CPU preparation. READY is not acceptance. Main must assign
one exclusive GPU and the existing STRICT_RLM_CALIBRATION_API_KEY without storing
its value. Do not attach to another service or change any frozen historical source.

Read-only readiness:

```text
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/adaptive-filter-pilot-v1/driver.py verify
```

Exact proposed scientific argv (outer operation cap2730 seconds):

```text
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/adaptive-filter-pilot-v1/driver.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/adaptive-filter-pilot-v1/outputs/attempt-001
```

This creates one owned historical-step8/fixed-c32de service, runs40 distinct
episodes in frozen five-job blocks, and releases only owned service processes.
The unchanged qualified lifecycle plus narrow disappearance observer is privately
imported from the pinned receipt-uptake driver. Work2580/inclusive2700 plus outer
2730 seconds; cumulative collection≤2280 and one absolute deadline. No retries.

After terminal and owned release, CPU-only projection:

```text
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/adaptive-filter-pilot-v1/driver.py analyze --output /project/alex_phd/runs/rlm-research-r4/sidecars/adaptive-filter-pilot-v1/outputs/attempt-001
```

Retain original raw episodes, native routing captures, operator transcripts,
all errors/nulls, STATUS/TERMINAL and release receipts. Compare logical cells by
four contexts, preserving shared global IDs; do not claim48 independent episodes.
The user-answer constant2 baseline is6/8; accuracy alone does not show filtering.
