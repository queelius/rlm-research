---
status: cpu_qualified_pre_gpu
date: 2026-09-10
planned_calls: 192
---

# Query-conditioned TREC interface implementation

The isolated sidecar freezes the approved 192-call comparison without creating
an output attempt. It reuses the qualified official-TREC dual-model lifecycle,
native renderer, Qwen3-4B base, and fixed c32 adapter. New code is limited to
the panel/prompt builder, two-contract scorer, collector, owner, and focused
qualification fixtures.

The exact input closure contains 128 label-blind selected official-test rows,
eight fixed batches, six cyclic ordered pairs, 48 fresh shared seeds, all 192
native bodies, private gold, provenance, and the named-input seed scan. Both
interfaces state identical pair definitions and six category definitions.

CPU qualification exercises a genuine choice-bearing native response through
the HTTP collector and renderer, and executes the actual owner through the
registered service wrapper/config path up to an intercepted inference-process
launch. No GPU, service port, model generation, or scientific output occurred.
