# CPU preparation plan

1. Freeze the deterministic 128-record panel, six category pairs, 48 fresh
   seeds, 192 coordinates, exact native bodies, projected gold, provenance and
   seed-catalog scan.
2. Score full-six and A/B/other contracts without repair, retaining malformed
   observed outputs as zero and unavailable endpoints as NULL.
3. Reuse the qualified four-worker native transport and dual-model lifecycle,
   with an exact attempt namespace and complete raw cost union.
4. Qualify inventory, pair identity, prompt/schema differences, scoring,
   native response authentication, and actual owner-to-collector/service
   composition on CPU. MAIN alone may launch the GPU run.
