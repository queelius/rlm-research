# Fresh synthetic join: externalization × record order32

MAIN approved CPU implementation2026-09-10, with strict authenticated native-final correctness as the shared primary in both representations. This supersedes the compound file-use primary in the motivating two-backups idea note. Actual file/copy use and executed correct order-invariant joins are separate mechanism measures, never primary prerequisites.

## Frozen question and generation

Does file-only packaging induce faithful retrieval and lower copying cost on new synthetic purchase worlds, and do computational errors remain sensitive to irrelevant record order? Eight worlds,48 purchase facts/world,12 customers with four purchase events each, four sampled products drawn from K/L/M/N/P/R/S/T, query the first two sampled products. Each of48 products is sampled independently uniformly from the world's four products; duplicate purchases are allowed. This unconditioned generator does not impose the prior panel's engineered answer sizes. Gold sizes are computed only after generation, disclosed including zero, and never used for rerolling/selection.

Generator seeds981591011–018, order seeds981591201–208, dispatch master981591001 and sample seeds981591101–108 are frozen in source before model outcomes. Fresh customer c+4digit and record r+5digit IDs are uniformly sampled without replacement, excluding explicit historical ID inventories and prior new-world IDs. No new ID-length advantage versus original nativejoin. For each world a separate RNG shuffles integer record indices without consulting product/query/gold; the second order is exactly that permutation reversed. Query, customer list, fact multiset and gold remain identical across both orders.

Explicit novelty inventory: WORLDS.json in root-native-partition-join-v1, root-record-interface-v1, root-record-externalization-v1 (three copies of four worlds), and root-partition-report-pilot-v1 (four older worlds). Pin all four manifests; require new record/customer IDs and complete fact triples disjoint. Do not infer global research/pretraining-unseen content. Schema, products, task/query generator family, model and interface are familiar; the follow-up was selected after externalization16's strategy/cost signal and one order-sensitive error.

## Thirty-two contemporary native endpoints

Eight worlds × random/reverse order × I inline+file/E file-only =32. One sample seed/world shared across all four cells. Four-cell cyclic rotations put each cell at each dispatch position exactly twice; worlds dispatched by frozen hash order. Four workers; completion order is not controlled. Record order is semantically irrelevant, not automatically a favorable versus unfavorable order. No grouping/sorting by queried products and no filtered failure selection.

Both arms have the same released Qwen3-4B cdbee identity/no LoRA, native existing purchase/coding role, optional Python/child inventory, schema/query and file metadata. evidence.dat bytes match exactly within each same-order I/E pair; full JSON is also inline only in I. As in externalization16, context.txt mirrors each arm's inline block (I fullJSON, E empty); thus a representation package, not all-file-identical pure prompt manipulation. query.txt is common original query. Across order cells evidence.dat and I/context.txt contain the same facts in reversed order. No host answer, algorithm, example, historical transcript, forced retrieval/children, output grammar, new acquisition or trained policy.

## Metrics and decision boundary

Shared primary: strict authenticated native final JSON array of unique valid customer IDs in ascending order, exactly equal to dataset/oracle join. No extraction/reorder/repair. Authenticated malformed final0; missing/unverified finalNULL; retain all32 planned slots and availability/NULL bounds plus operational success. Report eight world means, within-world order-discordance and I/E paired results. Four cells/world are repeated measures, not32 independent worlds. Realized empty answers remain and are reported separately.

Separate mechanisms: actual evidence.dat/context.txt/query.txt access, decoded file state versus copied literal fidelity, complete relevant evidence, executed grouping/intersection/other valid computation, order-sensitive one-pass errors, intermediate observations and final concordance. No sampled-code reexecution; code mentions/AST and correct final alone do not prove executed aggregation. File use is not required for primary or successful computation; faithful literal joins can count as mechanism success with their own cost. Across random/reverse cells report whether correctness and observed algorithm are order-invariant, not proof of universal invariance.

Primary outcome changes are exploratory. File uptake/lower copying cost without accuracy change supports a packaging/strategy result. Persistent order errors after correct loading motivate a minimal operator diagnostic, not more retrieval reward or epoch extension. Do not pick checkpoints or acquire new models from these results.

## Runtime, cost and ownership

Reuse the qualified externalization16 native/capture/owner/service seam in this isolated sidecar. No installed-source edits/public compatibility. Actual2560 action/8192context, temperature0.6/top_p0.95, four workers/120s episodes, no retry.1500outer =1380inclusivework+90ownedcleanup+30margin; startup≤180, collection≤1140, closure60. Last cleanup bounded by remaining owned1470 and90seconds. Inclusive caps are maxima, not waits.

Prewrite32 NULL slots before service. Retain all native root/optional-child physical attempts, returned completions, observed input/output/cache and missing usage, pretransport rejection and elapsed failure time. No acquisition cost or hidden source calls. Attempts are not all confirmed GPU generations; provider billing unknown/not measured, no token-to-FLOP claims.

MAIN alone accepts/launches/manages service/GPU/queue. CPU fixtures use authored native actions and intercepted model launch only; no scientific outcomes. Exact owner→service→intercepted Popen, actual native file/child/final fixture, collector32 inventory, generator/seed/disjointness/manipulation/strict-primary tests precede READY. No old artifact changes. This experiment's author cannot provide an undisclosed independent audit.
