# Operator SFT Implementation Plan

> Agentic execution: use executing-plans and test-driven-development inline; no subagents or GPU authority.

Goal: implement DESIGN.md's72 complete native trajectories,6 complete updates and48paired free endpoints in this external sidecar.

Architecture: exact-path dependency loaders isolate qualified joint numerical/native components. Inputs are prepared without a starting choice; explicit START_BINDING is required before any READY or execution. Owner stages enforce immutable absolute final reservations.

Tech: existing native Python3.12 environment, ACP/IPython, tokenizer, PyTorch/PEFT, existing an27 lifecycle. No installs or old-source changes.

Spec: DESIGN.md. The8400/8100/8280 inclusive nesting,72slot all-or-none corpus, fixed6 treatment, current coding role and pending binding are global constraints.

## 1. Frozen protocol/input construction

Files: od_study.py, od_protocol.py, prepare.py, test_protocol.py.

- [ ] Write tests that import the new module only inside test bodies; assert72train/24per-policy readouts,180child/324root actions, group exclusions and original query truth.
- [ ] Run focused tests and record missing implementation failures.
- [ ] Implement label-blind template/name schedules and fixed six gate members; preserve source receipt and all192 train groups. Build authored producers/reductions with no gold/map literals and actual-state scalar stop.
- [ ] Run authored CPU scope/operator examples with deliberately wrong predicted labels; assert targets follow predictions, not gold. Verify independent string/numeric literal masks.

## 2. Native capture and binding

Files: od_binding.py, od_collect.py, test_native.py, test_entry.py.

- [ ] Write tests for invalid/absent starting binding, actual file-only task prefix and real helper/schema q+12hex IDs with four public fields.
- [ ] Run failures; implement exact-path aliases around existing QSR/CE native environment and qualified provider interception.
- [ ] Capture authored fixture through actual root→tool→typed child→root graph with fake physical model transport only; verify map accumulation, wrong predictions, terminal scalar, current-action masks and physical versus authored calls.
- [ ] Reject structural failures without reroll; preserve planned72 corpus slots and stop admission. Exercise actual collector CLI parsing and descriptor/policy identity.

## 3. Fixed six-pass learning

Files: od_learning.py, od_train.py, test_learning.py.

- [ ] Test .45/.50/.05 per-trajectory weighting and current-action-only masks with literal fixtures; test gate six fixed rows/nonliteral headroom.
- [ ] Implement reuse of qualified loss/update and checkpoint writer; fresh Adam and fixed72×6, no partial optimizer commit or checkpoint5 substitution.
- [ ] Run focused tiny CPU Adam test proving parameter delta, cursor and checkpoint persistence; verify six-example projection includes overhead.

## 4. Staged owner and final qualification

Files: owner.py, test_owner.py, CPU_REPORT.json, implementation report.

- [ ] Test exact capture1800/train3600/final1200+1200/finalize300 transitions, failure→finals and absent6 plannedNULLs.
- [ ] Implement owner using qualified owned service lifecycle and explicit composed imports; no automatic resume.
- [ ] Intercept only service process launch while running real owner→service config/descriptor/binding path; test actual collector/trainer argv and no wrong generationalias.
- [ ] Run focused tests, preserve source/input hashes and detailed cost projection. Send CPU-ready-for-review status; do not write READY until MAIN supplies and approves START_BINDING.

No Git commits are made to this external non-repository sidecar. Historical feasibility and all sealed source/output directories remain untouched.

## Execution status, 2026-09-10

MAIN approved exact857 starting binding after reference calibration; START_BINDING.json now records its authentic conversion receipt and base identity. Steps1–4 are implemented in the od_* modules. Fresh focused aggregate:14 tests passed in25.93s, including the actual authored CPU native trajectory, typed IDs, full composed owner/config/descriptor/intercepted launch, learning-failure final transitions, and tiny actual Adam step/expired no-op. Checkpoint persistence uses the unchanged qualified writer and ancestry validator; no new GPU checkpoint has been produced or claimed. The numbered unchecked entries above preserve the original prospective checklist rather than implying a run happened. CPU_REPORT and READY seal follow these focused checks; MAIN alone accepts/launches.
