# Diverse-operator complete-transition SFT reachability

Status: MAIN approved CPU implementation on2026-09-10; no READY/GPU/service/launch authority until MAIN supplies the final starting binding after reference24. This is a prospective revision of the measured Option A note, not the unchanged36-trajectory/12-update proposal. It uses72 unique trajectories and6 full-corpus updates, preserving432 trajectory exposures but broadening the training inputs. There is no A36 comparison, hence no causal breadth claim.

Question: does supervised acquisition→actual live accumulation→query-sensitive count/distinct-user/visible-weight reduction→native stop improve free held-out composition reachability over the SAME unchanged starting adapter? No claim of general adaptive decomposition, selective acquisition or reward inaccessibility. Low66c is not silently relabeled as a released baseline. Starting candidates857/efab/66c are not interchangeable aliases: the exact checkpoint path, adapter/config hashes and lineage receipt must be supplied in a single frozen START_BINDING.json before READY. Current native coding role is common throughout, not compact selected from two CE successes. Fixed c32 checkpoint0128 child is unchanged and never trained.

## Corpus and supervision

Use all12 existing QSR training contexts,16 public records each/192 source groups. Three supported operator/scope queries per context ×width4 and16 =72 unique fixed trajectories. Data are previously research- and child-training-exposed, not pristine. No old sampled maps or evaluation traces enter the corpus. Four semantically equivalent query templates and REPL variable-name layouts rotate by independent deterministic schedules, not by target label/gold/operator/scope. Their exact balanced cross-tabs and IDs are frozen before capture. All-scope wording is unambiguous, with exact public id/user/text/weight meanings and no category field in either file.

Each teacher root action is AUTHORED, executed through the real native root→IPython→child→root graph. Initial action imports helpers and loads actual records.json only; no invented records/state. Width4 uses four real c32 typed acquisitions with cumulative-map observations; width16 uses one. Full72 corpus therefore requires180 physical c32 acquisitions and324 authored root actions (180producers+72reductions+72stops). The typed child grammar binds requested IDs/categories, not gold or aggregate. Native histories contain genuine wrong child labels unchanged. Reduction uses the same live accumulator and actual record users/weights, prints its actual resulting scalar; terminal target is exactly Answer: that observed scalar, including dataset-wrong scalars. Host gold is scoring-only and cannot influence query/source selection, actions or targets.

All72 slots exist before capture. Any structural capture failure preserves all attempted artifacts and stops corpus admission—no retries, replacement, success filtering or partial-corpus training. Later slots remain explicit planned unrun. Capture-all precedes a fixed six-example forward-only gate spanning three operators and both widths; members fixed label-blind, not selected from correctness. Gate separates nonliteral producer/accumulation/reduction NLL from string literals, envelope/control and terminal scalar tokens. Numerical literals are excluded from mechanism spans as well as strings. Need genuine nonterminal headroom and a measured projected cost fitting the train reservation; saturated scalar-copy CE cannot pass the gate. The exact finite/headroom threshold is4/6 corrective mechanism means above0.1nat; no gate-dependent update count or corpus substitution.

SFT uses qualified current-action-only native token-prefix labels; preceding root actions, observations, child tokens and setup are masked. Joint per-trajectory loss mass is.45 equally shared across producers,.50reduction,.05stop; normalized across72 trajectories. These are objective masses, not gradient-share claims. Fresh AdamW lr1e-4,weight_decay0; frozen base/c32, rank8 FP32 trainable LoRA, clip1. Each update is one complete shuffled72-trajectory pass. Save checkpoint1…6 with model, Adam cursor/state, RNG, delta norms, corpus/native-source ancestry and prior checkpoint hash. Do not commit partial updates. Primary trained policy requires exact authenticated checkpoint6; missing6 is an unavailable planned treatment, never substitute5. Resume requires separate authorization and starts after a committed update, without replaying old capture or restarting Adam silently.

## Final readout

QSR readout04…11, eight source contexts disjoint from all training and reference calibration00…03; three held-out operator×scope families per context, one new paired seed =24 endpoints per policy/48planned. They remain research/child-exposed. No training-state continuation, forced child, final grammar, answer fallback or learned-policy checkpoint selection. Both policies use identical exact tasks, current native coding role, source files, native root2048/context8192 and frozen c32; baseline first then fixedSFT6 is declared service-time-order confounding. Native return/branch authenticity is distinct from orchestrator completion. Malformed authentic finals strict0; missing final/transport/loop or unavailable treatment NULL, with separate operational planned-denominator sensitivity and bounds. Record actual executed acquisition/reduction/stop mechanisms and physical/root-child/token costs, not just final scalar agreement.

## Inclusive budget and feasibility

Outer8400 =work8100 +owned cleanup180 +outer margin120; owned8280. Work reservations add exactly: capture1800 (service startup and release INCLUDED), gate/train3600 (model load/checkpoints INCLUDED), baseline final1200 (startup/release INCLUDED), fixedSFT6 final1200 (startup/release INCLUDED), finalize300. Stage deadlines are caps, not waits: each is the minimum of current time plus its cap and the owner work deadline minus all later reservations. Every early completion advances immediately; early stages cannot consume either final reservation. Learning failure/failed gate still enters baseline readout and records trained-treatment NULLs if checkpoint6 absent. No additional diagnostic partial-checkpoint readout.

Measured old joint capture16/40child/72actions took290.051s: linear72 forecast1305.23s plus service overhead within1800, not guaranteed. Old joint4 updates on16 took182.568core/197.753total:72×6 proxy1232.33core seconds (6.75×);432 trajectory exposures,1944 authored root-turn exposures across six passes. Old target/forward tokens per16-corpus pass5016/113096 imply22572 target and508932 forward tokens/new72 pass,135432target and3,053,592forward exposures total. These are only same-length proxies: hex IDs, weights, longer question variants and real cumulative observations can enlarge attention cost. Six measured forwards project full6-update cost as measured/6×72×6×3, plus300s fixed load/checkpoint allowance, and must fit remaining3600stage allocation. Training is not authorized merely to fill the GPU. Final1200/policy is a cap, not a guarantee that all native roots return.

## Minimal implementation and provenance

New isolated sidecar only; reuse qualified joint numerical loss/checkpoint machinery and QSR/CE actual native files/task/typed-routing seams through exact-path imports with explicit aliases. No generic `from collect import...` composition. New modules handle protocol/input preparation, binding, capture/free collector, training and staged owner; no refactoring old code. Focused CPU tests cover task truth/label mutation,72/180/324 inventory and split/template/name balance, actual typed q+hex records, genuine authored native root→child→root prefix/masks, wrong-label preservation, loss weights/Adam/no-partial commit, stage reserve/missing6, and composed owner→service→descriptor/config→intercepted process argv. No live model, services, GPU or locks during preparation. READY remains absent until MAIN final binding decision and source/input/CPU review.

## Implementation approval and exact start, 2026-09-10

The earlier pending-start wording records the design stage. MAIN subsequently approved the exact served released-reference857a7ce6 adapter/config/conversion/base identities in inputs/START_BINDING.json. This is a least-assumption lineage choice, not calibration superiority or bare-base equivalence. Exploratory reference calibration was2/8, success8 was3/8, interface4 was2/8 (availability8/8,8/8,5/8); all successes were zero, all policies0/4 nonzero and no child calls. Calibration00–03 and readout04–11 remain disjoint. No policy choice uses the new study's readouts.

The857 source is a key-converted released-reference adapter with CONVERSION.json, not an optimizer checkpoint; no state.json is invented. Training begins fresh Adam. Gate and six updates share one loaded model in a single bounded process, with no gradients before gate passage. The learning rate1e-4 is the unchanged qualified joint recipe. The inherited physical-record field paid_model_call is a legacy transport label, not evidence of billing; actual dispatched calls including failures and unknown output/cache costs are recorded separately. Scientific runtime stays native coding role/root2048; the authored CPU fixture verifies the same cap.

Template/name schedules inspect context index and width only, never operator/target/gold. Existing supported scope assignments correlate with context parity, however, so template×width×scope is not a fully independent factorial. MAIN accepted this limitation before READY without redesigning inputs: baseline/SFT comparisons remain paired; no causal attribution to template, width, scope or breadth is made.
