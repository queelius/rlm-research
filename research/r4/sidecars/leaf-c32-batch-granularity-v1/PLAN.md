# Bounded implementation plan

1. Pin exact two source contexts/gold and six native wide templates from the sealed diagnostic. Label-blind16 partitioning; freeze new paired seeds after prepared-source collision scan. Separate public data/private labels, and verify label mutation does not change requests.
2. Generate76 native request bodies/ordered wires and original→new coordinate receipt. Tokenizer roundtrip of every wide prompt must be exact; all small prompts preserve original prefix/suffix and only replace Records JSON. Confirm max input+2048≤8192 and schema matches current IDs/categories.
3. Minimal four-worker native generate collector with per-request dispatch/response/result checkpoints, strict complete-map scorer, full interrupted inventory/cost union, no retries/tools. Explicit missing native versus authenticated invalid distinction.
4. Qualified corrected native lifecycle owner via explicit module paths/aliases. Actual owner→service/config/descriptor→intercepted inference Popen and owner→collector parser fixtures. No GPU, listener, live lock or model load during tests.
5. Focused tests and immutable READY/source/input/command receipt. MAIN independently accepts and launches. Stop editing at seal.
