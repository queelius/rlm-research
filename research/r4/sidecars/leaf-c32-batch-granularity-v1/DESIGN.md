# Matched c32 batch granularity: exposed512-record diagnostic

MAIN approved CPU preparation, not GPU launch. Falsifiable question: holding the512 records, IDs, order, category definitions and fixed c32 native child interface constant, does contiguous16-record classification improve complete-map semantic fidelity relative to100/100/56 batches?

The two contexts are exactly boundedV2 rows20/30 (scale-state02/03 at256 records), selected purposively after genuine root accumulations failed because of child labels. They are root-research/catalog/child-training exposed, not random or pristine. Historical wide outputs identify the prompt/source interface only; BOTH experimental arms receive fresh calls. No root rerun, map repair, gold constraint, sample reroll, stronger weights or source replacement.

Each of two fresh paired seed blocks uses both contexts under W=100/100/56 and S=16 contiguous partitions:2×2×(3+16)=76 calls,1024 predicted labels/arm. Seed and original record order are common within each context/replicate. The native child system/tool template and user definitions are reconstructed from the exact retained original child prompt; only the Records JSON list and its exact-ID/category schema change for small batches. Wide native prompt IDs must match historical IDs exactly before READY. The only wide request sampling change is the fresh seed. Fixed c32 checkpoint0128 on pinned Qwen3-4B Instruct2507 base; max_tokens2048,temperature.5,top_p1,top_k−1,min_p0, original stop tokens and native parser. Typed grammar supplies IDs and canonical categories, never gold labels. No tools are executed; a returned wrong tool route is authenticated model-invalid0, not transportNULL. The inherited dual-LoRA service may also load unused fixed24 root adapter; only c32 receives scientific calls.

Four asynchronous workers, with deterministic fixed arm-first balancing across the four context/seed blocks. Each request checkpoints prepared body, actual dispatch timestamp, received raw response, native token/parser verification and score; no retries. Dispatch hook marks an attempted transport, not proof the GPU accepted it. Store server created/requestID where returned, client queue/start/dispatch/receive/finish and timeout timings; do not infer server scheduling latency from timestamps with unknown clock alignment. Prefix caching follows the qualified service, so cache effects/unequal call counts are disclosed.

Primary per-record semantic correctness is admitted only from an authenticated native returned completion with a complete exact-ID/canonical-label JSON object. Authenticated malformed/wrong-route responses contribute strict0 for the entire requested batch; missing/failed/unverified transport remainsNULL for its records. Complete-map validity and per-ID canonical fidelity are separately reported. Shape-only diagnostics may describe present labels but never salvage primary, reorder output or fix labels. Output object key order is descriptive, not a constraint of the existing map protocol.

Report per-context/seed paired semantic counts and NULL bounds over the same256 records; only two context clusters, not512 independent contexts. Complete-map rates have unequal denominators12wide versus64small calls across repeats. Target count/weight error derived from actual predictions is a secondary host diagnostic and requires all256 labels; it is not executed root success. Category confusion, position, ID fidelity and known/unknown physical tokens/times are secondary. This is a batch-granularity package: input/output length, number of calls and overhead all change; no equal-FLOPs or pure-attention claim.

1200s outer,1170owned,1050work shared from owner start. Startup cap180s inside work; collector stops new/active calls by work−30, retaining30s harvest reserve. Each call timeout≤90s and remaining collective work. Release cap90s after work, then30s owned finalization; outer margin30s. Stages advance early; caps are not waits. All76 planned slots survive startup/collection/interruption failures; raw returned responses/unknown attempts remain in the disk ledger even without RESULT. No automatic resume or endpoint reroll is authorized.
