# Mixed-cardinality authoritative leaf SFT

Exploratory two-curriculum follow-up,2026-09-08. A trains on sizes1,5,16,32; B on1,5,16,64. Both start from exact original converted adapter857a7ce6, not the earlier trained child. Two epochs, each of the same5065 training groups exactly once each epoch. Per-size quotas differ by at most one record; larger final partial arrays are explicit residuals, not duplicated examples. Stable group sorting, per-epoch seeded shuffle, deterministic bucket assignment and separate array-order shuffle; A/B use the same bucket assignments. Source300 validation and489 test stay disjoint and unchanged.

LR1e-4, AdamWwd0, clip1, microbatch2, accumulation up to16 arrays/step, native assistant-only token-normalized loss including terminator, BF16 frozen base/FP32 original rank8 LoRA. Same native tools/template/definitions as prior SFT, no grammar training, no truncation. At most3600 cumulative optimization seconds per curriculum; checkpoints every16 optimizer steps and each epoch include optimizer/RNG/exact cursor and hashes. Fixed finalepoch2 checkpoint, never validation/test selected. Monitor the same fixed5 validation300 each epoch; final fixed5 test489 is directly paired with prior original baseline. Additional size-stratified test evaluation is a separate queued command. Matching record exposures does not match array counts, target/prompt tokens, padding or optimizer steps; manifest and step metrics report these differences.

Prospective endpoints include fixed5 test retention and six exact64-record composition prompts at finalepoch2: strict whole-array coverage/accuracy, quartile-position accuracy (1–16,17–32,33–48,49–64), entity-prediction versus gold counts per quartile, and truncation. These were chosen after the parent's finding of strong early-position but collapsed late-position schema64 predictions; this is an adaptive mechanism experiment, not a blinded test. The additional489-group evaluations at each size1/5/16/32/64 are queued separately with the same strict primary scoring. No offline repairs or alias fallbacks change rewards.

CPU qualification found that semantically identical tool dictionaries have different outer JSON key order in prior-SFT native prompts (type/function) and frozen serving-request prompts (function/type). Training and fixed5 validation/test preserve exact prior-SFT rendering. Final64 HF evaluation preserves exact frozen greedy-probe prompt IDs instead; both ID variants and this caveat are retained. No tokenizer/message normalization conceals the difference, and neither frozen helper nor active probe is edited.

HF backend sanity probe: existing selected c32de checkpoint, six exact64-record child prompts from frozen batch64 comparison, native tokenizer/tools template unchanged, greedy decoding, cap1024, no schema. A matched vLLM greedy6 request set is separately frozen in PROBE_SPEC.json. Strict first-response scoring and raw tokens retained. This controls sampling temperature against the new greedy vLLM probe; backend batching and HF FP32 versus vLLM BF16 adapter arithmetic still differ. No GPU calls during preparation. Freeze source/recipes/data before readiness; no previous SFT or active sources are edited.
