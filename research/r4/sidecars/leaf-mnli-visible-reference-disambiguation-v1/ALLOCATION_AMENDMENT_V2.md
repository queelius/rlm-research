# Additive allocation amendment V2

Approved after the 40-call draft selected and wrote its eight contexts, before any model output.
Preserve the draft source and selected `DATA.json` unchanged. Add `aligned_legacy` as the sixth arm,
yielding the full visible-relation (wrong/alien/aligned) by wording (legacy/explicit) grid:48 calls.
This estimates the aligned wording response rather than treating a wording gain in wrong-visible as
specific to that conflict. Simple effects and their descriptive interaction are primary; none is
proof of a latent mechanism. Eight clusters cannot support equivalence claims.

The same texts, requested tags, golds and paired seed remain within each six-arm block. Six arm
orders rotate deterministically; with eight contexts the first-position count cannot be perfectly
balanced. Natural wordings are frozen without artificial filler. Outer cap becomes1440 seconds;
work1320, owned1410, startup180, cleanup90 and outer margin30. MAIN alone may launch.
