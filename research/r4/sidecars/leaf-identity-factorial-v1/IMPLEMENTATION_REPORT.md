# Ready for parent review: remove the counter's accidental source-ID meaning

CPU preparation completed September9,2026. No model calls, GPU/service actions,
environment changes, launch acceptance, or historical-source edits. READY.json
is published last by seal.py after a fresh immutable-input verification.

## What is frozen

384 calls: q/p source prefix × overlapping/disjoint numeric IDs × meaningful,
ordinal or constant output tags × eight reused64-record contexts × two unchanged
presentation permutations × two fresh seeds. Four TREC and four SST contexts
remain the source units, not384 independent examples. Old selected child c32de
is the only weight; this is a correspondence control, not training or an RLM gain.

The fixed seed master is981269001; sampling seeds are1127389105 and1656446467.
No master/seed collision was found in113 named sidecar SPEC/READY/RECIPE/CAMPAIGN/
SEED JSON files outside outputs and this sidecar. Exact inventory hashes and
search scope are in SEED_AUDIT.json; this is not a global all-history proof.

Disjoint four-digit values are selected by label-independent hash ordering and
assigned to the original stable source ranks. Values0000–0064 and last-three
suffixes001–064 are absent. Exact affine/mod10000 mappings from source rank or
either displayed order are rejected. IDs remain attached across both seeds,
permutations and prefix swaps. This is not an offset or ordered-number encoding.
The approved exclusion does not forbid suffix000: one selected ID is3000 in
context1. It shares zeros with the constant tag but is not numeric0000 and does
not overlap any ordinal number. No post-selection change was made to hide this.

All96 q/overlap requests reproduce identity96 byte-ordered serialized request
objects apart from the new sampling seed. Within each of128 three-arm matched
groups, question text/order/source IDs, system and tools are identical. Truthful
tag instructions and the exact grammar are necessarily different across arms.
The12 conditions are interleaved over32 coordinates; each condition occurs at
each dispatch rank two or three times, with forward/reverse complete cycles.

## Verification evidence

- Six desired regressions failed before implementation (pytest-123, exit1).
  Seven focused checks now pass; CPU_TESTS.json preserves the final stdout.
  Additional coverage verifies all384 source/permutation/prefix crosswalks.
- A reversed-ID fixture produces0/64 primary displayed accuracy and64/64
  numeric-source diagnostic agreement. Disjoint and invalid diagnostic scores
  stay null; wrong tags cannot produce semantic alignment.
- The real pinned collector processed12 fake HTTP calls, preserved12 exact wire
  bodies and per-call checkpoints, and scored all three arms in all four
  prefix/namespace combinations. No tool execution or real network model call.
- Owned lifecycle fixture preserves finally-release on startup failure and
  enforces2280s work deadline/1830s collector subprocess bound, leaving120s
  cleanup reserve inside the2400s owned cap.
- Fresh native CPU qualification processed all384 actual typed requests and
  compiled72 unique exact schemas. All1,122 distinct source/output tag strings
  tokenize to five tokens; standalone q/p prefixes are one token. Full prompt
  sizes match across namespace/prefix within the same task/arm, but arms have
  different instruction lengths. Largest prompt2838 + output3072 =5910 <8192.
- Environment: existing Prime Python, vLLM0.28.0, Transformers5.6.2,
  HTTPX0.28.1, XGrammar0.2.1, PyArrow24.0.0. Native import printed upstream
  Torch enum-deprecation and no-CUDA-runtime warnings; qualification exited0.

## Reuse and scoring boundary

study.py privately imports the authenticated identity96/padding/anchor graph.
Only new data/request construction and factorial summaries are owned here.
driver.py extracts the pinned identity96 run function and count-checks384-call
and cap substitutions. owned.py count-checks namespace/count/cap substitutions
in the pinned owned wrapper. SOURCE_ADAPTERS.json records exact input hashes,
edits and adapted-code digests. No frozen source file was rewritten.

The existing collector retains actual alias, physical prompt IDs, wire body,
timing, tokens, unavailable cache fields, errors and per-coordinate scoring.
The unchanged suite lifecycle checks process ownership including PRL::Inference
titles and releases only its owned service/workers in finally. Parent alone
serializes GPU use and supplies the actual MIG/LD/API-key environment.

Primary scoring always uses displayed-position gold. Overlapping ordinal outputs
also receive a separately named numeric-source diagnostic, never a replacement
score. Disjoint mapping coverage is zero and diagnostic correctness is null.
Invalid completed output has strict failure but no semantic alignment; unrun and
infrastructure outcomes remain null. Each call retains aligned record gold and
predictions, class-count errors/L1 and lexical object key order; cell L1 is the
mean of call-level L1 values, not a pooled histogram difference. Paired item
gains/losses, strict/semantic differences and overlap interactions remain separate
by task, context, permutation, seed and prefix.

## Launch and immutable identities

Parent-reviewed exact argv is in READY.json; default launch is existing Prime
Python plus this sidecar's owned.py --directory owned/attempt-001 (absolute paths
in READY). Collection1800s, owned2400s inclusive, outer2430s, cleanup120s;
four workers,120s/request, T0.5/top_p1/top_k−1/min_p0,3072 output, zero retries.
Expected warm collection20–25min; no filler or automatic retry after partial stop.

SPEC SHA256 `150e2510a804042dc499fc1b9d8a195473d9f697c784a2bb929a803e96417792`;
spec identity `d69d5a83f427fe835ebf96538d2eb882dd6b41dc8dbf8540d709a0a9df61df3a`.
DATA SHA256 `323d893df45cda8d0275f21cd9503b2f9a340135c90be35d62d55341edc23c52`.
Weights are byte-identical to the prior authenticated old-child binding:
`b38b6a7eaf2f19918bcfd78d9c45999d61a38763301213f2290976237273e084`.
Source/input/grammar/prompt/dispatch hashes are in SPEC and final READY; the latter
also binds this additive report and seal script. No outcomes were used to pick
numbers, seeds, schemas, checkpoint or dispatch.
