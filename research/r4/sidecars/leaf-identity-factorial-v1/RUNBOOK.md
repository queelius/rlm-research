# Parent-only launch after acceptance

Scientific design and readout: ../../ideas/2026-09-09-identity-factorial-design.md.
The approved grid is384 calls on old child c32de: q/p source prefix × overlapping
or random disjoint numbers × meaningful/ordinal/constant tags × eight exposed
contexts × two unchanged permutations × two fresh seeds. This is not training.

CPU preparation and verification (no service/model calls):

```sh
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-identity-factorial-v1/driver.py prepare
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-identity-factorial-v1/owned.py --verify
```

After independent parent review/acceptance only, inherit the actually assigned
exclusive MIG UUID, existing driver LD environment and API-key environment. Do
not substitute CUDA=0, empty CUDA, another user's GPU or an obsolete endpoint.
The exact owned launch argv will be published in READY.json. Default paths are
owned/attempt-001 and outputs/attempt-001, both required absent at launch.

The wrapper starts one old-child alias using the unchanged authenticated suite
lifecycle, consumes its actual service/endpoint-original.json and checks live
version/model bindings. It releases only authenticated owned processes in finally,
including known PRL::Inference title handling. No retries or extra cells.

Collection cap1800s; inclusive owned2400s measured from main entry, with120s cleanup
reserve; parent outer2430s. Four HTTP workers,120s/request,T0.5/full support,3072
output and8192 model context. Parent coordinates GPU availability and serialization.

Every completed call, wire body, expected/actual prompt-token identity, score,
usage (including unavailable cache details), timing/error and per-coordinate
checkpoint is retained. STATUS/analysis preserve partial results; FINISH records
owned release. Nonzero or partial status is evidence, not authority to retry.

Primary is displayed-position correctness. Numeric-source scoring is a separate
prospective diagnostic only for overlapping ordinal outputs. Disjoint diagnostic
accuracy stays null with zero mapping coverage. Invalid format is strict failure
with unavailable semantic alignment, not64 established wrong classifications;
infrastructure/unrun remain separate/null. Counts are compared per call before
aggregation, and task/context/prefix/namespace contrasts are retained separately.

All physical requests use exact-cardinality ordered-tag grammars; no free-output
arms, repair, gold-assisted lookup, or tool execution. Tag values/instructions are
the intervention; realized token costs need not match. Contexts have already been
exposed, and this leaf-only comparison is not an end-to-end RLM result.
