# Bounded CPU preparation

Approved scientific design: ../../ideas/2026-09-09-identity-factorial-design.md.
Decision: ../../operations/2026-09-09-continuous-allocation/IDENTITY_FACTORIAL_DECISION.md.

1. Capture failing focused regressions for namespace/permutation invariants,
   384 balanced conditions, primary/diagnostic scoring and lifecycle cleanup.
2. Implement only new factorial construction/scoring; authenticate and reuse the
   existing request collector, physical wire audit and owned service lifecycle.
3. Qualify every actual typed request, exact schema and prompt budget on CPU;
   freeze source/data/dispatch/seeds/model/grammar/tokenizer identities.
4. Run focused tests and CPU verify, report limits, then publish READY last.

No GPU, network model calls, service launch, environment change, historical edits,
acceptance or queue mutation. Parent owns review and subsequent launch. Invalid
format, infrastructure errors and unavailable semantic alignment remain separate.
