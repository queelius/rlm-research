# BA18 fixed endpoint readout

Execute the already approved comparison: actual released base and the sole qualified BA18 step1 adapter each receive train18 and outcome-blind held18, 72 physical calls total. No optimization, retries, example selection, or score-dependent phases.

1. Implement thin study/collector/metrics/owner using the frozen native requests and the accepted runtime-an22-5801-v1 service_wrapper_v2.py lifecycle. Verify a real four-call HTTP collector fixture, both aliases and exact prefixes, missingness and semantic-vs-strict ID grading.
2. Authenticate the actual checkpoint/Adam/replay receipt, freeze READY and hand to MAIN for review and exclusive GPU launch. Caps: 600 science / 700 owner / 800 external seconds; four case workers, per-call raw checkpointing and authenticated cleanup.

All additions stay in this independent sidecar. Training and inherited sources remain immutable. Gold is host-only. A base request uses the actual base path, never an invented adapter. Ordering-sensitive primary and unordered-known-unique semantic results remain separate.
