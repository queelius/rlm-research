# Fixed BA18 endpoint: selection learning, not procedural copying

Compare actual released Qwen3-4B-Instruct-2507 with the sole BA18 step1 adapter. Each receives the frozen train18 and held18 requests, 72 physical calls total, with matched prompts/seeds/T=.5/384 output tokens. The 18 material cases each run two paired repeats; four case workers rotate model order. All conditions run regardless of score; nine held stage cases, not 18 independent items. No optimizer or eligibility repair occurs here.

The source training used released base plus newly initialized zero-B LoRA, BA reward over present classes, G2 RLOO /18, eight credited and ten zero actions, selection-array tokens only, LR1e-4 and biased token-TIS cap2. The fixed checkpoint is qualified using actual initial/updated tensors, saved gradients, Adam moments/counters, all18 source actions and eight replay gates. This is not exact sequence importance sampling or a general decomposition-learning claim.

Use `READY.json` argv only after MAIN admission. It invokes native Python `owner.py run --outer-seconds 700`, under MAIN's shared GPU flock and external `timeout 800`. Science cap600 seconds, owner700, external800. No retries or outcome-dependent phases. Output is `outputs/attempt-001`, new and immutable; raw request/response bytes, decoded token arrays/logps, prompts, per-call starts/returns, usage and errors are checkpointed per call. Service cleanup is the accepted authenticated current-allocation lifecycle. Full owner exceptions retain details/tracebacks without copying private request headers.

The service is exactly `runtime-an22-5801-v1/service_wrapper_v2.py`, loading only cp1; the base control requests the real released-base model card by its actual path. The role map is service naming, not recursive execution. One qualified current service serves both arms, with live model/version/adapter/base checks and full runtime provenance. It is not the older eager/flag-on no-LoRA width service: do not pool old flat results or claim matched old-runtime costs/batch invariance. FP32 saved LoRA is cast to BF16 for native serving.

Primary: fixed-denominator strict ordered eligible-ID-set exact, with malformed/invalid/unavailable separate. Also report unordered known-unique semantic exact, present-class BA, confusion/F1/Jaccard and all-ID/empty baselines. Ordering-only invalidity must not be called hallucination. BA means explicitly retain their semantic-valid denominator; no missing-as-wrong or conditional-recall promotion. Train results diagnose in-sample fit; held nine cases test this small generated family only. Record physical costs across all72, per-arm/split observed token subtotals and unknown-usage counts; sum of concurrent request seconds is not elapsed time.

The focused CPU fixture runs four actual HTTP collector calls with both aliases, raw decode and exact frozen prefixes; the full72 schedule, missingness, unsorted-known IDs, and8192 bound are checked. The fixture does not claim GPU runtime qualification. READY binds actual checkpoint qualification, source closure and CPU evidence. MAIN is the only launch authority.
