# Shifted source cues72

MAIN approved option2 of `ideas/2026-09-09-correspondence-to-rlm-next-options.md` (SHA4d4e6f3e…). Fixed c32de validation-selected research child, exactly the12 existing sparse-source contexts (four each TREC/SST/AG,64 records each), two fresh seeds981327011/021, matching/constant/shifted tag rules:72 direct component calls. No training or new source selection; all contexts exposed.

All arms have identical text, record order, tools, positional-label instructions and native rendered prompt IDs within a context/seed. Every output item is an ordered tag,label object. The schema fixes matching tag to current displayed ID, constant to p0000, shifted tag to actual source position `(i+17)%64` using zero-based i. Matching/shifted preserve the exact source-ID multiset, but no equal generated-token or compute claim follows. The shifted cue deliberately conflicts with the positional instruction; it is not a neutral control or a measurement of internal attention/free ID retrieval.

Primary remains strict displayed-position correct/64 and matching-minus-constant and matching-minus-shifted, separately per task. Average two paired seeds within each of four context clusters, retain unknown pairs/bounds. Separately freeze named-record alignment on complete valid matching/shifted arrays and named-minus-displayed difference; shifted named gold is exactly the ID named by that output slot. It NEVER replaces or repairs primary scores. Constant has no named source and its named metric is NULL. Report full64 validity/correctness, histogram L1, per-position profiles, same label repetition caveat and actual role-free component costs. Invalid completed output0; unavailable or unrunNULL, with semantic alignment unavailable on invalid arrays.

Common sampling: temperature.5, full support/top_p1/top_k−1/min_p0, max3072, full8192 context, existing native chat/tools rendering and fixed original4B base plus c32de adapter. Four concurrent individual requests, balanced six-permutation triple order over24 context-seed units; no guarantee matched child trajectories. No retries, decoder repair, answer extraction or output-selected replacement.

Caps:600 collection,750 work,870 owned including120 final cleanup reserve,900 parent outer. Frozen qualified single-LoRA suite/serve lifecycle remains unchanged; no new server launcher and no inherited-`__file__` substitution. Parent alone assigns actual MIG/LD/key environment and launches/cleans. Preserve per-call wire/body/token/usage/errors/status and all72 planned slots. Expected one warm A100≈10–12min maximum work, not a measured throughput promise.

Interpretation: shifted named alignment above displayed alignment supports following supplied source cues under this constrained contradictory package. Preserved displayed-position benefit despite wrong cues favors a more generic varying-token/position account. Mixed/absent effects narrow the account. This small exposed-context component test does not establish end-to-end RLM gain, architecture novelty, an attention mechanism or unconstrained ID retrieval. Literature boundaries are in the approved options note; no new novelty claim is introduced.
