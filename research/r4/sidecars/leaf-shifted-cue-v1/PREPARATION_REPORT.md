# CPU preparation scope

Approved option2 implemented only in this new additive namespace. Historical science, lifecycle and service sources are unchanged. Exact source closure, adapter closure, seeds, dispatch, request bodies, physical prompt IDs and schema proofs are frozen in SPEC/READY and their member artifacts.

Focused test-first checks cover the full72-coordinate grid and exact triple request equality; shifted ID multiset/rotation; strict malformed/duplicate/key-order/cardinality handling; named alignment never replacing positional primary; actual original launcher path and caps; ordered wire-body validation; and all72 requests through the real reused collector with a fake HTTP transport. Initial tests failed before implementation; the combined test run subsequently exposed the legacy suite prepending unrelated helper paths ahead of the owned CLI. The new wrapper restores its own import precedence after loading the qualified suite. Seven focused tests passed after that scoped fix. No model or GPU calls were made.

The CPU preparation command additionally compiles each unique ordered schema using installed xgrammar, rejects wrong tags/labels/order/cardinality, and renders all72 requests through the actual vLLM typed-tool serializer. It checks24 equal-token triples and input+3072≤8192. Synthetic output-token lengths are recorded only as fixture lengths, not predicted or equal realized inference cost. CPU_QUALIFICATION.json and CPU_TESTS.json are authoritative preparation outputs; READY is absent if any check fails.

Runtime reuse is narrow: authenticated collector run and owned-wrapper source substitutions have exact occurrence counts and adapted hashes. Private scorer/request callbacks change only this process's imported modules. There is no scheduler or new server. Source adapter details are in SOURCE_ADAPTERS.json. Seed collision checks cover named top-level sidecar SPEC/READY/RECIPE/CAMPAIGN/SEED files and inputs PLAN files, not all possible files or the entire research history.

Scientific limitations: these12 source contexts are already exposed; the shifted cue deliberately contradicts positional instructions; repeated class labels can agree under both scoring alignments. Matching and shifted share tag multiset but not guaranteed generated token count. Named-record accuracy is a secondary diagnostic only and NULL for constant or invalid arrays. This is a component mechanism screen, not free ID retrieval, internal attention evidence, model-generation replication or end-to-end RLM benefit.
