# Normalized eligibility: selected-ID list versus complete decision vector

Question: on identical mechanically normalized public records, does a positional decision vector reduce eligibility filtering/enumeration errors compared with listing selected IDs? This is a new output interface, not the same format, learned delegation, or a new recursion method. Both request/output lengths can differ. No outcome or annotation filtering.

Twelve NEW stage contexts use the exact prior fresh12 dimension schedule: history1/width6 stage0,1; history1/width12 stage2,0; history1/width20 stage1,2; history3/width6 and width12 each stage0,1,2. Check revisions remain1; format3/features1/chain/level helper-width-local-v1 unchanged. Generation seeds202609380000..11; two sampling seeds per context202609390000..23. Roots and all candidate IDs are disjoint from original source, width, held9 and fresh12 panels, and from each other. History subsets have different instances, not a causal history manipulation.

Both arms see exactly the same public effective records, order and policy using the pinned, unchanged public normalizer; only the final output-contract suffix changes. List asks for the complete sorted eligible-ID list. Primary list correctness is unordered; sorting remains a separate strict diagnostic. Vector asks for exactly `{"eligible":[true,false,...]}` with one literal JSON boolean per `stage.effective_candidates` entry in that exact public array order. Host mapping only zips the validated full vector with that order. No eligibility computation/filter, answer lookup, omitted-ID replacement, or label-informed formatting.

Reject wrong length, numeric0/1, strings/null/nonbool, duplicate/extra keys and non-JSON/fences. Invalid vectors stay `ids=None`, exact false, BA/precision/recall None—never empty-set fallback or partial repair. Missing/unknown remains separate. Primary exact retains all24 planned attempts per arm. Report strict validity, invalid-known, missing, per-arm valid-set BA/precision/recall with denominators, and matched-valid paired BA separately. Do not compare conditional valid-only metrics without their denominators.

48 new calls, one natural call per answer in both arms; paired seeds, alternating arm order; released Qwen3-4B-Instruct-2507 without LoRA, T.5,384 maximum completion tokens, total≤8192, four context workers. Science600/owner700/external800s. Freeze all actual prefixes before labels/model outputs. Label-blind full candidate output token checks do not use or choose labels; actual native costs/finish reasons are reported. No token-cost matching or added generation budget.

The owner and collector are unchanged proven fresh48/native-V3 service code with explicit new bindings. Sources/roots/prompts/native prefix plans/host-only labels/fixtures are sealed in CPU_READY. Canonical parsed response hashes and raw response byte hashes are distinct. No shared source or live environment is changed.

Focused red→green parser test includes swapped public order, length, numeric0/1, nonbool, duplicate keys and list sorting/uniqueness. One localhost HTTP fixture traverses current native collection/decoder/scoring for two pairs including a malformed vector and checks current owner imports. It calls no model/GPU. Preparation: `CUDA_VISIBLE_DEVICES='' /project/alex_phd/envs/prime-rl-5990b1b/bin/python prepare.py`. MAIN reviews all new Python files and CPU_READY, then alone launches its argv under the external800s cap/GPU flock. Preserve every attempt and all12 context units.
