# FinQA fresh16 replication, unchanged two-example condition

MAIN only launches the exact CPU_READY argv: native Python, this owner.py, `run --outer-seconds 700`. Science600s, owner700s, external800s. No GPU/model calls during preparation.32 physical calls,16 paired questions, four question workers. Qwen3-4B-Instruct-2507 base, no adapter, T=.5,384 output tokens,8192 total context. New prospective paired seeds202609300000 through202609300015. Both arms share each context's seed; different panels have different examples and seed namespaces.

Selection is ascending SHA256(official revision + newline + example ID), first16 distinct page filenames after excluding all16 old filenames. Selection reads no gold/annotation/operator/difficulty criterion. Original official dev revision0f16e2867befa6840783e58be38c9efb9229d742; acquisition/license/evaluator provenance inherited in closure. All16 selected cases remain even if annotations or model programs are problematic. Gold programs/targets remain host-only, never demonstrations or prompt fields.

Identical admitted interface.py is imported, not rewritten: same two synthetic questions/facts and arm-specific demonstrations, same original science.py interpreter and provided-target numeric metric. Only public examples and prospective seeds change. There is no new prompt variant, output-budget increase, syntax repair, row-lookup fallback, SFT/RL or recursive-method claim. Source-number presence is not faithfulness; supplied targets are not semantic truth.

Read order: study.py and prepare.py selection; interface.py unchanged-source binding; collect.py/metrics.py/owner.py thin runtime bindings; test_fresh.py; CPU_TESTS/CPU_READY. Preparation reconstructs the selection independently in a test, verifies exact old prompt-builder equivalence on all32 new tasks, and reuses the admitted actual native first-pair HTTP fixture under new owner/collector/metric bindings. It retains30 unknown slots in the partial fixture. The live collector never loads host gold and the released scorer uses the unchanged bounded JSON DSL, not Python eval/exec. Real dispatch qualification is mandatory; inherited trusted owner-source compilation is not execution of model programs.

Question: does usable DSL grammar and source-grounded arithmetic recur on pages not previously selected by this campaign? Report within-panel direct/DSL exact/valid/unknown counts and all16 paired outcomes, with costs and mechanism. Do not call cross-panel accuracy differences regression or a paired treatment effect; page and seed blocks change together. Natural one call/answer per arm, token totals not matched. No further prompt variants or exploration are authorized by this READY.
