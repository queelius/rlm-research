# CPU preparation report

The package freezes 192 exact requests over 16 new, paired context units. A distinct inventory
snapshot was written before selection and excludes 1,280 named prior premise groups. The eligible
counts after exclusion are government 194, slate 217, telephone 216, and travel 256; the first 64
hash-ranked groups per genre were selected without substitution. Gold labels are used by the
inherited complete-three-label eligibility rule and nowhere in ranking or prompts.

All 192 schemas compile and accept their canonical output. The maximum native prompt length is
4,728 tokens and the maximum prompt plus output allowance is 7,800, below 8,192. Opaque keys occupy
the `k`+12-hex namespace and were checked against public, shifted, and alien `m`+12-hex IDs. Exact
key validity remains only a forced-grammar contract diagnostic.

Focused CPU qualification covers exact inventory/pairing, hash permutation and namespace rules,
gold-hidden request construction, whole-contract rejection without partial salvage, separate opaque
and numeric gates, complete NULL inventory accounting, an authenticated nonempty native-response
transport, and the actual registered wrapper-to-`Popen` service seam. No GPU or service call was
made during preparation.
