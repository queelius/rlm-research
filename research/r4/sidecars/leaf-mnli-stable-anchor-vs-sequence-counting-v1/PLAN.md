# CPU preparation and launch boundary

1. Freeze hashes and normalized exclusions for every named MNLI DATA, PUBLIC, GROUPS, PLAN, and
   selection inventory before selecting contexts.
2. Select the first 64 eligible premise groups per genre by the prescribed hash and freeze all 192
   exact native requests.
3. Compile every grammar, validate canonical keyed outputs, audit namespace collisions and prompt
   capacity, and run focused protocol/scoring/owner/transport tests with CUDA hidden.
4. Seal a READY identity over sources and inputs. MAIN alone may review and launch the exact owner
   command. This package does not reserve or launch a GPU.
