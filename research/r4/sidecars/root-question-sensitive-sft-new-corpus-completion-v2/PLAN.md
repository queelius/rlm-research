# Plan

1. Verify both failed attempts and the immutable complete72 corpus.
2. Verify the frozen six-row gate is a subset of that corpus at the actual training entry.
3. Run unchanged model load, gate, objective, six updates, and checkpoint ancestry.
4. Only after exact checkpoint6, run the unchanged metadata72 readout once.
5. Preserve all failures/NULLs and release the owned GPU service.
