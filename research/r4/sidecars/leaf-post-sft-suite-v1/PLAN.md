# Bounded CPU plan

1. Read frozen serving and qualified lifecycle V2 dependencies; verify import/amendment on CPU.
2. Add three focused tests: ordered suite commands/endpoints and fixed-final binding semantics; finally cleanup after stage failure; known Prime title/changed PID rejection through reused lifecycle.
3. Implement one coordinator, source/command identity seal and scoped run CLI; no GPU invocation during preparation.
4. Wait only for correspondence controls' final READY, authenticate its actual source/spec closure. Verify focused tests and CPU original binding. Freeze source manifest, write READY last with parent launch command and caps.
