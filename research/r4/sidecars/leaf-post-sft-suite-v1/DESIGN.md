# Post-SFT leaf suite: two owned service stages only

Parent-authorized CPU preparation; GPU launch is exclusively a later parent command after the root RLVR campaign V2 releases its device. No new research cells, retries, resume scheduler, training or edits to sealed sources.

Stage1: start exact original857a7ce6 + oldSFTc32de129 dual LoRA via frozen leaf-role-routing-v1/source/serve.py. Bind and run correspondence representation24 then rotation32 on the oldSFT endpoint; bind/run SST-2 original120 then oldSFT120. Release all captured owned inference process identities and verify ports18601/18611/18621 free. Stage2: authenticate completed A/B fixed-final epoch2 RESULT/SELECTION/checkpoint/optimizer/RNG/source closures, write explicit fixed-final decision (not validation-selected), start A+B dual LoRA, bind/run SST-2 A120 then B120, release exact owned workers.

Reuse campaign_lifecycle_v2 claim_service/snapshot_descendants/stop_service without calling its install/run/campaign state machine. Dependency inspection: imports campaign, campaign_common, campaign_native and their frozen native capture/export modules; native interpreter is already qualified and CPU import + amendment verification is read-only. Its immutable campaign/pilot closure becomes an explicit suite dependency. Preserve PRL::Inference title handling and PID/start_ticks/UID/process-group checks. No broad process scan or cleanup: observe only descendants of authenticated owned launchers/service processes, and signal captured owned identities. CPU collector commands use a narrow Popen polling wrapper so it can snapshot service descendants during collection; group signals recheck the same V2 stable process identity and allow10s INT/5s TERM/5s KILL. Each service directory matches V2 SERVICE_REQUEST/BINDING ownership envelope.

Whole suite7200seconds:7080seconds work envelope,120seconds reserved for final command/service cleanup. Each service readiness cap600seconds; correspondence runs cap330seconds (their internal300), SST runs1230seconds (internal1200), binding120seconds. All subprocesses also bounded by remaining work envelope. Stop on first failure; preserve stage commands/logs/bound specs/partial output and structured error. finally releases the owned launcher then V2 service/workers. Identity mismatch refuses arbitrary PID signaling and reports incomplete release; no claim of guaranteed cleanup after SIGKILL, machine loss, or unprovable ownership.

Fresh runtime preflight checks vLLM0.28.0, both aliases' exact adapter root and base parent, actual inference config max_model_len8192 and lora_dtype auto. Frozen suite sources, input READY/SPEC/manifests and commands are recorded before execution. A/B completion is required before suite starts, never inferred from an intermediate checkpoint; choices are fixed independently of transfer outcomes. Existing research scorers and seeds/prompts remain unchanged. No endpoint/tool prompt rewrites or new conditions.

Parent amendment: permit already independently specified leaf-mixed-size-schema-control-v1,16calls/weight on the same4 TREC validation64 contexts×2seeds×free/schema, after each corresponding SST job while that alias is warm. Its exact path/CLI/weights/call budget are frozen in this suite. Inspect its READY once at suite startup: if present authenticate its declared driver SHA, snapshot READY and hash it before any calls; its own bind/run authenticates the frozen research input/spec closure. Otherwise record explicit skipped-not-ready, do not wait or invent cells. This adds at most64calls and remains within the same7200-second global envelope. It is a separate TREC follow-up, not an SST-2 condition. No later mid-suite discovery/retry if it was absent at startup.
