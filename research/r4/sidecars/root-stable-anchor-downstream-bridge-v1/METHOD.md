# Prospective audit method

After exact OWNER_TERMINAL and parent EXIT/release relay, authenticate all 16 leaf and 64 root
records from native artifacts. Preserve observed-invalid as strict zero and unavailable as NULL.
Rebuild each canonical map from raw leaf content, never producer scores; score all-record and late-16
accuracy. For every root endpoint separately record API invocation, map delivery, retained state,
relevant-ID consumption, actual executed scalar, agreement with a host reducer over the predicted
map, strict `Answer: N`, gold correctness, and correct-and-faithfully-performed. Do not execute model
programs during audit. Report count and weight separately, paired eight-context contrasts, plan by
encoding interaction, conservative NULL bounds, physical root/leaf union, shared-map provenance,
and known/unknown token fields. Apply only the gates frozen in the design and eligibility amendment.
