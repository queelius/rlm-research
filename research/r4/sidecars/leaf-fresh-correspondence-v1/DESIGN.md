# Fresh exact-group correspondence96

MAIN approved `ideas/2026-09-09-fresh-correspondence-main-design.md` and the complete frozen data-feasibility report. Four64-record AG News test contexts plus four64-record SST training text-unit contexts;512 normalized unique source groups. Two exact released Qwen checkpoints, two seeds981318011/021, plain/matching/constant;96 calls. No research adapters. Matching-minus-constant is primary per task/model, four equal context clusters; plain is secondary. Invalid observed0, unavailable NULL. No output-driven replacement, repair, truncation or model smoke.

Before selection: normalize NFKC+casefold+whitespace collapse, SHA256 UTF8. Exclude whole conflicting-label groups and all groups appearing in the frozen feasibility input-membership catalogs. Rank eligible groups by SHA256(`981318001:task:selection:group_hash`), choose first256/task and divide into four consecutive64 blocks. Representative is the earliest original source-row index, not preferred label/text length. Assign each group's source ID from a context-specific SHA256 order of integers1000–9999, yielding qNNNN IDs distinct from ordinal1–64; selection, order and IDs never use labels except whole conflicting-group exclusion. Keep complete original source text and all duplicate source-row provenance.

Fresh means disjoint from the named bounded historical input/reservation crosswalk, not every possible embedded prompt or external repository, not unresolved SST parent/subphrase or news-event near duplication, and not model pretraining/post-training. Underlying dataset licenses remain unknown/unconfirmed. SST train units include phrases; no full-sentence claim. Public upstream rows are not themselves research exposure.

Use identical pinned Qwen144 native package/settings and per-call collector. Models serial qwen3 then qwen35; instruction/schema differences between forms remain part of treatment. Same literal source order/context within each model; native template differences between models remain. No architecture speed inference from serial wall. Four concurrent individual calls retain qualified scheduling; not promised lockstep matched generation.
