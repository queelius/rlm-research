# Bounded CPU implementation

Focused tests first: label-independent selection/conflict exclusion, exact96 grid and paired sources/schema, actual48-row driver STATUS and bound global imports, actual two-stage owned lifetime/clipped deadlines. Reuse pinned Qwen144 native grammar/renderer qualification for96 with counted cardinality adaptation. Freeze complete source rows/provenance/hash-order/IDs, positive exclusions, all requests/tokenIDs/schemas and old shard identities. Verify hashes once; no large shard rehash or model calls. Publish READY last and preserve local failures. MAIN alone accepts/launches.

Exact caps1800 outer/1770 owned/1650 shared work,2×600 collections,120 final cleanup reserve. All96 planned calls remain the denominator when startup/collection fails. No retries, rerolls, source edits beneath accepted jobs or new environment/image.
