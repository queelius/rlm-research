# Fixed-model paired decoding replication

Goal: test whether the short-panel23→25 exact signal survives two fresh decoding seeds per context, not a new training seed or dataset. All16 original short-held contexts ×2 new seeds ×2 fixed models =64 planned episodes.

Architecture: preserve exact original model task/context/tool prefixes; regenerate only task/coordinate identifiers and seeds202609270000+2*row_index+repeat. A shallow collector uses the reviewed raw-native core and terminal hooks. The prior qualified service owner is projected with two explicit arm substitutions; source files remain unchanged. One checkpoint module authenticates both existing cp32 and fixed RLcp1, never selects weights by new outcomes.

Global constraints: same seeds across models, T.5,2048tokens/action,six total actions,zero children,four workers,8192 context; all32 per arm retained, no old-control reuse under new seeds. Both arms run regardless of results. Science900/owner1100/external1200seconds per arm. No GPU launch/optimizer/installation or T1 changes. Context n16, not64 independent items.

1. Freeze exact identifiers/seeds/input map, source/checkpoint closure, and focused real payload/model-binding/collector-entry tests. Do not execute generated model code in CPU preparation.
2. MAIN review and launch each fixed arm under shared lock. Explicit missingness and full physical costs remain in raw receipts. No checkpoint or context selection from these outputs.
