# Semantic prediction-map externalization48

MAIN approved CPU implementation2026-09-10; no GPU/service/lock/queue authority. Question: does withholding inline predicted labels, while providing the same real map as a file, induce faithful state retrieval/reduction under the current native coding role? This transfers the externalization question from direct purchase facts to semantically fallible classifier evidence; no broad memory/decomposition novelty or historical CE causal comparison.

Fixed actual served857a7ce6 released-reference adapter, exact config/conversion/base identities; not bare-base equivalence or a best-ancestor choice. Frozen c32 child. Eight QSR training00–07 contexts,16 public records each, three existing supported operator/scope queries per context. These are research/child-training-exposed and current operator-SFT training contexts, not fresh test data. Root policy is fixed857, not the live SFT checkpoint. One new paired seed per context/query,24 pairs/48 roots; balanced INLINE/FILE dispatch order. No policy or source selection from current SFT outcomes.

Raw records are file-only in both arms: byte-identical records.json/context.txt with id,user,text,weight; labels absent from these files. query.txt is the common base query, not the arm-specific prompt. One real full16-ID c32 acquisition per context,8 physical sources total. Typed source grammar constrains exact public IDs and canonical categories only, no gold/semantic values; same qualified final-only source prompt and1536 cap. This deliberately differs from CE's historical free-JSON source protocol. Failed transport/native authentication or malformed/incomplete map gates all6 affected roots to source-NULL, with no retry, replacement, repair or semantic filter. Canonical wrong labels remain unchanged.

Every eligible root receives identical labels.json bytes from the actual returned raw map. Both arms have the same truthful role, task, file/field descriptions and optional native Python/child tools. INLINE appends those same raw map bytes to data.prompt; FILE does not. No authored solution, historical program/transcript, algorithm/example, scalar hint, forced retrieval or forced recursion. The map is additional child-prediction evidence, not dataset truth. Optional child reacquisition remains available and its actual costs are separate from the8 shared source calls.

Primary is strict authenticated dataset final correctness. Co-primary mechanism is actual supplied-map-consistent EXECUTED scoped reduction, reported separately. Final scalar agreement alone is not execution evidence. Retain native code plus following actual observations for manual dataflow audit without sampled-code reexecution; distinguish file reads, literal reconstruction, scope/ID fidelity, acquisition/reclassification, arithmetic and actual final. Dataset-correct and prediction-consistent scores can disagree when labels are wrong. Malformed authenticated finals strict0; genuinely missing finals/transport/source failures NULL with bounds and planned operational sensitivity.48 original endpoints remain in inventory. Eight context clusters,24 paired query blocks; not48 independent contexts. No history/dataset answer repair or final extraction/reordering/fallback.

Budget2400 outer =2220 work +150 owned cleanup +30 margin; owned2370. Startup≤180, source acquisition≤300 with root1500 plus closure120 reserved. Root stage starts immediately after source completion, capped at1500 and work minus120; four workers/120s episodes,2048/action,8192 context, unchanged QSR sampling. Caps are not waits. Physical8 source attempts and all native root/optional-child attempts/unknown usage are retained. Hypothetical standalone endpoint accounting charges its one source separately, not48 extra physical acquisitions. Provider billing unknown; token counts are not FLOPs.

Minimal reuse: qualified CE native wire/strict-final accounting, QSR task/setup/typed transport, and an27 dual-adapter lifecycle through unique exact-path aliases. New protocol/plans, labels-file task wrapper, typed source request, count/budget owner changes. Focused CPU tests:48/8 pairing and label mutation; only-inline prompt difference and all common file hashes; actual typed schema/prefix; actual native two-arm file-read/optional-child/final fixture; source failure→6NULLs; strict0 versus NULL; exact owner→collector/service/config/descriptor/intercepted Popen. No training dependencies or environment changes.
