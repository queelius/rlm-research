# Approved bounded implementation

1. Test and freeze eight exposed sources,48 paired root slots, common truthful queries, fixed857/c32 and fresh seeds without host-label selection.
2. Reuse qualified native capture with a new task wrapper: labels.json actual raw bytes, base plain_query unchanged, inline appended only to data.prompt. Test actual setup/prefix and typed optional transport.
3. Adapt source collection to exact public-ID/canonical-label grammar,8/48 counts, strict source gating and reserved300/1500 stages. Keep dataset primary and executed-map mechanism separate.
4. Compose a new owned lifecycle entry with2220/2370/2400 caps; validate exact argv and actual service configuration under intercepted launch.
5. Retain focused CPU receipts and source/input provenance; seal READY for MAIN acceptance. No GPU, service, lock, queue or current SFT modifications.
