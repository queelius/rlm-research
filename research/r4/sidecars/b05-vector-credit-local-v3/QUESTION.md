# Candidate-local V3 directory-handshake repair
Same fixed 64-action local-credit update. V3 only permits the owner-created attempt directory, then preserves the exact allowed-file check. V1 and V2 failures remain immutable.
