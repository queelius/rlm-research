# Preparation boundaries

Main approved a narrow teacher-forced local diagnostic. Initial CPU comparison found fresh96 matching/constant prompt inequality (first AG4684/4683tokens, first difference611). MAIN independently confirmed and authorized a new common neutral format paragraph before any replay model call or READY. The original draft is preserved and both authorities are pinned.

Test-first: five core tests failed before replay.py existed, then passed with a tiny actual CPU GPT2 model, independent preceding-logit calculation, tokenizer merge fixture, prior-only gold prefix fixture, nonfinite rejection and finite-set normalization. A sixth test first failed because run.py was absent, then exercises a real tiny forward interrupted by a fake clock and requires partial candidate persistence with missing conditions/probabilitiesNULL. No real Qwen model was loaded during preparation. QUALIFICATION.json stores the final fresh test stdout and all actual tokenizer/length results.

Native chat rendering is the exact authenticated earlier typed_ids function, including explicit template kwargs. Forward computation is native HuggingFace, not vLLM probabilities or constrained-decoder scores. Small source hashes and versions are pinned; prior full shard byte hashes are reused with immutable size/mtime/inode checks. This is not a fresh multi-GB tensor rescan. The master collision check is scoped to named sidecar metadata/input-plan files, not a global research-history guarantee.

No original source, active job, environment, model state or GPU was changed. No new scheduler or service is created. READY is the final publication operation and requires MAIN acceptance before launch.
