# Conditional local cue replay

This implements MAIN's outcome-informed design plus its explicit prompt correction, both pinned. Eight exact fresh96 contexts, four AG/four SST,64records each; positions16/32/48/64; matching/constant/shifted17 current tags.32 units/96conditions/288 complete canonical-label forwards. Released Qwen3-4B-Instruct-2507 cdbee75… only, no research adapter. Master981328001 identifies a deterministic inference plan, not sampling replications.

The original draft's claim that matching/constant fresh96 prompts were equal is false. PREFIX_CORRECTION_EVIDENCE records all eight old differences. This study instead replaces only that format paragraph with the already qualified neutral shifted72 instruction, preserving source text/order/definitions/tools and actual typed Qwen3 rendering. Its new common prompt IDs are shared across replay conditions; they are NOT original fresh96 prompt IDs. No old source is edited.

Prior output objects are supplied constant p0000 tags with displayed GOLD labels. Only the current tag changes; no current/future gold enters the conditioning prefix. This is explicitly teacher-forced diagnostic input, neither sampled behavior nor training. Shifted names source position(i+17)%64 and deliberately conflicts with positional label assignment.

Every candidate contains a full canonical label plus closing quote/object and the next comma (positions16/32/48), or final array close (64). Full output text is tokenized once per candidate. The scored prefix is the shared prefix of the standalone incomplete history and all completed candidates; boundary merges cause recorded backoff, never silent label truncation. Physical chat IDs are fixed and output tokens are appended. Candidate log probabilities sum the correct preceding-position logits in FP32 without length normalization or grammar masking. Only after obtaining all finite raw scores is softmax applied over the finite canonical set. These are conditional finite-set probabilities, not calibrated task probabilities.

Primary is displayed-gold normalized probability and matching-minus-constant over all32 units, reported by task/context. Argmax and shifted-named-gold probability, and the prospectively defined displayed/named-disagreement subset, are separate diagnostics. Shared labels may make the named target equal displayed gold. No attention, free-ID-retrieval, whole-RLM or model-training claim.

HF BF16/SDPA forward, model.eval, requires_gradFalse, inference_mode, no KV cache. Native rendering environment and actual HF forward environment are pinned separately. Model weight shards retain authenticated prior byte hashes plus launch-time immutable stat checks; no real model is loaded during CPU preparation. All candidate input lengths must fit8192, otherwise preparation fails without cropping/reselection.

One parent-assigned A100,750s work from runner entry,120s cleanup allowance within900s parent cap. Sequential candidate forwards avoid inference services or new schedulers. Each complete or interrupted unit is atomically checkpointed with raw token/sequence logprobs; unrun units/conditions remain missing, never0. No automatic resume/retry/replacement. MAIN alone launches and handles outer cleanup. All output/input hashes and actual clocks/memory are retained.
