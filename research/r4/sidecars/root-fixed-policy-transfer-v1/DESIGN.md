# Frozen fixed-policy root-transfer48

Implements MAIN-approved ../../ideas/2026-09-09-fixed-policy-root-transfer-design.md (SHA3d32336edf6326209a00a267790fae086becf8c6fcf04da1dc5aa322d5ef0ed2). Its adaptive selection, source-boundary and causal limitations remain authoritative. No change to sealed joint training or the prior join acquisition failures.

Four fresh16-record groups are selected label-independently from the exact78 left after the frozen QSR448. Original53-manifest/legacy exclusions are authenticated, not relaxed. All64 are from the frozen child's training partition; root-new is relative to that named closure, not whole-system-unseen. Four context clusters, two count queries u03 and u01/u03, two fresh seeds, all three policies =48 free endpoints. Native first prefixes are frozen; optional first-four-record example and accurate field/map text are byte-identical to old joint free prompts when rendered on their old contexts. Root batching is free; no new width, map, teacher history, gold, counting code or forced recursion is supplied.

Fresh namespace981401xxx had no matches in the bounded source/plan scan before source creation. Selection SHA256([981401001,'source',normalized group]) first64; per-context order hashes independent of labels. Record IDs q0001…q0016, users u00…u03 cyclic and context rendering match the old layout. Targets NUM,HUM,NUM,HUM; single/union queries and answer contract unchanged. Sixteen paired seeds981401101…116 and a common hash-shuffled dispatch schedule, with one independent fixed service phase per policy. Phase order is frozen in READY; order/clock confounding is disclosed.

Actual fixed selections are verified through the pinned original joint ancestry code: unchanged low66c, joint step4 ffa49801…, reduction-stop step4 eaa80a9…. Child c32de129… resolves checkpoint0128 (two completed source-train epochs), not checkpoint0032. No training/checkpoint competition/gate/control acquisition/replay occurs. The original service wrapper and qualified an27 lifecycle are reused with actual config/descriptor/launch-entry CPU qualification; MAIN alone launches.

The native collector is the pinned joint collector in a process-private qualified module with only two count-checked logging additions at each root/child physical provider seam: mark attempt before awaiting transport, preserve raw response text. Scientific request JSON, settings, role routing, native task/environment, first-prefix check, full-final-branch authentication and strict scorer are unchanged. Entrypoint rejects capture/controlled/subset modes. New bindings preserve old actual root aliases and fixed child, adding new study provenance. No historical or installed source is modified.

Each phase has its original480s cap,180s per episode,2048 tokens/action,8192 context, four workers. Outer2400/work2100/owned2280 inclusive clock with180 cleanup/120 outer margin. All48 slots exist before service startup. After a phase failure and authenticated release, the next fixed policy may run while work remains; no failed coordinate is retried. MAIN termination stops after release, rather than advancing. Release failure stops the owner. Native-final NULL remains distinct from observed incorrect/malformed0, available-only accuracy and planned-denominator operational0 sensitivity. Orchestration completion is not scientific availability.

The additive COST_LEDGER inventories all attempted physical records even when a whole phase is killed and its own terminal ledger cannot be written. Attempt count, returned completion count, known token usage, unknown usage and unmeasured billing are separate. Old paid_model_call metadata is explicitly not provider billing. Rejected requests are not assumed GPU generations. Prior training is sunk work reported separately; all new readout calls are physical with no reused acquisition. Per-phase errors, raw episodes and all planned NULLs remain immutable; actual state use requires trace audit, never text-presence inference.

CPU preparation uses existing environments only. Focused tests cover disjoint inputs, free pairing, fixed selected ancestry, exact original prompt rendering, strict finals/NULL, cost rejection, actual owner→service config/descriptor/Popen boundary, free collector dispatch, and termination release. Service/model boundaries are intercepted and fixtures never constitute model results. No new authored native Python execution or model service is required to qualify this unchanged native seam. Historical root runtime qualification remains source-pinned.
