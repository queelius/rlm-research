# Fixed72 BA18 dose10 endpoint readout

Exactly the admitted original selection readout: fresh released base and fixed new dose10cp1 each answer original train18 and exposed held18 (72 physical calls). Same36 tasks, prefixes, seeds, T.5,384 output budget, four case workers and balanced arm order. All conditions run regardless score. Held nine cases are reused research evidence, not a new confirmatory panel. No optimizer here.

Only changes: the qualified sole LR1e-3 checkpoint/alias and additive output/READY paths. The actual prospective qualifier verifies full original likelihood/replay/Adam/weights and exact original zero-B initialization, preserving sequence tolerance1e-4 and token tolerance1e-5. It never qualifies on accuracy. Do not serve the earlier LR1e-4 adapter as the updated arm or load any procedural/SFT adapter.

Reuse accepted `runtime-an22-5801-v1/service_wrapper_v2.py` with one new LoRA and the actual released-base model card. Raw request/response/checkpointing, strict versus unordered semantics, present-class BA, missingness, cost subtotals and authenticated cleanup are unchanged. Fresh base controls are collected in this service; older responses are not silently substituted. Any cross-run source/base drift is diagnostic, not grounds to select a better control.

MAIN runs READY argv under shared flock and external800-second timeout. Science600/owner700/external800, no retries or additional calls. Output `outputs/attempt-001`. CPU preparation authenticates the real checkpoint, all72 exact source payloads, inherited collector/owner binding and current lifecycle; actual execute reaches CUDA-hidden guard after complete verification. No GPU authority for the preparer.

Interpret dose as a single bounded local-fit/transfer/overshoot comparison. Do not launch more doses based on this readout. Keep malformed/out-of-shard and unavailable distinct; strict ordering-only differences do not establish selection learning. Compare all18/split with semantic-valid denominators explicitly shown.
