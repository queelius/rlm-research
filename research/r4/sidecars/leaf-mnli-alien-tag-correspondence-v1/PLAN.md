# CPU preparation

1. Materialize the deterministic 48-row rotated paired plan and context-specific alien dictionaries.
2. Scan named MNLI/request/catalog files for collisions at recorded start/end timestamps.
3. Render actual native prompts and compile all 24 exact schemas; test every label at every position.
4. Run focused source/scoring/owner-to-collector tests with no GPU or model service.
5. Seal all local and inherited source/input/model pins for MAIN review. Do not launch.
