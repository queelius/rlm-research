# Implementation report

The bridge delegates native collection, strict six-class scoring, public J1 reduction, renderer,
service wrapper, ownership adapter, and lifecycle to the already accepted supplied-plan ceiling.
The additive binding swaps only c32 for the exact mixed checkpoint. Tests cover science-coordinate
equality except explicit policy/call identity, request equality except model identity, exact checkpoint lineage, the frozen cluster gate,
all 120 old physical control pins, real native fake-transport execution, reducer integration, and
registered launcher entry.
