# Plan

1. Authenticate exact mixed update24 and all milestone checkpoint state files.
2. Start one qualified service with the unchanged root model and mixed child binding.
3. Execute the frozen supplied-plan40 requests once, without retry or repair.
4. Release service and retain all raw native, cost, episode, and terminal artifacts.
5. Compare against pinned old c32 receipts using the precommitted four-cluster and exactness gate.
