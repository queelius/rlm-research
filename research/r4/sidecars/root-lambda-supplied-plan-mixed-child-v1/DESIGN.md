---
id: root-lambda-supplied-plan-mixed-child-v1
status: prospective_cpu_prepared
date: 2026-09-10
planned_episodes: 8
planned_child_calls: 40
---

# Mixed-child supplied-plan J1 bridge

Question: does the fixed mixed-contract update24 improve genuine six-class TREC maps and the
downstream public J1 reducer, rather than only the projected A/B/other metric?

This run reuses the ceiling study's exact eight episodes, forty tasks, four nested context clusters,
size-64/256 denominators, public records, full6 prompt token values, grammar, seeds, and
record/dispatch order. Only call IDs, policy/model identity, and the served adapter change: exact
mixed update24 replaces c32. Host gold remains scorer-only. One qualified service makes forty no-retry
calls under a 1,200-second cap; old c32 physical receipts are pinned and never rerun.

Primary metrics are episode final J1, absolute error, and exactness. Secondary metrics are genuine
six-class accuracy and task-relevance diagnostics for target/target_b labels. Before outcomes, the
gate is frozen as: summed (equivalently mean, because each cluster has two sizes) absolute error
improves in at least three of four clusters; exact finals improve by at least two of eight episodes;
and availability does not decline. Both policy results and the child's optimizer source are
research-exposed. This makes no root-planning or compute-matched repair claim.
