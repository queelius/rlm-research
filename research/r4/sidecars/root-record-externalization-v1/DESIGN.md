# Record externalization16

MAIN approved CPU implementation2026-09-10 after independent record-interface24 audit. Only MAIN may accept/launch GPU/service/lock/queue work. This new external sidecar is isolated; all accepted historical sources remain immutable.

Question: does leaving raw purchase facts in the file, rather than additionally preprinting them, induce actual native file use and avoid copying/parser cost?16 new roots=4 exposed fixed worlds×2 fresh paired seeds×2 arms. I shows full JSON inline and in evidence.dat; E leaves the inline evidence block empty. Same exact JSON evidence.dat bytes/order, query/customer domain, common truthful field/schema/file metadata, released Qwen3-4B cdbee75f17c01a7cc42f958dc650907174af0554/no adapter, native coding role and optional Python/child inventory. No algorithm/example/helper/gold/forced file or child use. Same prompt bytes after removal of only I's raw JSON block. context.txt contains that arm's visible block; query.txt unchanged. This is an adaptive exposed-world representation package test, not new offloading architecture or whole-system generalization.

Four worlds retain48 ordered facts each and gold cardinalities3/5/7/9. Master981492001; paired seeds981492101–108; hash-shuffled eight blocks with four I/E and four E/I orders. All16 NULL slots frozen before launch; no resampling/rerolls. Two seeds/world are repeated observations, four context clusters. Primary strict authenticated whole JSON list of distinct ascending known customer IDs equal full-source oracle. Invalid available final0; missing/unauthenticated native finalNULL. Planned operational success counts bounded nonreturn unsuccessful; available accuracy and NULL bounds separately. No intermediate-result or prose rescue.

Mechanism audit must use actual code plus subsequent observations: evidence.dat open, typed decoding, source fidelity, grouping/both-product intersection, intermediate result and final concordance. Record mentions/AST alone do not establish execution. No sampled-code reexecution in analysis. Retain policy loops, context errors, physical attempted versus returned requests, all token/cache unknowns, elapsed; billing unknown. No acquisitions/hypothetical charges.

Budget1200 outer/1080 inclusive work/1170 owned: startup≤180 within work, collector reserves30 for closure, four workers120s/episode,90 cleanup+30 outer margin. Root2560/action,8192ctx,.6/.95, same optional-child transport, no root grammar/retry. Source composition reuses qualified record-interface owner/collector/study/native/service wrapper and actual native fixture with only isolated namespace/count/representation/input changes; no installed/runtime source edit.

Minimum CPU qualification before READY: test actual only-inline difference and exact files/facts/pairing; actual owner→service wrapper/config/collector entry (model launch intercepted); all16 pre-native failures retained; native two-arm fixture opens actual files and checks exact hashes/prefixes/tools, optional child route and final branch using authored responses only. No scientific model service runs during preparation. MAIN independently accepts immutable READY, then launches.
