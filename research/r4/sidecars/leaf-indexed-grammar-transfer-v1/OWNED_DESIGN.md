# Additive parent-owned grammar160 service wrapper

Added after the HTTP study's immutable source/input freeze at parent request; does not change driver.py, study.py, requests, sampling or treatments. The wrapper loads the unchanged qualified leaf-post-sft-suite-v1 suite.py/start_service/command/release_service and its authenticated V2 process lifecycle. No new service implementation, broad process cleanup, port takeover or retries.

Explicit --operation-root selects the NEW parent operation's PLAN/ACCEPTANCE. A privately loaded frozen coordinator has ROOT assigned to that directory before validating exact acceptance; no old anchor acceptance is inherited. Parent must accept the actual owned wrapper/HTTP READY/WEIGHTS/OWNED_READY closure and exact command. GPU and existing calibration-key environment are inherited only from that accepted operation.

Two semantic aliases: strict-rlm-qwen3-4b-role-sft-selected-v1 is old c32de; strict-rlm-qwen3-4b-leaf-indexed-final-v1 is fixed indexed epoch2/step204. The existing service calls their descriptor slots endpoint-original.json and endpoint-selected.json. Those filenames are NOT claims of original-base versus selected weights: binding and descriptors explicitly contain true adapter/config hashes and paths. The HTTP binder reauthenticates both aliases/models/base/config before use.

Start the2700-second overall clock before service startup. Reserve120seconds for authenticated owned cleanup, leaving2580 seconds for startup/binding/collection. Service readiness cap600, CPU binding cap120, HTTP job cap1830 with its own1800 collection bound and the original overall-start epoch. Each command is also clipped to the remaining2580 work deadline. Cleanup is in finally on success, failure, timeout, SIGTERM or SIGINT; only newly created authenticated service/launcher/worker identities are released. External coordinator cap2730seconds gives30seconds exceptional cleanup grace; report any excess beyond the2700 envelope as operational overrun, never extra sampling budget.

HTTP outputs remain sidecar outputs/attempt-001. Owned service/wrapper outputs go beneath the parent's explicit operation directory. Source closure and focused fake-lifecycle tests are frozen separately in OWNED_PREPARED.json, then OWNED_READY.json only once the HTTP READY and exact final WEIGHTS exist. No preparation command launches a service or contacts a model.
