# Indexed training × output representation × inference grammar

Parent-approved bounded design, 2026-09-09, fixed before the indexed checkpoint or its aggregate benefit is known. CPU preparation owns this sidecar only. Parent owns all service/GPU operations.

Question: do correspondence gains require inference grammar, and does the fixed-final indexed SFT transfer output-format competence from TREC to sentiment? Compare old selected c32de against indexed-SFT fixed final epoch2/step204; never choose another checkpoint from these outcomes. The models also differ in training curriculum and token exposure, so their comparison is not an ID-only training ablation or equal-training-compute contrast. This is an isolated leaf task, not end-to-end RLM performance.

Exactly160 first-response batch64 calls: two weights × anonymous/indexed output × free/exact grammar × ten contexts × two paired seeds981263401/981263402. TREC: the six exact exposed test64 compositions/384 source groups from leaf-composition-transfer-v1; they are reused evaluation data. SST: cached pinned stanfordnlp/sst2 validation has872 normalized sentence groups. Exclude all256 groups from leaf-sentiment-transfer-v1 and all256 disjoint groups from leaf-correspondence-anchor-transfer-v1, leaving360. Select the first256 remaining groups in lexical normalized-hash order, without inspecting labels for selection, forming four64 contexts. Normalize NFKC→casefold→whitespace collapse; preserve representative sentence bytes, physical source-row/index provenance and conflict metadata. Newly selected means not prompted in either earlier SST study, not unknown to base pretraining or never host-loaded.

Apply one frozen label-independent permutation per context: ascending SHA256 digest of [sidecar namespace, "context-permutation-v1", dataset, source context ID, source question-group ID]. Preserve original positions and the permutation. Assign batch-local q0001…q0064 in this permuted input order, matching indexed-training ID convention; do not additionally test out-of-order-ID generalization. All eight cells receive identical ID→text bytes for a context. Both formats retain the qualified generic system, native tools, task definitions and sampling. Anonymous requests one label per input ID in an exact-length JSON array; indexed requests exactly one ID→label object entry per input ID. For each representation, free versus exact differs only by the structured_outputs field. Exact grammar enforces canonical enum plus cardinality/required IDs/no extra properties; it never includes gold labels or copied-question constants.

Counterbalance all eight cells with deterministic cyclic/reversed orders across context/seed coordinates. T.5, top_p1, top_k−1, min_p0, common3072 output cap, four concurrent calls,120s HTTP timeout, no retries, tools never executed. One parent-owned qualified dual-alias vLLM0.28.0 endpoint; semantic adapter bindings and live alias/root/base validation are mandatory. No source or service surgery. A2700s overall envelope starts from the parent's supplied launch epoch; collection is additionally capped at1800s. Preflight/verification reduces the remaining envelope, never extends it. If no launch epoch is supplied, overall starts at driver entry and explicitly excludes earlier parent service startup. Parent must bound startup externally to claim the inclusive45-minute envelope. Checkpoint exact request/wire/raw response/score after every call; preserve unfinished rows as unrun/null after timeout or failure.

Primary reporting separates strict whole64 correctness, complete output-contract validity, aligned canonical item correctness/coverage, missing or duplicate IDs/cardinality, and actual cost. Parse only complete JSON; reject duplicate IDs, nonfinite values, wrong shape, missing/extra IDs and noncanonical labels. No partial-prefix primary credit, silent aliases, repairs, retries, majority voting or inferred alignment. Structurally invalid outputs have unavailable item semantics, not64 proven semantic errors. Exact-length aligned outputs can expose canonical/noncanonical item diagnostics, but noncanonical outputs fail task validity. Indexed scoring uses exact IDs, not emitted object order; report input/source/output positions separately. Schema fixes syntax/support, not semantic truth.

TREC and SST are separate tasks, with six and four context clusters; seeds/cells repeat640 unique questions, not160 independent datasets. Record per-context paired effects and per-item confusion, position quartiles, full aligned accuracy, truncation, actual physical prompt/output IDs, logical/cache/uncached/output tokens, missing-usage flags and wall time. Equal cap is not equal actual compute. Grammar versus free must have identical physical initial prompts within each weight/format; weight pairs must also match physical initial prompts and sampler fields apart from alias. Across formats only the shared input section/system/tools/definitions are identical; the output instruction necessarily differs.

Freeze source/data/requests/typed-rendering hashes before sampling. Final acceptance waits for completed indexed RESULT+SELECTION and the exact epoch2/step204 state, adapter/config/Adam/RNG member closure and prepared manifest identity204bc845987825514afe0f5a395917d933871e78b097de39db6736b72b5d23ca. If training does not complete/authenticate, indexed work remains unrun/null; do not substitute a favorable intermediate checkpoint. No result-dependent prompt/data/cap changes.

Promote a grammar-independent format effect only if indexed-minus-anonymous direction persists in free output with adequate valid coverage; otherwise narrow it to inference-constrained output. Positive SST direction under the fixed checkpoint strengthens task transfer but four contexts cannot support a broad generalization claim. Retire the broad interpretation if effects are confined to grammar/cardinality or exposed TREC. Existing dataset license/revision caveats remain: underlying SST/TREC corpus licensing is not inferred from software licenses.
