---
status: frozen-cpu-preparation
question: Does the fixed LR1e-4 update change cp32 performance on 32 untouched four-needle contexts balanced over requested ordinals 1-4?
---

Both fixed policies are evaluated on all 32 prospectively frozen records with paired seeds. This is a same-task transfer readout. The LR1e-4 dose was development-selected on exposed short contexts, most observed wins were output-boundary-only, and neither arm is pretraining-clean. A gain cannot establish improved retrieval or learned decomposition.

Both arms use the identical terminal-strip-disabled parser, external context, root prompt, fixed zero-LoRA child, temperature 0.5, 2048 tokens/action, six total root-plus-child turns, and four workers. Primary score is raw byte exact; the official SequenceMatcher-based MRCR score and procedure/copy diagnostics are secondary. No checkpoint or arm is selected after outcomes.
