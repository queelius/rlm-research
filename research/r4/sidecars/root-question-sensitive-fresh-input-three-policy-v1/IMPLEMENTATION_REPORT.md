# CPU preparation report

The first feasibility probe incorrectly inherited the original QS preparer's self-skip and reported
245 eligible groups. The independent scanner includes original QS training, development, and
protected groups as required, freezing 1,819 excluded and 181 eligible groups. This correction
occurred before selection and gold computation. The first 128 namespace-hash-ranked groups are
frozen; the other 53 remain unused.

The package ports the prior changed-metadata formula to new record IDs rather than copying values.
All nine operator families and 72 coordinates passed two-oracle gold agreement and gold-independent
native setup checks. The actual CPU `env.run_slot` qualification reached a fake provider once under
each of the three nonempty two-model role bindings. Every first transport used the identical
1,083-token prefix. The qualification used no GPU or model service.

Four focused tests cover the corrected inventory, 128-group selection, nine-family plan, metadata
transform, exact adapter hashes, fixed child, paired task identity, and actual transport receipt.
Preparation created no scientific endpoint and did not inspect any policy outcome.
