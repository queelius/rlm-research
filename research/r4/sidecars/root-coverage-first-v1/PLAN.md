# Bounded implementation plan

1. Test-first: omitted partial counts, byte-identical complete suffix, permanent conflicts/idempotent repeats, unchanged original observation;48-coordinate rotation and sequential triple dispatch.
2. New ledger payload differs only in allowed arms and displayed-summary projection. Private bridges load exact old adapter/overlay/native/lifecycle source hashes. No frozen source/image edits.
3. Authenticate both fixed adapter/config/state closures; reuse unchanged four input context/task files, generate two-root×three-arm plan and identical within-weight first native prefixes. Freeze bounded seed scan.
4. Run one real rootless native fake-provider fixture (three root/two child calls), verifying physical token graphs, child-only grammar, partial omission and complete visibility. No real model or service start.
5. Freeze SPEC/source/input/test/fixture hashes; write READY last and exact parent launch/runbook. Parent inspects before launch. Independent terminal audit remains outside GPU ownership.
