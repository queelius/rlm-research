# Corrective reduction SFT v1

CPU-prepared implementation of approved Option A, plus DESIGN_AMENDMENT.md. This sidecar changes only the prospective study; old sources and all earlier outputs remain untouched. No actual model, runtime container, optimizer-on-model, or service was invoked during preparation. MAIN alone accepts READY and launches under its independently owned single-GPU lock.

## Scientific protocol

32 fixed captures: eight training-only16-record contexts, two queries, one width16 or four width4 producer actions. There are80 actual c32 requests and152 authored root transports, including eight separate metadata-error actions on fixed width4 cases. Each producer executes natively in the live REPL. The correction reconstructs only actually printed maps into fresh names, reloads actual records, filters `user`, counts the target category and prints a scalar. The final is authored from that actual scalar, even if the child's semantic labels are wrong. No host-gold repair, sampled-code host execution, fallbacks, resampling or replacement.

First eight fixed members are captured before the forward-only gate. Exact physical prefixes must re-render from the native graph; target suffixes include native EOS and cannot exceed8192 tokens. The gate measures first-producer, corrective and masked terminal NLL. Corrective spans partition target tokens into map payload, copied Python string literals, scope/merge/reduction/print code, and other/boundary tokens. At least4/8 code-span means must be≥0.1 nat/token and median full corrective mean≥0.05. These are practical spend floors, not significance thresholds or gradient-share estimates. Gate cost projection is3× measured per-example forward time×32×4 for each arm (not a promise; load/checkpoint overhead excluded), required≤600s per arm and compatible with the remaining envelope. Failure stops before further capture/training.

Capture remaining24 and16 evaluation-only controlled states, then release inference before training. Each learned arm independently starts the same low66c adapter, fresh AdamW lr1e-4, weight decay0, clip1, rank8 FP32 LoRA/BF16 base, fixed seed981351002, four complete32-example updates. Current-action example-mean/token-mean CE gives one selected turn per example: corrective or first producer. All earlier root/child/tool history and error turns are masked; final training weight0. The arms match example mass and steps, not target tokens or FLOPs. Every complete update immediately saves adapter/config/Adam/RNG/cursor and chained ancestry; only fixed checkpoint4 is evaluated. No partial-step substitution or automatic resume.

Readouts: each of unchanged, first_producer and corrective gets32 free roots (16 new user-set compositions,16 regression) and16 controlled continuations;96/48 endpoints stay separate. All use the accurate common prompt. Controlled acquisition is physically shared once (40 c32 calls). Every policy reexecutes the authored producer actions in its own native REPL, replaying the exact saved actual c32 replies only after equality of the ENTIRE child request (model, prompt, sampling, caps, cache salt; no exceptions). First sampled root prefix must equal the original genuine source corrective prefix. Replays are not new model calls. New post-boundary child calls are real paid calls. Per-endpoint standalone cost adds that state's acquisition; owner reports40 shared calls versus40 hypothetical per policy/120 total, plus exact token-ID counts with unknown cache usage. Shared state and all controlled traces are evaluation-only.

Availability reuses pinned visibility final_capture/score: actual final-branch prompt+completion token identity, native returned finish, matching final text and no outstanding tool calls. Whole-strip strict Answer:N scores available malformed finals0 and unavailable/unrun NULL. Trace flags or last HTTP200 alone are insufficient. Raw physical requests, role/typed audits, graph episodes and final branch indices support later executed-action/data-flow audit; the collector deliberately leaves actual_scoped_reduction=NULL pending that audit, rather than claiming use from AST/prompt markers.

Training128 raw record groups are disjoint from held-out64 and enumerated reserved/excluded group catalogs. Training raw groups were previously training-exposed. The readout panel is already research-exposed, including sibling visibility experiments; new user-set compositions are prospective, not a virgin benchmark. Error/layout confounding is explicit (all error probes width4). This study tests corrective observed-state use, not general adaptive decomposition. Near-ceiling unchanged performance under the corrected common contract is a plausible result.

## Ownership and invocation

READY records the exact native owner argv and fresh outputs/attempt-001. Parent must privately bind STRICT_RLM_CALIBRATION_API_KEY and CUDA_VISIBLE_DEVICES for exactly one GPU, never place credentials in argv/logs. Credential and runtime checks precede output creation. No overwrite/retry. Existing an275780 runtime/lifecycle adapters retain the actual service wrapper/source identity symmetrically for start/release and actual580.159.04 libraries; no old compatibility path.

Owner work6900s, owned inclusive7080s, outer7200s (180 cleanup +120 parent margin). Ceilings clipped to shared clock: actual capture1800 total; initial gate300;600 per training;450 free and300 controlled per policy; service readiness900 aggregate/180 each. Service startup/release and GPU subprocess ownership use the qualified lifecycle. Partial artifacts and all144 planned NULL inventory survive terminal failure. MAIN retains GPU/lock authority and handles any explicit future continuation.
