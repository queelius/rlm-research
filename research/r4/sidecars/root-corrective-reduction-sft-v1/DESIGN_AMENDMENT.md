# Common accurate-observation contract, 2026-09-09

MAIN authorized this additive clarification during implementation after a sibling visibility experiment. All three policies and training/capture/free/controlled inputs now say records have actual `id`, `user`, `text` fields; records/context files contain no category labels; observed child maps are predictions, not dataset labels. The original generated PROMPTS.json remains historical. PROMPTS_ACCURATE.json is the sole operative prompt input, with native token IDs re-rendered. Group membership, seeds, targets and the free/controlled split are unchanged.

Eight predefined training cases deliberately perform a bad metadata lookup in a SEPARATE authored root tool action after all genuine producer actions. This added error-history turn is masked in both SFT arms. No first-producer control target contains known-bad code. It is an explicitly authored error-state intervention, not an incorrect instruction about real fields. The correction uses the real `user` key and only map literals actually visible to that root. Native trace completion and current-prefix rendering must verify these states before training admission; failed captures are retained, never replaced. The fixed index-mod4 membership places all eight error probes on four-batch cases; this nonfactorial error/layout confounding is reported rather than silently rebalanced. Training capture totals remain80 actual c32 calls;152 authored root transport calls include the8 separate probes, none are sampled-policy calls.

This isolates corrective state use after genuine multi-action map observations versus a matched producer-action SFT control and unchanged low66c. It does not establish intrinsic root inability or general adaptive decomposition. Common contract clarification may itself raise the unchanged baseline; the controlled readout can therefore be near ceiling. All arms receive the same clarification. No evaluation trace, map or correctness signal enters training. The held-out64-group panel is research exposed; this is prospective action/composition transfer within that panel, not an untouched benchmark claim.
