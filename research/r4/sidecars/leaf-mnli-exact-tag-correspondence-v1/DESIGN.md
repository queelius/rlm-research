# Exact requested-tag correspondence: MNLI32

Prospective CPU implementation approved by MAIN on 2026-09-10. No model has been launched by this sidecar's author. This follows the sealed shifted32 result; it is an adaptively selected exploratory conditional-generation test, not fresh replication or repair of earlier failed outputs.

## Question and comparison

Does a public ID naming another visible record redirect label selection after exact requested-tag emission is enforced? There are 32 contemporary endpoints: eight existing exposed contexts of 48 displayed MNLI pairs, two fresh paired sample seeds per context, matching versus fixed cyclic shift17. Both arms have exact per-position requested-tag grammar. All three relation labels remain available independently at every position; no gold-dependent grammar, class quota, balancing rule, or original source IDs.

Common messages, records, order, field definitions, requested_tag instruction, released Qwen3-4B weights/tokenizer, final-only classification role, and absence of tools are byte-identical to the corresponding shifted32 prompt. Each requested_tag always names an existing public ID. Matching uses the displayed ID; shift17 uses ids[17:]+ids[:17]. Only local cache namespace, fresh seeds/order, and the out-of-band exact decoder differ from historical shifted32. This is not spontaneous remapping competence or a test of unconstrained output formatting.

The schema has 48 prefixItems with exact public tag const, label enum entailment/neutral/contradiction, required tag then label, no additional properties, items false, and min/max48. Native decoder qualification must accept all three labels at every position, reject wrong IDs/order/cardinality/fields/labels, and prove gold mutation cannot affect requests/schema. No response rewriting, sorting, partial-array rescue, retries, fallback, or answer synthesis.

## Sources and exposure

Use the immutable original leaf-mnli-correspondence-v1/DATA.json selected from cached MNLI validation_matched revision da70db2af9d09693783c3320c4249840212ee221, parquet SHA350c26950b55f460b50d36c76aef87d64b49c78812d7abf7bf97e5fede10f186. Actual dataset mapping0 entailment,1 neutral,2 contradiction is retained. Eight contexts contain 128 selected premise groups/384 pairs; genres government/slate/telephone/travel have two contexts each. Public IDs are normalized-text-derived; hidden gold, original IDs, genre and parse metadata never enter messages. The ancestor source selection and exposure/license receipts remain in the transitive READY closure. Nonfiction OANC licensing is mixed; do not claim blanket MIT or globally unseen/pretraining-unseen text.

These exact contexts and requested-tag prompts were evaluated before this study. Prior shifted32 had all32 native responses but shift0/16 full contract valid; its gated named-record statistic was undefined with0 eligible, not0/522 accuracy. Post-hoc ungated305/522 named versus111 displayed motivated this constrained follow-up and is not confirmatory evidence. Old outputs/seals/primary scores stay unchanged.

## Frozen endpoints, metrics, and interpretation

Master981581001; one paired sample seed981581101+block for each of16 context/repeat blocks. A master-seeded shuffle freezes block dispatch order. Arm order is balanced within context over the two repeats. Four workers preserve that launch-order plan but completion order may differ. Freeze all32 planned NULL slots before service startup and retain them even after failure.

Primary: whole-output strict requested-tag-contract-gated displayed-pair label accuracy,768 planned items per arm. Available malformed output scores0; no native response/unverified response remainsNULL with item bounds[0,48]. Report availability, conditional valid/available summaries and planned-denominator bounds separately. Native tokens, finish reasons, branch/content and exact transport request/prefix must authenticate every sampled response.

Predeclared mechanism diagnostic applies only to valid shift17 outputs on the data-only differing-gold subset:261 unique positions/522 across two seeds. Report displayed, requested-named, and third-label counts, with both planned and actually eligible denominators. Zero eligible remains undefined. Scorer retains named count/eligible count; displayed/third counts can be computed from retained valid predictions and frozen gold without response repair.

Aggregate paired differences at eight context means (average two seeds); descriptive genre results only. No record/seed independence claim. Strong shifted displayed accuracy with little named preference would weaken semantic-redirection explanation. Lower shifted displayed accuracy with named dominance despite valid tags supports interference under this conditional decoder, not an internal-attention mechanism. Any remaining contract failure is retained and prompts transport/decoder diagnosis, not rescued primary accuracy.

## Native composition, clock, and accounting

Isolated local protocol/study/owner/collector/scorer reuse qualified shifted32 READY_V2 bdbd0bb85ea46dda085912131b285c4e939de48e09cd147594f7eb2a6db9ef4b. The explicit wrapper binds the pinned original MNLI study before its inherited service imports. Released model/no adapter, no tools, vLLM0.28.0, BF16,8192 context,3072 output, temperature0.5/top_p1, disabled thinking, no prefix caching, four workers,90 seconds/request. This uses the established research inference transport; no public RLM API compatibility is added.

Outer1200 seconds, inclusive work1080, owned1170 = work+90 cleanup, then30 outer margin. Startup≤180 counts within work. Collector reserves its final30 work seconds for flushing; no hidden timeout extension. Private credential preflight and process-owned lifecycle remain unchanged. MAIN alone may accept/launch/release real service. CPU tests intercept actual Popen after owner→wrapper→qualified service configuration and verify exact attempt/collector paths.

Report physical request attempts separately from returned native completions, returned input/output/cache usage with missing usage unknown, and elapsed startup/collection/release. A dispatched request is not proof of GPU generation or billing; provider billing unknown/not measured. No acquisition calls are added: historical dataset preparation/evaluations are reused provenance, not new physical model work. Actual models and service settings must be authenticated after launch.

## Qualification and seal

Focused tests cover the real source composition, first-request native typed rendering, exact decoder, source/label boundary, prompt/prefix equivalence, paired plan and strict gate. READY contains the inherited source/weights/tokenizer/data closure plus local sources, inputs, CPU receipts and exact executable argv. No original files, models, locks, active service or queue are modified.
