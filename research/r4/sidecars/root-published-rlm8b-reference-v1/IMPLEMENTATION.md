# CPU implementation handoff

The fixed48 reference is implemented;16 focused tests passed in18.67s (`CPU_TESTS.xml`). They exercise literal task truth/label-blind prompts, native final/text/token mismatch, actual isolated FINAL and FINAL_VAR and no fallback, actual native HTTP→root→child→root observation→FINAL_VAR, owner→installed suite→service→validated inference config→intercepted actual Popen, lifecycle launcher hash authentication, collector GPU hiding, coordinate-wise interrupted harvest, cancellation versus ordinary failure, and exact-name container reclamation. No model service or GPU was started. New source only; no accepted ancestor changed.

Actual first native prefixes have1935–1950 tokens, plus8192 root cap fits32768. Both real local tokenizers produce identical IDs under the common released RLM template and have identical151669-token vocabularies. One actual historical first prefix is captured through the real container/native recorder; the remaining23 use exactly substituted task/metadata/rendered system and are compared under both tokenizers. This qualification does not claim24 live model calls. `inputs/NATIVE_PREFIXES.json` freezes full messages/IDs and source receipt. No tools/grammar enter the transport body. The preliminary `.apply_chat_template(tokenize=True)` measurement counted a returned BatchEncoding mapping (2 keys), NOT tokens; it was invalid, replaced before freeze by rendered-text→flat encode and a regression. No scientific data were changed.

Paper reconstruction7198621f… is approved with original inconsistencies preserved. `inputs/PAPER_RECEIPT.json` and original/applied diffs expose the contextual line-number repair; actual brace-rendered system is independently captured. Historical commitbeb0603… remains a compatible candidate, not verified training commit. New seeds2026091001/2026091101–124 have zero prepared-source scan matches excluding this sidecar and outcomes. Initial design981651xxx and a981671xxx candidate collided and were discarded before execution.

The first harmless image import probe lacked `rich`. Only a new external pure dependency target was installed: rich15.0.0,python-dotenv1.2.2,pygments2.20.0,markdown-it-py4.2.0,mdurl0.1.2.564 dependency files are pinned; no active environment mutation. Real container Python3.11.16; host3.12.12,torch2.13+cu130,transformers5.6.2,tokenizers0.22.2,vLLM0.28.0,httpx0.28.1. `inputs/ENVIRONMENT.json` records versions and hashes. Initial isolated fixtures passed2tests/8.89s; native composed fixture passed1/7.40s; final complete batch16/18.67s. Earlier qualification directories are preserved, not overwritten. Test-first missing-module/flat-token/harvest/cancellation/reclamation failures occurred before corrections; no scientific attempt exists.

MAIN-requested interrupted harvest preserves each frozen endpoint RESULT even if aggregate ROWS is missing; truly missing rows stay NULL and partial physical calls remain. MAIN signals prevent later phases, ordinary phase failure does not. Collector termination can bypass its `finally`, so the owner additionally reclaims only recorded unreleased `rv8b-<32hex>` containers from this attempt, never another session's containers. Owner terminal reports unreleased state rather than successful completion. Per-endpoint cleanup can exceed worker time by20s, and service escalation can exceed30s reserve; there is no promise that24 endpoints finish in each1620s phase. Both phases advance immediately on early completion.

Remaining research limitations: GPU8B fit/startup not empirically qualified; serial order and root+child package confounds; exposed small panel; historical prompt/scaffold mismatch to unknown training bytes; native malformed versus missing-final distinctions; legacy official batched usage is not authoritative. Physical outside request ledger is authoritative where observed; provider billing/unknown timed-out tokens remain unknown. Strict primary and actual executed acquisition/reduction/nonzero evidence must be independently audited without executing sampled code.

READY includes exact launch/verify arguments and source/input/model bindings. MAIN retains acceptance and launch authority. Sources are frozen once READY is generated; no automatic rerun or output reuse.
