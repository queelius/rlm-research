# Additive V2 accounting qualification

MAIN approved exactly two independent-review corrections before scientific launch. Original owner.py SHAf1f11bcea6b707f81379493e2e227458df7d8c177261372648fbe186fc4c4741, collect.py SHA95c8e4b2f1810516ea8b0652628f86f600df70a0a479a9f39b380a67b5475b3d and READY.json SHAbe084ff6109ed23ab4e78c0236382aadda9a89f1a15073e1350c97de04824b2f remain untouched.

`owner_v2.py` loads the pinned original owner and overrides only harvest, collector argv, and the READY verifier binding. Every coordinate now reconciles embedded calls with on-disk REQUEST/RESPONSE/RESULT, even when episode RESULT already exists. Canonical role/index/path identifies one logical physical request; embedded duplicates are counted once, conflicts are retained as metadata, unmatched embedded known calls remain, request-only entries retain unknown usage, and raw returned usage is preferred when observed. The original endpoint score, terminal, native-availability decision, and outstanding-request count are not recomputed or salvaged. Owner-level ROWS contains the reconciled ledger; earlier collector/episode snapshots remain unchanged. Reconciliation runs after owned collector/service teardown, not while sampling.

`collect_v2.py` loads the pinned original collector and overrides only final authentication plus its READY binding. It checks actual current-iteration block-result final before standalone FINAL_VAR retrieval, matching historical core/rlm.py352–360. It requires matching last native root text/current iteration/terminal and zero outstanding requests; previous-iteration observations cannot authenticate a current final. Standalone FINAL_VAR and literal FINAL retain their historical order. No sampled code is evaluated by this correction.

Actual isolated native fixture: first root acquires a child observation and stores answer; next root invokes FINAL_VAR inside a repl block. V1 authentication rejects this actual final, V2 accepts it.15 narrow old+new tests passed11.43s, including the composed service entry, native mismatch, ledger duplicates, unmatched/late request usage, no endpoint salvage and stale-observation rejection. The first fixture had incorrectly created and referenced a fresh variable in one block: historical self.locals is updated after execution, so this fell through to a separate retrieval and did not reproduce the bug. That failed fixture/receipt remain preserved; no official source was changed. The corrected two-action fixture uses a genuine existing variable.

README/design/scientific inputs, tasks, order, seeds, weights, common template, sampling, caps, no-tools/REPL behavior, service launcher, cleanup and unused attempt-001 namespace remain exactly V1. READY_v2 explicitly pins V1 READY/closure and added sources/tests/receipts; owner_v2 launches collect_v2. Both owner and collector run receipts use the V2 identity via local module bindings without mutating the original modules/files. No model/GPU service or queue was touched. MAIN/bridge re-review and acceptance remain required.
