# Response-joint V3 directory-handshake repair
Same fixed 64-action joint-credit update and paired initialization. V3 only permits the owner-created attempt directory, then preserves the exact allowed-file check.
