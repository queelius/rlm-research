# Scale-state24 implementation plan

**Goal:** CPU-qualify the approved24-endpoint fixed24 natural-scale study without launching models.
**Architecture:** New isolated sidecar over frozen composition/native runtime. New source allocator makes four256-record parents and stable16/128/256 prefixes; a single-policy owner retains qualified native scoring, physical ledger and lifecycle.
**Tech:** Existing prime native Python, renderer, typed c32 helper and rootless authored CPU fixture; no dependencies installed.
**Spec:** `ideas/2026-09-10-operator-scale-state24.md` SHA2ce0b0de4cacdf3ef511b4e50c7e660245ee22f6aee5347101f9a1aa6ebdc3df and companion YAML c63314ca2f223791a26b7414560baf5f663b0856f7421818adeee5a5fed79dae, approved by MAIN after full read. Design-only status in those historical files is superseded by this CPU implementation approval, not GPU authority.

Global constraints: exact24+c32, no new helper/map/width mandate/repair; four parents×three sizes×two queries; master2026091700 and eight paired-across-size seeds1701–1708; current pool2000 and selected1024 must reproduce approved digests. Keep all zeros and structural failures; no source substitution.2400outer/2370owned/2220work=180startup+1950collection+90harvest+150owned release+30margin, stages advance early. Composition audit interrupts work on MAIN terminal relay.

- [ ] Source/protocol: write failing `test_plan.py` for nested stable records, literal count/weight truths and source-label mutation invariance. Implement `ss_study.py` explicit hash-bound imports and `prepare.py:build(pool)` deterministic allocation. Verify tests and pinned real source preview; freeze inputs without reroll.
- [ ] Native/owner: write failing `test_runtime.py` for exact24 collector CLI, full24 missing-result inventory plus independent physical ledger, actual composed service config interception and genuine native root→two children→observed reduction→final. Implement `ss_collect.py`, `ss_owner.py`; use qualified ledger directly, not endpoint cost snapshots. Keep all final rules unchanged.
- [ ] Qualify: build all24 actual root prefixes and public files; host-label/answer changes cannot affect them. Validate source membership/collisions/seeds and natural full-map child cap measurements without rejecting256 tasks. Run focused tests only in owned CPU fixture paths; preserve failed receipts.
- [ ] Seal: `seal.py` pins approved designs, transitive qualified sources, exact selected root/c32/base assets, source inventory/selection, all inputs and CPU evidence. Produce READY with actual owner argv; run that exact owner verify. Report paths/hashes to MAIN; no launch or live-queue edits.

Using writing-plans/test-driven-development with inline execution under explicit delegation. No subagents or Git integration needed: ownership is this new external sidecar only. Existing source/active output trees remain untouched.
