# Fixed24 nested scale/state24: approved implementation

MAIN approved isolated CPU preparation of the exact prospective design in `ideas/2026-09-10-operator-scale-state24.md` (SHA256 `2ce0b0de4cacdf3ef511b4e50c7e660245ee22f6aee5347101f9a1aa6ebdc3df`) and its YAML (`c63314ca2f223791a26b7414560baf5f663b0856f7421818adeee5a5fed79dae`). Their historical design-only status is superseded only for this CPU implementation; GPU launch remains MAIN-only.

Question: can the fixed SFT24 root retain and combine actual evidence as record scale exceeds the convenient single typed-full-map route? Four source parents each supply nested16/128/256 records; count and visible-weight-sum make24 endpoints. Exact fixed24 root94022838… and frozen c32de129… are unchanged. All native files, coding role, optional child transport and task semantics reuse the qualified dose/composition interface. No supplied label map, new helper, repair, batching mandate, recursion reward, or root decoding grammar is added. Records expose only id/user/text/weight. Targets vary by parent as prespecified.

The label-blind source rule allocates1024 of2000 eligible groups after accepted composition128 and prior dose64 exclusions. Refreshed positive root inventories, IDs and seed namespaces are pinned in PROVENANCE. Nested sizes keep original IDs/order/users/weights; they change gold and difficulty. Four parents, not24 independent groups, are the analysis clusters. Source novelty is only under the named root inventory: TREC child-training/catalog exposure remains. No gold/length/behavior filtering or reroll occurred; all24 happen to be nonzero. Zero baseline0/24 and pooled best constant40 scores2/24 are descriptive, not selection criteria. Eight sampling seeds are paired across three sizes; fixed hash order avoids systematic small-to-large dispatch.

All24 actual root prefixes were rendered and file setups inspected on CPU, with private gold0→999999 leaving bytes/prefix unchanged. Max initial prefix1042 fits8192 with2048 action cap. An authored native two-child fixture genuinely loads records, receives disjoint8-ID predictions, updates one live map twice, computes a positive weight sum and returns an authenticated native final. The fixture teaches no scientific sampled policy and has no GPU calls. Child-template input budgets are measured from its exact tokenizer-roundtripped child request, replacing only the public question payload:256 stresses one typed-full-map route, **not every valid one-call strategy**. Constant-label output serializations are illustrative non-gold measurements, not output lower bounds. No size is dropped if later tool observations overflow.

Primary is strict authenticated dataset-final correctness with planned8 per size, operator panels, four-parent paired contrasts and NULL bounds. Authenticated malformed scalar finals score0; missing RESULT, unverified/no-final branches remain NULL. Optional reacquisition is permitted and counted. Mechanism claims require observed contributing child outputs and executed reduction, distinguishing persistent accumulation from literal reconstruction. Wrong child labels remain untouched; supplied-observation consistency is separate from dataset truth. No sampled-code reexecution. Missing aggregate cannot erase endpoint RESULT, and endpoint RESULT cannot suppress physical calls; all disk physical receipts are counted independently with unknown usage retained. No missing-RESULT score salvage or automatic retry.

Budget is2400 outer/2370 owned/2220 work: nominal180 startup +1950 collection +90 harvest;150 service-release/emergency +30 outer margin. Owner starts clocks before verification, advances early, limits collection to work_deadline−90, and bounds release/harvest by owned deadline. Stage estimates are caps, not waits or guarantees of completion. Collector retains inherited4 workers/180-second episode caps; nested long outputs may consume these caps. Per-call receipts/graphs/RESULTs checkpoint observations; all24 planned rows exist before service start. Cancellation stops the sole phase and attempts owned release; no second phase or resumed scientific attempt exists.

Explicit path+alias imports retain the corrected native collector and qualified ledger. Fresh composed owner→installed lifecycle→service wrapper→inference config/Popen is intercepted on CPU; exact fixed24/c32 descriptor and8192/two-LoRA settings are asserted. No external model process starts during qualification. Starting weights are not chosen from composition outcomes; those remain unread pending MAIN terminal relay. This is a scale/state diagnostic, not training or evidence of broad compositional competence.
