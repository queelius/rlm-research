# Phase192 runbook

Parent-only future launch from this sidecar:

`/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-reminder-phase-v1/owned.py --directory /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-reminder-phase-v1/owned/attempt-001`

Read READY/SPEC/source closure, then MAIN publishes separate acceptance and exact predecessor binding. One exclusively assigned GPU and inherited qualified API-key environment are required. No launch authority is conveyed by CPU_READY. Outer900 seconds; inclusive owned870, shared work750, collection600, cleanup reserve120. All caps count from original launch/collection starts and never reset per phase.24 eight-call Williams units sequential inside/four units concurrent. One fixed c32de service, no study retries or replacement calls. Setup/collection caps preserve incomplete coordinates and raw failures rather than repairing or extending the experiment.

Scientific grid:12 exact exposed contexts from sparse144,4 per TREC/SST-2/AG News,64 records each; phases0/1/2/3, matching or p0000 constant, seeds981310011/021. The original981308 proposal is retained in the design document and superseded by MAIN's collision amendment. No new source selection, old outcome pooling or cue96 inspection by author. Root RL/SFT models do not enter this component experiment.

One neutral prompt and identical input/system/tools/alias/sampling apply to all8 conditions/context/seed; only the ordered structured-output schema changes.16 objects/48 strings per call. Object order is exactly tag then label. No anchor-position list or cue-specific instruction leaks into the model prompt. CPU qualification must establish all192 full native typed prompt vectors,60 assigned schemas and168 within-unit comparisons proving24 complete8condition prompt-equality units. Native grammar changes allowed output actions and autoregressive prefixes; not a wording-only intervention.

Exact old learned adapter c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3, config ec773bc3b9de58af98a7c4dce9a40af795a4d05cd47284dbbfb27eac99b74174, alias strict-rlm-qwen3-4b-role-sft-selected-v1. WEIGHTS binds base and artifact paths. Retain the qualified native template, service/capture and actual cache policy; read raw cached counters rather than infer costs.3072 output-token allowance,8192 context,120s/call,four concurrent workers. No tools are executed, no generated host code or training likelihood is invented.

Primary: within each of the same61 records at positions4–64, compare matching-minus-constant correctness at distance0 versus distance3. For i>=phase, distance=(i−phase)%4; before the first anchor it is undefined, never wrapped. Each core record occurs once at every distance across phases. Average records, then two seeds within a context, then four equal contexts separately per task. Require the whole eight-call block for its primary and both seeds for a context mean; otherwise NULL. Completed malformed/empty arrays give0 for all positions, unavailable/provenance-invalid calls giveNULL. All64 remain in overall/whole-array outcomes; positions1–3 and first/final anchors are descriptive boundaries. Full0–3 profiles, phase/arm cells, raw/schema errors, count L1, input/cache/output and stop costs remain visible.

The projection in analysis.json reparses raw model response content using the frozen phase schema, but is implementer-provided, not independent primary audit. An independent auditor must check raw wire request bytes/ordered hashes, full actual prompt IDs, response alias/output vectors, raw usage including cached tokens and exact source/gold lineage. A partial key-order prefix is not full-schema conformance. Preserve192 planned/null denominators and physical all-work costs. No label realignment, answer repair, post-hoc endpoint substitution or removal of expensive failures.

Interpretation: this same-record design reduces differing semantic support between cue distances, but phase still changes earlier sampled output/history and object/string positions. A positive contrast is a property of this constrained correspondence package, not proof of internal attention, learned root planning, heldout generalization or equal realized compute.
