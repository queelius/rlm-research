# Bounded implementation and qualification

1. Publish outcome-independent DATA_READY first; preserve it thereafter. DONE.
2. Reuse authenticated canonical16 actions and create48 prospective native prompts.
3. Capture real child executions in the eventual MAIN-owned GPU phase; freeze
   actual graph-aligned terminal examples before any optimization. Not CPU-generated.
4. Apply tested per-example action CE plus optional0.1 terminal CE, four fresh-Adam
   updates/arm; save adapter/config/Adam/RNG/input identity every update.
5. Reuse exact owned service lifecycle and native collection for all three roots.
6. Focused CPU: label-independent selection RED→GREEN; tiny actual PEFT update,
   matched coefficients, target masks, finite gradients, saved tensor/Adam equality;
   stored native real graph→physical second prefix equality; executable new-panel
   collector/metadata/entrypoint import. No host generated code or model call.
7. Bind source/input manifests and publish READY last; MAIN alone accepts/launches.
