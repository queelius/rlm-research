# CPU preparation report

New owned namespace only; no historical source, GPU, runtime service or model calls.
DATA_READY and data bytes were published first and remain unchanged. MAIN's later
normalization clarification is bound by the final READY; the panel's historical
approval hash is retained as history, not claimed to hash the amended live document.

The thin scientific delta is actual-child authored capture, graph-aligned terminal
targets, optional0.1 per-example terminal CE, and the new64-group readout panel.
The16 canonical actions remain byte/token-identical. Existing adapter audit,
checkpoint save, native schema/typed hooks, local runtime, ownership and service
release mechanisms are reused under exact source pins. Teacher true dataset count
is logged only as a host diagnostic; dummy runtime verifier0 is not gold, and the
target remains the actual observed scalar. Synthetic authored-root transport
probabilities are never policy likelihoods or training fields.

One narrow new-readout correction implements the already-declared availability
boundary: native ok=false/error with no root reply is NULL even if its last HTTP
tool request returned200. Completed ordinary empty replies remain failure0.
This addresses the demonstrated historical broker _queue.Empty distinction;
it does not change any historical score/source. Focused fixture RED→GREEN.

Qualification: selector RED→GREEN; actual tiny PEFT update with fixed action
coefficients and optional terminal mass, all prefix/observation labels masked,
unchanged frozen base, adapter/Adam save-reload equality; stored real native
canonical graph plus tool scalar exactly reconstructs second physical token prefix;
new panel native task IDs/source strata and actual private collector imports.
No fixture probabilities are training data. No new live native fixture is required:
the prior owned canonical action fixture is reused, while current runtime corpus
must independently satisfy exact graph/physical-prefix and actual-scalar guards.
CPU_TESTS records exact invocations/output. Library deprecation/Conv1D notices
are retained, not treated as failed scientific tests.

Caps:1080 teacher capture,480 each training,420 each readout,180 each serving start,
all clipped to one3450 work deadline;120 cleanup,3570 owned,3600 parent outer.
Every planned arm/coordinate is explicit in top-level terminal inventory even on
failure. Actual native retries, cancellations and child HTTP attempts are retained;
there are no new study rerolls or availability-based replacements. Final selection
is fixed4 for both new arms. No executable resume is implemented (see RESUME).
