# Question-sensitive SFT72: approved native complete-transition curriculum

Implements the immutable approved allocation MD/YAML named in APPROVAL.md. This sidecar is external and isolated. MAIN alone decides GPU acceptance/launch after warm final; preparation beforehand overlaps CPU work without selecting a warm checkpoint. No original studies/files change. Author bridge_audit is not an independent analyst of this implementation.

## Frozen source and exact tasks

Refreshed named actual/accepted root selections left976 of the pinned scale2000 groups. Exact approved label-blind compact-JSON SHA rank selected320 once:8 train16-record contexts,4 dev,8 protected. All source/path/line/exclusion/seed/public-ID/native-ID receipts are in inputs/PROVENANCE.json; all public records/texts/labels in PUBLIC/HOST_GOLD/GROUPS. TREC train, c32-training/catalog exposed; root-new only under the named inventory, protected only against this new process. No global unseen claim or gold/coincidence reranking. Historical broad candidate catalog alone is not execution; original base-pool exclusions retain actual broad selections.

Each context has the approved nine exact slots (see ALL_SPECS): T1/T2 per-user threshold count; M1/M2 per-user maximum; J1/J2 B-weight among A users; P1/P2/P3 count/distinct/weight anchors. Train thresholds2/6, protected4/8. Train maximum EVEN/ODD, protected ALL/EVEN. Train conditional clockwise adjacent category pair EVEN/ODD; protected reversed pair ALL/EVEN. Label cycle/indices and scopes exactly follow the approved table, with r0/r1 repeated—not gold-balanced. No training/protected conditional pair overlap. All categories/scopes individually represented in training; protected operator/parameter recombinations are not entirely unseen operators. Conditional ALL changes both pair and scope, not a pure factor contrast.

Train72 trajectories. Protected72 tasks/policy ×2=144 mandatory free endpoints; dev only M1/M2 on4 contexts ×2policies=16. All180 defined native tasks are prefix-qualified but only160 readout slots execute. All160 are preinventoried before any service; no rerolls or checkpoint selection. BASELINES dev describes all36 defined specs; EXECUTED_BASELINES separately describes actual8 dev slots. Zero floors train32/72, protected26/72, executed dev3/8 remain. ANTI_COINCIDENCE includes fixed pooled/count/max/role-reversed/all-scope/first-train-literal controls; diagnostics do not select cases.

## Native capture and learning

Exact fixed24 adapter94022838a64a530e1abc8daf6cd43502d8aec7d549b6bfec10dd4928b0ca9006 and frozen c32 child. Same accurate coding role, optional typed-child grammar, four public files records.json/context.txt/query.txt/batch_contract.py,2048 root action/8192 context,T.5/top_p1. No card/task.json/formula/map/example/aggregation helper at readout, no forced children or batching width. Literal user scope and clear mathematical task semantics are part of the new question, not a teacher algorithm printed in its prompt. Max users with no A contribute0; threshold strictly >; scoped conditional B records count once.

Each training coordinate gets its own genuine native REPL and one physical16-record c32 acquisition. Authored producer decodes/prints actual map; authored reduction applies actual operator/scope/parameters to that state; terminal target is the actually observed scalar. Wrong semantic predictions remain.72 physical acquisitions and216 authored root actions planned; no historical-map reuse, gold repair, synthetic map, retry or success filter. Native graph re-render verifies each actual prefix. Corpus admission verifies exact coordinates, three action kinds, episode-local unique physical child receipt, and all current-action masks. Structural failure stops complete-corpus admission; no partial corpus substitutes. Authored transport uses synthetic logprobs only to carry target tokens; they never become RL policy likelihoods.

Fresh Adam0 from fixed24, six predetermined full72 updates,lr1e-4,weight_decay0,gradient clip1, per-trajectory role mass .45 producer/.50 reduction/.05 terminal. Loss only actual current root actions including end token; all preceding prompts/history/observations/child tokens masked. Nonliteral NLL excludes both Python string and numeric literal tokens and boundary/transport tokens; not a gradient-share claim. Four variable-layout choices are independently hashed, not perfectly balanced or independently causal.

Exact6 gate trajectories: train00/T1,01/M1,02/J1,03/P1,04/P2,05/P3. MAIN explicitly approved cost/validity-only: finite all-role/nonliteral NLL, native mask support/prefix<=8192, actual memory/forward measurements and remaining budget; no old4-of6 NLL>.1 or efficacy predicate. All measured values, including nearzero anchors, reported. Projection is six measured complete trajectories /6 ×72×6×3 plus300 load/checkpoint proxy, not guaranteed time. Gate executes only at future MAIN GPU launch; CPU toy timing is never admission evidence.

Qualified native model loader verifies released base and fixed24, FP32 rank8 LoRA only, no child loaded for training. New entry additionally pins named504 parameter mapping. Every optimizer update must create moments for all trainable tensors with matching shapes; checkpoint each1..6 contains weights/config/optimizer/RNG, cursor0, ordered example IDs, moment step ordinal and prior state link. Missing fixed6 is unavailable treatment even if5 exists. No extra updates or later best-checkpoint selection.

## Lifecycle and accounting

8100outer/8070owned/7920work. Capture≤1200, load/gate/train≤2100, dev≤300, protected≤3900, finalization≤420; cleanup150 and outermargin30. To avoid paying four extra startups for dev, each readout policy uses one shared service:≤150 dev plus≤1950 protected budget including actual startup and90 release. Protected collection deadline subtracts startup/release and excludes only actual dev elapsed. Two policy phases≤2100 each, fixed unchanged then sft6; serial/cache/time confounds disclosed. No stage borrows mandatory readout reserve for gradients; early completion advances. Ordinary training failure still allows baseline readout; missing6 has all plannedNULL; MAIN termination starts no further phase.

Exact owner→qualified wrapper→actual inference config and NATIVE/TRAIN argv composition tested by intercepting process launch only. No model/service launched in CPU qualification. All physical files unioned across capture/dev/protected/other, not RESULT summaries; attempts, HTTP responses, choice completions and known/unknown usage separate. Authored transport not counted as model generation. Rejected request not established GPU generation; billing unknown. Preserve failed coordinate dirs, elapsed training/forward cost and all160 NULLs separately from strict observed0. Cleanup uses qualified lifecycle with90 per-service reserve; overall150 guard is not a license to extend collection.

## Outcome interpretation

Strict authenticated native final against dataset gold, planned denominators and NULL/operational bounds. Report protected composition48/policy separately from primitive24/policy, zero/nonzero, seen/max-scope versus held-out scope/parameter strata, both-task signatures and eight protected clusters. Dev is a narrow interpolation diagnostic after fixed training, not selection. Every available final receives actual executed operator/scope/map-provenance/scalar/final audit, including faithful-wrong child semantics, wrong-operation coincidence, literal recovery and unknown; never infer fidelity solely from host scalar agreement or execute sampled code. New-curriculum package is not an isolated parameter-diversity, SFT-vs-RL or learned general intelligence claim. Practical promotion additionally needs no protected availability loss/primitive operational regression and faithful composition improvements across≥3 contexts; otherwise diagnose rather than promote.

## CPU qualification boundary

Three authored native composed fixtures prove real acquisition→observation→root targets→current-action losses. J1 additionally enters actual trainer CLI using the captured native trajectory expanded72 times in an explicitly synthetic fixture, 31-way toy vocabulary at CE and504 tiny trainable tensors; six genuine CPU Adam/checkpoints validate routing, masks, mass, ordinals/RNG, not real GPU fit,72 independent captures or transformer numerics. Actual TRAIN interpreter imports qualified PEFT/model-loader dependencies without loading a model. Old outputs and rejected prep receipts are preserved. Only focused tests/source closure are required before MAIN review, not broad infrastructure polish.
