# Question-sensitive SFT72 Implementation Plan

> Execute inline using executing-plans and scoped TDD. MAIN owns review/launch; approved external sidecar isolation replaces Git worktree creation. No subagents or broad suites.

**Goal:** qualify exact20-context SFT72 plus160 native readout slots with authentic capture/training linkage.
**Architecture:** new pure parameter/scoping oracle and source allocation; thin aliases over qualified od capture/learning/train and existing native two-LoRA lifecycle. No new model API or numerical framework.
**Tech:** pinned NATIVE/TRAIN Python, native ACP/IPython, authored CPU provider, tiny torch training fixture.
**Spec:** APPROVAL.md and approved ideas/2026-09-10-question-sensitive-sft72-allocation-proposal.{md,yaml}.

## Task1: Pure table and one-time input preparation

Files `qs_problem.py`, `qs_study.py`, `prepare.py`, `test_inputs.py`, `test_problem.py`. Tests first: exact nine train/protected specs and unseen clockwise/reverse pairs; strict threshold2/6 versus4/8; scoped zero max, duplicate A/B and reversed category semantics; `answer(records,labels,row)` equals independent enumeration. `prepare.build(pool)` takes first320 by approved compact-JSON SHA rank, split128/64/128, fixed labels-blind user/weight/public IDs; fail insufficient/collision/prefix rather than resample.

- [ ] Write/run RED oracle/table/input test; implement pure table and narrow qualified `qs_study` aliases.
- [ ] Refresh/pin current actual+accepted root source selection and seed/public/native-ID inventories; run allocation once. Emit PUBLIC/GROUPS/HOST_GOLD/TRAIN_PLAN/DEV_PLAN/FREE_PLAN/GATE_PLAN/PROMPTS/NATIVE_TEMPLATE/PROVENANCE/BASELINES/EVALUATION_PLAN/START_BINDING. Include exact20/320, training72, dev8/policy, protected72/policy and all160 NULL inventory.
- [ ] CPU native setup verifies exact four public files, private-gold/prompt invariance and <=8192 with2048 action for every task. All gold/baseline collisions reported, no reranking.

## Task2: Authentic acquisition and training-input linkage

Files `qs_protocol.py`, `qs_collect.py`, `qs_learning.py`, `test_capture_training.py`. `producer(row,index)` only accepts index0 and makes one actual16-record typed c32 call; `reduction(row)` executes the exact operator/scope/threshold against the returned decoded map; terminal is observed scalar. Reuse current-action row builder/nonliteral spans and original graph/native validation. Collector exact capture72/free8or72 admission; preserve physical failures and raw artifacts.

- [ ] Write/run RED native fixture: real ACP/IPython→one authored provider child→observed map→three current-root targets→TEACHER; wrong child labels retained and private hostgold has no effect.
- [ ] Implement qualified aliases. Assert capture never physically dispatches authored root targets and all root prefixes/full current-action masks match genuine traces; copied string/numeric literals excluded from nonliteral spans.
- [ ] Feed that actual TEACHER through qualified `weighted_turns` and `losses` into a tiny CPU model/update. Assert three role weights .45/.50/.05, masked-history perturbation does not create extra target mass, and actual Adam update. No standalone synthetic-only mask proof substituted.

## Task3: Trainer, fixed checkpoint binding, lifecycle/reserve

Files `qs_train.py`, `qs_binding.py`, `qs_owner.py`, `test_train_owner.py`. Reuse qualified model loading, current-action gradient update, save/checkpoint validation. Start exact24 with fresh Adam, save every1..6; no old optimizer continuation. Actual trainer entry loads this sidecar's captured corpus and exact gate coordinates. Source-pin all exact aliases.

- [ ] RED tests actual trainer CLI→captured corpus→tiny model path and six real optimizer updates/checkpoint/RNG/cursor; all504 mapping checked on later real model, no child training load. Fail missingfixed6 despite existing5.
- [ ] Implement finite/native-mask/cost gate and measured projection, maintaining readout reserve; settle any material gate assumption with MAIN before seal.
- [ ] RED/green actual owner→servicewrapper→intercepted inference/trainer subprocess argv using real bindings, credentials presence only, TRAIN CUDA visibility and exact stage caps. Baseline readout survives ordinary training failure; external termination starts no later stage. All160 slots/raw-union costs survive absent RESULT.

## Task4: Focused seal and handoff

Files `seal.py`, `DESIGN.md`, `RECIPE.json`, CPU receipts/READY, O/question-sensitive-sft72-implementation.md.

- [ ] Run only focused new pure/native/linkage/owner tests under qualified CPU Python (TRAIN where required), retain initial failures. No GPU runtime gate execution.
- [ ] Pin accepted active source closure, actual selected raw data/exclusions, adapters/config/tokenizer/environment and all160/72 frozen inputs. Write READY exact owner argv and verify separately.
- [ ] MAIN full source/review then launch only; no source changes after READY. Any subsequent analysis author overlap disclosed. Future terminal audit verifies all available semantics and full physical cost union.
