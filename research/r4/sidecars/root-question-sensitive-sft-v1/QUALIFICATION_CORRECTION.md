# Preserved first seal failure

CPU_REPORT.json/qualification-final-001 remain unchanged. Its returncode1 and detailed failures are authoritative: an unsorted test glob selected a later root request as the first prefix. Physical0001 matched the frozen prefix and the actual collector authenticated it. Sorting the fixture reader fixes this; no science inputs or runtime source changed for this correction.

The failed report's constant `tiny_real_optimizer_updates:6` was the intended qualification path, not six updates executed in that failed run: the new assertion stopped all three fixtures before their numerical section. Earlier GREEN_LINKED_TRAIN_OWNER.xml had successfully executed the six-update path; the new CPU_REPORT_v2 separately records its rerun. Do not combine these receipts or treat fixture responses as scientific model work. There was no rejected scientific attempt or READY; the first READY is written only after the complete rerun passes.
