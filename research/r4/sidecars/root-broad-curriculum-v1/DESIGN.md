# Broad16: root aggregation across documents, sizes and requested categories

Approved CPU implementation of [the main brief](../../operations/2026-09-09-continuous-allocation/BROAD16_IMPLEMENTATION_BRIEF.md), using immutable candidate data MANIFEST3b00b9a7…. No acceptance or launch by this preparer.

Start the exact converted original root857a7ce6… with fresh Adam and seeded RNG. Hold childc32de129… fixed and never load it into the training model.16 fresh generations use24 episodes each:3 distinct contexts ×8 samples;24 training contexts at16/32/64 records appear twice, with different targets.48 scheduled task groups cover HUM/NUM/ENTY/LOC equally. Native root actions alone receive tokenTIS/PPO credit; child/tool/prior-token observations are masked. Preserve the authenticated prior loss, episode/turn weighting, BF16base/FP32adapter, LR5e-5, zero decay, clip1, TIScap2, PPOepsilon0.2 and distribution guards. One actual full-batch update follows all forward/backward passes for each fresh collection; no stale collection reuse.

Validation:8 question-group-disjoint contexts ×one balanced trained target ×2 fixed seeds =16 episodes at0/4/8/12/16. Primary policy is fixed-final16 regardless of validation. SELECTION records final16 plus a descriptive earliest maximum; no replacement of final16 with that maximum. A STOP remains partial. Every update saves adapter/config/optimizer/RNG/state, input lineage, gradient/delta and correction capture through the original atomic commitment mechanism.

Final transfer is original then final16,48 paired coordinates/96 episodes:6×64 compositions ×one trained+one reserved requested target ×2seeds (24pairs);4×128+2×256 ×one trained target ×2seeds (12pairs);3×32 leaf-test-exposed documents ×two trained targets ×2seeds (12pairs). Internal phases and outputs are `transfer-original` and `transfer-final`. Neither optional SST nor leaf-validation-exposed candidate strata enters this registry. DESC/ABBR are query-target transfer, not unknown classes. Paired seeds do not remove serial serving/order variation. All variations within one document are clustered; training rewards use fresh unpaired streams.

Metric: exact final count and complete observable/admitted/planned denominators, with actual calls/tokens, policy/child aliases and physical sampled IDs retained in native raw traces. A valid observed wrong answer is not an infra null. Recovered child-overflow exclusion stays exactly the qualified narrow rule; no new negative admission, repaired responses, forced recursion, retry/reroll, or fabricated old likelihoods. No mixed reward group stops the campaign with evidence retained.

Caps:18000 work seconds;18120 inclusive main-entry exception cap with120cleanup reserve;18150 parent outer cap. Collection1800/round, training600load+optimization plus inherited bounded CPU authentication/serialization allowance, service-ready180, validation900, shared two-policy transfer3600.8 concurrent episodes; unchanged native8192causal/2048sampled-token request limits and existing environment/child deadlines. External context files need not fit one model request; no automatic chunking or larger context window is introduced. Keep actual inherited MIG/LD environment. Parent checks an adequate allocation window and exclusively empty GPU before launch. Expected3–4h is not guaranteed; partial commitments survive the cap.

Data provenance remains the candidate closure: named prior-root/source-group exclusions, public TREC with unresolved underlying license, child-SFT-supported versus previously leaf-test-exposed strata distinct, unknown whole-history/pretraining/paraphrase overlap. This is not a breadth-only causal comparison: data breadth, updates and attempted rollout count change together. A matched narrow16 campaign is a separate future decision.
