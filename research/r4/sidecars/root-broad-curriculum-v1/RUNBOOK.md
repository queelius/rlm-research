# Parent-controlled launch

No model service or training was started by preparation. Authenticate READY's SHA and entire bound source/input closure before parent acceptance. Preserve all existing outputs and unrelated processes.

CPU check from this directory:

```sh
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python campaign.py verify
```

Exact parent job argv (inherit the actual assigned MIG UUID, driver LD path and existing local service key; do not replace CUDA_VISIBLE_DEVICES with0):

```sh
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-broad-curriculum-v1/campaign.py run
```

Output: `outputs/attempt-001`, required absent. Work18000s; inclusive exception18120s; parent outer18150s plus the usual bounded owned-child cleanup. The source entrypoint owns a namespace lock and serial service release; an external accepted operation must provide the existing shared GPU lock/empty-device handoff. No generic scheduler is included.

Terminal evidence is FINAL.json or STOP/INTERRUPTED_AFTER_COMMIT with saved checkpoints retained. There is no automatic resume or reroll. If no mixed task group exists, the generation stops without an update. The parent alone may authorize a separately named continuation after diagnosis.

Model lineage: original root857a7ce6…/empty Adam → consecutive1..16; fixed childc32de129…. Primary selection artifact must bind16. Earliest validation maximum is descriptive and is not the transfer checkpoint. Preserve raw native collections, routing evidence, export rows, correction captures, generation commitments, service owners/release markers and stdout files; do not equate CPU synthetic likelihoods to sampled-model evidence.
