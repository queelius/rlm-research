# Bounded implementation and verification

1. Read the approved brief/data proposal; preserve immutable source inputs and original starting weights.
2. Test first: generation7/8/9/16, exact24-coordinate admission, scheduled cardinalities/seeds, fixed-final selection, observed vanished-process boundary. Initial five tests fail because the adapter is absent.
3. Privately hash-authenticate original helpers. Count-check only the generation16, validation schedule/denominator, exact24 and final-policy source seams. Record executed transformed-source hashes. Native make_tasks reads only this80-task registry; no original pilot task injection.
4. Qualify actual public prompt/context bytes and host-gold mutation invariance. A focused test caught literal escaped newlines; correct before writing task identities. Build the immutable47-context/80-task input registry and all384/80/96 coordinates.
5. Exercise real tiny PEFT16 updates/save/reload/Adam and unchanged root-only masks; exercise real adapted coordinator with GPU/process boundaries replaced by CPU fakes from step7 through final16. These are fixtures, not research rollouts or four-billion-parameter arithmetic verification.
6. Freeze sources/inputs with CAMPAIGN, AMENDMENT, LIFECYCLE_V2 and PREPARED. Native qualification then authenticates the full binding/rendered tasks and runs a trusted rootless root→child→root CPU fixture on a new task. Its likelihoods are explicitly synthetic and never become research training inputs.
7. Publish implementation report and READY last after qualification. No further source edits; parent reviews/accepts/launches. Actual first native rollout generation supplies the research likelihood/mask evidence.

Reuse attribution: root-rlvr-campaign-v1 common/train/coordinator/native; independent-seed wrappers as the pattern; recovered-child continuation's exact unsampled overflow exclusion; lifecyclev2 PRL::Inference/owned descendants; root-seed-lifecycle-continuation-v1 observe_or_absent wrapper for FileNotFoundError/ProcessLookupError only. No frozen source or active environment edits.

PREPARED is the campaign identity check during CPU qualification. Final READY is a separate main-entry run gate and publishes exact closure/argv/caps after successful qualification. This two-stage publication avoids advertising readiness while source files are still being edited.
