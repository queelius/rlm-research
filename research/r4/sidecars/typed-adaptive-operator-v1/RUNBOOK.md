# Typed adaptive operators24/32

Read the bound typed-adaptive-operator design. Main alone accepts/launches; no GPU authority from READY. This is an operator-authored plan comparison, not sampled root planning or root training. Same exposed four128-record contexts and all source files from adaptive-filter-pilot-v1; no outcomes select data. Two fresh seeds, user-all16/user-filter16/shared-global-all16:24 physical,32 logical. Each global result is referenced twice, never double-charged or treated independently. All plans load the full public file before selection; filtering reduces child classification work, not host payload reads. User2/1/2/2 and global16/18/22/20 skew retained.

All three arms use c32de child, same ordered canonical grammar through exact public batch_contract request/depth matching and image8cfe. Operator and public request/parser files are unchanged. Ordinary tools remain described but effective child action space is constrained from first response; syntax does not guarantee semantic accuracy or remove nulls. No root model request or root behavior likelihood is manufactured. No new retries, repairs or fallback; inherited nano transient retries measured inside cap.

One service, startup180, cumulative collection1440, work1680, owned1800, outer1830 seconds.120 cleanup reserve; original collector60 finalize reserve and >50% execution failure stop after8. Four serial-triple workers. Native/API/schema failures and unanswered execution remain explicit nulls, per original adaptive operator contract. Cheap early failure is not efficiency: compare costs for paired completed successes separately from all planned work. Retain raw operator transcript and physical native calls/tokens/schemas/ancestry. Analysis is implementer-owned, not independent validation.

CPU attempt001 expired the inherited45-second per-runtime ACP setup limit before any provider call, while the private CPU/store was also serving typed36. Its raw failure and original experiment source are preserved. New-study per-runtime setup is120 seconds symmetrically, still bounded by the same collection/owned absolute cap; no accepted job setting changed. This is not a throughput guarantee or model episode reroll.

CPU attempt002 completed the actual all16 native typed-child path and valid aggregation with one fixture provider request, but filter16 setup exceeded its160-second fixture cap before provider dispatch. Its failure and partial raw evidence remain frozen. Main's bound partial-qualification decision accepts this composed evidence alongside the unchanged original filter operator proof and exact schema checks for36 distinct batch requests/136 planned sessions. This is NOT a fresh typed-filter native PASS. The real24 run tests that path without repair or replacement; shared VFS startup contention remains an operational risk.

Main provides exclusive CUDA_VISIBLE_DEVICES and existing STRICT_RLM_CALIBRATION_API_KEY. Exact argv in READY; CPU verify is `driver.py verify`. `driver.py analyze --output …` is optional post-terminal CPU projection. Private image/store/wrapper already qualified and can be shared by distinct owned containers; no image/config modifications or rebuild. Frozen source closure is never amended after READY.
