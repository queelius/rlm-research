# Randomized source identity versus a changing ordinal counter

Approved brief: operations/2026-09-09-continuous-allocation/IDENTITY_COUNTER_BRIEF.md.
Reuse the exact exposed padding128 eight64-record contexts, old c32de child and
pinned base/native protocol. No training or new untouched-data claim.

Source ID ranks q0001..q0064 are an independent namespace/master981266001 hash
permutation. Each group keeps that ID through two independently hashed presentation
orders. Two hash-derived sampled seeds and three exact-grammar array-of-object
arms yield96calls: matching randomized source ID, unrelated displayed ordinal
p0001..p0064, constant p0000. Six arm orders cycle across32 coordinates (counts5/6).
No labels affect IDs, order, seeds, arm allocation or grammar. All arms classify
the same displayed record at the same array position.

Only truthful tag instructions and exact tags differ. Input text/order/source IDs,
system/tools, full canonical label enum, T0.5/full support,3072 output cap and8192
context are fixed. Qualify q/p standalone prefixes and all corresponding tag token
lengths before freezing; a mismatch stops preparation, not a silent alternative.
Per-call exact tag/cardinality/canonical scoring uses private frozen padding code;
semantic alignment, class-count vectors and object lexical order are retained.
Invalid complete outputs fail strict scoring but are not64 known semantic errors;
infra/unrun and unavailable semantic contrasts remain null. No repair or code execution.

Primary matching-ID minus ordinal is paired by context, presentation and seed;
secondary ordinal-minus-constant and matching-minus-constant. Report tasks apart,
with four source context groups per task and permutations/seeds nested. A surviving
matching-ID advantage motivates source correspondence; ordinal matching meaningful
and beating constant revises toward positional bookkeeping. Neither proves an
internal attention mechanism; prefix/instruction/grammar token content differs.

Private collector/scorer imports; the qualified single-alias service lifecycle is
reused unchanged. Owned wrapper is a scoped copy with only namespace/caps changed.
Four workers,120sHTTP,zero retries,600s collection,1200s owned from main entry with
120s cleanup reserve,1230s proposed parent total. Parent alone accepts/launches.
