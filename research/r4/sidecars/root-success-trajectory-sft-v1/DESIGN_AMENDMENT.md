# Proposed additive three-arm shared-control readout

Pending MAIN approval; the original approved DESIGN.md is unchanged. Training/export work continues exactly as approved while this readout decision is pending.

MAIN reports the adaptive-RLVR predecessor stopped after7 committed updates because round8 supplied no mixed within-prompt group. It has no prescribed final8 readout. Add its exact last-saved7 checkpoint as a third arm in this NEW prospective readout only; do not rewrite its original STOP, selection policy or study claims.

Arms are `baseline` (unchanged efab interface-SFT final4), `success_sft` (this study's fixed final8), and `rl7` (exact adaptive round7 COMMIT/state/adapter/config/Adam/RNG closure). Authenticate the last-saved checkpoint and original STOP before freezing the new input manifest. Fixed7 is selected for availability after that stop, not validation performance, not successful completion of the original eight-update RL recipe. Labels and readout metadata must preserve this distinction.

Each arm uses the SAME24 source coordinates/prompts and fresh paired seeds981308101–108/201–208/301–308, unchanged typedc32 child/runtime/sampling:72episodes total. Determine all three sequential weight phases before inference by sorting arm names by SHA256 of the UTF-8 string `981308001:<arm>`. No new serving interface, concurrent weight switching or parent ownership changes. The private binding validator gains one exact lineage branch; the native collector is unchanged.

Primary comparisons remain success-SFT8 versus baseline, and additionally RL7 versus baseline; success-SFT8 versus RL7 is a paired exploratory package comparison. Report gains/losses by task and context cluster/stratum, not72 independent observations. Optimization/data/token budgets differ, so none is a controlled objective-only or equal-compute effect. Existing exposed-task caveats remain.

Expanded caps:1200s training process;900s each native collection;4380s shared work;4500s inclusive owned execution with cleanup reserve;4530s parent envelope. Startup and verification consume the shared budget. No cap resets or retries, and only actually unfinished/unrun coordinates are null at a deadline. All source/data/test identities and all72 coordinates are frozen before READY. MAIN retains acceptance and launch authority.
