# CPU preparation summary

The approved design and separate three-arm amendment are implemented without accepted-source changes. The new external sidecar contains six small production files: study.py for exact token/candidate identities, prepare.py for cached native reconstruction/source freezing, train.py for the eight full-corpus weighted updates/checkpoints, binding.py for the three exact root lineages, readout.py for private composition of the qualified native collector and launch.py for its serial owned lifecycle.

The real teacher-data reconstruction completed in21.83s CPU: all27 selected episodes/114 root turns match the original qualified export exactly. Maximum sequence2639, actual targets15,256, no injected terminator/crop, no source-group overlap between candidate320 and readout512 source groups. All fixed24 fresh task prompts match their existing full native token prefixes. Bounded seed scan found no conflicting981308xxx source/spec entries in its declared namespaces; it is not a claim of exhaustive historical seed uniqueness.

Test-first evidence is retained in tool history:3 input/mask/candidate tests,3 loss/tiny-optimizer tests and2 binding/clock tests initially failed because their production modules were absent, then passed. Final rerun output and source identities are frozen in CPU_TESTS/READY. The actual tiny CPU test exercises Adam state, checkpoint hash/cursor validation, RNG restoration and duplicate-write rejection. No actual4B model was loaded for qualification.

Native/typed/provider execution code is inherited, not re-rendered for training. The existing exact native checker is run against every selected teacher and compared to its prior export; old likelihoods are corroborated but not used in the SFT objective. No evaluation raw outcome or later adaptive training row is part of this export. RL7 is read only for the separately approved exact fixed-arm identity. The original RL STOP and all prior successful-trajectory screening artifacts remain intact.

Known limitations are preserved: the successful traces contain copied initial examples and recovery; this is a larger selected-success package, not efficient-plan distillation or an isolated content effect. Fresh readout seeds do not make the exposed tasks scientifically unopened. The phase order is RL7 → success-SFT8 → unchanged baseline. No live behavior reproducibility or determinism guarantee is made. CPU READY is not MAIN acceptance or launch authority.
