# Complete-success trajectory SFT — approved design

MAIN explicitly approved CPU implementation on2026-09-09 after reading the sealed training-only feasibility report and method. This document records that approval, not GPU/launch acceptance. All work is in this new external, non-Git sidecar; accepted predecessors and live outputs remain immutable.

Question: does a fixed package of complete, evidence-supported successful/recovery trajectories improve the interface-SFT root beyond its unchanged final4 checkpoint? This is not a content-only or compute-matched effect.

Data are exactly the27 confirmed candidates from fixed adaptive-RLVR training rounds01–05, identified by CANDIDATES.json SHA7586a082fba7259287bb3433d95d6026636ac3f97136a5226a015ce72d2de5c3 and SEAL SHAbc2529d41a582cce062994853f1b79bd35b4df6527162425570f3f3fc3577770. They contain114 physical root turns and15,256 actual action tokens. Keep every root turn, including recovery and the initial taught helper; deduplicate none. There are no identical whole action sequences, but one repeated code sequence. The27 traces cover7 contexts/8 context-query pairs (10global/17single-user), with answers0:1,1:4,2:3,3:4,4:6,6:5,9:1,14:3. Two provisional successes and all evaluation/later-round data are excluded.

Reconstruct teacher sequences from the qualified native physical graph and corroborating wire/role/typed records before export. Preserve the exact current action suffix and all historical prompt tokens, without retokenization, cropping, appended stop tokens, grammar repair or invented likelihoods. Only current depth0 action labels receive loss; all prompt/child/tool/observation tokens are masked. All114 physical actions presently include their recorded151645 stop token; this is observed evidence, not permission to append it. Prior real logprobs may remain in provenance but are not used by SFT. Fixture likelihoods cannot supply data.

Start from exact interface-SFT final4 efab2913e7fe9f5f9b381ae6eb67bb56071070f86b237e6145f98654816aad64 with fresh AdamW, never its old optimizer. Eight FULL-CORPUS updates, LR2e-5, weight_decay0, AdamW defaults(beta0.9/0.999,eps1e-8), clip1, unchanged rank8/FP32 LoRA and BF16 base/SDPA/dropout0/gradient-checkpointing settings. Each update gives equal weight to episodes, then equal weight to root turns within that episode, then mean physical action-token CE. Thus turn coefficient is1/(27 × episode_root_turns × turn_action_tokens). Record actual floating coefficients/masses, weighted CE and separate token NLL. Every update consumes all27 episodes:216 episode exposures,912 root turns,122,048 target tokens overall. Finite nonzero gradient/delta required; no synthetic success or zero-gradient completion.

Checkpoint after every optimizer update with adapter/Adam/RNG, full-pass cursor, data/source identity and cumulative elapsed training time. Resume only the exact contiguous committed chain at complete update boundaries; no double update, silent partial-pass continuation or validation selection. Fixed final8 is the only treatment readout. An interrupted partial gradient pass is discarded; preserved completed checkpoints remain evidence.

Readout: unchanged efab baseline versus fixed final8, fixedc32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3 typed child, identical qualified local runtime and task prompts. Existing24 source-disjoint root readout coordinates (8validation/8query/8length), fresh paired seeds; these tasks are exposed to this research and helper training, not new independent test data. No training candidate comes from them. Master981308001 determines a single hash-fixed phase order; trainingseed981308002; evaluation seeds981308101–108/201–208/301–308, subject to the bounded namespace check recorded during preparation. Sampling and episode budgets unchanged (.5/full support,2048 per call,4workers).

Primary: paired strict exact-answer gains/losses and differences within the24 coordinates, with context clusters and stratum breakdowns. Failures, completed empty/bad-format answers and infrastructure/unrun nulls remain distinct. Secondary: helper uptake, visible recovery, root/child physical calls, token/cache costs and wall time; no inferred semantic plan-quality improvement from answer agreement alone. Service/phase order remains a disclosed nuisance.

Caps:1200s training process including load/verification/checkpoint work;3180s shared work from owned execution start;3300s inclusive with cleanup reserve;3330s parent envelope. Training and both phase services are serial, assigned/accepted/launched only by MAIN under its shared lock. No preparation-time GPU/model calls, service starts, environment changes or installs. Reuse qualified lifecycle and release before independent CPU analysis.
