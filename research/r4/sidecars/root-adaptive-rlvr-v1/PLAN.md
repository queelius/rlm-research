# Bounded CPU implementation plan

Only this new sidecar is writable. No GPU/model/real-optimizer action is authorized during preparation. Parent review and a separate MAIN starting-weight binding precede launch.

1. Pin root-interface-sft-v1 READY/data/API sources and the exact inherited campaign loss/checkpoint/role-export/lifecycle helpers. Freeze all 184 coordinates, task identities, public versus host-gold boundaries, descriptive gold skew and fresh-seed audit. Read no SFT readout outcomes.
2. Write focused failing tests for allocation, start-binding/reset, binary/null outcomes and exact typed-native root export. Implement the smallest study/common/native adapters to pass them; no exporter path admits authored qualification traces.
3. Adapt sixteen-attempt group authentication to a private copy of the verified trainer module while leaving its loss, FP32 load, optimizer restoration, checkpoint and correction-capture checks intact. Add tiny CPU persistence/stale/double-step checks if the inherited test seam remains directly reusable.
4. Add the serial owned-service coordinator and explicit bind/verify/run commands. Bind starting checkpoint only through MAIN's later decision; a prepared state must truthfully say WAITING_START_BINDING. Start/end readouts use the frozen same coordinates. Fixed final8 selection is mechanical.
5. Run focused CPU tests and one exact native graph fixture/replay; retain failures and hashes. Publish source/data/runtime closure and PREPARED last. Publish launchable READY only after MAIN's authentic starting-weight decision is available and all runtime compatibility seams are actually qualified. No broad test suite or speculative installation.
