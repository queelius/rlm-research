# Schema × map-contract clarity:96 native trajectories

Approved by MAIN on2026-09-09. CPU preparation only; MAIN owns model/GPU/lock/queue launch. This is a fresh paired prompt-factorial on the same four16-record contexts and eight context/query blocks, not an independent task-generalization sample. No training, adapter update, generated-code host execution, new acquisition, oracle input, answer repair, retry or helper.

F/schema changes exactly two phrases: `id, synthetic user metadata, and original question text`→`id, user, and text fields`; `using their public user metadata in records.json`→`using the user field in records.json`.

M/map_contract is explicitly a WORDING PACKAGE: scopes the initial no-label claim to records.json/context.txt, replaces the old supplied-map/provenance description with explicit child predictions rather than authoritative labels, and changes permission wording from supplied map/ordinary Python to labels.json/ordinary Python/additional child calls. It is not a pure consistency or contradiction-removal effect; explicit additional-child permission, naming, provenance, salience and length change together. Actual child capability remains enabled in every arm.

F0M0 must be byte-identical to the original native-c32/Python/no-helper prompt in root-supplied-map-reducer-v1. F1M1 must be byte-identical to the current example-present/file-only prompt in root-example-map-visibility-v1. Both anchors are checked across all8 blocks and recorded in PROMPT_ANCHORS. Crossed arms change only their declared phrases. The optional first-four-record API example is always present; maps are file-only, never inline. No count_labels helper is supplied. No padding is added.

Use fixed actual RL4 adapter2286be3f7c0c9cc0e22c8ef8e3473b7d8eb6ec4b7a789ca380a0af9d3b944c71 and c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3. Authenticate actual model directories/configs/descriptor/cards, not stale inherited campaign_policy metadata. All actual role/typed captures and native branch tokens are retained. Inference only: no optimizer/checkpoint or loss-mask mutation; historical synthetic acquisition wrapper likelihoods are not sampled policy/RL data.

Reuse the four actual completed maps sealed by MAPS_READY SHAe9774cabd25640a747436f4f8e1174c3a72ca536693e5e242b3ef926d8f98632. Preserve exact original labels.json serialization and all errors. Same bytes in every treatment/repeat. Root remains free to read records/maps, use ordinary Python or make children. All inputs/source/acquisition captures are pinned; no gold enters prompt construction.

MASTER981347001 and24 fresh seeds981347201–981347224: three predeclared paired seeds per eight blocks, each shared over four cells. No collision in sidecars/ideas excluding all outputs/qualification directories before source creation. Hash-ordered24 pairs rotate cells; each cell occurs six times at each nominal position. Four workers follow the immutable launch queue; completion order is runtime-dependent. Three repeats improve conditional sampling precision but leave only four context clusters. Report all24 paired differences and aggregate within block/context; do not treat96 endpoints as96 independent tasks.

Primary: strict dataset correctness on authentic final native root text, whole stripped ASCII Answer: N, physical final-branch token alignment, stop/length and no pending tool calls. Format and availability are separate; unrun/error/tool endpoints remain NULL. Report correct/planned and correct/available, all four cells, paired repeat/block contrasts and interaction. Separately report strict supplied-map consistency, actual map loading and requested-user scoped reduction from raw code/tool evidence, scalar observations and extra child calls. Filename/scalar markers alone are not proof of state use. Inspect generated code as text/AST only on the host.

Every hypothetical endpoint pipeline is charged its entire relevant historical c32 acquisition plus all new root/child physical calls; historical acquisition is not newly paid and is not amortized across96 endpoints. Report new physical totals separately. Synthetic authored wrapper requests/tokens are transport accounting only and remain separate. Raw wire usage is authoritative; unknowns remain unknown.

Qualified visibility owner/lifecycle/runtime are reused with work1650s (includes service readiness at most180s), owned1770s, MAIN outer1800s and120s cleanup. Four workers, each episode at most180s clipped to shared work deadline. Credential preflight precedes output creation; exact unused outputs/attempt-001 only; child collector writes outputs/attempt-001/rollout. No mismatch between owner and collector paths. Finally releases only owned service. MAIN privately supplies credential/GPU/lock; preparation never calls model/service/GPU.

READY pins sources, inputs, rendered prompts/native task hashes, old/new anchors and inherited accepted closure; OWNER_READY pins lifecycle/credential and scientific READY. Focused tests cover96-row pairing/order, exact endpoints and crossed factors, actual native rendering/labels-only injection, deadline and cleanup, and real owner→collector expired-cap96-NULL accounting with external boundaries faked. Outcomes require independent raw audit after MAIN terminal/release; no blind root-incapacity or training-extension claim follows from this factorial.
