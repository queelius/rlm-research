# Approved implementation plan

New isolated external namespace; no accepted files, environments or image are changed.
Authority: ideas/2026-09-09-checksum-bridge-main-approval.md and checksum amendment
9542fb49bade8ce2566723b38c20c356de1ee9800cb4dea0c060ca5c61d0e63a.

1. Test exact public contract/depth matching, consistent child prompts and ordered schemas,
   strict broker projection (no semantic repair), and original-result immutability.
2. Install a source-pinned supervisor overlay only inside new owned runtimes; retain actual
   native child messages, tokens, log probabilities, graphs and root observations.
3. Qualify actual native root→child→broker return→subsequent root with synthetic provider
   responses. Those probabilities are fixture evidence only, never training data.
4. Freeze 32 coordinates, four named contexts, two global tasks, two fresh seeds and
   balanced paired representation order. Same root prompt/API/weights within each pair.
5. Test real entrypoint imports, pair dispatch/cancellation and all-32 terminal accounting;
   publish source/input closure, fixed weight identities, caps, exact argv and READY last.

Fixed low-SFT8 root66cce + childc32; master981326001/seeds981326011/021.
Caps: 1500 collection, 1650 shared work, 1770 inclusive owned, 1800 outer.
No new retries, forced plans, gold repair, root grammar or GPU calls in preparation.
Inherited transient native retries remain possible within the same total cap.
