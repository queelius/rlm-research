# CPU_READY is not launch authority

MAIN assigns one otherwise free GPU and credentials through the qualified owned service.
No environment or image mutation and no silent fallback. The local runtime image/store
is ephemeral: validate existing CPU_READY/store identity at launch; absence is a stop.

Exact proposed argv (external outer cap1800s):

```
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-child-representation-bridge-v1/driver.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-child-representation-bridge-v1/outputs/attempt-001
```

The flat driver imports time explicitly, authenticates READY, uses unchanged owned
start/release seams, and clips collection to shared work deadline. Final collector
STATUS waits for cancellation finalizers before counting rows. No episode reroll.
TERMINAL planned32 is also emitted on recorded scientific failure, preserving nulls.

After owned release, the optional implementer projection is:

```
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-child-representation-bridge-v1/results.py --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-child-representation-bridge-v1/outputs/attempt-001 --destination /project/alex_phd/runs/rlm-research-r4/analyses/root-child-representation-bridge-live-2026-09-09/implementer
```

This is not independent validation. Independently reconstruct primary endpoint, strict
maps and native costs from raw rows/episodes and role/typed wire captures; inspect final
root-branch evidence for arithmetic-fidelity claims. Runtime logs are root-writable.
Preserve every failure and all32 denominator slots even if service/collection stops.

CPU evidence: qualification001 failed before provider/runtime startup because the old
query package lacked a global task key. Preserved PREFLIGHT_FAILURE.json. Qualification002
uses corrected frozen-template construction: actual six native CPU-provider requests,
2root+1child each arm,46.715s; initial and subsequent physical root request objects equal
for the same predicted labels. Actual array/map child schemas/prompts differ consistently;
raw child graph and native output preserved; broker maps equal. Fixture likelihood is
synthetic, not model evidence or training data. No GPU calls in preparation.
