# Frozen question and interpretation

96 = six exposed64-record contexts × two count questions × one task-specific
paired seed × two prescribed roots × four arms. The original root is857a7ce6…,
the historical step8 root473210b1…, childc32de129… throughout. Current independent
seed/broad16 outcomes never select this model pair. Seeds981274301–981274312 are
candidate inputs until prepare.py's bounded prior-manifest collision audit passes.

unchanged U retains original bytes. taught_raw D gives honest public schema plus
the full six-label helper example, without the familiar procedure paragraph.
restored_raw R differs from D by inserting exactly that paragraph. restored_receipt
V differs from R only in an interface-availability sentence and the minimal
receipt extraction/access block. All exact prompts, code examples and96 typed
model contexts/request metadata are in inputs; no gold count or labels enter
runtime setup. Offered examples are executable, not injected actions or fabricated
successful outcomes. Original numeric scoring, broker/native schemas, action masks,
sampling, no-retry handling and strict map parser remain unchanged.

Williams quadruplets are balanced within each root. OriginalA and originalB split
tasks by alternating context/category parity; step8 covers all12 in between.
This counterbalances which root appears first across matched tasks, but does not
remove every root-by-period interaction. Within-root R−D helper uptake is primary.
It tests paragraph restoration under common teaching, not retrospective causation
of receipt72's failure. V−R is receipt availability+minimal documentation, not a
pure parser change. Three new prompt lengths for the first human task are777/890/898
tokens versus unchanged566; realized compute is not claimed equal.

Keep code intent/source inspection, actual helper-bearing child requests, valid
requested-ID maps, receipt-access claims, extension beyond the first four records,
and observed mapping aggregation separate. matching_child_model_requests counts
matching physical model requests, while corroborated_helper_request_claims counts
root-writable helper_request events with native matches. Multi-turn matches stay
ambiguous at API-dispatch level. Boolean episode uptake can be established by
one or more matches without pretending each matching turn is another API call.
The helper request is checked as embedded text in a rendered native prompt, not
as equality to the whole model prompt. Uncorroborated helper-signature calls stay
unknown rather than manufacturing non-use. root_calls/child_calls in exporter
proofs denote verified turns; all physical attempts are reported separately.

Thresholds, cost criteria, exposure limitations and the SFT/retirement decision
table are in the approved design. No decision or training follows automatically.
Complete empty policy replies score zero under the original contract but remain
explicitly unanswered; execution failures and unrun/censored outcomes remain null.
No semantic validation or outcome-conditioned user-only causal comparison.
