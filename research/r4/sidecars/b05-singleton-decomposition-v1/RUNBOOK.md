# Fixed singleton decomposition screen — MAIN launch only

Question: can released base4B apply the same local eligibility rule more accurately when each physical call contains one candidate, rather than producing a whole list/vector? This is fixed decomposition and model-package competency, not learned depth or an RL gain.

Current evidence: vector48 lost 9 semantic-exact comparisons despite every losing array being full-valid; three separate width20 arrays had length19. Short output alone did not improve completeness. Keep both fresh whole controls. Do not compare valid-only confusion tables with different denominators.

Six new stock roots at generation seeds202609420000..5: widths6/6/12/12/20/20, stages0/1/2/0/1/2, history1/check revision1. Public input and full request schedule freeze before host gold. No candidate filtering: each original normalized effective record stays intact, with unchanged policy/domains, in its own scalar request. Normalization resolves public mechanical state, never eligibility. No host-selected plans or dependency claims.

Two repeats, root seeds202609430000..11; singleton seed=rootseed+1000+public candidate index. Some integer seeds coincide across different prompts by this approved formula; request/call identities remain distinct. Cyclic arm order depends only on case+repeat; four stage workers, all176 calls planned regardless of scores. Scalar returns exactly `{"eligible":true}` or false. No output IDs, repair, majority vote, or automatic retries.

152 singleton calls +12 list +12 vector =176 physical calls, producing12 fixed stage/seed units per arm (six independent context units). Same released Qwen3-4B-Instruct-2507, no adapter, T=.5, qualified eager/no-prefix-cache/batch-invariant native runtime. Scalar16 and whole384 output caps; at width20 the aggregate scalar output cap320 remains below384. Nevertheless repeated input/policy and152 calls mean this is not compute-matched. Scalar literal+EOS fit is label-blind feasibility, not a guarantee the model obeys within16.

Primary semantic complete eligible-set exact on all12 tasks/arm. List ordering-only violations are separate from unordered known-unique-ID semantics. Full vector validates width/literal booleans. Every scalar is needed for a complete union: missing/unavailable never means false, malformed-known never becomes an empty-set answer. Both unknown and invalid-known candidate counts survive even when a stage has both. Available partial scalar decisions have separate coverage/confusions, not full-stage credit. Matched-valid per-candidate/BA/precision/recall/F1/Jaccard and constant policies accompany exact; no valid-only denominator sleight of hand. Invalid-known is an observed task failure, not an unavailable model call.

Actual request bytes, native response bytes, canonical response digest, chosen token IDs/logprobs, finish, usage, starts, errors and prefixes checkpoint per call. Owner retains final real dispatch receipt and exact owned-process release. Inherited `response_sha256` is the canonical parsed JSON digest; raw file hash can independently be recomputed from the preserved response path. No raw response substitution or unknown cost as zero. Summed request durations are not concurrent elapsed time.

Caps: science900 / owner1000 / external1100 seconds. Owner includes startup and90-second cleanup reserve, so startup can reduce the effective science allowance. No continuation or data replacement on a partial run. MAIN's outer process must use the shared GPU flock and external timeout1100. CPU preparation never starts a service.

CPU verification:

```sh
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/b05-singleton-decomposition-v1/owner.py verify
```

MAIN-only command, inside its owned GPU lease and private credential scope:

```sh
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/b05-singleton-decomposition-v1/owner.py run --outer-seconds 1000
```

Fixed output: `outputs/attempt-001`; refuse existing output. Check `CPU_READY.json` closure before admission. No token outputs are used to choose cases or suppress an arm. Promote only as exploratory fixed-scope competency if complete semantic accuracy improves across multiple contexts without a truth-class tradeoff; measure natural extra cost. A later fresh varied-rollout RL screen requires separate approval. No optimizer or automatic follow-on.
