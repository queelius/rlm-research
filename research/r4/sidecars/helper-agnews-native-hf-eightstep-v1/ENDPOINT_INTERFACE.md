# Fixed fresh512 endpoint — no evaluation admitted by CPU readiness

Primary endpoint is exactly checkpoint8, never a selected intermediate checkpoint. The existing AG256 has already informed exploratory work; the separate `helper-agnews-broader-data-v1/inputs/HELDOUT_PUBLIC.json` contains fresh512 and remains model-unqueried until the finally predeclared endpoint. Native evaluator implementation can proceed independently during a subsequently approved training run; this sidecar does not issue inference calls on fresh512.

Trainer command under MAIN's shared GPU lock/external timeout5200:

`/project/alex_phd/envs/prime-rl-5990b1b/bin/python owner.py run --owner-seconds 5000 --admission-json ADMISSION.json`

Run from this sidecar or use absolute paths. `ADMISSION.json` is intentionally absent until MAIN decides. Its required fields are: `authority: MAIN`, `decision: APPROVE_FIXED_EIGHTSTEP_DOSE`, `training_ready_sha256`, `endpoint: fixed-step8-versus-c32-on-fresh512`, `start: fresh-original-c32-not-one-step-continuation`, a nonempty `reason`, `heldout512_consulted: false`, and nonempty `reviewed_evidence_sha256` mapping exact pilot/replica report paths to hashes. Admission rechecks the first AG pilot's complete existing one-step qualification and actual optimizer state. A positive first step is not auto-admitted.

Output: `outputs/attempt-001/step-001` … `step-008`, each with service/raw actions, collection, masks, qualification/replay/capture/optimizer-intent/completion receipts. Checkpoint s is `step-NNN/checkpoint-NNNN/`, containing adapter/config, full `optimizer.pt`, `rng_state.pt`, `OPTIMIZER_LAYOUT.json`, `state.json`, `EVAL_BINDING.json`, `STEP_COMMIT.json`. Parent receipt is `parent-inputs/step-NNN.json`. `state.parent_policy` exactly equals the previously authenticated parent record, beginning at original c32. Every commit pins its parent plus the actual native binding/raw requests/responses/masks/gates and current optimizer step.

Full endpoint eligibility: `owner.py endpoint` calls `core.verify()` and `core.parent_for(9)`, authenticates all eight committed links/complete stage owners, and requires `FINAL_RESULT.json` status `UPDATED_STEP8`, `primary_endpoint_eligible: true`. An interrupted or failed stage cannot be silently retried. `--resume-after N` is permitted only when all previous steps are committed and next step's directory is absent; it restores full Adam/RNG from checkpoint N. There is no automatic partial-action resampling or gate relaxation.

Final native request source: broader data `inputs/HELDOUT_REQUESTS.json`, exactly128 B4 requests/512 IDs per arm. Reuse unchanged text, token IDs, labels, schema order, seeds202609126000–202609126127 and temperature0, with a fresh same-runtime c32 baseline and checkpoint8 child binding. Restore the saved `schema_ordered_json` into `body.sampling_params.structured_outputs.json` before sending. The fixed alias stays `strict-rlm-qwen3-4b-role-sft-selected-v1`; root weights stay unchanged.

The previous AG256 evaluator hardcodes64 calls and is **not** a valid512 collector without a new explicit128/512 accounting wrapper. Any independent evaluator must authenticate the final adapter/config/commit and all eight optimizer counters (8), require actual batch-invariant EngineCore marker and clean release, save raw request/response envelopes if practical, and account for unavailable IDs separately. Primary comparisons are paired correctness/wins/losses/per-class confusion on fixed512, with training cost separate from two evaluation arms. No intermediate or best-checkpoint panels.

Measured current AG one-step owner257.1377s and HF76.3524s imply roughly35–50min for eight native/HF reload cycles with larger prompts/optimizer IO; caps are600s/step,5000s owner/5200s external. Native350s and HF180s leave about2× observed component headroom. This is an estimate, not measured multi-step throughput. Training-side effects remain an exploratory data+dose package, not an established gain or comparison against SFT.
