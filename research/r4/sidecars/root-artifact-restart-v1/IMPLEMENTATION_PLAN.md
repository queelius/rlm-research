# Artifact restart48 implementation plan

> Execute inline with executing-plans and focused TDD. MAIN owns GPU and acceptance; no subagents or launch.

Goal:48 all-fresh native endpoints with truthful Q/A/M user prompts and equal authorized pre-correction files. Architecture: a pure export/protocol module, native task adapter, fixed-input collector and existing qualified lifecycle owner. Stack: pinned prime-rl native environment, low66c/c32 LoRA service, existing an27 lifecycle.

Spec: ideas/2026-09-09-state-continuation-feasibility.md SHA4fb4e4fd24e1f5b37527659bc53ca2185f5a031d0c90b72e90e6fa6230e26d56, approved by MAIN.

Global constraints: new sidecar only; all16 source states;48 planned endpoints; no gold, corrective/final future or private child history in packages; common query.txt distinct from arm prompt;2400s inclusive cap; no GPU/service/lock calls in CPU preparation.

- [ ] Export/protocol: test a branched graph with private child and future sentinels; implement ancestry cut and strict complete map-piece extraction; test output package equality across Q/A/M and deterministic paired scheduling. Public API: `cut(trace, alias, producer_count, expected_prefix)`, `package(context, source_id, messages)`, `prompt(goal, files, arm)`, `plan(states)`.
- [ ] Freeze16 real sources: verify actual pre-cut graph tokens/rendered prefixes, map observations and40 physical child records. Write only sanitized artifacts into mountable inputs; store source provenance and scoring truth separately. Pin old source closure and actual tokenizer/model bytes. Produce48 native prompts and native task hashes.
- [ ] Native task/collector: test `data.prompt` differs while all setup bytes including query.txt remain equal; subclass current native task setup for common files. Reuse genuine role/typed/native captures and strict final metrics. Preserve every planned row as NULL before execution, raw per-call and per-episode checkpoints, and actual source acquisition cost separately. Do not create a model-facing historical graph or replay producers.
- [ ] Owner: test actual owner-generated argv through collector parser and CLI help; use exact new attempt, private credential preflight and qualified lifecycle start/release. Shared deadline owner-start+2100 work,2280 owned,2400 outer; collection at most1800 and startup at most300. No retries/cleanup beyond owned stage.
- [ ] Final: run focused CPU tests, freeze READY closure, verify owner entrypoint in a fresh subprocess, write O/artifact-restart-implementation.md and send MAIN exact launch argv/hashes. No acceptance/GPU launch here.
