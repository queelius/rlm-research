# Frozen restart48 protocol

All-fresh-root state-representation/restart pilot, NOT native-history continuation. MAIN approved the feasibility memo; only this new sidecar and its implementation report are owned here. No GPU/service/lock/acceptance action during preparation.

16 source states are exactly all rows of corrective CONTROLLED_PLAN:4 previously research-exposed contexts ×2 query compositions (`u02`; union `u00,u02`) ×2 widths (16 and4). Eight `categories` width16, eight overwritten `labels` width4; no metadata-error states. Authored producer histories and actual c32 child observations are exported only before the source corrective turn. Corrective code, count, final, dataset gold, child-private graph branches, old kernel objects/logs and sampled readout outcomes are excluded from every package. Dataset truth remains scoring-only outside runtime setup. Original uninterrupted controlled outcomes are descriptive references only.

Each source has three fresh native sessions: Q quotes its content-preserving JSON historical root-visible messages; A references common artifacts; M adds inline public metadata. Every arm can read the same archived history, actual map pieces and metadata. The original accurate-field goal is common. Arm-specific `data.prompt` is distinct from common `plain_query`/query.txt. Common setup writes and byte-attests records.json, context.txt, query.txt, batch_contract.py and every state file. No producer replay, old variables, Python state import, model-facing historical graph or provider-token substitution. Q is quoted data in the actual NEW user message.

Source ancestry/token checks retain the native three-token assistant generation header from the next graph node, never its completion. All16 source prefixes re-render exactly. Every exported map is matched to the actual paid child's returned native tokens, retains wrong predictions and covers exactly the source record IDs. Readout selection never uses labels, gold, teacher count or later policy success.

Unchanged low66c root SHA66cce4009628ff7d8e047f08e6fb3a29febdbf8fb3ed8303871f69860f1151d5 and fixed c32 SHA c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3, reusing the accepted source binding/service lifecycle. Fresh master981359001;16 seeds981359101–116 shared by each triple. Hash-ranked source ordering and cyclic arm positions give6/5/5 positions per arm. T0.5/top_p1.0,2048 per-call native cap,8192 context cap,180s per-endpoint wall cap,4 workers. Actual fresh prompt lengths: Q2474–3077, A1206–1232, M1503–1581; none truncated.

2400s outer envelope measured from owner entry: work≤2100s (startup≤300, collection≤1800), owned deadline2280,180 cleanup and120 outer margin. Private credential preflight before output/process creation; MAIN owns one GPU and lock. Exact new outputs/attempt-001 only; no overwrite/retry. Per-call typed/role captures, actual graph episodes, setup attestations and per-row checkpoints persist. All48 planned NULLs are written before service launch; missing endpoints never become zero. Strict whole-strip Answer:N scores actual authenticated native finals; returned malformed/capped finals are observed0 unless strict-correct. Genuine missing/unreturned/unverified final remains NULL.

Primary measures: strict dataset accuracy, supplied-state consistency, actual scoped reduction/retrieval and reclassification costs. The collector retains programs and actual tool observations but leaves actual data-flow flags unscored until trace audit; AST/file mentions/scalar agreement alone are not reuse. Tool timing, raw wire requests/usage and per-endpoint elapsed remain available for paid retrieval accounting. The inherited task's placeholder answer0 is NOT this study's primary score; explicit scoring-only host labels determine the retained row reward after native final verification.

Costs:40 historical physical acquisitions,37340 input/2430 output/34528 cached,28.243s summed child time, shared and not newly paid. Each hypothetical endpoint charged its full source acquisition; across48 that is120 historical calls/112020 input/7290 output/103584 cached, plus all new root/child work. Gross source-capture wall280.985s includes future authored correction/final transport and teardown and is labeled gross, not minimal acquisition. Pre-cut observed time is separately bounded from first producer-provider arrival to corrective-provider arrival (excluding setup). CPU export cost recorded in EXPORT_COST; runtime setup and file verification timing recorded per endpoint. Unknown usage is never invented as zero.

Native setup qualification uses the real task hierarchy with a filesystem-only runtime double, no containers. Tests cover source privacy/cut, omitted-root parent and generation header, wrong-label/coverage retention, pairing/prompts, actual owner→collector argv and CLI help, missing NULLs, exact namespace, byte-equal setup across both widths×3 arms,16 source and48 fresh native prompt rerenders. Upstream SWIG deprecation warnings are retained. Readiness is CPU qualification, not a claim this new runtime task has already executed on GPU.
