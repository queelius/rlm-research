# Recovery V2 nonempty transport qualification

V1 repaired the collector import surface but was not sufficient for queue admission because its run
fixture used an empty capture slice. A real prepared free row then exposed the task-hook keyword defect
described in `FAILURE_CAUSE.md`. V2 fixes that recovery-local wrapper, seals the successful nonempty
`env.run_slot` qualification, and supersedes V1. Scientific inputs, conditions, seeds, root bindings,
budgets, and outcomes are unchanged. No model or GPU call was made.

