# Procedural-card headroom48

MAIN approved CPU implementation from ideas/2026-09-10-recuris-rlmopt-next-levers.md SHA ac6cbd47eb530bf6dadfc17693315fcb636e636d71deb39e5ee63d4fb893057e and YAML52e8011864fcb8e47a3c0a1248d68dbee582a45bce67b90098265af7a73c3670. Exact card below is submitted for narrow MAIN content review before input freeze. No GPU launch authority.

Question: can fixed24 execute these compositions when their general procedures are stated, or does native execution still fail under an explicit algorithm scaffold? The independent prior composition audit already establishes operator substitution; this is not contingent on selecting favorable task-spec72 outcomes.

All eight exposed composition-transfer contexts ×three original composed queries ×two arms=48. Control U: original unmodified free-task prompt and all original files. Treatment P: append exactly two newline bytes and the one identical CARD.txt below to that prompt, with no other task file or helper. The card contains ALL THREE procedures in every treatment task; no task-specific retrieval or tuning. Its complete native-token length must be≤250 before freezing; if not, return to MAIN for a content amendment, not silently truncate.

## Exact shared card

```text
General procedures for the three composed questions:
For a count of users whose qualifying-weight sum exceeds a threshold, first total the weights of category A records separately for each user. Then count users whose total is strictly greater than the threshold; do not threshold the pooled total.
For a maximum per-user qualifying-weight sum, first make those per-user category A totals, then take their maximum. A user with no category A records has total zero.
For category B weight among users who also have a category A record, first form the set of users with at least one category A record. Then sum the weights of category B records owned by those users, counting each B record once, not once per A record.
Use the question's categories and scope, and the records' user and weight fields. Category labels remain predictions, not verified truth. These procedures supply no labels or answer and prescribe no batching width or child-call count.
```

## Fixed comparison

Exact step24 root94022838… and c32 child; original coding role/native optional tools and exact-ID child transport; file-only record/context/query/helper bytes identical in both arms and to ancestor. No supplied map, source labels, examples, executable reduction helper, forced calls or width. The card deliberately teaches algorithms and adds prompt tokens: this is an instruction-plus-context package, not mere parameter access, search, learned composition or unseen transfer. Native root final remains strict Answer: N, no extraction/repair. Gold stays scorer-only and card serialization never accesses it.

All24 original blocks retained, including four realized zero and20 nonzero answers, no balancing/filtering. Eight context clusters, not48 independent trials. Same paired fresh seed per block, prospectively frozen collision inventory, balanced12/12 arm-first dispatch. Original metadata may carry historical root-new fields; current exposure is completed composition evaluation, child-training and catalog exposure. No primitive tasks: cannot claim non-regression on primitive skills. A future policy/search promotion needs protected primitive anchors and separate unopened groups.

Primary strict native dataset correctness/all48 planned; no-final or missing RESULT NULL, bounded operational failure0 separately. Report per-family/context paired outcomes, native availability/bounds, zero/nonzero baseline and physical attempts/completions/unknown usage. Mechanism requires actual requested operator, category and scope in executed programs and observations; host map-answer agreement or code mentions alone do not establish execution. Distinguish genuine child-label use, faithful child-semantic failures, wrong-operation coincidence, literal recovery and unknown. No sampled-code reexecution.

OneA100;2048 root action/8192 context,temperature0.5/top-p1,4workers/180s episode. Outer2700=work2550+cleanup120+margin30; owned2670. Startup≤180,collection≤2340,harvest≤30, release≤120 with inclusive deadlines, early advance. Qualified owner releases before final harvest; all collection ends before work deadline and release/harvest finish within owned cap. No retry or phase extension, all48 preplanned before service. Expected15–25min, not guaranteed. All physical requests including failed/missing-RESULT paths retained; rejected attempts are not established GPU completions, usage and billing unknown when unreported.

Exploratory decision: ≥4 additional faithful composed executions per24 treatment, spanning≥3 contexts, positive planned strict difference and no availability loss motivates a procedurally scaffolded headroom claim only. Coincidental/literal improvements retire the card. Continued execution failures despite explicit procedures prioritize execution-grounded training/recovery over broad text search. No in-run prompt update or best-card selection.
