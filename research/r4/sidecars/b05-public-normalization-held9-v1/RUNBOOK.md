# B05 public mechanical normalization before eligibility selection

MAIN only launches the exact CPU_READY argv, `owner.py run --outer-seconds 700`; science600s, owner700s, external800s. Released Qwen3-4B-Instruct-2507 base, no adapter, same proven B05/FinQA V3 native service and post-release dispatch qualification; T=.5,384 output,8192 context, four stage workers.36 fresh calls: nine frozen selection-RL held stages×two seeds×raw/normalized. Widths6/12/20 each have three stages. Same paired seed slots202609330000..17, unchanged IDs-only output contract. The raw arm is newly generated, not a reused old score.

Raw prompts/requests exactly equal the frozen HELD_PUBLIC/EVAL_TASKS entries. A pure function reads the public local shard and emits every implementation in original order with formats, effective cost/latency/capacity/quality, updated features, and greatest-revision required-check booleans/revisions or null if missing. Applied rows only; feature updates are union of ALL additions then removal of ALL removals, not chronological updates. The original policy/domains/stage IDs remain. It never compares numeric fields against policy bounds, combines check outcomes, returns eligibility labels, filters IDs, calls a reference solver or reads host labels. Even conspicuously out-of-policy candidates remain. Host labels are copied only after both public arms are frozen, and used only in released scoring.

Normalized instructions state that mechanical joins are done and that the model must apply the original policy/checks itself; raw instructions stay exact. Thus representation and explanatory wording jointly change, not a pure byte-format intervention. Source token cost differs and is reported; each answer still costs one natural model call. This is deterministic harness preprocessing, not learned delegation, eligibility offloading or a recursive-method claim. It reveals facts required for eligibility but does not decide eligibility.

Primary: unordered known unique-ID-set exact; report BA (mean recalls over present positive/negative classes), precision and recall on explicit valid-ID-set denominator. Invalid known model output remains a failure for exactness and separate for BA; unknowns are not wrong/zero-cost. Strict sorted formatting is separately reported. Nine stage-context units,18 matched seed pairs; no18-IID-context claim. Preserve every arm/repeat and all held exposure caveats.

Read normalize.py first; then prepare.py/study.py, collector/owner/metrics, and tests. Two focused tests cover draft/applied deltas, union-minus-removals, greatest check revision/missing checks, out-of-policy ID retention, exact raw/new frozen native HTTP requests and decoded canonical-response inventory, unsorted-but-semantic-valid IDs, partial2-known/34-unknown accounting and current owner binding. Trusted owner-source compilation is unchanged infrastructure, not model-code execution. No shared/live source edited. No generation or GPU work during prep.
