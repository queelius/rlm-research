# QSR contract × predicted-evidence diagnostic24

Status: approved bounded CPU preparation; MAIN owns acceptance and launch. No campaign changes or warm-start authorization. This experiment was selected after early QSR sparse rewards and prior interface/copying failures; later QSR window3 committed Adam1 and window4 has mixed rewards. It does not diagnose those later weights.

## Question and immutable boundary

Does initial low66c fail because the native coding role does not fit a task-solving final contract, because it fails to acquire usable category evidence, or because it cannot apply the requested operator even when complete predicted evidence is visible? Use exactly the first two QSR training contexts (`training-00`, `training-01`), their three original supported operator questions, and unchanged source records, IDs, weights, gold and child c32. These are exposed training contexts, not fresh evaluation; six blocks nested in only two context clusters. No training, checkpoint choice, new worlds or claim of independent replication.

Pin QSR GROUPS/PUBLIC/PROVENANCE/PLANS and source, initial adapter SHA `66cce4009628ff7d8e047f08e6fb3a29febdbf8fb3ed8303871f69860f1151d5`, child SHA `c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3`, released base manifest and complete qualified runtime chain. Root records retain exact `id,user,text,weight`, including original `q`+hex IDs. Private gold is never included in prompts, files, acquisition requests or output grammar.

## Common query clarification and four arms

For every all-scope question in ALL arms replace only `Among records belonging to all four users,` with `Across all records, regardless of which of u0, u1, u2, or u3 owns the record,`. Preserve the original suffix, operator, target, gold and all non-all-scope query bytes. Store original and amended question and exact diff. This is a new common matched baseline, not a direct comparison with historical QSR's ambiguous wording.

Factor R is actual root system role: original native coding system versus exact compact system `Solve the user's task. Tools are optional. When finished, return only Answer: N with a nonnegative integer.` Root only; child roles/routing unchanged. Factor E is raw public task versus same task plus the complete, structurally valid, genuinely sampled c32 predicted ID→category map inline. State explicitly that these are child predictions, not dataset truth. Raw public records/files, query and optional native Python/child tools are otherwise identical. No mandatory decomposition, counting helper, algorithm, copied answer, demonstration or constrained decoder. Existing optional request/strict-map helpers remain exactly the QSR contract; neither aggregates nor repairs. The map arm tests an evidence-visibility package, not oracle evidence. Native role editing is a process-owned experimental harness override, not an installed/public RLM API change.

Six context/query blocks × four arms × one fresh paired seed =24 roots. Namespace `qsr-contract-evidence-20260909-v1`, frozen seeds independent of QSR. Counterbalance the four arm positions cyclically over blocks and freeze dispatch order; four workers. Same seed within a block does not make token paths independent. Report paired/context-group contrasts descriptively, not 24 independent clusters.

Root finals and the two separate source acquisitions are free/unconstrained. Recognized optional native child batch requests retain the unchanged QSR exact-ID grammar hook, common across all four arms and authenticated in typed traces. No child transport capability is added or removed.

## Two genuine source acquisitions

Exactly one fixed final-only free-JSON classifier request per context to c32, using the pinned existing `request_for` contract and public IDs/text only. No tools, gold, semantic oracle, exact-output grammar, repairs or acquisition retries. Preserve actual requests, response bodies, token IDs, usage, finish reason and errors. Require exact requested key set, duplicate-free JSON object and canonical category strings, but never gate on dataset accuracy. Semantically wrong labels remain byte-for-byte in map evidence. Complete maps are immutable and shared across the three questions and both roles. Structurally unavailable sources make their six map-conditioned root slots NULL; raw roots still execute. Acquisition failures remain in full pipeline inventory/cost and may make the main factorial uninformative.

## Native execution, outcomes and costs

Authentic ACP/IPython native root trajectories, optional child calls, genuine terminal capture; no one-shot substitute. Authenticate actual first root system/user/tools and rendered token IDs against frozen expectations; child model routing remains c32. Actual native runtime receives exact records and query bytes. The host only scores; never supplies answers. Primary strict dataset-native-final correctness remains unchanged: exact `Answer: N` contract, original gold, no extraction or repair. Separately report native-final availability, strict correctness among available, planned-denominator operational success (nonreturn unsuccessful), NULL bounds, valid-but-wrong and numeric prose diagnostics. Map-consistency is a separate secondary deterministic calculation over actual acquired predictions with the same query operator/scope. Report observed program execution/output, scoped aggregation and stopping, not merely mentioned code, successful map load or tool-call count. Do not execute generated code in the CPU audit.

All24 slots and2 acquisitions exist before any model request. Retain source-gated, unstarted, timeout, policy looping/context exhaustion, rejected request and infrastructure causes distinctly. Count physical request attempts versus returned completions separately; missing usage and provider billing remain unknown. New physical accounting includes two acquisitions plus all native root/child attempts; per-hypothetical-pipeline accounting adds the shared source acquisition once to each map pipeline while explicitly separating reuse from new physical calls. A transport attempt is not proof of GPU generation; pretransport validation rejection is recorded separately, never as a sampled completion.

Actual root action cap is2048 tokens and context8192; native sampling retains temperature0.5/top_p1.0/top_k−1/min_p0, with the action cap explicitly set and verified at the outgoing transport. The two final-only c32 acquisitions use1536output tokens, temperature0.5/top_p0.95, no grammar/tools, one request each and shared150s acquisition cap. They are a separately pinned acquisition contract, not sampled native child invocations.

## Budget and decision

1200s outer,1080s work,1170s owned,120s/episode,4 workers; fixed24 roots and2 sources, no rerolls or timeout extensions. Qualified an27 private-credential/lifecycle owner, strict attempt-path validation,90s cleanup reserve and30s outer margin. Source acquisition consumes the same inclusive work deadline. No GPU/service/lock/queue changes during preparation.

Evidence-role gains with map-consistent execution motivate harness/contract adjustment; raw failure with map success motivates acquisition/reachable-state training; failure even on structurally complete mostly correct maps with wrong operators motivates diverse full-trajectory SFT, not another copied terminal target. Wording is common and therefore cannot be causally isolated here. Sparse evidence or two-cluster mixed outcomes trigger smaller diagnosis, not a capability-ceiling claim. Warm-start remains a separate unapproved design.
