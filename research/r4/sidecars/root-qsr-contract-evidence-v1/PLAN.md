# QSR contract × evidence implementation plan

> For agentic workers: use superpowers:executing-plans inline. MAIN forbids delegation, GPU/service/lock/queue actions and historical source changes.

**Goal:** Prepare executable, provenance-pinned diagnostic24 under DESIGN.md.

**Architecture:** Reuse qualified QSR native task and an27 two-adapter lifecycle; add a process-owned root-system override and a narrow diagnostic collector. Two final-only c32 acquisitions precede balanced24 native roots. Independent host scoring never enters model inputs.

**Tech stack:** Existing pinned Python runtime, aiohttp native proxy, ACP/IPython, deterministic JSON manifests, pytest authored fake-provider fixtures.

**Spec:** [DESIGN.md](DESIGN.md).

Execution note: CPU preparation used the isolated external sidecar, not an installed-source edit or repository branch. Qualification-native-001 proved the inherited2048 action cap; qualification-native-002 proves the explicit2048 cap and attempted-versus-returned accounting. CPU_TESTS.json is a preserved rejected combined test run: its delayed generic test imports selected QSR's owner. The production collector and owner ledger now load exact local paths, and test entrypoints mirror that qualification. Final status is determined only by CPU_TESTS_FINAL_V2.json and READY.json, not this note.

## Global constraints

Only this new external sidecar and its preparation report may change. Preserve historical inputs and seals. Exact low66c/c32; first2 training contexts/3questions; common all-scope clarification;24roots/2sources;1200outer/1080work/1170owned/120episode/4workers,90cleanup+30outermargin. Root finals and two acquisitions have no grammar; recognized optional children retain exact QSR typed grammar. No gold repair, new training, reroll or generated-code execution by the auditor. Count physical model work; billing is unknown.

### 1. Frozen data/protocol

- [ ] Create `study.py`, `protocol.py`, `prepare.py`, `test_protocol.py`.
- [ ] Red test: `assert len(protocol.plan()) == 24`; all arms share query/gold/source IDs and paired seed; all-scope original/amended diff only; non-all query exact.
- [ ] Implement `public_contexts()`, `queries()`, `plan()`, `amend_query(query)`, `strict_map(raw, ids)`, `score(final, gold)` by authenticated narrow reuse. Write PUBLIC/GOLD/PLAN/PROVENANCE using exclusive-create manifests; preserve original source records and acquisition requests.
- [ ] Test duplicate/missing/extra/unknown map labels fail structurally, wrong canonical labels are preserved, gold absent from public prompt/files, and namespace/source/checkpoint mismatch fails closed.

### 2. Native role and collection

- [ ] Create `native.py`, `collect.py`, `test_native.py`, `test_collect.py`.
- [ ] Red test actual authored native first request equals frozen system/user/tools under both roles; child role is not changed.
- [ ] Implement process-local bootstrap/ACP wrapper using pinned engine source. Both arms keep optional tools and same public files; compact root system only. Implement two c32 source requests and all24 preplanned result slots. Derive map-conditioned prompt only from actual structurally complete raw source response; leave failed slots NULL.
- [ ] Preserve raw provider requests/responses and native role/final traces; score strict native terminal and secondary map-consistent target separately. Operational ledger distinguishes rejected requests, sampled completions and unknown usage.
- [ ] Run authored native fixture through real rootless ACP/IPython stack: first request, optional executed Python, child routing and final authentication. No actual model calls or sampled code execution.

### 3. Qualified owner and seal

- [ ] Create `owner.py`, `test_entry.py`, `test_lifecycle.py`, `RUNBOOK.md`.
- [ ] Red test composed actual owner→qualified an27 service-wrapper entry under intercepted launch/ports and exact runtime/private-credential seam, output attempt mismatch rejection, and allNULL inventory before service.
- [ ] Implement1080work/1170owned with strict phase deadline,4workers/120episodes and final cleanup. Signal/deadline never reruns or silently advances after shutdown; owner records complete attempted cost even interrupted collector.
- [ ] Run focused protocol/composition/native tests only and preserve CPU qualification artifact. Verify source/input/model pins after JSON write/read, then exclusive-create READY with exact executable CLI and source identity. MAIN reviews and accepts; no launch by this agent.
