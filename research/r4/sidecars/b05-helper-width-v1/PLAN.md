Goal: determine whether partitioning a larger public B05 stage into two/four complete candidate-local shards reduces omitted eligible IDs or invalid claims, at a fixed total384 output-token budget.

The approved immutable candidate design is `../../ideas/b05-helper-width-feasibility-2026-09-12/DESIGN.md`. No old/live inputs or source are modified. New files are study.py (frozen nine-case schedule and source bindings), interface.py (existing strict scoped-ID validator), collect.py (existing native-wire collector with14 calls/case), metrics.py (union and fixed-denominator metrics), owner.py (existing qualified V3 lifecycle with126-call accounting), prepare.py, test_width.py, RUNBOOK.md and receipts. No new model/runtime/framework.

1. Implement the thin adapters, run two focused CPU fixtures, authenticate full original rows/requests/budgets, and seal READY_RUN plus source inventory. No GPU, optimizer or model query.
2. MAIN reviews READY and decides admission. If admitted, all126 fixed calls execute under600 science/700 owner/800 external caps and automatic owned release. No result-selected continuation, input replacement or threshold change.
