# CPU preparation plan

1. Freeze the named-inventory exclusion scan and mechanically select 16 new
   contexts from the pinned MultiNLI validation-matched parquet.
2. Freeze 192 exact bodies, prompt token IDs, schemas, fresh paired seeds and
   rotated 12-arm dispatch.
3. Reuse the qualified positional scorer/native collector, but bind the new
   context source, 192-slot owner, exact attempt path, and owned wrapper.
4. Exercise label-mutation selection invariance, all-cell inventory, grammar,
   nonempty transport, and actual registered service-entry composition on CPU.
