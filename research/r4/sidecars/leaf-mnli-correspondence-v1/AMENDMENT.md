# Prelaunch review amendment

MAIN identified an ambiguous sentence after the first CPU prefix proof: “not their order” could deny directional premise→hypothesis entailment. The final prompt instead says “Do not infer the relation label from a record’s position in the batch or from its ID.” Directional definitions remain unchanged. No selected texts, IDs, labels, ordering, seeds, control tag or decoder constraints change.

The initial REQUESTS/PROMPT_IDS/ORDERED_REQUESTS/CPU_NATIVE/SEED_AUDIT JSON files are retained as superseded CPU preparation artifacts. Only the corresponding `_V2.json` files are executable inputs. No actual model outcomes were obtained under either version.

SELECTION_QUALIFICATION adds deterministic per-row arbitrary canonical-label reassignment, stronger than the original global cyclic relabeling receipt, and reports the exact raw premise variants within every selected normalized premise group. This does not select replacements or balance gold labels.

Dispatch uses eight pre-outcome shuffled permutations and their reversed second-seed orders, with shuffled context/seed blocks. Every context/seed has all four cells; this is paired/reverse-order balance, not a claim of exact Latin-square frequency at every dispatch position. Four asynchronous workers can still create completion-order differences.
