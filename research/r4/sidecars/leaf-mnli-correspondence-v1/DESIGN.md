# MNLI free correspondence80 — approved CPU implementation

MAIN approved the single experiment in `ideas/2026-09-09-free-correspondence-nli-design.md` (SHA afc5fb343ac844880460b0bcd8652e011e00c17961a16ca80b7a6e7cb41fe364), with the constant-token matching and explicit admission refinements below. CPU implementation only; MAIN owns acceptance, GPU, services, locks and queue. No launch or replacement of accepted successors is authorized here.

Question: does matching source-tag output help classify relational records when tags are generated freely, rather than inserted by exact-ID grammar? This is one task-transfer screen, not a new indexing algorithm, attention mechanism, general NLI capability claim or whole-RLM benefit.

## Fixed source, population and policy

Use the cached `nyu-mll/multi_nli` revision da70db2af9d09693783c3320c4249840212ee221, validation_matched parquet SHA350c26950b55f460b50d36c76aef87d64b49c78812d7abf7bf97e5fede10f186. Preserve original acquisition/license and23:00 source receipt. Renew the bounded named-input exclusion scan before selection. Source pool:9,815 unique pairs,3,334 premises,3,151 exact-three premise groups; proposed government/slate/telephone/travel genres supply2,513. No arbitrary-history, near-duplicate or pretraining nonexposure claim. Nonfiction OANC source terms and attribution apply; not blanket MIT.

Eight contexts: two per named genre, each16 normalized premises × all three distinct hypotheses =48 pairs. Select128 premises/384 pairs by a fixed label-blind hash after exact text exclusions; keep all original majority labels, including repeated classes within triples. Exact-three grouping is not one-of-each-gold selection. No premise crosses contexts. Interleave each premise's three text-hash-ordered pairs into three independently permuted16-row blocks. Keep complete texts, not parses or source metadata. Public IDs are m+12hex derived solely from normalized premise/hypothesis text; collision is an explicit preparation failure. Original pairID/promptID/label/genre never enters a model-visible record. PairID suffixes can leak author-intended classes and must remain host-only. Gold-label mutation leaves selection, public IDs/order, prompts and schemas unchanged.

Released Qwen3-4B-Instruct-2507 revision cdbee75f17c01a7cc42f958dc650907174af0554, no adapter. Reuse qualified final-only classifier system, no advertised/executable tools, no reasoning field or explanation, thinking disabled, temperature0.5/top_p1/top_k−1/min_p0, penalties1/0/0,3072 output allowance,8192 context, BF16, four concurrent sequences, prefix cache disabled. No training, root actions, acquisition, retries, repair or output salvage.

## Exact contrasts

Two object outputs, tag then label: matching requires the displayed ID at each position; constant requires one fixed absent control tag. Cross with free decoding versus identical shape/cardinality/canonical-label/generic-tag grammar. Generic tags match m[0-9a-f]{12}; the grammar never supplies literal IDs or enforces source membership, uniqueness or ID order. Both arms must generate their tag values. All48 objects are required, labels are entailment/neutral/contradiction. Free/structured bodies differ only by structured_outputs. Directional NLI definitions, source records/order and sampling remain identical across each comparison; only the matching/constant instruction differs between representations.

Control-tag refinement: encode the serialized one-field object {"tag":ID} for every selected public ID with the actual tokenizer. Target the modal field-token length (tie nearest median, then lower length). Generate a fixed256 label-blind hash-derived absent candidate tags and choose the earliest candidate minimizing distance to that target. No per-source-ID rejection sampling or model-performance selection. Freeze the candidate receipt, chosen tag, each source field length, control length, full histograms and residual total mismatch. Measure actual model output tokens separately; no equal-FLOP claim.

Two fresh paired seeds per context, balanced four-cell order gives64 primary calls. Add16 fixed matching/free singleton references: lowest text-hash pair from each context under both corresponding seeds. They diagnose task difficulty, not select/rewrite batch outcomes. All80 coordinates fixed before launch; no adaptive refill.

## Admission, metrics and costs

Availability requires an authentic unique returned assistant branch, exact expected native prompt tokens, completion token/text or parsed-tool-envelope identity, model and usage consistency. Missing, failed or unverified endpoints areNULL with explicit bounds. Authenticated malformed/wrong-route completions are observed strict0; no tool execution. Finish=length is flagged, not automaticallyNULL; a complete contract-valid capped answer is scored.

PRIMARY strict positional correct-label count requires exactly48 canonical items with correct fields/order AND matching IDs at their displayed positions, or the exact constant tag everywhere. Any full-contract failure scores0 strict labels. Singleton contract is identical with cardinality1. Separately report full shape/canonical-vocabulary validity (without requiring correct tag values), positional semantic correct count conditional on that shape, per-position ID/control-tag fidelity, missing/extra/duplicate IDs, field-order validity, whole-batch validity and all-labels-correct. Shape-only predictions are diagnostics and never replace primary or reorder by IDs. Partially parsed arrays are not salvaged.

Report matching−constant separately by decoder, eight context means with two-seed variation and genre descriptives; repeated hypotheses share a premise and are not independent treatment trials. Keep singleton references separate. All planned rows survive before startup and after failures. Preserve each physical request, ordered body bytes, response/error, exact native tokens and usage, caches, finish, elapsed time. Unknown usage remains unknown; billing is unknown, not equated with physical attempts. No new child acquisition costs. Host selection/tokenization costs recorded separately.

## Clock, checkpoints and decisions

2700 outer =2610 owned +90 outer margin;2610 owned =2490 work +120 cleanup. Work includes startup≤180 and reserves60 for final collection; four workers,90-second requests. Every call is checkpointed; owner preplans all80 NULLs before service startup. No automatic retry/resume. Unfinished calls remainNULL rather than silently reduce denominators. CPU native prompt+3072 must fit8192 before freezing; no text crop or outcome-based replacement.

Practical success is a matching semantic/ID advantage on free outcomes across most context means, surviving structural support that never forces IDs. A structure-only advantage establishes contract support, not freely reliable correspondence. Flat batching with useful singleton accuracy marks a transfer boundary; uniformly weak performance leaves NLI competence unresolved. A positive does not justify another exposed ID grid or displace source-sufficient, actually consumed RLM evidence work. Main approval and focused exact-entry/native/parser/data qualification precede READY; no broad suite or new framework.
