# Canonical source loader48

Approved by MAIN2026-09-10. CPU preparation only; MAIN owns acceptance and launch. This is an optional source-access interface ablation, not a reduction helper, automatic repair, training experiment or public API change.

Question: with raw records and the same fallible semantic predictions already available as files, does an optional conventional Python loader improve faithful use/recovery of source evidence? Semantic-map48 had20 faithful reductions, nine dataset-wrong because predictions were wrong; file-only increased reads but also shell errors and fabricated maps. Fresh-order32 independently reported file-only uptake but failures from fabricated replacement state, reset sets and order-sensitive joins. Those adaptive observations motivate this experiment; exposed inputs and familiar tasks are not fresh confirmation. We authored this successor and cannot independently audit it.

## Exact manipulation

Whole eight semantic training00–07 contexts ×three supported queries ×FILE/LOADER =48 new roots,24 paired fresh seeds. No failure-only sampling. All four common data files have identical bytes in each pair: records.json, context.txt, query.txt and labels.json. Neither arm displays raw records or maps inline. FILE preserves the previous semantic FILE prompt byte-for-byte. LOADER appends only a truthful description of one additional treatment-only module, source_state.py. This module and description are a declared interface package, not a pure prompt-only or persistence intervention.

The conventional interface is `import source_state`, then optional `source_state.load()`. Return value is an ordinary Python dict with exactly two keys: `records` (a list of ordinary dicts, original id/user/text/weight fields in original order) and `predictions` (an ordinary dict mapping exact public record IDs to the actual acquired canonical-category strings). Every call decodes the same private original records/map JSON strings into fresh independent objects. Edits to returned records, labels, names or intermediate objects do not change the source returned by a subsequent call. No auto-call, restoration hook, retries, error swallowing, variable repair, custom read-only container or hidden namespace reset. No query, scope, filter, join, category choice, aggregate, gold, answer or validation against correctness in this module. It is conventional Python, not a security boundary against deliberately modifying module/function internals or source code.

Proposed exact treatment addition: “Optional Python source access: source_state.py provides source_state.load(). It returns a fresh ordinary dict with exactly 'records' (the original list of id/user/text/weight record dicts) and 'predictions' (the original record-ID-to-predicted-category dict). Each call decodes the same canonical source snapshots; editing returned objects does not change later loads. It does not interpret query.txt, classify, select, aggregate or answer. Importing or calling it is optional.” No algorithm or example solution is supplied. Existing supplied-map helper32 taught an explicit reducer; this loader only returns source objects and leaves all computation and stopping to the root.

## Sources, models, scoring

Freeze the exact eight completed semantic acquisitions and raw maps, including all13 wrong labels. Authenticate their saved request/token branches prospectively with the qualified source verifier and require structural/native validity only; no new acquisition, semantic gating, replacements or gold repair. Record original source paths/hashes/timestamps/requests/responses plus reused lineage in new inputs/output receipts. Data are research/QSR/operator-SFT/child-training-exposed. The inherited catalog-new source_split metadata is corrected in this new task to an explicit exposed-input description, common to both arms; no historical replication claim about metadata. Query/gold/public fields stay exact.

Fixed857 zero-effect released-weight reference adapter/c32 child, same qualified two-LoRA routing, current native coding role, optional native Python/typed-child behavior unchanged, root action2048/context8192, temperature0.5/top_p1, four workers,120s episodes. Fresh namespace canonical-source-loader-20260910-v1, paired seeds981631101–981631124. Balanced alternating arm-first order across24 blocks; no seed reuse from prior conditions or best checkpoint selection.

Primary strict whole authenticated native final dataset correctness, same Answer: N contract. Malformed available finals0; absent/unverified finalsNULL, with available accuracy, planned operational success and bounds. Authentic action-syntax/no-final failures are operational failures, not exogenous random missingness. Separately report map-consistent final, actual file/module/source use, correctly executed scoped reduction, abandonment/fabrication after loading, actual recovery and costs. Code mentions/AST alone are insufficient; inspect executed code plus following observations without reexecuting sampled programs. Eight context clusters,24 paired blocks; no independent-record claims. Source errors can make faithful reduction dataset-wrong.

## Budget and qualification

1800 outer =1650 work +120 owned cleanup +30 margin; owned1770. Work reserves startup≤180 +root collection≤1440 +closure30. Every deadline is inclusive and bounded by remaining reservations; stages advance early. Service release uses at most120 within owned time; no hidden collection extension. All48 NULL slots preexist before service. No rerolls/retries; any missing coordinate remains planned NULL.

New physical ledger counts only actual root/optional-child attempts and returned completions. Reused eight source calls retain their historical physical cost once in a separate ledger; hypothetical per-endpoint full pipeline charges its one historical source, not48 new acquisitions. Provider billing unknown. Failed native requests/unknown usage retained. Prior source/model training costs are provenance, not newly performed work.

Reuse source-pinned semantic/CE native capture and lifecycle via explicit isolated cl_* aliases, not installed-source edits. Focused tests cover exact source/shape/fresh-copy behavior, common files and treatment-only module/prompt, paired plans, actual owner→qualified wrapper→intercepted Popen, actual owner→collector CLI/no acquisition, and real CPU ACP/IPython optional-module-load→mutation→reload→native-final fixture. Authored fixtures never count as model work. READY only after those pass and immutable source/input closure. MAIN acceptance/launch remains required.
