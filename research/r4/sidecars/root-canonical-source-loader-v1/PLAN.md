# Canonical source loader48 Implementation Plan

> For agentic workers: execute inline with superpowers:executing-plans. MAIN prohibits delegation and owns GPU/service/queue operations.

Goal: runnable isolated source-access ablation with48 fixed native endpoints, no original modifications.

Architecture: cl_study pins qualified semantic dependencies; cl_protocol freezes old data/maps plus new paired coordinates and emits an optional source-only module; cl_native writes treatment module and common data; cl_collect replaces new acquisition with authenticated reuse; cl_owner retains proven one-service lifecycle with1800 inclusive envelope.

Tech stack: existing pinned prime-rl Python/native ACP/IPython/Qwen renderer; no environment/package changes.

Spec: DESIGN.md in this new external sidecar. Explicit external isolation overrides generic worktree/commit instructions; no repository changes or broad tests.

## Tasks

- [ ] Protocol/source accessor: first write test_protocol.py assertions for exact two-key return, fresh copies after edits, no query/gold dependency, raw map error retention and48 paired slots. Observe missing implementation failure. Implement cl_study.py and cl_protocol.py; pass focused tests. Interface: module_source(records_json: str,map_raw: str)->str; build()->input dict; reused_source(row)->original authenticated record.
- [ ] Native setup: first test common file bytes, treatment-only source_state.py, unchanged FILE prompt, optional loader description and actual2048 first request. Implement cl_native.task using qualified setup and explicit aliases, with no system/tool override. Prepare all actual-source prefixes via prepare.py into immutable inputs.
- [ ] Collector/lifecycle: first adapt focused entry fixtures to require no acquisition POST, all48 outputs on authored execution failure, correct historical/new cost split, exact owner paths and real service composition. Implement cl_collect.py using counted source adaptations and cl_owner.py with180/1440/30 reservations plus120 cleanup. Preserve each historical source receipt separately under reused-acquisitions.
- [ ] CPU native fixture: qualify_native.py runs two actual native ACP/IPython episodes against authored CPU responses. FILE loads common files conventionally; LOADER imports module, mutates a returned object, reloads and verifies original values, performs authored fixture-only reduction and authenticated strict final. Optional native child fixture remains qualified/common. Save completed request/response/traces/observations.
- [ ] Closure: run only focused tests, source/native rendering and actual optional loader fixture; retain failed qualification artifacts. Verify old pins unchanged, freeze READY with all source/input/test/native receipts, exact argv/caps/lineage. Save O/canonical-loader-implementation.md and send MAIN every new source/design/report path. No service/model/GPU launch.
