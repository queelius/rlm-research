# Root receipt ablation v1

Decision: operations/2026-09-09-continuous-allocation/RECEIPT_IMPLEMENTATION_DECISION.md,
plus RECEIPT_FORMAT_AMENDMENT.md: use free indexed ID→label JSON objects, not
array-of-tag/label records, following the already completed padding-control evidence.
72 episodes = 24 matched triples = six exposed64-record contexts × two target-count
questions × two fresh seeds × unchanged/indexed_raw/receipt. Six triple orders each
occur four times. Fixed historical root473210… and recovered childc32de; no training.

Only new runtime-local helper files are added in the two new arms. Unchanged arm
uses byte-identical historical prompt and only context.txt. New arms share a public
catalog and indexed prompt builder, with optional target-only vocabulary and no
mandatory decomposition. Receipt availability plus minimal documentation is the
primary intervention. The new arms remove the historical supplied procedure/example
equally; their contrast to unchanged is therefore a practical harness-package
comparison, not a pure indexing effect. Receipt versus indexed_raw is incremental.

Qualification-map-attempt-001: three actual pinned rootless/native episodes with deterministic CPU
fixture replies, nine fake provider calls and zero model/GPU calls. These fixture
token/logprob values are not behavior likelihood evidence. No live smoke required.
Qualification-attempt-001 is preserved superseded array-format CPU evidence (nine
additional fake calls, zero model/GPU calls), not the current qualification.

The inherited collector is reused through a hash-gated private AST loader; exactly
two queue chunk literals change2→3 so one full coordinate triple stays within each
worker. Existing native request capture, scoring, deadline, fail-stop and task
checkpoint persistence remain unchanged. All72 raw outcomes retained; no retries,
answer repair, label fallback, or conversion of infrastructure failures to negatives.

Caps: 2100s collection,2280s work including service readiness,2400s owned wholejob,
2430s external outercap plus ordinary owned cleanup grace. At least120s reserved
for owned cleanup. Eight concurrent coordinate triples, each internally serial.
Expected15–25min on one A100; forecasts are not guarantees. Stop on inherited >50%
execution failures after8 recorded episodes or deadline. Never replace censored rows.

READY.json contains exact argv/input/source closure and qualification hashes.
Main alone reviews, accepts, supplies the existing credential and exclusive
CUDA_VISIBLE_DEVICES, and launches after predecessors release their service.
No preparation command starts a model service. Never attach to another service.
Driver reuses original campaign.start_service/owned_command and lifecycleV2 cleanup.
CPU analysis is a separate `driver.py analyze --output ...` invocation after the
driver exits and releases GPU authority; no analysis work holds the service open.

Primary: strict exact final success receipt−indexed_raw. Secondary: both new arms
versus unchanged, with gains/losses/ties, observable denominators and scheduled
operational yield separate. Six context groups, not72 independent problems.
Record raw native attempts/call/token costs, syntax markers, helper uptake, visible
receipt errors and requested-subset coverage. Receipt access is not proof of
causal consumption. Root-writable receipt diagnostics need raw-native corroboration.
Invalid labels_by_id remains null. No aggregate six-label vector is required for
target-count correctness; no gold labels or aggregate answers enter helper files.

Preparation files freeze once. Failed runtime attempts remain as evidence; changes
require explicit amendment and fresh output namespace, never hidden retry.
