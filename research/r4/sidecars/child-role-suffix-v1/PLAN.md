# Narrow implementation and freeze plan

1. Authenticate pinned native collector, role overlay, nano engine, task builder, design and original/fixed-child weights. Keep all existing sources immutable.
2. Test before implementation: depth-gated real message construction; concurrent ContextVar isolation/reset; allowed versus prevented dispatch; retained wrong/empty/null outcomes. Capture expected RED, implement minimal private seams, then GREEN.
3. Qualify two concurrent real owned rootless episodes with deterministic operator-authored native-provider fixtures: root→child→child tool continuation→root. Verify every physical graph prefix, identical initial root IDs and only suffix changes in both child prefixes. No model/GPU calls.
4. Reuse frozen V2 owned-service lifecycle and the observed process-absence fix. Test release after collection failure and preservation of the original absolute clock. Do not write a scheduler.
5. Freeze four exact tasks, sixteen coordinates, sampling seeds893350289/62794825, original857a/c32de bindings and complete source identities. Preserve posthoc selection and scoped seed-collision caveats.
6. Repeat seven focused CPU tests, bind qualification artifacts and publish READY as the final preparation write. Parent reviews/accepts/launches; no source edits after publication.

Readout runs separately on CPU after service release, consuming per-episode and per-call raw checkpoints once. No training export or answer repair. All partial and unattached request evidence remains available.
