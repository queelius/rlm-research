# CPU preparation report

The approved sixteen-episode suffix-only pilot has a qualified pre-render insertion seam. No GPU/model calls, new environment, shared/frozen source edits, service launches or acceptance changes were performed. Operator-authored fixture code executed only in two new owned rootless containers, never generated code on the host.

## Evidence

Five initial focused tests covered source-hash rejection, actual message construction, ContextVar concurrency/reset, dispatch ceiling semantics and paired task/seed order. Subsequent focused checks cover owned cleanup and the parent's readout concern: a completed correct1 or wrong0 remains observable beside a later budget-censored/null episode. The pre-seal run passed all seven; seal repeats the exact set and records complete stdout/stderr/argv in FOCUSED_TESTS.json. RED was observed for missing runtime, plan and owned-envelope contracts; actual message construction also caught an indentation error before GREEN. No broad suite was run.

The actual native proof is [qualification-attempt-001/RESULT.json](../../../../ARTIFACTS.md#unpublished-files "Not published: qualification-attempt-001/RESULT.json"): two concurrently active arms, eight deterministic CPU provider-fixture calls, real TrainClient/rendering/graph and owned nano runtime. Root prompt lengths were1231→1294 in both arms. Child control prefixes590→635 and suffix637→682 show the intended47-token increment at initial and continuation calls. Every graph prefix was reconciled against captured native token IDs and fixture response IDs. Child physical prompt text after removing the exact suffix matched control, including tools/user/tool observation. Distinct private runtimes received correct arm overlays and the host ContextVar reset. Runtime/experiment/qualifier sources remained unchanged after that proof.

Synthetic fixture logprobs are explicitly not model measurements or training data. The proof does not claim the real child will obey the suffix, use tools, or follow identical trajectories. It establishes only the intervention/capture mechanism.

## Files and reuse

- `runtime.py`: count-checked private nano/native/collector transformations, host arm scope, final request-hook budget and conservative outcome states.
- `experiment.py`: exact inherited broad task construction and root wrapper, sixteen frozen coordinates, immutable descriptor binding, bounded collector supervision.
- `qualify.py`: actual two-arm native graph/rootless CPU proof.
- `owned.py`: one-service envelope using frozen suite/V2 ownership plus previously qualified process-absence observer; no scheduler.
- `results.py`: strict observable outcomes, per-invocation calls/tools/prefixes, wire/token/hash evidence, arm pairs, conflicts needing review and unattributed partial audits. It does not perform RLVR admission or repair outputs.
- `driver.py`, `prepare.py`: small collection/readout and immutable prepare/seal entrypoints; four focused test files plus design/plan/runbook.

Source closure reuses authenticated original native capture, role definitions, renderer/client code and qualified lifecycle. Original root857a7ce6 and c32de129 fixed child are authenticated from existing disk bindings. Candidate data derives verbatim from the prior broad PUBLIC/HOST_GOLD/provenance, and only context/question text reaches the runtime. Historical `training`/`validation` labels do not make these newly selected diagnostic tasks unseen. The seed audit is explicitly scoped to named plans, not every integer in the research store.

## Interpretation and operating boundaries

The1800s inclusive clock begins at main entry, including verification/import/startup; collection≤1500s and cleanup has120s reserved. Four pair workers, sixteen episodes,2048 allowed last-native-request-hook entries. These entries count transport attempts, not guaranteed provider receipt or successful inference. Prevented entries and request-only/canceled transports remain distinguishable. The inherited error-rate/time stops remain active, and native error/admission rules are not widened.

Recovered errors do not automatically erase an observable scored root answer. Conversely empty/malformed answers, infrastructure nulls, canceled partials and unrun rows are not conflated. If budget censoring differs by arm/order, report the imbalance rather than crediting censored controls as low-cost policy failures. The collector can discard incomplete graph traces on cancellation; independent raw dispatch/audit records are retained without inventing episode attribution.

The suffix may conflict with root-generated code or aggregate requests. Actual child user payloads are retained and flagged by a clearly named lexical/manual-review screen, not rewritten. A decrease in calls caused by suppressing useful answers is not success. This is a selected small component/harness-policy diagnostic, not a general efficiency or learning result.

READY is published only after the final source/input closure and focused checks succeed; its presence and hash are the readiness authority. Parent alone controls acceptance and launch behind existing work.
