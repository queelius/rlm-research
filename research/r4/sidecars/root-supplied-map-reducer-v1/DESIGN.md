# Supplied map and executable reducer diagnostic

MAIN's ruling for allocation5780 explicitly preserves the September9 approved
design: last bounded-RL4 root2286be, fixed c32de child, native c32 maps versus
privileged dataset maps, ordinary Python versus pure count_labels. The initial
brief's low66c/raw-context description is superseded. CPU preparation is independent
of complete-demonstration SFT outcomes; this design is fixed before diagnostic inference.

Use the exact four16-record contexts from complete-demonstration DATA_READY,
single-user u00 and union u00/u01 questions. One fresh coordinate seed per
context/query is shared across four treatments:32 root endpoints, eight paired
blocks and four source clusters. This is exploratory and not independent policy
training replication. Source groups are newly reserved relative to the nine
named catalogs, helper exposure persists, pretraining exposure is unknown,
and dataset licensing remains unresolved.

Acquire one full16-record native c32 map per context through an authored root
wrapper in the existing native runtime. Each wrapper requests one actual child,
prints its raw answer and submits a dummy terminal; wrapper likelihoods are
synthetic transport placeholders, never RL/SFT evidence. Freeze all four raw
episodes and map statuses. No reroll, gold-aware repair or acceptance filtering.
A malformed or unavailable acquisition leaves that context's native-map endpoints
NULL with an explicit acquisition reason; all planned oracle endpoints remain.
Acquisition costs are separate shared costs, with four-way and two-way amortized
views clearly named; they are not repeated as newly paid costs per root endpoint.

Every root has the original records.json/context.txt and a source-bound labels.json.
Oracle treatment is explicitly labeled as privileged frozen dataset labels. Only
helper arms receive count_labels.py with the documented pure function
count_labels(labels, requested_ids, target). It validates map labels, unique IDs,
and requested-ID coverage, then counts exact target labels. It has no gold,
metadata, query, filesystem access, automatic submission or label repair. The root
selects IDs from public user metadata and must submit its own strict final.
The helper is an API-plus-instruction treatment, not isolated arithmetic ability.
Existing optional child access remains available and any additional calls are
charged to that root endpoint. These are assisted evaluations, not normal end-to-end
capability. Labels stored in the REPL are not claimed to be model-visible until read.

Primary endpoint: exact Answer: integer and dataset correctness, with available
malformed/empty finals scored0. Native execution failures with no final, incomplete
and unrun endpoints remain NULL. Separately record format, complete supplied/scoped
coverage, final consistency with supplied-map reduction, actual integer tool
observations, parseable emitted Python, native child calls and full physical costs.
An integer tool observation equal to the host reduction is scalar agreement,
not by itself proof of correct scope or executed reduction. Arbitrary emitted
programs are retained for manual source/trace audit; never execute them on host.
The pure helper performs no logging or filesystem access. Literal helper-call and
bare-variable zip markers are descriptive code screens, not proof of executed
correct reduction or dictionary corruption; use actual code/observation audit.

Hash-order the four treatments independently within each paired block. Fixed seeds
and prompts are frozen in READY. Four work-conserving native workers after four
sequential acquisitions; no model runtime launched during CPU preparation.
MAIN owns service, runtime lifecycle and scheduling. Collector accepts absolute
shared work deadline, capped1200s; acquisitions capped120s each, individual root
episodes capped180s. Outer1350s, work1200s, cleanup120s. Persist each completed or
interrupted endpoint and all unrun planned coordinates. Preserve partial artifacts
on any failure. No training and no checkpoints beyond per-episode evidence.

Allocation5780 runtime binding reuses the qualified runtime-an27-5780-v1 private
store and wrapper through its pinned adapt_stack function. The collector performs
no model-service start or stop. MAIN retains its separate accepted service-driver
wrapper. No old cache is created, mutated, relocated or cleaned by preparation.
