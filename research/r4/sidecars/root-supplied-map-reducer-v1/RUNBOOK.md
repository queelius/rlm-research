# MAIN launch handoff

No model or container was launched by preparation. Use the pinned
inputs/BINDING.json with MAIN's accepted service_driver wrapper. Its root alias
retains the prior bridge name but the authenticated actual adapter is fixed last
bounded-RL4 SHA2286be, not low66c. Fixed child remains c32de. Check live model cards.

CPU verify:

    /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-supplied-map-reducer-v1/prepare.py verify

Collector invocation is in READY.collector_argv. Replace only endpoint and absolute
shared work deadline with the owned authenticated service descriptor and MAIN's
deadline. Use MAIN's outer1350s/work1200s/cleanup120s lifecycle. Collector clips
itself to1200s and never starts or stops the model. It reuses the already accepted
runtime-an27-5780-v1 stack adaptation and requires that qualified store/owner.
Do not create an old cache or extend the allocation cap to finish this diagnostic.

Expected outputs: INPUTS.json, PLANNED_NULL_ENDPOINTS.json, four acquisitions/
context directories with raw authored wrapper episodes, all physical requests,
native audits and MAP.json; MAPS_READY.json; readout/episodes, readout/rows and
native typed/role audits; TERMINAL.json and OUTPUT_INVENTORY.json. Every observed
row supersedes its prospective NULL placeholder. If a process terminates before
its terminal inventory, retain all partials and use the frozen32-coordinate plan
to report unavailable/unrun values as NULL. Do not relaunch missing coordinates
under the same attempt identity or replace native-map acquisition failures.

Report each of four cells as correct/available/planned8, and the eight four-way
paired blocks. Context is the cluster. Acquisition c32 semantic agreement is a
separate source-label comparison; disagreement is not independently adjudicated
semantic error. Record full actual acquisition cost separately and additional
root-run child calls per endpoint. Acquisition cost is shared across four native
map endpoints per context (two queries by two reducer conditions); per-question
two-way attribution is a separate view, never added to full costs.

Automatic fields include strict final accuracy, exact format, supplied-map
consistency, scoped supplied coverage, valid emitted Python, actual integer tool
observations, literal count_labels calls and bare-variable zip risk. The
actual_map_consistent_reduction field starts NULL pending manual inspection of
actual code plus native observations. A displayed matching scalar alone is not
proof. An AST zip marker alone does not prove the variable was a map. Never run
generated code on the host to resolve this audit. The diagnostic's pure helper
reads no gold/metadata/files and performs no auto-finalization.

Limitations: no new native container qualification for this sidecar; exact native
rendering, file-injection boundary and scoring are CPU-tested against the existing
qualified stack. First invocation may fail and is retained as exploratory evidence.
No host task-specific answer is in the normal prompt. Only oracle treatment
provides dataset labels; neither treatment is ordinary end-to-end performance.
