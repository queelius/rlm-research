# Original-weight scope72 contrast

Parent approved this additive model-only intervention after old-child scope72 and its unchanged replay. Question: is the observed requested-ID/local-input effect specific to the old trained child?

Use the exact original key-converted step0 adapter857a7ce6, authenticated config and immutable504-FP32-tensor conversion proof. Require an actual endpoint descriptor and live alias/root/base match. Never impersonate the old child. Preserve the frozen four inspected validation contexts/256 question groups, all72 request bodies except model, both seeds981261601/602, full-supportT0.5,1024output cap,4workers and20minute cap. This is an adaptive comparison on reused validation, not an untouched confirmatory test.

Implementation: additive wrapper privately substitutes disk/live weight validators and bind metadata while retaining frozen collector, scorer, source checks and physical token capture. New ATTEMPT records the actual original adapter. Bound spec records the intervention and hashes; old sources/outputs remain untouched.

Known CPU/live tool-key serialization mismatch is explicitly retained. The frozen CPU hash reflects raw tool-dict order; vLLM typed tool models reorder fields before rendering. No flag is flipped and no token IDs are changed. Reconcile raw physical sequences in analysis.

Plan: real-descriptor RED/GREEN plus wrong-weight/live-root rejection; publish READY last; parent alone binds and launches service/client. Then compare valid arrays, aligned correctness, requested-target diagnostics and all-call costs by the same original-position coordinates. No reward repair, retries or GPU calls during preparation.

Launch with the qualified native Python, CUDA_VISIBLE_DEVICES empty for this client:

    python driver_original_weight.py bind --endpoint-descriptor ACTUAL_ORIGINAL --spec-path NEW_BOUND.json
    python driver_original_weight.py run --spec-path NEW_BOUND.json --output-dir outputs/attempt-original-weight-001
