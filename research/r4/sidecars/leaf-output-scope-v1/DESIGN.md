# Accepted leaf output-scope experiment

Parent-approved bounded CPU preparation. No GPU/model/service calls are delegated.
Use the four exact64-record contexts from the sealed correspondence rotation spec:
256 unique but already inspected validation question groups, not untouched test.
Use only the old c32de selected child and two fresh parent-assigned seeds
981261601/981261602. No new training, selection, gold-derived input or majority vote.

All input records carry the same stable original record_index ID q0001–q0256.
All three arms share the instruction to classify only requested IDs in listed
order, the semantic definitions/vocabulary, native system/tools and provider
settings. Return an anonymous enum array of exactly the requested cardinality.

1. all64: all64 visible, request all64;8 calls.
2. full16: all64 visible, request one contiguous original16 block at offsets
   0/16/32/48;32 calls.
3. local16: only that same target16 visible, same requested IDs/instruction;32 calls.

The complete record block is byte-identical between all64/full16. The complete
instruction/definitions/requested-ID prefix is byte-identical between matching
full16/local16; only non-target input records are removed. Input order never
rotates. Output alignment uses only the declared target list, without inference,
repair or relabeling. Output-scope1→2 also changes selection burden and cost,
not just output length; it is not proof of an attention mechanism.2→3 isolates
extra visible records conditional on identical requested targets.

Reuse the exact qualified first-response proxy at /v1/chat/completions, not
TrainClient generation or a recursive rollout. Native system/tool contract and
physical prompt template are unchanged. Tools are advertised but never executed.
T=.5, full support,1024 actions/call, four workers,60s call timeout,1200s total
including live metadata checks, no retries. Local/native token IDs are retained;
no added logprob request or probability claim. Caller supplies an authenticated
old-child endpoint; bind is CPU-only, run checks live version/alias/root/base.

Score whole-array/cardinality/enum validity, aligned canonical correctness by
original-position quartile, and matched per-record contrasts. Target counts for
HUM and NUM are host-only: report16-record quartiles and reassembled64 contexts,
warning that cancelling classification errors can yield correct counts. Every
arm labels the same512 repeated assignments across two seeds; denominator units
remain four contexts/256 groups. Malformed arrays are unaligned, not silently
64 or16 wrong classifications. Infrastructure/unrun outcomes are null.

Report all observed logical/cached/uncached input and completion tokens plus
missing-field counts. full16 includes four repeated full-context prefixes—never
deduplicate cost because records match. Freeze every body, target map, seed,
source/input hash and CPU-rendered prompt hash before generation. Actual aliases
are bound additively and may change only body.model. No prior source/spec changes.
