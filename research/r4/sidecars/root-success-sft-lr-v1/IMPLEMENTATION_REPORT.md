# CPU preparation result

New experiment only; no GPU/model/service calls. Approved MAIN design implemented through pinned immutable complete-success source adapters. Changes are two LR literals, fresh study/coordinate/checkpoint bindings, two readout arms, shortened shared lifecycle caps, and one descriptive readout-provenance phrase. Exact training corpus bytes and all eight orders reused;24 full native-rendered prompt/token identities verified. Target exposures remain122048, masks unchanged, child never loaded by trainer.

Focused tests cover fresh24 seeds/crosswalk, actual underlying dispatch24 and cancellation writes, actual collector24-row terminal cardinality with fake environment, accepted high-LR versus rejected low-LR Adam resume metadata, and the actual owned execute function's two-phase48/shared-clock flow with only external process/service boundaries replaced. Five tests pass5.14s. Initial RED exposed missing adapter; first integrated run exposed generic `train`/`launch` test-import collisions from inherited modules. Explicit private test loads fixed test isolation; no harness/scientific behavior was changed for that issue. CPU_TESTS.json records the final fresh seal-time run. This is bounded CPU qualification, not live inference or numerical reproduction of the full model's eight-pass trajectory.

Native interface/image/capture and original collector remain unchanged. Full inherited source/input closure is retained, including the already qualified teacher provenance rather than another data-export pass, plus the low final8 checkpoint/state/Adam/RNG. READY is published last. MAIN alone accepts/launches; independent raw endpoint/training audit follows owned service release. Existing design explicitly motivates this by earlier exposed outcome evidence; no claim of new holdout or optimal LR.
