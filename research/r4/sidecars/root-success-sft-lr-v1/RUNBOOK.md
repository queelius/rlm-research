# Owned execution

MAIN alone launches after exact READY review. Native Python: `/project/alex_phd/envs/prime-rl-5990b1b/bin/python`. Run `launch.py verify` CPU-only, then `launch.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-success-sft-lr-v1/outputs/attempt-001` under3330s outer cap, one assigned empty GPU and inherited owned lifecycle. Existing endpoint credential environment is supplied by MAIN and never printed.

The launch trains high LR once (1200s cap), then serves each fixed8 according to frozen phase order, collecting24 rows each (900s each) and releasing each owned service in finally. Inclusive3300/work3180 share one initial clock. Original immutable sources, native renderer/typed grammar hooks/local image and raw capture are unchanged. No accepted-job edits or runtime rebuild. Completed malformed/empty is0; unavailable/unfinished/unrun NULL. All48 planned denominator persists even if training/first service fails. No automatic fallback to partial checkpoint, API repair, replacement episodes or study retry; inherited bounded transient native retries must be audited separately.

The tiny CPU lifecycle test substitutes process/service boundaries only: no GPU/model service is invoked. It exercises actual execute() phase count, command weights and shared deadlines; the actual underlying native dispatcher independently handles24 distinct rows and cancellation writes. Fresh native renderer confirms all24 prompt/token identities and task hashes. Teacher proof is reused by exact immutable corpus hash, not rerun. Source closure includes original proof and low checkpoint state/Adam/RNG; this is an exploratory paired recipe contrast on exposed tasks, not an independent test set or LR sweep.
