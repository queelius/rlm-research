# CPU preparation and run plan

1. Focused RED tests: paired source/order/masks, LR optimizer validation, complete lifecycle48 and collector24 cardinality/global bindings.
2. Authenticate old source and immutable EPISODES/AUDIT; create fresh24 coordinates preserving native prompt bytes/IDs and task-hash crosswalk. Bind exact low final8 and efab state.
3. Private trainer/readout/launch adapters only. Train once from efab with fresh Adam through fixed8, checkpoint every complete pass; no automatic resume/reroll.
4. CPU tests and native prompt qualification; READY last. MAIN alone accepts/launches.

3330s outer;3300 inclusive owned;3180 shared work;1200 training;900 each readout;180 service startup clipped by shared work. Cleanup consumes remaining owned envelope. No timer resets. All48 planned readouts remain denominators after any early failure. Training selection is fixed8 only; incomplete training does not silently substitute another checkpoint.
