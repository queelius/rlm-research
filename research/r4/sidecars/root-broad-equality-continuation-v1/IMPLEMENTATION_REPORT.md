# CPU-qualified continuation: saved round1, no new admission

The approved exact8192 child-context rejection branch is implemented in a private
copy of the original verifier. Only its regex and equality length predicate
differ; every child/alias/hash/request/sampling/unsampled-response check remains.
The original >8192 function still runs unchanged. Other failures remain fatal.
No raw outputs or historical sources were edited; no GPU/model/service actions.

Four focused CPU checks pass, including the three actual equality failures,
unchanged three overlong cases,13 rejection mutations, and real coordinator
dispatch to training1 without service/collection/validation0 replay. The initial
three desired tests failed before implementation, and actual fixtures continue
to assert the old rule rejects equality. One actual tiny PEFT test passes:
emptyAdam0→step1, exact checkpoint reload/no duplicate step1, persistentAdam→step2,
base unchanged and zero child/observation loss. Its one expected toy-config
warning is retained. CPU_QUALIFICATION.json records commands/stdout/stderr.

The actual prepared export reproduces all24 pre-amendment row fields exactly.
All three previously excluded episodes keep rewardnull and empty turns; zero
sampled graph nodes removed and zero recovered episodes admitted. The group has
15 mixed episodes (7 small/8 medium), not24; the six admitted all-wrong large
episodes remain nonmixed. Validation0 is authenticated unchanged, not sampled
again. EXPORT_PROOF.json preserves exclusion certificates and row identities.

The real training-interpreter --preflight passed, including its subprocess call
to the **new native verifier in the existing Prime interpreter**. That proof
replayed24/selected15 and matched exact GROUP/MANIFEST bytes, root857a,
emptyAdam, childc32de, source graph masks and original grouped advantages.
Native/coordinator imports are lazy so the training environment need not install
Verifiers; no environment was changed. This pre-freeze import correction is
covered by the successful actual preflight, not merely a mocked dispatch test.

At parent launch, new output references the immutable prepared export and old
validation0, copies only the small generation identity, and trains step1 first.
The original broad16 coordinator, optimizer, recipe, source plans, observer
absence fix and lifecycle remain in use. Narrow subprocess dispatch selects the
new native/training wrappers. Later rounds2–16, validations4/8/12/16 and fixed
final16 paired transfer remain unchanged. No implicit resume/retry is exposed.

Remaining work16829.460386276245s plus the1170.5396137237549s already consumed
equals the original18000s budget. Inclusive exception16949.460386276245s;
parent outer16980s plus120s signal-cleanup. Intervening elapsed gap is recorded
separately. Parent assigns the real MIG/LD/key environment and owns acceptance.
CPU prepare/verify never opens the campaign lease or reserves a GPU.

AMENDMENT SHA256 `d570e4d099b535ad7c7b287285cd34f9c22f4526470d91899bf7e3f4431772ab`.
GROUP SHA256 `a66d19e9ceb95bdd39dd92ae6a3a008ed2568304435d45a0060859ca12d93007`.
MANIFEST SHA256 `4cc4cb650787a20e5776e834b1221ed0c355469c22e06da1a261a6c710d7487a`.
The amendment pins1824 prior-stage JSON inputs; READY adds the complete source,
qualification and prepared-export closure. READY is the final publication write.

Exact launch argv is existing Prime Python plus this namespace's driver.py run
--output outputs/attempt-001, all absolute paths in READY. Parent acceptance is
required. The original STOP is preserved. This is an exclusion-only continuation,
not evidence that the excluded long/errorful trajectories are valid training.
