# Narrow equality-boundary continuation

Approved decision: ../../operations/2026-09-09-continuous-allocation/BROAD16_EQUALITY_CONTINUATION_DECISION.md.
Only the exact unsampled child8192-prompt rejection family is added; existing
overlong rule and all role/token/sampling checks remain. All three affected rows
stay excluded. Reuse24 existing raw rollouts and16 validation0 outcomes without
new sampling. Original857a/emptyAdam begins step1 on15 mixed episodes, then the
frozen rounds2–16, validation schedule and fixed-final16 transfer remain.

Capture actual RED/positive/negative CPU fixtures; prepare immutable new export
and row/graph identity proof; qualify inherited stages and tiny Adam0→1→2 with
checkpoint recovery/no duplicate step. Freeze source, oldSTOP, input/proof/export,
remaining budget and argv; publish READY last. No GPU/launch/environment changes.
