# Varied vector rollouts: collection and diagnostics only

Question: do fresh varied normalized-vector G4 samples provide enough unambiguous decision-level reward contrast for a later matched local-versus-joint credit experiment? No optimizer, LoRA initialization, mask or loss is implemented. This does not promote the vector as a better answer interface; vector48 had lower exact accuracy than lists.

Train16 NEW contexts: eight width6, eight width12; within each width four history1/four history3; check revisions1; stage index=i mod3. Gen202609440000..15, four seeds202609450000+4*i+r. Collect exactly64 vector calls, allgroups/samples retained, including zero contrast, missing and invalid full arrays. No label or outcome filtering and no shortcut teacher answers. Public normalization, boolean schema and policy are unchanged.

Freeze held12 NEW cases BEFORE any model outputs: widths6/12/20 × histories1/3 × check revisions1/3, one of each combination; stage index=i mod3. Gen202609460000..11; prospective two eval seeds202609470000+2*i+r. These24 held calls are not executed by this owner. Held check-revisions3 explicitly tests an unseen bookkeeping condition after public normalization; different instances are not a causal history/revision comparison. All train/held roots and all candidate IDs are disjoint from each other and previously frozen B05 panels, whose paths/hashes are retained.

Both public panels, full candidate order and exact native prompts/requests freeze before host labels. HOST_GOLD and HOST_BASELINES are host-only: two independent solvers agree; per-case positives/negatives, positive fractions, alltrue/allfalse exact/BA are computed before outputs without changing case selection. Collector reads TRAIN_PUBLIC only, never held prompts or labels. Labels enter post-collection metrics only.

Released Qwen3-4B-Instruct-2507, no adapter, T.5,384 output cap,8192 total context, four context workers. Caps science600/owner700/external800s. Same qualified native service/collector; owner changes only48→64 metadata/new bindings. Actual raw requests, responses, action IDs/logprobs/text are saved. Response SHA in call records means canonical parsed JSON digest; raw file bytes remain separately recoverable.

Strict full-vector validation precedes all per-candidate diagnostics. Invalid/missing output keeps ids=None and no local credit. All16 groups remain in RESULT; only groups with four valid full arrays receive G4 contrast diagnostics, explicitly marked. Candidate correctness minus other-three mean, joint BA/exact contrasts and zero-contrast groups are recorded, not silently removed. These diagnostics do not choose a training objective.

Native span diagnostics match actual completion decode, parse literal booleans, map each character span to monotone native-token offsets, and preserve each boundary-overlap token. A token overlapping two candidate values fails qualification. `qualified` means unambiguous span mapping; `exclusive_boolean_token_feasible` separately means no structural-boundary overlap. Escaped field names or nonmonotone decode may remain valid outputs but fail span qualification; no repair occurs. No prompt/environment tokens are mapped. Future training must separately approve any boundary overlap and qualify all immutable evidence.

Focused tests: native boolean spans versus actual tokenizer, invalid full arrays, hand-checked G4 contrast, and one localhost HTTP4-call fixture through current owner/collector/decoder/64-row metrics including invalid output and held-boundary checks. No GPU/model calls. Prepare with `CUDA_VISIBLE_DEVICES='' /project/alex_phd/envs/prime-rl-5990b1b/bin/python prepare.py`; MAIN reviews CPU_READY and alone runs its argv under the external cap/flock. Preserve every attempt.
