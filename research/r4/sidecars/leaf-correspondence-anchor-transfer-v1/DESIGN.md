# Fresh grouped correspondence-anchor transfer

Parent-approved N1-inspired exploratory comparison; no training, server ownership or GPU calls during preparation. The old selected c32de adapter is fixed prospectively from the positive indexed development screen (405/512 versus181/512). This is not a novel generic method.

Compare anonymous enum arrays with exact-ID→enum objects. Both receive the identical ordered ID→question mapping and semantic definitions; output instruction/schema is the intervention. This narrows the earlier screen's combined input/output representation change. No question echo, repair, majority vote or inferred alignment. IDs belong to original source records and remain stable across permutations. Object schema property order follows input order and is preserved in exact serialized request bytes; actual output order is measured, never assumed for ID scoring.

TREC: reuse exactly six exposed test64 contexts/384 question groups from leaf-composition-transfer-v1. Two fresh label-independent hash permutations per context, master910883. SST: from cached pinned stanfordnlp/sst2 validation, exclude all earlier256 normalized sentence groups, select256 remaining groups by normalized hash ordering without labels, compose four64 contexts and apply the same permutation protocol. Normalization is NFKC→casefold→whitespace collapse; SST is a different binary task, not a pure label rename. Dataset data licenses remain unknown/unspecified where the cached official sources say so; software Apache-2.0 is not a dataset redistribution grant.

Sampling seeds910901/910907, distinct from permutation master. Exact600 calls: batch5 covers all64 records in13 chunks on permutation0 only, two formats×two seeds×ten contexts=520; batch64 covers both permutations with two formats×two seeds×ten contexts=80. Not a full batch5 factorial. Dispatch64 stress first, then batch5 competence, counterbalancing formats and preserving every planned unrun coordinate. All calls share3072 output cap, T.5/full support, actual qualified system/tools, typed vLLM0.28 rendering, four workers and hard1800seconds after service handoff. No retries; infrastructure failures stop the inherited collector and remain null.

Primary: paired context-level indexed−anonymous correct/64 on TREC64, with validity reported separately and permutation/seed nested. Conditional aligned accuracy is also reported; malformed arrays are not interpreted as64 semantic errors. Batch5 competence/size comparison uses permutation0 only. Secondary: corresponding SST transfer direction, strict whole64 correctness, coverage, original-ID/output-position errors across permutations, exact physical inputs, logical/cached/completion costs and wall time. TREC and SST remain separate; there are six and four context groups, not600 independent examples.

Exploratory promotion criteria inherited from N1: indexed valid64 coverage≥95%, TREC improvement≥10 percentage points, no>2-point batch5 loss and a positive direction in both permutations. Positive SST direction strengthens transfer; otherwise narrow the claim. These are screening rules, not significance tests or permission to select another checkpoint. Cap3072 explicitly supersedes N1's tentative1536 using the previously qualified common representation budget.

Every output retains full raw response, physical prompt/output IDs, exact request bytes/hash, actual adapter alias and separate operational/format outcomes. Gold stays in host-only design/scoring data, never provider bodies. Freeze source/data/request/typed-prompt hashes before sampling; authenticate actual old endpoint and live alias/root/base at launch. Existing sidecars and the post-SFT audit remain untouched.
