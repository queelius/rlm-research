# Mixed-size checkpoint free/schema control

Parent-approved narrow follow-up, CPU preparation only. Parent owns service and GPU calls. Publish without examining B transfer/validation outcomes.

Question: after original, old five-item SFT, and fixed-final mixed-size A/B training, does batch64 failure reflect output contract failure, semantic positional collapse, or both? This is not a new indexing treatment, source split, or checkpoint search.

Exactly4 weights (original, old_sft, Afinal, Bfinal) ×4 existing validation64 contexts ×2 paired seeds ×2 conditions =64 calls;16 per weight. Inputs are unchanged correspondence rotation offset0 records1–256 in the original validation720 question-group-hash order. They are reused validation questions, not untouched test. No source-test reads or label/outcome filtering. Original and old_sft have already been observed; A's invalid-array signal motivated this comparison; B results are not used to choose cases or settings.

Schema64 is exactly the frozen correspondence rotation offset0 body: same native system/tools/definitions/question list, temperature0.5, top_p1, top_k-1,min_p0, cache_salt0, seed981261401/402, return_token_ids=true and max_tokens1024. Free64 removes ONLY structured_outputs. No instruction, output cap, parser, fallback, retry, example or posthoc repair change. Native contract dictionary order is the sealed runtime-order-v2 order. The two bodies have identical physical prompt inputs; actual provider prompt IDs will be captured and compared, not merely assumed.

Within each context/seed, free/schema order alternates by context_index+repeat; same16-coordinate template across weights. Pair by context, source question, seed and format; observed async timing is retained. Four workers per weight,120-second individual request timeout,600-second live preflight+collection wall cap. Parent may run weight jobs sequentially while each alias is warm; total cap40 minutes, expected a few minutes. Checkpoint every call, including errors/cancellation; no retries or resumes.

Authenticate using the frozen SST driver helper, not selected names alone. Original857a7ce6 and old c32de129 must match exact adapter/config bytes; old selection path is fixed. Afinal/Bfinal map to authenticated A/B epoch2 endpoints with206/204 updates, MANIFEST identity, RESULT/SELECTION agreement, checkpoint state, optimizer/RNG/member hashes and training-source closure. Their bytes are bound only after completion; no intermediate checkpoint or test-selected substitute. The supplied local endpoint descriptor and live alias/root/base/version must agree before dispatch.

Use unchanged strict canonical anonymous-array scoring from sealed correspondence. Whole64 contract validity and aligned semantic accuracy/coverage stay separate; invalid arrays are unaligned, not64 semantic errors; infrastructure is null. Report per-question labels/confusion, positional quartiles, copied source/group IDs, completion/truncation, model/output token IDs, physical input equality, all observed logical/cached/uncached/output costs and wall time. HUM/NUM counts remain secondary and may conceal cancelling intermediate errors. Do not count seeds or64 repeated assignments as independent contexts.

Promotion evidence: a weight×schema improvement in contract availability without late-position semantic recovery favors a format-only explanation. Recovery of aligned late-quarter/per-question correctness under schema at A/B, compared with old/original, supports improved batch-shape semantics at these fixed checkpoints. If B's benefit disappears under matched schemas, retire the claim that varied-size training repaired semantic correspondence. Mixed small-sample effects remain exploratory; four reused compositions cannot establish generic transfer.

Implementation: privately load sealed correspondence collector/scorer and wrap only request generation, per-call parent-coordinate mapping and weight binding. Reuse SST authentication read-only. No changes to either sealed source. Focused CPU tests for exact body delta and physical-input parity,16/64 pairing, source order, strict scoring, actual fake-network dispatch, and bad-weight rejection. READY last, bind/run commands supplied to warm-suite coordinator.

