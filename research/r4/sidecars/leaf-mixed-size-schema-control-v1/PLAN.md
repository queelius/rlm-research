# Approved bounded implementation plan

Use the existing planning/TDD/verification workflow inline in this additive external sidecar. No user clarification or GPU authority is required for the already approved design.

1. Publish DESIGN.md before inspecting B outcomes; write focused tests and observe missing implementation failures.
2. Reuse sealed correspondence request/scoring/collector in a private import;16 fixed paired coordinates and4 fixed weight conditions. Remove only structured_outputs for free arm.
3. Reuse SST authenticate_weight, mapping Afinal/Bfinal to A/B. Bind completed weights through new immutable descriptors/specs; no model contact during preparation.
4. Verify request equality, strict scoring/coverage/null costs, source hashes and real collector fake-network dispatch. Freeze SPEC and exact CPU evidence; READY last. Send bind/run commands to parent and suite agent.

The checklist is a frozen plan, not mutable completion evidence. READY.json records completion. No broad suite, source rewrites, new data acquisition, training or live launch.
