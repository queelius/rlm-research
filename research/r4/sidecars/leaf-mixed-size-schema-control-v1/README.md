# Mixed-size free/schema control: parent launch only

Exactly16 calls per weight: four reused validation64 contexts ×two paired seeds ×free/schema. All input text, native tools, definitions, temperatures, seeds and1024 output caps equal frozen correspondence rotation offset0. Free64 removes only structured_outputs. No retries, fallback, repaired arrays, new data or adaptive weight selection.

## Bind and run while each endpoint is warm

Use an actual parent-owned endpoint; never substitute a planned alias without live verification. The descriptor must bind the original857a7ce6, old-selected c32de129, fixed-final A epoch2/206 updates, or fixed-final B epoch2/204 updates. A/B are rejected until authenticated complete RESULT/SELECTION/state/member/source closure exists.

```bash
MIX_DIR=/project/alex_phd/runs/rlm-research-r4/sidecars/leaf-mixed-size-schema-control-v1
MIX_PY=/project/alex_phd/envs/prime-rl-5990b1b/bin/python
MIX_WEIGHT=old_sft
MIX_ENDPOINT=/PARENT/ACTUAL/endpoint.json
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 "$MIX_PY" "$MIX_DIR/driver.py" bind --weight "$MIX_WEIGHT" --endpoint-descriptor "$MIX_ENDPOINT" --spec-path "$MIX_DIR/BOUND-$MIX_WEIGHT-attempt-001.json"
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 "$MIX_PY" "$MIX_DIR/driver.py" run --spec-path "$MIX_DIR/BOUND-$MIX_WEIGHT-attempt-001.json" --output-dir "$MIX_DIR/outputs/$MIX_WEIGHT-attempt-001"
```

Weight names: original, old_sft, Afinal, Bfinal. Four workers and600-second total preflight/collection cap per weight,120-second request timeout, atomic checkpoint each call. Max40 minutes across all four weight jobs. Parent suite schedules each after primary correspondence/SST work, while its alias is warm. CPU bind/verify never contact the service; run checks live vLLM0.28 and alias/root/base before dispatch. All destinations must be new.

## Measurement

analysis.json contains unchanged strict per-record classification, whole-array contract coverage, aligned denominators, positional quartiles, confusion and HUM/NUM host-count diagnostics. Each free/schema pair records actual physical-prompt-ID equality and per-question changes only when jointly aligned. Wrong/partial arrays are unaligned, not64 semantic errors. Infrastructure failures are null, not policy failures. Raw request/response, provider prompt/output IDs, finish reasons and all observed input/cache/output costs remain in call checkpoints; missing costs are explicit.

The same256 source question groups, four contexts and paired seeds are reused across weights and previous validation experiments. This is an exploratory weight×format mechanism control, not fresh validation, pure indexing, generic batching novelty or proof of free planning. Count accuracy may hide cancelling errors. Physical prompt equality is checked from provider data, not inferred solely from request similarity.

## Frozen implementation

Privately reuse the sealed correspondence request/scorer and actual collector; map each call to its original offset0 coordinate for qualified CPU physical-prompt hashes. Only the free arm deletes structured_outputs. Use the frozen SST authenticate_weight helper for checkpoint provenance (Afinal→A, Bfinal→B). Neither source is edited. Parent/spec/source drift rejects binding and execution.

CPU_CHECK.json records exact-body equivalence, schema compilation inheritance, tests and binding checks. DESIGN_FREEZE.json records design publication before the observed absence check for B's final RESULT; no B outcome is used for preparation. SPEC.json declares all64 calls before weights are bound. READY.json is written last.

