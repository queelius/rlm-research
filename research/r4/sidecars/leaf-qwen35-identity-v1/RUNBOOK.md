# Parent-only launch

Use READY.json exact launch_argv under the actual exclusively assigned MIG UUID and established LD/API-key environment. Do not replace CUDA with0. Launch only after predecessor ownership is resolved and the parent confirms no collision under the established lock. No new scheduler is supplied.

`/project/alex_phd/envs/prime-rl-5990b1b/bin/python owned.py --directory /project/alex_phd/runs/rlm-research-r4/sidecars/leaf-qwen35-identity-v1/owned/attempt-001`

Parent outer cap2670s. Scientific outputs are `outputs/qwen3` and `outputs/qwen35`, per-call checkpoints, ordered HTTP wire bodies, provider prompt/output IDs and per-stage STATUS/analysis. Owned lifecycle evidence is under `owned/attempt-001/{qwen3,qwen35}` and terminal at `owned/attempt-001/TERMINAL.json`. There are no repairs or retries; a failed first stage prevents second-stage launch. Independent analysis should run after release.

CPU verification: hide CUDA, set PYTHONDONTWRITEBYTECODE=1 and run READY verify_argv. It checks sources and the stat identities of shards fully hashed at preparation; it does not repeatedly read gigabytes per request. Any changed model shard requires new authentication before acceptance; no silent refresh. GPU kernel execution for Qwen3.5 is unqualified until this parent-launched attempt.
