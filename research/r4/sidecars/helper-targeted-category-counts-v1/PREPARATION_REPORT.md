# Targeted helper comparison: CPU preparation

Implements the approved96-call design in five Python files with no GPU launch or modifications to sealed sources. PLAN.md links the unchanged approved DESIGN hash. READY.json is the authoritative source manifest, exact argv, environment versions and CPU test receipt; MAIN must review it before launch.

`target_study.py` reuses the sealed size-study loader, public-only full16-record prompts and native v4 lifecycle. It freezes96 ordered requests and validates the literal `[target, "other"]` schema locally; the old validator cannot accept `other` and is not repurposed. Every category definition remains present. `metrics.py` scores both arms against the identical host predicate, keeps80 target-count tasks separate from96 physical calls, and reports uniform-single-target and reusable-all-target costs. The positive/negative confusion and missing branches use fixed planned denominators, undefined rates stay null, invalid count maps never become zero, and missing full maps fan out without multiplying physical calls.

`owner.py` saves exact credential-free request and response wire bodies, parsed records, request ID, finish, actual token inventories and hashes per call. It continues through ordinary request/validation errors, with no retries or adaptive stopping. The unchanged900-second owner and1000-second outer cap leave explicit unattempted slots and attempt clean release. Pre-exec v4 evidence is checked before calls; final qualification additionally requires an actual EngineCore log line entering `batch_invariant.py`. If the runtime suppresses that trace, collected counts are retained but runtime qualification and promotion fail rather than inferring enablement from the parent environment.

Focused tests compare a hand-scored same-gold full/binary fixture, missing-full-map fanout/unknown costs, actual frozen96-request/context inventories and native dependency loading, and real tokenizer response validation including non-stop, extra IDs and array-of-pairs rejection. CPU_TESTS.json records the final fresh run. Native SWIG deprecation warnings are inherited and do not indicate a GPU invocation. A test-only dummy credential permits CPU dependency loading; it is never used for a service launch or included in experiment requests.

Readout interface: outputs/attempt-001/RESULT.json contains `physical`, `tasks`, `per_target`, `datasets`, `cost_views`, `targeted_consistency`, `runtime_qualified`, startup and owner time. `datasets[dataset].prospective_screen` reports the predeclared1.25-token tradeoff screen and the runtime-gated result, not an optimal budget or established gain. All full tradeoffs remain visible. Source c32 and dataset exposure provenance are inherited and hash-pinned, with fresh calls throughout; this is a now-exposed exploratory panel, not selector training, unseen-from-pretraining evidence, or a general root/recursion result.

MAIN argv: `/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/helper-targeted-category-counts-v1/owner.py run --outer-seconds 900`, wrapped in shared GPU flock and external timeout1000. Exact output is this sidecar's `outputs/attempt-001`; an existing attempt is rejected.
