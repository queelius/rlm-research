# Yes/no56 CPU preparation

Implements the approved verbalizer-only comparison on all128 frozen TREC records, with fresh full8 and yes/no48 calls. No existing source or live job was modified. READY.json is authoritative for source closure, exact argv, environment versions and CPU evidence.

`yesno_study.py` starts from the original TREC request schedule. Full-control bodies remain exactly equal to the frozen originals. Only the targeted instruction prefix and output schema change; category definitions, target identity, question text, IDs, order, wrapper, model seed and other sampling settings remain fixed. INSTRUCTION_DIFFS.md displays the exact six instruction edits, and inputs/SCORING_MAP.json explicitly defines yes as target membership and no as the other five categories.

Actual token decoding and exact wire request/response persistence reuse the sealed native seams with temporary restored import aliases. No old owner executes. `yesno_metrics.py` keeps CALL/raw predictions as yes/no and makes a separate host-scoring copy mapping yes to the request target and no to its complement. It reuses the sealed matched confusion/count/cost implementation. Per-target behavior additionally records predicted positives, all-yes/all-no maps, and attempted/unattempted/unavailable maps. No logical prediction reconciliation or gold substitution occurs.

Primary48 target-count tasks use only this run's fresh controls. The old literal/other run contributes only a distinctly labeled secondary diagnostic on its seven previously completed TREC blocks, whose indices and result hash are frozen before launch. Secondary output reports confusion and count metrics plus fresh full-control drift; incomplete new controls do not produce a misleading numerical drift. The prior entity-positive collapse motivates an output-calibration hypothesis, not a proven mechanism.

Focused CPU tests check56/896 inventory, preservation of full bodies/definitions/records, ordered yes/no schema, context bound, hand-scored explicit yes/no mapping and collapse, missing-full-map fanout, baseline cost reuse, raw prediction preservation, native dependency loading and real-token acceptance/rejection of yes/no versus category/other. Only inherited SWIG deprecation warnings are expected; test credentials never launch a service.

One750-second owner, external850-second MAIN timeout and shared GPU lock; no GPU launch here. Maximum1024 output tokens and8192 context remain unchanged. Each call checkpoints wire/token/schema/ID/finish/usage evidence; errors and capped tails remain explicit. Runtime requires fresh pre-exec flag attestation and final actual EngineCore batch-invariant trace. The unchanged1.25-token exploratory screen requires complete comparable availability; it is not an optimal budget or deployment claim.

MAIN argv: `/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/helper-trec-yesno-counts-v1/owner.py run --outer-seconds 750`. Output: `outputs/attempt-001`, rejected if it already exists. RESULT.json contains inherited `tasks`, `per_target`, `datasets`, `physical` and `cost_views`, plus `binary_behavior`, `scoring_mapping` and `secondary_verbalizer_diagnostic`. No tail continuation or selector training is included.
