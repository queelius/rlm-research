# Typed helper child36

CPU preparation only until main separately accepts and launches the exact READY argv.
Read the bound design and TYPED_CHILD_IMPLEMENTATION_DECISION.md for scientific scope.

All six exposed64-record contexts, both fixed count queries, U/R/T, twelve fresh coordinate seeds. Historical root473210… and childc32de…; no current-checkpoint selection. Main contrast T−R; U is familiar calibration. R/T root prompts, helper files, native sampling except qualifying child schema, and weights are identical. Root remains free. A contract-matched child must emit a requested-order six-canonical-label map, which suppresses child tool actions; this is not wording-only or semantic validation. Custom/binary/subset-vocabulary children remain untreated. Origin is exact public request plus trusted invocation/depth, not Python helper attribution.

All arms use private image8cfe… and its qualified private store/wrapper, never an accepted source/image mutation. Inherited nano transient retries remain possible and measured; no new retries, replacement episodes, repair or grammar fallback. Existing strict final nonblank-line numeric scoring is unchanged, including completed scored-empty failure=0. Infrastructural/incomplete nulls are separate. Implementer projection is not independent audit.

One owned service; ready180, cumulative collection1800, work2280, inclusive2400, outer2430 seconds. Absolute clocks never reset between startup/collection/cleanup;120-second cleanup reserve. Four workers each process one triple serially. The inherited collector retains its >50% execution-error stop after eight episodes and60-second finalize reserve. Explicit CPU analyze occurs after TERMINAL and owned GPU release.

Main supplies one exclusive CUDA_VISIBLE_DEVICES and existing STRICT_RLM_CALIBRATION_API_KEY; never serialize credential values. Exact proposed command is written in READY. Run uses the unchanged service/lifecycle plus pinned disappearing-process observer. There is no scheduler, acceptance, GPU launch, optimizer or model call in preparation.

Same seeds mean intended variance reduction, not guaranteed matching sampled actions. Preserve first root native prompt/sampling/token equality and first eligibility; do not label adaptive divergent child calls paired. Raw wire, ordered/canonical schema digests, trusted aliases/invocations, native token IDs, helper logs, statuses and physical attempt costs remain available for independent audit.
