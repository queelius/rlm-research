"""Unchanged actual-tensor/Adam/12-replay/20-skip qualifier, scoped to its original study."""
import study
source=study.bound('fresh8_trainread_checkpoint',study.PRIOR/'checkpoint.py',study=study.prior)
OUTPUT=source.OUTPUT;CHECKPOINT=source.CHECKPOINT;RECEIPT=source.RECEIPT
verify_checkpoint=source.verify_checkpoint
binding=source.binding
