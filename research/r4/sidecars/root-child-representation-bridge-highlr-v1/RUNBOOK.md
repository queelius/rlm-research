# Fixed last-highLR SFT8 bridge replay

MAIN authorization: ideas/2026-09-09-bridge-policy-panel-amendment.md. Only the root
checkpoint changes from fixed low66cce to fixed last higher-rate SFT step8,0ba42364…;
c32de child,32 coordinates, two seeds, prompts, public records, examples, array/map
contract, native capture, broker projection and strict scoring remain frozen.
This is a policy-by-format comparison on exposed repeated coordinates, NOT an
independent-seed/data replication or a best-checkpoint selection.

The transport root alias deliberately stays byte-identical and contains the historical
word low8. This is a stale alias name, not a weight claim: actual BINDING/endpoint/model
cards and captured adapter SHA establish highLR SFT8. No other model configuration is changed.
The original scientific schema name and qualification references likewise identify
the reused package; this new READY and RUN identify the checkpoint-only replay.

Only small authenticated loaders are new. The original driver runs unchanged. The
collector's four immutable input reads target their original frozen directory; all
32 IDs and prompt token vectors are exact. Unchanged adapter/bridge/contract/overlay
modules execute under their original source identity. No file copies, runtime rebuild,
scientific outcome reads or original source changes are needed.

The original actual six-call CPU fixture remains the native raw-child→broker-map→root
qualification. New focused tests execute the real pinned native role.route for depth0
and1 with this binding, check exact root-only delta and32 coordinates, and import both
real executable entrypoints. This is composed CPU evidence, not new model inference or
a new live runtime fixture. Original root-readable config caveat applies unchanged.

Proposed exact launch, MAIN alone assigns GPU/credentials and outer1800s:

```
/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-child-representation-bridge-highlr-v1/driver.py run --output /project/alex_phd/runs/rlm-research-r4/sidecars/root-child-representation-bridge-highlr-v1/outputs/attempt-001
```

Collection1500 clipped by shared1650 work; owned1770/outer1800,120 cleanup reserve.
Same no-new-retry policy and inherited transient retries under cap. All32/null/invalid
and actual native cost/width/uptake/semantic/fidelity accounting remains required.
No launch authority is conferred by CPU_READY. Original sidecar projection can be
adapted read-only after terminal; do not call its original argv against the old output.
