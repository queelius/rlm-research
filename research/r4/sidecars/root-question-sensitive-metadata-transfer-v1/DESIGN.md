---
question: Does question-sensitive SFT adapt its routine to new user names, weights, and thresholds?
status: prospective-exploratory
date: 2026-09-10
unit: eight reused protected contexts, nine questions per context
---

The original QS readout improved strict correctness28→57/72, and MAIN's
trained-policy review found62faithful calculations,52correct. We need to test
whether this is a task-sensitive routine or a routine tied to the original
metadata. This follow-up uses both exactfixed24 and newQSfixed6 checkpoints.
No further training or selection; fixedc32child. It is not new text, unseen
operators, an independent training replicate, or global generalization.

Take every original protected context and question. Replace user namesu0..u3
withu4..u7 bijectively; every query receives the corresponding scope names.
Replace each weight by8+SHA256([namespace,'weight',record_id]) mod8, giving8..15
instead of1..7. Change threshold questions from4/8 to13/25 respectively.
Keep record IDs/text/order, category targets and operator families unchanged.
All72questions are retained regardless of gold or model behavior. This is a
joint metadata-shift package; failure would motivate a component ablation,
not identify one shifted field as causal. Count/distinct questions change
only user names, providing a useful within-study anchor.

Use fresh paired seeds988621001+i in original dispatch order and fresh episode
and native context IDs. Both models receive identical new public files/query
and native prefixes within a pair. Trained6first thenfixed24; originalfreshseed
replication stays a separate study. Fourworkers, temperature.5, root2048,
context8192, same nativePython/file/recursivecall interface, no query cards.
Codegenerated bythe model executes only through the existing research runner;
analysts never execute sampled programs to manufacture outcomes.

Primary strict correctness and task-faithful executed calculation; all144slots,
missingRESULT=NULL, authenticatedempty0 underQSrule, no rerolls/repair/salvage.
Report composed48/primitive24, bycontext, zero/nonzero and operator. Keep
renaming-only count/distinct16 separate from weight-dependent56questions.
Gold computed by two independently written trusted oracles must agree; gold
changes must not change any nativefiles/prefix. Preserve source/inputhashes,
complete physical/native records, tokens/cache/unknown costs andphase/release.

A positive conservativepairedgain and improvements in at least6/8contexts
would support metadata transfer on these families, not novel composition.
If gain orfaithfulness collapses, prioritize a renaming-only/weight-only
factorial and training diversity overmore updates on the sameexamples.
3900outer/3870owned/3750work;1800perservice includingstartup/release;
fixedcheckpoint inputs andperepisode checkpoints. MAIN author/reviewer overlap.
