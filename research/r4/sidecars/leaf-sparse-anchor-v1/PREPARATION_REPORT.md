# Sparse144 CPU preparation

READY `25b80395b129dae68d559357281b67f5e34e9337ca99887440018f69d3b53847`; SPEC `1627f461a52811e68dfab15bd5a0ec1f173ef71accee493b50bc5fcb31c173c3`. The frozen scientific files were not modified after publication. This is an additive handoff note, not a new scientific variant or acceptance.

All 144 complete native typed prompts qualify with the unchanged 3,072 output cap. Input ranges: TREC 1,896–2,145, SST2 2,593–3,093, AG 4,520–4,962. Maximum 8,034/8,192 leaves 158 tokens of context headroom; no crop or truncation was applied. Forty-five distinct mixed schemas compiled and accepted canonical nongold fixtures while rejecting wrong anchor key order and nonanchor object types. Matching/constant synthetic token counts coincide within each task/cadence; repeated-label fixtures range 771–963 dense, 305–497 at cadence 4, and 173–365 at cadence 16. These are structural fixture counts, not predicted actual model cost.

Seven desired study tests and two driver tests failed before implementation; all nine passed together in 9.72 s during prepare. The fake inherited HTTP collector exercised valid output, observed malformed output and provider HTTP 500 in three calls, preserving distinct scores/nulls. The ordered physical hook was exercised independently. Seed audit found no collision for master 981296001 or sampling seeds 981296011/981296021 in 128 named frozen namespace files; scope is bounded, not global history.

Fresh post-seal `owned.py --verify` exited 0 and authenticated 152 named source/input entries plus exact c32de/config/base identity. Runtime/environment, source-adapter edits and full typed token IDs are recorded in the frozen JSONs. One source-checked private run seam changes 96→144 and collection 600→900 only. The owned wrapper retains one clock, known process-absence guard and exact-owned-service release. No GPU/model/network/service calls, environment installs, old-source changes, outcome reads for data selection, process signals or parent acceptance were performed by preparation.

Launch/caps/ownership are in READY and RUNBOOK: parent 1,230 s, owned 1,200 s, shared work 1,080 s, collection 900 s, four workers. Main alone reviews, accepts, provides the existing shared lock/empty-GPU/exact predecessor and actual MIG/env, and launches the published argv.

Known projection limitation: on a structurally malformed parsed array, optional `anchor_key_order_valid` can retain a true value for checks reached before the structural failure, rather than establishing all anchors' order. Primary `schema_valid` is false and strict score 0 in that case; raw bytes and precise `parse_status` are preserved. An independent readout must inspect raw object order and use null when full order is unobservable, never promote that auxiliary flag alone. No remedy or reward repair was applied to frozen source. Across cadences the representation and visible anchor-position instruction change intentionally; that is a token/accuracy package comparison, not isolated model attention.
