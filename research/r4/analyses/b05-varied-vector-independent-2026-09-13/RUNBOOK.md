# Varied64 one-shot CPU audit

Use the sealed argv once. Missing terminal writes PENDING and stops; unqualified terminal writes HOLD with no waiver. MAIN may request a later immutable output directory. No GPU, optimizer, loss or new experimental preparation.

Reuses the reviewed plain native and runtime auditors plus independent vector grader. Regenerates all28 frozen train/held public roots and verifies disjoint prior panels, metadata, retained candidates, effective fields, public order and every frozen request. Actual64 training calls are checked against their exact model/prefix/seed/384/T.5 requests, canonical versus raw response hashes, native ID/logprob/decode records, grades, costs and runtime/release. Held24 prospective requests remain unexecuted.

Boolean spans are independently located by walking the validated full JSON array; cumulative decodes of actual action-token prefixes give native offsets. Compare every boolean character span, action-token index and structural-boundary overlap with saved diagnostics. Invalid arrays remain unmapped. No text re-encoding is substituted for actual native action IDs. Exact-only boolean token feasibility remains distinct from unambiguous mapping with punctuation overlap.

All16 G4 groups retained. Independent candidate correctness matrix and RLOO `(4*r-sum(r))/3` are checked against saved per-candidate contrasts; joint BA/exact contrasts checked separately with floating-point tolerance. Only four-valid-output groups qualify for these diagnostics; excluded groups remain in primary64-attempt denominators. This is not automatic data selection or training admission.

Focused tests check known boolean positions/native tokenizer, hand-computed four-way advantages and the existing actual four-call HTTP fixture including invalid outputs. No broad suites or repeated live-state polling. Report useful contrast and exact token-boundary feasibility to MAIN and the trainer-preparation agent only after actual qualification.
