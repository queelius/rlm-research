# Equal-row SFT audit: pre-outcome method

Written September 9, 2026 before this auditor opens any new row-mean training or readout outcomes. MAIN reports the accepted job is waiting behind Qwen144; this auditor does not poll the waiter or read live outputs. The study was motivated by exposed prior SFT results, and this method is not a claim of hypothesis discovery independent of those results.

## Scope and independence

Study: `sidecars/root-interface-sft-row-mean-v1`, READY identity `48a7c10f6b308868e2a9fc02a5b718687086a9a9c246cb111a7f80c1629e25a3`. New R is fixed equal-row checkpoint four, trained from historical root `473210b18c4be163cd46a894614cb14934a7908e45bd97f96720928a7770ccdd` with fresh Adam. T is the existing global-target-token fixed checkpoint four `efab2913e7fe9f5f9b381ae6eb67bb56071070f86b237e6145f98654816aad64`. Both retain child `c32de129…`.

This auditor did not author the new weighting intervention but authored/shared its original SFT and native harness components, reviewed later integrations, and knows earlier SFT/RL outcomes. This is independent reconstruction of this new comparison, not an independently developed training system or an outcome-blind audit of all historical research. New R/T outcomes are not consulted during preparation. Evaluation records must never become training candidates in this audit.

Only this analysis namespace is writable. No GPU/model/network/service actions, runtime fixture, queue change, scientific-source edit, dataset reselection, reroll or generated-code execution. After focused parser preparation, await MAIN's terminal trigger; no autonomous polling or idle outcome sampling.

## Training comparison

Authenticate the original 32 frozen rows, four ordered effective batches, training seed 981284002, historical starting adapter, original recipe and new loss/source identity. Reconstruct every shifted supervised count from `labels[1:] != -100`, verify one unchanged EOS and exact current-action masks. No retokenization, row duplication, exposure/order change or prior optimizer restoration is allowed.

For each effective batch of 16, original T uses `sum(CE_i)/sum(n_i)`; R uses `sum(CE_i/(16*n_i))`. The inventory is 1,520 helper and 83 terminal target tokens per epoch, 64 row exposures and 3,206 target-token exposures overall. Independently reproduce scalar terminal objective mass per batch and executed FP32 coefficients from the row ledger. Call these coefficient/objective masses, **not measured gradient shares**. Parameter gradients depend on row contents, clipping and Adam. New row-mean NLL is not numerically interchangeable with the old token-mean NLL; retain the separate token-normalized monitor.

After terminal, authenticate R RESULT/SELECTION and all four checkpoint states/member hashes once, fixed final4, actual final Adam cursor, load audit, fresh optimizer, base/adapter precision, row IDs/counts/coefficients/objective sums, finite gradient and actual weight delta. Bind T's existing RESULT/SELECTION/final checkpoint independently once. No large base-model or checkpoint hash loop per episode, and no new model forward test. Verify the only declared scientific training change is loss normalization; distinguish source-provenance wrapper changes from numerical intervention.

## Primary readout

48 newly sampled trajectories: R/T paired on the same 24 exposed task coordinates, fresh seeds validation981300101–108, query981300201–208, length981300301–308. R precedes T in two separate fixed service phases. Keep all planned coordinates even if missing, stopped or unrun. Report R−T wins/losses/ties/unknowns and all-planned bounds, with validation/query/length and each of the eight source-context clusters separate. Do not pool prior readouts or treat 48 episodes as 48 independent contexts.

Independently reconstruct strict whole-reply `Answer: N` correctness from the saved raw root reply and host gold, matching the frozen scorer's surrounding-whitespace handling. Completed available wrong/malformed/empty replies are zero; incomplete/provider-unavailable/operationally invalid or unrun coordinates are null. No bare-number, prose, decimal, sign, label or answer repair. A later phase/study cap must not erase an earlier completed result.

Unlike the RL audit, **do not introduce a training-admission gate** based on trace errors. Preserve raw endpoint, collector availability/error/censor fields, episode/trace status and native-graph validity separately. If a returned final response coexists with a trace failure, expose both facts and compare exactly with the frozen collector rule. An input/alias/prefix-integrity discrepancy is reported as material experimental invalidity, not silently rescored or fixed.

## Native evidence and prespecified diagnostics

Index each completed phase once. Verify frozen plan/crosswalk, actual aliases/checkpoint/config/base, common typed-child hooks/runtime, exact first physical/native prompt IDs against the frozen arrays, and paired first-input/sampling agreement. Audit native branch prefixes, sampled completion IDs/logprobs and root/child role separation where the graph is complete. Incomplete graph proof remains unavailable and must not manufacture a likelihood.

Reconstruct actual final-answer protocol validity separately from a validly formatted wrong number. Inspect the first root structured tool call as data: tool envelope present; code JSON-extractable; Python AST parsable; exact byte equality with the frozen four-record HELPER. Missing/unextractable/AST-invalid code yields unknown for the conditional exact-copy metric, not false. A whitespace-only change is not exact copying. Report its denominator and actual native first-helper requested IDs independently; lower exact-copy rate alone is not better planning.

For source-authenticated typed helper calls, report actual helper use, duplicate-free exact requested-ID map validity, semantic label-occurrence correctness, unique requested/available query-relevant IDs, and requests beyond the initial four. Repeated label occurrences are not independent question groups. Neither prose nor root-writable logs alone prove coverage. Where complete unambiguous maps are present on the actual final physical branch and supported by native child values, a descriptive map-implied count diagnostic may distinguish child semantic errors from root count/final-format failures; unsupported/partial evidence stays unavailable, never a repaired primary endpoint.

Decode embedded wire response/usage JSON, retain root/child attempts and known/unknown logical input, cached, uncached and output token costs. Failed/request-only tails count as cost evidence with honest uncertainty, not invented completions. Compare matched observable pairs and jointly correct pairs separately; no jointly correct pairs means successful-task cost ratio undefined. Provider-reported usage is not independently measured unique GPU work, and inherited transient retry capability must not be mislabeled zero retries.

## Interpretation and publication

Better protocol but unchanged poor coverage/counting supports a contribution from this weighting change, not a complete solution curriculum or an identified copying mechanism. Less helper copying without strict gains could instead be weaker interface competence. Flat/negative results address this exact four-step intervention, not all normalization schemes. Same training seed/order, a reused historical T checkpoint, sequential phases and small exposed clusters limit generalization and causal mechanism claims.

Publish a small terminal metric summary promptly, then a readable report, exact source/artifact hashes, per-coordinate outcomes, training ledger checks, null/censor details and reproducible CPU audit code. Any unexpected failure/analysis limitation gets an additive note; the frozen study and this method remain unchanged. MAIN owns all launch/cleanup/follow-up decisions.
