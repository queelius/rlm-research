# Chosen-token confidence: a narrow post-hoc routing lead

On the already exposed six contexts/two repeats, select the two parent-vector decisions with the lowest saved chosen-native-token log-probability, breaking ties by public position. Then substitute only those two cached singleton answers. Ranking never reads gold or singleton success. Eleven of12 full parent vectors are valid and rankable; the remaining invalid vector stays unranked and a failure, without partial mapping or repair.

This cached counterfactual captures7/10 available singleton corrections, introduces no errors, and changes complete sets4→6/12. Uniform random choice of two distinct candidates has analytical expectations1.467 captured corrections and4.3 complete sets on the same rankable population. It is not a newly run routing experiment. Both new exact answers are repeats of **one** width20 context, not two independent demonstrations.

The signal is weakly resolved:115/132 saved decision log-probabilities are exactly zero; five of11 units have a tie at the second/third rank boundary. Chosen-token probability includes inseparable punctuation/whitespace and preceding sampled answers; it is not a calibrated binary confidence. A simple first-two-public-positions control already captures5/10 corrections, though it leaves exact4/12. Confidence therefore contributes two additional corrections over that control, again the same width20 candidate in both repeats. On the other width20 context the one recoverable error ranks17th and is missed. On a width12 context, two of three errors are selected, so neither repeat becomes exact. The persistent low-quality false positive cannot be fixed even when selected: its singleton also answers incorrectly.

The22 selected cached singleton calls used14,952 input+132 output tokens. Together with the12 parent-vector calls this would be34 physical calls and43,310 total tokens, versus164 calls/132,140 tokens for all parent vectors plus all singleton decisions in this saved panel. These are summed observed cached costs, not measured runtime for an implemented routing policy; a future control must match query count and preserve invalid-parent handling. Random-two expectation matches call count, not exact input-token length.

This motivates at most a prospective fresh-case fixed-k2 confidence-versus-random comparison using identical actual parent answers and independently seeded singleton acquisitions, with all cases retained. It does not establish a useful learned confidence estimator or learned delegation. No model calls, prompt variants, training, or new experiment implementation were performed by this analysis.

Reproduction: `../confidence.py`; focused arithmetic/tie test `../test_confidence.py` passed. [CONFIDENCE.json](CONFIDENCE.json), SHA256 `71e55919fa4bbeebeb5bec92fd84549ce4ef85d7d0491e649585487d97bac8b9`, pins the actual source program, full prior native audit, input panel and consumed raw records. The original primary176-call result is unchanged.
