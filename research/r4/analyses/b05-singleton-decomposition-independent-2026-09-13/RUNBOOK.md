# One-shot singleton176 native audit

CPU only, no polling or collector mutation. `analyze.py seal` authenticates the source READY, independently recomputes six source projections/gold and exercises the archived real HTTP fixture. `analyze.py run --output outcome-001` checks terminal once; absent terminal produces PENDING, not a watcher. Use a new output directory on any later invocation.

Reuses reviewed raw-wire/native decoding/runtime audit and independent list/vector grading; new scalar parser/union is independently written, never imports the collector's scalar/union implementation. The source decoder is reused, so this is source-to-raw verification, not an independently implemented native protocol. Unknown and malformed-known candidates never default to false. Every scheduled scalar is required for complete-stage exact/BA. All176 physical calls,36 stage outputs,12 units/arm and six context units remain explicit.

All model/prefix/seed/body/token/usage and both raw/canonical response hashes are retained in REPORT provenance. Recompute matched-valid arm denominators, width/pair counts and natural input/output costs. Explain candidate disagreements using public facts and host-only rejection reasons. No gold-selected routing policy is tested; saved chosen logprobs are not full entropy.

```sh
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/analyses/b05-singleton-decomposition-independent-2026-09-13/analyze.py run --output /project/alex_phd/runs/rlm-research-r4/analyses/b05-singleton-decomposition-independent-2026-09-13/outcome-001
```
