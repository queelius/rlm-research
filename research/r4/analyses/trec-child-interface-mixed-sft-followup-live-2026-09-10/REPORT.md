---
date: 2026-09-10
status: complete
scope: CPU-only native and training-chain audit
---

# Mixed-contract continuation result

The exact owner terminal and sole parent EXIT authenticate a complete 192-call run, exit 0, no
timeout, released service, and no remaining GPU processes. Raw replay recovered 192/192 available
responses, all with unique provider request IDs and `stop` finish reasons; replayed native objects
and strict host-gold scores match RESULT and terminal inventory exactly. No NULL appears.

On the research-exposed primary new-pair panel, mixed update24 scores 734/768 through full6 and
665/768 through ABO. Relative to the exact c32 receipts (725 and 590), the paired gains are +9 and
+75 labels. It therefore clears both frozen gates: ABO gain is 9.77 percentage points (required at
least 5), while full6 changes by +1.17 points rather than declining (allowed loss at most 2).
Balanced accuracies are 0.9544 full6 and 0.7650 ABO.

The result sits between and usefully combines the pure controls. Against pure full6 training it is
-11 full6 but +85 ABO labels; against pure ABO training it is +27 full6 but -15 ABO labels. On the
research-exposed development panel it scores 746/768 full6 and 706/768 ABO, versus reused c32
receipts of 726 and 605. These results support the narrow exploratory conclusion that deterministic
balanced interface exposure can acquire substantial compact-contract skill without the pure-ABO
arm's observed full6 retention loss. They do not establish outcome-pristine generalization.

The training chain is fixed c32 to true completed update24 with fresh Adam/RNG. All checkpoints at
6/12/18/24 have the correct 24/48/72/96 cursor, manifest-bound file hashes, populated Adam state at
the matching step, finite moments, and present Python/torch/CUDA RNG state. Training took 57.17
seconds. The new rollout used 192 physical requests/responses/results and 231,692 input, 47,089
output, and 148,912 cached tokens, with zero unknown usage cells; old controls were not rerun.

The first reader execution exposed one legacy-schema seam after successfully replaying the new
calls: reused query coordinates lack an explicit panel field. The documented additive repair maps
only those already-designated rows to development. The native replay uses the frozen trusted
producer protocol and is a consistency/replay check, not an independently reimplemented metric.
