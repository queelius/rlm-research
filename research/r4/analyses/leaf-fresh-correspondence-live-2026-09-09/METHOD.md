# Fresh correspondence96: independent pre-outcome audit method

September9,2026. Frozen before this auditor reads any fresh96 responses, aggregate scores or inference outcomes. MAIN reports an original launcher failure before client/model calls and is preparing a separate lifecycle-only recovery; these operational facts are not classification outcomes. Exact original science READY `09e3071801e07bd78d998013b666eeb7e6437c7062228b2a0ef925f87e135545`, SPEC `9e0618c5714b82374328e7679acf65dd8bdd691f1edfd83e072b4281930ec580`. Scientific output locations remain `sidecars/leaf-fresh-correspondence-v1/outputs/qwen3` and `outputs/qwen35`. Bind the actual recovery operation/acceptance/launcher later, without changing these endpoints or the method.

## Independence and gate

The auditor did not author fresh96 scientific selection/driver/scorer or recovery implementation. The auditor authored the earlier bounded fresh-data feasibility work and has shared upstream native/Qwen preparation and prior result exposure; this is independent raw scoring, not an independent inference implementation or independent dataset-discovery decision. Reuse the authenticated independent Qwen144 parser, never the study scorer as authority. Do not inherit any stronger authorship claim from its old report.

Read frozen inputs/source/cache and synthetic fixtures now. Do not open raw responses, aggregate scores, active inference logs or current outcome files until MAIN terminal trigger. No polling, GPU/model/service calls, process signals, experiment edits or reruns. If a technical failure terminates recovery, preserve it as the result; do not await nonexistent completed calls. All96 planned slots remain visible.

## Population and primary comparisons

Exactly two released instruction/post-trained checkpoints without research adapters: Qwen3-4B-Instruct-2507 cdbee75f17c01a7cc42f958dc650907174af0554 and Qwen3.5-4B851bf6e806efd8d0a36b00ddf55e13ccb7b8cd0a. Eight64-record contexts (four AG News public test, four SST-2 public training text-unit contexts), seeds981318011/981318021, plain label arrays / matching-source-ID objects / constant p0000 objects:96 calls,48/model,512 source records/groups.

Primary: matching-minus-constant displayed-position strict correct/64, separately for each dataset/model. Average the two paired seed contrasts within each of four context clusters; all eight planned seed pairs stay in the table. A missing contrast remains NULL; an incomplete context does not silently become a differently weighted primary mean. Show available-case descriptive values and planned-denominator bounds separately. Secondary matching-minus-plain, whole64 correctness, validity, semantic accuracy conditional on fully valid vectors, per-position/quartile accuracy, label confusion and histogram L1. Cross-model interaction is exploratory; different templates/tokenizers/post-training and sequential service stages prevent architecture attribution.

## Source selection and exposure audit

Independently reconstruct NFKC+casefold+collapsed-whitespace SHA256 groups from exact pinned official cached parquet. Exclude whole conflicting-label groups and exact groups in the frozen bounded25-history membership/reservation crosswalk; do not mistake all raw upstream membership for research exposure. Rank by SHA256(`981318001:task:selection:group_hash`); retain first256/task, earliest raw source-row representative and all duplicate indexes, partition consecutive64 blocks. Verify source IDs from label-independent context-specific ordering of1000–9999, full text, label mapping, row indexes and public gold separation. Do not call author selection functions for this verification.

Confirm512 unique groups, no selected exact excluded group, four contexts/task, and the stated43/256 one/two-word SST units. Preserve full selected phrase lengths rather than dropping fragments. This is new to the bounded named research-input crosswalk, NOT globally novel research data or unseen pretraining/post-training data. SST parent/subphrase overlap and AG event/near duplication remain unresolved. Underlying dataset licenses are unconfirmed; loader licenses do not establish dataset rights. Frozen source revisions/acquisition manifests/parquet hashes and selection records are required. No new source reservation, filtering or outcome-selected replacement.

## Strict independent parser

Parse the entire JSON reply; ordinary JSON whitespace is allowed, but no trimming/extraction of fences or commentary. Reject duplicate keys and nonfinite values, nonarrays, cardinality other than64, noncanonical label strings, extra/missing object keys, or an incorrect current displayed source ID. Plain requires canonical strings. Objects require exactly tag and label; matching tag equals the record's actual ID at that slot, constant tag equals p0000. Wrong tag at position64 invalidates the entire array; no valid-prefix or numeric/semantic realignment, decoder repair or count rescue.

Primary object validity uses the declared KEY SET, not insertion order. Report actual full tag-before-label order separately; a reversed-key otherwise valid array remains primary valid with a key-order diagnostic. Order checks cannot mistake a partially inspected prefix for full conformance.

Completed HTTP200 sampled malformed/empty/nonarray/tool-only response is observed strict0, with zero aligned labels. HTTP error, absent/cancelled/unrun response or unavailable provenance is NULL. Keep raw observable strict endpoint separate from any provenance quarantine, so a physical mismatch is visible instead of silently rescued or discarded. Semantic errors in a valid array remain available wrong labels. Every slot retains failure subtype, first bad position/raw cardinality and exact raw path.

## Physical request and model identity

Authenticate frozen source/selection closure once, cache results and avoid repeated multi-GB weight hashing. Verify prior full-shard-hash observations through pinned manifests and unchanged size/mtime/inode; disclose that this is not a fresh full tensor byte audit. At terminal bind actual service/config/endpoint/model/version/release evidence, including no LoRA, BF16 eager,8192 context, prefix-cache disabled and Qwen3.5-only language_model_only/GDN alignment settings.

Join all96 expected coordinates to exact ordered wire body bytes and request hashes. Independently verify original record text and ID order, actual schema position tags/canonical enums/exact cardinality, six-order dispatch rotation, two seeds and per-model aliases. Same-model representation pairs intentionally differ in instructions/schema, not source records; cross-model token prefixes need NOT match. Confirm physical returned prompt-token IDs equal each frozen native-rendered reference, prompt/output usage lengths, actual alias, output token IDs and finish/reasoning/tool fields. Compare within-model source triples and cross-model request bodies modulo alias, acknowledging template differences. No native trace graph exists for these direct component calls, and tools are not executed.

All requests use temperature.5, top_p1, top_k−1, min_p0, repetition penalty1, presence/frequency0, max3072, explicit nonthinking. All complete inputs fit8192 with output reserve (prepared max7915); no crop. Report unexpected reasoning/truncation/protocol failures explicitly.

## Cost, lifecycle and report

Extract raw prompt/completion/total and cached counters, parsing string-encoded raw JSON when necessary. Missing cache is NULL, not assumed0 from configuration. Retain each attempted HTTP request, known token-cost coverage, duration, stop and response status; distinguish preflight from model requests and request-only/cancelled transport from verified completion. Sum concurrent call durations is not elapsed wall. Report total and jointly observed/valid paired costs; avoid architecture-speed claims.

Original failed attempt, explicit recovery amendment and actual parent/owned clocks are separate. Approved original caps:1800 outer/1770 owned/1650 work,600 collection/model, startup≤300 clipped,120 final cleanup reserve. Verify exact recovery caps/argv and whether cumulative earlier failure time is additive; do not invent a reset-adjusted total. Authenticate owned release evidence. Preserve the initial launcher_sha256/old __file__ failure and MAIN-released orphan evidence; no classification cost or failures attributed unless actual calls exist.

After terminal, run one bounded raw reconstruction pass, promptly report availability and primary task/model contrasts, then finalize ROW_LEDGER/METRICS/SOURCES/REPORT and content hashes. Only then crosscheck implementer projection and preserve discrepancies. Parser corrections must be additive and explain cause; primary endpoints and sealed method stay unchanged. Do not pool these fresh SST text units with old sentence cohorts, or claim component output correspondence proves RLM planning/general reasoning.
