# Additive recovery/audit implementation binding

The sealed METHOD and METHOD_INPUTS remain unchanged. MAIN assigned accepted operation `operations/2026-09-09-after-boundedrl-fresh96-recovery`, acceptance SHA `9a5e621940c95aa34e90158ae808283b9107f0379a2142306c2ec73a899dbe3b`, waiting parent4138703/start1077269993. The analysis does not poll that process. Recovery READY `97e505cdd5cc56418878432cd32bacbe35b2f8ce88c568e146b1f0d60619ef3f` changes the actual launcher/owner namespace, not the frozen original scientific96 requests, seeds, models, scoring or output paths. Actual outcome files remain unopened during preparation.

Read-only inspected recovery.py/owned.py/PLAN and ATTEMPT_RELATION. The new launcher sets its actual `__file__` before inherited main and rebinds suite.SERVE plus lifecycle c.ROLE together. Original4.307307s parent failure is separate: zero scientific client command, not a classification failure. The recovery has a separately authorized1800-second outer budget, not an erase/reset of that failed attempt. Final audit will bind original failure and recovery execution/release evidence separately. The first attempt's absent TERMINAL is preserved rather than invented.

`audit.py` reuses only the authenticated independent Qwen144 JSON parser, hash `1c228de1153e65671e592dd1d3d83ef8da3a94047ba12ac6ce2d6802b408eaef`. The new loop handles all96 planned slots, both lifecycle success and failed/partial stages, raw versus provenance-trusted endpoints, per-call input/token identity, context-paired bounds, costs, labels and physical wire capture. It does not import a scientific scorer. Stored scores are compared only after independent endpoints exist. Full source selection/parquet hashing was completed once during METHOD_INPUTS preparation; terminal pass verifies changed/new operation files and actual consumed artifacts without rehashing all model shards.

Five new fixtures were written first and failed because audit.py was absent. They then passed after implementation: raw-score independence and reversed-key diagnostic; completed empty0 versus unrunNULL; alias/prompt-ID quarantine retaining raw64-correct evidence; tool reply0 and unknown cache; absent-slot paired bounds. Nine inherited independent parser fixtures had already passed in the method freeze. Synthetic fixture probabilities/outputs never enter a research dataset or live client.

Audit code writes only this namespace. Execute only after MAIN terminal trigger:

```
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/rlm/bin/python /project/alex_phd/runs/rlm-research-r4/analyses/leaf-fresh-correspondence-live-2026-09-09/audit.py --main-terminal-trigger 'MAIN confirmed fresh96 recovery terminal; exact terminal message/time recorded here'
```

This command requires the accepted job EXIT marker; it does not wait, inspect GPU state, launch, signal, retry, alter admission, or write any scientific file. Any unforeseen raw-format adaptation will be recorded additively, preserving this implementation seal and the original method.
