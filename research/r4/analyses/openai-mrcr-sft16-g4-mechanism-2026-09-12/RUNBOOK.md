# Checkpoint16 paired G4 readout

This is a binding-only reuse of the reviewed cp32 native-to-trace scorer and official grading path. It imports the actual new cp16 collector and its real step16 alias, verifies the cp16 fixed binding, redecodes actual native token arrays under the same terminal hooks, and retains the same32/8G4 planned denominators, unavailable handling and actual-final-text checks. One actual archived cp16 HTTP/tool/final fixture exercises these exports and paired coordinate mapping. No model inference or generated-code execution is performed.

The cp32 baseline is its frozen independently raw-redecoded report (28/32 exact, all32 available, zero mixed groups). Pair cp16 against it by record ID, repeat and the unchanged seed/T.5, never by the newly namespaced coordinate ID. Retain correct/wrong/unavailable, gains/losses, first-action token changes, teacher-AST and clean-stdout transitions, plus costs and separate service/cache histories. Eight context units are not32 independent tasks. The cp16 condition was an adaptive midpoint choice made after cp32 collapse but before cp16 outputs, not original training-primary selection.

Do not infer a useful two-mask optimizer comparison solely from copy-related mixed rewards. The analyzer checks exact full first-turn native logprob-array equality alongside token-path identity: with the same initial HF policy, those first-action token-TIS RLOO terms cancel within a complete group. This is a cancellation candidate, not a computed HF gradient comparison. All-zero advantages remain NO_UPDATE; no optimizer is admitted.

MAIN or a later CPU task may run `CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python analyze.py check --output REPORT-001.json` after attempt-001 owner terminal. The command checks once and returns PENDING otherwise. No polling or GPU job is installed. T1.0 is independently analyzed by the other agent and is not rescored here.

