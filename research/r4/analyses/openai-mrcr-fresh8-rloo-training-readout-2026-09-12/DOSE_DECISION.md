---
status: prospective CPU assessment before new training readout
candidate: one fixed LR1e-4 step from original cp32 on the identical fresh8 native batch
comparison: accepted LR1e-5 step, not an additional step from its checkpoint
optimizer_authority: MAIN only; conditional and not launched here
---

The serving-precision audit rules out complete update disappearance: the fresh RLOO effective B@A change retains cosine0.913 after BF16 casting and over11million elements change BF16 value. It does not rule out small probability shifts, quantization perturbation or too-small behavioral dose. The exposed held32 and long16 are flat; one four-needle whitespace win is unreplicated.

A single10×learning-rate arm is the smallest dose screen if MAIN selects it after the training readout: same original cp32, exact frozen32 trajectories/seed, same12 nonzero finals and20 skipped trajectories, denominator32, RLOO/TIS2/clip1 unchanged, one fresh Adam step. Only LR changes from1e-5 to1e-4. It must not become repeated epochs, replay until success, a compatibility sweep or an automatic multi-update queue. Run the fixed training32 and a frozen held32 readout regardless training accuracy. The same exposed held contexts remain development evidence, not a new generalization claim.

If LR1e-5 already improves training while transfer is flat, higher dose tests stronger local fit against generalization cost—not failure to learn at all. If train behavior is flat,10×LR tests whether this boundary signal is behaviorally dose-limited. If train regresses, do not admit10×as an automatic remedy. An increased train score without held benefit remains copying overfit; increased malformed actions/retrieval failures warns of shared-weight interference. No positive result here establishes learning what/how much to delegate.

Because first-step Adam rescales coordinates, preclip gradient24.3 versus an earlier different-batch gradient0.0044 is not an effective learning-strength ratio. LR10× is a clearer recipe intervention than interpreting those norms. It may also cross more BF16 grid thresholds, so any behavioral difference is a served-checkpoint dose effect, not pure FP32 optimization causality. Fixed LR1e-4 and LR1e-5 are two declared arms, not best-checkpoint selection.

The primary remaining scientific weakness is reward support: uniform-zero selector-failure groups provide no relative-reward signal, and the three mixed groups mostly contrast final whitespace. More copying dose cannot establish retrieval or delegation learning. A later global-reward report task needs actual feasible report contrast and fresh root splits; no such optimizer is implemented here.
