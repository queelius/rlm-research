# Fixed-RL decoding repeat: one-shot analysis

Both new owners must finish before analysis. Run native Python on analyze.py
check with CUDA_VISIBLE_DEVICES empty. Neither old32 control generation nor
new32 updated generation is substituted with a prior result. Both models use
the same32 new seeds on16 original contexts. This is not a training-seed repeat.

The source is the reviewed original fixed-baseline RL native audit, with one
explicit arm-directory replacement and new study bindings. Raw token parsing,
actual native response conversion, unchanged grading, causal mapping, exact
initial prefixes, failures, physical costs and paired context accounting reuse
that tested code. No new claim of an independently reimplemented mapper/parser.
One focused test rejects wrong arm paths and verifies all new paired seeds;
source and inherited closures are hashed. No generated program is executed.

The saved original training cost appears only as checkpoint context; this run
has no new optimizer step. Unknowns remain distinct from wrong answers. Output
RESULTS.json/REPORT.md is write-once. A pending check does not poll or score
partial outputs. MAIN verifies CPU_READY before execution.
