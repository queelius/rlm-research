# Fresh512: fixed readout and conditional next decision

Use the sealed `helper-agnews-fresh512-eval-v1/compare.py` result as the only scorer. The three predeclared comparisons remain c32→RL8, c32→SFT8 and RL8→SFT8, using512 records in128 shared B4 request clusters. Require source/endpoint/runtime/release qualification and complete predictions for primary accuracy. Preserve malformed, transport/deadline and unattempted cases separately. If the fixed RL dose aborts, compare only MAIN-authorized completed SFT/c32 endpoints and report RL training as aborted—not an evaluation scored wrong.

From the scorer's saved predictions, gold labels and frozen group membership, add exact label-churn diagnostics without another model call: changed IDs/count; gains and losses by gold class; wrong→different-wrong transitions; predicted-category transitions; and the128-group distribution of positive/negative/zero net correct changes. Reconcile changed labels as gains + losses + wrong→different-wrong. Report changed groups, neutral-churn groups, incomplete groups and the5×5 table of baseline0–4 correct versus updated0–4 correct. Use the existing descriptive128-cluster interval, not an independent512-item p-value. These are views of one paired result, not new selectable metrics.

Report physical cost and phase boundaries. RL native prompt/completion/cached-token usage, native concurrent call span, HF duration, full step-owner duration and full-dose owner duration are separate fields; do not add overlapping durations or confuse the sum of four concurrent requests with elapsed time. SFT has no native training calls: its observed training224.440s and owner236.254s are already available, with256 teacher maps/1024 unique records/20591 supervised tokens. For each final arm report its own128 physical calls, reported token subtotals, unknown usage count, serial call duration and full owner duration. Charge the reusable c32 baseline physically once. Queue wait and source-verification overhead must not be relabeled as measured GPU compute. Differences in sampled-action exposure, objective and compute mean this is an exploratory RL-versus-SFT package/control comparison, not a compute-matched superiority test.

At13:29 UTC the CPU watcher had qualified RL steps1–5: training label decisions434,398,434,421,432 of512; mixed groups7,4,4,6,7; ESS110.518,126.151,122.281,123.275,124.958; every step's128 exact replay checks passed. Parent-relative adapter deltas were0.04021,0.02931,0.02565,0.02309,0.02298. These are different fixed128 training records per step, not a matched accuracy learning curve. Five-step owner durations sum1262.302s and nested HF durations384.627s; do not add them. Track contrast/ESS/delta jointly to describe signal and update scale, without selecting a checkpoint or inferring a heldout trend.

Decision branches are fixed before the readout. A qualification failure stops a primary claim without relaxing gates. Tiny or uncertainty-compatible net changes establish no gain; do not automatically extend dose. RL improvement over both controls would motivate an independent fresh-seed fixed-dose replication, not immediate confirmation. SFT matching/exceeding RL makes the ordinary supervised data-signal control and measured cost central. Negative results retain the losses and their class/group localization; no intermediate-checkpoint rescue.

If both completed trained arms change at most5 labels each relative to c32 (an exploratory roughly1% churn screen, not a statistical threshold), the smallest informative proposed follow-up is a teacher-forced likelihood diagnostic on16 B4 groups/64 records selected by a new fixed label-blind hash order over all128 call IDs. Selection must ignore correctness, disagreements and losses. Keep the original prompts, order, four-label schema and fixed endpoints. Score each host-gold compact JSON map as an output suffix; do not put gold answers into prompt instructions and do not update weights.

Proposed shape: one HF base-model load, one active exact-hash adapter at a time, eval/dropout-off/no gradients,16 full-sequence forwards per endpoint (48 for c32/RL8/SFT8), existing ordered-XGrammar gold-path support masks and one common T1 diagnostic normalization. Every gold token/EOS must have valid support; no permissive fallback. Primary diagnostic is paired group-level change in constrained gold-sequence NLL. Gold-label-token NLL with verified offsets is secondary and structure is reported separately. Any grammar/token-boundary integration must be CPU-qualified before MAIN considers launch. Proposed caps300/360s are unmeasured estimates, not admission.

Lower NLL with unchanged greedy outputs would show a small gold-likelihood shift on that fixed subset. Higher NLL would show wrong direction under this teacher-forced metric. Earlier gold labels are forced, and HF likelihood is not independently established native T0 calibration; neither result replaces the512 accuracy table or proves a global mechanism. Near-zero/mixed shifts are inconclusive. No adaptive subset enlargement, best checkpoint, new primary endpoint, or automatic follow-up implementation is authorized here.

Both existing CPU watchers remain live (PIDs175489/175496), with no audit-failure receipt at this check. They retain four-hour caps and30-second polling. MAIN remains the only GPU launcher.
