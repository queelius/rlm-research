# Independent free-ID attempt003 audit method

Frozen before reading any actual response content. MAIN reported95/96 retained records at19:43 and no response-content inspection. Scope is leaf-free-id-correspondence-v1 outputs/attempt-003; inspect outcomes only after OWNER_TERMINAL and rollout/STATUS exist and owner release is confirmed. Original failures, source versions and all prior seals remain unchanged. This auditor did not implement this factorial or its V2 scoring.

Authenticate READY_RECOVERY3, its original READY/READY_V2/source/input closure, fixed SPEC/DATA/REQUESTS/PROMPT_IDS, runtime descriptor/base identity and parent terminal. Pin raw outcome files before parsing responses. Verify96 planned requests:8 exposed AG News/SST-2 contexts ×2 original seeds ×3 arms(matching ID objects,constant tag objects,plain labels) ×2 decoders(free/exact). Verify every free/exact same-arm/context/seed body is identical except structured_outputs removal, including order-sensitive serialized bytes, and every actual HTTP wire body equals its frozen request. Check native rendered prompt IDs, actual token IDs/content, finish reasons, tools, prompt/output/cache usage, response model and request caps from raw captures. No generated-code execution or model/GPU/service operations.

Primary is V2 full-contract-valid correct labels on PLANNED denominator64/call,512/dataset×arm×decoder cell. Matching requires64 ordered objects with exactly tag then label, canonical labels, exact source IDs at their literal positions and no missing/extra/duplicate IDs. Constant requires64 such objects with every tag p0000. Plain requires64 canonical labels in position. Completed malformed/contract-invalid policy outputs contribute zero, including correct labels with unusable IDs. Infrastructure missing/unrun remains NULL with explicit0–64 bounds. Integrity errors are separately flagged; never silently promote a response with mismatched prompt/native binding. No regex/JSON repair, sorting IDs, positional relabeling or gold-conditioned output selection.

Independent scoring will separately retain shape validity, ordered fields, positional label correctness conditional on all64 canonical labels, ID copying/position coverage/duplication/missing/extra, and diagnostics for parsed wrong-cardinality arrays without converting them into primary success. Native output length/EOS and textual JSON validity are separate. Compare to collection V2 scores only after independent scores exist; preserve disagreements as evidence.

Report all12 dataset×arm×decoder cells, planned/observed/full-shape/full-contract rates, correct-label counts and NULL bounds. For48 same-arm free/exact pairs report paired correct-label/validity differences, wins/losses/ties/NULL and aggregate seeds within each of8 context groups. Cross-arm comparisons are descriptive: freely emitting64 distinct IDs is not equally difficult/costly as repeating one constant. Never call two seeds independent new contexts or exposed source panels a fresh replication. Inspect actual free ID errors and whether positional-label gains survive failures of their mapping contract. Report complete-output correctness separately if useful.

Cost: each physical request counted once, including failed/request-only outcomes; aggregate input/output/cached/uncached known sums and unknown counts, actual model completions and summed latencies separately from parent/service wall/startup/cleanup. No retries or replacement sampling. Explicitly separate attempt003 science from prior failed startup/namespace attempts; retain their hashes/cost evidence but do not mix their NULLs into the96-science denominator.

Decision support: if free matching preserves contract and paired gains, propose fresh-ID/data/length-held-out validation before any generalization claim. If matching positional accuracy survives but contract validity collapses, grammar/source-pointer or copy-alignment training is the leading bottleneck; require disjoint data/ID namespaces and report both contract and semantic accuracy. If exact alone gains, preserve decoder dependence rather than claiming free source-correspondence ability. If infrastructure dominates, report bounds and prioritize an additive scientifically matched completion only with new authority. No training/grammar fix is implemented by this audit.
