# Contract × predicted-evidence24: independent audit

Compact role alone improves strict formatting, not semantic task success: all six compact/raw roots execute a wrong classification rule and return zero. Compact role plus the one valid predicted map supports two actual scoped reductions, while its third root produces an unfinished action with invented records. This is a narrow conditional finding in **one exposed context**, not a role-only rescue, broad operator competence, or a reason to select a new training role automatically.

## Complete planned inventory

All24 roots =two exposed QSR training contexts ×three operator/scope queries ×two root roles ×two evidence conditions. Same low66c root and frozen c32 child; six paired seed blocks nested in two contexts. All-scope clarification is common across all four arms, so historical QSR percentages are not a causal baseline and its wording effect is not isolated.

| Role/evidence | Correct / planned | Native finals | Format-valid | Valid wrong | Malformed | NULL | Missing-outcome bounds |
|---|---:|---:|---:|---:|---:|---:|---:|
| Native/raw | 1/6 | 5 | 1 | 0 | 4 | 1 | 1–2/6 |
| Native/map | 0/6 | 3 | 1 | 1 | 2 | 3 | 0–3/6 |
| Compact/raw | 0/6 | 6 | 6 | 6 | 0 | 0 | 0–0/6 |
| Compact/map | 2/6 | 2 | 2 | 0 | 0 | 4 | 2–6/6 |

All original scientist scores agree with the independent rescore. Correct/planned is an operational sensitivity, not zero-imputed primary accuracy; available-case accuracy is20%,0%,0%,100% on different availability sets. The last figure means only two endpoints, not a general ceiling. Six NULLs share one source acquisition failure; two more lack native finals. Strict whole-strip `Answer: N` is unchanged: verbose or code-fenced correct numbers are observed invalid0, not salvaged. All six distinct task gold answers are nonzero:1,1,2,4,1,19.

## Actual source acquisition

Both final-only free-JSON source requests returned authentic stop completions from c32. Neither used tools or a structured decoder. Input/body/model, actual tokenizer prefix, completion IDs, decoded text and usage agree with the frozen requests.

- training-00: exact16-ID map,15/16 dataset labels correct. q9e5136fd97e2 is predicted entity instead of description/abstract. This error is irrelevant to all three human-being queries: map-implied answers1,1,2 equal dataset truth. The actual raw map, including the wrong label, was shared unchanged across both roles and three queries.
- training-01: physically returned JSON contains15/16 IDs, omitting q5ec644e47787. This is **observed structurally invalid source output**, not missing transport. Two retained labels also disagree with dataset truth. Its six map roots remain preplanned source-gated NULL, with zero root dispatch; raw counterparts still ran. No source repair, partial-map scoring or reacquisition was performed.

Consequently the map interaction is identifiable only descriptively on training-00. Per-context correct/native-available out3, native/raw→native/map→compact/raw→compact/map: training-00 is1/3→0/3→0/3→2/2; training-01 is0/2→0/0→0/3→0/0.

Within the five raw pairs with both finals, compact has0 wins/1 loss/4 ties, one incomplete pair. Within the two available map-role pairs, compact has2 wins/0 losses, four incomplete. Evidence under native role has0 wins/1 loss/2 ties among three complete pairs; evidence under compact has2 wins/0 losses among two. Only count-single and weight-union blocks have all four finals; their descriptive interactions are+2 and+1 in binary-score units. These are two queries in ONE context, not independent interaction replications. Exact six-block NULL-preserving contrasts are in RAW_AUDIT.json.

## Execution, not just answer agreement

Every one of68 native physical transports is a ROOT request. There are **zero optional native child calls** in all four arms, despite unchanged optional typed child capability. The two separate source calls are not root-initiated acquisition. Actual programs and parent+tool-ID-matched observations were inspected for all18 dispatched roots; sampled code was never executed by the auditor. Full per-coordinate mechanism and action/observation hashes are in SUPPLEMENT.json.

Compact/raw executes all three operators, but on wrong evidence: training-00 searches original question text for the literal phrase `human being`; training-01 tests an absent category field, exact text `entity`, or an empty category default. It prints0 and stops with correct formatting in all six cases. One distinct-user action also drops its requested scope. This is superficial operator execution, not semantic classification/reduction.

Compact/map count (`156717…`) reads real records, filters actual u1, transcribes the inline category map and executes a count that prints1, then strict1. It mistypes non-target, in-scope q3efb121356a2 as qefb121356a2; target membership and answer are correct, but the full transcription is not faithful. Compact/map weight (`43927f…`) faithfully transcribes all16 predictions, joins IDs to the actual u1/u2 records and sums their actual visible weights, prints2 and stops strict2. These are genuine scoped **literal-observation reductions**, not hidden gold or live acquired-map state.

Compact/map distinct (`099582…`) instead writes invented example records with altered users/text/weights into a2048-token tool envelope. It is truncated before a valid call can be decoded: one authentic physical length completion, zero executed tools and no final. This is a capped malformed action, not a transport failure or a completed wrong answer.

Native/raw's sole strict success (`f24d3f…`) executes a lexical person-question heuristic over actual u1 records and counts1. Native/raw weight-union and distinct-single manually reach the correct number in verbose/code-fenced finals but fail strict format; neither executes the semantic reduction. Other manual classifications/sums are wrong.

Native/map does not demonstrate faithful evidence reduction: count loops through bash/f-string errors and ends in an unexecuted code proposal; distinct writes the map to a file but assumes the user assignments instead of reading/joining them; weight writes over context.txt and submits Python under bash, failing before the sum, then guesses13. Mentioned code, written maps and final scalar agreement are not execution.

The other native NULL (`33721c…`, native/raw count-all) follows jq failure and repeated Python-under-bash syntax errors. Its24th action lacks a returned observation; the retained executor reports a shell-message queue failure. It is not an authenticated final. There are no unseen-return imputations or reruns.

## Cost and verification

| Cell | Physical root requests / returns | Input tokens | Output tokens |
|---|---:|---:|---:|
| Native/raw | 38/38 | 124,353 | 4,362 |
| Native/map | 13/13 | 29,911 | 2,432 |
| Compact/raw | 12/12 | 9,996 | 1,412 |
| Compact/map | 5/5 | 6,042 | 3,110 |

Two unique sources add1,417 input and485 output tokens. Physical total70 requests/70 returned completions,171,719 input and11,801 output tokens; reported cached prompt tokens151,696, with no unknown usage. All ledger totals reconcile. The generic raw helper's170,302 input-native figure excludes message-only source API requests; SUPPLEMENT explicitly restores their independently verified1,417 prompt tokens. Billing and equal-FLOP claims remain unsupported.

Hypothetical independent pipelines charge the full source acquisition once per map endpoint, including the six source-gated endpoints:12 hypothetical acquisitions versus two physical. Summed hypothetical totals are80 requests,178,804 input and14,226 output tokens; these are NOT new physical work. Actual and hypothetical per-endpoint costs are retained separately.

OWNER_TERMINAL is complete/error-null, all owned identities exited and ports free; parent exit0/GPU empty, owner240.185s and outer240.645s. Exact original source/output and operational hashes are sealed; no source or run outputs were modified. Root2048/context8192, source1536 and declared sampling remain unchanged. Actual binding, descriptor/base, live alias cards, first native messages/tools/tokens, setup file bytes, role-only routing, all graph token branches, decoded finals and outstanding-tool checks pass. No child was called, so typed capability is verified by frozen source/qualification, not claimed as newly exercised transport.

Method/PARSER_READY were frozen before outcomes. PARSER_AMENDMENT documents an additive CE-record schema adapter: CE records carry role-map hash and alias, not transfer's per-call adapter-hash field; actual binding/descriptor/live-model evidence is checked rather than inventing it. Original parser preserved. Four focused source/native-branch tests passed, including actual-record wrong-binding and unresolved-tool regressions.

The auditor did not author CE, but contributed reused lifecycle/native and earlier QSR/joint components; this is independent study rescoring, not an independently built stack. Both contexts were exposed QSR training inputs; there is no data-generalization claim.

## Decision

Preserve the conditional evidence-use signal, the source correspondence failure and raw semantic-acquisition failure separately. The study does not justify choosing compact as a universal training role: it removes verbose formatting while strengthening six wrong zero routines. Await the separately prepared fixed-start/reference calibration before selecting a common policy/role. If further training is warranted, target genuine acquisition→query-sensitive evidence reduction→stop across diverse states, not another terminal-copy dose; include free execution and source completeness. No new training is authorized by this audit.
