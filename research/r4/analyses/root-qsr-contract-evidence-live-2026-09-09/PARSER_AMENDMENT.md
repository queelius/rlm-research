# Additive actual CE record-schema qualification

Written after the two source outputs and native record keys were inspected, before root outcome scoring. Original METHOD/PARSER_READY/audit.py are preserved. CE intentionally records actual_alias and canonical role_map_sha256, not transfer's per-call model_sha256. The original shared authenticator would raise KeyError rather than authenticate any CE root. The additive native_auth.py checks actual service BINDING equals frozen BINDING, descriptor adapter/base and live-model cards, role-map canonical hash, routed wire alias, exact native graph and prompt/completion identity, unique physical return, decoded final text and parent+tool-ID-resolved final branch. It does not fabricate a per-call hash. Native-final criteria, strict scoring, NULL denominators and input selection are unchanged. audit_v2.py only selects this adapter.

The final supplement will also separate source API prompt-token vectors (available in raw prompt_token_ids) from native root/child input vectors, and charge both physical sources once versus each source once per hypothetical map endpoint. The frozen generic cost helper does not infer input token IDs from a message-only source request; its native-input total is therefore not the complete cross-interface input total. Actual usage remains retained. This is accounting disclosure, not a changed treatment.
