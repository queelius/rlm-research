# AG News identity × weight: independent prospective audit method

Frozen 2026-09-09 before this auditor reads any AG96 model outcomes. This is an audit specification, not an outcome report or launch approval. The auditor authored neither this study nor its scorer. Implementation of the independent projection is deferred until the parent's terminal trigger. No GPU, model, service, process-signal, source mutation, broad test run, or episode replacement is authorized here.

## Inputs, authentication, and terminal gate

Research root: `/project/alex_phd/runs/rlm-research-r4`. Scientific source: `sidecars/leaf-identity-agnews-weight-v1`. READY SHA256 `8c4e037ab93e406246c0198b27d5102b04d72c1f6eb741696addeb333e0333e8`; SPEC SHA256 `052926cdb4b64cb38863241565bce8cf70f312fdf8922d383017075d3c1fc876`. `SOURCE_CACHE.json` records a single bounded authentication of 133 named source/input/weight/qualification/READY files, sharing reads with the cue-order audit. Historical outcome-bearing SELECTION and synthesis pins were deliberately not read; their expected hashes are retained as exclusions. Both actual adapter files were authenticated. The base-model manifest is authenticated; this is not a fresh rehash of every unlisted base shard.

Operation `operations/2026-09-09-after-child-role16-agnews96`: PLAN, ACCEPTANCE, handoff and review hashes are cached. The exact predecessor is child-role16 parent PID 3000046/start ticks 1074613864; the operation owns waiting, not this audit. Before reading raw outcomes, require the parent's terminal trigger and inspect finalized scientific `outputs/attempt-001/STATUS.json`, owned `owned/attempt-001/FINISH.json` and release record, plus operation EXIT/TERMINAL evidence. Terminal failure is eligible for a failure audit, not a complete-result claim. If still mutable, report pending and return; do not poll. Hash each finalized new artifact once while reading and reuse the source cache; authenticate small launch bindings and disclose discrepancies rather than rerun heavyweight verifiers per call.

## Frozen population and source provenance

The complete Cartesian grid is 4 AG test contexts × 2 fresh sampling seeds × 2 source prefixes × 2 fixed weights × 3 tag rules = 96 distinct planned requests, each with 64 records: 6,144 planned record assignments. Twelve weight/prefix/tag cells each contain 8 calls and 512 assignments. There are only four context clusters, not 96 independent samples or 6,144 independent observations. Seeds are 981275411 and 981275421; master 981275401. Retain both seeds, all four contexts, both q/p prefixes and every arm regardless of result.

Source is `fancyzhx/ag_news` test split at revision `eb185aade064a813bc0b7f42de02595523103ca4`. Independently reconstruct the frozen host-gold join from the authenticated Parquet: NFKC → casefold → whitespace-collapse normalized text, SHA256 group identity, all 7,600 source rows, then the first 256 lexically sorted unique group hashes partitioned into four consecutive blocks of 64. Check duplicate/conflicting labels, selected-group digest, original source row indices, exact full text and mapping 0/1/2/3 to World/Sports/Business/Sci/Tech. Reconstruct declared display permutations and disjoint numeric source IDs using the published deterministic hash sort; compare DATA and SPEC batches, not just their stored hashes. No semantic/outcome-based reselection. Verify all 256 group IDs are unique across these four clusters and state the bounded freshness evidence. Do not infer absence from pretraining, historic news overlap, or semantic near-duplicates. Underlying data license remains UNKNOWN; research-use wording is not a permissive grant.

Fixed adapters: original `857a7ce6907c759a8c1b478eb53029d3b700b81c3094d8edb3394e52def9fcb6` and historical old SFT `c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3`. Base Qwen3-4B-Instruct-2507 revision `cdbee75f17c01a7cc42f958dc650907174af0554`. No current-root or AG-outcome checkpoint choice. Validate runtime binding/config hashes, actual endpoint descriptors, server models preflight and response model aliases against each coordinate. Original alias is `strict-rlm-qwen3-4b-role-original-v1`; old alias is `strict-rlm-qwen3-4b-role-sft-selected-v1`.

## Independent request and raw-output reconstruction

Build the complete coordinate ledger from the Cartesian factors and frozen dispatch rule, checking IDs/batch indices/start=0/size=64/permutation=0/number_namespace=disjoint. Independently join every raw `calls/<id>.json` and `wire/<id>.json`; unexpected or duplicate coordinates are protocol failures, never silently deduplicated. Raw `raw_response_text` is authoritative; parse it independently, cross-check the saved parsed envelope, and extract choice 0/message content, tool calls, finish reason, token IDs, response alias and usage. Do not import the author's score or summarize function as the source of truth. Compare those stored projections only after independent scoring.

For each POST, verify exact captured `body_utf8` and its SHA, request/coordinate identity, model alias, seed, sampling controls, max_tokens=3072, tools, messages, grammar, and return-token-ID flags against frozen requests. Reconstruct the visible ID→full-text mapping in display order and ensure no host-only labels/source-row metadata leaked into the request. Meaningful tags equal each displayed source ID; ordinal tags are the opposite prefix followed by 0001..0064 in displayed order; constant tags are opposite-prefix 0000 repeated 64 times. Check all schema prefixItems, canonical-label enums, const tags, exact cardinality, two properties/no extras and tag→label physical order. q/p numeric namespaces exclude suffix collisions with 0001..0064 as designed.

Within all 48 adjacent weight pairs, require bodies identical except model alias and complete physical input token-ID arrays equal. Within each of the 32 weight/context/seed/prefix tag triples, require identical ID-bearing input, system message and tools; instruction and grammar intentionally differ. Prefix variants preserve text, numeric IDs and display order, with only declared source/tag-prefix changes. Reconstruct the typed native prompt once per unique body if a discrepancy requires it; otherwise compare the stored full PROMPT_IDS arrays directly with actual returned `prompt_token_ids`, not only hashes or lengths. Check input-token usage equals array length and output IDs are present; retain output token IDs and finish/length flags without coercing text repairs. CPU-qualified maxima (4,707 input + 3,072 output ≤ 8,192) are limits, not realized compute equality. Equal standalone tag lengths do not imply equal full prompts, cache state, generation work or latency.

Parse content with a duplicate-key-rejecting JSON object-pairs hook and nonfinite-value rejection. Require a top-level array of exactly 64 objects, each with exactly tag/label string fields, the specified per-position tag, and a canonical AG label. No code-fence stripping, numeric-ID alignment, sorting, label normalization, majority repair or answer fallback. Independently record every object's actual key order and conformance to the qualified decoder. The inherited AG strict scorer itself treats the two property keys as a set; therefore report any physical key-order violation separately and show its impact rather than silently introduce a new primary score rule. For conforming cases both checks agree.

## Denominators and estimands

Report a mutually exclusive ledger of unstarted/unrun, attempted-without-valid-response (timeout/cancellation/HTTP/provider/alias/provenance failures), completed response with invalid schema/tool response/nonstring content, and completed valid response. Preserve unanswered/length-stopped responses and their raw bytes separately. Missing data are null, not observed wrong labels. A complete malformed response has frozen strict accuracy 0/64 and whole-call success 0, but no aligned semantic accuracy. A valid response has `correct/64` against DISPLAYED-record gold, whole64 success, and a 4×4 gold→predicted confusion matrix. If an envelope/provenance fault makes an otherwise readable answer scientifically unauthenticated, retain its raw observable score descriptively and quarantine it from the validated comparison with an explicit reason. This must not become silent exclusion of difficult examples.

Show all 96 planned calls and all 6,144 planned assignments, attempted/returned/schema-valid/provenance-valid denominators, observed strict accuracy, conditional aligned accuracy, and conservative all-planned verified-success coverage distinctly. For unobserved calls also give attainable strict-accuracy bounds (missing 0..64); do not present lower-bound coverage as measured accuracy. Never average null as zero. Report exact missing-usage counts, not sums with hidden zero imputation.

Primary: meaningful-minus-ordinal displayed strict accuracy within each fixed weight × source prefix, and old-minus-original difference in that effect. Compute paired differences within each context/seed/prefix; a difference requires both responses observed and authenticated, and the weight interaction requires all four. Report complete-pair counts, jointly schema-valid pair counts and every context/seed difference. Average nested seeds within context before giving equal-context means/ranges; present raw assignment totals alongside, without pseudo-independent uncertainty. A second aligned-only contrast is explicitly conditional and cannot replace primary strict scoring. Give q/p effects and their interaction without selecting the better prefix.

Secondary: ordinal-minus-constant and meaningful-minus-constant; old-minus-original by arm; whole64 success; schema/tag/key-order violations; confusion/class support; per-call predicted-minus-gold class count vector and L1; aggregate signed error separately from sum of per-call L1 to expose cancellation. AG numeric-source remapping is not a planned diagnostic. No new rescued accuracy or permutation search.

Use declared dispatch_order, cell_order, weight_order and actual start/end times to reconstruct balanced adjacency, concurrency and seed-specific first-weight exposure. Tabulate the primary paired effects and costs by weight-first order and seed; with only four clusters these order interactions are descriptive and cannot separate every scheduling/cache effect. Four workers balance starts, not necessarily completions or realized cache conditions.

## Cost, interpretation, publication

Count actual captured POST attempts and returned model responses separately from planned coordinates, wire-hook invocations and `/version`/`models` preflight GETs. `model_called=true` is a local attempt flag, not proof that a provider executed or returned a response. Reconcile raw usage logical input, cached input (only if actually reported), uncached input, completion, full ID lengths, per-call durations, overlap-aware collection wall time, service startup/release and owned/operation elapsed. Missing or ambiguous physical work remains unknown; no retry or repair call is initiated by this audit. Study client has no episode retries and stops after first error; verify actual artifacts for extras rather than assuming 96 physical attempts.

Caps: collection 900 s, shared work 1,080 s, inclusive owned 1,200 s with 120 s cleanup reserve, outer 1,230 s; 4 workers, 120 s request timeout, temperature .5/top_p 1/top_k -1/min_p 0. Compare actual elapsed to the original clock without phase resets. Do not add concurrent call durations to represent wall time or interpret token counts as exact GPU FLOPs.

Publish one bounded independent raw audit ledger, artifact manifest and plain-language REPORT after terminal; preserve raw failures and reconcile all deviations from stored analysis.json. Explain this as a four-cluster exploratory AG component screen of a tag-instruction-plus-grammar package and fixed historical weight contrast. It is neither an attention mechanism proof, randomized SFT treatment effect, independent training-seed replication, whole-RLM improvement nor new confirmatory holdout result. Further diagnostics require an additive labeled post-hoc method; do not revise this method after outcomes.
