# Prospective analysis: native interface SFT

MAIN wrote this before training or readout outcomes, after exact launch acceptance. Compare fixed initial473210 with fixedfinal4 from32authored examples, commonc32de typed child and local-runtime READY77df23b8. This is a small interface/output-competence intervention, not complete planning demonstrations. Evaluation data are the exact896-group root-excluded/leaf-training-supported allocation; source-test novelty and base-pretraining exclusion are not claimed.

Reconstruct all48 planned root episodes from frozen EVAL_PLAN_FINAL, PUBLIC and host gold. Report validation8pairs, query-transfer8pairs and length-transfer8pairs separately, grouped by source context and with repeated seeds nested. Endpoint is unchanged whole ASCII Answer: N: completed observable empty/malformed/wrong0; unavailable provider/setup/incomplete captureNULL. Preserve planned denominator, coverage and unknown binary gain bounds; report paired gains/losses/ties only where both endpoints are observed. No best-checkpoint selection, score repair, outcome filtering or numeric-answer-only primary. Gold-count skew and constant-count baselines are descriptive, not selection rules.

Authenticate exact32 current-action label masks/EOS,64exposures/four updates, starting checkpoint/fresh Adam, bf16 frozen base/FP32 LoRA, saved Adam/RNG/cursor and adapter delta. Report optimizer/checkpoint and total train/service/runtime/collection time separately. Helper model is never trained. Target-token weighting puts only83/1603 per-epoch target tokens on final answers; record this limitation rather than treating examples as equally weighted.

For actual readouts bind first full physical root token IDs to frozen prefixes; root model aliases/adapter/config must match assigned phase and child remain fixed. Root grammar must be absent. Eligible typed child decisions/native wire schemas must match the public batch source catalog and same common runtime/image; source-bound valid formatting is not semantic correctness. Equal inputs/seeds do not make adaptive trajectories deterministic.

Secondary trace readout: valid first Python request, actual descendant calls, requested/returned IDs and complete-map coverage, correct label assignments against host gold, metadata filtering, and whether returned observations are consumed. Separate scripted training targets, observed sampled policy behavior, direct root-only shortcuts and unobserved mechanism; never infer helper origin from arbitrary text. Use authenticated native ancestry. No generated code executes on host.

Union physical request IDs count root/child/cached/uncached/output usage once, retain retries/errors/missing usage, include failed and censored work. Both-phase relativecost on successful paired endpoints is distinct from all-planned cost; a cheap failure is not an efficiency win. SFT readout author is leaf; any implementer projection must be labeled and primary independently checked. Source manifests, raw row/call ledger, METRICS and plain-language REPORT live here, after owned service release. No CPU audit holds GPU ownership.
