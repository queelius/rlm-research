# Sparse144 independent audit method

Written after scientific completion but before this auditor reads any sparse144 outcomes. The144-call scientific schedule, source contexts, prompts, grammars and metrics were already frozen before inference. This is a postterminal/pre-audit method, not a new prospective experiment. Auditor did not author sparse-anchor study/collector; inherited component/native framework is shared with earlier work. No result-based selection, source/output edits, model calls or GPU/service access.

Authenticate sparse READY/SPEC/input/source closure once, then retain the hash cache. Verify scientific STATUS/owned FINISH/service release and operation exit if present. Actual output is `sidecars/leaf-sparse-anchor-v1/outputs/attempt-001`; ownership records are `owned/attempt-001`. Read each raw request/response ledger in one bounded pass, preserving raw-path/hash bindings. Never use saved score summaries as primary evidence. Do not regenerate scientific analysis/output files.

Population144 physical calls/9,216 planned labels:12 exposed64-record contexts (four each TREC/SST2/AG News)×two common sampling seeds981296011/981296021×cadence1/4/16×matching/constant tags. Source IDs/presentation are the exact first prior disjoint-q namespace presentation; compare group IDs, text, labels and input order against frozen parent-source crosswalks. Four task-specific source clusters, nested seeds/cadences/arms—not144 independent data points. No new-source/pretraining-held-out claim; preserve provenance/license limits.

Primary: matching minus constant strict correct displayed assignments within each cadence, tasks separate, including all64 positions. Each task/cadence/arm has8 calls/512 planned assignments. Report planned/observed/null and fully schema-valid calls separately; complete observed malformed/schema-invalid output is0/64, infrastructure/provenance/unrun isNULL. Report observed-correct/observed-labels and all-planned successes with coverage rather than silently treating null as observed error. Whole64 success requires valid schema and all64 correct. Paired context/seed deltas require both observed; separately show joint validity, wins/losses/ties, and context-level totals. Primary is not prefix accuracy or only-anchor accuracy.

Independently decode raw content with duplicate-key/nonfinite rejection. Require an array of exactly64 items; at positions1,1+k,… require an object with exactly keys tag then label in that order, matching exact displayed source ID or constant p0000 as assigned. At nonanchors require a bare canonical label string. Reject extra/missing keys/items, wrong tags, wrong types, wrong key order, noncanonical labels, prose/code fences/trailing data. No coercion, trimming inside labels, source-ID numeric realignment, answer repair or prefix rescue. Invalid output contributes no aligned labels under the frozen strict scoring. The stored auxiliary `anchor_key_order_valid` can describe only a prefix examined before a later failure; it does NOT imply complete schema validity. Record its relationship to independently derived full schema status and failure position.

For valid arrays, compare every displayed label to the host gold for that exact source group. Report semantic correctness separately from representation/grammar failure; per-label confusion, whole-array success, class-count signed errors and count-vector L1. Count-L1 is defined only for fully valid arrays and does not establish positional correspondence; repeated class labels and offsetting errors remain caveats. Preserve call/context/seed/cadence-level records and summarize by task. No rescued semantic score for malformed arrays.

Per-position diagnostic: distance since last anchor `(position−1)%k`, with planned/observed/valid-aligned denominators and correct assignments per distance; separate anchors(distance0) from unanchored strings. This is descriptive and conditional when excluding invalid outputs; no causal attention or decay claim. Compare same input position across matching/constant and cadences where observable; report context consistency, not independent significance for repeated positions.

Physical audit: request coordinates/alias/seed/sampling and ordered body/schema digests must match frozen144 requests; complete returned prompt token IDs must equal frozen PROMPT_IDS where present. Verify input text/system/tools equivalence within each cadence pair and intentional tag-rule/schema differences. Across cadences output representation/prompt length/token work changes, so accuracy-versus-output-token Pareto is not a fixed-compute or pure-attention effect. Actual matching and constant tags also differ lexically (q IDs versus p0000), retained as design scope. Temperature.5/top_p1/top_k−1/min_p0/max3072, fixedc32de child alias/descriptor, no tool execution or followup turns. Record finish/length stops, errors, duplicate/missing records and exact dispatch/call order without assuming deterministic paired outputs.

Cost: count every captured physical attempt once, including invalid outputs/errors and any request-only evidence; union aliases/token IDs/raw response usage. Decode nested/raw JSON-string usage where needed: cached/uncached fields may be lost by normalized usage. Report known totals and unknown-call denominators for physical prompt/cached/uncached/completion, plus elapsed-call and owned wall time separately. Compare matching/constant within cadence and dense/sparse matching across cadence; no token-to-FLOP assertion and no cheap-failure efficiency claim. Cross-cadence tradeoff should retain all-planned work and joint-valid subsets separately.

Publish early independently recomputed primary/cost ledger, then METRICS.json/REPORT.md/provenance SEAL in this analysis namespace only. Keep original scientific scores untouched and flag discrepancies. No broad tests, repeated checkpoint hashing, runtime fixture or GPU lock. Main reviews the independent result and decides subsequent research.
