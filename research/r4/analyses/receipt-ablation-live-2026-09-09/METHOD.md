# Frozen receipt72 audit method

September9,2026. Frozen before reading any receipt-study outcome. Scientific
SPEC90000411c23cc92ad1ed21906cd30a5687bfa315e509b19eecc9e3792532b198 and
READY1eba49031384deda22bbfdd9d234cc7ff1b43e63c0b72081af2c7c270a5b74f2 stay
unchanged. Main's operation-local lifecycle observer wrapper is provenance, not a
scientific arm. Analyze only after receipt outputs/attempt-001/TERMINAL.json and
operation2026-09-09-after-identity-receipt/attempt-001/receipt72/EXIT.json exist;
check collection STATUS and owned SERVICE_STOPPED too. Missing markers or capped
jobs remain visible rather than a reason for replacement data or a hidden retry.

## Questions and primary units

Primary receipt minus indexed_raw exact final success. Practical secondary
indexed_raw minus unchanged and receipt minus unchanged.72 scheduled episodes
are24 matched task/seed triples across six exposed64-record contexts, two target
counts and two seeds. Six arm orders occur four times. Contexts are repeatedly
exposed development data; no new independent root seed or held-out confirmation.
Use all scheduled coordinates and fixed historical step8 root473210…/childc32de.
Do not select another checkpoint, contexts, seeds or interpretation from outcomes.

## Independent final-score and identity reconstruction

Authenticate once the frozen operation/scientific source/input closure and actual
service binding; hash unique files once, no per-episode model rehash. Reconstruct
all72 coordinate/task/context/seed/arm identities, triple grouping/order and actual
native depth→alias/request sampling. Compare phase/run spec and original public
tasks and host-only gold. The gold is used only for final scoring here, never to
certify helper receipts or retroactively repair child labels.

Recompute the original strict numeric task outcome from the actual completed root
terminal reply, not saved derived metrics alone. Preserve its exact existing
contract: final nonblank line; at most one matching **, __ or backtick outer
wrapper; case-insensitive `Answer:` followed by signed integer and optional final
period/exclamation; integer equality to the original gold. Earlier correct numbers
do not count if the terminal line is malformed/wrong. Cross-check frozen scorer,
trace reward and stored derived result. Use actual complete trace/root_reply
semantics from the pinned trace implementation. Execution/transport/cancellation
failures and unrun episodes remain null, never manufactured negatives. A complete
observable wrong or malformed policy answer remains zero. Saved partial traces
may be absent on inherited infrastructure failures; say so explicitly.

Report scheduled, recorded, execution-complete, observable, strict-success and
unanswered/infra/censored counts separately. Give all three paired gains/losses/
ties over jointly observable triples, with context/question/seed summaries and
explicit incomplete comparisons. Scheduled operational yield is not semantic
accuracy on unknown episodes. No training reward/action-mask modification occurs.

## Helper receipt/consumption audit

Read original raw helper audit JSONL from trace.info, preserve bytes/hash and
missing/invalid audit cases. Root can write these files; they are diagnostic claims,
not trusted proof of native calls or data flow. Independently reconstruct each
claimed source catalog/selected ID→record-byte binding from frozen public inputs;
check context hash, selected record/group IDs/text hashes, unique requested IDs,
query and caller vocabulary. Rebuild the deterministic indexed helper prompt.

Where available, corroborate claimed request_text and source selection against the
captured **actual child** native request (correct depth/alias), via exact request
content within the decoded/rendered prefix and session/request linkage. Corroborate
claimed raw_answer against actual child terminal output, distinguishing it from an
arbitrary intermediate completion. Ambiguous matching, missing call linkage,
root-modified catalog/logs or absent partial traces remain explicitly unverified;
never force a match or count root-writable logs as independent corroboration.

Independently strict-parse each claimed raw ID→label JSON object: reject duplicate
keys, non-JSON constants, fences/prose/truncation, missing/unknown keys, invalid
types and noncanonical caller-vocabulary values. Record original lexical key order.
Map validity is exact requested-subset structural coverage; order-independent access
is permitted only for a fully valid map. Invalid labels_by_id must remain null.
No source-wide six-label vector, all-context helper call, particular decomposition,
or semantic label correctness is required for target-only task success. Never use
host gold to label a receipt as semantically validated.

Report helper request/result/error counts, optional helper uptake, valid/invalid
maps by reason, requested-subset coverage, explicit receipt() accesses and observed
root recovery/syntax/tool-use markers. Access is not proof of incorporation into
the final aggregate; code/trace snippets can show an observed use but not hidden
reasoning or causal mediation. No helper call means coverage not applicable, not
a missing-label reward penalty. Do not merge distinct queries/vocabularies into
one semantic context-coverage denominator.

## Cost and causal limits

Account for all raw native physical attempts, root/child roles, actual prompt/
action/cache usage, finish reasons, errors and wall time. Compare stored usage to
wire/token sequences; missing fields stay missing. Distinguish cumulative request
time from job/service elapsed time. Release accounting comes from owned release
and operation EXIT, not an assumption that a closed port means all GPU work ended.

Unchanged versus new-arm comparisons are practical harness-package effects: the
two new arms remove the historical supplied procedure/example equally. First-task
prompts are566 tokens unchanged,314 indexed_raw,349 receipt; all prompt hashes and
counts are frozen. Receipt minus indexed_raw estimates receipt availability plus
its minimal consumption documentation, not parser internals alone. Identical
helper arguments yield identical child prompts, but adaptive root choices can
diverge; actual child calls are not automatically paired.

A null/negative exact-success difference, or gain only with substantial extra calls/
tokens, fails the practical benefit hypothesis here. Better structure alone is not
better task success. Low uptake indicates an offered-interface usability failure,
not proof that an actually consumed receipt cannot help. Semantic child mistakes
can survive perfect validation. Positive results warrant new-context replication,
not a novelty claim or confirmation. Freeze any method correction additively and
do not change the scientific run. All audit writes remain in this analysis folder.
