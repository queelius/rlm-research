# Counterfactual-card96 prospective author-disclosed method

The analyst authored this experiment. This is an author-disclosed audit, not an independent implementation assessment. MAIN/question_cards review source independently. Freeze timing and exact attempt-directory existence are recorded honestly; no scientific outcome is read until MAIN's explicit terminal relay. No GPU/service/queue/source edits, retries, scalar extraction, generated-code execution or rescoring of prior studies.

## Frozen population and challenge boundary

All96 planned slots: eight exposed16-record parent contexts × three composed query families × original/counterfactual × U/P. Twenty-four shared fresh seeds/blocks, not24 independent context clusters. Frozen GATE ce10e939b210bcc11c14d47cdf27a2ab57799146f9c1670f503bb006b9c16880 supplies all selected counterfactuals. Fixed label-blind weight hash, then explicitly gold-conditioned query/parameter selection to separate four fixed confusers; no new model outcome selected any task. This is a targeted challenge on exposed records, not unconditioned/fresh-context accuracy. Keep original4/24 and counterfactual6/24 zero answers, both bestconstant6/24 and pairbestconstant1/24. No resampling or outcome-selected subset.

The same183-token card covers all three procedures. U remains original native contract, P adds the card. Explicit algorithm plus context package, not learned composition, pure token-length intervention or primitive-retention test. Counterfactual changes weights and selected query categories/threshold, not weights alone. Exact original U first-prefix/files and all within-variant U/P files are frozen. Check counterfactual records.json/context.txt mirrors and query.txt/native question consistency. Host labels/gold and GATE receipts never enter model-visible task files. Source exposure, actual fixed24/c32 identities, role/template/tools, root2048/context8192/T.5/top_p1, paired seeds and clocks must match READY and actual wire/service artifacts.

## Primary and paired metrics

Primary is strict authenticated native Answer:N correctness against each actual row's dataset gold, all24 per cell. Independently recount threshold_users using that row's threshold and strict >, maximum over scoped per-user A totals including zero users, and B-weight counted once per record for scoped users with at least one A. Never inherit threshold5 for selected counterfactuals. Gold agreement is not evidence that model code implements the operation.

Missing RESULT, no native final or unverifiable branch remains primaryNULL; authenticated malformed final is observed0. Report native availability, conditional accuracy, planned operational correctness where bounded failure is unsuccessful, and missing-native best/worst bounds separately. Explicitly classify never-started, attempted exception without RESULT, sampled invalid tool/loop/no-final, provider context rejection and genuine unknown. Policy-induced missingness is not random or exogenous. Never promote recovered raw completion into an absent endpoint RESULT.

Report P−U separately on original and counterfactual, full four-cell tuples by24block, within-arm both-variant-correct signatures/24 (unknown if either native unavailable), available signature denominators and operational/bounds separately. Report eight parent-context mean differences/signs, family, zero/nonzero and fixed-confuser strata; descriptive interaction only. Cluster-aware intervals, if shown, resample the eight complete parents, not records/queries/seeds. No equivalence or abrupt capability claim.

Exact practical gate: positive counterfactual strict P−U; at least four additional manually verified requested computations across at least three contexts; counterfactual P native availability >= counterfactual U. Original direction is reported separately and cannot compensate for failure of counterfactual availability. Prior card48 failed its practical gate and remains unchanged. Both variants' results may motivate diagnostics even if promotion fails, but do not label this practical promotion.

## Native authenticity and cost closure

Pin immutable outputs only after relay. Reuse source-pinned corrected native readers. Keep internal transport UUID and provider request_id namespaces distinct. Join typed request to exact full actual request body and full returned token/logprob vectors; where repeated bodies/tokens leave ambiguity, require matching provider IDs from the same authenticated namespace. Reject unresolved ambiguity/conflicts, never convert a reader defect into scientificNULL. Parse actual renderer final branch, require root alias, exact reply, no tool branch and completed trace agreement. Authenticate every returned root/child branch where possible, first prefixes, native sampling, actual model descriptor/config/tokenizer and typed-child grammar. No substring integer rescue or stale observation promoted to final.

Union every physical request file across all96 directories and outside-plan extras, including attempted failed/missing RESULT episodes. Distinguish physical attempts, HTTP responses, choice completions, token-validated completions; known input/output/cache totals plus unknown counts. Rejection/pretransport attempts are not established GPU generations; billing unknown. Retain child work, partial root actions, preflight calls, service startup/release and elapsed opportunity cost separately. No RESULT-only ledger. Verify actual owner3600/work3450/owned3570, early stage advance, all96 preinventory and cleanup/release; no retry/cell-specific cap.

## Mandatory all-available semantic review

Read ALL available final traces: actual sampled programs and subsequent observations, not AST/keywords alone and never reexecute. Record requested operator, exact category A/B, actual scope and actual row threshold; child acquisition/decoding/map provenance; variable resets/retention; observed accumulation; grouping/filter/threshold/max/conditional sequence; intermediate scalar and native final. An externally unioned map agreeing with final is only a diagnostic ceiling.

Classify genuine requested computation correct; genuine requested computation wrong due to observed leaf semantics; wrong operator/scope/category/threshold (including correct coincidences); unsupported literal/manual reconstruction; unknown. Literal recovery is not live-label computation. Faithful-wrong leaf attribution requires that executed operation is correct on actually used observed labels; use of every earlier batch requires actual state/dataflow evidence, not host union. Mark query-specific zero coincidences, pooled substitute baselines, constant answers and card procedure uptake separately. Review all wrong available finals too. NativeNULLs get observable failure mechanisms without inventing an unseen computation. Raw program/observation paths and evidence support each classification.

A positive result supports prompted procedural headroom on a targeted exposed challenge, not SFT efficacy, broader transfer or each individual sentence's causal effect. Preserve all96 results, availability losses and costs in the report and publish an immutable final closure. Any reader correction is additive with timing and unchanged primary rule.
