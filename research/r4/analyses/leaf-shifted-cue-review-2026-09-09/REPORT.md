# Independent shifted72 source review

No material science or launch blocker found in READY `51e9f51aeddbdaad9de792f65b0563552fccaf5e0344cd81d6a9a02763a0b980`. I read the MAIN approval, design, all three production files and seven fixtures, then the inherited collection/descriptor/scoring/lifecycle seams relevant to this adaptation. No model calls, new outcomes, broad suite, service start or frozen edits were used.

The frozen 72 requests have exactly 12 exposed contexts (four per TREC/SST/AG), two seeds and three arms. Independent checks confirm all 24 triples have byte-identical ordered bodies after removing only structured_outputs, identical recorded CPU prompt IDs, and max complete input plus 3,072 output tokens of 7,766 ≤ 8,192. This validates the frozen CPU artifacts; live prompt IDs still require the executed collector check. Each exact schema has 64 ordered tag/label objects, canonical-label enums, no extra properties and the declared source-tag mapping. Shifted tags are precisely IDs[17:] + IDs[:17], sharing the matching ID multiset; constant tags are p0000. Selected questions, input order and seeds are unchanged across each triple.

Displayed-position correctness remains primary. Named-record alignment is separately computed from the ID named by each slot and is NULL for constant or invalid arrays; it never substitutes primary labels. An independent alternating-label synthetic check yields shifted positional 0/64 versus named 64/64, demonstrating the distinction. Duplicate keys, wrong order/tags, cardinality and noncanonical labels are rejected. Completed invalid arrays score zero strict assignments; infrastructure/unrun stays NULL. Repeated class labels can agree under both alignments, and forced schema cues are not free ID retrieval or internal-attention evidence. The built-in projection retains null counts and paired differences; final inference should retain four source-context units per task and not pool tasks or treat repeated seeds as independent.

Binding remains exact c32de with the qualified Qwen3-4B base. Descriptor and live /models checks authenticate adapter/base/alias before collection. The owned code executes its adapted wrapper with the actual new __file__, while suite.SERVE and suite.c.ROLE continue to refer to the same unchanged leaf-role-routing-v1/source/serve.py. It does not reproduce the recent rebound-launcher hash mismatch. Ownership retains PID/start/UID/PGID/descendant guards; the only process-observation change catches proven process-absence races. Both actual new entrypoints independently pass --help imports.

One inherited launch epoch flows through 750-second work, 870-second owned and 900-second parent envelopes, with 600-second collection and four concurrent calls. Verification/startup and remaining work share that clock; collection is clipped, and release runs in finally. The parent must enforce the declared outer cap and exclusive device/lock. Per-call wire bodies, IDs, usage/errors and all 72 planned slots are retained. CPU final projection remains inside the owned command before release, a known small scheduling-cost limitation, not permission to hold the GPU for an independent audit. No equal realized token/compute claim is justified by fixed caps or the shared tag multiset.

CHECKS.json binds the exact 29 directly inspected artifacts, immutable population/prompt/mapping checks and actual import results. The author's seven-test/fake-HTTP/schema qualifications were read and are attributed, not claimed as an independent full rerun. Seed uniqueness is limited to its recorded named-metadata scan, not every historical file. No changes requested.
