# One-shot fresh12 singleton replication audit

Reuse the completed176 auditor and independent union unchanged. Exact-count source adaptation changes only12 cells/history/checks, generation/decode seeds, vector/singleton arms,328 calls and24 tasks/arm. Recompute normalized facts from original tables and both solver gold, all native requests/tokens/model/prefixes/seeds/usage, actual runtime, complete scalar unions and paired denominators. The archived new native fixture exercises actual bindings before seal.

No GPU, model calls, source mutations or polling. `seal` writes immutable audit CPU_READY; `run --output outcome-001` checks terminal once and emits PENDING if absent. A later invocation must choose a fresh output directory. All304 scalar+24 vector calls and48 stage outputs retained; repeated decisions are not independent contexts. Unknowns never become false; invalid-known separate.

```sh
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/analyses/b05-singleton-replication-independent-2026-09-13/analyze.py run --output /project/alex_phd/runs/rlm-research-r4/analyses/b05-singleton-replication-independent-2026-09-13/outcome-001
```

Interpret matched-valid error corrections, structural cells, constant policies and physical costs, not just exact counts. Twelve generated contexts broaden this synthetic family; normalization already resolves history/latest checks. If gains replicate, choose between new-data transfer and budgeted routing based on remaining errors/cost; no oracle or learned route is tested here.
