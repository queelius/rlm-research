# Reminder phase192 independent outcome method

Written after terminal notification and before this auditor opens any outcome
aggregate or raw response. Parent disclosed completion192/exit0 and original
zero-call launch failure only. Prior source-matching results are known research
context. Auditor authored shared component ancestors, not this scientific phase
study or launch repair. This is independent outcome reconstruction, not independent
framework authorship or prelaunch preregistration.

Authenticate frozen READY/SPEC, DATA, REQUESTS, full PROMPT_IDS, current study and
executed recovery sources plus actual single c32de adapter/config once. Do not import
the author's scorer or analysis. No model requests, service/GPU/process changes,
generated-code execution, output repairs or accepted-file edits.

Reconstruct192 rows:12 exposed contexts (four TREC/SST/AG), two seeds981310011/021,
four phases, matching/constant. At index i, object iff i%4=phase;16 objects and48
canonical strings. Ordered keys exactly tag,label, no duplicate/extra keys, exact
source ID for matching or p0000 constant, complete64 array. JSON NaN/Infinity,
trailing prose, wrong types/tags/order/labels/cardinality all invalidate the complete
array: strict position scores zero, alignment unavailable. Unrun/canceled/provider
or provenance unavailable stays NULL. No null-to-zero conversion or best subset.

Primary same-record core is positions4–64 (indices3..63). At distance d use phase
(i-d)%4 and compute matching-minus-constant position correctness. Average61 records,
then paired two seeds/context, then four equal contexts/task. Primary is A0-A3,
positive direction, with all four A profiles and underlying arm profiles retained.
Full eight-call blocks required; both seeds required for context means and all four
contexts for task primary. Early boundary indices0..2 never modulo-wrap before an
anchor. Report their undefined-distance counts separately; preserve all64 whole-call
scores, whole64 perfect, confusion/count-vector L1, grammar failure and NULL counts.
Twenty-four seed blocks are nested in12 contexts, not independent labels. No pooling
with sparse/cue/released-model calls or selection of the best distance after results.

Each raw CALL must match its exact coordinate and frozen ordered body; independent
wire bytes, schema order/hash, alias, sampling and provider physical prompt IDs must
reconcile. Re-render physical native template with actual typed tool serialization,
compare all eight prompts within each block (schema-only body differences), and
record output token/usage/cache/unknown/stop/call clocks. Record actual sequential
eight-call Williams order and max concurrent units; no calls are shared. Compare
all observed costs and jointly valid phase-paired costs descriptively. Provider usage
is not GPU FLOPs or unique uncached inference.

Lifecycle: distinguish original~48s launch failure with zero model calls from the
accepted additive recovery's~353.9s. Read retained original FINISH/log and recovery
runner/markers, do not silently reset scientific caps or claim a model reroll.
Preserve original failure; recovery changes unbound time/run count/path seams only.
No historical sources altered. Parent retains launch/recovery authority.

Report equal-context primary/profile, every context and block, strict validity vs
semantic gain, per-task count/cost and source hashes. Positive local decay narrows
record-difficulty confounding but not autoregressive history/action-space effects;
flat/inconsistent result weakens that account, not equivalence proof. No hidden
attention, unfamiliar-data or whole-RLM planning claim.
