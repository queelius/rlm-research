# Adaptive40 audit implementation — ready, outcomes not read

Assignment switched from AG to adaptive before any AG analysis file or outcome read, preserving scientific authorship independence. This auditor provided the adaptive source review but authored no adaptive study/operator/collector/scorer source. Main's METHOD remains unchanged. The newer assignment authorizes CPU implementation before terminal; scientific output reads still require an explicit parent terminal trigger.

Prepared `adaptive_audit.py` plus ten focused tests. Reuses only the authenticated CPU native graph/wire/usage projection and static code-evidence helpers from the completed uptake audit; does not import the adaptive author scorer or execute operator/model programs. The 400 expected inputs are parent-accepted authority, not falsely claimed as a fresh independent full-closure audit. Needed bounded data/source files are authenticated as read.

Independent reconstruction confirms the source TREC training partition, all named exclusions,1454 remaining groups, deterministic512 membership, synthetic-user assignment, full public text, exact host labels/counts, and40 physical/48 logical references. Inputs remain leaf-training-supported, not globally unseen or pretraining-clean. User gold remains2/1/2/2; no resampling.

Tests cover null versus malformed endpoints, strict map failures/no repair, shared global accounting, filter-only public requests, incomplete paired contrasts, nullable operator ancestry, preservation of duplicate physical-request identities, and native qualification005 cases. The four existing fixture episodes are read only: zero operator root calls, correct root/child token/graph/wire/logprob identity, invalid operator output→null, and reversed valid map key order reduced by ID. A forged runtime-only map is not native-verified evidence. No new rootless runtime or provider was started.

RED: initial import failed because implementation was absent; the later runtime-only-map test failed on the missing operator projection. GREEN:10 tests passed in0.98seconds, exit0. Full source input reconstruction also exited0. Hashes and exact future audit command are in IMPLEMENTATION_READY.json.

After parent terminal trigger, the runner first checks actual terminal/status/release/phase/endpoint/models/overlay records, then reads raw episodes and physical artifacts once. It writes raw ledger, physical attempts, metrics and source hashes without overwriting earlier outputs. The final plain-language report and selected static incorporation review are intentionally pending, not declared implemented automatically. Missing or divergent terminal evidence must be reported as a failure/pending boundary, never an excuse to inspect live outcomes or poll.

No adaptive outcomes, GPU/model requests, services, locks, signals, environment changes, accepted-source edits or new experiment preparation.
