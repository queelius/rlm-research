# Independent leaf role/tool96 audit method

Prepared after MAIN announced accepted attempt002 waiting behind restart48, before reading any attempt002 response or score. Auditor did not implement this study. Read-only science access, CPU tokenization/parsing only; no GPU, service, environment, lock, queue, source changes, generated-code execution or subagents. Timing is the actual UTC written in METHOD_READY, not the earlier design's nominal timestamp. Prior exposure includes earlier free-ID and corrective research; no new panel outcomes.

Audit source READY_RECOVERY and preservedV2, exact attempt002 routing, released base/model-directory identity and actual parent/owner clocks. Preserve pre-model attempt001, its startup failure and costs. No silent retry, seed/coordinate replacement or mixing of failure inventories. Immutable scientific artifacts and source pins precede outcome reads.

Authenticate four catalog-fresh64-record contexts (two AG News, two SST-2 train text-unit contexts),256 normalized groups, source revision/split/index/gold labels, conflict exclusions and named exposure rescan. Catalog-fresh means absent from the named scanned experiment catalogs, not globally unseen text or model pretraining. There are four context clusters, not24 independent clusters or6144 independent draws.

Cross coding/classifier system package × tools advertised/absent × matching-ID/constant-tag/plain output × free/exact decoding. Verify all96 coordinates, one paired sampling seed, retained full JSON request bodies, native rendered prompt IDs and within-format free/exact equality except structured_outputs. Confirm matching and constant differ only prescribed output contract/schema, all conditions show identical ordered record IDs/text. Check exact schema constrains shape/order/required tags and permitted labels but never gold labels. No ID reordering, repair or permissive extraction. Explicitly retain fixed role/tool dispatch-order/time confound: decoder order alternates, role/tool order does not; observed dispatch/completion time and cache changes may covary with role. Role replacement is a wording package, not a pure isolated role token.

Independently authenticate each raw native response: actual model, single assistant choice, prompt/completion token vectors, usage, finish and text or parsed tool envelope exactly match retained wire evidence. Tool-directed outputs are scored but never executed. Parsed native tool routing and literal tool wrappers/code/prose are separate observed failures. HTTP/parse/native-envelope errors and unrun slots retain NULL; authenticated capped/invalid outputs are observed zeros. Do not infer billing/generation merely from model_called or HTTP attempt.

Independent whole-output scorer: duplicate JSON keys/constants/trailing text reject; exactly64 ordered items. Plain requires canonical label strings. Object arms require exactly tag,label fields, correct field order, canonical labels and string tags. Matching requires exact ordered input IDs, constant requires p0000. Only full-contract-valid output contributes row-label correctness on the planned64-per-call denominator. Separately report shape validity, conditional positional accuracy among shape-valid outputs, full-contract-conditional accuracy, emitted tag position/missing/duplicate/extra/order diagnostics and finish/route. No gold-dependent grammar, remapping, code execution, markdown stripping, prefix extraction or repair.

Preserve every planned slot with correctness bounds0..64 for missing native outputs. Report free-decoder full-contract-valid planned-denominator row accuracy first, with validity and native availability denominators explicit. Exact decoder provides a separate representation/grammar contrast. Compare paired condition counts and per-context differences, emphasizing classifier/no-tools matching-vs-constant/plain. Do not collapse role effects across format/decoder or infer generalization from four contexts. Separate policy routing/format failures, semantic label errors and infrastructure NULLs.

Costs: all physical model request attempts, received/provider/native-verified completions, raw known prompt/output/cache usage, unknown usage/billing, elapsed owner/collector/parent intervals. Count failed attempts even if scores NULL; no invented0 cache or paid generation. Actual tools advertised are an interface condition, not executed child trajectories. Final source-derived report, independent focused parser fixtures, immutable manifests after terminal/release. Fast trustworthy metric signal to MAIN followed by full report; audit never blocks successors.
