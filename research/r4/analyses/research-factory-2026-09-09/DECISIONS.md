# Decisions and corrections

These are research decisions, not a launch queue. Stable linked records are in [CATALOG.json](CATALOG.json).

1. Narrow the component claim: fresh96, shifted72 and same-record phase192 support output-source correspondence. Bridge32 remains contrary end-to-end evidence with limited checksum exposure. Do not claim internal attention or broad decomposition.
2. Preserve availability corrections additively. Taxonomy overturns high-LR's blanket availability claim for one broker error. Keep frozen0 and separate native-finalNULL/query net[-1,0]. Preserve RL/plan null sensitivities and coverage's different declared completed-empty rule.
3. Do not credit unused refill. RL4 found mixed groups in mandatory collection; extra refill was never exercised.
4. Target executed reduction/state handling, not imagined restatement failure. Actual displayed scalars were faithfully copied. A host-inferred answer from maps is not a computed scalar the model saw.
5. Do not blindly extend first-action-only SFT. Canonical/filter-first ties1/16 and is worse than unchanged. Full-procedure or context-sensitive teaching needs a separate bounded test.
6. Make composition falsifiable. Constructed lossy summaries are controls. Require ordinary reports, exact nonrecursive computation, input/compute controls and later learned choice on held-out structures. Original-input-visible whole RLM and report-only replay remain distinct.

A component gain promotes a component claim. Whole-task gains under adequate exposure are needed to promote system usefulness. New records, released models, new root training and unknown pretraining exposure are separate axes.

No candidate is confirmatory-ready. Negative findings, selected examples and unavailable endpoints remain discoverable. MAIN owns the current cross-node handoff; pending policy-panel/new-demonstration outcomes are not imported. Full historical catalog expansion and finalized future pilot designs are explicitly pending, not prerequisites for this first snapshot.

