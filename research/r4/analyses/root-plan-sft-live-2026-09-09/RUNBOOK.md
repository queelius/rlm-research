# Plan-SFT independent terminal audit

Prepared, not run on new outcomes. METHOD_READY is frozen and the eight focused CPU tests pass. Existing source-review REPORT and all accepted experiment artifacts remain unchanged. The only modifications to the privately imported native auditor are internal request-ID indexing and treating implementation-projected rewards/totals as comparisons rather than authority.

After MAIN confirms the actual scientific terminal or accepted operation EXIT, run once:

```bash
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/analyses/root-plan-sft-live-2026-09-09/audit.py audit --main-terminal-trigger
```

Writes EARLY.json, AUDIT.json and SOURCES.json exclusively in this analysis directory using exclusive creation. It never takes a GPU lock, polls, runs a server or executes generated code. It inspects adapter/Adam/RNG tensors on CPU, not a real model. Missing phases remain NULL; no earlier checkpoint substitutes. If an integrity assertion stops the audit, retain the traceback and any completed outputs; inspect the exact mismatch before an additive correction rather than rerunning blindly.

After the one-pass audit, write a concise independent REPORT and final seal from these machine artifacts. Lead with filter_first versus canonical over 16 paired coordinates/four context groups, then trained-versus-unchanged package effects, actual relevant/irrelevant child scope, all-attempt costs and joint-correct conditional costs. No new outcomes were read to prepare this script.
