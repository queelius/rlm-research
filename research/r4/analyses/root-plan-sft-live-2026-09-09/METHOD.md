# Authored plan-SFT: independent prospective terminal audit

Frozen before opening this study's training/readout outcomes. The analyst reviewed the source and reproduced the original zero-model-call import failure; corrected READY_V2 is `8a90b12ff1c4aed797ba52afded3197be7dcc6cd57c5fa756732249703683f61`. Original READY and failed entrypoint evidence remain immutable. The accepted operation is `operations/2026-09-09-after-freshrecovery-plan-sft48`, with parent 4159142/start_ticks 1077339235 and acceptance `d9ab8d5c93f5190120efd0a730cec4d5506c76f1ed2e8d26fa7a9d4fd4270804`. No outcomes have been supplied for this study. The symmetric LR amendment was informed by a prior study and is not scientifically unopened.

## Question, population and fixed comparisons

Two fresh-Adam authored-first-action training packages start from exact low-LR success-SFT8 `66cce400…`: canonical classifies all visible records, while filter_first selects publicly relevant user records before child classification. Eight global examples are identical; eight single-user examples differ only in the selected-record assignment. Both use 16 examples, four shared full-pass orders, seed 981320002, LR 1e-4, rank-8 FP32 LoRA/BF16 base, clip 1 and zero dropout. Fixed checkpoint 4 is required; no best/intermediate checkpoint substitutes. These are operator-authored initial actions, not verified successful teachers or sampled behavior likelihoods; no final-answer turn is trained.

Primary is filter_first minus canonical strict outcome over the same 16 coordinates (eight query-transfer, eight length-transfer), matched seeds 981320201–208/301–308. Report each trained arm minus unchanged low8 as secondary package contrasts. There are four source-context clusters, not 48 independent tasks. Report per-context and query/length summaries and complete-pair wins/losses/ties with NULL count and worst-case net bounds. Fixed phase order canonical→filter_first→unchanged introduces service/time nuisance; shared first-prompt and sampler parity are checked, not assumed deterministic. The 384 train, 320 query/length and 192 unused-validation normalized source groups are disjoint locally; public/base-pretraining exposure remains unknown. These readout compositions are exposed to this research, not a new benchmark test set.

## Authored CE and actual update audit

Authenticate the prior independent 32-row native rendering/mask audit by hash, then check the exact active row bytes and provenance again without executing authored code. There must be 16 equal native prefixes across arms, eight identical global actions, eight assignment-only single-user differences, no gold counts/reward/advantage/old-logprob fields, and only authored action tokens plus the honest 151645 terminal token under CE. Prompt, child and observation tokens carry no loss. Verify 2,472 targets per arm/pass, 9,888 per arm across four passes, 64 example/root-turn exposures per arm, and 19,776 combined targets.

Independently authenticate each actual training RUN/load audit and contiguous state1→4 chain, exact original adapter, fresh empty Adam source path, persisted optimizer/RNG/cursor, member hashes, final selection and no child parameters. For every pass reconstruct the shared shuffled order and per-row float32 coefficient 1/(16 × actual action-token count); verify coefficient masses, individual weighted CE, summed objective and separate global token NLL. Read actual Adam step counters/LR/betas/eps/weight decay and finite positive recorded gradients. Independently compare committed adapter tensors to the fixed start, checking key/shape/dtype identity and finite nonzero deltas; do not load a real model or use GPU. Incomplete training remains incomplete, with its saved states retained but no final-arm substitution.

## Independent strict outcome and native boundary

After MAIN's terminal trigger and an actual scientific TERMINAL or accepted operation EXIT, score raw episode `root_reply` using full `Answer: ([0-9]+)` after outer whitespace stripping against host-only gold. Completed empty, prose, bad syntax and wrong counts are observed 0 if the final native response is available. Unrun/setup/provider/incomplete/censored states are NULL, never 0 by default. Preserve raw completed endpoint scores separately from later runtime/finalize errors; report graph-clean eligibility separately. The implementation's projected rewards and totals are comparisons only: disagreements are explicit integrity findings, not scoring authority or repaired answers.

Authenticate each phase's current selected root, fixed c32de child, base manifest, binding, actual /models crosswalk, exact local runtime and initial native prompt IDs. Reconcile all pretransport role-hook requests, result records and typed records by internal request ID; filenames are not authoritative. Check wire IDs, completion IDs/logprobs and full-support T=.5/seed/max2048 against captured native records. Verify typed grammar is depth1 only and raw returned child maps have exact requested keys/semantic enums. Reuse the previously qualified WireTrace branch/root-only mask proof; retain graph/prefix failures as explicit unknowns, not proof of absent model work. Semantic call-parent edges alone do not establish physical ancestry or root consumption.

## Scope, mechanism and cost diagnostics

For each episode report actual child invocations, relevant/irrelevant requested and valid-map source IDs, first requested batch, repeated label occurrences, and label agreement against host labels. Full map availability is not proof of root consumption or faithful aggregation. Describe first-action structured-tool/AST validity, literal selected-record assignment, action length and finish reason; AST-unparsed fields stay NULL. No generated code is executed and no generic static interpreter is built. No lexical marker alone establishes a correct plan.

Count every physical attempt, including request-only, failures and retries, by role; reconcile causal graph tokens separately. Sum known logical input/completion/cached/uncached usage with missing-count denominators, never fabricated cache zeros. Report total cost across all calls, and paired costs conditional on both arms strictly correct as a descriptive efficiency diagnostic with its selected denominator. Include optimizer time, startup/service/collection/release and outer operation time separately; no summed per-request latency is labeled elapsed GPU time. Joint-correct conditioning is not a causal performance estimate.

## Execution, missingness and publication

Preparation reads frozen sources, rows, plans and previously sealed review evidence only. Terminal execution is one pass with cached hashes/JSON reads; no full transitive rehash per episode, GPU lock, polling, service/model calls or source/output mutation. Missing phases and files remain in all 48 planned rows. Authenticate final operation/runtime closures once and preserve nonzero/timeout states. Publish early independently reconstructed primary counts, then AUDIT/REPORT/SOURCES and a seal. A source-binding or new parser discrepancy is recorded before any narrow additive correction; this method and original outputs are never rewritten to fit outcomes.
