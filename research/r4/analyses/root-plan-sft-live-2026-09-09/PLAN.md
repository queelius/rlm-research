# Plan-SFT terminal audit implementation plan

> Execute inline in this isolated analysis directory; no subagents or Git/source changes.

Goal: implement METHOD.md using the already qualified native/physical audit primitives, independent authored CE checks and a terminal-only command.

1. Freeze METHOD and an authenticated direct-source/input manifest without opening this study's outputs. Reuse the sealed pre-outcome source/token review as the immutable closure anchor.
2. Write focused fixtures for authored-mask/no-likelihood validation, actual float32 row weighting, NULL-preserving paired/joint-correct accounting, internal request identity and native wire equality. Observe failure before implementing the new primitives.
3. Add the small audit adapter. Reuse native phase parsing privately with exact pinned changes only for internal request IDs and nonauthoritative collector-score comparison; implement both four-step authored training-chain checks directly.
4. Run the focused CPU fixtures, prepare the immutable expected population, and publish AUDITOR_READY last with an exact terminal invocation. Do not read outcomes until MAIN triggers it.

No installs, real-model loading, GPU calls, generated-code execution, service control or live-input edits. External research-store isolation is explicit; a repository worktree is neither needed nor authorized here.
