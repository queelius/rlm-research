# Close the accepted single-dose contrast

Reuse the fully reviewed72-call independent saved-native auditor with exact dose10 READY/checkpoint/model bindings. Authenticate all request/model/prefix/seed/token/rawresponse/canonical/usage fields and source gold; independently retain strict/semantic/BA/confusion/per-context-width outcomes and all changes. Do not reimplement training mathematics.

Add the SHA-pinned LR1e-4 completed audit as descriptive reference. Each dose retains its fresh within-service base comparison. Show cross-run base token/set changes, current and earlier semantic-valid denominators, and difference in BA effects only on common-valid four-arm coordinates. Missing or invalid rows are never silently aligned as zero. No outcome-selected examples, best-dose selection or further model/training calls.

One focused fixture checks the actual dose binding on a saved native base response and tests zero same-report contrast and common-valid denominator exclusion. Seal, check terminal once; if absent preserve PENDING and leave the command for MAIN. No polling. Complete report/source checkpoint is the handoff priority as quota approaches winddown.
