# One-shot independent FinQA demonstration comparison

Reuse the corrected zero-shot native auditor without source edits; bind only SIDE, ATTEMPT and READY_SHA to the admitted two-example sidecar. Pin the baseline independent report and every original source/native artifact it authenticates. Pin both frozen context/call lists, host targets and new TOKEN_AUDIT. The new study's complete closure is independently checked once by the reused audit at terminal analysis, not repeatedly while waiting.

`analyze.py seal` runs one focused unknown/validity/score/error-delta test and seals CPU_READY; it never checks the future output. `analyze.py run --output <fresh-directory>` verifies the receipt, makes one terminal existence check, writes PENDING or HOLD if needed and stops. No polling/GPU/model calls. Do not overwrite a prior outcome directory.

After qualified terminal, the original auditor recomputes native request/template prefixes, checkpoint/LoRA/routing, completion IDs/text, canonical-vs-raw response hashes, strict safe-DSL/scalar interpretation, provided-target scores, costs and all32 inventory rows. Output native/REPORT.json retains actual raw paths/hashes. COMPARISON.json joins all16 contexts across both arms by exact ID/seed, not by result quality; REPORT.md summarizes. Unknown pairs cannot be scored as losses and removed error tags are undefined for unknown outputs.

Compare zero-shot versus demonstrations separately within each arm, keeping natural1-call costs and unequal token totals explicit. No pooled64-IID claim. All16 supplied labels and their known semantic/annotation limitations remain; no gold-dependent scaling, syntax repair, annotation filter or new prompt variant. Manually inspect all changed arithmetic paths and primary-score disagreements after completion, distinguishing invalid interface, operands, arithmetic/precision, output scale and source/question mismatch. This exposed-panel run calibrates interface use; it is not SFT/RL or a novel recursive method.
