# Smallest next comparison: direct final-answer surface control

Recommendation only; no implementation or launch authorization inferred.

The quoted-report arm improves exact3→5, but both wins were already correct answer content in the direct baseline. The remaining Nevada/Maness full sentences and Christian correct-name sentence show a terminal answer-format bottleneck. Before another extraction design, ask: **Can a single direct final with a clearer answer-phrase instruction recover those exactness changes without an extra helper?**

Use all12 exposed contexts, identical frozen flexible raw4, original question, base4B/V3 engine binding, final JSON schema, T.5, max1024 and the same paired direct final seeds. Reuse the sealed original direct12 controls; spend only12 new final calls. Change only the final instruction, not evidence, schema, selector, planner or model. A proposed generic rule is: “Put only the shortest standalone answer phrase in answer, not a sentence restating the question or an explanation. Retain necessary quantity qualifiers and requested measurement units. Do not change which entity or relation the evidence supports, invent an absent bridge, or use external information.” Preserve the existing support_idxs obligation and lack-of-evidence behavior. Freeze exact wording before any run; no gold examples, component answers or per-case instructions.

Primary: unchanged official answer exact and support metrics for all12, paired wins/losses/unknowns against the cached direct control. Secondary: inertly distinguish answer-form changes from a changed entity, missing selected source, contradictory answer, or truly improved evidence join. Boston and tornado are diagnostic examples, not a filter or a precondition. Retain all12 even if this worsens abstention or drops units. Same seed is not identical prefix: the final instruction is the deliberate intervention, and every other native-body/input difference must be audited.

Cost:12 newly spent physical calls. Including cached selector/planner acquisition, both original and proposed natural policies cost4 calls/question (48 each), unlike the quoted arm’s5. Token length is not matched. There is one proposed alternative, no sweep/best-prompt selection, no new training, and no gold-based extraction or posthoc metric repair. Existing3/12 and5/12 scores remain frozen. A fresh3/12 control is unnecessary if exact source/engine/request binding is maintained except the declared instruction delta.

Decision: if the cheap instruction reproduces the two answer-surface wins without factual harm, prioritize a terminal answer contract rather than claiming a decomposition gain. If not, retain the uncertainty: extra report context may change generation beyond formatting, but current evidence still does not identify a new solved relation. Either result is exploratory on an exposed panel.

Second priority, not this next run: source-linked, query-conditioned relation extraction that distinguishes an entity’s own properties from relatives’ and does not demand a single paragraph explicitly state the entire multi-hop answer. The Copenhagen wrong-subject/valid-quote example motivates that test, but merely relaxing quote checks or automatically accepting valid substrings would not address semantic grounding. Missing sources in Idol/Italy/house music are a separate acquisition constraint.
