# Independent quoted-relations audit

CPU only. This analyzer never calls a model, executes generated code, changes source owners or rescales their metrics. MAIN alone owns GPU admission. Five focused regression tests use archived actual native HTTP-double boundaries, including malformed/invalid-quote reports and a wrong-person relation attached to a valid quote. Per verification-before-completion, the seal records actual test output before claiming readiness.

Run once after the owner terminal exists, with a new output directory:

```bash
CUDA_VISIBLE_DEVICES='' /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/analyses/musique-source-quoted-relations-independent-2026-09-12/analyze.py
```

All12 exposed contexts are retained. Independently re-decode new24 and cached48 native responses, reconstruct exact token prefixes and compare request contracts, model binding and paired final seeds. The new helper sees only the frozen question/raw4. Its final sees the identical cached direct payload plus the report—even when the helper report is invalid. Strict independent report/schema/quote checks are compared with receipts. Exact native final strings are independently scored against host-only gold; no gold enters any inference path. Owner scores are preserved, with mismatches reported separately. Unknown dependency/native outcomes stay unknown.

REPORT.json includes native calls, evidence hashes, costs, paired scores and diagnostic fields. MECHANISM_EVIDENCE.json retains all12 raw sources/reports/finals, annotation provenance and component facts for subsequent inert adjudication. Exact quote substrings do not establish the relation's subject or truth; answer-string presence and annotated-support coverage do not prove a successful evidence join. Separate final-format errors from entity/relation errors, and never retroactively replace official scores with semantic judgments.

Cost: new physical24 calls; cached direct12 and acquisition36 separate. Conditional finalization1 versus2 calls; full natural policies4 versus5 per question (48 versus60 total). Same source count and final seed, not token- or call-cost-matched. This tests an extra extraction/report step on fixed exposed cases, not novel or learned routing.

The seal performs one terminal-presence check, no polling. MAIN subsequently reported completion and explicitly authorized one-shot analysis plus all12 mechanism review.
