# Additive wording correction: card48 comparison

The final paragraph of `ALL_AVAILABLE_SEMANTIC_RETROSPECTIVE.md` incorrectly described the separate procedural-card48 study as including a “context/different-question manipulation.” That phrase is withdrawn.

The verified card48 contract preserves every original task question byte, record/context byte, helper byte, and file byte. Its `U` first native prefix is identical to the original. The `P` treatment only appends the same 183-token shared algorithm card to the root prompt. It therefore changes the **experimental intervention and research question** and adds prompt context; it does **not** change the task question or record context.

The narrower methodological distinction remains: card48 tests explicit shared algorithm instructions, whereas the proposed follow-up tests direct declarative operator parameters without supplying an algorithm. This correction changes no interface72 score, mechanism judgment, or prior seal.

