# Pre-outcome independent high-LR complete-success SFT audit

Freeze before reading root-success-sft-lr-v1 new training/readout outcomes. Analyst authored the older low-LR source and the ledger audit but did not implement this new LR adapter. MAIN's exposed historical5/24→9/24 motivation is known; it is not pooled into the new result. Fresh sampling seeds are not new source holdouts. No GPU/model/service/process/queue actions, sampled-code execution, training export, or source/output changes.

## Fixed comparison and source boundary

READY a70e9f63b66921ecbfcd793fee53b28fc682e4e56ca2ddb357ba280d08bc826a; RECIPE46594fb7d8e5452f51a144c6660adc89999004d724cd3496f5b702e2ada8339b. Authenticate the frozen source/input closure once and cache hashes, never inside episode loops. Independently inspect the counted private source transformation: exactly two optimizer/reload LR literals2e-5→1e-4; otherwise same training function, loss, start, precision, masking, clipping, AdamW, seed/orders and update count. Distinguish intended input change from realized floating training behavior; coefficient mass is not gradient share.

The exact27 confirmed rounds01–05 training-only candidates/114 physical root turns/15,256 action targets per pass must be byte-identical to the original sealed corpus c0f40ff19c8a6f906b551202ddc733488300a3e752a97098e316fe7e17f81259. No heldout examples, new extraction, deduplication, rewritten actions, cropped/retokenized prefixes, omitted recovery turns or fabricated probabilities. The prior source/native candidate audit is an authenticated anchor, not rerun against evaluation data. Check turn masks/labels/prompt lengths/stops directly and compare all eight deterministic episode orders against INPUTS and the original orders.

Start efab2913e7fe9f5f9b381ae6eb67bb56071070f86b237e6145f98654816aad64 with fresh AdamW; seed981308002 intentionally reused. BF16 base, FP32rank8 LoRA, no dropout, LR1e-4, betas.9/.999, eps1e-8, WD0, clip1. Eight complete corpus updates, one optimizer step only after each full pass. Loss is equal episode→equal root turn→mean physical action CE; prompts/child/observations have no loss. Expected full totals216 episode exposures,912 root turns,122,048 targets. The root trainer never loads or updates c32 child weights.

When terminal training artifacts exist, audit RUN/LOAD_AUDIT, each contiguous checkpoint state/member hashes, Adam step/LR/moments and RNG/cursor continuity, finite nonzero gradient and actual adapter tensor changes. Check all114 stored coefficients per pass at their real FP32 precision, coefficient×target count masses/sum, weighted CE and separate token NLL, and source-bound turn/order identity. Recompute bounded adapter deltas against exact start and previous checkpoints; do not claim a nonzero delta proves learning. Preserve any incomplete full pass/failed gradient/checkpoint and no-update state. Selection must be exact fixed8, not validation-max or last-save fallback. Authenticate high selection/RESULT and low fixed8 adapter66cce4009628ff7d8e047f08e6fb3a29febdbf8fb3ed8303871f69860f1151d5 independently, including actual state/config/optimizer/RNG links. No old Adam imported.

## Paired endpoints and grouping

New plan7e8256191e4c85db548f016cb97f5dacf6868495d1855d57010ec915512a5335 has24 coordinates per root:8 validation (four context groups×two queries),8 query transfer (two contexts×four queries/repeats),8 length transfer (two contexts×four queries/repeats). Forty-eight planned episodes; eight source-context clusters overall. Same public context/task/prompt/native initial token IDs, fixed c32 child, local runtime and budgets. Seeds981314101–108/201–208/301–308 pair low/high. Fixed ordered services low→high; alias strings may be the same across phases, so classify treatment by authenticated phase checkpoint/binding, never alias name alone. Preserve this order/service nuisance and inspected source/leaf-training exposure.

Primary: strict whole ASCII `Answer: ([0-9]+)` after outer stripping; observed completed wrong/malformed/empty0, correct1; unavailable/incomplete/runtime/provider/graph-failure NULL. Keep string agreement separately if a final exists despite operational failure. No permissive extraction or response repair; previously completed outcomes remain observed after a later cap. Retain all24 planned/arm and all48 outputs/unrun positions. Report high−low paired gains/losses/ties/unavailable, overall descriptive mean and each stratum separately; report context means/contrasts with their real denominators, not48 independent trials. No pooling old readouts or checkpoint selection from these results.

Secondary: first structured tool/format/finish reason/completion length, literal taught-example copying, child-helper uptake, source/query-relevant coverage, final versus auditable complete child target count, and token/call costs. Use strict source-bound maps and actual delivered observation/native branches; runtime-writable helper logs alone are claims. Wrong non-target labels are not by themselves a failure of the asked target count. Unsupported or partial semantic interpretation stays null; no general Python interpreter or code execution. Static AST-unparsed is null, not absent behavior. These diagnostics are exploratory and cannot alone establish mediation or an internal mechanism.

## Actual native/provenance and costs

At terminal, reconcile immutable row coordinates/raw hashes with actual role/typed/native request hooks, trusted depth/parent IDs, sampling .5/full support/root max2048, fixed child schema and actual root/child/base/config/adapter binding. Verify Qwen3 enable_thinking renderer, exact native request prompt IDs, response action IDs/logprobs, official WireTrace physical branches/masks and each initial frozen prefix. Semantic parents establish invocation relationships separately, not physical message ancestry. Child constrained probabilities must never become root targets. Record real finish reasons, structured-message parse emptiness, and all failed/uncommitted/unknown attempts without manufacturing likelihood or reward.

Decode raw JSON-string wire usage; separately total logical prompt, known cached prompt, returned completion and sampled root/child action tokens. Missing cache fields remain null. Reconcile typed pretransport-hook/role-hook/returned/error/graph-committed funnel by IDs rather than adding duplicate layers. Describe unobservable retry attempts as unknown/lower bounds; no new study retries or replacement episodes are authorized. Do not describe operational setup failures as model non-submission.

Authenticate operation acceptance/command/exit, RUN/terminal, each phase collection/status and service-release closure once. Training process≤1200s; each readout≤900s;3180 shared work from before verify/startup,3300 owned/3330 outer. Record actual training/collection/service/cleanup/CPU tail and any setup/null cost separately; summed parallel episode times are not GPU-seconds. Analysis runs after release and never holds the scheduling lock.

## Publication

Seal METHOD_INPUTS/METHOD_READY before any new outcome access. Await MAIN terminal trigger; if it arrives after freeze, one bounded cached raw/tensor pass produces early paired counts, then REPORT/METRICS/SOURCES/seal. A stopped/incomplete run gets a failure/status report, not an inferred fixed8 or efficacy result. Preserve all failures and projection corrections additively. New high-LR empirical outcome cannot change this method or the fixed candidate/weight selection.
