# Fresh FinQA16: sparse arithmetic benefit recurs, with important target noise

The sealed independent audit passed with **zero discrepancies**. All32 native calls were available and stopped normally; none approached the384-token limit (longest64). Owner complete/runtime-qualified/released in56.616s.

Primary supplied-target scores are **direct1/16, DSL2/16**. Eight DSL programs are valid. Within this fresh panel there is **one DSL win, no losses, one both-correct pair and14 both-wrong pairs**. All16 cases were inspected and retained. The old16 remains a separate result—direct1/16, DSL3/16, validity7/16—not a matched regression comparison or a pooled64-IID sample.

The sole primary disagreement is clear: the program divides Arkansas residential customers390668 by total customers438701, producing.89051; direct answered12%. Both arms correctly answer−864 for the mortgage-change question, and the program explicitly subtracts the correct1356 and2220 values. These accepted fresh-panel program answers are faithful arithmetic, not the doubled-total cancellation seen in one old-panel win.

There is also an unambiguous **semantic program success that remains primary-wrong**: the requested average of1923,2040 and1734 is1899, which the program returns and the written annotation agrees with. The supplied execution target is2850; its inert gold-program text adds3 to the sum then divides by2. This defect is documented, not repaired or filtered. Two direct answers also match ordinary displayed precision while failing strict target equality:−1.29% versus target−.01289, and4.27 versus target4.26667.

The other failures are informative, not merely parser trouble. One program correctly calls table_average then wrongly divides by3 again. Others use a whole-period denominator, reverse a change's sign, omit units, mistake column/date headers for numeric values, or select a different year's quantities. Whole-row aggregation is often inappropriate when rows mix years, totals and breakdowns; one invalid trailing-backtick program repeats the old cancellation pattern, but was not rescued. Neutral0 can trigger a false source-literal-presence flag even in the correct1899 program; literal presence is not a faithfulness metric.

Annotation and scale caveats remain: one target is percentage points where many others are fractions; some gold values use rounded prose instead of detailed table values. No gold-dependent scaling, rounding, syntax cleanup or target replacement was applied. [Every case is recorded](MECHANISM_REVIEW.json), including all these limitations and uncertain direct-answer causes.

Cost: direct19678 input/155 output tokens; DSL24414/460. Total44707 tokens, all usage known. Each arm uses one natural call per answer, but token costs are not matched. The result supports a narrow conclusion: the demonstrated interface can produce usable arithmetic on fresh pages, while source addressing, calculation choice and annotation quality remain limiting. With only one strict discordant pair here, general superiority is not established. These are prompt demonstrations, not weight learning. No further FinQA variant or GPU job was prepared.

Evidence: [independent result](RESULT.json), [native proof](../../../../../ARTIFACTS.md), [all16 mechanism review](MECHANISM_REVIEW.json). Independent RESULT SHA `af5cce0efab3866e86deb0f7b7f7fcae61eef84c4b65f8a02a5961e8143ee3fa`; native proof SHA `921492b89dc728d536c54a368587affefd2f6718800fcf0ffd0e75c18edb6c0a`; original run RESULT SHA `e245140628d68664ddbd785951f9716d34490b3b2b9f083dc72a7b2b65156b27`.
