# Independent prospective record-interface24 audit

Scope: runtime_port-authored `sidecars/root-record-interface-v1`, only `outputs/attempt-001`, successor `operations/2026-09-09-after-fixed-transfer-record-interface/attempt-001`. Auditor did not implement this study. Historical native-join source was authored by this auditor, so reuse is disclosed; no independent re-authorship claim for that underlying harness. No new outcomes have been inspected. At23:22:49UTC2026-09-09 the exact science attempt directory was absent. METHOD_READY records actual freeze time and source closure. Never execute sampled programs, mutate study/runtime/source, or control GPU/services/locks/queue.

## Frozen question and inventory

Retain all24 planned coordinates:8 paired world/seed blocks nested in4 previously exposed worlds; T sentence-inline/sentence-file, F sentence-inline/JSON-file, I JSON-inline/JSON-file. Independently parse all48 facts per world and compare ordered triples across every package, public file and prompt against WORLD records; verify customer/product domains, duplicate IDs and original queries. Independently compute the intersection of buyer sets for both requested products and compare host gold. No sorting/repair of sampled answers. Audit actual fixed no-adapter base manifest, role/depth routing, seeds, sampling2560/.6/.95, context8192, same advertised tools, exact first message/user/tool bytes and rendered token prefix. Distinguish preparation fixture from actual sampled requests. No acquisitions or copied historical source-gate credit.

## Native primary, uncertainty and pairing

Authenticate native final through exact full token branch and physical response, not `root_reply` or producer score alone. Require last root ordinary response, matching trace reply, finish stop/length, no tool_calls, authenticated prompt+completion IDs. Strict output: whole JSON array, strings only, known customer IDs, unique and ascending, equals independent gold. Capped but complete strict response may score; flag length separately. Malformed available completion scores0; unverified/missing native final remainsNULL. Never promote extractable prose, manually inferred answer or successful intermediate output into primary success.

For each arm and each world report planned/available/strict-valid/correct/NULL; strict correctness among available separately, operational planned-denominator correct/8 with nonreturn unsuccessful, and native-final sensitivity bounds[correct/8,(correct+NULL)/8]. Primary contrast F−T, secondary I−F, descriptive paired differences by eight seeds and four world clusters. List wins/losses/ties and missing-pair bounds. No24-independent-cluster or confirmatory significance claims. Historical percentages are not direct causal controls: truthful descriptions, formats and token lengths are bundled common/arm differences.

## Mechanism: executed dataflow, not markers

Retain all root/child assistant actions and tool observations in exact event order. Manually adjudicate each trajectory using code AND actual following outputs/errors/state: (1) attempted/successful evidence.dat open; (2) sentence parsing or JSON typed decoding; (3) faithful source literals versus omitted/altered/reordered/copy-only data; (4) grouping by customer, both-product membership/intersection, dedup/sort; (5) actual intermediate buyer/result sets; (6) agreement between observed computed result, strict native final and full source gold. An AST, open/loads string, final numeric/list agreement or code quotation alone is not execution proof. Child calls are genuine additional policy actions/cost, not automatic success. Record route misuse, parsing exceptions, zero/empty result sentinels, incomplete subsets, incorrect operators, correct intermediate but invalid terminal, and looping/context exhaustion separately. Unknown mechanism stays unknown; no generated-code reexecution. Trace logs may not expose successful file opens absent output; do not invent such observability.

The historical join report and its erratum (4/8 direct Python cases used relevant facts) are prior motivating evidence only; neither silently replace nor pool with this panel. Check the actual revised interface rather than assuming an architecture ceiling.

## Complete accounting and closure

Merge per-request initial, physical-wire and result records by authenticated request ID, retaining attempted calls even if an episode or collector died. Distinguish native logical requests, physical transport attempts, returned model completions, HTTP/pretransport rejections and missing response usage. Sum root/optional-child observed input/output/cache/uncached tokens with explicit unknown counts; never infer cached=0 or provider billing. Attempt≠confirmed GPU generation. Include owner/startup/collection/failure elapsed and host serialization/preparation costs separately; no extraction charges or fictitious hypothetical acquisition fees. Reconcile producer row/summary/cost/status versus independent raw inventory; flag discrepancies without changing sources.

Freeze completed output hashes only after terminal/release, or label a partial snapshot explicitly; preserve original24-slot inventory if interrupted. Report any material source/availability discrepancy immediately. Final artifacts: source/outcome pins, independent JSON/TSV inventory, per-coordinate trace mechanism evidence, concise REPORT.md and operations/2026-09-09-allocation-5780/record-interface-audit-report.md, final manifest. Monitoring uses bounded waits on the exact attempt; no busy polling or new scope.
