# Record-interface24: independent outcome audit

The typed-inline package produced **3/8 operationally correct native finals**, versus **2/8 for the typed-file offer** and **0/8 for sentence inline/file**. This is not evidence that the model loaded typed files: **all24 roots copied inline evidence into Python; none of42 sampled programs attempted a file read**. Every correct final followed an observed correct reduction. The strongest actionable finding is avoidance of brittle sentence parsing and copied-data/quoting failures, not a missing intersection operator or demonstrated file-interface benefit.

## Inventory, pairing and uncertainty

|Package|Planned|Native finals|Strict valid|Correct|NULL|Correct/available|Operational|Native bounds|
|---|---:|---:|---:|---:|---:|---:|---:|---|
|T: sentence inline/file|8|5|5|0|3|0/5|0/8|0–3/8|
|F: sentence inline, JSON file|8|7|7|2|1|2/7|2/8|2–3/8|
|I: JSON inline/file|8|7|6|3|1|3/7|3/8|3–4/8|

Strict means the whole native response is a unique, ascending JSON list of known customer IDs equal to the independent oracle. No extraction, reorder or intermediate-output rescue. All61 returned raw completions finish `stop`, not length. All24 planned rows are recorded; the five native-final NULLs remain NULL, and separately count unsuccessful operationally.

Correct counts by exposed world0/1/2/3 (two seeds each) are T **0,0,0,0**; F **0,1,0,1**; I **1,2,0,0**. Available counts are T **2,2,1,0**; F **2,2,2,1**; I **2,2,1,2**. Gold cardinalities are3,5,7,9; no zero-answer world. An always-empty strict answer scores0/8.

Primary operational F−T is+2/8: two wins, zero losses, six operational ties. Only five pairs have both finals; those have one win/four ties. Native missing-final sensitivity for F−T is **−1/8 to+3/8**. Secondary operational I−F is+1/8: two wins, one loss, five operational ties; six complete pairs have two wins/one loss/three ties. Its native sensitivity is **0 to+2/8**. Equal-size four-context mean differences equal these operational proportions; neither seeds nor24 trajectories are independent world replicates. There are four exposed clusters, no significance or new-world generalization claim.

## Actual dataflow and failure mechanism

All42 returned `ipython` actions have an authenticated subsequent observation, including actions in incomplete trajectories. Manual review covers every program and observation; no generated program was reexecuted. Static literal diagnostics preserve raw code and are not model execution or answer repair.

39/42 actions copy all48 source triples in exact order with no altered facts. Two I actions copy47, omitting the irrelevant-to-query `r38226/c1648/P`; both fail before reduction. Another I action copies45, omitting three facts including decisive `r42017/c2926/T`. It successfully decodes JSON strings, groups by customer and intersects requested products, but observed/final output omits c2926. Thus its operator is correct on incomplete copied data, not on the task evidence.

The two F successes parse copied **sentences**, using a correct regex or delimiter/terminal-period stripping; neither loads JSON. Three I successes use a flat literal triple list, literal dictionaries, or actual `json.loads` of a copied JSON array. Each yields the exact observed gold list followed by an identical strict final. A second I trajectory successfully uses `json.loads` but suffers the45-record omission above. Four other I trajectories never reach a completed reduction: malformed quoting or literal backslash-newline syntax causes errors; three then guess wrong final subsets (one nested-list invalid), one exhausts context.

Repeated sentence-parser errors dominate T/F: wrong split delimiters, retained product periods/prefixes/spaces, or catching an error and skipping every record. Nine available finals faithfully return observed erroneous `[]`. One additional NULL trajectory observes48 per-record parsing errors and `[]` but never returns a final. Another T trajectory returns a wrong manual list after three parsing exceptions. Across42 observations there are19 IndexErrors,5 SyntaxErrors,1 NameError,1 JSONDecodeError and16 observations without these exception classes (including the caught-per-record errors). No correct intermediate result is lost to terminal formatting in this panel.

All four transport-cancelled episodes show repeated policy parsing errors before the120-second limit (three returned actions in three cases, four in one), followed by an attempted request with no returned response. These are policy-associated bounded noncompletion with transport manifestation, not clean exogenous availability dropout. I/block4 has two returned malformed copied-data actions, then a **local pretransport** rejection: prompt8484 exceeds8192. No third physical model request or generation is established. Producer `KeyError: node` and independent frozen parser's missing-wire error are secondary incomplete-trace manifestations; original NULL and raw errors remain intact.

## Authentication, source and cost

The prospective method froze1788996229.345338 and parser1788996467.533522 while the exact attempt was absent, before actual launch1788999094.734001. Frozen files and source closure remain unchanged. The source audit independently verifies every ordered48-fact package/file/inline body, oracle, eight paired seeds and actual first prefix/tools for all24. Full graph/final branch authentication covers19 finals; additive raw checks authenticate **all61 returned completion token arrays and decoded native messages**, plus41 exact preserved-history continuations and tool-observation suffixes. The remaining continuation is the local pretransport rejection, not a wire request. Four timeout episodes lack completed episode/trace artifacts; their retained native histories support the observations, but do not supply a completed runtime file-attestation trace. This limitation does not invent file use.

Actual service descriptor/config agree: released Qwen3-4B-Instruct-2507 revision`cdbee75f17c01a7cc42f958dc650907174af0554`, no adapter/LoRA, BF16,8192 context, prefix caching disabled, four sequences, vLLM0.28.0. Actual sampling is2560/action,.6/.95,paired seed, no output grammar; all requests root-depth0, no child calls. All world facts are inherited from the exposed nativejoin panel; truthful representation metadata and prompt lengths are bundled changes. Prefix lengths T1335,F1347,I1650. The earlier join erratum's4/8 concerns report packages retaining all query-product facts, **not** direct Python successes. Historical percentages are not pooled controls.

|Arm|Logical requests|Transport attempts|Returned completions|Observed input|Observed output|Unknown usage rows|
|---|---:|---:|---:|---:|---:|---:|
|T|27|27|24|67,036|22,688|3|
|F|20|20|19|44,233|15,170|1|
|I|19|18|18|59,805|20,044|0|
|Total|66|65|61|171,074|57,902|4|

Observed cached tokens0 and uncached171,074; all four unreturned physical requests have unknown input/output/cache/uncached usage. Attempt is not proof of GPU generation or billing. No new or reused acquisition charge exists; CPU host serialization/rendering4.5401s is separate. Provider billing is unknown. Collector471.3364s; owner511.3598s including startup/release; parent command514.0912s, exit0/no timeout/GPU PID inventory empty at release. The preceding4565.23s queue wait is not this job's model/service time. All attempted work and failure time remain charged to this physical run.

## Decision and artifacts

Smallest useful successor: a contemporary matched **JSON inline+file versus JSON file-only** comparison, with identical schema/query/file metadata and no parser algorithm or forced file use. It directly tests whether externalizing the raw records induces actual file loading, reducing copy/quoting costs; the current F−T result cannot establish that mechanism. Keep four-world exposure and package effects explicit. More epochs, a capability ceiling claim, or repaired historical answers are not supported.

`AUDIT.json` is the unchanged prospective-parser output; `OUTCOME_PINS.json` pins the terminal attempt. Additive `TRACE_DIAGNOSTICS.json`, `NATIVE_ADDENDUM.json`, `MECHANISM.json` and `INVENTORY.tsv` retain all-coordinate/request evidence. All producer primary rows and aggregate costs agree independently. FINAL.json pins the complete analysis and operation report. Method/source authorship caveat: this auditor did not implement record-interface24, but authored the reused earlier nativejoin harness.
