# Prospective independent MNLI correspondence80 audit

Auditor scope: runtime_port-authored `sidecars/leaf-mnli-correspondence-v1/outputs/attempt-001`, no GPU/service/lock/launch/queue authority. No new outcome read before method freeze. `METHOD_READY.json` records actual source identity, timestamp and attempt existence. Record-interface24 has priority when MAIN signals its launch/terminal. Own contract-evidence24 is not independently audited by this agent. No sampled-code execution or source fixes.

## Source and exact experiment

Independently read cached validation_matched parquet and its actual Hugging Face class-label metadata; require0 entailment,1 neutral,2 contradiction, consistent with pinned dataset card. Verify revision da70db2af9d09693783c3320c4249840212ee221, parquet350c2695…, acquisition/checksum/card/license receipts. Preserve OANC/mixed terms and attribution, not blanket MIT. Recompute normalization (NFKC/casefold/whitespace), text-pair hashes/public m+12hex IDs, dedup/conflict/exact-three eligibility, bounded exclusions and label-blind hash selection. Check all128 premises/384complete pairs against source rows, original labels and original hidden IDs; no one-of-each-label assumption. No premise crosses contexts. Verify text-hash interleaving and all original text bytes. Check the named-input exclusion receipt's actual scope and skipped files; it is not arbitrary-history, near-duplicate or pretraining nonexposure. Record any structural label-dependent eligibility separately from actual label-blind rank/order invariance.

Audit all80 frozen coordinates:64 primary =8contexts×2pairedseeds×matching/constant×free/generic-shape;16matching/free singleton references are separate. Two contexts each government/slate/telephone/travel,48pairs/context. Eight context means are repeated-measure clusters;384records,128premises and2seeds are not independent treatment units. Freeze all planned item denominators before model work:768items/cell,3072primary item evaluations overall;16singleton items separate.

Independently parse every V2 ordered request, public record and schema. Source original pairID/promptID, genre, labels and parses must not enter public records or prompt bodies; full premise/hypothesis texts remain. Check directional NLI definitions and the amended anti-position/ID-inference sentence; superseded V1 files remain historical only. Shape grammar supplies48/cardinality, object fields/order intent, canonical label vocabulary and generic m[0-9a-f]{12} tags, not literal source IDs, membership, uniqueness or source ordering. Matching/constant differ only tag instruction; free/shape differ only structured_outputs. No tools, grammar oracle labels, partial text crop or answer-based replacement. Verify actual tokenizer control-field lengths, candidate selection and +175token residual over384source fields rather than claiming equal generated length or FLOPs.

## Native admission and independent strict scoring

For every attempted call compare actual ordered HTTP body/hash and native prompt token IDs with frozen V2 inputs; independently re-render prompts using pinned actual tokenizer. Require actual released base/no-adapter alias and manifest, one assistant choice/index0, no unverified reasoning/function branch, exact completion IDs/text or faithfully authenticated parsed-tool envelope, allowed finish and exact prompt/completion/optional total usage. Preserve authenticated wrong-route tool attempts as available strict0; execute no tools. Authenticated malformed output is0, missing/unverified/transport failureNULL. Finish=length flags truncation risk but does not discard a complete valid whole output.

Primary strict positional correct-label count is admitted only if the WHOLE array has exact48objects (singleton1), no duplicate object keys, exactly tag then label fields, generic-format tag and canonical label strings, plus every matching tag equals its displayed position or every constant tag equals the chosen control. Any full-contract failure scores0strict items. Never rescue partial arrays, extract prose, reorder by tags, forgive duplicate/missing IDs, or substitute shape-only semantics. Separately quantify complete shape validity, positional semantic correct count conditional on shape, field-order validity, per-position source/control tag matches, missing/extra/duplicate/wrong-order tags, exact whole-batch correctness, output caps and lengths. These diagnostics do not replace the primary.

## Estimates, availability and costs

Per decoder: matching−constant paired strict-label accuracy at each context (mean of two seeds) and eight-context mean; show individual seeds, context/genre descriptive results, win/loss/tie patterns and NULLs. Planned-denominator operational accuracy counts missing bounded outputs unsuccessful, separately strict accuracy among available and native-final bounds[observedcorrect,observedcorrect+NULLitems]/planneditems. Shape-conditional semantics and tag fidelity require their own denominators. Keep16singleton references separate and do not choose/rewrite batch outcomes using them. No confirmatory significance or global transfer claim from this adaptive task screen.

Reconstruct physical attempts from REQUEST checkpoints and returned responses from RESPONSE/native branches, not collector aggregate alone. Include attempted rows whose RESULT is absent, status failures, request elapsed and owner startup/failure/cleanup elapsed. Known token usage and unknown counts stay separate; caches/uncached unknown unless observed. Attempt is not proof of sampled GPU generation or provider billing. No acquired child maps or hypothetical extraction charges. Host selection/render time is separate. Pin all terminal attempt artifacts after MAIN release; reconcile every producer score/status/summary against raw independent evidence and report discrepancies immediately. Seal independent JSON inventory, concise Markdown report and final manifest additively; no source or historical artifact mutation.
