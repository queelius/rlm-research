# Independent prospective semantic-map48 audit

Auditor bridge_audit did not author semantic-map48 (runtime_port did). Some qualified ancestors were authored by this auditor; this is independent assessment of the new manipulation/run, not wholly independent ancestry. No new outcomes inspected. METHOD_READY records actual freeze epoch, exact source READY/closure, existence/count snapshot and parser/tests. Freeze is intended before launch; report actual timing rather than assume it.

## Inventory and estimands

Exactly8 acquisition slots and48 root slots:8 training00–07 contexts ×3 existing queries ×INLINE/FILE,24 paired seeds. Preserve every coordinate and acquisition failure, including roots never started. Primary strict authenticated dataset final uses whole reply.strip() matching Answer: nonnegative integer, exactly as frozen scorer; no prose number extraction. Malformed authenticated finals0, missing/unverified native finalNULL. Present C/N available accuracy, C/48 operational success, and [C/48,(C+NULL)/48] native-final bounds; arm denominator24. Co-primary mechanism is executed supplied-map-consistent scoped reduction, distinct from scalar agreement, syntax/AST mentions and dataset accuracy.

Pair by frozen block/task/context/seed; show both arms per block, per-context three-query means, operational FILE−INLINE differences and complete-pair descriptive counts. Eight clusters, not24/48 independent contexts. No inferential significance or confirmatory novelty claim. Operator/scope and zero/nonzero-gold are predeclared descriptive strata, not selected/resampled tasks. Report map-consistent final separately from dataset final.

## Source and native authentication

Check every READY source/input pin and actual served857 zero-effect reference adapter/c32 paths/config/base/tokenizer; do not copy stale campaign ancestry or bare-base equivalence claims. Audit actual binding/descriptor/live aliases/serving evidence. Preserve source label errors, never gold repair. Authenticate each exact source request system/user/seed/output1536 and public-only16 IDs/text; grammar requiredIDs and all six canonical labels, no gold literals. Check native prompt/output IDs, decoded content, sole branch, status/finish and usage. Parse full JSON object with duplicate-key rejection and exact ID set/canonical labels. Native length finish can qualify only if complete valid map. Missing/invalid source gates its six root endpoints; semantic errors alone never gate.

 independently recount all frozen dataset answers from public user/weight and hidden dataset label map, and all predicted-map answers from each actual raw acquisition. Map raw whitespace/key order/errors retained. For each eligible pair, verify prepared and actual first request differ only by appending '\n\n'+raw map; system/tools/sampling match. Check labels.json SHA equals exact returned raw map bytes and is identical in paired roots; records.json/context.txt/query.txt common bytes. Unlike record externalization16, context.txt must not mirror inline map visibility. Check query.txt is base unambiguous query, not arm prompt. Direct raw records remain external in both arms. Source_split inherited metadata may be stale: actual QSR/SFT training00–07 exposure governs, not catalog-new strings.

For every actual root/optional-child call, authenticate role routing, native wire prefix and sampled suffix, response text/tools and full ancestry branch. Match final root reply to actual non-tool native final, not producer aggregate. Root2048 action/8192 context; optional children retain qualified exact-ID transport. Check whole continuation token history, tool-call response placement and first prefix. Include canceled/failed attempts even if no final episode returned. Never run sampled programs.

## Mechanism adjudication

Read each retained code/tool call and its following real observation in graph order. Record evidence.dat/labels.json/records.json/context.txt/query.txt opens separately (evidence.dat is legacy diagnostic only, not expected current file), actual decoded map/record state, literal reconstruction/completeness/errors, overwritten or reacquired labels, selected IDs/users/category, count/distinct/weight operation, printed/returned intermediate scalar, and final concordance.

Per endpoint code co-primary as yes only when actual execution/observations support applying correct requested scope and operation to complete relevant supplied map/record state and producing its independent implied answer. Faithful literal reconstruction may count as executed supplied-map reduction but must be separate from actual file retrieval. Wrong supplied labels can yield co-primary yes with dataset primary0. A correct final without executed evidence is not co-primary yes. Observed failed/wrong reduction is no; absent or insufficient trace evidence is unknown, with bounds. Also report operational mechanism sensitivity counting all non-demonstrated slots as0, without changing unknown labels. Program AST or mentions alone never establish execution; no model-generated code reexecution.

Separate failure causes: source structural/native gate, exogenous service/transport, policy loop/context exhaustion, local pretransport context rejection, syntax/runtime error, wrong file/path/state, literal omission, wrong scope/operator, further reclassification, scalar-final mismatch/format, unstarted cap. Policy-induced failure remains operational failure and native NULL, not silently dropped exogenous dropout.

## Cost and timing

Deduplicate request checkpoints/results by authenticated request ID; never sum audit/physical/role copies. Separate8 source physical attempts, native root attempts, optional-child attempts, sampled returned completions, pretransport rejection, unknown usage/cache counts, and HTTP failure. Attempt is not proof of GPU generation; billing unknown/not measured. Count attempted elapsed failure time. Source reused once physically for six endpoints; hypothetical standalone endpoint ledger charges one full source acquisition per endpoint, explicitly not48 new physical acquisitions. Dataset/reference provenance costs separate from new execution. Compare paired root cost and full hypothetical pipeline cost.

Reconcile owner start/work2220/owned2370/outer2400,180 startup,300 source,1500 root,120 closure reserve,150 cleanup/30margin; parent EXIT/release supplied by MAIN. No cap extension/retry/run mutation. Freeze final actual output inventory/pins only after terminal/release relay. Concise first signal may precede manual mechanism seal and must be marked preliminary. Final REPORT/AUDIT/FINAL plus O semantic-map audit report include read depth, disagreement log, per-context results, NULL sensitivity and ranked smallest informative follow-up, not a claim of new general capability.
