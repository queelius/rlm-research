# Prospective terminal-strip-disabled arm: no material launch blocker found

Reviewed the sealed hook, wrapper, checkpoint/owner/collector seams, preparation and six-test source/receipt for READY SHA `686e484b6ffa0338b6b699537b10e4ea84f436a29d76baa0ec704f895a629d91`. A fresh CPU-only `owner.py verify --stage held-checkpoint32` passed with identity `f09078f6e68626be5d861752b7dabc6174572ec5c19e51d87222402f1d2efd31`.

The two hooks correspond to the observed boundaries. The parser compiles one hash-pinned trusted dependency function with exactly `content=text.strip()` changed to `content=text`; reasoning extraction, stop handling and tool parsing are otherwise unchanged. The RLM ACP callback first executes its original metadata/session check, then restores exact `ACPTurn.reply` to the trace. The callback is reached only after the successful-response check in the existing ACP method. Both hooks are scoped and restored; no gold-dependent patch or shared source edit occurs.

I independently replayed **all 66 actual saved cp32 actions** with and without the hook. Reasoning/tool parses did not change; no tool-bearing action's content changed; exactly eight terminal contents changed. This directly supports the intended archived-data mechanism. In general the parser's final content field can also precede a tool call, so the intervention is not structurally restricted to terminal packets. That did not affect this archived panel; check actual post-run tokens and prefixes rather than assume it can never happen.

The four archived actual native tool→final fixtures each have two captured requests, two ROOT returns, zero children, exact first prefixes and complete causal mapping. Exact spaces/newlines/curly punctuation reach the root and receive unchanged official reward 1. Extra line feeds and apostrophe corruption remain exact failures (with the original partial continuous scores, not forced zero); malformed tool JSON remains an empty invalid terminal. The other two focused tests preserve reasoning/tool semantics and restore the old parser after scope. I read these real fixture outputs and six-test receipt; I did not repeat the costly container fixtures. Their hashes and my fresh checks are in `TERMINAL_HOOK_REVIEW.json`.

The new owner reuses the completed fixed cp32, exact held16×2 schedule/seeds and frozen train gate. Original baseline and primary scores remain immutable. Limits: reasoning-adjacent newline processing is intentionally still present, so this is not a general lossless parser. Fresh sampling might differ despite paired seeds; even a 25-exact result needs action-level matching before assigning every gain to the hook. MAIN retains sole GPU launch authority.
