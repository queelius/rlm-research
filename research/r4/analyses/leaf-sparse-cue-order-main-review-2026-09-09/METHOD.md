# MAIN independent primary audit: sparse reminder field order

Written after all96 calls finished, before aggregate outcome inspection. MAIN
previously accepted/reviewed implementation and inspected one call's metadata
while locating raw artifacts; this is independent raw arithmetic, not a blinded
prospective external replication. No author scorer or previous auditor imports.

Reparse all original response text with duplicate-key rejection. Exact64 array,
objects only at positions1,5,...61, expected matching-source or constantp0000 tag,
assigned field order, canonical labels everywhere. Any full-format failure scores
all64 positions zero; unavailable request/response remains NULL, never repaired.
Score by displayed source position, never realign labels after the fact.

Preserve A matching/tag-first, B constant/tag-first, C matching/label-first,
D constant/label-first. For each full four-call context/seed block, compute
I=[(C-D)distance1-(C-D)distance0]-[(A-B)distance1-(A-B)distance0].
Each distance has16 positions/call. Average the two seeds within each of4 exposed
contexts/task, then contexts; no independent-label significance test. Positive
I means the matching benefit shifts toward the next slot under label-first.
No inference that hidden attention is the cause; different distances still hold
different records. Report all four distances, paired signs, class-count L1,
full64 correctness and costs. No endpoint promoted based on observed outcomes.

Authenticate frozen SPEC/READY, exact requested coordinate/wire/ordered schema,
complete native input token hashes and output lengths, actual model preflight
and available release records. All physical attempts count toward cost, even
invalid responses; known cache is separated from uncached input. Parent observed
zero return and empty GPU processes; missing ownedTERMINAL marker stays missing.
Compare implementer projection only after independent raw aggregation.
