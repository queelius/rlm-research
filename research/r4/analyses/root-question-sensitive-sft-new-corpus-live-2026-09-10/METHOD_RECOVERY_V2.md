---
title: New-corpus completion-v2 audit amendment
date: 2026-09-10
status: prospective_before_v2_readout
author_overlap: true
---

# V2 audit binding

The scientific comparisons and native semantic criteria in `METHOD.md` remain fixed. The original
attempt captured all 72 teachers but hid CUDA from training. Completion v1 preserved CUDA and loaded
the model, then exposed a second pre-optimization omission: the new-corpus package had never frozen
the six-row NLL gate inventory required by the qualified trainer. Neither attempt evaluated a
treatment checkpoint or readout.

Completion v2 adds one prospective input receipt. Its gate is T1, M1, J1, P1, P2, P3 from new-corpus
training contexts 00 through 05, mirroring the original gate's context-family layout. All six are
already members of the immutable 72-teacher corpus. Verify exact row equality, order, IDs, and the
gate receipt hash before accepting training. This is a disclosed infrastructure/input amendment,
not a post-outcome selection; no readout outcome existed when frozen.

Bind the six-update ancestry and metadata72 outputs only to
`root-question-sensitive-sft-new-corpus-completion-v2/outputs/attempt-001`. Bind teacher data only to
the original complete72 capture. Cost accounting must include that capture once, the original
18.524-second failed training subprocess, the 52.237-second completion-v1 owner (including model
load and its failed training stage), and all V2 training/readout costs. Do not call either failure an
optimizer update. Historical unchanged and original-QS6 comparators remain reused, not rerun.

I authored the source study, both recoveries, and this reader. This is an authenticated native
reconstruction with author overlap, not an independent implementation audit. Outcome reading remains
blocked until MAIN relays exact V2 owner terminal, parent exit, and clean release.
