# Prospective independent composition96 audit

Exact scope: `sidecars/root-operator-composition-transfer-v1/outputs/attempt-001`, READY `9cfd1febd1101ece4fd1f69d4a1e64909a7822f83ea0299cbc78d8e2284398b1`. Freeze before this scientific attempt exists; do not read outcomes until MAIN relays terminal/release. No model/GPU/services, source edits, sampled-program execution, rerolls or selection. Auditor did not author this study; shared ancestor operator/native/runtime contributions are disclosed. Existing CPU review independently verified128 raw-source records and48 task truths/native prefixes. Source exposure is root-new only under the named refreshed inventory, child-training/prepared-catalog exposed.

Primary panels are **primitive and composition separately**, each24 planned endpoints/policy. Fixed SFT24 versus SFT6, same48 coordinates/seeds, serial24 first. The experimental unit is eight paired context clusters; report all eight panel differences/bounds, individual paired task outcomes, six family cells and zero/nonzero strata. Do not treat48 tasks or128 records as independent experimental replicates. Seven zero answers remain (3 primitive,4 composition). Report strict native-final success, availability, available-only conditional accuracy explicitly labeled, planned-denominator operational lower bound and worst/best NULL bounds. Group differences use [lower24−upper6, upper24−lower6]; available-pair differences are separate and never replace planned views. No efficacy-selected checkpoint or historical-score control.

Independent primary is computed from pinned raw-source-derived truths in the CPU review, not copied from producer rewards. Native admission checks actual selected root/c32 role bindings, live service config/descriptor, first physical native prefix, final graph ancestry token identity, physical native completion, decoded final text, actual assistant final route and no unresolved tools on the final ancestry. Authenticate source-written RESULT claims independently; preserve all source values and report disagreements. The final root answer may be authenticated even when an earlier recoverable call failed. Per-call integrity findings must not silently turn every successful recovery into NULL. An authenticated final `Answer: N` whole-string after outer whitespace stripping receives dataset reward0/1; malformed authenticated finals are strict0. Missing/unverifiable final, unresolved tool branch or unreturned endpoint is NULL. Returned length-capped complete finals count under the same strict contract with an explicit cap flag. Orchestrator completion is not native availability.

Inventory every96 frozen coordinates, including not started, started with no physical receipt, physical attempt without RESULT, retained exception, trace/no-final and native-identity failure. Harvest each episode/physical file even if aggregate/owner harvest is missing; never let a stale/missing RESULT erase a genuine final or costs. Closure is exact OWNER_TERMINAL plus all started-service release receipts, or an explicit MAIN-supplied parent EXIT plus releases when the owner receipt is unavailable. Preserve failed closure, never fabricate a successful owner receipt.

Costs: union all on-disk physical records, including extra/unplanned directories, independent of endpoint availability and RESULT. Count confirmed transport attempts, returns, failed/unconfirmed returns, model root/child/unknown roles, known and unknown prompt/output/cache usage, and serialized native input tokens where known. Preserve unpaired role/typed request records as unconfirmed dispatch intent, not additional confirmed GPU generations; hard kill can prevent a proxy's final physical-file write. Reconcile matching physical versus role requests without counting the same call twice. Unknown usage is not zero; provider billing is unknown. Report wall times and concurrency separately, all failed-context costs, historical training/acquisition as historical rather than new evaluation inference. READY `no_new_acquisition` means external assets, not zero fresh c32 calls.

Automated parser extracts sampled root code, tool observations, native call paths, raw child replies and timing **as evidence only**. It must not label execution or faithful reduction from keywords, AST shape, scalar agreement or final correctness. Post-outcome manual annotations are a separate versioned artifact referencing exact node/request/file hashes. Review every successful endpoint and all available composed endpoints; also examine null/failure classes and contrasting paired losses. For each claimed mechanism, trace actual acquisition request→returned labels→root-visible observation/state→executed code→observed scalar→native final. Distinguish source-file reading, child reacquisition, accumulation, per-user grouping/threshold/max/cross-category use, literals/manual arithmetic, fabricated or wrong state, and errors/abandonment. A printed scalar is not sufficient by itself. Do not execute sampled code, repair maps or infer hidden state.

Map-consistency is a separate diagnostic only where actual complete or provably query-sufficient acquired state can be reconstructed from authenticated child returns/observations; retain semantic child errors and unknown state. Multiple/reclassified labels require exact executed last-state provenance, not a convenient union. Independently evaluate the six declared formulas on that observed state only after coverage is justified. Dataset correctness, map-consistency, and faithful observed execution are separate counts. Any post-outcome selector/annotation judgment is disclosed; no automatic code-match primary.

Exploratory decisions retain the approved gates: primitive improvement in≥6/8 context means with nonzero faithful successes; composition additionally needs≥2 composed families across≥4 contexts with faithful nonzero success. These are decision heuristics, not hypothesis tests. Missingness/bounds can make the conclusion inconclusive. Serial policy order, shared threshold/max subcomputation, source exposure and adaptive motivation remain limitations. Freeze parser/tests before launch; seal raw and report artifacts after closure.
