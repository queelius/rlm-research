# Prospective endpoint-RESULT admission clarification

MAIN clarified the source-study primary before parser freeze and before scientific launch. The original METHOD remains preserved. Its sentence about a missing RESULT not erasing a genuine final concerns **retaining evidence**, not authorizing primary completion salvage.

An absent/unreadable per-episode `RESULT.json` remains a planned **primary NULL**, even if an independently authenticated final can be reconstructed from `EPISODE.json` and native physical records. Such a final is retained only as `independent_native_diagnostic` and listed separately in `missing_result_native_final_diagnostics`, with raw provenance. It must not change the primary score, denominator, bounds or paired contrasts. Any unavailable source result caused by a scorer/verifier defect remains disclosed with the source value intact; no silent repair is authorized.

A missing/stale phase aggregate or owner harvest is different: reconstruct the original endpoint-level primary from existing per-episode RESULTs, authenticate their native evidence, and union all physical calls/costs. Missing endpoint RESULTs still remain NULL. Independent scoring may expose discrepancies in existing endpoint results, which must be reported explicitly rather than rewriting source outputs. Manual dataflow and observed-label consistency remain separate diagnostics, with no sampled-code execution.

The parser was updated prospectively to enforce this boundary; a focused regression supplies an authored complete native diagnostic with no endpoint RESULT and verifies primary NULL. No scientific sources, inputs, outputs, scheduling or admission rules were changed.
