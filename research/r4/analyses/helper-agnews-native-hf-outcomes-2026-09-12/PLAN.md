# Bounded source-to-output audit

1. One analyzer and focused actual prior AG B4 saved-native-call fixture. Authenticate
   training128 exact request/response bytes, chosen probabilities and host rewards;
   reuse qualified trainer/evaluator receipts, independently rederive simple counts,
   RLOO and importance diagnostics. Decode both64-call AG256 endpoint inventories.
2. Seal CPU watcher receipt and start CUDA-hidden four-hour watcher at30-second
   intervals. Stop at final queue/evaluator exit or horizon; write fixed-denominator
   REPORT.json/MD, explicit missingness and next-decision bounds. No GPU or source edits.

Evaluation limitation: the unchanged V2 collector saves native completion IDs,
decoded text, request/response hashes and usage, not whole wire envelopes. Audit
those against exact frozen bodies/schema; do not claim full eval response-envelope
authentication. Training does preserve exact wire bytes. This is a source-to-output
audit, not an independently authored trainer or fresh statistical confirmation.
