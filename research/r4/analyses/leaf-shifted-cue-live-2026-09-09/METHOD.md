# Independent shifted-cue72 audit method

2026-09-09. Frozen before reading this study's outcomes, while MAIN reports plan-SFT
still active and shifted72 waiting. The scientific schedule predates this method.
This auditor did not author shifted study/data/collector/scorer, but authored the
motivating design options and contributes to shared upstream harnesses. Independence
means an independent raw outcome reconstruction, not independent hypothesis generation
or a separately implemented inference system.

Scientific source: sidecars/leaf-shifted-cue-v1. Authenticate READY/SPEC and the exact
named source/input/weight/qualification files once in PREOUTCOME_AUTH.json. No output,
inference log, stored analysis or live model response is read during this preparation.
Operation: after-plansft-shifted72; MAIN supplied PLAN c36b9096… and acceptance c7aee4a9… .
Authenticate full operation hashes, actual process/launch, terminal, STATUS, FINISH and
owned service release only after MAIN's explicit terminal trigger. Failed startup or
cap stop is a valid terminal outcome with unavailable planned calls, not a rerun request.

## Population and strict primary

Exactly72 first-response component calls: 12 already-exposed contexts, four each TREC,
SST-2 and AG News;64 records/context;two seeds981327011/021;matching,constant,shifted.
24 context-seed triples. Weight is the fixed c32de validation-selected research adapter
on the pinned original Qwen3-4B base, NOT either released no-adapter model. No new data,
weight selection, historical pooling or whole-RLM inference. Preserve inherited split,
license and exposure caveats without treating four contexts as128 independent examples.

Reconstruct displayed ordered IDs/text/gold from DATA and bound lineage; source permutation0.
Matching tag=displayed record ID; constant=p0000; shifted=ID at zero-based(i+17)%64.
The input order and positional instruction do not change. Shift is a deliberately
contradictory semantic cue, not a neutral varying counter or an attention measurement.

From actual response text, independently parse JSON with duplicate-key/nonfinite rejection.
Require exactly64 objects, exactly keys IN ORDER tag,label, exact forced tag for that arm,
and canonical string label. Unlike the older Qwen144 scorer, this study's frozen primary
rejects reversed object key order. Empty, malformed, short/long, late wrong tags, unknown
labels or extra text are completed-invalid0 with no semantic vector. No extraction,
prefix salvage, tag-based score repair or decoder retry. An order-valid prefix does not
establish full schema validity. Infrastructure/error/cancelled/unrun/unverifiable is NULL,
not0; preserve raw-observable versus provenance-trusted denominators if they diverge.

Strict primary correct/64 means predicted label equals the DISPLAYED record's gold.
Report matching-minus-constant and matching-minus-shifted separately per task, paired by
context/seed. Each task/arm has8 calls and512 planned labels. Average two seed differences
within each of four context clusters only when both exist; retain incomplete contexts,
available descriptives and worst/best missing bounds, never silent complete-case weighting.
Report all72 planned/null/valid counts and whole64 exact success; do not change primary
to named-record correctness or select a preferred seed.

## Predeclared explanatory diagnostics

On complete valid matching/shifted arrays only, label alignment with the ID named by
the forced tag and named-minus-displayed difference. Constant named alignment=NULL.
Matching named and displayed necessarily agree, a useful integrity check. Shifted
displayed/named gold can coincide by repeated class labels. Freeze the DISAGREEING-GOLD
subset: positions where displayed gold differs from shifted-named gold, defined using
host gold alone before outcomes. Report its size per context and both accuracies, plus
neither-label responses and class confusion; never promote this subset to primary.
Report full64 profiles and class-count L1 only for complete valid vectors. Histogram
agreement cannot establish record correspondence; a shift preserves the gold histogram.
Give empirical marginal-label chance references as descriptive baselines, not a random
assignment model or significance test. Constant class predictions and repeated-label
agreement can inflate both aligned scores. Tags are grammar-supplied, not freely learned
ID retrieval. No causal internal-attention or generic model-family claim follows.

## Physical identity, provenance and cost

Before outcomes independently check72 unique coordinates, exact24 balanced six-order
triples, full request/ordered-byte hashes and24 equal native prompt-token vectors; remove
ONLY structured_outputs when comparing triple request objects. Reconstruct schema tag
vectors and ordered properties from public IDs, labels and shift17. At terminal join each
actual wire capture by coordinate and compare body UTF8/hash with frozen request bytes;
verify all actual prompt-token IDs and output-token IDs, alias, sampling, reasoning/tool
fields, stop reasons and token limits. Sampling:temperature.5/top_p1/top_k−1/min_p0,
max3072/context8192; use actual frozen body for all other settings. Four concurrent
individual calls, not guaranteed sequential triple execution: source schedules balanced
orders, audit actual timestamps separately. Same seeds do not guarantee identical samples.

Verify actual endpoint/service alias/base/c32 config and adapter hashes once, native
version/launcher/source closure and owned release. Use cached prior authenticated weight
closure plus appropriate immutable artifact stat/hash checks; no repeated large tensor
hashes or source scans per call. No GPU/service queries, signals, installs or rerolls.
The component route is native chat/completions with rendered tools, not recursive RLM
or root-training export. Tools are supplied but not executed.

Count actual HTTP/wire attempts and returned/unavailable calls separately; no retry inferred
from a missing response. Read raw wire usage, parsing a JSON-string body if present, for
prompt/completion/total/cached/uncached tokens and missing counters. Verify actual cache
configuration; do not assume it is off or cached usage0. Report matching/constant/shifted
realized output tokens and paired differences; same tag shape/multiset is not equal compute.
Include failed/null costs, all observed costs and jointly observed/valid costs distinctly.
Concurrent duration sum is not elapsed wall. Caps600 collection/750 work/870 owned with
120 cleanup reserve/900 outer; compare actual absolute times and no late raw-record drift.

## Output and bounded source verdict

After terminal perform one cached raw read, write ROW_LEDGER/POSITION_LEDGER/METRICS,
RAW_SOURCES and readable REPORT/seal only here. Send MAIN early primary/null/cost result,
then finalize context/order/disagreement diagnostics. Compare stored author scores only
after independent reconstruction; preserve any discrepancy rather than adapting endpoint.

Inspected study.py, driver.py, owned.py, DESIGN and RUNBOOK plus frozen static population
and request/CPU qualification artifacts. The inspected scorer explicitly enforces full
object order and shifted named alignment is kept secondary; the run adapter imports time
and declares counted72/cap substitutions. No material scientific scoring flaw identified
within this bounded inspection. This is not exhaustive runtime/lifecycle review; actual
native attempts and terminal integrity remain unaudited until the trigger.
