# A plain-language figure from sealed same-record evidence

Additive visualization only. Do not edit the sealed phase192 source/analysis or
pool it with the preceding sparse-ID or released-model experiments. Authenticate
METRICS69a9dd008323844589c5ada24f44b1b33a6c23f0927cd2802212f7dd580ac32f.
Recompute the plotted raw matching and constant accuracy from all24 complete seed
blocks, average the two seeds within each of12 contexts, then four contexts/task.
Each distance covers the same61 records per context. Show zero-to100percent axes,
all four distances, both arms, and all three tasks. No fitted curve, selected
context, smoothing, error bars or population confidence claim.

Generate data-native SVG/PNG and a compact numeric/provenance JSON in this new
directory. Inspect the PNG. Distinguish correct label proportion from whole-batch
success (none of the192 batches is entirely correct). The phase experiment uses
one fixed research-trained helper, not both released models. This illustrates an
observed local correspondence benefit; it does not measure internal attention.
