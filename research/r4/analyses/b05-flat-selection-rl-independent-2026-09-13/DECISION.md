---
question: Does one BA18 selection-only policy-gradient step change eligible-ID decisions and transfer beyond the nine training stages?
comparison: actual released base versus fixed sole step1
planned_calls: 72
material_units_per_split: 9
repeats_per_unit: 2
source_READY_sha256: a32cae2ac08ec93327805eef6afc886e8f15e20fd00361bff90488db614b2d52
mode: CPU_saved_native_one_completion_check
---

Verify every frozen model/prefix/seed/cap and actual raw response against saved tokens/text/logps/usage. Reuse the reviewed native decoder; independently calculate strict and unordered semantics, BA present-class rule, confusion and paired changes. Include unknown and malformed branches separately, physical call inventories and costs. Training arithmetic is trusted through the already qualified receipt, not reimplemented here.

Inspect every changed semantic set using host reference eligibility and rejection reasons derived from original public candidate records. These identify scoring consequences, not model reasoning. Distinguish sortedness changes from actual set additions/removals. Report train and held separately with per-width/context accounting; all-ID and empty baselines are mandatory.

Conditional next decision (proposal, not admission): held semantic gains beyond sorting warrant a fixed seed replication on fresh held stages. Train-only gains call for one bounded dose or fresh-item contrast, chosen prospectively, not an optimizer sweep. No local behavior change warrants checking saved probability/ID shifts before further training. Harmful validity or precision/recall tradeoffs require a concrete cause rather than aggregate promotion. Nine cases per split and one seed do not support a broad learned-delegation claim.

Run once after terminal. If absent, preserve PENDING and leave the command; do not poll or launch models. The resulting JSON retains full semantic outcomes and compact changed-candidate evidence; the Markdown avoids giant raw responses.
