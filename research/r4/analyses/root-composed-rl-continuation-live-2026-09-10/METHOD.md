---
date: 2026-09-10
status: before_final_readout_content_inspection_after_training_inspection
author: MAIN_coding_agent_not_human_reviewer
study: root-composed-rl-sparse-continuation-v1
training_attempt: outputs/attempt-002
readout_attempt: outputs/readout-attempt-002
---

# Fixed continuation audit

Training windows4..8 have finished. MAIN inspected their endpoint counts,
window decisions and optimizer-step reports before writing this note. The
fixed-last72 readout is running; no final-readout scientific content has been
read. This is not a preregistration. The schedule, last-checkpoint choice and
original native endpoint contract predate the run and are unchanged.

Authenticate exact READY_V2, the training OWNER/parent EXIT, all five native
exports and every checkpoint chain from saved Adam2. Check model/config/Adam/
RNG/state/correction hashes, actual optimizer cursors, trainable parameter names
and source identities. Keep the homogeneous sixth-window noop and all120
planned attempts. Report training successes as descriptive on-policy outcomes,
not an accuracy learning curve over interchangeable batches.

For the readout require its exact OWNER/parent EXIT, fixed72 native replay and
binding to the final training checkpoint. Compare to reused originalSFT start,
checkpoint1 and checkpoint2 on identical coordinate/seed/prompt definitions.
The principal interpretation is fixed-last minus SFT start; intermediate
checkpoints are descriptive, not candidates for best-model selection.

Report all72,48composed and24primitive outcomes, eight context clusters,
known-pair wins/losses/ties, observed-invalid zeros and missing endpoints as
NULL. Tight missing-outcome bounds retain known one-sided endpoints; they are
not confidence intervals. Strict final-answer correctness is not evidence of
faithful execution. Keep process-semantic review separate and explicitly mark
its extent and reviewer; no sampled code is executed for this audit.

Count new training and readout physical requests/results/HTTP choices and
provider input/output/cache usage directly, missing fields unknown. Historical
control calls are reused, not rerun or counted as new GPU work. Break out
rollout/capture, actual optimization, checkpoint and owner wall time.

The qualified checkpoint2 auditor supplies reusable loading, summarization and
raw physical-cost reading only; it never supplies checkpoint2 labels for the
new policy. Its source and the native exporter are pinned by hash. All new
analysis artifacts are additive; frozen producer and old reports remain intact.

Interpretation remains exploratory: one rank8 root training realization,
specialized tasks, exposed evaluation contexts, fixed imperfect child, and a
numerically qualified sparse-head implementation after earlier engineering
failures. The different batches and number of usable reward groups prohibit
treating optimizer ordinal as equal training dose across future LR runs.
