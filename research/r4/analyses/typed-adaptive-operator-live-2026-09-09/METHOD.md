# Prospective implementer analysis method: typed operators24

Frozen September9,2026 before opening any scientific rollout outcomes from typed-adaptive-operator-v1. The author implemented this experiment; all resulting REPORT/METRICS are IMPLEMENTER ANALYSIS, not independent primary validation. Main must commission/read independent primary review before promoting findings. The prior untyped adaptive outcomes and typed-operator preparation failures are already exposed design inputs. No outcomes of current operators24 were inspected for this method. Typed36 is separately independently audited by another agent and is not duplicated here.

## Frozen population and comparison

Scientific sidecar `sidecars/typed-adaptive-operator-v1`, READY SHA `a7b4c072d1e815ce89f4316816a50b1139c270edb07efd8dca67e7cae12b43e4`, SPEC `afbaa4192868e3d43d40041e45bf20d71634ed5972f31deacdf235512b877c8b`. Actual output `outputs/attempt-001`. Operation `operations/2026-09-09-after-typed36-operators24`, exact parent3732785/start1075629096, scientific launch epoch1788952704.714. Use frozen PLAN/PUBLIC/LOGICAL_CELLS/REQUESTS/CANONICAL_REQUESTS and source closure, authenticated once and cached, not repeated model tensor hashes.

All24 planned physical executions remain denominators: four exposed128-record TREC leaf-train-supported context clusters×two fresh seeds981292801/981292802×user_all/user_filter/global_shared. There are32 logical references, NOT32 independent samples: eight global executions serve both all16/filter16 references. Contexts are the four independent task clusters; two seeds per context are correlated repeats, not eight new contexts. Preserve frozen six-order-plus-two-extra balance and actual dispatch order. Fixed c32de child, exact original operator/public contract, image8cfe persistent private store (not the newly qualified local cache). Historical473210 root is a service role binding, not sampled root behavior in this operator experiment.

Primary comparison is typed user-filter16 minus typed user-all16 for strict final count correctness, separately all-planned correct/8, observable correct/observable, null/8 and paired complete outcomes. Show every context×seed pair, context summaries, gains/losses/ties and both/one/neither observable. Missing/null is never recoded to an observed failure or silently dropped. Shared global accuracy calibrates full128-record counting and is reported8physical, with identical logical references disclosed. No arbitrary confidence claim from four exposed clusters. No outcome selection/reseeding or comparison to a substituted checkpoint.

## Terminal gate and raw scoring

Do not read output text until scientific TERMINAL and rollout STATUS plus owned service FINISH/release/STOPPED evidence and operation completion/exit are finalized. Before then, existence/process identity/lifecycle timestamps only may be inspected, without stdout/inference/episode text. If terminal evidence is absent, report pending and await main trigger. A terminal failure remains analyzable with all24 planned/null accounting; never rerun an episode or repair its output.

Reconstruct each frozen execution from raw saved episode and coordinate, not merely the stored derived score. Match exact IDs and reject duplicate/unplanned coordinates. Recompute inherited `episode_metrics` semantics literally: traces must exist and all declare is_completed; exactly one trace yields a reply; strip surrounding whitespace, then require whole ASCII `Answer: ([0-9]+)` with fullmatch. Completed nonblank malformed output is observed0; completed valid wrong integer is0; correct integer is1. Empty/nonstring/missing reply, absent trace, or incomplete execution is NULL even if a partial transcript contains a plausible count. No Unicode digits, comma normalization, answer extraction, final-line rescue or partial aggregate rescue. Record schema-validity separately from completion/strict correctness. Check host-only frozen task gold and public record membership; do not inject gold into runtime or change task labels. Stored scores/rewards are comparison fields only; disagreements are flagged.

## Source selection, maps and aggregation

Use raw runtime operator transcript as a claim and corroborate against actual native wire requests/answers and trusted coordinate/depth/invocation identities. Transcripts are runtime-writable, not cryptographic proof of an authored instruction's execution. Expected selected IDs are all128 for user_all/global_shared, and the exact frozen query user's8 for user_filter. Preserve source order; expected batches are8×16 or1×8. Across planned24 this is136 child invocations (64user_all+8user_filter+64global_shared), not a claimed actual request count. Distinct canonical batch requests are36. Record planned vs transcript-entered vs native-dispatched vs returned vs strict-map-valid vs aggregation-complete separately. Ordinary unmatched or repeated child/model turns remain explicit; do not equate API invocation count with physical model request count.

For each actual child request authenticate exact public request bytes, requested IDs and selected public texts; schema comes only from requested IDs plus the six canonical labels. Verify actual nested native `sampling_params.structured_outputs.json`, required-ID/property order (using preserved ordered-schema JSON/hash and raw serialization where available), additionalProperties=false, label enums, trusted depth1 and correct c32de alias. Root requests, if any, are unexpected and must be shown—not invented as zero from an empty exporter. No child is credited to root training. Reuse already qualified graph/native exporter checks, add bounded raw correspondence checks rather than rebuilding the whole native framework.

Strict-map parsing rejects duplicate keys, missing/unknown IDs, nonobjects, noncanonical labels/nonstring values, nonfinite constants and malformed/empty JSON. Key-set validity and returned key-order equality are separate diagnostics. Parse raw native answers and transcript answers separately; record ambiguity where multiple requests/turns cannot be uniquely linked. Never rescue a map from prose/tool text. Once structurally valid, compare each ID's canonical class with host-only source labels: overall and per-context/seed/arm, selected-user subset vs all records, confusion counts and numeric false-positive/false-negative counts. Count repeated source records as repeated classification observations, not distinct examples. Aggregate valid maps using actual query membership, check exact reduction and final answer; a correct count with offsetting class errors is not all-record correctness. No new coverage/semantic reward is imposed.

## Physical cost and interpretation

Deduplicate physical native attempts by trusted request ID, retaining conflicting duplicate records as an integrity issue. Union requests across24 physical episodes; count each shared global request once regardless of its two logical references. Include orphan/unlinked/request-only attempts and unknown usage explicitly. Actual wire token IDs determine physical prompt length; returned native usage provides prompt/cached/completion totals, with known sums and unknown-call denominators. Report root and child separately, alias/depth, finish reasons, HTTP/native errors, latency, unconfirmed dispatches and inherited transient retries where visible. The frozen projected `model_calls` is not accepted as physical attempt count without linkage. Aggregate sum of episode durations is concurrent workload time, not elapsed campaign wall time; report collection/service/cleanup elapsed separately.

Compare filtering versus all costs both across all planned work and among paired completed correct successes. A failed early batch is not evidence of efficiency. Show actual child calls, physical prompt/completion/uncached tokens where known, and wall-time descriptives with null coverage. All vs filtered batches legitimately contain different semantic inputs; matched seeds do not guarantee identical actions/outputs. Child grammar constrains effective action space and excludes tool use from first response despite tools remaining described; it is not wording-only or semantic validation. This is a full operator-plan comparison, not an isolated learned routing effect.

Both operators load the full public records file before filtering, so fewer classified records/calls does not imply fewer host payload reads. No free/model root is studied and no learned adaptive planning/root-SFT claim follows. User gold2/1/2/2 means constant2 gets6/8; global gold16/18/22/20 varies. Selection skew and easy eight-record user subsets prevent attributing user accuracy alone to adaptive intelligence. These exposed developmental contexts, unknown dataset license and CPU composed qualification limits remain prominent.

## Outputs and bounded execution

After terminal, call the frozen implementer projection once (or call its analyzer directly without unnecessary lifecycle verification) and retain its raw output unchanged. Write supplemental raw-recomputed METRICS.json and readable REPORT.md only in this analysis namespace. Bind exact METHOD/source inputs/lifecycle/raw consumed file hashes once in a manifest; no live-log full rehash loops. If a raw or graph inconsistency blocks a denominator, show its missingness and scoped examples, not a repaired score. Publish salient denominators early after raw verification, then sealed artifacts. No GPU calls, source edits, process signals, fixture reruns or new training/evaluation. Await parent direction after report. Main still needs independent primary review.
