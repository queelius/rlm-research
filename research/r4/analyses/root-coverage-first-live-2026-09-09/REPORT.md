# Coverage-first48: independent outcome audit

Coverage-first improves strict answers over always-counts in both fixed roots, at higher observed model cost. This supports a narrow whole-RLM presentation intervention: withholding the partial histogram can encourage continued collection. It is not a general planning result, a fixed-trajectory mediation test, or a cost-saving result.

| Fixed root | Arm | Correct / planned | Strict format | Complete returned-map coverage | Root + child calls | Uncached input | Output |
|---|---|---:|---:|---:|---:|---:|---:|
| success-SFT8 66cce | map | 4/8 | 6/8 | 6/8 | 28+23 | 8,635 | 6,949 |
| success-SFT8 66cce | always-counts | 2/8 | 8/8 | 2/8 | 18+16 | 1,772 | 2,506 |
| success-SFT8 66cce | coverage-first | 5/8 | 5/8 | 5/7 known, 1 unknown | 42+44 | 5,702 | 6,481 |
| efab final4 | map | 2/8 | 3/8 | 3/8 | 55+53 | 6,294 | 11,706 |
| efab final4 | always-counts | 0/8 | 6/8 | 0/8 | 16+8 | 1,242 | 1,094 |
| efab final4 | coverage-first | 2/8 | 3/8 | 5/8 | 24+25 | 8,195 | 5,465 |

All48 planned coordinates were attempted and have an observable endpoint under the frozen rule;0 endpointNULL. One completed-empty response scores0 and is separately marked as a harness failure, not interpreted as ordinary semantic failure. Four exposed context clusters per root, two seeds each:48 episodes are not48 independent tasks.

## Paired primary and interpretation

Coverage-first minus always-counts gives3wins/0losses/5ties for success-SFT8 (+3/8), and2wins/0losses/6ties for efab (+2/8). Context-mean differences for validation00/01/02/03 are respectively [1,0,0,0.5] and [0,0.5,0,0.5]. Each root benefits in two contexts, not uniformly. Targets are public NUM/HUM/ENTY/LOC with gold global counts3/6/14/11, not four copies of a NUM task. The between-root difference in effects is secondary and only1/8.

All16 primary pairs have exactly equal initial root token inputs **and** sampled initial output token sequences here. All48 initial prefixes match frozen full native IDs, with no root grammar; paired-seed matching remains a variance-reduction intent, not a general determinism guarantee. The intervention starts only when a delivered eligible child map changes a later tool observation.

Every31 strict final answers equals the last ledger histogram's count for the publicly asked class, including wrong partial counts. This is a descriptive consistency check against returned model labels, not verification of semantic truth or proof the root read the summary. Coverage-first reaches complete returned-map coverage in10/15 episodes with available ledger traces, versus2/16 for always-counts. Completion does not ensure a correct final answer: efab coverage-first completes5 but answers only2 correctly, and only3/8 outputs are strict. The map baseline already completes9/16 and answers6/16 correctly. Thus coverage-first mainly improves on a harmful partial-count display; its advantage over unchanged maps is only1/8 for success-SFT8 and0/8 for efab.

## Native/projection audit

Independent endpoint and cache primitives—not the author outcome scorer—reconstruct all48 rows. All352 physical request starts have returned native result records,183 root/169 child. Actual endpoint descriptors and live model cards match both adapter paths/hashes and the fixed c32de child/base. These are research adapters on released Qwen3-4B, not two released-model families. Inherited SPEC campaign_policy/selection metadata still names historical473210/child selection; the actual role models are the authoritative served-weight evidence and were checked separately.

All352 forwarded native bodies match the parallel typed-capture bodies; prompt IDs match normalized prompt IDs, actual aliases and model hashes match the fixed bindings, seeds/temperature match coordinates, and root structured output is absent. Sampled completion IDs appear in the exported graphs for every request.278 have a unique exact-output node candidate;74 have multiple candidates because outputs repeat. This audit does **not** turn token equality into a unique per-node ancestry claim. Physical request IDs/invocations, not the ambiguous graph matches, define costs.

159 eligible returned-map claims round-trip the public ID/text catalog, pass independent duplicate/missing/unknown/canonical-label checks, and match external native child response bytes plus the source-bound native decision request. All159 delivery claims were replayed with deduplication/conflicts. All125 recorded root tool observations match the independent projection and have zero sampled loss/logprob credit; each appears in a later physical native root prompt.104 observations are partial and21 complete. Coverage-first omits the added counts key only when partial; complete coverage-first suffixes match always-counts byte-for-byte for the same state. Map observations preserve the original text. Original truncation is preserved, not counted as intervention. No observed projection mismatch or missing later-native containment.

Runtime ledger events remain root-writable claims: external wires corroborate returned values and actual presented observations, but do not prove Python helper origin or that a caller consumed a delivery.169 physical child requests are not159 API deliveries; repeated/extra model turns must not be silently equated with helper calls. Existing qualification was an actual5-call CPU native fixture, not learned behavior; its source/projection review remains separately sealed.

## Failures, cost and limits

Coordinate6aecc3b6… (success-SFT8, coverage-first, validation03, seed981312011) has a completed-empty root reply and a real ACP/IPython `set_broker_scope` `_queue.Empty` harness error. Its initial native request returned, but no child/ledger state was exported. Frozen completed-empty scoring is0; coverage is **unknown**, not0. It is not a provider outage, finalization-only timeout, or evidence the root deliberately stopped early. The other47 episode records are ok. No unrun coordinates, provider-result gaps or endpointNULL are hidden.

Union cost:527,568 input tokens =495,728 cached +31,840 uncached, plus34,201 output tokens. Cache details are known for all352 from parsed JSON-string native response bodies. Root cost is363,063 input/349,552 cached/13,511 uncached/21,093 output; child164,505/146,176/18,329/13,108. All costs include failed/wrong episodes. On the success root's only two jointly correct primary pairs, calls increase16→20 and uncached input732→1,465 (output1,688→1,276). E fab has no jointly correct primary pair. This selection-conditioned comparison does not establish efficiency.

Owned science wall779.796s (outer reported780.310s), inside2670owned/2700outer. Success-root collection336.680s; efab341.543s. Each root's service startup was about46.46s; both explicit owned-process release records confirm identities exited and ports free. No training occurred. Sequential service order, cache history, exposed contexts and small clusters limit generalization. No architecture-efficiency inference.

## Research consequence

Retain coverage-first as an exploratory harness candidate, not an established best interface. The strongest next question is whether completing evidence still fails at final answer formatting/aggregation under an otherwise matched task. The already approved paired plan-SFT study addresses query-sensitive selection separately; do not change its corpus, targets or LR from these outcomes. A future small fresh-context map-versus-coverage-first comparison is more informative than pooling this exposed screen or expanding always-counts alone. No new experiment is authorized by this report.

Independence is bounded: the auditor did not author coverage-first's projection/collector, but did author its inherited ledger/native adapters. Raw inputs were cached once for primary/projection replay; finishing work reused row/call ledgers and added small request-start/release evidence. Full raw hashes,48-row ledger,352-call cost ledger, paired metrics and focused parser tests are linked by SEAL.json. MAIN's independent primary review remains separate.
