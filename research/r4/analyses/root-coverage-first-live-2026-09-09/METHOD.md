# Coverage-first48: pre-outcome audit method

2026-09-09. Written before reading experimental output rows, episodes, scores or native outcomes. Source/spec and CPU qualification code may be inspected before the method; synthetic qualification artifacts are not model outcomes. Audit begins only after MAIN's terminal/release trigger. No GPU/service authority or source/output mutation.

Independence boundary: this auditor did not author coverage-first's projection, plan or collector, and will implement its own endpoint/usage/ledger reconstruction without importing an author outcome scorer. The auditor **did** author the inherited accumulation ledger/native adapters and the new high-LR sidecar; this is not independent of the common harness. Scope is a bounded source/projection and raw-result audit, not exhaustive runtime certification.

## Frozen experiment and primary

48 planned physical episodes: two fixed roots (success-SFT final8 `66cce4009628ff7d8e047f08e6fb3a29febdbf8fb3ed8303871f69860f1151d5`, efab final4 `efab2913e7fe9f5f9b381ae6eb67bb56071070f86b237e6145f98654816aad64`) × map/always_counts/coverage_first × four exposed global contexts × seeds981312011/021. Fixed childc32de and immutable image8cfe. Per-root24; per-arm/root8. Four context clusters, two within-context seeds; no claim of independent48 tasks. Root services execute success_sft8 then efab; retain order/service confounding.

Primary is paired coverage_first minus always_counts strict correctness within each root, with each context's two-seed mean, eight coordinate pairs and four context effects. Map contrasts and the difference between root-specific treatment effects are secondary exploratory. No pooling old outcomes or new checkpoint selection. Paired seeds are variance-reduction intent, not identical sampled initial actions.

Independently use whole-string `Answer: ([0-9]+)` after surrounding whitespace stripping, ASCII digits only. Completed observed correct integer1, other observed text including completed empty0. Unavailable, unfinished, censored and unrun are NULL, never silently0. Preserve strict raw-observable endpoint alongside episode.ok, finalize/runtime errors, provider status, graph/capture validity and missing evidence. Do not erase an observed final response solely because later finalization failed. If physical provider failure makes observation ambiguous, expose both raw candidate and unavailable primary with reason. All48 coordinates remain in the ledger and denominators; give planned, attempted, raw-returned, observable, valid-format, correct and NULL separately.

## Source and preparation audit

Authenticate READY, SPEC, complete new source, input hashes, and source-pinned inherited observation/supervisor/native seams once and cache the closure. Read actual model descriptors/cards at outcome time: root alias→path→adapter/config and fixed child/base, not decorative campaign metadata. The SPEC retains inherited historical `campaign_policy`473210 metadata; audit actual role models independently and flag this provenance caveat, not misidentify the served weights.

Projection review: map appends nothing. Both ledger arms use the same source-bound delivered-map tracker. Coverage-first omits only the `counts` key from the added suffix while partial; after complete conflict-free coverage its suffix must equal always_counts byte-for-byte for the same ledger state. Original tool text and its original truncation are unchanged in all arms; no removal of counts already printed by the policy. Summary includes six canonical labels when enabled, coverage/resolved/unqueried/conflicts/partial always. Repeated IDs deduplicate; disagreeing labels remain conflicts excluded from counts. Unknown IDs, altered source text, malformed maps, wrong depth/parent are ineligible. No host gold/asked-label lookup or forced tool use.

Inspect actual five-call CPU fixture artifacts, not just RESULT assertions: three root and two child requests, typed grammar only on source-bound child calls, exact returned-map bytes and public requested IDs, partial omission then complete full summary, same content in real tool nodes and later native root input, no tool-node sampled mask/logprob credit. Compare independent state projection against raw fixture observations. This fixture exercises coverage_first/efab only; complete-versus-always equality for common state and map nonchange additionally rely on source/CPU state checks, not a separately completed three-arm native fixture. Synthetic provider logprobs are scaffolding, not actual learned behavior.

## Outcome raw reconstruction

Read frozen coordinate plan and host-only gold/public records, then each row/episode/native request/result once. Bind file SHA and raw episode reference. Reconstruct task/gold from frozen source, not row score. Verify exact48 unique coordinates, within-root/context/seed triples and dispatch rotation, missing/unrun handling and cancellation completion before terminal cardinality. Validate initial prompt text/full token IDs/alias/sampling/absence of root grammar for every available first root call. Compare decoded/token initial actions separately from input equality, and record earliest ledger-eligible delivery/observation.

Replay ledger claims per episode and root invocation from native-corroborated delivered child requests/results: exact public ID+text round-trip, exact canonical label map with duplicate/missing/extra rejection, eligibility, delivery, deduplication and permanent conflicts. Record requested, observed-returned, delivered-claimed, corroborated, unique/resolved coverage and complete-state timing separately. Check original raw child answers unchanged. Compare expected projected suffix to each actual tool observation and later native root input. A broker drain is not proof caller read/used a result; runtime-writable event logs are claims, corroborated where possible by external native wires and graph ancestry. Missing logs/corroboration are unknown, not zero uptake.

Measure whether root actually reaches complete coverage, answer equals its full or partial observed histogram, and remaining source records at final response. These are descriptions of returned model labels, not semantic truth; host-gold comparisons are audit-only. Changed observations can change future plans/calls. This is whole-RLM behavior under a presentation intervention, not fixed-trajectory mediation, learned planning proof or an access-control guarantee against reading full runtime logs.

## Cost and failures

Union actual physical request IDs per root/child; preserve attempted/no-result/failed/ineligible calls and any inherited retry evidence. Full sampling and output token IDs are primary wire evidence. Parse `native_wire_response.body` when it is a JSON string; use `usage.prompt_tokens_details.cached_tokens` from that body, not normalized response fields that can drop cache data. Report input/cached/uncached/output tokens and availability coverage, root/child split, per arm/root/context and success-conditioned secondary costs with explicit selection. Never infer token counts from characters or silently treat unavailable cache detail as zero.

Report collection/startup/training-none/service cleanup/wall separately, owned2670/shared2550/900 per-root and outer2700 cap accounting, terminal/release paths and unrun phases. No architecture efficiency inference from sequential wall time. Report every infrastructure failure and distinguish score/graph-null conclusions from learning effects.

Publish independent scorer tests, a48-row machine ledger, METRICS, readable REPORT, source/raw-reference hashes and final SEAL. Do not change this method after outcomes; any diagnostic expansion is an explicitly dated additive note. MAIN independently reviews primary results before synthesis.
