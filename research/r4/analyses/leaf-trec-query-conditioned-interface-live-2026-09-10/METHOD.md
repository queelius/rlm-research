---
id: leaf-trec-query-conditioned-interface-v1-native-audit
status: prospective_pre_outcome
date: 2026-09-10
planned_endpoints: 192
---

# Query-conditioned TREC192 native audit method

Audit every frozen endpoint after exact terminal relay. Reconcile the recorded
request body with the frozen body, authenticate the unique native completion
against model, prompt tokens, completion IDs/logprobs, usage, finish branch and
renderer, then independently apply the frozen full-six or A/B/other contract.
Completed malformed outputs are observed zero; absent, failed, or incoherent
native envelopes are NULL. Never repair a map or infer a response from prose.

Report four 768-label cells (base/c32 by full6/A-B-other), projected A/B/other
accuracy, exact 16-record batches, and TP/FP/FN for each projected class. The
full-six output is projected only after an exact complete six-label map. The
constant-`other` reference is exactly 512/768 labels (2/3) in every cell by the
six cyclic-pair construction; it is descriptive, not a sampled model arm.

Use the 48 batch/pair blocks as matched units and eight batches as correlated
source clusters. Report interface effects within model, c32 effects within
interface, their interaction, per-pair/per-batch effects, native availability,
NULL bounds, and the full REQUEST/RESPONSE/RESULT physical union with known and
unknown input/output/cache usage. The official-test panel is research-exposed;
this is exploratory child classification and implies no root result.

This reviewer authored the study and reader, so the audit is reproducible and
raw-native but not author-independent. No outcomes were read while freezing
this method and parser.
