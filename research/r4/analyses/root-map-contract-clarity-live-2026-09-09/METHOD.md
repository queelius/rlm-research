# Independent clarity96 native/state-use audit method

2026-09-09. This auditor authored runtime plumbing, the supplied-map reducer audit and prospective SFT/partition-backup code, NOT the clarity scientist collector or its outcome scorer. Before this method: read clarity design/protocol/collector/metrics and input-plan structure only; no current clarity outputs, cell scores, raw episodes or TERMINAL were read. MAIN reported completion/release and later disclosed seeing aggregate availability/correctness; MAIN is not outcome-blind. The auditor was told exactly one endpoint is unavailable, but no correctness aggregate/cell value. Historical visibility near-ceiling supplied-map consistency and older reducer/complete-SFT outcomes are known. This is a source-informed, prospectively specified independent audit, not a claim of complete outcome blindness.

Audit all96 fixed coordinates: schema-field factor F and bundled map-contract wording factor M, each2levels,24 paired seeds nested8 context/query blocks and4 source contexts. Do not substitute96 independent tasks or24 independent source samples for the four context clusters. M changes source-label scope, prediction provenance, filename salience and explicit child permission together; do not call it a pure contradiction-removal effect. Optional API example, map-file-only delivery, actual maps, rootRL4 and childc32 remain common.

First hash the complete finished current-output inventory and parent terminal artifacts without parsing their values; verify scientist READY/OWNER_READY and exact input/model map-source pins. Retain this METHOD_READY before parsing current outcomes. Then independently reconstruct native availability, strict reward and costs from graph episodes and raw role/typed captures, not scientist rows/aggregate flags. Rows provide a comparison only after reconstruction.

For every endpoint require actual root alias/adapter binding, initial prompt native IDs, final physical root capture, and unique native branch prompt+completion token identity. A returned stop/length scalar-text branch must equal the graph root reply and have no outstanding tool calls. Whole-strip ASCII `Answer: N` is the strict contract. Authenticated malformed final is0, missing/failed/unverified/pending-tool final is NULL. Do not zero-impute the known unavailable endpoint. Show planned and available denominators, format and supplied-map consistency separately. Independently recompute requested IDs from real `user` and query family and semantic gold count from scoring-only source labels; preserve map errors.

Rebuild all24 four-cell seed blocks; report each cell and paired F/M contrasts at the other factor fixed, block-average factorial effects/interaction only on required available pairs, then query/context aggregates. Show unavailable contrast denominators. No significance or broad generalization claim from96 correlated trajectories. Report repeat variability and four-context dependence plainly.

State use requires executed trace evidence, not prompt/AST presence: inspect code as text/AST only, match each action to its actual tool return, track successful loading of exact labels.json, actual requested-user filtering and reduction to a scalar, and distinguish successful use, failed attempt, recomputation, unsupported final or ambiguity. Do not execute sampled code on the host. Count new child requests separately and inspect whether roots reclassify instead of using the supplied map; scalar/map agreement alone is not proof. Review all96 final branches in a compact raw ledger and inspect all gain/loss/error/ambiguous mechanisms. Purely descriptive markers remain labeled markers if not execution-qualified.

Use raw native wire usage to total new physical root/child calls and input/output/cached tokens, retaining unknown values. Charge every hypothetical endpoint its entire relevant historical acquisition, separately from physical new calls; historical acquisition is reused, never newly paid or silently amortized. Synthetic historical authored wrappers are transport only, not sampled policy. Audit map-file byte hashes against fixed inputs and recorded runtime setup/overlays when available.

Seal a bounded audit report and machine-readable rows, contrasts, state-use annotations and source/output hashes. Rank next tests from actual changes and limits, not automatic training/epoch extension. Only this new analysis directory and O/contract-clarity-audit-report.md are writable. No GPU/service/container/lock action, source mutation, new acquisition, generated-code execution or old-study broad audit.
