# Clarity96: accurate fields help; map agreement mostly does not establish reuse

Independent CPU audit, 2026-09-09. All96 fixed coordinates included. I authored runtime plumbing and other studies, not this scientist collector. METHOD_READY was frozen before I parsed these outcomes; I knew one endpoint was unavailable and knew prior study results. MAIN had seen the aggregate before this audit, not per-cell traces. No outcome blindness claim for MAIN. No sampled Python was executed by this audit.

## Endpoint result

| Cell | Correct / available (planned) | Strict format | Supplied-map-consistent | New child calls |
|---|---:|---:|---:|---:|
| F0M0 | 14/23 (24) | 18 | 17 | 30 |
| F0M1 | 10/24 (24) | 15 | 13 | 31 |
| F1M0 | 17/24 (24) | 22 | 21 | 25 |
| F1M1 | 17/24 (24) | 24 | 23 | 21 |

58 correct among95 available,96 planned;79 strict finals. Independent reconstruction agrees with every scientist availability/reward row. All95 actual final token sequences decode to the recorded final text and match native graph ancestry; actual model, physical prompt/completion IDs, usage and sampling checked across367 calls. Initial96 prompts rerender identically. All95 finals stopped normally, none reached length. Strict parsing is whole-strip ASCII `Answer: N`; prose containing a correct answer remains observed invalid0.

The sole NULL is F0M0, validation-00/single_user/repeat0, coordinate `ba60e5934ffd8331d2bfa83180ace701a4614f5dc92b4bb7be85b7d8a42eeaa3`. After an observed metadata KeyError, the root requested a record inspection. Its native tool branch is authenticated, but no tool observation or final was returned: Jupyter broker setup failed with `_queue.Empty` inside `get_shell_msg(timeout=30)`. This is a genuinely unreturned endpoint, not an observed final answer and not an invalid wrong-route completion. No zero imputation. Parent exit0, job750.250s, GPU PID list empty after release.

F replaces misleading field descriptions with actual `id`, `user`, `text` and directs user filtering to `user`. M jointly changes source-label scope, map-as-predictions wording, filename salience and explicit permission for additional children. It is a bundled intervention, not pure contradiction removal. Optional first-four API example, file-only actual map, RL4 root and c32 child remain common. Root adapter SHA2286be3f7c0c9cc0e22c8ef8e3473b7d8eb6ec4b7a789ca380a0af9d3b944c71; child SHA c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3.

## Paired contrasts and dependence

F at M0: +2/23 available paired blocks (3 wins,1 loss); F at M1: +7/24 (7 wins,0 losses). M at F0: −4/23 (1 win,5 losses); M at F1:0/24 (all ties). Four-cell interaction sum+4/23,5 positive and1 negative blocks. On the23 complete blocks, equal-seed-block factorial main effects are F+4/23 (+17.4 percentage points), M−2/23 (−8.7 points), interaction+4/23. These are descriptive correlated-rollout contrasts, not independent-source estimates.

There are only4 source contexts,8 query blocks,3 paired seeds each—not96 independent tasks. The complete-case interaction's entire net+4 and M's entire net−4 occur in validation-01. Context sums of F at M0 are +1,0,+2,−1; F at M1 +1,0,+3,+3 (train-00,train-01,validation-00,validation-01). One missing comparison is in validation-00. Per-query correct counts below retain three planned repeats; starred count has only two available.

| Context / query | F0M0 | F0M1 | F1M0 | F1M1 |
|---|---:|---:|---:|---:|
| train-00 / single | 2 | 2 | 3 | 3 |
| train-00 / union | 3 | 3 | 3 | 3 |
| train-01 / single | 0 | 0 | 0 | 0 |
| train-01 / union | 0 | 0 | 0 | 0 |
| validation-00 / single | 1* | 1 | 3 | 3 |
| validation-00 / union | 2 | 2 | 3 | 3 |
| validation-01 / single | 3 | 0 | 3 | 3 |
| validation-01 / union | 3 | 2 | 2 | 2 |

## Executed mechanism, not AST presence

All165 root tool actions were read as88 normalized program groups, linked to actual tool observations and labeled by data flow.164 returned an observation; one inspection did not. Each successful scalar reduction was checked against an actual scalar output. Supplied-map scalar outputs all equal independently reconstructed requested-user map counts. Python source was parsed only; it was never evaluated. STATE_USE_V2 is the authoritative chronological annotation; the earlier scratch STATE_USE used group order for selecting the last scalar, corrected before report sealing with unchanged aggregate metrics.

| Endpoint mechanism | F0M0 | F0M1 | F1M0 | F1M1 |
|---|---:|---:|---:|---:|
| Successful supplied-map load | 11 | 1 | 18 | 18 |
| Executed scoped supplied-map scalar | 2 | 1 | 10 | 3 |
| Scoped supplied-map list, no executed count | 1 | 0 | 1 | 0 |
| Executed scoped new-child scalar | 15 | 9 | 10 | 17 |
| Any new child endpoint | 24 | 24 | 20 | 21 |
| Observed KeyError endpoint | 8 | 10 | 0 | 0 |
| Any executed scalar, including wrong scope/heuristic | 18 | 11 | 20 | 20 |
| Strict final agrees with last executed scalar | 16 | 7 | 20 | 20 |

Categories can overlap. Only18/96 endpoints demonstrably use the supplied map for a scoped scalar or materialized scoped list;16 execute the supplied-map count.48 actually load it. Many load into `labels` then overwrite with `strict_map(child.answer, ...)`, or load into `labels_json` while reducing the separate new-child `labels`. F1M1 has18 loads but only3 supplied-map scalar reductions;17 reductions use new children. Thus23/24 supplied-map agreement does not imply23 map users. The optional example is often copied as a first-four acquisition, adding a child before the real query work.107 new child calls occur across89 endpoints despite all endpoints already having a map.

F eliminates all18 observed field-error endpoints. One F0M1 endpoint also returns two SyntaxErrors before metadata/text-field repairs; neither failed attempted load is counted as a successful load. Two actual wrong-scope scalar endpoints and one text-heuristic scalar are separated from true scoped label reduction. One program mentions labels.json only in a comment; it is not counted as a load. A literal map copied into a later action is counted as new-child state use only after matching its preceding genuine whole-map observation, not as supplied-file use.

Concrete paired traces (full coordinates, code and returned observations in RAW_BRANCHES/AUDIT):

- validation-00/single/repeat2: F0M0 `2d53ed0c8c74…` hits synthetic-metadata KeyError, then wrongly treats record IDs as user IDs, produces0 and a prose-invalid final. F1M0 `8b3d995eb52a…` filters actual `user`, loads the map, executes2 and returns strict2. F0M1 `64311e190831…` eventually repairs two syntax errors and two wrong-field accesses and executes correct2, but final prose is still invalid0. This is both genuine recovery and remaining output-contract failure.
- validation-01/single/all3 repeats: F0M1 returns prose-invalid correct-scalar descriptions; F1 cells return strict0. These3 paired improvements should not be called gains in semantic classification. At repeat2, F0M1 `986e80421b0e…` executes a correct child-derived0 after KeyError repair, yet adds prose. The exact answer parser is unchanged.
- validation-00/union/repeat2: F0M1 `08552b9437ae…` prints a newly acquired map but performs no count, then answers4 despite the observed map supporting3. F1M1 `477576dd32d4…` loads then overwrites the supplied map, counts the new-child map and returns3. This is successful executed reduction, not evidence of supplied-file reliance. The analogous F0M0 repeat1 `5a62057ade8a…` also answers4 from an uncounted map; the paired F1M0 loads and counts3.
- validation-01/union/repeat0 is a real F loss: F0M0 `049d749a2c0b…` acquires a child map, but then materializes requested IDs from supplied `labels_json`, whose human count supports2; it returns2 without an executed scalar count. F1M0 `c4470929859d…` loads the file, overwrites it with new-child labels and prints those categories; it returns1. F0M1 and F1M1 likewise return1. The mechanism is replacement with a different child map, not general inability to count.

The fixed supplied maps agree with dataset counts in6/8 query compositions (18/24 per cell). Both train-01 queries have wrong supplied-map scalars, retained unchanged; all cells score0 for all6 corresponding repeats. Across this already exposed panel, dataset accuracy is bounded by classification as well as evidence use. Map consistency is not privileged-gold accuracy. Host gold is used only in this scorer, never inserted into prompts or repair actions. Prior exposure and selection of these4 contexts forbid claiming fresh held-out generalization.

## Costs and provenance

| New physical cost | F0M0 | F0M1 | F1M0 | F1M1 |
|---|---:|---:|---:|---:|
| Root + child calls | 102 | 111 | 85 | 69 |
| Input tokens | 131590 | 155446 | 100582 | 78045 |
| Output tokens | 10489 | 13335 | 7622 | 4874 |
| Cached input tokens | 126896 | 147728 | 97952 | 75792 |

Total260 root+107 child=367 new physical model calls,465663 input/36320 output tokens,448368 cached; no unknown usage values.459.623s is summed physical call time, not job wall/GPU time. Root/child splits are in CORROBORATION.json. F1M1 matches F1M0 dataset accuracy and uses fewer calls/tokens here; cache/order and repeated shared contexts limit latency extrapolation.

Exactly4 historical c32 acquisitions were reused unchanged, independently matched to actual native child outputs and byte serialization:4637 input,599 output,2352 cached,7.423s summed child call time. They were not newly paid. Charge each hypothetical endpoint its entire one relevant acquisition: across96 endpoints,96 hypothetical historical child calls,111288 input/14376 output/56448 cached. Added to new physical endpoint work, the standalone-accounted aggregate is463 calls,576951 input/50696 output; this is not actual new spend. Per24-cell historical addition is24 calls,27822 input,3594 output. Each historical authored wrapper has2 transport actions and0 sampled/paid root model requests; do not count those as learned decomposition. Full original acquisition elapsed time is retained per source map separately from child-call time.

Frozen source closure verifies the task setup writes the exact pinned labels.json bytes and all4 source maps match actual acquisitions. Actual loads/printed values/counts corroborate their use. Runtime overlays record engine source hashes, not an independent post-write hash of every per-runtime map file; no claim of96 independent filesystem byte attestations is made.

## Ranked follow-ups

1. Keep accurate real fields common. On new disjoint compositions, isolate map guidance/filename from explicit additional-child permission, retaining genuine actual maps and free roots. Small paired comparison:16 new query compositions ×2 map-wording conditions ×2 seeds=64 endpoints, count executed supplied-map reductions, new reclassification calls, strict finals and dataset accuracy separately. Do not silently forbid children or use gold maps. This tests the remaining M ambiguity; current M bundle is not a clean mechanism.
2. Read the already-running corrective-SFT experiment under its frozen free/controlled split. Actual multi-action observation-to-action learning remains distinct from a supplied-file prompt fix. Promote only if it adds real executed scoped reduction on excluded compositions over the common accurate-field baseline, not merely format or already-correct scalar copying. This audit does not authorize changes to that sealed experiment.
3. For remaining classification-limited queries, compare actual map acquisition strategies on fresh compositions while holding accurate root reduction instructions fixed. Dataset gains require correcting evidence, not just obeying its wrong scalar. Avoid training on these repeatedly exposed evaluation traces or extending epochs blindly.

Focused verification:3 independent primitive tests passed; audit reconstructed96 endpoints; all165 action memberships checked; native corroboration verified96 prompt rerenders,367 wire token/usage/model/sampling records,95 decoded finals and4 historical acquisitions. No GPU, service, lock, live-queue, old-source or outcome mutation. Seal contains method, raw output pins, analysis sources, row/contrast/state-use records and report.
