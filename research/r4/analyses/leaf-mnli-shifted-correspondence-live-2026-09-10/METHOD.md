# Outcome-blind independent shifted-MNLI32 audit

Scope question_cards-authored leaf-mnli-shifted-correspondence-v1/outputs/attempt-001. Auditor did not implement this study. Actual launch1789002105.3948743; freeze timing/result-file count recorded honestly, no response content read before freeze. This may be post-generation, not a pre-launch claim. READY_V2 authoritative; original rejected READY and its wrapper-binding amendment preserved. No GPU/services/locks/launch/queue/source mutations or generated-code execution.

32 planned=8 previously exposed MNLI48 contexts×2 fresh paired seeds×matching/shift17, common requested_tag prompt and generic shape grammar. Reuse independently source-verified MNLI80 full-text/label map/public IDs via exact pinned DATA/READY plus audit seal; explicitly exposed source, no fresh replication. Recompute expected shift as ID[(i+17)%48], same tag multiset/complete texts/order/field schema in both, only requested_tag values change. Same final-only/no-tools role,.5/top-p1/3072/8192 caps; source IDs are not grammar literals. Validate all new ordered bodies/rendered native tokens and actual service identity. Verify32 balanced paired slots and keep every planned NULL.

Primary displayed-label count only when whole48-object native output has no duplicate keys, exact tag then label fields, canonical tag/labels and every position's requested_tag. Any authenticated whole-contract failure strict0; unavailable/unverified finalNULL. No repair, tag reorder, partial-array extraction or shifted named-label substitution. Retain shape-positional semantics and requested-tag fidelity separately, including own/displayed-ID copying versus requested-ID copying, missing/duplicate/permuted tags. Primary remains displayed premise/hypothesis relation.

Freeze differing-gold subset from data only: positions whose displayed-record label differs from the record named by that position's requested_tag. For shift17 whole-contract-valid outputs ONLY, count named-record-label matches and displayed-record-label matches on this same subset, with third-label errors and exact diagnostic denominator. Report gated denominator and potential excluded/NULL items; never quietly treat failed tag outputs as diagnostic correct/incorrect labels. Any optional shape-only exploratory diagnostic must be labeled separately, not change this contract-gated diagnostic or primary. Matching named-label equals displayed label trivially and is not a treatment control for this diagnostic.

Report per-arm768planned item correctness/16endpoint availability/contract/shape/tag fidelity and NULL bounds, eight context means averaging two seeds, paired matching−shift17 effects/wins/losses, individual contexts and genres descriptive only. No record/seed independence or unexposed-confirmation claim. Fixed shift17 changes correspondence while retaining output-tag diversity, but common requested_tag field/prompt differs from prior MNLI80; do not use old results as new controls.

Authenticate all actual ordered requests, prompt/completion tokens/text/finish/usage and model/noadapter identities; wrong-route tools available0 if authenticated and never executed. Physical attempts versus returned responses and usage unknowns separate; billing unknown. Freeze exact terminal outputs only after MAIN release; reconcile producer rows/summary without editing. Preserve source receipt/licensing/exposure scope from independent MNLI80 audit. Final artifacts include per-position display/named diagnostics, context pairs, request pins/cost ledger, concise report and FINAL.
