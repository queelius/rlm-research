# Additive retrospective semantic review

MAIN requested this outcome-aware independent review after composition failures exposed a distinction between observed-map scalar agreement and actual requested computation. It is not preregistered, not a new primary score, and does not modify the sealed dose audit or raw outputs.

Role disclosure: this reviewer implemented the continuation/readout experiment, but did not author the original independent outcome/dataflow audit. This is an independent reconstruction of that audit's semantic claims, not an author-independent audit of the experiment.

Scope: all29 SFT24 strict successes and the five available wrong endpoints previously called faithful reductions of leaf errors. Read each entire root program/actual observation sequence, trace parent links and final; compare requested operator, users/scope and target to the actual successful reduction. Classify correct-operation live-label reduction, wrong-operation/scope coincidence, literal/manual recovery or unknown. Harmless extra users absent from the declared data universe are noted, not automatically treated as wrong scope; omitting a real user from an all-record question is a scope error even when it changes no answer on this instance. Full-record slices16/32 are full only because actual context cardinality agrees.

A displayed integer plus host-unioned map agreement is insufficient. Identify the actual program that generated the observed integer and its upstream variable/map binding. For multi-batch paths distinguish overwritten current maps from host unions, and literal-ID recovery from observed map accumulation. Never reexecute sampled programs. Child-label disagreement is checked against immutable dataset labels; diagnosing its answer effect is a host counterfactual, not a repaired model result. Preserve all29 original strict scores and original availability/costs.

This review independently uses actual EPISODE JSON and raw physical receipt pins, authenticates them against the prior sealed readout episode hashes, and reads all34 selected root execution/observation sequences. It relies on the prior sealed native endpoint authentication rather than rerunning the entire dose training/readout audit. Native source/code/query evidence is preserved per row. Findings amend interpretation only; claims outside these34 endpoints are not expanded.
