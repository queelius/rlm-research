# Next recommendation: source-quoted relations before final answering

Recommendation only. No implementation or launch is included in this analysis.

Rank this ahead of another source-allocation comparison. The eight-candidate pool already covers every annotated support in9/12; flexible selection covers every support in6/12, yet selected Jesus Christ evidence is ignored, house-music country qualifiers are treated as causal claims, and the composer case binds a death location to the wrong person. Those errors can now be tested while holding source acquisition completely fixed. A format-only final ablation is cheaper, but would not directly test these entity/bridge failures.

Smallest comparison: retain all12 completed flexible source sets, exactly four original paragraphs per context, with no new selection or planner calls. Compare the saved direct final with one new source-quoted relation-extraction call followed by one new final.24 new physical calls,12 new finals,12 paired units. No case selection based on success, gold coverage or review classification.

The extractor sees ONLY the original question and those four exact paragraphs, never the eight-candidate pool, gold answers, decomposition questions, support labels, or this review. Use base4B/T=.5 and a512-token cap. Ask it to emit up to four useful local subject–relation–object records, preserving entity identity and linking each to an exact quote and original paragraph ID; optional missing-bridge descriptions must be distinguished from observed facts. It should not answer the final question or reject a local fact merely because that single fact cannot answer the whole nested question. No supplied gold component questions or case-specific examples.

A minimal structured return can be `{"relations":[{"subject":"…","relation":"…","object":"…","paragraph_id":0,"quote":"…"}],"missing_links":["…"]}`. The schema constrains source IDs to the selected four. CPU-side qualification checks exact schema, known IDs and quote substrings against those original paragraphs, but does not use gold or pretend that substring matching proves the relation is true. Save the raw report even when invalid. Known schema/quote errors enter the final as an explicit helper-error field with no accepted relations; the same four original paragraphs remain present in every final. Transport failure makes the new policy outcome unavailable, while any observed final is retained diagnostically. No hidden replacement, gold-based correction, or new helper retry.

The new final receives the identical original question and four sorted original paragraphs, plus the extractor report as untrusted data. Keep the existing final instruction, JSON answer/support schema,1024-token cap, T=.5 and the old per-question final seed202609230000+16×index+3. The only new final-input component is the relation report. Use a fresh helper seed namespace, e.g.202609240000+16×index. This tests explicit extraction plus an extra sequential computation step; it does not isolate representation from extra compute.

Reuse the old direct finals only after pinning their actual public payload, model/no-adapter binding, tokenizer, native decoding/serviceV3 sources and seeds. CPU fixtures must reconstruct the old source payload exactly and exercise the new report→final boundary, including a wrong-person quote, an out-of-source quote and a malformed helper return. Do not rerun an unchanged direct arm if its binding matches. Do not modify any live/shared source.

Cost accounting:24 new physical calls. Conditional on cached source acquisition, direct finalization costs one model call and extraction→final costs two. Including the unchanged two selectors and planner, natural full-policy costs are4 versus5 calls per context (48 versus60 over12). The added512-token extraction and changed final prefix mean token cost is not matched. Record existing referenced acquisition costs separately from newly spent GPU work. Proposed bounds remain four question workers, science700,owner950,external1050 seconds and8192 actual prefix+requested-output cap.

Primary metrics remain original answer EM/F1 and support EM/F1, with all12 paired endpoints and unknowns retained. Separately inspect exact quoted relation retention, entity binding and answer-form differences. A concise reformulation turning a correct-content sentence into an exact match is a format effect, not new retrieval. A changed answer with no requested relation in the selected sources is not faithful composition. If relation evidence improves without final accuracy, focus next on final selection; if extraction invents or discards facts, that helper is the failed component. Do not rewrite the3/3 baseline or promote a new method claim.

This is an exposed diagnostic test of evidence use/composition, not learned routing, recursion, novelty or confirmatory transfer. MAIN alone decides admission.
