---
status: prospective_pre_outcome
date: 2026-09-10
planned_physical_calls: 48
planned_derived_episodes: 32
---

# Prospective native audit

After exact OWNER/EXIT terminal relay, validate READY closure and independently authenticate all 48
request/response/result triples, native token decoding, model/seed/body identity, provider IDs,
usage, and complete maps. Preserve every planned coordinate. Unavailable required batches yield NULL;
authenticated-invalid batches yield observed-invalid; neither permits partial salvage.

For each selection, derive `single_a` by unconditional A overwrite and `agreement_abstain_a_b` by
overwriting only identical valid A/B labels, retaining baseline labels on disagreements. Report
agreement, abstention, wrong-label agreement, corrections, regressions, selected error recall, merged
accuracy, J1 scalar/exact/absolute error, qualifying-user and contribution errors. Gold is consulted
only after selections and native derivations are fixed.

Primary paired diagnostics are task-aware minus confidence within policy, agreement minus single
within selection, and their descriptive interaction, overall and separately at sizes 64/256. Retain
all NULLs and conservative bounds. Apply the frozen promotion rule: ≥7/8 valid per derived policy;
task-aware lower absolute J1 error in ≥6/8 under agreement; agreement better than single in ≥6/8 for
task-aware; ≥2 additional exact J1 repairs over confidence-single; and no material availability or
token-cost imbalance.

Count the A-only 24 calls as the physical cost of standalone single and all A+B 48 calls as consensus
cost. Also report the shared-derived accounting explicitly: derived policies and arms reuse calls and
are correlated, not independent physical experiments. The frozen 40-call ceiling is shared historical
acquisition counted once separately. No calibration, novelty, or new-context generalization claim.

