# Typed helper36: outcome method before main outcome inspection

September9,2026,10:51UTC. Main implemented only launch authority and reviewed
scientific sources; padding authored the study. Main has seen lifecycle/native
HTTP status progression, not episode outcomes or sampled completion contents.
This method is written after launch, not misrepresented as prelaunch registration.
Scientific comparisons, seeds and primary metric were frozen before launch.

Use frozen SPEC0dac73d688eb41c123145a35b00a2abc741e173aaf08d3bd2613f44554e044f1,
READY1da910883ed4ddeb99a660c11fd579ccf35db373db996b11c5ec6a4495e83fb1 and
main ACCEPTANCE03a7aa5b46896c2ded7e02ccfe5eda930481eae17eb281dba5e31c581da45ae0.
Reuse source closure honestly; independently reconstruct the relevant public
contract, native roles/wires and raw endpoint rather than repeatedly hashing
heavy model files or trusting an implementer's projection.

After terminal/owned service release, account for all36 planned coordinates,
three arms and12 task/seed triples nested in six exposed contexts. Root473210 and
childc32de fixed. T−R paired strict endpoint is primary; U calibration. No outcomes
selected on helper uptake. Preserve recorded/observable/malformed/empty/null/unrun
denominators. Inherited strict last-nonblank-line scorer allows the established
wrappers and numeric syntax; completed scored-empty is0, incomplete infrastructure
is null. Do not substitute adaptive pilot's different unanswered-null contract.

Recompute endpoint from raw final/host gold; preserve author-score disagreements
and graph errors rather than repairing or dropping them. Report all-planned success
coverage, jointly observed gains/losses/ties and unknown bounds; cluster summaries
are descriptive with only six contexts, not hundreds of independent model calls.

Verify actual root/child adapter identities, trusted depth/invocation metadata,
initial task text exact public-helper round trip, one immutable decision per
invocation, and actual ordered schema transport. Schema derives only requested
public IDs and the fixed six labels; no root schema, semantic validator, hidden
gold or fallback. Unmatched/binary/subset contracts remain untreated. Contract
matching is not authenticated Python-helper origin. Root-written logs alone are
not evidence of model action. Child grammar suppresses tool outputs and changes
the action space; do not call it an instruction-only effect.

Check R/T initial native prefix and sampling equality separately from sampled
action equality. Equal seeds need not produce identical first actions. Record
first eligible child action and exposure; later adaptive child invocations are
not pairwise counterfactuals. Count roots with no eligible children in primary.

Secondary: eligible/treated invocations, returned complete maps, raw parsed-empty
versus nonempty sampled tokens, unique/occurrence ID coverage, semantic label
accuracy using host labels, and visible root consumption. Any manual examples
are descriptive and selection criteria must be stated. Exact final success does
not prove complete coverage. No sampled code is executed on the host.

Count union of physical native calls once, including request-only tails, actual
retries, error usage unknowns and child work; report input/output/cache/uncached
tokens, stop/length/error counts, root/child split, overlapping wall versus sums.
Include new image/runtime startup separately, without claiming comparison to
historical images is a controlled speed benchmark. No CPU audit holds GPU locks.

The result chooses the next experiment; it does not establish generic novelty,
adaptive planning, generalization, or a causal fraction explained by child format.
