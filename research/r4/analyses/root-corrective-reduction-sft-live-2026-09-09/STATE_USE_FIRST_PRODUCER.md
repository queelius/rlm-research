# First-producer executed-action ruling

Independent manual inspection covered all15 unique free programs, all57 executed actions and their recorded observations in ACTIONS_first_producer_free.json. Every program imports/loads records, chooses a slice, calls the actual child, parses its map and prints that map. Slice variations include full16, four sequential batches, off-by-one/incomplete ranges and overlarge stop bounds. None filters the actual `user` field or executes a scoped category reduction. Therefore **0/32 free episodes demonstrate an executed user-scoped reduction**. This does not assert that the model performs no internal reasoning; it distinguishes native executed actions from an unsupported inferred mechanism.

The16 controlled continuations have16 paid root calls and zero new paid child calls. Their40 executed producer actions are the authored, whole-request-verified source reconstruction, not sampled policy choices. There are no sampled tool actions after the controlled boundary, hence **0/16 demonstrate a sampled executed scoped reduction**. Primary dataset accuracy is1/16, strict map consistency0/16; the one dataset match is not evidence of faithfully reducing the actual supplied predictions. Two stop outputs are explanatory prose rather than whole-strip Answer:N. They invent user-to-ID associations; their embedded counts do not repair the strict primary endpoint.

Free primary accuracy2/32 is split1/16 new composition and1/16 regression, concentrated in one context. These are preliminary within-arm descriptions until unchanged/corrective comparisons are available. Full physical request/final token identities and controlled replay were independently checked in the corresponding READOUT artifacts.
