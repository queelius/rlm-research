# Unchanged executed-action ruling

All39 unique free programs (96 executed actions) and all six controlled programs (41 actions, including40 authored source producers) were inspected as text/AST plus native observations, never executed by the auditor.

Free: four episodes demonstrate faithful reduction of actual child predictions with actual user scope. Program hashes/row suffixes are: `72e1d0c8` / free-f8d8765f (NUM IDs correctly selected from all four printed child maps, then records filtered to u00/u01, scalar1); `39a3ee1f` / free-0c22a08f (user-filtered actual child map, HUM value count, scalar1 although dataset gold2); `cd14b96b` / free-f644aa75 (u02-filtered records classified by actual child, numeric-value count1); `c6f382ed` / free-c51caa0f (u00/u01-filtered actual child map, numeric-value count3). Each has a genuine returned scalar and native final. Thus faithful observed-map scoped reduction is4/32, independently of dataset correctness.

Two other episodes execute actual user-scoped counts using locally authored keyword classifiers instead of actual child-map reduction (`977b6e3c`, scalar0 but gold1; `f03729d0`, scalar0/gold0 but final prose is format-invalid). These are data-dependent heuristic reductions, not evidence of faithfully using child predictions. One additional program `7af65dc0` scopes records correctly but zips IDs with iteration over a label dictionary, thereby treating ID keys as category values and returning0; this is an actual reduction data-flow bug, not faithful map use. Other programs primarily inspect files, acquire/print maps, or contain errors; user-filtered child acquisition without a subsequent executed reduction remains separate from the four demonstrated reduction cases. No assertion is made about hidden internal reasoning.

Controlled: one sampled corrective program (`f25c3617`, controlled-5e75ccc4) copies a category dictionary that exactly equals the preceding actual observations, loops over actual records with the requested u00/u02 scope, checks actual categories for human being and prints0. An earlier unused text-filter assignment is erroneous but does not feed the executed count. Actual scalar, strict final and gold all equal0. This is1/16 faithful actual-map scoped reductions. Other15 controls have no sampled tool action after the genuine source boundary; the40 authored producers are excluded from policy-action evidence.

Unchanged primary endpoints are10/32 free (7/16 new composition;3/16 regression),32/32 available,25/32 strict format; controlled2/16 dataset correct,1/16 actual-map consistent,16/16 available,9/16 strict format. These four-context exposed-panel observations are not independent-record replication or a fixed capability ceiling.
