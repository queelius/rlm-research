# Outcome-blind corrective-SFT independent audit method

Frozen before inspecting gate measurements, captured child answers or readout outputs. Prior exposure: MAIN reports first eight captures complete and forward-only gate starting; no measurement values or outcomes supplied. Auditor did not implement this sidecar. CPU/read-only experiment access; no model loading, GPU, service, environment, lock, source fixes, generated-code execution or cancellation.

## Scope and denominators

Authenticate sidecar root-corrective-reduction-sft-v1 READY and operative accurate prompt amendment, exact attempt001 and owner/parent clocks. Audit progressively from immutable completed stage artifacts while GPU work continues. A prospective objective-floor or cost gate failure is an intended scientific/spend decision, not an infrastructure failure; source/protocol/availability errors remain distinct. Preserve all 96 free and 48 controlled planned endpoints as NULL if unavailable/unrun, with success bounds 0..N rather than invented zero policy accuracy. Do not interpret overall owner complete=false alone as infrastructure failure.

## Capture and objective audit

Independently join fixed plan, public records, actual physical requests/responses, native graph and TEACHER. Confirm one width16 or four width4 actual c32 producers in the same REPL, actual returned canonical maps exactly covering displayed IDs, no semantic gold repair, separate observed metadata error where planned, fresh-name correction with actual user scope, actual scalar and native terminal target. Reconstruct count from saved actual child maps and public records without executing generated code; separately compare scoring-only dataset gold. Authenticate served low66c root and immutable c32 child via actual binding/descriptor/model directories, not stale campaign metadata.

Re-render native graph prefixes and exact authored action suffixes/EOS where possible using CPU native renderer; compare all physical IDs and masks. Every training turn must have contiguous current-action-only labels, prefix masked -100, native EOS, no truncation, no reward/advantage/old-logprob fields. Synthetic transport likelihood is not behavior-policy evidence or RL. All producer/error/history/terminal turns except the single selected current action are masked. Partition corrective target token offsets into actual observed map payload, Python string literals, mechanism code and boundary/other; copied literal overlap cannot contribute to mechanism NLL. Initial eight are the fixed first eight, not selected after outcomes.

Independently recompute objective floor from retained numbers: at least4/8 mechanism mean NLL>=0.1 and median whole corrective NLL>=0.05. Confirm forward-only zero updates, diagnostic terminal training weight0, and exact training-time projection mean forward seconds x32x4x3 per arm<=600. Audit shared-envelope projection separately and label startup/checkpoint overhead exclusion. No independent new model forward calls; numerical NLL is authenticated retained GPU evidence, not an independently recomputed model evaluation.

## Training and checkpoints if reached

Each arm independently starts low66c, fresh AdamW1e-4/weight decay0, FP32 rank8 LoRA on frozen BF16 base, fixed seed981351002, four complete32-example updates, equal example mass1/32 and per-example token-mean CE. Check exact exposure order, cumulative target token mass, causal shift, gradient norms, no partial optimizer steps, and final weight0. Inspect safe checkpoint metadata and tensor state on CPU only as needed: adapter/config/Adam steps/RNG/cursor/ancestry hashes; fixed checkpoint4 only, no replacement. Token counts/FLOPs are not matched by equal example mass. Child remains untouched.

## Readouts if reached

Audit every raw free/controlled endpoint actual native final branch and matching request/completion IDs, returned stop semantics and exact whole-strip Answer:N. Available malformed answers=0; infrastructure/unrun=NULL. Independently recount dataset answers and controlled actual-map consistency. Controlled replay requires entire actual request equality to source (including alias, tokens, seed, caps/cache), actual saved child response equality and exact first sampled correction prefix; no field exceptions. Authored/replayed transports are not new paid calls. Audit executed scope/reduction/state use from actual action/observation data flow rather than AST/prompt markers alone, retaining uncertainty for ambiguous traces.

Keep free96 and controlled48, new-composition versus regression, arm, source context and user-set composition distinct. Shared raw context groups, repeated queries and reused state are not independent task samples. Common accurate contract can produce a high unchanged baseline; exposed data are not untouched replication. Controlled-only gains indicate state-use conditional on supplied genuine history, not free reachability or general decomposition improvement.

## Cost, reporting and seal

Count physical paid root/child calls and exact token IDs, including failed calls/unknown outputs. Keep authored transport, shared replay, physically once controlled acquisition, hypothetical per-policy acquisition and new post-boundary calls distinct. Cache usage stays unknown if not captured; wall clocks are overlapping intervals, not additive GPU-seconds. Authenticate parent terminal/release before final outcome seal. Report a trustworthy fast gate or metric signal to MAIN, then full source-derived report with failures, planned NULLs, ranked informative followups and SHA256 manifest. Preserve historical attempts and all sources.
