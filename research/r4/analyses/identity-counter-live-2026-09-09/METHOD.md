# Frozen identity96 raw/wire audit method

September9,2026. This method is frozen before reading identity96 outcomes. Only
design/source files were inspected. Analyst owns this analysis directory, not the
scientific sidecar. Analyze once after all three terminal gates exist: sidecar
outputs/attempt-001/STATUS.json, owned/attempt-001/FINISH.json, and operation
2026-09-09-after-seed-continuation-identity/attempt-001/identity_counter96/EXIT.json.
Main may trigger execution. Do not interpret partial outcomes or change this method
in response to them. A later correction is an additive, explicit audit amendment.

## Fixed question and units

Primary meaningful randomized source-ID tag minus changing unrelated ordinal tag;
secondary ordinal minus constant, with meaningful minus constant descriptive.
Analyze TREC and SST separately. Eight exposed64-record contexts, four per task,
two source-presentation permutations, two sampled seeds and three exact-grammar
arms yield96 calls and32 coordinate triples. Permutations/seeds are nested repeats,
not additional independent source contexts. No fresh held-out or training claim.

Source SPEC SHA c6ab4f82525424b5aef77a21be1dc7fe46c333bf54575a48070b27a850a5565b;
READY SHA6ec385d09255add56329139e6e231a070e3929b0792c42eb37f7c65f1165fc61;
study SHAe61c5cbae6484654855642e631332373c875dc5939ee2d1a849b896856475924.
All paths are under sidecars/leaf-identity-counter-v1 unless stated otherwise.

## One-pass reconstruction, not acceptance of saved scores

1. Authenticate frozen SPEC/design/request/weight provenance and actual endpoint
   binding. Compare run SPEC to prepared SPEC; check model alias, adapterc32de,
   base/config, version and preflight model cards. Hash each distinct file once;
   reuse that in-memory inventory rather than rehashing model closures per call.
2. Independently reconstruct source-ID rank and both source permutations from
   parent padding DATA, namespace leaf-identity-counter-v1, master981266001,
   source_context_id and group_id. Verify all source records/text/gold labels are
   preserved, IDs are a bijection q0001–q0064 and stable across presentation orders,
   and label content is not an input to ID/presentation assignment. Reconstruct
   the two SHA-derived sampled seeds and96 coordinates, including six arm orders
   cycling across32 triples. Preserve the intended5/6 order counts, not forced
   exact balance. Reattach each output position to the actual displayed source
   group and its unchanged gold label, not to tag rank or anonymous position alone.
3. Check all96 planned bodies against wire body_utf8 and SHA, saved request object
   and request SHA. Compare system/tools, model/sampling/caps, ID-bearing physical
   input block, question order and source IDs across each triple. Only truthful
   arm instructions and exact tag schema may differ. Confirm actual returned
   prompt_token_ids exactly match frozen PROMPT_IDS, not merely equal lengths or
   hashes inferred from content. Verify prompt usage matches IDs and no input
   truncation is hidden by the3072 output/8192 context budget. Missing wire or
   prompt-ID evidence is an explicit audit limitation, not silently a pass.
4. Independently inspect every exact schema: array length64, prefixItems64,
   items=false, object keys tag/label, both required, no extra properties, exact
   expected tag at each displayed position, full task-specific canonical label
   enum. Meaningful uses the randomized source ID, ordinal p0001–p0064, constant
   p0000. Preserve and report returned object lexical key order separately from
   schema validity; valid tag/label objects need not be called semantically correct.
5. Parse raw_response_text independently with duplicate-key and non-JSON-constant
   rejection; compare its object to raw_response. No fences/prose stripping, prefix
   salvage, label normalization, fallback, generated-code execution or inference.
   Tool-return/non-text responses are invalid, not a label array. Recompute strict
   schema validity,64-way exact label assignment count, whole64 exactness and
   canonical class-count-vector errors/L1/exactness. Invalid complete outputs get
   strict zero under the existing contract but no64 invented semantic errors;
   semantic correctness/count vectors stay unavailable unless fully aligned and
   valid. Infra/unrun remain null and separate. Cross-check independently derived
   values against the frozen scorer and stored scores; discrepancies stop any
   clean audit claim and are reported, never averaged away.
6. Reconcile planned96, raw call IDs, wire records, duplicates/unplanned calls,
   STATUS unrun/stop reason, owned FINISH/release and operation EXIT. Retain all
   physical attempts, errors, length stops, tool responses, actual seeds/aliases,
   prompt/cache/output usage and wall seconds. Do not count per-call elapsed sums
   as elapsed GPU wall time. Stored analysis is a comparison target only.

## Prespecified readout and interpretation

Report each task/arm's planned/recorded/observable/valid counts, strict correct
labels, semantic aligned-label denominators, whole64 exact and aggregate count
vectors. For each contrast give paired label gains/losses/net by call, context,
presentation and seed, plus source-record group alignment and positional quartiles.
Report jointly valid semantic contrasts separately from strict observable contrasts;
do not subtract an invalid arm's invented semantic total. Show context consistency,
not an episode-level significance claim with96 independent observations.

Authenticate the predeclared q/p standalone token audit and corresponding tag token
lengths; this does not establish equal realized compute. Report actual prompt/cache/
output tokens and duration by arm and paired deltas. Same ID-bearing input does not
mean the full prompts/grammar are identical; instruction/prefix/grammar content
differs. Meaningful > ordinal is consistent with a source-correspondence benefit,
not an attention-mechanism proof. Ordinal approximately matching meaningful while
both beat constant favors a changing-position/bookkeeping explanation in this
setting. Neither result alone establishes end-to-end RLM benefit.

Outputs: immutable audit metrics/source inventory plus a concise plain-language
REPORT.md, salient results to main early after integrity/score checks. No broad
test suite, repeated large hashing, GPU/model/service calls or environment changes.
