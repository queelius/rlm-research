# Bridge32: component agreement improves on count, but final answers do not

The primary checksum contrast is **array0/8 versus map0/8**, with8 ties, no gains/losses and no NULLs. Count, reported separately, is **1/8 versus1/8**, again the same winning pair and8 ties. All32 endpoints are actual available native finals, not returned tool calls mistaken for empty answers. This is no observed whole-task improvement in this small readout, not proof the representations are equivalent generally.

The exposure is important: only **3/8 checksum pairs** actually use an eligible child in both arms, versus8/8 count pairs. Checksum eligible calls are only4/8 records wide. Therefore the primary has limited matched exposure and does not test the stronger64-wide correspondence result. Free direct-root solutions remain valid; no-uptake cases are kept in the primary, not removed.

## Outcomes and component evidence

| Task / child representation | Correct / planned | Strict syntax | Episodes with uptake | Native label agreement | Recorded calls |
|---|---:|---:|---:|---:|---:|
| Checksum / array |0/8|3/8|4/8|299/320|176|
| Checksum / map |0/8|4/8|3/8|184/192|120|
| Count / array |1/8|4/8|8/8|567/628|224|
| Count / map |1/8|4/8|8/8|593/628|224|

All denominators include failures. Label denominators are physical label occurrences, including repeated records—not unique independent examples. Count's eight paired **eligible requested-ID/text sequences are identical**, so its+26/628 label-agreement difference is a useful matched component observation. Target false-positive/false-negative occurrences fall from3/6 to1/2. This does not establish mediation of task accuracy, which is unchanged. Checksum requested sequences differ in two pairs and its label totals have different exposure; do not compare those aggregate fractions as a paired semantic effect.

Four exposed contexts form the clusters: two32-record and two128-record contexts. All eight checksum tasks fail; the sole count success in each arm is query_transfer-01 seed981326011. The historical names `query_transfer`/`length_transfer` refer to source contexts; **both current tasks are global**, including the new whole-task package on the32-record contexts. These are not new source groups, independent training replications or a new benchmark evaluation.

Eligible invocation widths are overwhelmingly small:314 at4,34 at8/10,6 at32, none above32. Count has95 width4,16 width8/10 and3 width32 invocations per arm. Checksum has76 width4 plus2 width8 for arrays, and48 width4 for maps. Overall11/16 pairs have uptake in both arms, but checksum-specific exposure remains3/8. No forced-plan or uptake subset replaces the primary.

## Raw child → broker claim → root-visible evidence

The audit independently reconciles **744 typed requests/results and744 role requests/results**, all returned, with744 physical wire/token/logprob checks and744 exact native graph prefixes. There are390 root and354 child calls. Every child call qualifies at ordinary depth1 under the exact frozen public-ID/text contract; root grammar is absent. Actual array/map prompts and ordered schemas match the public catalog, and raw child graph messages remain arrays/maps rather than overwritten broker projections.

All354 raw child returns satisfy their exact representation/cardinality/enum contract. All354 delivered-event claims match exactly one trusted native child invocation, host-requested ID list, original/transformed request, raw response and independently recomputed canonical broker map. Invocation/result/delivery order is present, with no duplicate delivered event. All354 child semantic-call edges point to actual root call nodes; these semantic edges are not substituted for physical message ancestry.

The event files remain **root-writable claims**, not cryptographic proof of broker delivery. Their consistency with the native trace is reported separately from literal maps on the actual root branch. Complete unconflicted supplied/delivered unions occur in array/map4/3 checksum and6/7 count episodes. Complete corroborated literal-map coverage on the final root branch occurs in4/3 checksum and3/3 count episodes. Printing a scalar instead of every label can be a valid efficient strategy; lack of a displayed full map is not itself a failure. Conflicting repeated labels remain conflicts rather than last-write-wins repairs.

No root action explicitly names bridge_config, bridge_events or the installed representation modules in the recorded code. This is only a literal-marker screen:331 cells parse as Python and27 do not; absence of those names is **not proof of treatment blindness or absence of dynamic filesystem inspection**. The configuration and overlay are root-readable, as disclosed before inference. The same initial root prompt/sampling is verified in all16 pairs, but initial sampled outputs already differ in two checksum pairs: query_transfer-01 seed981326021 and length_transfer-01 seed981326011. These divergences precede child treatment and cannot be attributed to the representation; serving/scheduling/numerical variation remains a nuisance. Count's initial outputs match in8/8 pairs.

## Concrete reduction failure—not a final-copying failure

In count/map length_transfer-01 seed981326021, the corroborated child maps cover all128 IDs without conflict and imply the correct count28. The root's Python program successfully builds `all_labels`, but its reduction is outside the loop and uses `labels`, the final batch:

```python
for ...:
    ...
    all_labels.update(labels)
total_count += sum(1 for label in labels.values() if label == 'human being')
```

The actual source is preserved verbatim in FOCUSED.json; the ellipses above mark omitted lines, not executable replacement code. Tool node34 displays1; final node35 says `Answer: 1`. Only the initial four-ID map is visibly printed to the root model; the full accumulated map exists in the REPL program's state, not as a displayed128-label observation. The model faithfully reports its program's wrong reduction—it did not visibly see28 and then copy it incorrectly. [Raw episode](../../../../ARTIFACTS.md#unpublished-files "Not published: ../../sidecars/root-child-representation-bridge-v1/outputs/attempt-001/rollout/episodes/350d218773adb4bcb4cf3041615055c32c5cf692a175f683676d689ff1e3c747.json"), SHA `e521b64bf4e3128a8bd7c18e74b077bded77b43e5334fef3e7ad15277f27e617`; code node7 SHA `5bfc3857aa6c41ee0b723d46d8ec4fdf3f63df86dab61162225af983b8647705`.

All six count episodes ending in displayed scalars copy those scalars faithfully; two displayed scalars are correct and yield the paired success. Seven checksum episodes end with displayed32/128 rather than a computed checksum; their wrong final answers need not copy those irrelevant lengths. There is **no observed correct-last-scalar/wrong-final case**. A host-computed scalar from a label union is kept distinct from an executed reduction. This single selected program example diagnoses a real path, not its prevalence or a treatment effect.

## Costs, provenance and disposition

Across all744 recorded calls:1472633 logical input tokens,48854 output tokens,1423600 cached input and49033 uncached input; all four usage fields are available for every call. These include repeated prefixes and failed code attempts. Checksum array/map output totals are10749/8239 with unequal uptake; count totals13068/16798. Map count generation adds3730 output tokens for the same requested record sequence, a cost alongside its higher label agreement. No summed request latency is labeled GPU time.

Collection elapsed509.470s. Accepted operation elapsed561.407s, exit0/no timeout, all32 recorded and no unrun coordinates. The release record verifies owned identities exited and ports free; the parent EXIT reports no GPU processes. The1770 owned/1800 outer cap was not reached. No analysis held the GPU scheduling lock or launched/signaled any process.

READY `ab16721e…`, SPEC, accepted PLAN/ACCEPTANCE, exact low66cce root/c32de child, base manifest, live-model crosswalk, runtime/overlay boundaries, source-derived gold and all matched initial prompts reconcile. Source agreement remains agreement with frozen TREC labels, not independent semantic adjudication; public/base-pretraining exposure is unknown. Raw source/response artifacts remain unchanged. Parser preparation first caught and fixed an auditor-only maximal-branch slicing assumption using the stored synthetic six-call fixture; that failure is retained. Synthetic fixture probabilities are not model evidence.

The next useful test is whether a prospectively fixed, stronger existing root can exploit the same child representation difference—the independently prepared last-saved-RL4 comparison addresses that question. Do not claim a correspondence mechanism was refuted from low uptake or small batches, and do not promote computed-finalization as the diagnosed fix. If reduction remains the bottleneck, test executable reduction/state handling on prospectively selected new context compositions, with no evaluation-item-specific retraining, semantic repair or automatic answer submission. No new experiment is launched by this report.
