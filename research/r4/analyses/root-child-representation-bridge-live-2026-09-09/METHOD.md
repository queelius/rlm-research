# Independent bridge32 terminal audit — prospective outcome boundary

Frozen before this analyst opens any live bridge outputs or derived results. Preparation reads only accepted source/spec/input files and the existing synthetic six-call CPU qualification. MAIN alone owns inference/lifecycle and will trigger terminal scoring. Source author results.py is not the scoring authority. Prior component results and the exposed source contexts motivated this experiment; it is not scientifically unopened research overall.

## Fixed question, units and primary

Exact READY ab16721ee537df09370d534d63fa4d8d542df1e1a17c903efac7c711c1a837d0; accepted operation PLAN83a4e7301572de3e672a5dede591fe9104033ababa47fccd5cec2eb9ff28bdcd, ACCEPTANCEe2372fca4bba71df78e64b34fb20c775a1d8fabe467ea0ea310e4fa4bad76b5b. Fixed low-SFT8 root66cce and c32de child; no training or checkpoint choice. Four exposed contexts (two32-record query_transfer, two128-record length_transfer) × global count/checksum × seeds981326011/021 × array/map child generation =32 planned episodes,16 pairs. Eight checksum pairs are the primary map-minus-array contrast. Eight count pairs are reported separately; no pooled primary or historical pooling. Context is the cluster; seeds/tasks nested in four contexts are not independent source samples.

Check the exact32 IDs, matching seeds/task/context/public bytes and balanced pair order before scoring. Four pairs run concurrently, their two arms sequentially in fixed balanced order. Authenticate actual start order/timing and initial root prompt/sampling/physical output parity. Identical seeds are variance control, not deterministic full-state counterfactuals. Report per-context and32/128-length breakdowns. Whole-task differences from old query-scoped studies prohibit attributing historical score changes solely to representation.

## Independent endpoint and missingness

Use whole stripped ASCII `Answer: ([0-9]+)` and independently recomputed host count/checksum gold from the frozen source labels and numeric ID suffixes. Each source ID contributes once. Checksum subset collisions and correct counts with wrong labels remain possible. No gold reaches runtime, no answer conversion/repair, no plan/coverage gate and no exclusions selected by model outcomes.

Require an actual returned native root final (normal stop or length with no parsed tool calls), exact native final content versus recorded reply and a matching physical trace branch. A completed trace plus a returned tool request is not an observed empty answer. Explicitly distinguish observed empty/prose/decimal/wrong0, unrun/setup/provider/incomplete/budget-prevented NULL, graph unavailable, and runtime/finalize errors after an already available endpoint. Preserve the implementer/raw completed projection separately if it differs. Report strict syntax, correct/available/planned denominators, matched wins/losses/ties/unknowns and tight binary worst-case net bounds; an unknown paired with known0 or1 has one-sided uncertainty. Do not censor an earlier real final because a later outer budget expires.

## Trusted invocation to child generation

Reconcile role and typed pretransport request hooks, result records and wire requests by internal request ID, not filename. Count request-only/provider failures/retries/returned attempts and their actual depth/invocation/coordinate. Authenticate low66c/root and c32/child alias+adapter/config/base, saved live-model crosswalk, source/runtime identity and physical body sampling (T.5, full support, frozen seed/cap). Root grammar must be absent. Only exact public ID/text requests at ordinary depth1 qualify; unknown/duplicate IDs, changed text, custom requests/vocabulary and other roles stay ineligible.

Reconstruct each qualified host decision from the public catalog and exact original and transformed child request, including requested ID order and schema/cardinality. Check actual grammar against that decision, native prompt IDs against wire IDs, completion IDs/logprobs against raw wire response, and native graph calls against physical roles. Arrays remain raw arrays in the child graph; maps remain raw maps. Broker projection must never replace sampled child IDs/logprobs. Non-eligible child actions remain visible costs, not assumed failures or bridge uptake.

## Projection, delivery claims and visible evidence

Independently parse the raw native child response: arrays require the exact requested length/enum values; objects require exact key set, no duplicate keys and canonical enum values. Project arrays by the already-declared requested order and maps by reordering keys only. Preserve semantic label errors and malformed pass-through; never insert missing labels or coerce a value. Report raw validity, record-level dataset agreement, target FP/FN, unique IDs, repeats and conflicts. Dataset agreement is not independent label adjudication.

Treat every bridge event as a root-writable claim. Corroboration requires matching coordinate, trusted root/child invocation, depth1/ordinary role, exact host-requested ID list/original request/transformed prompt, raw native response and independently recomputed broker serialization. Record zero/one/multiple matching physical calls without silently choosing one; retries can yield multiple matches. Check invocation→child_result→delivered ordering and duplicate events. A successful write_frame followed by a delivery log is not by itself proof the root model saw or used the value.

Separately flatten the final root WireTrace branch and compare exact token IDs with its captured physical prompt+completion. Locate actual tool observations, literal label maps/scalars and root code on that branch without executing generated code. Link a displayed map to corroborated projection values where possible; a union of child/delivery logs is not substituted for root-visible evidence. Distinguish availability, REPL receipt, model visibility and actual consumption. Report complete/conflicted visible coverage separately from complete delivered coverage. If a genuine displayed scalar exists, check final scalar copying and gold agreement; if only a map exists, derive a host diagnostic count/checksum but do not call it an executed computation or model answer. Partial/conflicting/unstructured evidence gets NULL, not invented completeness. Describe one or two trace-linked mechanisms only after aggregate denominators are fixed.

bridge_config.json and installed overlay are root-readable/writable. Inspect captured root code for explicit configuration/event/overlay access and record evidence pointers; a negative static screen is not proof of blindness or absence of dynamic access. The supervisor loads configuration before root execution, and host-native eligibility independently uses the frozen catalog; a later filesystem edit alone does not prove the trusted in-memory decision changed. This is not a cryptographic isolation claim.

## Exposure, effects and costs

Report eligible attempted/returned invocations, raw/projected validity and batch widths1–4/5–16/17–64/>64. Primary retains all pairs, including no-uptake/direct-root solutions. Report both-arm eligible uptake for checksum8, count8 and all16; fewer than eight eligible pairs overall is limited exposure. No post-treatment uptake subset becomes the primary. Small-width observations do not refute a64-wide component mechanism; map grammar supplies IDs and changes the action space together with instructions, not merely cosmetic formatting or internal attention.

Count all physical attempts, root/child returned calls, sampled action tokens and known logical prompt/completion/cached/uncached usage from raw wire JSON. Preserve unknown-usage denominators and request-only known input lengths; no fabricated zeros. Report per-task/arm/context costs, startup/collection/release and outer elapsed separately; summed request latency is not GPU wall time. Shared caps1500 collection/1650 work/1770 owned/1800 outer and inherited transient retries remain part of the comparison. Authenticate exact terminal/EXIT/release after MAIN trigger without GPU locks, polling or lifecycle intervention.

## Execution and output discipline

Cache immutable source/checkpoint authentication once; do not rehash the full846-entry closure per episode or repeat earlier audits. Pin the full READY/accepted boundary and directly used new sources, catalog/prompt/plan/gold files and qualification evidence. Focused fixtures test raw projection/key order/duplicate rejection, forged claim mismatch, tight bounds, sampled-final versus tool-request availability, and the actual stored six-call native array/map seam; synthetic fixture logprobs are never model evidence or training data. The terminal parser may reuse previously qualified native/usage/branch primitives with exact source pins, not the author's scalar projection as authority.

Publish early independent checksum/count pairs, then REPORT/METRICS/SOURCES/raw evidence and a seal. Preserve failures and all32 slots. No inference/service/GPU actions, source changes, queue edits, generated-code execution or promotion of a successful answer into proof of a good plan.
