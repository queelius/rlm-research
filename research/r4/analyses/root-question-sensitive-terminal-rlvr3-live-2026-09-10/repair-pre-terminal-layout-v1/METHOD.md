---
status: prospective_frozen_before_outcomes
date: 2026-09-10
study: root-question-sensitive-terminal-rlvr-recovery-v2
run: outputs/attempt-003
planned_endpoints: 336
manual_composed_endpoints: 96
---

# Prospective RLVR3 native outcome and training-chain audit

No scientific outcome may be read until `OWNER_TERMINAL.json` and the one exact
parent `EXIT.json` exist, the EXIT completion marker equals the owner-terminal
SHA-256, both records show released service/no live GPU process, and the parent
did not time out. The accepted campaign identity is
`e31ee5f2fef4819d1e79c8b2e26a9dd0bd0c57cca2ac11824e41e2c58af0681e`;
the READY SHA-256 is
`484331edd5c8805f302f614e125977230feb9b3f58297d8b6fd52e7aabbea7e5`.

The universe is fixed before outcomes: 192 composed training attempts in eight
windows (six exact task groups times four samples) and 72 protected coordinates
under each of QS6 `start` and fixed `rl_last`. Every planned row remains in the
inventory. Authenticated returned empty or malformed finals are observed zero;
missing, unreturned, or unauthenticated results are NULL. Report planned,
physical, recorded, endpoint-available, admitted, format-invalid, correct, NULL,
and exclusion reasons separately. A partial or integrity-failed window cannot be
trained. A complete homogeneous-reward window is a fixed-cursor noop, not a
replacement opportunity. No outcomes authorize rerolls or subset selection.

The primary effect is paired `rl_last - start` over the same 48 protected composed
coordinates, with wins/losses/ties/unknown and eight context summaries. The 24
primitive pairs are retention. Also report the full 72-pair result and every
operator/scope/threshold/slot/context condition; unknown pairs never enter a
known-pair denominator without disclosure.

For each actual update, replay the native export, reconstruct task-centered
population-normalized advantages from admitted binary rewards, and require the
selected episodes to be exactly the mixed-reward groups. Verify root-only current
action labels and masks, no child credit, old log probabilities, TIS weights capped
at 2.0, equal episode/turn mass, and correction diagnostics. Authenticate the
contiguous fixed-last policy chain and each adapter/config/state/Adam/RNG hash;
require fresh Adam at step zero, finite FP32 moments, one CUDA RNG payload, and
only LoRA optimizer parameters. Preserve and report all eight window outcomes,
including never-started, partial, excluded, noop, and update states.

Independently count physical records and native usage and compare them with the
owner cost ledger. Missing usage remains unknown and billing is not inferred.
Report collection capture, optimizer, service, readout, and total wall time
separately. The automated audit establishes execution/provenance and terminal
correctness, not semantic faithfulness.

`manual_pack.py` is the separate executed-composition reader. After the same
terminal gate it emits all 96 planned protected composed endpoints—both start and
fixed-last—including NULL rows. For every observed endpoint it retains the actual
root programs, tool observations, child response nodes, final, coordinate request,
and source hashes. A reviewer must read every observed trace and judge requested
operator, scope, threshold, acquisition, complete-map retention, and whether the
final uses observed state. No keyword heuristic or code re-execution is permitted.
Trusted `qs_problem.py` may diagnose wrong child labels only after actual child
labels are read; it does not replace trace judgment. Correctness, faithfulness,
and grounded faithful correctness remain distinct.

