# Decision-vector48 one-shot independent audit

Thin reuse of the proven saved-native core, with exact new sidecar/arm/count bindings and an independent full-vector parser. Regenerate the prospective12 public roots (seeds202609380000..11), confirm disjoint prior panels and unchanged effective facts/public order, and verify all48 frozen requests (seeds202609390000..23). Check actual released model/no-adapter, physical prefixes, sampling384/T.5, raw versus canonical response hashes, native completion IDs/logprobs/decode, lifecycle/release, every saved grade and all planned denominators. No shared-source edits, model calls, GPU or generated-code execution. Compilation is only of pinned reviewed audit source for thin bindings.

Independent vector grading requires the entire literal-bool array of exactly public width with unique/only key `eligible`; invalid/unknown maps to None, never a repaired empty set. List grades remain unordered exact with strict sorting separate. All24 attempts per arm stay in exact denominators. Matched-valid BA/precision/recall are separately checked against live metrics. Actual per-arm native input/output lengths and all errors/finish reasons are saved, not inferred from output caps.

CPU_READY seals two focused tests: reordered public candidates/invalid vector/G2 hand calculation, and the actual archived four-call HTTP native fixture. Run once. Missing terminal writes PENDING; unqualified terminal writes HOLD; do not poll. MAIN may request a fresh immutable output directory after terminal.

SUMMARY retains all12 case rows, FP/FN, both valid-set comparisons, history/width strata and two-seed G2 diagnostics per arm. For each full-valid G2 pair, compare each candidate's correctness indicator against its counterpart and count nonzero contrasts; also report episode BA/exact contrasts. Identical decisions have zero candidate contrast; opposing correct/incorrect decisions provide possible local signal even if aggregate BA cancels. Invalid full arrays contribute no per-candidate signal. This is diagnostic only—not a token mask, learning objective, training admission or proof of usable gradients. No new training code is prepared. Different history groups are different new instances; old panels are not pooled.
