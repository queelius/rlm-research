# Independent nested scale/state24 outcome audit

Prospective source-only preparation started 2026-09-10; exact scale outputs/attempt-001 absent at epoch1789019481.0107577. No scale outcomes read. Final seal records a second absence check. Reviewer independently inspected the implementation but did not author it. This is not independent design selection. Source is root-operator-scale-state-v1 READY `d10c9bb2b7a5f1e8e0d5f3146f2ccc6124d6bbda4373ca6692007af932e119ac`, fixed24 root94022838… and fixed c32 child, no training. MAIN controls all GPU/service/queue actions and supplies terminal authorization before any scale outcome reading. Intermediate outcome audit has priority.

Primary: all24 preplanned native free-task endpoints, four source parents × nested16/128/256 × count/weight. Each size has8 planned, each size/operator4. Exact paired sampling seeds across size for each parent/operator. Only four parent clusters: no24-sample independence. Source1024 groups are disjoint under the named root inventory; child-training and catalog exposure remain. All24 gold answers nonzero; pooled best constant40 is2/24, zero0/24, no selection/reroll. Independently verified source truths are pinned from the prior CPU CHECK.

Score unchanged strict authenticated whole native final after existing outer-whitespace strip: `Answer: ` plus ASCII decimal digits, integer equal to source gold. Available malformed/wrong=0, missing endpoint RESULT or no authentic completed final=NULL, never salvaged from execution output or stray worker/native artifacts. Operational success treats bounded no-final failure as unsuccessful but does not replace the NULL inventory. Report per-size/operator/context planned accuracy, native availability, conditional accuracy and conservative bounds. Pair128−16,256−16,256−128 within parent/operator; report each parent’s two-question mean and paired NULL bounds. Preserve all zero cases if discovered by independent recount; no answer-based subset changes. Do not apply a complete-data bootstrap when NULLs exist. Descriptive four-parent signs, not equivalence or generalization claims.

Authenticate actual fixed checkpoint/c32 directories, hashes/config, live descriptor/model list and sampling8192context/2048root+child action, native coding role/files/query/first prefixes. The same underlying records retain IDs/order/users/weights within nested subsets. Gold must not enter task files or prompts. For every returned physical branch independently validate raw completion token/logprob identity; for every claimed final additionally match actual raw request ID, typed parsed branch, trace completion/reply, alias, tokens/logprobs and finish. Re-render/check remaining native prefix and parsed branch evidence from qualified pinned runtime during manual closure, without executing sampled code. A preliminary parser result alone is not a sealed native claim.

Raw costs: union all exact-coordinate physical receipts independently of endpoint RESULT; retain pretransport records, attempted HTTP failures, late requests, unknown usage and request identity conflicts. Distinguish physical attempts, HTTP returns, successful choice payloads, token-validated/native-authenticated completions. Unknown usage is not zero, request rejection is not confirmed GPU generation, and provider billing remains unmeasured. Pin and separately inspect all before/after service preflight artifacts and outer lifecycle receipts. Count actual generation calls/usage there when established; model/version metadata GETs are not model completions. Missing preflight payload/usage remains unknown, not assumed zero. Historical training and acquisition costs are reused, not newly incurred evaluation work.

Mechanism: distinguish merely issuing ≥2 calls from ≥2 **useful contributing acquisitions**. Useful acquisition means an authenticated response supplies identifiable actual-record predictions that contribute to the observed final computation; repeated/redundant calls, toy examples, failed/unknown maps and discarded labels are separate. Persistent accumulation requires actual observations demonstrating earlier and later batch evidence coexisting/being used in the executed reduction. A host union of captured labels is only a diagnostic reconstruction, never proof that the root held or used that union. Copied literal ID/label maps and manual repairs are labeled separately, not persistent accumulation. Track actual records-file/context reads, batches/cardinality, parsed coverage/duplicates, observed map overwrites, errors/repair, reduction scope and displayed-scalar/final concordance. Distinguish faithful reduction over wrong child predictions from incorrect aggregation, incomplete evidence, literal answer and unknown state. Never reexecute sampled programs; AST/code mentions alone cannot establish use. All strict successes and salient failures are manually inspected; every slot gets reviewed/unknown status.

Size intervention: full records and gold differ across nested sizes; this tests the whole fixed-cap size/state burden, not isolated memory capacity. Native root initial prefixes remain compact because records are external.256 exceeds the convenient measured typed-full-map route, not every valid one-call strategy. No forced batch width/helper/source map/grammar change. A larger-size failure may be child semantic error, map serialization/token budget, looping, root state loss, final formatting or infrastructure; classify from actual traces, not size alone.

Caps2400outer/2220work/2370owned; nominal180startup+1950collection+90harvest,150owned release/emergency+30margin. Four workers,180episode; phases advance early. Reconstruct actual before/after service and collection clocks without adding overlapping intervals as GPU kernel time. Freeze raw hashes before terminal analysis and reverify at final seal. Old source/output/audit seals immutable.

## CPU parser plan

Use this isolated analysis namespace and inline test-first preparation. Reuse immutable independent intermediate reader for native final/physical union, with exact new source/output bindings. Test nested24 grouping/eight seed blocks, NULL bounds and rejection of host-union-only mechanism claims; tests never execute sampled programs. Freeze source/input and prior parser hashes plus method/tests before scale outputs exist. After MAIN terminal relay, run preliminary inventory, then add actual native/service/cost/dataflow closure and REPORT/FINAL. No model action or broad suite.
