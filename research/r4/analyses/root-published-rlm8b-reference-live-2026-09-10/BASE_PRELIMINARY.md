# Released base8B only: preliminary independent audit

MAIN explicitly released the base phase for outcome reading after its PHASE_TERMINAL; RLM phase remains unread. Frozen METHOD/PARSER unchanged. Additive partial_base.py invokes their native/scoring/cost functions, with base-only immutable outcome pins. This is not the final48-pair report.

Base is0/24 strict,7/24 authentic native finals,17NULL,0/7 conditional. The sole zero-gold case is an authentic bare `0`, not the required `Answer: 0`; no primary repair. Six finals are bare scalars and one is `Answer: 0`. Even a diagnostic bare-scalar interpretation would recover only that zero, not a nonzero solution. All23 nonzero coordinates have0strict. Native unknown-answer bounds0..17 and operational0/24 remain separate; missingness is policy/interface/deadline dependent, not random dropout.

All103 physical requests and83 returned native completions are reconciled;20 have unknown usage. Known input142,501/output110,294/cache0. There are285 logical calls, including182 post-deadline pretransport rejections: these are not additional physical model generations. Phase elapsed1481.02734s and release succeeds. Provider billing is unknown. No producer/auditor score disagreement.

Actual child acquisition occurs in15/24 endpoints (43 native child completions), so this is not a universal refusal to delegate. Every one of the43 returned child responses contains thinking markup and none is an exact label-only string. Observed root loops compare the full `.strip()`/`.lower()` response with a category literal; their zero accumulation is not faithful semantic-label reduction. Index5 executes all four scoped child acquisitions yet returns0 through this interface mismatch. Index11 likewise acquires four scoped children, prints0, then asks for nonexistent variable named `0` and times out. Other loops consume historical error-as-text results after deadlines, potentially finishing with misleading zeros. No complete-map semantic-faithful success is established.

Several sampled programs use `eval(line)` and observe `NoneType` not callable; the historical official LocalREPL source deliberately assigns eval=None (line113), not a new recorder bug. It also returns child failures as strings (lines258/263), explaining continued postdeadline loops. Other observed failures include missing json import, empty-line JSON decoding, nonexistent category-field lookup, keyword heuristics and malformed FINAL_VAR usage. All24 observation programs were inspected without execution; per-coordinate notes and raw links are in BASE_MECHANISM.json.

Await the full RLM phase before a package comparison. A future explicit no-thinking base control may test the response-interface mechanism, but only after actual template support is verified prospectively; no stripping/repair of current sampled responses, no change to the running study, and no weight-learning conclusion from this base result alone.
