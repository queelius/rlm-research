# Independent prospective published-RLM8B reference48 audit

Method written2026-09-10 at04:24 UTC; exact science attempt-001 was checked absent at04:24:03. No outcomes inspected. The auditor reviewed but did not author the experiment and identified the two prospectively corrected V2 defects. This is independent outcome reconstruction, not independent scaffold selection. MAIN controls launch/lifecycle; only CPU audit is authorized.

## Frozen estimand and inventory

48 endpoints: released Qwen3-8B versus released mit-oasys RLM8B, each24 paired tasks across eight exposed QSR contexts readout04–11 and three questions/context (count-union, distinct-all, weight-single), same paired seed/task. Each policy has23 nonzero and one zero-gold question. Primary is strict authenticated native `Answer: N` correctness /24 planned; available malformed/wrong finals are0, absent or unauthenticated native finals remain NULL. Show availability, available-final accuracy, lower/upper planned bounds and separate operational success where a bounded failed trajectory is unsuccessful. Show all23 nonzero and the zero question separately, plus families and every paired context. No old results as new controls, resampling, best-checkpoint choice, extraction/repair or invented answers.

This is a serial base-first whole-package comparison: both root and child weights change, fixed policy order, concurrency/timing and request histories may differ. The common paper-derived prompt and historical official beb0603 scaffold are not authenticated original training conditions. Eight exposed context clusters—not48 independent replications—and one sample/question permit exploratory paired differences/signs, not confirmatory causal attribution or a size/root-only claim. Primary reported comparison is RLM minus base, equal-context means; whole24-task point difference and paired NULL bounds. Use context bootstrap10,000 draws/seed2026091401 only when all paired finals are available; otherwise retain conservative bounds and complete-pair descriptive results without random-missingness assumptions.

## Source and native authentication

Pin READY_v2 SHA5815d3583cddb82060343ed5d5427044df06ce129002cd1b026fdf0dcc2c8924, the preserved V1 closure, exact48 plan/tasks/host gold, model/tokenizer/template receipts, paper reconstruction and official parsing/execution source. Independently recount answers from frozen QSR records/labels/users/weights/query operators and verify all24 same-policy-pair context/query bytes. Dataset labels remain the upstream frozen oracle; this bounded audit is not a new full TREC raw-dataset acquisition audit. Verify host gold is not present in worker task/public context. Preserve exposure and seed collision scope; no globally unseen claim.

For each policy authenticate actual binding/config/descriptor/live preflight to its exact full-weight path and common RLM template, BF16/noLoRA/context32768/maxseq2/no reasoning or tool parser. Every physical body must agree with retained worker completion event role/index/messages, fixed seed digest(taskseed,role,index), T0.6/top_p0.95/top_k20/min_p0, penalties, root8192/child4096 output caps and alias. Independently render prompt IDs with the actual policy tokenizer and common template; compare native prompt and completion IDs, decoded text, unique assistant branch, finish and usage. First root body also matches frozen first-prefix/task metadata. Length finishes remain authentic observations and then face the same full-final contract, with cap flags retained.

Native final provenance requires completed episode and worker terminal with no error, zero observed outstanding requests, the last authenticated root response matching current iteration prompt/text/final, and actual final lineage. In historical precedence, admit the first truthy final from the current iteration's actually observed REPL blocks; else authenticate standalone FINAL_VAR retrieval code/stdout/no stderr; else literal FINAL content. Previous-iteration observations, code mentions, arbitrary terminal strings and partial responses cannot authenticate a final. Never execute sampled code. An absent episode result retains planned NULL even if a partial worker artifact looks promising; diagnostic evidence is separate, not completion salvage. Independently recount then compare producer score; any disagreement is investigated and disclosed before final claims.

## Physical ledger and failures

Reconcile every coordinate from all on-disk REQUEST/RESPONSE/RESULT directories plus unmatched embedded call identities, including late calls when episode RESULT already exists. Deduplicate only the same role/index identity (the recorder forbids retries); retain conflicts and raw paths. A request-dispatch hook is a physical attempt, not proof of sampled or billable GPU work. Count HTTP returns, authenticated completions, known/unknown usage/cache, rejected/context-exhausted/pretransport calls and outstanding/partial requests separately. Do not infer missing usage or billing. Freeze outcome hashes before reading branches after MAIN's phase/terminal relay; terminal audit rechecks unchanged files. Reconcile all48 outer rows, phase releases, collection/startup/cleanup/parent clocks and skipped stages; clocks overlap, so do not sum wall times as GPU kernel time. Errors/failures are stratified as observed policy behavior, budget exhaustion, transport/service, provenance failure, or unresolved—not excluded as random availability.

## Mechanism review

Automatically index authenticated child calls, actual execution_start/observation pairs, stdout/stderr, native root actions and final lineage, but do not score evidence use from code keywords or AST alone. Manually read every successful endpoint and salient failed/contrasting traces, with all48 assigned a reviewed/unknown status. Separate: actual child acquisition (request and returned content), direct/manual/literal final, executed computation grounded in acquired predictions where dataflow is identifiable, faithful computation over semantically wrong child labels, incorrect/abandoned reduction, and unknown. A genuine child call is not proof its labels were used; successful final is not proof of computation. Root reads of original context and copied/reconstructed literals are distinguished. Observation-only evidence may not identify all internal dataflow: mark unknown rather than reexecute. Append mechanism annotations with exact raw event/call links; never mutate raw outputs or mechanically repair results.

## CPU preparation plan

Use writing-plans and TDD inline in this independent external namespace (no Git/source branch or subagents). Create test_audit.py first: strict malformed-vsNULL, native mismatch, current-block/standalone/literal/stale precedence, late-call ledger unknowns/deduplication, and paired bounds. Run RED, then implement audit.py with pure independent primitives plus read-only source/outcome orchestration; run GREEN. Freeze method/parser/source closure in METHOD_READY/PARSER_READY after verifying source-only inventory and absent science. No model/service tests. Outcome execution requires explicit MAIN phase/terminal relay and a separate outcome-mode command; source preparation never scans outputs. Final report and additive mechanism/timing artifacts receive a separate seal.
