# Qualified terminal reader (before outcome reading)

`audit.py --main-terminal-released` is the exact outcome entry. It refuses all outcome reads without the explicit relay flag, requires the exact OWNER terminal to report released, verifies its frozen source closure, and pins scientific outputs before parsing. Actual service identity failure retains raw cost/native-token evidence but makes primary finals NULL.

The reader reuses the independently source-pinned MNLI native/whole-contract primitives, not the study producer's scorer. All48 raw call directories are primary inventory; unexpected directories join the physical cost ledger only. REQUEST/RESPONSE/RESULT form an attempt union; individual known input/output/cache fields survive partial usage receipts, including orphan responses. HTTP returns, choice-bearing HTTP-success payloads, native-authenticated finals, contract-invalid observed zeros and unavailable NULLs remain distinct. Retained request plus response may authenticate a complete leaf final even when a producer RESULT receipt is absent, as in the qualified predecessor leaf reader; it never infers a final from incomplete text, missing request identity or an unauthenticated response. Any disagreement with producer scores is surfaced explicitly.

There are11 prelisted contrasts: three wording effects, six visible-relation simple effects, and two aligned-referenced interactions. These implement the METHOD's explicit named list. Its earlier loose phrase “all six simple-effect means” does not correctly count that list; the named list governs and no contrast was added or selected from outcomes. All use eight context means with full planned intervals, separate complete-context means/signs and per-genre cells. Named diagnostics additionally report the common eligible context subset across the two wrong-visible wordings.

Fresh qualification:8 tests passed in4.85s (dependency deprecation warnings). The all48-call authored raw fixture uses the real pinned tokenizer and frozen request wires/prefixes, includes one corrupt native prefix, one wrong-tag complete output and one extra orphan HTTP500 response, and verifies service-identity failure retains costs. This is CPU artifact simulation, not sampled scientific completions or a model/service run. Tests also cover strict field/cardinality/duplicate-key failure, nonstring tags, per-field unknown usage, all11 contrast values/NULL bounds and the no-relay read guard. Initial syntax-red and intended function-red receipts remain preserved.

No original METHOD, accepted sidecar, current output or prior sealed report was edited. The source-selection/native-prefix review remains separately sealed. Reviewer overlap is with predecessor controls/readers, not authorship of the current six-arm experiment.
