# Prospective independent audit: visible-reference disambiguation48 V2

Reviewer: bridge_audit, independent of the new study's author. The reviewer authored some predecessor fixed-output controls/readers, which is disclosed rather than claiming independence from the entire campaign. No outcomes read. The exact scientific attempt was absent at the 2026-09-10 11:41 UTC existence check. The seal records its own timestamp and a second existence check; MAIN's terminal/release relay is required before outcome reading.

Accepted candidate: `sidecars/leaf-mnli-visible-reference-disambiguation-v1`, READY_v2 SHA256 `afc0acd650166af799c9bc826040af855c995a666378381b6229e4c67ae49904`; exact future output `outputs/attempt-001`. Preserve the historical40-call DESIGN/PLAN and allocation amendment: the actual frozen plan is six arms/48 calls, not the superseded five-arm design.

## Frozen estimands

Eight fresh-relative-to-named-inventory contexts, each48 MNLI pairs from16 premise groups; two contexts per government/slate/telephone/travel. Six contemporaneous arms cross visible relation wrong/alien/aligned with legacy/explicit wording. One paired seed/context, four workers, deterministic six-arm rotations with first-position counts2/2/1/1/1/1 in frozen arm order. Record the actual dispatch/order matrix and its genre/time confounding, not an assertion of perfect balance.

Primary per arm: whole-contract-gated displayed-label correctness over384 planned labels. All six simple-effect means and descriptive interactions use eight context means, not independent records, seeds, or128 premise groups. Let y(relation,wording,context) be strict-correct/48 with a separate native availability flag. Report explicit−legacy for each relation; aligned−wrong, alien−wrong and aligned−alien for each wording. Report the two prespecified difference-in-differences: (explicit−legacy)_wrong − (explicit−legacy)_aligned and (explicit−legacy)_alien − (explicit−legacy)_aligned. Do not select a favorable contrast after reading results.

Keep the frozen native-final NULL inventory and bounds: an unavailable whole call contributes interval[0,48], not a repaired or inferred label vector. An authenticated malformed/wrong whole output is observed zero. Report operational planned-denominator point scores treating bounded nonreturns as unsuccessful, native-available conditional scores, per-context signs, jointly available contrasts, and interval bounds separately. Derive contrast bounds algebraically from each coefficient's sign, including four-cell interactions. No post-hoc primary rescore, equivalence or latent-ambiguity claim. The inherited10-point/6-of8 continuation thresholds are pilot decision aids, not absence/equivalence evidence; report them against their exact stated contrast rather than invent a new promotion rule.

## Source and binding verification

Independently recompute raw parquet label mapping, selected row texts/provenance, normalized premise/pair hashes,128 distinct selected premise groups,384 IDs and label-blind allocation from the pinned named exposure inventory. Check both premise and hypothesis exclusions, prior-ID/alien-ID/seed collisions, no original IDs/gold/genre/provenance fields in the request objects, and immutable draft DATA/PUBLIC preservation. Public text-derived IDs and familiar task/generator remain; no globally research-unseen/pretraining-unseen claim.

For every context and arm verify complete premise/hypothesis strings and order, the exact invariant requested-tag vector (original IDs left-rotated17), and identical per-position exact-tag grammar with all three labels free. Wrong retains original visible IDs; aligned uses local requested tags; alien uses unique absent visible IDs with positionwise tokenizer-length matching. Aligned/wrong share the visible-ID multiset. Re-render every first native prompt, verify exact wire serialization and original legacy wording, natural explicit wording, +8 token difference and all prefixes+3072≤8192. The explicit package changes same-object phrasing, opaque-address description and copying wording while removing the old ID-inference sentence; it is not a single latent ambiguity intervention. Alien comparisons retain repetition/association confounds.

Verify actual released Qwen3-4B model identity/source/template, no LoRA, tools or thinking, sampling .5/top_p1/top_k−1/min_p0/penalties,3072 output/8192 context,90-second request and4 workers. Inspect actual owner→V2 collector→qualified service argv/namespace and inclusive1440outer/1320work/1410owned/startup180/cleanup90/30margin. Request-body equality and returned native prompt/completion IDs plus usage and parsed branch authenticate scoring; do not substitute historical scores as fresh controls.

## Independent outcome scoring and diagnostic gates

Parse the whole native answer as exactly48 ordered JSON objects, unique keys, exactly tag then label, exact per-position requested tags and canonical labels. No partial-array rescue, reordered IDs, extracted JSON or label repair. Preserve strict whole-array accuracy and row-label primary separately. Shape-only positional correctness and tag/field/order fidelity are diagnostic, never primary replacements.

For wrong-visible arms only, use the predeclared subset where the displayed record's gold differs from the record actually named by its requested tag. Among whole-contract-valid calls count displayed/named/third predictions, show all potential subset sizes and eligible denominators, and retain undefined diagnostics when no call qualifies. Displayed+named+third must exactly exhaust eligible differing-gold items. Do not diagnose named-record following from equal-gold coincidences, compare wrong-arm diagnostics on selectively different eligible contexts without disclosure, or infer an internal binding mechanism from behavioral labels. Genre results have only two contexts each.

## Availability, physical cost and closure

Inventory all48 planned calls before aggregation. Union REQUEST, RESPONSE, RESULT and summary artifacts; distinguish never dispatched, dispatched/failed, response-proven attempts, successful choice-bearing completions, native-authenticated finals and contract-valid finals. A request hook is attempted transport, not proof of GPU generation. Preserve usage from any retained raw response even if RESULT is missing or authentication fails; retain per-field unknown counts, prompt/completion IDs, finish/length caps, elapsed times and error causes. Do not infer provider billing. Include startup/release and preflight metadata separately from actual generations.

Finalize a source-pinned report with every native branch, eight-context paired tables/interactions, diagnostic denominators, raw cost union and explicit limitations. Freeze corrections additively if a reader defect is found; never modify accepted source/old seals or turn recoverable raw data into an available final without the frozen contract. No generated-code execution, GPU/service/queue action or reroll by this reviewer.
