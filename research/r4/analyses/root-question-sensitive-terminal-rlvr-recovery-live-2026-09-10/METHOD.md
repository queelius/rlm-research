---
status: prospective_frozen_before_outcomes
date: 2026-09-10
study: root-question-sensitive-terminal-rlvr-recovery-v1
planned_endpoints: 336
---

# Prospective native and training-chain audit

No scientific outcome is read until both the immutable `OWNER_TERMINAL.json` and
the exact parent `EXIT.json` exist, bind one another by SHA-256, report no live GPU
PIDs, and confirm service release. Attempt-001 is an integration failure with zero
physical requests and is excluded from all scientific estimates.

The endpoint universe is the frozen owner inventory: 192 training attempts plus 72
protected coordinates under each of `start` and `rl_last`. Authenticated returned
empty or malformed finals are observed and score zero. Missing, unreturned, or
unverified finals are NULL, never zero. Report planned, physical, observed, correct,
NULL, format-invalid, costs and token usage separately by phase, arm, operator,
scope, threshold, slot/family, and context. Primary effects are paired protected
`rl_last - start` differences over the same 72 coordinates, with wins/losses/ties/
unknown and context-cluster summaries. Primitive24 is retention; composed48 is the
primary task-sensitive readout. Fixed-last means QS6 itself if zero updates commit.

For every actual update, independently authenticate the 24-row window export,
recompute task-centered population-normalized advantages from admitted binary
rewards, and verify exact selected episodes. Verify root suffix masks, prompt labels,
native old log-probabilities, TIS weights capped at 2, equal episode/turn mass, no
child credit, and correction diagnostics. Authenticate the checkpoint adapter,
config, state, Adam moments and RNG files; require a contiguous optimizer cursor,
fresh Adam at step zero, exact previous-policy linkage, finite FP32 moments, and
nonzero finite root-LoRA change. Record noops and all unadmitted/partial attempts.

All request/response/result/episode/audit and cost-ledger files are hashed. Compare
the owner cost ledger to independently counted physical records and usage; missing
usage remains unknown. Capture, optimizer, service and total wall time remain
separate. Correctness alone is not semantic faithfulness; a later manual trace audit
must assess requested operator/scope/threshold and actual child-label use.

