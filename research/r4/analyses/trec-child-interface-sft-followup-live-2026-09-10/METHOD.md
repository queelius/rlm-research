---
status: prospective_frozen_before_outcomes
date: 2026-09-10
study: trec-child-interface-sft-followup-v1
new_endpoints: 480
reused_c32_development_endpoints: 96
---

# TREC child-interface continuation audit

Do not read outcomes until the exact owner terminal is bound by the sole parent EXIT,
all owned services are released, and the EXIT reports no timeout or GPU processes.
`complete` and exit code zero are reported but never substituted for endpoint availability.

Audit all 480 frozen new coordinates and merge only the 96 c32 rows from the already
exposed query-conditioned panel, matching context, pair, interface, and seed. The primary
new-pair panel is the decision-relevant comparison: unchanged c32, full6 continuation,
and ABO continuation under both interfaces. Report each policy/interface cell, paired
policy differences, and the policy-by-interface interaction. Development results are
secondary and explicitly research-exposed; no outcome-pristine claim is allowed.

Authenticated malformed or empty results are observed zero. Missing, unreturned, or
unauthenticated results are NULL, excluded from known-pair means, and retained in lower/
upper bounds. Report projected A/B/other support, confusion, and balanced-class bounds.

Require both arms to start c32 with fresh matched Adam/RNG and authenticate checkpoints
6, 12, 18, and true fixed-last 24. Verify adapter/config/state/optimizer/RNG hashes,
assistant-only masks, context order, optimizer cursor, and no best-checkpoint selection.
Inventory every native REQUEST/RESPONSE/RESULT and sum raw usage with unknown fields left
unknown. Separate training, capture/readout, service, and total time. No GPU/model call is
part of this audit preparation.
