# Checkpoint16 and terminal transfer: stage-once extension

This operational extension inherits METHOD.md and the sealed STEP12 audit without
changing endpoints, graph admission, source splits, optimizer checks or the fixed
final16 primary. It is frozen before this agent reads round13–16 or validation16
outcomes. The parent disclosed all16 COMMITs and finalized validation16 collection;
transfer outcomes and selection have not been read by this agent.

Read only terminal stages: training13–16, validation16, then both transfer phases
after their exports/FINAL and operation release are present. Publish each stage
once in separate artifacts. Reuse the sealed prior input fingerprints and small
projections; load checkpoint12 once as tensor anchor, then only checkpoints13–16.
Never reopen old episode graphs or old optimizer tensors. Read only each stage's
authenticated serving-log prefix, not later appended log bytes. No lifecycle lock,
GPU, process signal, generated-code execution, reward repair or rerun is authorized.

Reuse the already corrected nullable diagnostic root_reply projection. A new pair
projection records the actual task hash and SHA256 of the exact task prompt. It
does not alter scoring, rendering, prompts, datasets or source traces. Validation
pairing uses unchanged episode IDs. Transfer pairing uses the frozen pair_id:
original/final episode IDs differ by design. Require paired task/context/seed,
source/repeat/split and actual task/prompt bytes to agree. Unknown/missing sides
remain null. Present raw observable scores separately from graph-admitted scores.

Primary fixed-final16 transfer contrasts report all48 paired coordinates, gains,
losses and ties among complete pairs, alongside every null and its reason. Context
groups, not96 nominal episodes, are the repeated-data unit. Keep five planned
strata separate: composition64 trained targets12, composition64 reserved DESC/ABBR
targets12, length128 eight, length256 four, and leaf-test-exposed32 twelve pairs.
Report target and context summaries without pooling exposure into novelty claims.
The validation maximum is only descriptive and must be the earliest maximum among
0/4/8/12/16; it never replaces fixed-final16 as primary. Report optimizer/mask/
correction checks, actual physical request/return discrepancies, logical/cached/
completion tokens and unknown usage. Combine prior stage costs once, distinguishing
original stopped attempt, continuation, service/optimizer work and release/CPU tail.

If a future stage is absent, stop the audit at the last sealed stage and report
pending. If terminal artifacts declare a stop, preserve it as partial evidence,
not fabricated zero outcomes. No live stage is scored merely because files exist.
