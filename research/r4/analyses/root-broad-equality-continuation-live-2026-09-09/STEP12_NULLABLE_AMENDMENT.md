# Additive diagnostic-null projection amendment

The first STEP12 audit invocation exited 1 in its first new stage, round9, before writing any stage/checkpoint projection: inherited audit.py line192 indexed `ep['traces'][-1]['root_reply']` for its report field even when an incomplete trace legitimately omitted that field. The strict scorer runs only for observable terminals and was not the failing expression. No checkpoint tensors had been loaded. The traceback and an isolated test of the exact inherited expression reproduce KeyError('root_reply'); the other empty/no-trace/actual-text cases pass.

Preserve audit_step12.py and STEP12_METHOD_READY.json unchanged. New audit_step12_nullable_v2.py replaces only this diagnostic expression with a nullable accessor. It does not change strict terminal parsing, terminal observability, graph reconstruction, exported rewards, training admission, masks, correction guards or any scientific source. An observable terminal missing its required scorer input still raises through the unchanged scorer path. The amendment records no outcome totals and does not inspect later stages.

The isolated regression uses the real inherited expression before the wrapper exists (one failure, three passes), then the wrapper accessor afterward. The next run repeats the necessary partial first-stage reads from the failed process; no old raw stages are rescored and no completed checkpoint audit is repeated. All new stages still receive the same finalized-file checks and single-pass cache within the successful invocation. This is an audit implementation failure, not an inference retry or additional campaign failure.
