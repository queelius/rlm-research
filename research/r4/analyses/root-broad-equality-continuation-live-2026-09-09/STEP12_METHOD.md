# Checkpoint12 incremental audit scope

Frozen before this analyst reads any new round9–12 or validation12 outcome. Parent disclosed only that validation12 exists. A metadata-only check found its rollout/STATUS.json and export/MANIFEST.json. All five new stages must have finalized status/export and the four updates must have COMMIT/state/result before scoring begins; an absent or unfinished stage is pending, not a failed outcome.

Inherit METHOD.md (b470f2cd2c8056f18ff8cb3c9c05797682f31fdd3f276a2d22ba32f9ec6f337d), the original BROAD16 method, and sealed STEP8_PUBLISHED.json (8f2be71aaeb07a45c8fbf1cc425e03622834fcdfa20ae281f3e2b5f014ea59e1). Fixed final16 remains primary. This is interim training/validation evidence only; no checkpoint13–16, final selection or transfer outcome is opened.

Reuse pinned audit_step8.py and its independent raw/native scorer, exact policy substitutions, bounded serving-log prefix reader, root-only mask checks and actual Adam/adapter/guard checks. Only new stage coordinates and publication filenames change. Extend the immutable hash cache with the sealed STEP8 source inventory; do not reauthenticate entire prior closures or rescore old raw graphs. Read the checkpoint8 anchor adapter once, and load only new checkpoints9–12. The step8 source seal and small prior audit projections are evidence references, not new scientific observations.

Audit rounds9–12 (96 planned attempts) and validation12 (16 planned attempts) once. Record exact terminal/execution/graph admission, selected mixed rows versus homogeneous valid rows and infrastructure exclusions separately. Preserve raw endpoint scores alongside admitted diagnostic rewards. No broadened exclusion recognizer, new export, reward repair or execution of model-generated code. Authenticate every new update's COMMIT/state/INPUTS/GROUP/manifest/correction/member identities, preceding policy, Adam cursors/order, exact adapter load, actual finite deltas, root action credit and independent five-guard reconstruction.

Paired validation summaries compare the same IDs/context hashes/tasks/seeds against sealed step8 and the sealed original projection. Report raw observable pairs separately from graph-admitted pairs. If either terminal is unobservable or its score is not binary, the raw paired contrast is null, not an implicit wrong answer or tie; graph-invalid outcomes remain null in the admitted contrast even if their raw terminal is observable. Retain all planned denominators and missing-side reasons. A later cap cannot censor earlier complete answers. Task/length/target tables keep the eight context groups and nested seeds; training task rotation is not a fixed-task learning curve.

Costs include all retained physical request/result records, roles, reported prompt/cache/completion counts, unknowns, per-stage collection and new optimizer/checkpoint durations. Serving log reads stop at each authenticated prefix length, even if subsequent activity is appended. Partial or unassigned evidence is a material limitation, not synthetic cost. Do not query GPUs, processes, launchers or acquire any lifecycle lock. No indefinite polling. Publish new STEP12 projections, STEP12.json, STEP12_SOURCES.json, REPORT_STEP12.md and a seal; keep every prior artifact unchanged.
