# Complete-demonstration SFT — independent outcome-blind audit method

Frozen after MAIN reported actual child capture underway, before this auditor opened any capture, training or readout output from the new attempt. The auditor has read the prospective design and implementation only. Earlier bridge/plan outcomes are exposed and motivated the study; this is outcome-informed exploratory research, not a confirmatory preregistration. MAIN alone owns GPU and lifecycle. No outcome scoring until MAIN explicitly reports terminal and release.

## Accepted boundary and question

Scientific READY c9fc31943e81f8b3597e134a5e26b6d9c162e868356d20a37765081fca4b5b23, identity974c38ad8d0fdbb59f734f7fc2fb8b07e8bb1ac825a868e645ff508ff14a9604. Recovery output is `sidecars/root-complete-demonstration-sft-v1/outputs/attempt-002-an27-credential-recovery`. Preserve attempt001, which MAIN reports failed before model/corpus creation because the parent lacked a credential. Bind RECOVERY_READY and its exact counted output-path replacements; no scientific seed/target/prompt/objective changes are implied. Never read or serialize credentials. Authenticate RECOVERY_BINDING, ALLOCATION_RUNTIME and accepted parent command/EXIT/release after trigger. Parent operation is `operations/2026-09-09-an27-complete-sft-recovery`, cap3600s. The initial command/process identity is monitoring context, not outcome evidence.

Question: does adding authentic scalar terminal supervision to the same16 authored canonical actions improve strict native-final answers relative to matched action-only SFT? Both arms start low66c, use fresh AdamW at1e-4 for four full16-example updates, and select fixed checkpoint4. Complete objective is mean16(action token-mean CE)+0.1×mean16(terminal token-mean CE); action-only keeps the identical action coefficient. This is matched action loss mass, not equal token/forward compute or equal gradient contribution. A third unchanged-low66c root controls retraining. The child stays actual c32 throughout capture/readout and is not trained.

## Capture and supervision audit

Preserve all16 source-authored canonical examples in exact source order. Independently match each action row and code to prepared ACTION_ROWS and original ROWS_canonical. Reconcile authored proxy physical attempts with native role/typed captures by internal request IDs and example identity, retaining retries/request-only/errors. Do not mistake an HTTP200 tool action for a native final. Root proxy responses are authored, with synthetic transport logprobs; child requests/returns must authenticate c32 model, descriptor, live model cards, exact native prompt/completion IDs and actual logprobs. Synthetic root logprobs are never sampled likelihoods, RL admission or on-policy evidence. Host dummy verifier rewards cannot filter examples or become training labels.

Each completed example must have the exact original first root prefix/action tokens, one actual scalar tool observation and a second root request whose graph-rendered prefix equals physical native wire IDs. Independently check the final branch and renderer/tokenizer prefix, the scalar's strict nonnegative ASCII integer text, terminal content `Answer: N`, terminal IDs plus151645, and native final equality. The target N is the actual observation even if frozen dataset gold differs. Report actual-scalar versus independently recomputed scoped dataset-count disagreements; never relabel a wrong observation. Preserve zero, partial capture and failure without replacements. Exactly16 valid examples and immutable CORPUS_READY must precede either optimizer. No generated code is executed by this audit.

Validate each turn's input_ids/labels/loss_mask/prompt_length: all system/user/history/action/observation/child prefix positions are masked, only the current authored root target suffix is trained, no truncation, token lengths≤8192 and EOS151645. Training rows must exclude reward, advantage, behavior/old logprobs, admission or equivalent sampled-policy fields. Raw authored trace placeholders may remain explicitly tagged AUTHORED_NOT_RL; this does not authorize RL export.

## Optimization and checkpoint audit

Authenticate identical corpus digest, starting adapter/config, base and all data-order seeds. Check four complete updates per arm with the same deterministic16-example order per update and no partial optimizer step. For every ledger term recompute coefficient1/(16*n_action) or0.1/(16*n_terminal), target counts, nominal masses, actual FP32 coefficient masses, weighted CE from CE sum, and per-kind/full totals. Report terminal/action token exposures and actual elapsed compute separately. Validate rank8 FP32 trainable LoRA only over frozen bfloat16 base, dropout0, fresh empty AdamW state, lr1e-4/weight_decay0 and clipped finite nonzero gradients from source/load records. Coefficient mass is not an observed gradient share.

Independently inspect four checkpoint state chains, hashes, optimizer step counters1..4, selected checkpoint4, adapter nonzero change, optimizer moments and recorded RNG/source provenance. CPU `torch.load(weights_only=True,map_location='cpu')` is permitted for owned saved checkpoint state; no model instantiation or GPU load. Validate physical served weights/config/descriptor/live-card crosswalk rather than aliases or stale prose. If a required artifact is unavailable, distinguish unknown verification from demonstrated training failure.

##48-slot readout, endpoints and comparisons

Freeze exact16 coordinates crossed with unchanged/action_only/action_terminal before outcomes. All share native initial prompt IDs, T.5/full-support seed/cap, source records and typed c32 interface; actual trajectories may diverge. The panel has four16-record contexts: two helper-training and two helper-validation,32 normalized groups per partition,64 groups total. Audit source-line text/normalization, label provenance, the named nine-catalog exclusion boundary and training/readout disjointness. Do not call helper-validation training-supported, globally fresh, license-resolved or pretraining-clean. Two query families (single u00 and union u00/u01) and two seed repeats yield16 matched coordinates; seeds in prepared coordinates are distinct after offsets. Context is the cluster.

Recompute scoped gold from original public user IDs, target labels and host labels. Strict endpoint is whole stripped ASCII `Answer: ([0-9]+)` with exact integer gold. Actual returned native final requires stop/length, no parsed tools, exact native content versus trace reply, and exact physical final graph branch. Completed empty/prose/decimal/wrong integer is observed0; unrun/provider/setup/incomplete/no-final is NULL. The implementer's native_unavailable helper is not this audit's authority. Preserve an earlier real final if later outer handling fails. Graph unavailable is separately flagged; no inferred answer from author metrics or a displayed scalar.

Primary matched contrast is action_terminal−action_only over16 slots, with correctness and strict-format outcomes shown separately. Secondary contrasts are each trained arm−unchanged. Report all-slot available/correct/NULL, wins/losses/ties/unknown and tight binary bounds; context, helper partition and query-family strata separately. Do not pool48 as independent observations or infer significance. If only format improves, describe format learning. Do not extend this small recipe automatically if neither outcome improves.

## Semantic coverage, cost, publication

Audit physical attempt/request/result counts, role/depth, child typed eligibility, raw maps/source-ID/text/grammar order, native wire/graph IDs/logprobs and scoped target FP/FN agreement. On the final root branch distinguish delivered child values from literal root-visible maps/scalars, complete/unconflicted coverage, actual reduction and final copying. Preserve partial/conflicted/unparsed cases. Host-derived counts are diagnostics, not executed outputs. Parsed static code can identify scope/filter/state mistakes but cannot prove consumption or treatment blindness. No generated program execution.

Count all capture and readout physical attempts, synthetic root target tokens separately from actual sampled child/root tokens, known logical input/output/cached/uncached usage and unknown-usage denominators. Keep service readiness/startup/collection/training/release and outer elapsed separate. Authored proxy responses may lack usage: retain unknown, never fabricate zeros. Account for original failed setup separately from recovery experiment GPU work. Caps capture1080, training480/arm, readout420/arm, work3450, cleanup120, owned3570, outer3600 are ceilings within shared deadlines, not additive guarantees.

Prepare narrow source-pinned parser/mask/bounds/ledger fixtures using CPU only. Reuse qualified endpoint/graph helpers through a new adapter; never execute their old hardcoded main. Preliminary readout publication, if useful after terminal trigger, must clearly label full corpus/checkpoint/semantic/cost stages still pending. Final report goes to this operation's complete-sft-audit-report.md, with per-slot evidence, hashes and additive seal in this analysis directory. No source/GPU/service/queue mutations, subagents, broad tests or literature work. Audit preparation/completion must not block MAIN's ready successors.
