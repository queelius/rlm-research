# Accumulation32: cumulative returns worked, but did not produce accumulated reductions

Independent outcome audit of `root-acquired-evidence-accumulation-v1/outputs/attempt-001`, after MAIN's terminal/release relay. runtime_port did not author this experiment; contributions to inherited scale/native/lifecycle code and the prior CPU review are disclosed. The METHOD was frozen at 08:58:51 UTC before the attempt existed. Its parent binding changed through MAIN's documented warm-orphan cleanup/requeue; the scientific attempt/inputs did not change. Parser implementation and manual mechanism annotations are post-outcome, under the unchanged prospective method. This is an exposed, adaptive four-context-cluster package comparison, not a confirmatory independent-record trial.

## Result and uncertainty

| Arm | Strict correct / planned | Native available | NULL | Success bounds |
|---|---:|---:|---:|---:|
| B: batch-local dict | 1/16 | 5 | 11 | 1–12/16 |
| C: cumulative dict | 0/16 | 4 | 12 | 0–12/16 |

All nine available finals satisfy the exact `Answer: N` format. Conditional accuracy is 1/5 versus 0/4; operational planned-zero rates are 6.25% versus 0%. There are only three both-available pairs: zero C wins, one C loss, two ties. Thirteen pairs include a NULL. The sum of paired C−B success bounds is −12 to +11 out of16; the observed planned-zero difference is −1/16. Missing outcomes are not converted into model-invalid zeros in the primary data.

All 256-record endpoints are NULL in both arms. At128 records, count is B1 correct/2 available versus C0/3; weight-sum is B0/3 versus C0/1, each panel planned4. Parent-cluster `(correct,available)/4` results are: parent00 B(0,1), C(0,1); parent01 B(0,1), C(0,1); parent02 B(0,1), C(0,0); parent03 B(1,2), C(0,2). Cluster planned-success differences are 0,0,0,−0.25. Every gold is nonzero; zero succeeds0/16 and the predeclared best constant40 succeeds2/16. The lone actual success is22, not this constant.

## What actually executed

The cumulative return mechanism was exercised in **six C endpoints across all four parent clusters**, but **zero C endpoints completed a requested reduction over accumulated multi-batch evidence**. Zero B endpoints did so either. The predeclared promotion rule therefore fails: the multi-parent return criterion passes, but the additional-four-faithful-reductions criterion, no-availability-loss criterion and positive strict-dataset difference do not.

Two retained EPISODE/ledger traces show genuine10→20→30-label and16→32→48→64-label returned snapshots. Four additional missing-EPISODE NULLs retain exact physical native input histories with real tool observations: singleton maps grow1..21,16-sized maps grow16..112 and16..96, and50-sized maps grow50..200. These snapshots exactly match the chronological union of the same endpoint's actual returned child maps. The last snapshots21/112/96/200 were included in rejected overlength root requests; the latest snapshots in successfully returned root calls were20/96/80/150. Thus runtime return/retention is distinguished from a later successful model forward, and none is primary-final salvage. Named mapping assignments and printed returned values support retention; the10-record trace only prints its returned snapshots and does not retain a named aggregate for reduction.

All nine actual finals instead follow **one complete128-record child acquisition**. Six execute the requested operator with the actual all-user scope on those fallible labels: B4 and C2. Only B1 is dataset-correct; the other five are wrong because the acquired labels imply a different result. Three other finals execute wrong user scopes: nonexistent `u00`-style names or `user0`-style names yield0, and omission of actual user `u3` yields69 rather than the all-map85 (gold89). Every available final copies an actually printed scalar; this does not make the wrong-scope operations faithful.

Concrete examples (row indices are frozen dispatch order; full IDs, code/observations and native request references are in AUDIT_v3/MECHANISMS):

- B row19 `e1e639dd…`: actual128-map→numeric count22→printed22→`Answer: 22`. The map has18 semantic disagreements overall, including one false positive and one false negative for the target that cancel. The operation is faithful, not a perfect classification result. Paired C row18 prints21 from its map, versus gold22.
- B/C rows15/14: actual entity map implies40, executed count40 and final40, versus gold26. Fourteen false entity labels explain the discrepancy; no host-gold repair is applied.
- C row10 `5be19261…`: repairs mistaken whole-JSON parsing of the accurately described JSONL file, then receives cumulative16/32/48/64. It subsequently emits a malformed tool envelope with an enormous numeric literal, not an executed reduction/final.
- B row28 `3698f121…`: acquires all256 in sixteen actual batches but repeatedly overwrites `batch_labels`. Native input tool observations show requested weight reduction failing `KeyError: 'q579e602db9b5'` because the mapping contains only the last16 IDs. Three observed reduction attempts fail, then the context overflows. This is direct evidence of the overwrite failure, not merely absence of an accumulator keyword.
- B row8 `297741d6…`: an unnecessary tqdm import fails, followed by repeated `uv pip install tqdm` actions and “No module named uv” observations, with no child acquisition; this is a separate reachability failure.

The19 retained episode ledgers contain83 successful decoder calls (B70/C13), each matched to exactly one prior actual child response using native response identity, raw bytes, timing and observed variable flow. All83 current/returned/cumulative-ID contracts agree. These19 ledgers contain no failed validation, conflict or observed returned-dict mutation/reload. They are not evidence that none occurred in lost episodes: raw native prefixes separately show duplicate-requested-ID validation failures in B row20 and malformed child JSON in other NULLs. Seven B endpoints have observed batch-local overwriting across successful acquisitions. No executed accumulated running-total double-count, fabricated-label deposit or private-ledger introspection was found in the retained observation evidence. The missing episode ledgers remain unavailable; they are not recreated as authentic ledger receipts.

## Native validity, source closure and failure taxonomy

All32 first actual root prefixes and root seeds match the frozen plan. Exact fixedSFT24/c32 aliases/adapter identities, base/config,8192 context and full-support sampling were checked against the actual service binding and role wires. All627 returned physical/native completions agree in token IDs, completion logprobs and usage lengths; retained19 graphs additionally pass root/child branch ancestry checks. Nine final text/branch identities authenticate independently; scores agree with every source RESULT. Exact English/raw public files remain source-bound and common within pairs; only helper return behavior/description changes. Host truth was independently enumerated from the frozen public records and private labels for every task, and agrees with all frozen golds. Inputs remain the exposed1024 unique records from the accepted scale panel, not new source groups.

There are19 RESULT/EPISODE artifacts, **not19 available finals**. Ten end in malformed native tool envelopes with no parsed assistant content: seven length-capped and three stop-finished. They are not authenticated malformed final-answer strings and remain native NULL. Thirteen other endpoints retain FAILURE but no RESULT/EPISODE after approximately180-second per-episode timeouts. Each has a physical backend HTTP400 request exceeding8192 input tokens (8198–10655), forwarded as HTTP500 by the bridge; ten are root requests and three are full256 child requests. These are visible context-admission/transport failures, not guessed semantic errors. Raw completions and tool-history diagnostics are retained without changing the missing-RESULT primary NULL rule. Orchestrator `complete=true` does not establish endpoint availability.

## Physical cost and closure

| Arm | Root / child attempts | Returned completions | Known input / output tokens | Unknown usage calls |
|---|---:|---:|---:|---:|
| B | 343 /118 =461 | 456 | 1654407 /54890 | 5 |
| C | 119 /60 =179 | 171 | 516259 /35327 | 8 |

Total640 physical attempts,627 native returns,13 failed attempts; known input2170666/output90217/cache1987520. Cache is B1539648/C447872. Each usage field is unknown for13 calls, not zero. Actual sent native input IDs total2282471, including rejected requests; this is distinct from returned-provider usage. The640 role requests/results form a bijection with the640 per-coordinate physical records;1280 typed records are mirrors, not additional calls. No unmatched dispatch intent, duplicate identity, unplanned physical coordinate or omitted NULL-episode cost was found. C's lower token count is not a performance gain: it also has fewer available finals and different failed acquisition paths. Provider billing and FLOPs are unknown.

MAIN parent COMMAND epoch1789031001.1684167; owner950.612635 seconds; parent951.1013626 seconds, exit0 and empty GPU. SERVICE_STOPPED verifies all captured owned identities exited and ports free. Parent EXIT epoch1789031952.2766473 and next card COMMAND1789031953.1822217 are MAIN-relayed, a0.905574-second handoff. No1800 outer/1770 owned/1650 work overrun is observed. Exact parent COMMAND/EXIT/stdout and service/owner artifacts are pinned; original warm-orphan cleanup is not attributed to this experiment's endpoint costs.

## Queue implication and analysis integrity

Do not promote cumulative snapshots alone as a capability fix or rerun this free package unchanged. The strongest new mechanism evidence is that the helper can restore cross-call retention, yet trajectories still fail to reach a completed reduction/valid terminal and verbose accumulated observations hit context limits. A future narrowly controlled persistence test would need to distinguish compact retained state from repeated full-map printing and then observe reduction/stop, without supplying an algorithm or forcing our preferred batch plan; no new implementation is authorized by this report. Current warm RL and task-parameter evidence should guide whether such a test is worth prioritizing.

Four focused CPU parser tests passed. No sampled code was executed, no model forward/service/GPU action was taken, and no original files changed. AUDIT_v3.json is the authoritative parser table. Earlier AUDIT.json/v2 are preserved drafts: v1 checked the nonexistent `tokens.logprobs` field rather than `completion_logprobs`; v2 still overmatched identical child request bodies across pairs in diagnostic role membership. V3 uses exact response identity as well. Neither correction changed primary scores or physical totals. Final wire checks have zero discrepancies and the640-call endpoint bijection passes. Manual mechanism labels are explicitly post-outcome and separate from source primary scoring. FINAL_SEAL pins analysis files,3566 scientific output files, accepted source closure and operational parent receipts.
