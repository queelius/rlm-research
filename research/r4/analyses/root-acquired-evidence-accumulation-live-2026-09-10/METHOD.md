# Prospective accumulation32 outcome audit

Freeze before launch/outcome reads for `sidecars/root-acquired-evidence-accumulation-v1/outputs/attempt-001`. READY SHA `ca0210997b73190f010ea56d7c40361ba6360e605f00d97b75f573364eb040a8`, identity `d7727c26572295084d5f01a8374c1ee09da41fe79be97d2fa88ddcada38bf74e`. The accepted waiter is `operations/2026-09-10-after-warm-accumulation`, waiting on exact warm PID3013619/start921876972/PGID3013618. MAIN supplies actual launch/terminal relay. No GPU/service/lock/queue or sealed-source changes by this auditor.

Independence: runtime_port did not author accumulation32, but authored inherited scale/native/runtime components and CPU-reviewed this implementation. Prior scale/composition evidence and the intervention's approved decision thresholds are known. No new accumulation outcomes have been read; this is an exposed, adaptively chosen package comparison, not a confirmatory or whole-system-unseen test.

## Inventory and primary scoring

Reconstruct all32 slots from frozen FREE_PLAN/EVALUATION_PLAN: sixteen paired large blocks, batch-local B versus cumulative C, one common seed per block and fixed balanced8/8 arm-first dispatch. Four parent clusters, eight nested128/256 contexts, count/weight questions; all1024 unique records are scale-exposed. Every per-arm gold is nonzero; host baseline zero0/16 and constant40=2/16 are frozen without selection. Verify exact source/public order/query/seed, common records/context/query files, treatment helper bytes/description, fixedSFT24/c32 and model/base/config binding. No historical outcome comparison is causal.

Independently authenticate each actual native final using trace branch ancestry, physical request/response IDs and completion tokens/logprobs, real root/child aliases/weights, first-prefix equality, sampling/caps and native decoded text/root reply. Outstanding tool routes or absent/unverified finals are NULL; malformed authenticated final text is strict0. Returned length-capped complete finals follow the declared source contract with a separate cap flag. Source RESULT absent means primary NULL even if a raw complete final can independently be authenticated; retain that only as a provenance-bound diagnostic. Reconstruct stale/missing aggregate summaries from existing per-coordinate RESULTs without changing endpoint admission or rewriting originals.

Report strict dataset correct/available/NULL on all16 per arm, conditional correct/available secondarily, operational planned-zero view and `[correct,correct+NULL]` bounds. Report per-pair B→C wins/losses/ties only where both available, unknown pairs and worst/best difference bounds. Show size/operator panels and all four parent-cluster results; no independent-record significance, best-seed selection or post-outcome block deletion.

## Structural admission is not native acquisition

The decoder accepts structurally valid caller-supplied JSON with exact current requested IDs/canonical labels. It has no native call registry. A successful ledger entry alone does **not** prove genuine child evidence. The common silent JSONL ledger is runtime-writable, introspectable, reloadable and removable; the harvested trace.info copy is an observation, not an authentication oracle. Missing ledger/finalize information remains unknown, not reconstructed into claimed model-visible state.

For every planned endpoint, retain actual root code/tool observations, native root and child call timeline, and ledger raw bytes/hash when available. Manual post-outcome mechanism labels are separate from prewritten primary scoring. Join each successful decoder input to prior actual child returns by exact raw bytes/IDs, program variable flow and timing; content equality alone does not establish causal use when literals or ambiguous variables intervene. Classify entries as genuine child-derived, copied/literal, edited/transformed, contradictory, fabricated, or unknown, preserving ambiguity. Keep all physical calls, including outputs never decoded. No sampled code is executed by the auditor.

Track attempted decoder calls versus successful deposits; sequence numbers include failed validations. Check invalid/truncated/duplicate/missing/extra-ID calls produce no deposit, repeated identical inputs add no coverage, latest valid decoder input wins conflicts, and returned snapshots are fresh dicts. Check overwrites, returned-dict mutation, module reload/tampering, ledger access or alternate custom decoders as actual behavior, not silently repaired errors. Privileged host labels must never repair maps or substitute into rewards.

## Actual accumulation and compatibility

Require at least two useful disjoint actual acquisitions and observed earlier/later labels coexisting in the model-used object before calling the mechanism accumulation. A host-unioned map, a ledger store never used by the root, token/AST presence, matching final scalar or literal reconstruction is insufficient. Inspect executed code plus its actual returned observations, lineage of the used mapping, requested operator/category/scope, displayed scalar and link to the final. Distinguish faithful execution consistent with actual fallible labels from strict dataset correctness and from wrong-but-faithful child-semantic errors.

Inspect all correct finals and all endpoints with successful decoder calls; summarize acquisition/reduction/stop failures for all others. In C, legitimate old batch-running-total code can double-count prior values, and growing printed snapshots can cause context pressure. Count these as observed treatment compatibility/cost effects, not infrastructure defects to repair. Reacquisition, abandoned cumulative snapshots, mistaken scope/operator, hidden-store introspection in B, and fabricated/literal evidence are alternative mechanisms. No automatic compact print, answer fallback, model-code repair or selective oracle follow-up is allowed.

Report the predeclared exploratory promotion rule literally: multi-batch state actually returned in at least two parent clusters, at least four additional faithful requested accumulated reductions in C per16, no loss of native availability; practical promotion additionally needs positive planned strict-dataset difference. These are decision thresholds, not statistical significance. If free trajectories do not reach successful multi-batch decoding, the study has not tested an exercised persistence mechanism.

## Cost and closure

Union raw physical records and native/role/typed request/response/result/failure evidence independently of endpoint RESULT and ledger availability. Reconcile duplicates by request identity, retain late records, failures and partial work, report unmatched dispatch intents separately, and preserve per-field unknown token usage. Report root/child physical attempts and authenticated responses, input/output/cache known totals, unknown counts, observations/snapshot size, and wall time. No provider billing/equal-FLOP claim; no historical acquisition counted as newly physical work.

Verify planned inventory before service, actual startup/collection/release/harvest timestamps against1800 outer/1770 owned/1650 work, startup≤180/collection≤1440/release≤120/harvest≤30 and serial one-service ownership. Record exact parent COMMAND/EXIT, service-stop and OWNER_TERMINAL/release hashes. Missing closure or incomplete harvest is disclosed, never fabricated or a rerun trigger. Pin all scientific raw artifacts and analysis code/tables/manual annotations at final seal. Until MAIN relay, inspect only source/CPU qualification and lifecycle/count metadata, not new episode outcomes.
