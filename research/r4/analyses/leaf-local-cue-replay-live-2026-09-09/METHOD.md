# Independent local-cue conditional replay audit

Frozen before opening this experiment's outputs. The experiment has already finished; MAIN disclosed only completion of 32 units / 288 candidate forwards and 101.7447 seconds. Earlier sampled cue results motivated this study, and the bridge report is already exposed. This is a posthoc-hypothesis, pre-scoring audit, not preregistration or new task evidence.

Scope: all eight frozen fresh96 contexts (four AG, four SST), positions 16/32/48/64, three current tags, every canonical label. Released Qwen3-4B without a research adapter. Four positions are nested in each context; there are no independent sampling replicates. Prior displayed gold labels are supplied as identical constant-tag history. The corrected neutral prompt is new and shared; it is not the unequal original fresh96 prompt pair.

Primary: independently recompute each complete condition's finite-label softmax from its saved unmodified summed token log probabilities; displayed-gold probability and matching minus constant, over all 32 fixed context-position units, with separate task and eight context summaries. Report sign counts and positions descriptively, not iid confidence claims. Secondary: argmax displayed accuracy (ties retained), shifted minus constant/matching, and shifted-named-label probability on the prospectively fixed displayed/named disagreement subset. These are host-derived conditional diagnostic probabilities, not calibrated task probabilities, sampled accuracy, guided-decoder scores, training targets, or attention measurements.

Authenticate immutable READY/source/input and parent acceptance once, caching hashes. Independently reconstruct prior-only gold prefixes, current matching/constant/shifted17 tags, closing boundaries, full candidate tokens, common-prefix backoff, exact shared physical chat IDs and preceding-token causal positions. Verify all candidate targets, finite raw scores, FP32-sum tolerances and finite-set normalization without using the author aggregate as authority. Check recorded model class/dtype/eval/no-grad/no-adapter and immutable shard stat binding; no repeated model-shard byte scan, model load, inference or generated-code execution. Source-bound HF forward semantics and captured results support the audit; logits themselves were not retained, so their numerical content cannot be independently recomputed without another model forward.

Missing, partial, nonfinite, interrupted and unrun units/conditions stay distinct and null; never renormalize a subset of labels. Preserve author fields and report discrepancies. Costs include every attempted/completed candidate forward, summed full input lengths and scored continuation tokens, no KV cache, actual runtime/load/parent wall and peak memory. These full-prefix teacher-forced costs are not sampled API usage or ordinary generated-token cost. No new experiment, repair, model/service/GPU call, queue edit or old artifact mutation is authorized.

Publish AUDIT, METRICS, REPORT and SOURCES in this new namespace. Focused synthetic score/missingness fixtures precede raw output scoring. Seal method and authenticated inputs first; result access begins only afterward under MAIN's existing terminal authorization.
