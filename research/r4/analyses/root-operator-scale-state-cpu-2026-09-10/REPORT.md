# Independent scale-state24 CPU review

Verdict: ready within the declared exploratory scope. No material science, source, native-entry, cap or score defect found. This is a read-only independent review; the reviewer did not author the study and made no experiment-source, GPU, service or queue changes.

Source reviewed in full: DESIGN, PLAN, ss_study, prepare, ss_collect, ss_owner, qualify, seal and both test modules. Also inspected the reused physical ledger and source representative loader's actual read/normalization/label path. Historical lifecycle proof is reused narrowly, not requalified as a new framework.

Independent `check.py` (no producer oracle import) authenticated all1,024 source question strings, normalized group IDs, source-line labels and public-ID uniqueness; reproduced label-blind1,024 selection from2,000; independently recounted all24 count/weight answers. Four256-record parents are disjoint;16/128 subsets preserve exact original IDs/order/users/weights. Eight question seeds are paired across sizes. No zeros occurred; pooled constant40 gets2/24. No reranking/filtering. Source freshness is only under the pinned named root inventory, with child-training/catalog exposure disclosed. The review does not claim globally unseen text.

Fresh runtime tests: 3 passed in38.79s (two dependency deprecation warnings). The real owner→qualified service wrapper→inference configuration/Popen path was intercepted before any model process; exact checkpoint/c32 descriptor, two-LoRA8192 configuration and preplanned24 were checked. The authored native fixture loaded the actual records file, executed two disjoint8-ID child calls, accumulated their returned maps in one namespace, displayed a positive weight sum, and returned the authenticated native final. This proves harness feasibility, not sampled-model competence; no generated scientific program was reexecuted. A missing-result/late-physical fixture preserved planned NULL and both physical costs, including unknown usage.

Caps and estimand: owner starts inclusive clocks before verification; nominal180 startup +1950 collection +90 harvest within2220work,150 additional release/emergency within2370owned,30outer margin to2400. Release advances early and is bounded by owned deadline. No phase retry or forced child width. Record-scale comparisons have four parent clusters, not24 independent samples; nested evidence changes gold and computational burden together. The source correctly says the measured256 typed-full-map route is pressured, not all one-call strategies. Exact measured child input8111–8367 plus2048 output exceeds8192; two inputs alone exceed8192. Constant-label output lengths are illustrative, not a proof of a lower bound or a model result.

Accounting limitation carried forward, not a blocker: the inherited ledger's `returned_native_completions` counts choice-bearing physical responses and does not itself authenticate token/renderer identity. Independent outcome review must retain raw request/response validation and distinguish HTTP returns, successful choices, native completions and unknowns. It must also count service preflight separately from the free-endpoint ledger and never interpret attempted requests as all confirmed GPU generation or measured billing. No score rescue is introduced by harvest; incomplete/no-result remains NULL.

READY reviewed: `d10c9bb2b7a5f1e8e0d5f3146f2ccc6124d6bbda4373ca6692007af932e119ac`. Fresh CHECK and TEST_RUNTIME receipts reside beside this report. No changes requested before MAIN acceptance.
