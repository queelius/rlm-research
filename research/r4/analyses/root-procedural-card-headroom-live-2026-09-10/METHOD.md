# Procedural-card headroom48: prospective author-disclosed audit

This analyst authored the experiment. This is an author-disclosed outcome audit, not an independent experimental audit; MAIN independently reviewed source and will check outcomes. Method and reader are frozen while the accepted study waits, without outcome reads. Freeze records actual clock and attempt-directory existence; a prelaunch claim requires absence both before and after sealing. Only MAIN's explicit terminal relay authorizes outcome reads. No GPU, service, source mutation, retries, sampled-code execution, or answer repair.

## Population and estimands

Keep all 48 planned endpoints: eight exposed composition contexts, three composed questions, original U versus procedural-card P. Twenty-four paired fresh seeds/blocks, eight context clusters. All records, query and context files, weights, source labels, original U prompt and native task interface are frozen. Treatment appends the same 183-native-token general three-procedure card; this is explicit algorithm instruction plus extra prompt context, not retrieval, trained composition, novel transfer, or a pure token-length intervention. There are no primitive controls and no primitive-regression claim. All four zero and twenty nonzero answers per arm remain; zero baseline 4/24 and best constant 6/24 were fixed before launch.

Primary: strict authenticated native final correctness against frozen dataset gold, all24/arm; authenticate `Answer: N` under the unchanged outer-whitespace scorer, never extract an integer from prose or a tool observation. Missing RESULT, no final, or unverifiable branch stays NULL. Malformed authenticated final is observed incorrect. Separately report available-final accuracy, planned operational correctness (bounded failure is unsuccessful), and native missing-answer worst/best bounds; bounds are not missing-at-random assumptions. Classify unstarted, prepared-but-unknown, attempted failure without RESULT, sampled malformed tool/no final, context rejection, and other authenticated causes. Policy-induced loops are not exogenous dropout.

Report per-context and per-query paired U/P results, wins/losses/joint-wrong/joint-correct/NULL, zero/nonzero strata, and P−U operational difference with native bounds. Context means and eight context signs are the independent descriptive units, not 48 records or 24 independent seeds. No best configuration selection, equivalence, confidence from repeated records, or confirmatory p-value claim.

## Native/source/cost closure

Pin all accepted source/input closure plus READY and actual output bytes after relay. Independently recount host truths using frozen records/labels/operator definitions; do not place labels or truth into prompts. Verify original field/file equality, exact U and P first-prefix tokens for every attempted endpoint, guaranteed card presence only in P, actual fixed24/c32 served descriptor/model/config/tokenizer identities, root 2048 action/8192 context/.5 temperature/top_p1/paired seed, optional child transport behavior, and owner clock/release. A card word appearing in sampled code is not execution evidence.

Use the corrected unique exact request-body plus entire completion-token/logprob join: transport UUID and provider request ID remain separate fields. Authenticate actual renderer final branch, typed model/coordinate, completed root trace and reply agreement; reject ambiguity/conflicts. Preserve any reader defect additively, never recast it as scientific missingness. Parse every returned root/child native completion; do not promote orphan transport to a missing endpoint RESULT.

Union physical directories for all48 including failed/missing RESULT paths. Report attempted requests, HTTP returns, successful choice payloads, token-validated completions, known input/output/cache usage and unknown counts. A rejected/pretransport attempt is not established GPU generation; billing unknown. Preserve returned tokens from partial episodes, service preflight and lifecycle elapsed separately; metadata GET is not generation. Check for raw records outside planned directories rather than silently discard them.

## Mandatory semantic review of ALL available finals

Read every sampled root program and actual subsequent observations, without executing it. Per endpoint record requested operation/scope/categories, actual evidence acquisition/decoding, map coverage and provenance, variable overwrites/retention, actual grouping, threshold comparison, maximum, conditional user set, duplicate treatment, observed scalar, and final concordance. Categories remain actual c32 predictions, not oracle labels.

Classify genuine requested computation; genuine computation wrong because observed child semantic errors; wrong operator/scope/category (correct by coincidence or incorrect); literal/manual recovery; or unknown. A host union of printed maps matching a scalar is diagnostic only and cannot establish the sampled program's semantics. Check all wrong finals as well as successes. Separately report source acquisition and faithful reduction of observed labels, including faithful wrong answers. For multiple batches require observed contributions of earlier batches to executed computation, not a host-union ceiling. Include raw paths/node evidence for every final and salient NULL mechanisms. Aggregate algorithm-word frequency only as optional non-mechanistic diagnostic.

Decision: treatment improving genuinely requested computation on multiple contexts supports explicit procedural prompt headroom, not learned composition or primitive preservation. Scalar gains explained by coincidences, lost maps, or leaf error do not promote a policy-learning claim. Complete faithful reductions limited by c32 error motivate a distinct leaf intervention; no algorithm uptake motivates retiring this card as used. All interpretation follows the frozen panel, no outcome-selected omissions.
