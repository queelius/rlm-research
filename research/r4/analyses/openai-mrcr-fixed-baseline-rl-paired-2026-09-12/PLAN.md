# Fixed-baseline RL paired raw readout

Goal: independently recompute exact/official scores and mechanism/cost evidence for all32 short and16 long planned episodes versus frozen qualified cp32 controls, without more model calls.

One analyzer uses the accepted collector only for causal mapping/failure classification, the reviewed long-audit scorer and clean-observation definition, and actual renderer/native tokens for parser/prefix fidelity. All raw action arrays are compared descriptively; different weights rule out clamp-only/lossless-path attribution. No generated code is executed. Physical start/result/error/orphan calls are retained separately from mapped policy costs.

Tasks: (1) focused pairing/missingness and actual saved-raw fixture, then immutable READY; (2) one bounded completion check, save PENDING or terminal report and stop. No watcher or GPU launch. Main may rerun the one-shot command after both terminal files arrive.
