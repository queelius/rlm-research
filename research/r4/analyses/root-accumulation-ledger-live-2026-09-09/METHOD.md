# Ledger16 independent pre-outcome audit method

Freeze before opening any new ledger16 experimental outcome. MAIN has accepted the queued operation; only frozen design/source/spec/input metadata and CPU fixture/source seams are used for preparation. Analyst did not author this sidecar. Prior accumulation-failure example and these four development contexts are exposed; this is not an unopened test or prevalence estimate. No GPU/model/service calls, process signals, source/output edits, training export or generated-code execution.

## Frozen experiment and primary

Scientific READY4ccf83e1ea83357838d774514250e231c55bcc2a0d2494b88543db2c33b07561; SPEC044b5350b65b95504636727dd8ddd2bee485ec2e1be88006ef61be46fbdc73dc. Accepted operation: operations/2026-09-09-after-cue96-ledger16, ACCEPTANCE3a27e455307a3291ce8ff7d588eb087c77569c081aef5b4a3ab434b528e790a7. Authenticate frozen sources/qualification/input closure once and cache that result; never rehash the global closure inside an episode loop.

There are16 planned whole-RLM episodes: map-only versus appended cumulative ledger, four named exposed validation global contexts, seeds981306011/981306021; eight pairs nested in four context units. Root is exact efab interface-SFT final4, child exactc32. Pair arm order is fixed/balanced by context×repeat, pairs sequential, four pair workers. Retain every planned coordinate and the actual start order.

Independently score the completed available root reply using strict whole ASCII `Answer: ([0-9]+)` after outer whitespace stripping. Correct1; completed empty/malformed/wrong0; unavailable/incomplete/setup/provider failure NULL. Record strict string agreement separately from episode.ok/trace.ok/errors and native graph integrity: an observable correct string with runtime/finalize failure is not silently a clean primary outcome. If native answer availability and raw failure status disagree, preserve both projections and specify the reason; do not repair the answer or equate missing with wrong. Previous completed cases remain observed if a later cap stops another case.

Primary report: per-arm correct/observed/null out of all8 planned, eight paired differences where both available, gained/lost/equal and unavailable pair counts. Four context-level means/paired differences are the sampling units; no16-independent-observation uncertainty claim. If missingness exists, show planned-denominator success counts as a descriptive tally and available-pair contrast without imputing nulls. Preserve setup, provider, graph, incomplete/censored and unrun distinctions.

## Physical native and treatment identity

Bind actual operation command/exit, scientific RUN/STATUS/TERMINAL, source spec, service binding/descriptor/alias/base identity and release records once when terminal. For each episode reconcile planned coordinate/raw SHA, trusted depth0/depth1 invocation metadata, ACP/native request IDs, returned/error attempts and sampled graph calls. Use official WireTrace physical branches (not semantic_parents) to reconstruct prompt/action token sequences and compare native wire request IDs and response IDs/logprobs/counts. Semantic edges separately establish model-call parent/child ownership. Record failed/uncommitted/unreachable calls rather than forcing a clean complete graph.

Compare each actual first-root request against its frozen full token prefix, alias, .5/full-support sampling/seed and absent root grammar, and compare paired first-root requests excluding only transport IDs/times. Treatment cannot cause a first-root action difference: report such differences as pre-treatment stochastic/scheduling nuisance, not a suffix effect. Later trajectories may diverge legitimately; this is whole-policy harness support, not fixed-trajectory arithmetic mediation.

The executed engine uses unchanged `truncate_tool_output(raw)` in both arms, then appends only the treatment's bounded summary, before actual tool-message/log/graph/native capture. Verify that exact order on actual observations, separate original raw/truncated text, added characters/UTF-8 bytes, unchanged original truncation and physical prefix inclusion. No new truncation to reserve space. Summary is authored environment text: it must have zero sampled/action mask and no fabricated likelihood. If no subsequent root request contains an observation, it is generated-but-not-physically-exposed, not model uptake. Content equality alone without physical node/native prefix support is insufficient.

## Source maps, delivery and ledger trust

The JSONL ledger/config is runtime/root-writable, not independent evidence. Treat every initialized/child_result/delivered/root_observation item as a claim to corroborate, preserving event index and raw bytes/hash. Match exact episode/context/root and child invocation to external request-local role/typed captures and graph calls. A child invocation may have multiple model calls: retain all possible matches, identify a source-consistent terminal return when uniquely supported, and mark ambiguous/unmatched cases null. Never assume one child invocation equals one provider call or choose a convenient matching answer.

Reconstruct source eligibility independently from the actual child's request: exact public ID/text round-trip under the frozen request builder, nonempty unique IDs, exact canonical six-label JSON object, no duplicate keys/missing/unknown IDs or malformed labels. Compare raw child result bytes against actual native terminal content; no permissive parsing or label inference from unstructured prose. Unmatched/custom child requests remain ordinary, retained and excluded from the ledger. No host gold enters eligibility or reconstruction.

Separate produced native child result, ledger staged claim, successful broker-write claim, semantic return link, actual root observation exposure and demonstrated program use. The source commits after `write_frame` serializes/writes and awaits writer.drain(); this is not an application-level caller-read acknowledgement. Native completion/semantic edges plus a corroborated ledger delivery event support a reported broker-flushed result, not adversarial proof of receiving-code consumption. Actual subsequent root physical observation is a separate, stronger visibility check. With missing/contradictory delivery evidence, do not turn all produced maps into delivered coverage; report produced-map and corroborated-delivered-map views separately.

In each view, accumulate source IDs once; equal repeats are idempotent, conflicting labels make an ID permanently unresolved/excluded from every histogram bin. Do not select first/last/majority or repair using gold. Independently compare claimed cumulative summaries at each supported event: unique_seen, resolved, source_records, unqueried, conflicts, partial and six counts. Full resolved coverage requires every source ID and no conflict. Retain duplicate-query counts and unresolved records.

Report three distinct descriptive relationships only when observable: child-derived target count versus host gold; final integer versus complete resolved child-derived target count; final integer versus the specific last physically exposed corroborated summary. A matching count is agreement, not proof of causal reading. Partial coverage, absent summary, invalid map or ambiguity gives null for full-evidence fidelity, not false. At full coverage compute target FP/FN against source labels and report cancellation; wrong non-target class names alone do not invalidate the task's target count. The ledger never had access to those host annotations.

Root overwrite/requery and recovery markers are secondary static diagnostics. Use exact sampled root code and AST parsing only; AST-unparsed is null with its count, not zero absence. A repeated `labels=` assignment or repeated source-ID request does not itself prove lost state, inefficient reasoning or an accumulation error. Do not execute code or build a general interpreter. If final disagrees despite complete returned evidence, state separately what was available to REPL, physically visible to the model and directly evidenced by its program/output.

## Cost and publication

Count every captured pre-call request-hook attempt, returned/error provider attempt and graph-committed root/child call, including incomplete episodes; reconcile unique IDs rather than add duplicate capture layers. Decode JSON-string wire response bodies for logical prompt/completion and cached-token fields. If inherited HTTP retries overwrite a hook's single saved wire field or lack distinct records, report observable attempt counts/lower bounds and missing retry cost rather than assert exact all-HTTP totals. Unknown cache fields stay null, never zero. Keep logical input, known cached input, sampled action tokens, returned completion tokens and failed/unreturned attempts separate. Parent/service/collection/episode/runtime wall times are overlapping dimensions, not additive GPU seconds. Caps1200collection/1650work/1770owned/1800outer; no study retry. Lifecycle success alone is not inference success.

After MAIN terminal notification only: read each immutable raw/source item once, emit audit rows/pair and context summaries, concise REPORT/METRICS/SOURCES and a seal. Authenticate saved source hashes once, perform no repeated global hashing, and never retain GPU lifecycle locks for analysis. If observability is insufficient, publish measured feasibility/nulls and the smallest optional instrumentation need; do not redesign this accepted experiment or silently widen a parser after seeing scores. No live polling or future-outcome reads while preparing the parser.
