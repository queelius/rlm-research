# Bounded-refill RL: independent terminal outcome method

September9,2026. **Post-launch, before this auditor reads new outcomes; not prelaunch preregistration.** MAIN disclosed only that one checkpoint/window1 completed and window2 was collecting. No rewards, answers, losses or readout outcomes were supplied. Scientific READY `c5b1247d021a9d6f1c53a4f3aee7caa8352887bc009e57866b617c946e6d3ce7`, CAMPAIGN `21a515ca1a099fb96fe1c02174e60d2c88466dd5c4738f584155b9367b1e4dec`; actual operation `operations/2026-09-09-after-fresh96-bounded-rl`, parent4122816/start1077225991. Science `sidecars/root-bounded-refill-rl-v1/outputs/attempt-001`.

## Independence, gate and fixed comparison

Author of this audit did not author the new bounded-refill experiment, but contributed earlier root/SFT/native machinery, independently audited earlier adaptive RL and reviewed this final integration. Reuse the sealed [critical-source review](../root-bounded-refill-rl-review-2026-09-09/REPORT.md) and authenticated independent endpoint/native/Adam helpers, not implementer aggregate scores as authority. Prior results and the high-LR SFT result are known; they do not change the fixed low-LR start, endpoint, candidate order or method.

Freeze method/input pins now. Wait for MAIN's terminal trigger before consuming raw rewards/answers/checkpoints; no private polling, GPU/network/model calls, services/signals or source edits. After trigger, process each completed immutable stage and checkpoint once and cache its hashes/calculations. No repeated tensor scans. A STOP is a terminal result, not permission to resume/reroll. Preserve partial raw evidence and later unrun coordinates without making policy-negative labels from availability loss.

Primary endpoint: all24 paired same-task/same-seed before/after strict final answers, initial fixed low-LR success-SFT8 root66cce400… with fresh RL Adam0, fixed typed c32de child versus the **last actual committed RL policy0–4**, never best validation or a promised fixed4. Readout seeds981316501–524 are fresh but the eight context clusters/compositions are exposed. All48 planned slots remain. Report paired wins/losses/ties/unknown, net bounds, per-context and per-stratum (validation/query/length) results, with24 trajectories nested in eight contexts rather than independent datasets. Same-input trajectories are not deterministic replay. No historical different-seed pooling or LR-only causal attribution.

## True task endpoints versus training admission

Independently reconstruct from raw complete root replies and host-only gold. Exact ASCII `Answer: N` with surrounding whitespace allowed; all complete observable wrong, empty or malformed replies score0. Incomplete/missing/unobservable replies remain NULL. Preserve an observable strict endpoint even when provider/runtime/native integrity separately prevents training admission. Explicitly reproduce both values and reasons. Recovered physical errors do not automatically erase an observed task answer; admitted reward can still be NULL under the unchanged exporter.

Do not use `strict_successes` from export as task accuracy: it counts admitted rewards. Crosscheck independent endpoints against raw `strict_reward`/endpoint fields and admitted counts against export separately. A native-integrity exclusion is not relabelled successful training or a sampled negative. Strict syntax, numerical wrongness, completed empty, model-call availability and provenance quarantine remain distinct.

## Candidate support, consumed windows and no-op

Freeze all128 candidates: four windows, four ordered context×query groups of eight per window. Window groups1/2 are mandatory and length/family balanced; consume group3/4 only while fewer than two admitted-mixed groups exist, stopping at two mixed or four complete groups. One immutable full-window policy/generation covers the consumed prefix; no cross-policy concatenation or suffix carryover. Validate exact2/3/4-group legal prefixes and all actual attempted/cancelled/unrun coordinates. Incomplete subgroups cannot become a complete variable-cardinality update.

For each group report raw endpoint success/failure/NULL, admitted success/failure/NULL and exclusion reasons, actual mixedness, root tokens and whether selected. Recompute advantages within prompt among admitted rows and require every admitted episode from every mixed consumed group; homogeneous groups do not train. A lone mixed group at the four-group bound is eligible; no minimum admitted size beyond valid nonempty mixed support. Compare first-two support to consumed-prefix support descriptively, not as a randomized treatment or proof a policy learned to refill.

Distinguish four candidate-window opportunities from actual optimizer steps0–4. A true no-op advances the window cursor without launching training, changing adapter/Adam/RNG bytes, decaying moments or creating a fake checkpoint. Authenticate old/new policy identity and no trainer artifacts for that branch. Initial step0 has no persisted RL moments/RNG: report fresh initialization semantics rather than inventing initial RNG files. Later no-ops preserve the last saved members exactly; inference sampling state is not the trainer RNG.

Unconsumed suffixes, between-window time-cutoff candidates, within-stage cancelled attempts and prevented/unrun attempts are separate. Keep the full128-candidate ledger even if only a prefix was sampled. Do not assign sampled success to unattempted candidates or turn unsampled candidates into failed policy outcomes.

## Actual update and native likelihood proof

Authenticate each saved state/RESULT/INPUTS/generation/group/union/export/correction capture and adapter/config/Adam/RNG closure once. Step numbers follow contiguous actual Adam increments, while candidate_window can skip past no-op windows. Initial previous policy must be exact66cce400…/fresh Adam; later previous-policy bytes must match the preceding committed policy. A saved state is the commit even if RESULT is absent; record that distinction, do not apply the update again. Final selection must equal the last committed policy, with STOP/final/current-window distinctions explicit.

Inspect actual CPU-loaded Adam steps, finite moments, adapter shapes/dtypes, finite nonzero gradients/deltas and, where practical, recompute adapter delta from consecutive saved tensors. Child adapter is never loaded as trainable. Validate one unchanged same-forward token-TIS numerical path: actual processed native behavior logprobs, detached current proximal probabilities only for one full-batch step, clipping/guard diagnostics and equal episode→root turn→current-action token weighting. Coefficient mass is not gradient share. No operator/CPU fixture traces become training likelihoods.

For admitted native rows reconstruct WireTrace root/child ancestry, exact causal prompt/action token IDs, compact behavior logprobs, labels/loss masks, physical request/response token equality and seed. Prior/root/child/tool observation context remains masked; credited turns are depth0, unconstrained, current root alias/checkpoint. Child calls retain fixed c32de and exact request-local typed schema; no child action credit. Preserve failures in a discrepancy ledger instead of fixing scientific exports.

## Helpers, costs and operational clock

Inspect native-authenticated helper invocations and complete duplicate-free canonical ID→label maps; request coverage, valid-map coverage and correct-map label coverage are distinct. Distinguish extending beyond the literal first-four helper example from covering all relevant records. Where full returned maps permit a count diagnosis, distinguish correct map→wrong count from wrong/incomplete child maps, without executing sampled code or making root-consumption claims solely from writable logs. Examples and map/count diagnostics are descriptive, not new primary endpoints.

Index each stage's role/typed audit paths once. Count all retained physical attempts separately by root/child, returned/failed/request-only, with actual alias/HTTP/finish status and raw native prompt/completion/cache usage. Request-only or missing-cache counters remain unknown, not zero. Report logical token totals, cached/uncached known costs and unknown counts, call-duration sums separately from elapsed wall. Paired cost contrasts on jointly observable and jointly correct endpoints are descriptive; adaptation changes child trajectories, so matching first prompts do not imply paired child calls.

Check operation/owned start/end/EXIT, service and trainer ownership release markers, fixed3480 work/3600 owned/3630 outer, training cutoff2820 and normal-path660 final reserve. Actual failure can prevent final readout despite reserve. Report wait separately from experiment work; do not infer GPU inference efficiency from total wall. Never release or signal processes under this audit. Fresh96 is a separately accepted successor and not part of this RL endpoint.

## Deliverables and interpretation

After terminal publish early paired endpoint/admission/support counts promptly, then structured stage/window/checkpoint/128-candidate/48-readout ledgers, role-cost and discrepancy summaries, readable REPORT and exact source/content hashes. Cache completed work and reuse authenticated source proofs. Report unresolved provenance or missing evidence explicitly. Original scientific files, frozen critical review and this METHOD remain unchanged; any parser adaptation is additive.

The experiment asks whether this bounded sampling-plus-root-RL package moves an already trained policy under a fixed child, not whether refill is intrinsically superior to fixed sampling. No-op endpoint movement cannot be training. Homogeneous sampled successes do not establish mastery; homogeneous failures do not justify reward fabrication. Small exposed context clusters, one training seed, selected successful SFT warm start, availability exclusions and unequal realized optimizer/support costs limit mechanism and generalization claims.
