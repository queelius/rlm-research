# Terminal loop adaptation

METHOD remains unchanged and explicitly postlaunch/preoutcome. `terminal_audit.py` imports the pinned independent earlier adaptive audit (`a27d5770…`) as a private read-only helper, reassigning only its analysis/study/run roots. Its complete-stage parser retains exact native WireTrace/action/probability/role/map checks. This is reuse of previously independent reconstruction, not an import of implementer aggregate scores. The original helper/source is untouched.

New logic handles four ordered candidate windows separately from actual0–4 optimizer steps, all128 candidate slots and24 paired readouts. It independently derives support from admitted binary rewards, checks legal consumed prefixes and selected group membership/advantages, and audits actual saved optimizer/adapter/member/native-action identity. A no-op cannot have a trainer/checkpoint and keeps the old policy bytes. Per-window partial stages are retained without inventing an export or admitted reward; raw observable endpoints survive while native admission remains unavailable. At terminal, report whether a checkpoint state committed even if later metadata/RESULT failed.

Three new fixtures were written first and failed because implementation was absent, then passed: admitted rather than raw-success support, legal lone-mixed/no-op stopping versus overrun, and endpoint pairs with training exclusions/NULL bounds. Two unchanged inherited endpoint/cache fixtures passed during method freeze. No new model or runtime fixture is required; these are synthetic parser/cursor checks, not scientific likelihoods.

The one-pass CPU invocation after MAIN terminal trigger is:

```
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 /project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/analyses/root-bounded-refill-rl-live-2026-09-09/terminal_audit.py --main-terminal-trigger 'MAIN terminal confirmation/time'
```

It requires accepted operation EXIT and never polls. Complete stage/checkpoint results are cached inside this analysis namespace. Subsequent manual release/cost/consumption interpretation uses the same cached immutable evidence and is explicitly pending in machine metrics until completed. Any unforeseen parser/schema issue is recorded additively; original METHOD, science, saved outputs and this readiness seal remain untouched.
