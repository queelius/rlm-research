# Independent exact-requested-tag32 outcome audit

Freeze before reading any new outcome content. Assigned science: leaf-mnli-exact-tag-correspondence-v1/outputs/attempt-001, READY d6363ff660ec71df028cc91ce1e7f48d5a2e43e25cda63f5924d74de653c2114. The panel is8 exposed contexts ×2 paired seeds ×matching/shift17 =32 endpoints, not32 distinct contexts;48 items/endpoint and768 planned items/arm.

Authorship disclosure: this auditor did not author the exact-tag or shifted-tag successor. I authored the original MNLI correspondence sidecar/data preparation and contributed common an27/native runtime plumbing. Independence applies to this successor's outcome authentication/rescoring, not independent creation of its reused data or ancestry. Prior source-ID/MNLI/shifted evidence is known; no new response, summary, score or trace has been read at this method freeze. MAIN may have reviewed other studies; no claim of globally outcome-blind research.

## Frozen checks

1. Pin full assigned READY closure and exact32 plan; independently rebuild seeds/order/shift17, public visible records, exact tag schema and all-label freedom. Verify wire bodies/no tools and actual tokenizer prefixes; compare messages/prefixes with shifted predecessor only as source-level identity checks. Count eight contexts/128 premise groups/384 pairs and data-only differing-gold positions. No new outcome-based source selection.
2. After actual terminal and release, pin all attempt files before reading outcomes. Resolve all32 coordinates even when request/response/result files are absent. Verify service descriptor/config/live model cards: released cdbee/no LoRA, BF16,8192 context,vLLM0.28.0,prefix cache off,four sequences;1200 outer/1080work/1170owned. Retain missing owner/failed release as explicit operational nonclosure; do not invent receipts or rerun.
3. Authenticate each actual dispatched ordered body/hash and expected native prefix against the frozen request, including exact public tag const grammar. Verify model alias, unique assistant branch, complete prompt/completion token arrays, decoded text/tool envelope, role/finish and integer token usage. No tools were advertised; an authenticated wrong-route completion is observed strict0 without executing it. Missing/failed transport or inconsistent native identity staysNULL. finish_reason=length is an observed capped response: score its actual authenticated content normally and flag it, never automatically relabel infrastructure.
4. Independently parse the entire JSON response with duplicate-key/nonfinite rejection, exactly48 items, exactly tag then label fields, canonical string labels and tags. Full contract requires exact requested tag at every displayed position. Primary correct-item count uses displayed premise/hypothesis gold only when the whole contract passes; otherwise any authenticated malformed response strict0. No reorder, partial-array rescue, tag replacement, prose extraction, original-ID hints or semantic repair. Separately report shape-only displayed accuracy, per-position tag fidelity and whole-batch validity.
5. For shift17 only, on the precomputed positions where displayed and requested-named gold differ, count predictions matching displayed, named, or the third label. Gate this diagnostic on whole-contract validity, with eligible endpoints/items and planned differing-gold denominator both explicit. Zero eligible means undefined, not0 accuracy. Current source is expected261 distinct positions/522 across seeds; independently recount without selecting them from outcomes. Ungated old305/522 is historical motivation only, not a current primary or causal control.
6. Report exact per-arm counts, NULL bounds and conditional summaries; paired16 seed blocks nested within8 contexts. Average the two-seed contrast within context, then report eight context contrasts/mean; no item/seed independence or confirmatory p-value. Separate descriptive genres, availability and operational zero-imputation sensitivity; native primary is not silently zero-imputed. Inspect mechanism patterns by the complete predeclared differing-gold partition, not selected favorable examples.
7. Count physical dispatch receipts separately from returned completions and authenticated endpoints. Recount input/output/cache/uncached usage with unknown fields retained, including rejected/unauthenticated responses. Record length caps, HTTP/transport errors, timings, startup/collection/release and source seals. No new acquisition work is charged; reused data/old evaluations are provenance, not current model calls. Billing/FLOPs remain unknown.

Independent parser may reuse pinned prior independent whole-output/native-authentication primitives, but never imports the experimental scorer. Compare every independent score/admission with retained producer results and report all disagreements without modifying them. Fresh synthetic regressions cover exact displayed/named/third labels, invalid tag/order/duplicate fields, missing response, authenticated tool branch and token/text mismatch. Freeze parser/test hashes before any outcomes. Only this exact attempt may be monitored; no GPU, service, lock, launcher, queue or scientific-source writes.

Read depth at freeze: full new DESIGN/PLAN, protocol/scoring/collector/study/owner/service wrapper/prepare and science tests; full native-authentication dependency and prior independent MNLI/shifted audit implementations. Source-level only; no new outcomes read.
