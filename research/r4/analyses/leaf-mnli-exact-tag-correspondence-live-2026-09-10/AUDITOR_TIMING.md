# Auditor role and timing amendment

The existing `METHOD.md` and `METHOD_READY.json` were frozen by runtime_port before generation and
remain unchanged. This outcome auditor took over after all responses existed. Before writing the
independent parser, MAIN disclosed only the producer aggregate: all 32 endpoints available and valid,
matching 622/768, shift17 280/768, and requested-named 373/522 on differing-gold positions. Thus this
implementation is aggregate-aware and post-generation; it is not a pristine outcome-blind parser.
Metrics and denominators remain exactly those in the pre-generation method.

The auditor authored the generic shifted predecessor and the original MNLI correspondence data
preparation, but did not author this exact-tag implementation. Independence here means a separate
native/source/score recount without importing the experimental scorer, not independent ancestry or
ignorance of the aggregate result.
