# Outcome-blind short32/16 data freeze

Final source: `/project/alex_phd/runs/rlm-research-r4/sidecars/openai-mrcr-short-root-data-v1`.

Use **DATA_READY_V2.json** SHA256 `55f10b7f43063a5752927d48d13d6c3fc25c91facb67cb1434f7c04def73c95c`, identity `82e1d9507b3e9bd3e34ffdb49b9e7a4d99dc3eb95bca6d0172a008ff07857a21` (121 source/input pins). `verify.py` passed in fresh CUDA-hidden native Python. This is data-only readiness, not GPU or training admission.

Exactly the approved namespace `openai-mrcr-short-root-v1-20260912|FULL_ROW_SHA256` ranks89 singleton core components within the101≤8192-token short candidates. First32 are training, next16 heldout. Ranking was committed before selected-gold/demo/query audits; no outputs, correctness, topic or target content chose examples. All are from the original-date cohort. Their exact core pairs share no material with any other row in the101-row candidate pool. Unused full800 rows can still contain matching material; they are not optimization inputs here. Semantic/base-pretraining independence is not asserted.

The extra labeling-link check passed on ALL800 rows: each final question exactly matches the template reconstructed from the desired user request, one-indexed first/second occurrence and marker. No parse unknowns, mismatches or missing markers. None of the selected48 unprefixed gold answers occurs in any of the9 demonstration variants under exact or whitespace-normalized containment. All48 selected conversations retain one shared demonstration variant; it is not counted as disjoint core material.

`MODEL_INPUTS_V2.json` points to each original prompt JSON-string byte sequence and exact final query. External context is the original JSON document, not an invented transcript format. Future model metadata is limited to type/size: no index, needle, parser recipe, answer-length or extraction hint. Public provenance includes IDs, source coordinates and hashes. `host/HOST_GOLD.json` is0600 inside a0700 directory; the host-only answer/marker/index are not added to model prompts. The original conversations necessarily contain assistant answers as context.

CPU tokenizer counts use cached Qwen3-4B-Instruct-2507 at revision `cdbee75f17c01a7cc42f958dc650907174af0554`: original-chat input lengths3431–7865 train,3488–7346 heldout; raw external JSON token lengths3979–9007 train,3969–8542 heldout; final queries31–39 tokens. These exclude the future harness/environment framing and do not admit an8192-token root budget. External-file length is not neural prompt length.

V1's source-chat length field incorrectly counted the two keys in Transformers5's default `BatchEncoding`. The defect was observed before handoff. The additive V2 receipt checks actual token-ID equality between default `input_ids` and explicit `return_dict=False` on all48 examples, corrects only the length metadata, and retains the old artifacts visibly. Ranking, prompt/query bytes and protected gold never changed. Consumers must use `MODEL_INPUTS_V2.json` and `MANIFEST_V2.json`, not V1's length fields.

`official_score.py` preserves the pinned OpenAI card's `startswith` guard, single `removeprefix`, and `SequenceMatcher` ratio without whitespace normalization. It is not the DeepMind last-marker scorer. The pure scorer fixture and all48 source-byte/query/gold links passed. No original dataset, existing DeepMind five-context freeze, model weights or live owners were changed. No model query or optimizer step occurred.

## Proposed next experiment, not implemented/admitted

Question: does one root-only update on genuinely different short conversation cores improve procedure transfer to16 other cores, beyond the previous shared-context calibration? Start fresh Qwen3-4B with no research adapter, collect32×G4 complete trajectories on training only, use the OpenAI official continuous score, and compare the unchanged starting root with the fixed updated endpoint on16 paired heldout contexts. Keep original JSON/query inputs, no helpful parser instructions, and no heldout checkpoint/parameter selection. Future cross-source transfer to the untouched DeepMind freeze is a separate secondary question.

Use the verified actual V6 Python/native route as a runtime seam, but disable child delegation for the root-only question. Explicitly scope the six-turn/token limits before launch; changing representation and available children means V6 timings are a calibration proxy, not a guarantee. Preserve complete per-turn prompt/tool-observation/native-action traces, sampler settings and chosen-token evidence. An actual fresh-policy likelihood/importance replay qualification on those root trajectories is required before any update; old unqualified processed logprobs are not acceptable. Runtime failures are not silently scored or dropped; zero-reward successful trajectories are legitimate under the official scorer.

V6 had no terminal receipt when this proposal was written, so a numeric training cap cannot yet be evidence-based. Once qualified V6 costs exist, compute a prospective collection budget from128 times its observed valid-episode p95 wall time (divide by concurrency only if the same concurrent path is demonstrated), plus measured service startup/release. Apply1.5× headroom, add the independently qualified bounded root-HF update and artifact budget, and round the owner cap up to a minute; external timeout is owner+100s. For the32 physical baseline/updated heldout episodes use the same rule with32 instead of128. MAIN must review those numeric caps and the root-likelihood seam before implementation/admission; do not silently reduce the frozen32/16 or inflate the cap after seeing outcomes.
