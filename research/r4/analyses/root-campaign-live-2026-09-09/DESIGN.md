# Bounded campaign live analyzer

Read only root-rlvr-campaign-v1/outputs/attempt-v2-001 through round01/02 and validation00/02, or an earlier STOP/interruption. No launch/cleanup authority or model/network requests. Snapshot existing immutable JSON records and completed export/checkpoint identities into additive timestamped analyses; ignore pending files and changing logs. Missing coordinates are pending while active, censored/unrun at a recorded stop, never policy negatives. Completed wrong outcomes remain0 even with recovered provider errors. Export-admitted rewards/mixed groups are authoritative for update eligibility; raw native reward candidates are separately labelled provisional until export.

Report actual optimizer.pt step values on CPU, checkpoint/optimizer/RNG/source/input chain and parameter delta vs prior root, selected root-only action/mask totals with zero child/observation credit. Compare fixed8 validation00/02 by task+seed, identify first-root physical-prompt/sampling equality when audited, but do not assume determinism. Count role-audited root/child calls and tokens, terminal/tool/JSON/length/provider-error patterns and recursive usage; usage alone does not establish all-question coverage.

Three focused tests protect recovered-error admission, incomplete/censored handling and homogeneous/mixed group accounting. Prepare an initial pending snapshot immediately; parent can invoke snapshot again at first2+validation2 or STOP. Prior pilot2/8→4/8 unchanged→2/8 post provides replay-noise context only: different campaign seeds prevent pooling or treating it as a matched campaign baseline. Do not infer learning from a single tiny noisy change or interpret first two updates as an adequate campaign.
