---
status: prospective_pre_outcome
date: 2026-09-10
planned_calls: 40
planned_episodes: 8
---

# Native audit method

After exact terminal relay, verify READY, OWNER/EXIT release, all 40 planned coordinates, request
bodies, model/seeds, native token decoding, provider IDs, usage, and strict per-user schemas. Retain
all eight episodes. Any missing/unavailable required chunk makes its episode NULL; any authenticated
malformed chunk makes it observed-invalid. Strict is NULL for unavailable, zero for observed-invalid,
and computed only for a valid complete merged episode; absolute error is undefined in both failure
states.

Independently restrict to exact public `spec.users`, OR chunk A flags, add chunk B sums, and sum B only
for globally A-qualified users. Compare with the exact sealed full-label ceiling reducer result on
the same episode/chunk/seed inventory. Gold labels and answers enter only after native outcomes and
merges are fixed, to score exact J1, absolute error, qualifying-user error, and per-user B-sum error.

Promote only with at least 7/8 valid, 6/8 exact, at least four more exact than sealed label maps,
lower absolute error in at least 6/8 paired episodes, and no material availability/token-cost
disadvantage. Retire at no more than 2/8 exact or failure to lower error in at least 4/8. More than one
invalid/unavailable episode revises the interface. Count 40 new calls and 40 shared historical control
calls once; do not describe them as independent replications. No sampled code is executed.

