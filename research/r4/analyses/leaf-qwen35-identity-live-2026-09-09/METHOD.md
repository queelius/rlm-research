# Released Qwen identity144: independent audit method

Sealed before reading any outcomes from this study. MAIN assigned this audit while the accepted operation waited behind adaptive RLVR; startup/GPU execution of Qwen3.5 was not yet qualified. Scientific schedule and source were already frozen before this method. This auditor did not author this study's data/model/service/scorer; the auditor has contributed to shared upstream research harnesses and other sidecars. Independence applies to raw outcome reconstruction, not to an independently implemented inference system.

## Pins, population, and endpoint

Scientific source `sidecars/leaf-qwen35-identity-v1`; READY SHA256 `c62c85a4bd18fc81086c6f6883f7a608083175401b0fcf597643ba736d4ee979`; SPEC `6d54cdea8a9698cca8d9dc72e9eb8684a20521d1c0c0d92b0b49c806177a5f64`. Operation `operations/2026-09-09-after-adaptive-rlvr-qwen144`, assigned waiter PID3814686/start1075976013. Authenticate the actual operation/attempt records at terminal rather than assuming that initial waiter identity identifies a later inference process.

Exactly144 planned distinct first-response calls: two released model checkpoints ×12 exposed contexts ×two seeds ×three representations. Model order is Qwen3 then Qwen3.5,72 calls/model. Contexts: four TREC, four SST-2, four AG News,64 labels/call; task/model/arm cell8 calls/512 planned labels. Primary matching-source tag minus constant-tag strict displayed-position classification, per task/model with two seed contrasts averaged within each of four context clusters. Plain labels and matching-minus-plain are secondary. No new selection, adapter, training, semantic realignment, historical outcome pooling, or new holdout claim.

Exact checkpoints: Qwen3-4B-Instruct-2507 revision `cdbee75f17c01a7cc42f958dc650907174af0554`, alias `Qwen3-4B-Instruct-2507-no-research-adapter`; Qwen3.5-4B revision `851bf6e806efd8d0a36b00ddf55e13ccb7b8cd0a`, alias `Qwen3.5-4B-no-research-adapter`. Verify revision fields, model configs, tokenizer/template files, acquisition/license manifests, shard index, recorded full shard hashes and unchanged size/mtime/inode identities. Cache those closures once; do not rehash all multi-GB tensors per phase or per call. Explicitly label the bounded distinction: independently hashed small provenance/source files and immutable stat recheck against pinned prior full-shard-hash observations, not a fresh independent byte scan of every weight shard.

## Terminal gate and availability

Do not open raw calls, analysis.json, inference text, or model outcomes until MAIN terminal trigger or a read-only terminal-marker watcher establishes owned TERMINAL plus operation EXIT, all attempted model-stage FINISH and authenticated owned service-release markers. A failed startup can be the terminal research outcome; do not wait for a nonexistent completed72-call stage. Final-stage completeness, errors, cap violations, return code and cleanup are separate. No live process signaling, model contact, GPU inspection/control, source/output edits, retries, or replacement episodes.

Each planned coordinate remains in the ledger. Completed model response malformed/empty/tool-call-instead-of-array contributes strict0 with no aligned labels. HTTP/provider error, cancellation, unrun, failed startup, or unverifiable provenance is NULL, never0. Preserve a raw-observable endpoint before any provenance quarantine, then report trusted/graph-free component observability separately if there is a discrepancy. Primary paired difference is NULL if either arm is unavailable; jointly-valid semantic contrast is separate. All8 planned pairs/task/model remain visible; no omission-based reweighting. Per-context two-seed mean is primary only when both contrasts exist; incomplete contexts retain NULL plus available descriptive evidence.

Qwen3.5 startup may fail for unsupported GPU/kernel/runtime configuration despite CPU grammar/template qualification. Record exact root error, launch/preflight state, attempted requests and costs; all corresponding unrun coordinates remain NULL. If Qwen3 first stage fails and Qwen3.5 is consequently not attempted, distinguish blocked-by-stop from Qwen3.5 incompatibility. A technical startup failure is not negative classification evidence about that checkpoint. Never install, switch kernel/dtype/model, alter the image/config, or rerun under this audit.

## Independent raw parser and position scoring

Recompute from raw response body, not stored score. Reject duplicate keys/nonfinite JSON; require a JSON array of exactly64 items. Plain items must be canonical-label strings. Matching/constant items must be objects with exactly tag/label keys, exact expected source ID at that displayed slot or literal `p0000`, and a canonical-label string. No whitespace/code-fence/explanatory-text trimming, extraction, partial-prefix salvage, offset search, count repair, or source-ID realignment.

Important frozen endpoint distinction: this study's primary schema scorer checks the object key SET, not insertion order; `key_order_valid` is a separate diagnostic. Preserve that declared primary rather than retroactively tightening it. Independently test and report full actual `tag,label` key order at every object and exact assigned grammar conformance; a reversed but otherwise valid full object remains primary-valid with an order violation diagnostic. Do not infer full schema validity from an order field that can describe only a prefix. A separate strict-wire-order diagnostic may show additional protocol failures but never replaces the frozen primary.

Correctness means label equals host gold for the actual displayed record. Reconstruct each public input map's ordered IDs/text, source group/line/position/permutation lineage,64-record gold mapping and exact canonical vocabulary from bound source manifests. Source selection is the prior exposed sparse contexts; four clusters/task, repeated across arms/seeds/models, not9,216 independent samples. Record source split/license and public/pretraining exposure limitations without strengthening inherited claims.

Report per-call observed/schema/ordering/semantic status, first failure and raw cardinality, strict correct/64, fully correct array, class-count L1 only with a complete valid label vector, first-item and quartile/early-late position descriptives, class confusion and repeated-label count caveat. Preserve every real failure/null denominator. Focused independent parser tests precede current outcomes and include valid shape, late wrong tag, duplicate keys, wrong cardinality/noncanonical labels, reversed-key primary-versus-order distinction, observed empty, and unavailable NULL.

## Request and physical native identity

Independently authenticate frozen coordinate/request canonical and insertion-order hashes; join actual wire capture bytes to each coordinate. Verify144 planned uniqueness,72/model, exact seed pair981302011/981302021 and declared six-order rotation, source permutation0, aliases, all public input bytes and gold-host separation. Cross-model body equality is expected after removing alias, not cross-model token-vector equality. Same model/representation and corresponding source/seed should follow the frozen rendered prompt reference; arm wording/output schema intentionally differs.

At terminal verify actual service BINDING, inference config, SERVER_START/READY or FAILURE, endpoint descriptor, live version/model preflight snapshots, and their file hashes. Credential values may be read solely for equality within a local process if unavoidable but are never printed or persisted by the audit. No research adapter/LoRA/speculation; BF16/eager, max length8192, four sequences, generation_config=vllm, prefix cache disabled, no research FP32-head/router patches. Qwen3.5 alone has language_model_only and mamba_cache_mode=align with qwen3 reasoning/qwen3_coder tool parser. These are declared family/runtime differences, not evidence that model-family naming causes any performance difference.

All requests use temperature0.5, top_p1, top_k−1, min_p0, repetition_penalty1, presence/frequency0, max_tokens3072, explicit enable_thinking=False. Verify returned full prompt-token arrays against frozen typed-renderer vectors and lengths against raw usage, output-token vector existence/length, actual returned model alias and reasoning fields. Qwen3.5's native prompt ends with the declared empty-think block; Qwen3's ends with its native assistant prefix. Record unexpected reasoning, tool responses, truncation, missing tokens or physical mismatch, not silently treat them as intended non-thinking samples. Ordinary tools are described but not executed; this is the qualified `/v1/chat/completions` component route, not recursive RLM behavior or a generated root program.

## Costs, temporal comparison, and caps

Read raw usage rather than relying on normalized fields. If a raw body is JSON encoded inside a string, parse it before extracting prompt_tokens_details.cached_tokens. For every physical attempted request retain logical prompt, cached, uncached, completion, total consistency, known/missing fields, duration, and stop reason. Audit the actual prefix-cache-off configuration AND returned cached-token counters; nonzero/missing counters are reported explicitly, not assumed0 because a flag says false. Confirm physical POST count/no replacement retries from available wire and access records, separate preflight/adapter-management traffic if any appears.

Report total actual work, planned-null counts and comparable jointly observed/valid pair costs separately. Startups:300s/model; collections900s/model; shared work2400s; owned2640s inclusive with120s cleanup/service; outer2670s. Check actual absolute deadlines and each stage release. Sum of concurrent call durations is not elapsed wall time. Models execute sequentially, use different native templates/tokenizers and architecture/runtime settings; do not claim architecture efficiency from sequential wall/token differences. Equal source text, parameter-count marketing, and checkpoint family names do not establish causal attribution or equal realized compute.

## Artifacts and interpretation

Write one bounded raw reconstruction pass after terminal, with cached source authentication, ROW_LEDGER/METRICS/SOURCES and a readable REPORT plus content-hash seal, only in this analysis namespace. Compare stored projections only after independently computing endpoints; preserve discrepancies. Send MAIN early primary availability/accuracy/cost and startup outcome, then complete context/seed/order/distance/source details. No source scorer imports for independent scoring. Future audit-script fixes are logged and never alter raw scientific outputs.

Interpret any within-model matching advantage as this released checkpoint's response to the whole supplied representation/grammar package on exposed tasks. Report exploratory cross-model difference-in-differences descriptively, without claiming architecture, training data or family causes. Unsupported startup limits feasibility conclusions; small exposed clusters and shared seeds limit generalization. Neither valid grammar, correct tag nor histogram agreement establishes semantic correspondence or end-to-end RLM improvement.
