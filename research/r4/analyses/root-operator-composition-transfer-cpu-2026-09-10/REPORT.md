# Fresh composition96: bounded independent CPU review

**No material experimental-validity defect found in the sealed candidate.** MAIN acceptance/launch remains separate. Science READY SHA `9cfd1febd1101ece4fd1f69d4a1e64909a7822f83ea0299cbc78d8e2284398b1`, identity `ad5529320691c3e05133209e8da58620ca256d700ad902c949f742f9b04db4ef`, at `sidecars/root-operator-composition-transfer-v1`, relative to `/project/alex_phd/runs/rlm-research-r4`.

I did not author this new protocol, source allocation, or owner. I did author shared ancestor operator/native/runtime components, so this is study-level review, not independent implementation lineage. No source changes, GPU/model execution, scientific outcome reads, or sampled-code execution occurred. Authored CPU tool programs ran only in the previously qualified native fixture.

## Independently checked

- **Raw truth and six operators:** loaded the authenticated TREC source/partition independently; all 128 selected record texts, source lines, normalized groups, labels and public weights match. Independently enumerated all 48 host answers and compared them with both stored truth and the actual `ct_protocol.answer`; zero disagreements. Thirty additional literal edge assertions cover no A/no B, sums exactly5 versus6, tied maxima, repeated A users and repeated B records counted once. C1 is strictly >5; C2 returns a scalar maximum including zero users; C3 tests A-user eligibility but sums B weights exactly once per qualifying record. All six scopes include all users; A/B follow the predeclared four-category cycle and differ.
- **Allocation/exposure:** reproduced 2,192 old eligible groups minus the refreshed named exclusions =2,128, then exactly the label-blind hash-ranked 128. Eight disjoint16-record clusters, four records per user, four public fields and weights1–7. Verified every source/seed receipt hash. Refresh names35 root input manifests and47 seed-plan manifests, with139 source pins including the older closure. The 64 recent dose groups are excluded. Our raw-source and public-ID check found no collision; the additive author receipt also checks all128 IDs and8 native context IDs against3,740/139 named existing IDs. No source or outcome-based reranking occurred. Novelty remains **root-new under the named inventory**, with child-training and prepared-catalog exposure, not global/pretraining novelty.
- **Native/task interface:** reconstructed all48 first native prefixes using the actual composed task path; each matches the frozen IDs. Actual setup produces exactly `records.json`, `context.txt`, `query.txt`, and `batch_contract.py`; no new map, loader, algorithm, hidden task metadata, or forced batching. Records/query bytes match inputs; changing host answer to999999 leaves setup and native prefix unchanged. Largest prefix1076+2048 fits8192. A fresh authored native fixture exercised real root→typed c32→C3 reduction→strict final, and its retained final scorer uses the new operator truth rather than the inherited three-operator formula.
- **Paired model/source contract:** 48 unique task coordinates ×exact two policies =96; identical task coordinates, seeds, order and files within policy pairs; zero teacher probes. The exact root adapters are6=`efe7efc1b7b1ed642d04a1bee0ea4a3d5a6c1ac5b9575f89ad22d3e3ac2518cb`,24=`94022838a64a530e1abc8daf6cd43502d8aec7d549b6bfec10dd4928b0ca9006`; frozen child=`c32de1293c01bba5104eb1f123867649c71dd38e5194661ace4b17dcbbe66ba3`. Real composed owner→service wrapper→intercepted inference/config/descriptor fixture passed, as did collector CLI/alias dispatch. Actual `ct_owner.py verify` returned the sealed identity.
- **Incomplete work/accounting:** own authored regression makes phase1 time out, then retains phase2's completed endpoint alongside one returned and one incomplete physical call; unknown input/output/cache counts remain separate. A second regression raises MAIN cancellation, verifies no later policy launch, retains96 NULL inventory slots, and verifies owned release. The owner scans physical files independently of endpoint snapshots, so a present RESULT does not erase partial-call cost. Ordinary phase failure does not imply later-phase cancellation. No available-only primary or score salvage is introduced.

## Focused verification receipts

`CHECK.json` contains all48 independent truths/native checks and observed source/input hashes; `check.py` is the standalone independent checker. All checked source hashes were unchanged when author READY appeared.

Fresh execution of the selected author protocol/owner/native fixtures: **9 passed in27.53s** (`AUTHOR_TESTS_FRESH.xml`), with two dependency deprecation warnings only. Separate independent interruption tests: **2 passed in0.09s** (`INTERRUPTION_TESTS.xml`). The 30 literal edge assertions and48 raw-truth/native checks are additional assertions, not additional pytest test counts. The author subsequently reports11 total suite tests; this reviewer did not rerun the two added input tests because the independent all-input check already covers their substance. Their source was read.

Actual verify command: `/project/alex_phd/envs/prime-rl-5990b1b/bin/python /project/alex_phd/runs/rlm-research-r4/sidecars/root-operator-composition-transfer-v1/ct_owner.py verify`. It checked1,345 source and3,763 input pins and returned the above identity.

## Interpretation and operational limits to preserve

The fixed order is **sft24 then sft6**. Paired seeds do not eliminate serial-time effects. Eight context clusters, not48 independent tasks/policy; threshold and maximum share per-user accumulation. Primitive/composition means, all six families, nonzero/zero strata and NULL bounds must remain separate. Exactly7/48 question answers are zero: primitive3/24, composition4/24. Best-constant baselines are6/24 for each panel (primitive constants2 or3; composition constant1). All zeros remain, including two threshold zeros; do not infer composition from chance zero finals.

6480 outer /6300 work /6450 owned is implemented as two inclusive3000s phases, each startup≤180/collection≤2730/release≤90, plus300 harvest,150 emergency cleanup and30 outer margin. Stages advance early; reserves do not guarantee48 finishes/policy under long model loops. Ordinary unavailable endpoints remain NULL with explicit operational-zero sensitivity; real model malformed finals are not infrastructure NULL. Source-level cost means physical attempted/returned calls and known/unknown usage, not confirmed GPU work or billing. The READY field `no_new_acquisition=true` should be interpreted only as no new external asset acquisition: free roots can make **new c32 inference acquisitions**, which the ledger counts. This wording ambiguity does not change the actual protocol.

The design's original interim53 child-call motivation is preserved historically and corrected to75 in APPROVAL.md; the review does not use old outcomes to select new data. Native future mechanism claims still require actual observed dataflow and faithful computation, not mentions of grouping/functions or correct scalar alone. A torn artifact at hard kill can still require independent union harvest; this bounded review did not add recovery infrastructure.

Sources read fully: original approved MD/YAML, APPROVAL/PLAN, new protocol/study/prepare/collector/owner, all current tests and collision qualifier; qualified predecessor collector/study/owner and relevant native setup/loss/scoring paths. Review artifacts are external; no author namespace writes were made. `REVIEW_SEAL.json` binds the tested snapshot, frozen candidate closure and review artifacts.
