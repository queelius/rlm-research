# Additive reader correction, after authorized outcomes

The frozen extra final join incorrectly equated the typed transport UUID with the provider `generate-tokens-*` request ID. All 32 claimed native finals failed that additional join, although the prior native-final authentication passed. PRELIMINARY.json is preserved as a rejected audit intermediate, not a scientific zero-availability result. No study artifact, score contract, endpoint selection or original method changes.

The additive reader joins exact native wire request body and full completion token/logprob sequence, requires one unique physical receipt, and preserves the two distinct IDs. It also parses all returned native branches with the pinned renderer. A regression fixture explicitly uses different IDs and verifies that body/token identity succeeds while the old ID comparison fails.
