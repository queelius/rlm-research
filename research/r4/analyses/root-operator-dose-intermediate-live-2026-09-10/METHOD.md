# Independent intermediate-checkpoint outcome audit

Prospective preparation began 2026-09-10 05:41 UTC; exact science outputs/attempt-001 was absent at epoch1789018881.8258512. Final seal occurred after launch because correcting two stale early-review metadata pins delayed closure. No new outcomes read; this is an outcome-blind method, not a prelaunch seal. Exact timing is in METHOD_READY. Auditor reviewed but did not implement the study. Accepted source is root-operator-dose-intermediate-readout-v1 READY_v4 (not superseded V1–V3). MAIN controls launch and authorizes outcome reading by phase/terminal relay; this method never authorizes model, service, lock or queue operations.

Primary inventory: all64 free endpoints, sixteen fixed questions per exact checkpoint6/12/18/24, same paired question seeds. Checkpoint order18,24,12,6 and rotated within-policy dispatch are fixed. Forty-eight exact teacher-first probes (12/policy) are diagnostic only, sampled but never executed and never mixed into task success. Preserve every slot, including unstarted, prepared/attempt-unknown, attempted failure without RESULT, unfinished native episode and available malformed final. Existing dose missingRESULT failures were attempted, not unrun; independently union physical records regardless of endpoint RESULT.

Score only authenticated completed native final using unchanged original scalar contract (`Answer: ` followed by ASCII decimal digits, full string after the existing outer-whitespace strip; no prose/answer extraction). Available malformed/wrong is0, missing or unauthenticated final isNULL. Report strict correct/planned16, available and conditional correct, conservative native-answer bounds, and operational success with bounded no-final failure0 separately. Independently recount source gold from frozen records/labels/users/weights, compare producer without adopting its score. Nonzero13 and zero3 retained; no resampling, hindsight reparsing, retries or best-checkpoint choice.

Twelve context clusters have unequal task weights: four prior dose-new contexts contribute2 questions each; eight exposed original contexts contribute1. Report planned-question means and equal-context means separately, all context signs/paired availability for24−6,12−6,18−6 and descriptive trajectory6→12→18→24. No monotonicity/equivalence claim from sixteen tasks, no record/seed independence, no bootstrap with unknown native answers; show conservative paired bounds. Selection is an exposed, gold-baseline-balanced diagnostic subset, not newly unseen transfer. Familiar operators, prior readout selection and c32 child training exposure remain disclosed.

Authentication: verify exact checkpoint adapter/config and root+c32 bindings against fixed source receipts and actual service descriptors/model directories. Retain actual template, sampling/caps, startup/probe/free/release timing. Probe requests must equal pinned teacher bodies except alias and new seed; every returned token/logprob sequence must agree with raw HTTP body and renderer branch. Full endpoints require completed trace, unique native final physical identity, matching raw tokens/logprobs, typed response and trace reply, root alias and stop/length provenance. Authenticate raw physical response token/logprob integrity even when endpoint RESULT is missing. Record any remaining native rendering uncertainty rather than promote availability; manual branch checks may extend the parser additively without changing this rule.

Ledger: independent union of each exact coordinate's raw physical files (free) and REQUEST/RESPONSE/RESULT (probe), plus failure artifacts. Deduplicate physical identities only when proven same request; retain conflicts. Response proves physical attempt; REQUEST alone proves preparation, not sampled GPU work. Count attempted requests, HTTP returns, successful choice payloads, authenticated completions, known/unknown input/output/cache usage and elapsed separately. V3 counters were not union-total and omitted physical-only taxonomy; V4 corrects those issues, but its ledger is still a producer summary, never the independent authority. No billing inference; no unknown usage imputation or incomplete-coordinate cost dropping. Separate actual service preflight from evaluation requests and reused training/corpus cost.

Mechanisms: actual child request/response acquisition and cardinalities, optional batch width, observed parsed maps, actual program observations and independently identifiable scoped reduction/final concordance. Never infer execution from code/AST mentions alone and never reexecute sampled programs. Teacher-first acquisition syntax is intent, not executed acquisition or end-to-end competence. Inspect all strict successes and salient errors/recoveries; assign reviewed/unknown status to every coordinate. Distinguish faithful computation of semantically wrong child predictions, incomplete map, overwriting, literal/manual answer, invalid final, and policy-induced exhaustion with transport manifestation.

Caps:3300outer/3150work/3270owned, four750-second phases plus150harvest; each180startup/60probe/480free/30release, release bounded by now+30 and phase/owned deadline. Independent elapsed reconstruction does not sum overlapping clocks as GPU kernel time. Outcome pins taken before authorized analysis and reverified at seal; old artifacts remain untouched.

## CPU implementation plan

Inline execution in this new analysis namespace; no Git or subagents. Write authored tests first for strict0/NULL, request-only versus orphan response cost, physical-only attempted taxonomy, malformed native token/logprob identity and unequal context weighting. Observe RED; implement minimal pure parser helpers and narrow reuse of pinned independent dose native-final/program readers; observe GREEN. Freeze method, parser, source/input closure and exact112 inventory before outcomes. After MAIN relay, apply parser and manually authenticate remaining native/dataflow evidence, then seal concise REPORT/FINAL and raw/cost provenance. Tests do not execute sampled code or launch services.
