# Independent prospective audit: new-context MNLI matching/shift/alien48

Frozen before outcome inspection. At 2026-09-10 03:27:29 UTC the exact science directory `sidecars/leaf-mnli-new-context-alien-correspondence-v1/outputs/attempt-001` was absent. Source/design and prior studies were inspected; no new responses or scores were read. Auditor did not author this experiment (the auditor did author the separate operator-dose experiment). This audit is CPU-only and will neither execute sampled code nor modify accepted science.

## Inventory and primary question

Retain all 48 preplanned endpoints: 16 context clusters × matching, shift17, and alien; 48 labels per endpoint, hence 768 planned labels per arm. One paired sampling seed per context. Four genres have four clusters each. Neither individual records nor the three requests constitute independent context replications. Authenticate fixed released Qwen3-4B identity, request body and native prompt/output tokens, grammar, label domains, sampling, request routing, and complete response branch before scoring.

Primary displayed-record semantic accuracy requires the entire exact output contract: one complete 48-row array in displayed order, exact requested tags, tag then label field order, no extra or duplicate keys, labels from the three allowed names. Authenticated malformed or incorrect outputs score zero; unavailable or unauthenticated responses remain NULL, not repaired or reordered. Report strict known correct / 768, availability, native-available accuracy, and worst/best bounds. A separate planned operational success view treats failure to deliver a valid answer as unsuccessful, without changing frozen NULL scores.

Primary comparison is alien minus shift17, with matching minus shift17 and alien minus matching reported separately. An alien–matching difference near zero is not evidence of equivalence; no equivalence margin was preregistered. Report each paired context and genre, paired mean differences, fixed-seed (981623001) 10,000-replicate genre-stratified context bootstrap percentile intervals, and exact paired sign-flip sensitivity when all relevant endpoints are available. Intervals are exploratory with only 16 selected clusters and four within genre. Missingness is described causally where traces permit, not assumed random; report paired worst/best difference bounds when NULLs occur.

## Separate mechanism diagnostics

Report complete shape, exact tag fidelity, duplicate/missing/extra/order errors, whole-array perfection, and positional semantic accuracy conditional on shape. None rescues the primary. For shift17, only full-contract-valid endpoints enter the predeclared positions where displayed-record and requested-tag-named-record gold differ; count displayed/named/third-label outcomes with explicit eligible denominator. Alien aliases have no named-record semantic oracle. Report decoder-forced tag success as an interface property, not learned copying.

## Source and physical closure

Independently recheck actual selected MNLI rows and dataset label mapping, complete premise/hypothesis text, public text-derived IDs, hidden source identifiers/genre/gold, hash-selected premise groups and prior exclusions, alias collision scope and token-length matching, paired seeds, and balanced dispatch limitations. Pin exact dataset revision/license receipts and named exposure inventory; do not claim global or pretraining novelty. Prior old/new-context panels are exposed motivating evidence, not contemporaneous controls.

Compare every actual request against frozen REQUESTS/ORDERED_REQUESTS/PROMPT_IDS, including unchanged displayed evidence, common instructions, exact per-position tag grammar with all label options, and absence of tools. Authenticate actual service descriptor/config/preflight and no adapter. Count retained physical request attempts, HTTP returns, authenticated sampled completions and native token usage separately; rejected/pretransport attempts are not established GPU generations. Missing usage remains unknown. Provider billing is unknown/not measured. Include startup, collection, release and parent elapsed times and all attempted costs; no rerolls or suppressed failures.

After terminal/release, pin every actual outcome file before recounting. Prefer independently scored raw responses before comparing producer summaries. Preserve source/method seals; any parser correction is additive and explicitly dated. Deliver per-endpoint audit, paired/context/genre summaries, source/native/cost closure and concise report with limitations and decision implications.
