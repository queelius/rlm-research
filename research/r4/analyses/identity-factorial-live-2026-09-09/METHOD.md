# Audit method: disjoint numeric IDs and prefix swap

Main froze this method September9,2026 at07:30:46UTC before reading new factorial
outcomes. Execution began07:23:30, so the audit method is not preregistered before
execution. Scientific384-call design/inputs were frozen before inference:
READYff741613acaa31108dd3873879c24a581006495faafda91216096a35fdf711d5;
SPEC150e2510a804042dc499fc1b9d8a195473d9f697c784a2bb929a803e96417792.

Reconstruct384 planned calls:8contexts×2permutations×2seeds×2source prefixes
×2numeric namespaces×3output rules.24 dataset/prefix/namespace/arm cells each
contain16 calls/1024 labels. Four contexts per dataset are the source units;
repeated items/permutations/seeds do not create independent source examples.

Authenticate accepted/scientific closures once per stage, not per episode. Check
each actual wire request, fixedc32de model/alias, sampling, grammar, full physical
prompt IDs, raw completion, finish reason and usage. Link all retained physical
attempts. Independently parse64 ordered tag/label objects/canonical labels and
reconstruct displayed-position gold from original question/group IDs and frozen
source/permutation mappings. Never repair primary alignment from output intuition.
Compare independent and stored scores; disagreements remain explicit. Check raw
lexical key order separately, with reversed-order and invalid/null fixtures.
Never execute generated model code.

Primary: meaningful-minus-ordinal DISPLAYED-position accuracy in DISJOINT namespace,
separately by dataset/prefix. Report paired item gains/losses, call differences,
context sums and nested permutation/seed effects. Then show absolute overlap→
disjoint changes per arm and their meaningful-minus-ordinal interaction. Prefix
disagreements stay visible; constant padding is secondary. Whole64-item success
and per-call class-count error/L1 are separate; cancelling errors can hide behind
a correct aggregate count.

Prospective numeric-source diagnostic applies ONLY to overlapping ordinal calls.
Compare ordinal numerals to the source bearing that numeral, without replacing
primary scores. Disjoint diagnostic is unavailable with no mapping, not zero or
repaired by latent source rank. Include class-frequency/chance context because
few semantic classes are not unique entities. Extra posthoc analyses are additive
and explicitly labeled after outcomes.

Keep all scheduled denominators. Completed invalid format is strict failure with
unavailable semantic alignment; infrastructure/unrun/censored cases stay separate
and null where unknown. Report complete pairs, actual input/action tokens, known
and missing cache values, summed overlapping call seconds, service/job elapsed and
startup/cleanup separately. Equal standalone tag lengths do not imply equal cost.
DisjointID3000 shares suffix000 with the constant, not an ordinal: disclose this
secondary-arm caveat without dropping/changing frozen cells.

This behavior-only component test has exposed contexts, one fixed child and exact
constrained outputs. It establishes neither attention causality, new-source
generalization nor whole-RLM benefit. Specific surviving effects motivate new
contexts/models or uptake-aware RLM tests, not automatic extra SFT/mechanism sweeps.
