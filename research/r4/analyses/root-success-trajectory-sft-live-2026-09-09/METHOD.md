# Complete-success SFT72: pre-outcome independent audit method

September 9, 2026; written after launch and before this auditor reads any new training result or readout outcome. MAIN reports an allocator warning followed by checkpoint1; neither is treated as final success/failure evidence. Await MAIN's terminal trigger, without outcome polling, before scoring. The original adaptive RL and row-mean comparisons are known historical evidence, not part of this study's new primary sample.

## Scope and independence

Study READY `ced16becb584a709d7da0faa122b5343def395168909d52d66eb762fe6ef3a9c`, identity `50515d52dc000a674db3ecfef817e76fd21577782d94b516536a3c16548f7c4e`. This auditor did not author the complete-success study, candidate selection or new objective. Shared original SFT/native harness authorship and prior RL/row-mean outcome exposure are disclosed: independent new outcome reconstruction, not independent framework authorship. This method is postlaunch, pre-new-outcome; no claim of preregistration before research exposure.

Own only this analysis directory. No GPU, services, environment changes, accepted source edits, model requests, generated-code execution, new training candidates, rerolls or queue changes. Use immutable source/data and completed evidence; authenticate each large checkpoint closure once, never per episode. Preparation may inspect training-only source qualification, not live readout outcomes. Pending/partial/failed stages retain explicit status and planned denominators.

## Training identity and signal

Exactly27 confirmed training-only successful trajectories from adaptive RL rounds1–5:114 root actions,15,256 targets. No evaluation or later-round candidate, no synthetic/operator trace, no provisional success, no deduplication, no omitted recovery/initial helper turn. Verify membership against candidate seal and exact native export; source-group training/readout overlap must be zero (declared320 training,512 readout source groups). These are exposed/helper-training-supported data, not unknown-pretraining-clean questions. Success selection is outcome-conditioned **within training data**, not unbiased behavior sampling.

Authenticate native graph/role/typed/wire evidence for the114 teacher actions, preserving exact prompt/action IDs, physical terminators and masks. Only the current real depth0 action suffix receives loss; child/tool/history/context are masked. Recorded old probabilities are provenance, **not used as the SFT objective**. Original teacher policies vary across adaptive generations; the new SFT root starts exact efab interface final4 with explicitly fresh Adam, not any teacher optimizer.

Eight full-corpus optimizer updates, shuffled episode order from981308002+update_index. Every update consumes27 episodes/114 root turns/15,256 targets:216/912/122,048 total. Loss is mean episode→mean root turn→mean action-token CE; per-turn coefficient is1/(27×episode_turns×action_tokens), executed in FP32. Reconstruct coefficients/mass/objective and separate token NLL; **coefficient mass and CE contribution are not gradient shares**. Same BF16 frozen base, FP32 rank8 adapter, LR2e−5, AdamW0 weight decay, clip1; no child parameter training.

After trigger, verify actual contiguous checkpoint1…8 states/member hashes, previous-state ancestry, full-pass cursor0, epoch=step, finite nonzero gradients/deltas, exact episode/turn order and targets, persisted Adam cursors, RNG artifacts, starting/load identity and invocation/resume clock accounting. An interrupted partial pass is not an optimizer update. No final8 substitution by an earlier checkpoint. Allocator warnings are classified using actual completion/failure/checkpoint evidence, not text alone. Delta is explicitly from the current load if recovery occurred; do not call a resumed-load delta a total start delta.

## Three policies; two primary baseline contrasts

Frozen order is RL7→success-SFT8→baseline. All share24 fresh coordinates/seeds:981308101–108 validation,201–208 query-transfer,301–308 length-transfer; common exact prompts, fixed typed childc32de, native image/local runtime and sampling(.5, full support,2048 per call). The eight contexts are already exposed; source-disjoint from this training corpus does not mean unseen by the research program.

Baseline is unchanged efab interface-SFT final4. Success-SFT is only this study's complete fixed final8. RL7 is exact last-saved adaptive checkpoint809fc46e… after the preserved original no-mixed round8 STOP. It is not original planned final8, successful completion of that RL study, or validation-selected. Authenticate all three lineages and service descriptors separately; no fake common-start/equal-compute claim. The new SFT source trajectories themselves come from the adaptive run, so the package arms are not independent learning histories.

Co-primary descriptive paired comparisons: success-SFT8−baseline and RL7−baseline. Report each24-pair wins/losses/ties/unknowns, all-planned bounds, per-coordinate and context/stratum breakdowns. Success-SFT8−RL7 is secondary package comparison. Do not treat72 trajectories as72 independent contexts, pool historical samples or infer equivalence from a flat small sample. Seed-matched first prompts are paired inputs, not guaranteed identical adaptive trajectories or deterministic replay.

## Endpoint, admission and mechanism

Independently reconstruct exact whole-reply `Answer: N` correctness with inherited surrounding-whitespace handling. Completed observable wrong, malformed or empty output is0; unavailable/provider/setup/incomplete/censored/unrun isNULL. Preserve raw endpoint, actual collector availability, trace/episode errors and native-graph validity separately. Do not add an RL training-admission gate to this readout. Recoverable errors cannot silently become nulls; completion does not silently certify native training admissibility. No parsing repair, bare-number/prose fallback or gold-based rescue.

For all physical attempts, verify authoritative internal request IDs (not filenames), role/model hashes, native/wire prompt and completion/logprob equality, actual child grammar eligibility and root non-grammar. Check each frozen first prefix and all paired first inputs/sampling. Track final strict syntax, correct number, helper uptake, exact four-record first-HELPER copying, actual requested/valid-map relevant coverage, child map semantics and visible recovery. Missing/invalid first code is unavailable for conditional exact-copy diagnostic, not a false exact-copy observation. Do not infer plans from root prose or root-writable logs alone.

Use conservative source-authenticated maps on the actual final root branch to distinguish semantic label errors, missing relevant evidence and map→count/final-format failures. Partial/conflicting/unrecognized maps are diagnostic unknowns, not endpoint nulls or repaired accuracy. Recovery observations/call errors are described without host execution of sampled code. Improved answers alone do not establish question-sensitive planning.

## Cost, lifecycle and interpretation

Report root versus child attempts, known/unknown input/output/cache/uncached tokens, request-only tails, completion/error states and per-episode wall time. Provider usage is not measured GPU FLOPs or unique uncached compute; inherited retries must not be called zero retries without evidence. Pair total cost for observable coordinates and jointly successful pairs separately; if no jointly successful pairs, successful-task ratio is undefined. Sum of parallel episode durations is not study wall time.

Frozen caps:1200s training process,900s per readout,4380s shared work/4500s inclusive/4530s parent. Authenticate terminal and parent-reported/retained exit evidence without process control. Earlier completed results remain observed after a later cap/failure. STOP without fixed8 is reported as incomplete, not quietly evaluated under a changed checkpoint rule.

Publish early grounded metrics after trigger, then readable report, exact per-coordinate machine metrics and consumed-source hash manifest. Reuse the sealed row-mean audit's independent endpoint/native/cost functions with a narrow three-arm/path adaptation; preserve its original source. Additive parser corrections must be recorded. Promote only the supported training-package effect on these exposed coordinates; keep format, coverage, child semantics and aggregation bottlenecks distinct. No evaluation data become training candidates in this audit.
