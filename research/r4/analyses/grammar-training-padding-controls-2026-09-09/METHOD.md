# Audit method frozen before this analyst reads the outcomes

Declared2026-09-09 at04:41 UTC. This additive results audit covers only the accepted
grammar160, its later B80 companion, and padding128. No outcomes from these three
studies have been read by this analyst before this declaration. The completed HF
comparison motivated B80 and is known background, not pooled evidence. Root96
results do not alter these comparisons. No experiment or source changes, GPU/model
requests, service signals or reruns are authorized. This analyst authored B80;
its results audit is not a fresh independent review of its code. Cite the parent's
separate B_CONTROL_SOURCE_REVIEW and PADDING_SOURCE_REVIEW.

## Questions and units

1. Does fixed-final mixed-size B match specialized indexed-final SFT within each
   task, anonymous/indexed representation and free/exact grammar? Primary contrast
   indexed-final minus B, using the frozen80→160 coordinate crosswalk. Old SFT is a
   reference. Same context/seed/representation/grammar pairs must have identical
   serialized request bodies except the truthful model alias and equal physical
   token IDs. B's later service stage remains a phase/time confound.
2. Does meaningful ID-tag output outperform identical-shaped placeholder-tag
   output, and does the difference depend on grammar? Pair within context, repeat
   and decoding mode. Anonymous and indexed arms are supporting reference cells.
   Instructions differ truthfully (26 prompt tokens); synthetic equal token lengths
   do not establish equal realized computation or neutral placeholder semantics.

Keep TREC and SST separate. Grammar/B use six/four context groups; padding uses
four/four. Two seeds and multiple cells are nested within groups, not independent
datasets. Report every context-pair difference, totals and positive/zero/negative
group directions; do not interpret isolated one-item differences as demonstrated
equivalence or superiority. No new threshold or outcome-selected subgroup.

## Score and mechanism checks

For each planned call retain completed status, provider/infra status, strict whole64
success, exact cardinality/ID/tag/canonical validity, aligned semantic correct count
and positions. Invalid completed outputs fail strict scoring but their semantic
alignment is unavailable, not64 established classification mistakes. Infrastructure,
missing and unrun outcomes remain null. No repair, prefix extraction, tool execution
or post-hoc scorer changes. Separate correctness differences among jointly valid
pairs from strict differences that include format failures.

Compute per-call gold and predicted label-count vectors only when all64 items align:
exact vector agreement, label-wise errors and total absolute count error. Never infer
aggregate-count accuracy from pooled histograms, since errors across calls can cancel.
Inspect lexical key order in free indexed objects and free tag objects. Existing
scoring permits label-before-tag if keys/values are valid; do not assert an ID-before-
label mechanism unless actual order supports it. Describe observed order without
changing primary scores. Record actual output tokens, input tokens, cache/missing
cache values, lengths/stops and stage/collection wall times.

## Raw audit and provenance

Wait for immutable STATUS plus wrapper FINISH/operation terminal markers (or an
explicit terminal failure). Process each stage once; cheap existence checks only
between stages. Authenticate frozen spec/source/weight closures once per stage,
reuse a local hash cache for shared immutable files, and hash all completed raw
call/wire files. Reference prior model/source audits; do not load tensor states or
rehash the entire model per call. Preserve discrepancies as audit findings.

Recompute strict scores from complete raw first responses using the hash-pinned
scorers, and independently check decoded arrays/objects for counts/key order. Match
raw body bytes against ordered frozen request serialization; verify request IDs,
aliases/checkpoint/config/live model cards, temperatures/seeds/full-support sampler,
output cap, physical prompt token hashes and reported prompt-token counts. Verify
actual returned prompt IDs against the qualified native template; do not substitute
CPU-untyped rendering. Compare recomputed aggregate fields with saved analysis.

Write reproducible CPU raw-audit code, per-stage immutable metrics/source hashes,
then concise combined METRICS.json, SOURCES.json and readable REPORT.md. Explicitly
list pending/censored stages until terminal, preserve partial captures and failure
reasons, and stop after these three operations. Audit completion never gates jobs.

## Interpretation boundaries

These are sampled vLLM component evaluations, not HF-greedy replication and not
end-to-end RLM orchestration evidence. The TREC compositions are exposed. SST groups
were fresh at the parent freeze but are reused across these studies; later stages
are not genuinely unseen data. Public pretraining exposure is unknown. A task- or
grammar-specific interaction motivates replication; broad competence, mechanism
causality and optimal training are not established by this small exploratory suite.
