# Prospective independent plan-SFT source review

Freeze before any new root-plan-SFT training/readout outcomes. Reviewer did not author this study. The original approval and prospective symmetric LR1e-4 amendment are authoritative; prior high-LR aggregate is disclosed development evidence, not an outcome of this experiment. No model/service calls, generated-code host execution, source edits, broad tests or outcome-based target selection.

Authenticate the eventual frozen READY once, then inspect only material scientific/integration seams: both arms start exact66cce400 low-LR success-SFT8 with fresh Adam;16 authored initial actions/arm from the same384 training source groups; global code/token equality and single-user selected-assignment-only difference; public literals and no host-gold labels/counts or observed/fabricated teacher-success/likelihood fields. Check actual qualified renderer prefix and authored tool-action terminator/mask, separating fixture synthetic-provider evidence from training targets.

Review four full shared-order passes per arm, LR1e-4/WD0/clip1/BF16 base/FP32 LoRA, equal-example then mean-action-token CE, exact target counts/coefficient normalization, optimizer/RNG checkpoints and fixed4 selection. No validation-max, low/high-LR root substitution, silently shortened pass or copied child-observation targets. CPU qualification demonstrates serialization/procedure under a synthetic child, not a learned teacher's accuracy.

Review unchanged66cce plus canonical4/filter_first4 readouts on16 matched fresh-seed query/length coordinates (48 planned), four exposed source context clusters/320 groups disjoint from384 training and192 unused validation. Primary eventual contrast is filter_first minus canonical strict whole Answer:N; secondary contrasts versus unchanged. Completed malformed/empty wrong0 only if observable; infrastructure, unrun and unavailable remain NULL. No repair or missingness-to-zero. Instruction/phase order and all caps are predeclared, not treated as randomized services or16 independent source contexts.

Check actual private collector namespace/cardinality, model/adapter/base/descriptor binding before calls, exact physical prompts, captured native root/child roles and all-physical-call cost. Scope/coverage/copying/answer-versus-evidence diagnostics are descriptive, with unobservable evidence null; a correct final does not prove a good plan. All-planned cost and selection-conditioned jointly-correct cost must remain distinct. Source/fixture hashes are cached once, not inside each model request. Previously failed pre-READY missing-context/API fixtures remain distinct setup evidence.

Review one2280 shared/2400 owned/2430 outer clock, two360 training and three420 collection maxima clipped to remaining budget, startup/release charged and120 cleanup. Inspect no fixed4 fallback, no retry/unsafe old-service cleanup, and preservation of incomplete/unrun records. Existing nano transient retries, if inherited, are a disclosed cost limitation, not a falsely claimed zero-retry guarantee.

Deliver a concise material-issues verdict with exact reviewed source/READY hashes. Do not infer live performance or run a broad confirmation suite. Any future terminal audit needs its explicit trigger and immutable outcome/input manifest.
