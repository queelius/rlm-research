# Root plan-SFT independent pre-outcome review

Verdict: no remaining material blocker in corrected READY_V2 `8a90b12f…`. Original READY `6c511ac8…` is not launchable: I reproduced its actual readout import failure before any model calls. The inherited source contains two PLAN-path references but its adapter expected one. The author preserved that freeze and added flat readout_v2/launch_v2 entrypoints. Both corrected executables now independently import successfully; their collection/workflow ASTs match the declared path/readiness substitutions only. All 2,438 scientific input hashes and 672 original source entries are unchanged.

Independent data checks re-rendered all 32 authored rows: 16 paired native prefixes equal, eight global targets byte/token-identical, eight single-user targets differing only at the selected-record assignment. Each arm has 2,472 action targets/pass and 9,888 across four passes. Prompt positions are fully masked; the authored action and 151645 terminator receive CE. No reward, advantage, behavior likelihood or teacher-success field enters these rows. The 384 training, 320 query/length readout and 192 unused-validation source groups are pairwise disjoint. This is exposed development composition, not new benchmark evidence.

The trainer reuses the qualified action-token CE/update helper with one turn per example, hence equal-example then mean-action-token loss. Both roots load the exact low-LR 66cce SFT8 with fresh AdamW at 1e-4, shared four-pass orders, BF16 base/FP32 rank-8 LoRA, clip 1 and zero dropout. Child/prompt/observation tokens have no loss. Checkpoints preserve optimizer/RNG/cursor; readout binding requires complete fixed step 4 and exact adapter/config/state, never a best or fallback checkpoint. The symmetric LR amendment is explicitly outcome-informed, while the start remains fixed low-LR step 8.

Actual CPU fixture evidence corroborates seven synthetic-provider calls: canonical 32 IDs/two child batches versus filter-first 8 IDs/one batch; both tool observations are `2\n`, uncredited. It proves authored procedure and native serialization under a synthetic child—not learned teacher success or child-model accuracy. The retained earlier missing-context/API setup failure supplies no training data.

The corrected path authenticates actual root/child/base descriptors/cards and unchanged native prompts, then dispatches 16 coordinates per root. Phase order canonical→filter_first→unchanged is fixed, with service/time nuisance; primary comparison is 16 paired outcomes over four source context clusters, not 48 independent tasks. One 2,280-second work/2,400-second owned/2,430-second outer budget includes startup, verification and release; phase maxima are clipped, fixed-step-4 failures do not substitute earlier checkpoints, and later unrun outcomes stay NULL. Inherited nano transient retries and incomplete semantic observability remain disclosed limitations. No endpoint repair or forced helper use was added.

I independently inspected source/inputs, replayed token construction, and checked the actual executable imports; I did not rerun the author's model/runtime fixture or broad test suite. Author reports 12 focused CPU tests passing. No new training/readout outcomes, GPU calls, generated-code host execution or frozen-file edits were used in this review. Machine evidence: METHOD_READY, BASE_AUDIT/BASE_SOURCES and DELTA_AUDIT in this directory.
