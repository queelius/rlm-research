---
date: 2026-09-10
status: prospective_before_outcomes
---

# Mixed-child supplied-plan40 audit

Before reading outcomes, freeze a raw-native recount of all 40 new calls and the 40 pinned c32
controls. The reader accepts the exact parent EXIT path relayed by MAIN, requires its marker hash to
bind the owner terminal, and requires exit 0, no timeout, release, and an empty post-exit GPU list.
It validates request bodies against prepared inputs, native model/choice/request/token/logprob/usage
identity, and independently decodes the response tokens.

Authenticated malformed maps are observed zero and invalidate their episode; missing, unreturned,
or unauthenticated responses are NULL. No partial-map salvage occurs. For each episode the reader
reports exact six-class accuracy, target/target_b/other projected accuracy, public J1 prediction,
absolute error, and exactness. It pairs all eight episodes and sums the size-64 and size-256 absolute
errors within each of four clusters. The frozen gate is lower error in at least three clusters,
at least +2 exact episodes, and no availability loss. It also audits new physical token usage and
pins the mixed 6/12/18/24 checkpoint lineage. Both cohorts are research-exposed.
