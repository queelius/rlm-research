# Targeted boundedV2 child diagnostic

Only completed endpoints at existing audit indices20 and30 are in scope. These are outcome-selected genuine multi-batch reductions already identified by bridge, not an independent whole-study sample. Reconstruct raw c32 native tokens, public ordered ID/text records, decoded prediction maps and fixed source labels; never execute sampled root code or alter labels. Record confusion by batch, input/output position and category, task-relevant signed errors and the root's residual error beyond the supplied-map scalar.

Manual text review is fixed prospectively to the first six disagreements in original source order from each endpoint, total12. This does not estimate an overall annotation-error rate. Consider plausible taxonomy ambiguity versus a clear mismatch under the written categories, without inferring an internal model cause or researching answers unnecessarily.

Compare neither different-text batch error rates nor an exposed outcome-selected pair causally. Recommend only a new matched-data test if warranted. No QS readout access, GPU/model execution, source mutation or follow-up implementation.
