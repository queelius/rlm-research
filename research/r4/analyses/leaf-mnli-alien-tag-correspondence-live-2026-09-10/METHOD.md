# Independent alien requested-tag48 audit method

Freeze after launch but before reading any alien outcomes. The auditor has seen only live attempt/result filenames, not response contents, aggregate scores or per-arm metrics. This is not a prelaunch registration or confirmatory design. MAIN owns launch and may know operational counts. Exact assigned attempt: `sidecars/leaf-mnli-alien-tag-correspondence-v1/outputs/attempt-001`, accepted READY_v2. I did not author alien48; I authored original MNLI data/runtime and new-context32, and contributed common native/lifecycle plumbing. Independence is successor outcome authentication/rescoring, not independent data construction.

## Frozen analysis

1. Pin accepted READY_v2 and its complete source/input closure, exact 48-row plan and eight exposed contexts. Independently verify matching, shift17, and the frozen alien dictionary; alien IDs have no named gold. Check bounded absence/collision receipt and per-position tokenizer-length matching as source facts, not global semantic neutrality or equal FLOPs. Reconstruct all actual typed no-tools request prefixes and exact positional tag schemas, label freedom and paired fields. Eight contexts × two seeds × three arms; 768 planned labels per arm, not 768 independent observations.
2. Resolve every planned coordinate even if never attempted. After terminal/release, pin raw attempt artifacts before scoring. Confirm owner/service descriptors, model/base/config, no LoRA and no tools, release receipts, source identity and exact ordered wire hashes. Missing owner/release is reported operational nonclosure, not fabricated. No rerun or output mutation.
3. Independently authenticate raw status/model/unique assistant branch, prompt and completion token arrays, usage, decoded final/tool envelope and finish reason. Unreturned/failed transport or inconsistent identity is NULL. An authenticated wrong-tool branch is observed strict zero and never executed. A returned length-capped branch is observed and scored by content, with length flag; never automatically infrastructure NULL.
4. Primary: full output JSON, unique object keys, no nonfinite values, exactly 48 objects, canonical string labels and tags, exact tag-then-label fields, exact requested tag in each displayed position. Only whole-contract valid endpoints admit displayed-label semantic item counts; malformed authenticated endpoints are strict zero. No reordering, partial salvage, semantic repair, tag replacement or invented alien gold. Separately report shape-only displayed semantic accuracy, tag-position fidelity and whole-batch validity.
5. Compare matching versus shift17 and alien versus shift17 as paired descriptive contrasts; also show matching versus alien. For each of eight contexts average the two paired seed contrasts. Missing endpoints retain explicit [0,48] bounds and complete-pair summaries; operational zero-imputation is labeled separately. Context/genre displays, no item-level independent significance or posthoc best-arm selection.
6. Shift17 only: precompute differing displayed/named gold positions from frozen data. On whole-contract valid endpoints report displayed/named/third label counts and eligible versus planned denominators. Alien has no named-label diagnostic; missing eligibility means undefined, not zero. No new source selection from these outcomes.
7. Count physical REQUEST receipts, raw RESPONSE returns, authenticated endpoints and missing slots separately. Sum trustworthy input/output/cache usage including rejected responses, retaining unknown usage/cache. Report actual timings, length/errors and physical costs; billing/FLOPs unknown. Existing source data and historical evaluations are exposure, not current acquisitions. No child/tool execution is permitted.
8. Reuse only independently written exact32 score/native primitives, not the experimental scorer. Extend prior parser's all-success assumption to planned NULL/error paths before reading outcomes. Compare every independent admission/score against retained producer values; preserve discrepancies. Focused authored fixtures cover alien/displayed semantics with no named gold, malformed fields/order, NULL, native mismatch and tool-route zero. Seal parser/tests before raw reading; outcome adaptation requires an explicit addendum.

Read depth at method freeze: full alien DESIGN/PLAN, protocol/study/scoring/collector/owner, full prior exact32 independent METHOD/parser and independent shared native authenticator. Prior exact32/MNLI findings are known. Remaining inherited source/control checks are hash-bound and performed before raw scoring; no claim of full ancestor re-review here. Only new analysis namespace and allocation report are owned; no GPU/service/lock/launcher/queue/scientific-source writes.
