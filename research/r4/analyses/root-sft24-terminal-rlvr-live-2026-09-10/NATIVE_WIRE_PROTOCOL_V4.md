# Additive native-wire parser protocol, before final readout

2026-09-10. Supplements METHOD.md, V3_BINDING.md and V3_STAGED_READ_PROTOCOL.md without changing metrics, primary admission or policy selection. At this write, six committed state.json paths exist and no attempt-003 readout directory exists; no final outcomes have been opened. runtime_port authored V3 and this parser: author-assisted audit, not independent V3 validation. MAIN/question_cards retain independent roles.

The draft parser.py and its seals remain untouched, as does the live owner's hash-bound wire_ledger.py. New native_wire_v4.py reads role-audit request/result plus typed-audit mirrors from explicitly selected scientific stages. Call identity is (attempt, stage, request_id), not request-body equality. Identical bodies with distinct IDs remain separate physical occurrences. Same identity appearing in multiple mirror files counts once; differing identity fields or returned payloads are an integrity defect, never silently selected. A result-only returned wire response still counts; a request-only or typed-only record is an unresolved intent, not a returned completion or zero cost. Each input/output/cache field retains its own unknown count. Missing cache usage is not zero. Failed HTTP responses count as observed attempts, but not authenticated model generations.

Authenticate HTTP200 wire choice tokens, prompt IDs, completion logprobs, finish branch, model alias and usage against native response records. Match typed response mirrors by request ID and actual native response identity, never count them as extra calls. The endpoint verifier consumes that explicit role-ID wire occurrence, not fabricated legacy physical files. It checks initial prefix, actual root adapter/model, final native graph ancestry, no unresolved tool routes, parsed content and source root reply. An authenticated malformed Answer string is strict0; a tool route or missing native final isNULL. A raw final without the required source export remains a separate diagnostic, not primary salvage.

Complete planned slots remain192 training (original attempt-002 window1 exactly once, new attempt-003 windows2–8) and96 final (original unchanged-first policy order, original48 seeds/tasks each). Training eligibility is separate from availability; no data selection or model changes follow these reads. Audit committed states and atomically closed exports only. Seal stage costs only after release/terminal; live lower-bound snapshots, if used, must be explicitly labeled and cannot be final totals. Combined budget charges381 original execution seconds plus V3 actual active time, with calendar/idle separated.

After MAIN terminal relay, review every available final's actual program path, not only keyword/AST matches or successes. Requested operator/category/scope, real returned-map use, live retention versus literal reconstruction, visible scalar and final identity are separate from child semantic correctness and gold coincidences. No sampled program is executed. Missing observations remain unknown. Group final contrasts by eight context clusters and primitive/composition/zero/nonzero, preserving all NULL bounds and failed-stage wire costs.

## Bounded implementation plan

New files only in this analysis directory; no source/service/GPU changes.

1. Add test_native_wire_v4.py: distinct stage/same request identity, mirror dedup, same-body distinct calls, conflicting duplicates, typed-only and HTTP-error unknown usage, native token mismatch, tool-route versus malformed final, missing export primaryNULL. Run before parser creation to demonstrate absent implementation.
2. Implement native_wire_v4.py as a small stage reader and native verifier, reusing pinned graph/parser arithmetic where valid. Run the focused tests against actual file fixtures, not mocked ledger counts.
3. Add a read-only later-checkpoint review using the qualified training environment on CPU. Check state/file hashes, saved Adam cursor/moments/RNG, input→previous optimizer/RNG identity, generation and no duplicated source window, native masks and retained numerical corrections. No large base/model forward.
4. Seal new method/parser/test sources and qualification artifacts with explicit original references; report paths, hashes and evidence to MAIN. Final behavior reading remains gated on MAIN relay even if paths subsequently appear.
