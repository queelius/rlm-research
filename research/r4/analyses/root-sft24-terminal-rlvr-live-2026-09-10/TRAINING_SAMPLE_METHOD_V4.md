# Bounded closed-training interpretation

Authorized by MAIN after the seven-checkpoint/closed-wire review. This is a post-training author-assisted sample, not a prospective efficacy endpoint or an all-positive mechanism rate. Window8's manifest24/24 reward1 and earlier commit/admission counts are already known. Neither final readout has been opened.

Aggregate every original192 slot from the frozen plans and the original attempt002 window1 plus attempt003 windows2–8 EPISODES/MANIFEST files. Separate attempted/raw rows, exported, native-available, clean training-admitted, within-identical-prompt reward counts, mixed-group selected, noops and actual optimizer seconds. A uniform all-success or all-failure group contributes no normalized-reward update; do not relabel it inaccessible reward. Host zero/gold coincidence is a separate descriptor.

Before inspecting their code, choose the lexicographically lowest coordinate ID among authenticated endpoint-positive episodes for each (window, task_id) prompt group, at most24 primary sample episodes. This automatically includes zero-gold groups. Additionally choose at most one lowest-ID zero-gold endpoint-positive but training-excluded episode per window if it is not already selected, at most8 extra. Record the full selection and hashes before reading programs. Do not substitute a more interesting/successful trace. Review actual root program/observations and child predictions, without executing sampled code; classify faithful requested operator/scope, wrong-operator or constant-compatible coincidence, child-semantic error, evidence acquisition and genuine live-map use versus literals/manual arithmetic. Unclear paths remain unclear. Findings describe these selected training trajectories only, never all192 or unseen finals.
