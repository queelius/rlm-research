# Fixed-policy transfer48 audit method

## Fixed question and population

Study: /project/alex_phd/runs/rlm-research-r4/sidecars/root-fixed-policy-transfer-v1/outputs/attempt-001.
All48 endpoints =4 contexts ×2 scope queries (u03 versus u01/u03) ×2 paired seeds ×3 fixed policies. Preserve unchanged low66c, joint step4 ffa49801…, reduction-stop step4 eaa80a9…; frozen c32 checkpoint0128 child, not checkpoint0032. No checkpoint selection from this readout. Four source contexts use64 source groups selected after QSR448 and the original53-manifest exclusions; verify exact exclusion/provenance receipt. Root-new is scoped catalog novelty; all are child-training-exposed.

Recompute count of target category among actual requested users. Show unchanged/joint/reduction_stop per16 planned plus each query8, four context means, two-seed descriptives and paired joint-minus-unchanged/reduction-minus-unchanged/joint-minus-reduction outcomes. Fixed independent service phases imply policy/time-order confounding; report exact frozen and actual phase order. No historical joint panel percentages as causal baseline. Every root is free-start; any teacher/shared replay is a material provenance violation.

Record user-field/actual-ID scope use, literal-list observed-label counts, variable accumulators, reacquisition, producer loops and stopping separately. Gains from extra physical child/reclassification work are not equal-compute competence. All readout acquisition is physically new; training is prior sunk work. Phase480s, episode180s, root action2048/context8192, work2100/owned2280/outer2400.

## Independence, timing and scope

This method is frozen before this auditor opens either new attempt's outcomes, native traces, result rows, terminal aggregates or service-output contents. Only prospective designs, implementation sources and input/READY provenance are reviewed first. MAIN may have operational or historical outcome knowledge; no claim that MAIN is outcome-blind. This auditor did not author either new study, but did contribute reused an27 lifecycle/native plumbing and earlier joint-SFT/QSR components. This is an independent new-study rescore, not independently implemented whole-stack validation. MNLI and record-interface are explicitly outside this audit.

Read only exact attempt-001 after MAIN's launch/terminal notices; no GPU, service, locks, launchers, queue writes, source edits, retries or sampled-code execution. A method addition required by an unforeseen artifact schema is additive and labeled before applying it, never a silent post-outcome rule change.

## Common endpoint and provenance rules

Start from frozen planned coordinates, not discovered successful rows. Verify READY canonical identity and pinned source/input hashes; bind OWNER_RUN, actual service bindings/descriptors and model/adapter hashes to that identity. Authenticate first native system/user/tools and physical token prefix, root/child model routing and actual sampling/caps. Independently traverse the final root node's actual parent chain/branch, concatenate graph tokens and compare exact prompt+completion native tokens and outgoing prompt. Require the same physical returned response, unique root final branch, no outstanding tool call, assistant role, decoded final content equal to root_reply, and stop/length return. Trace completed flag, last HTTP200, author score or matching text alone is insufficient.

A genuine returned malformed final is available and strict0. A final-less loop, context exhaustion, interrupted/unstarted slot, transport failure or unverified branch remains NULL in the native-final primary; keep operational failure subtypes and separately show planned-denominator unsuccessful sensitivity. Length is flagged, not automatically NULL: complete returned strict final is scored. Final-less authenticated tool actions are observed policy work, not a final answer. Do not alter study scores in place; report any independent discrepancy and evidence.

Recompute host truth from frozen records, labels and query semantics using authored arithmetic, never model code. Strict whole-strip fullmatch Answer: ([0-9]+), exact numeric equality; prose numbers/partial answers are diagnostics only. Per cell report planned n, available n, correct k, format-valid, valid wrong, malformed, NULL causes; k/planned as explicitly operational sensitivity, k/available when nonzero, bounds [k,k+NULL]/planned. Keep paired coordinates and context clusters; no item/seed/call independence.

## Executed-evidence mechanism classification

For EVERY actual episode inventory root actions, tool observations, child dispatches and final branch. Preserve exact code/observation excerpts and hashes. Parse Python AST only for indexing; never execute, eval, import or repair sampled programs. A planned or mentioned expression is not execution. An IPython action requires matching actual tool-call ID and returned tool observation; errored code does not establish later statements ran.

Distinguish: no acquisition; genuine child acquisition; returned evidence retained in live variables/files; host-source file retrieval; literal transcription of observed labels; manual arithmetic/counting without executable reduction; executed count/distinct/weight reduction over evidence with actual scope/IDs and authenticated output; unrelated scalar agreement; further reclassification; final-only guess; successful reduction without stopping. Literal lists reconstructed faithfully from observations are a separate mechanism, not fresh-variable continuation. Map-load, AST presence and final scalar agreement alone do not prove applied evidence. For ambiguous data flow mark uncertain rather than infer success. Summarize actual actions/cost for all rows and include selected gain/loss examples picked by fixed contrasts, not only successful code.

## Cost and seal

Deduplicate request/result/physical audit records by actual request identity; checkpoint-only records are retained. Count attempted transports, returned native completions, root versus child, pretransport rejections and missing usage separately. Do not equate attempts with GPU generation or legacy paid_model_call with provider billing. Compare raw token vectors/usage; distinguish token volume sent from verified/returned usage, and preserve unknown cached/output quantities. Include all failed/incomplete phase artifacts. No repeated historical training cost in new physical totals.

After OWNER_TERMINAL and authenticated service RELEASE evidence, freeze complete source/output manifest, rescore tables, mechanism rows, cost reconciliation, report and seal hashes. If terminal exists but release is unverified, report provisional results and lifecycle limitation rather than declaring released. Reports are bounded exploratory evidence with protocol violations explicit.

