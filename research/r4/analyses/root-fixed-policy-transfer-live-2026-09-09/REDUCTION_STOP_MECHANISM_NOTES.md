# Reduction/stop phase: all16 coordinates

Read all13 retained graphs' unique action programs and actual parent-linked observations; inspect all three absent-graph physical root streams and their retained observations in subsequent native prompts. Never execute sampled programs. Tool IDs repeat across turns, so parent plus ID identifies an observation. Below `scalar` means query-sensitive semantic count, not metadata lengths.

| Suffix | Actual mechanism and outcome |
|---|---|
| 7f7047af711904d1 | Import repair, scoped child acquisition, actual human-being filter list prints two entries; manual strict2 correct. No scalar. |
| 84e816759afcc80f | Full-context batches, empty out-of-range batch error, singleton reacquisition, faithful literal observed map transcription. Then wrong `k.startswith('u03')` on q-record IDs prints []; strict0 coincidentally correct. Not faithful scope/reduction. |
| c1a088b9aa583959 | No graph/final.13 physical root actions investigate missing jq/Python jq package; prior actions' observations retained in subsequent prompts. Last tool action has no returned observation before188.689s timeout. No child. |
| 78125aef62eaa20e | No graph/final.46 repeated malformed f-string metadata programs; actual SyntaxError observations, no child.48 physical calls; last prompt8230 rejected400, then190.288s timeout. |
| 65fceccab20ad228 | Actual scoped child map, then exactly transcribed literal map and executed numeric-label sum prints0; strict0 correct. Faithful literal-evidence scalar, not live-variable accumulation. |
| 9509088e32a3dc81 |49 repeated producer actions cycle slices, overwriting labels, no scope/stop.101 physical attempts; context8192 truncated tool envelope, NULL. |
| 7615ab649fab657c | Full batches followed by genuine u03-only acquisition; manual bare0 invalid. |
| 06beb7df06ddb970 | Full batches then genuine u03-only map; manual strict0 correct. No scalar. |
| ebe9c2abf09b2c9d | Repeated jq installation/lookup failures, then import repair and full batches, then genuine union-only map; manual bare2 invalid despite correct scalar. |
| 0181988bb52e68f7 | Four batches then26 attempts to read nonexistent record['label']; actual KeyError each time. No scalar reached; context8192 truncated action, NULL. |
| 96534751e5e4712b | File inspection/import repairs then genuine union child map; wrong manual strict2 although map has no numeric labels. |
| 11f019608d150115 | File reads then genuine u03 map without human labels; wrong/invalid2.0. |
| bfc5588a48f65ee8 | Four batches then scoped reacquisition, actual human-label filter list prints[]; manual strict0 correct. No scalar. |
| 8aa5c59c4c875e79 | Initial batches; missing-label error then text-as-label maps; repeated batch reacquisitions. No scoped scalar; context8192 truncated action, NULL. |
| d68da9aad89836ee | No graph/final. Repeated batches/full16 acquisition, then eight attempted scoped numeric len programs. Each fails BEFORE reduction at strict_map: last child map does not match requested union IDs. Subsequent native prompts preserve ValueError.62 physical calls; prompt8342 rejected400 then182.228s timeout. Count expression presence does not establish execution. |
| e382499a6868858a | Import repairs, genuine union map then literal-ID subset q0002/q0004 reacquisition; manual strict0 correct. No scalar. |

Total: one faithful executed scalar over transcribed observations; two faithful scoped filter lists; one wrong-scope empty list giving lucky strict0; no trained live accumulator-to-scalar-to-stop sequence. Six NULLs comprise three context-capped truncated action envelopes, two context-rejected/error-loop timeouts, and one post-action jq investigation timeout with unresolved executor-level cause.
