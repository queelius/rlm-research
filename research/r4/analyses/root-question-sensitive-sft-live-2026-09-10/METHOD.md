# Prospective question-sensitive SFT160 audit method

2026-09-10, before `root-question-sensitive-sft-v1/outputs/attempt-001` exists. Science READY SHA256 `92b950f663dbf76b96a4ca0e9d2e5841bb72d269b6d256f383a4e6521235fc07`, identity `9af3b4f1589a173bae7be54a1266bf4aecdbada1d5a3c27b603e61131269ddfa`. No new scientific outcomes have been read. MAIN retains launch/process/queue authority; audit preparation is CPU-only and additive.

bridge_audit authored this study. runtime_port independently reviewed its allocation/oracles and writes this outcome audit, disclosing authorship of inherited common native/runtime components, the earlier training-options note, and the separate warmV3 recovery. This is independent new-study outcome scoring, not independent validation of every inherited component.

## Frozen inventory, contrasts and units

Audit every planned endpoint: unchanged fixed SFT24 and exact new SFT6, each8 dev and72 protected, total160. Dev16 and protected144 remain separate; dev is not a checkpoint-selection panel. Exact checkpoint6 absence is an unavailable fixed treatment, never replaced with checkpoint5. Read all planned paths even if owner/aggregate is absent or incomplete. Existing per-episode RESULT takes precedence over stale aggregate; **missing RESULT remains primaryNULL**, even when a raw final exists (retain a separate diagnostic, not primary salvage).

Protected primary: strict integer correctness and actual task-faithful executed reduction, separately reported.72 paired tasks per policy are nested in eight contexts;24 primitive and48 composed questions share records within context. Report paired correct differences and lower/upper bounds, both-available transitions, eight context means, operator/slot/scope/threshold/category strata and zero/nonzero targets. No endpoint mixture mistaken for72 independent contexts; no dev/protected pooling. Report source-written inventories as well as independently authenticated primary scores, with every disagreement explained.

The exact approved table,320 source groups and180 defined specs are independently checked in `analyses/root-question-sensitive-sft-cpu-2026-09-10/REVIEW_READY.json` SHA5814efd62311d310f0c0778b5cd48758a087702369c5a7cc97ac7cb49ee73a5b. Actual executed dev is8 tasks per policy, not36 defined dev specs. The fixed source zero/best-constant baselines and anti-coincidence table are descriptive controls, not selection criteria. These are new-process train/dev/protected groups under named root-selection exclusions, all child-training/catalog exposed—not globally or pretraining unseen. Common native coding role and file-only raw-record interface are unchanged; no test-time card. This is a curriculum package, not isolated causal identification of parameter diversity.

## Native primary admission: new, explicit empty-final rule

Authenticate actual physical HTTP response and request, model/adapter identity, tokenizer/template, native prompt IDs and output IDs/logprobs, parsed response, exact final graph ancestry and branch, first native prefix, sampling/caps, and source coordinate. Root2048/context8192, fixed paired seeds and released source/config bindings must match. A trace completion flag or HTTP200 alone is insufficient. There must be no outstanding tool route and the final native assistant must have no tool calls. Stop and length are admissible final finish reasons only on such a final branch.

For **this new study only**, a fully authenticated returned native empty assistant is an **observed empty failure: strict0, format false, available true**. The normalization is precisely: native assistant `content=None`, independently parsed contentNone or empty string, engine/source-root reply exactly empty string, finish stop/length, no tool calls and no outstanding tools, nonempty authenticated generated token sequence, and an existing correctly bound endpoint RESULT. Preserve raw native contentNone, actual completion IDs, parsed content, finish reason, source reply and original source-scored availability/reward. No nonempty text may be normalized; no missing result/response/graph may be normalized. A native string must otherwise equal the parsed string and source-root reply exactly (whole-strip applies only when checking `Answer: N`). Authenticated malformed nonempty finals also score strict0. An actual length-capped generation is observed, not infrastructure-NULL, when this verified final contract holds.

This prospective rule deliberately differs from the older warm/card visibility scorer's exact None-versus-empty comparison. The frozen inherited QS source scorer may call the empty endpoint unavailable; the independent QS primary can call it observed0 **only under this precise authenticated-empty exception**. Other source-unavailable cases are not promoted. Older results/seals are not edited, reinterpreted, pooled or given a retroactive availability change. Source-observable/native-primary discrepancies will be listed explicitly. A focused prelaunch regression must demonstrate this exception and reject missing RESULT, unknown wire, HTTP400, mismatching nonempty content, unresolved tools and wrong route. A retained prior warm empty response may qualify the parser without reading new QS outcomes.

Unknown/missing transport, HTTP failure, failed native identity, unresolved graph/tool branch, genuinely unreturned endpoint or absent RESULT remainsNULL, with cause taxonomy and interval bounds. A completed raw native final without RESULT is diagnostic only. Planned operational-success counts use correct/plan as a conservative additional view; NULL is never silently stored as primary0. Do not run or repair model programs to obtain a score.

## Capture, learning, state use and cost

Capture has72 fixed trajectory slots,72 separate real typed c32 acquisitions and216 authored root actions. Authored root transport is not sampled policy generation. Authenticate each actual child response and native prefix; preserve wrong canonical child labels and resulting teacher scalar/final. No source resampling, success filtering or semantic repair. Missing/malformed structural capture stops admission as frozen; all missing capture slots remain recorded. Inspect the six predetermined finite/nonliteral-NLL/mask/memory/cost gate examples; no efficacy/NLL>.1 threshold is added.

Check exact fixed24 start, fresh Adam0, six full-corpus updates and per-update checkpoint ancestry,504 Adam moments/RNG/cursors/deltas, current-action-only masks, and producer/reduction/stop masses.45/.50/.05. Confirm observations, child outputs and earlier history are masked; no copied-map/host-gold loss substitution. Count actual commits separately from scheduled recipe steps and capture slots. Record gate/trainer load/forward/optimizer/checkpoint seconds and GPU memory as measured—not CE as scientific success. Protect the declared readout reserve and expose any fixed6 treatment failure.

After terminal/release relay, manually review **every available endpoint's actual program path**, not just successes. Link tool actions to observations by parent-node ancestry plus tool-call identity, not reused call_0 alone. Separate real child acquisition, decoding/file reload, live evidence retention, literal reconstruction, correct requested operator/category/scope/threshold, visible computed scalar and final use. Host computation on retained observed labels is explicitly a diagnostic oracle, not proof the root computed it. Label scalar coincidences, wrong-scope/operator successes, correct-operation failures caused by child semantics, and unsupported finals separately. No keyword/AST presence treated as execution, no sampled code executed. All maps retain actual wrong labels. Primitive retention and nonzero operation-fidelity are decision-relevant controls.

Cost is the union of all on-disk capture/dev/protected physical records and unmatched native role/typed intent records, including incomplete endpoints and late writes. Identify actual calls by stage/request identity and physical path/role; mirrors are not extra calls. Different identical-body requests count separately. Preserve unmatched known returns and unknown intents; do not report missing usage as0. Explicitly distinguish authored transport, physical attempted inference, confirmed HTTP responses/native generations, failed/capped responses, known/unknown input/output/cache tokens, and training forward/optimizer compute. No provider-billing or equal-FLOPs claim. Completed sample artifacts are never discarded because collector/owner harvest failed.

Final closure authenticates OWNER/parent exit, all service releases, actual serial policy order and stage elapsed/caps:8100outer/8070owned/7920work, capture1200, train2100, total dev300/protected3900, finalization420, cleanup150/margin30. Source seal and exact policy/data bindings remain immutable. MAIN terminal relay gates outcome reads; no intermediate outcomes change the recipe, protected selection or audit metrics. Complete report includes source/output hashes, all160 inventory, capture/training/readout costs and limitations, then ranked follow-up implications.
