# Additive terminal-reader handoff

The frozen METHOD and admission implementation are unchanged. Assembly occurred after MAIN's accepted launch, without opening QS dev/protected outcomes. Actual-layout qualification uses the first frozen older operator-completion free coordinate; old scores are not revised. Earlier V1 fixture receipts remain, with V2 receipts pinning tightened stage identity and payload/objective checks.

`terminal_reader.run(output, renderer, relay)` enumerates all160 original slots, verifies frozen source/input pins and exact24/new6+c32 binding, then authenticates all available actual native graph paths using the qualified prior wire reader and the frozen new-study admission rule. Missing RESULT is never promoted. It emits complete code/observation evidence for manual review, not automated fidelity. `panels.summaries` preserves dev/protected separation, primitive/composed and zero/nonzero strata, planned pair bounds and context clustering. All available actual paths still require the frozen post-terminal manual semantic audit.

`cost_union` scans every physical file regardless of RESULT, joins stage/provider IDs/body to role records, excludes authored target transports and retains unmatched failed/intended records separately as unknown rather than adding speculative attempts or zero usage. Successful unmatched role returns count as distinct only with actual provider identity. Provider usage is observed, billing unknown. Capture and both policies remain separate paths. New late records must be included in the terminal output seal, not truncated to these qualification fixtures.

`training_reader.closed_training` reads only closed TEACHER/state artifacts under capture/training; it never accesses readout paths. It checks all current-action labels/masks, code/literal span partition, actual child receipts, six full-pass seeded orders, corpus/previous-state identity, 504 finite Adam moments and ordinals, saved CPU/CUDA RNG payloads (not restored), and the saved role-balanced CE arithmetic. The first checkpoint's parent is fresh Adam0, not inherited moments. Live progress can report committed updates only; fixed6 remains the sole final treatment. The small reader does not reload the 4B model or rerun gradients. Final verification additionally checks actual adapter deltas/start identity and full lifecycle/cap closure from retained files.

Practical disposition follows the already approved allocation/design: positive protected composed strict gain and faithful-executed gain across at least three protected contexts, no protected availability loss, and nonnegative primitive operational difference. Report the joint and separate context counts explicitly. Fidelity-only improvement is a mechanism lead, not net promotion; teacher CE alone is not success. This clarification does not add a post-outcome selection rule.

No QS outcome access until MAIN terminal relay. After that: immutable inventory/native/raw costs, closed training and selection, all-available manual mechanisms including incorrect finals, final parent/release/elapsed union, then full report/seal. No scientific source edits, rerolls, model/code execution, GPU/process/queue authority.
