# Independent post-release audit extension

2026-09-09. Auditor bridge_audit did not author the partition collector or the original ordinal oracle. The original METHOD_READY (5ee936d06d5f68ecc24c79f825fc2d2a29aca839b98428a22974d29915cd65dc), METHOD, primitives and tests remain unchanged. This is an independent execution/reconstruction using those frozen independently written primitives, not a claim that the original method author was independent.

MAIN explicitly released outcome inspection after terminal/GPU release. Before this extension the auditor had MAIN's summary:88calls,8episodes,all5 endpoints0/8; one ordinary parent all12IDs versus gold3 and some256-token parent prose/length. No raw new outcome had yet been opened. Therefore this extension is not outcome-blind; the original method remains the prospective contract.

Additional diagnostics: retain every complete JSON-array substring found with JSONDecoder.raw_decode at each literal `[` position. A candidate must satisfy the original sorted/distinct/in-domain answer validator. Report candidate positions/values and whether any match gold; this never repairs or replaces strict primary scores, and multiple candidates remain ambiguous. Examine direct_short/expanded prompt/seed identity and output prefixes, and identical direct generations across partitions to expose dependence. Distinguish shape-valid child arrays from EOS-completed evidence; do not turn length outputs into complete reports. Reconstruct all actual prompt IDs/decoded generated IDs on CPU with the cached pinned tokenizer; no model import/load, GPU calls, generated-code execution, source fixes, or queue edits.

Freeze actual outcome and parent artifact SHA256s before parsing their values. Authenticate the source closure and independent ordinal data reconstruction. Rebuild dynamic parent messages independently from original records and actual raw child outputs/projections, then compare to requests and native rendering. Recompute all88 physical costs once, all40 endpoint hypothetical pipeline costs (shared extraction charged to both full/lossy), failure/format/cap categories, paired/partition endpoints and exact report implications. Read raw ordinary and direct-expanded content, not only collection summaries. Stop at a bounded sufficient-cap/format or semantic calibration recommendation if direct/parent floors prevent a communication-mechanism conclusion.
