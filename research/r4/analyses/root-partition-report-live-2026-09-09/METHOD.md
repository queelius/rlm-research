# Pre-outcome internal audit

This is an internal audit: its author also wrote the pilot collector. Separately
implemented parsing/reconstruction reduces common-code dependence but does not
make the review independent. MAIN will verify key claims. No output is to be
opened until MAIN reports the accepted partition parent terminal and explicitly
triggers outcome inspection. METHOD_READY timestamps this method/input/code
freeze; later amendments must be additive and say whether outcomes were seen.

Bound experiment: sidecars/root-partition-report-pilot-v1, READY
e87abba449fa1be385c52a3575cc40e5b683bcdd31c706119c3636a59a54cc7a,
accepted parent operations/2026-09-09-after-reducer-partition-pilot.
Four worlds, two partitions/world, one paired sample seed/world,88 planned
physical calls. The sampling unit is the world, denominator4, with8 paired
coordinates. Never call8 coordinates8 partition pairs or independent samples.

Pre-outcome checks reconstruct every synthetic record, RNG order and partition
in a separately written ordinal generator; recompute gold using an A-buyer/
B-buyer relational intersection; verify unique IDs,48 records/world,3×16
partitions, identical original parent record order and paired sample seeds.
Every cross-chunk local winner set is empty while world answers vary. This is
known structural loss in the deliberately lossy report, not model evidence.

After MAIN's trigger, first bind parent terminal/acceptance and actual INPUTS,
MODEL_LOADED and STATUS; authenticate the original frozen source closure.
Record model path/revision, no-adapter binding, native generation config,
CUDA/runtime identity, cold-load time, wall cap and completion/failure status.
Do not infer deployment from an alias or reuse author summary as evidence.

Enumerate all88 expected requests from frozen static requests and fixed parent
order. Preserve missing, request-only, interrupted, length-truncated,
malformed and valid outputs separately. Save artifact hashes and exact IDs.
For each actual call, independently render the messages with the pinned cached
tokenizer (CPU only), compare full prompt IDs and assistant prefix, reconstruct
content from generated IDs using actual EOS, compare raw_generation/content,
and check output token count/finish reason/max budget. Static requests must
equal the freeze; dynamic requests must have original records and the exact
advisory report serialization reconstructed from raw children. Never repair
JSON, trim model prose into an answer, infer an empty answer from an error,
execute generated code or treat a length-truncated report as complete.

Reconstruct each full projection and lossy projection from the same3 actual
raw extraction arrays; keep wrong triples unmodified. Confirm acquisition IDs,
report files and actual parent-visible text agree. Ordinary report strings
must exactly match the3 raw ordinary child outputs, with no oracle injection.
Separate strict shape validity, duplicate rows, evidence precision/recall and
complete extraction. Full/lossy exact symbolic implications and final agreement
with them are distinct from correctness on original records. Ordinary text
has no trusted implication parser; inspect specific error cases with cited
raw calls, and flag ambiguity instead of asserting semantic omissions from
endpoint failure alone. Correct answers do not demonstrate report use.

Primary tables: full/ordinary strict correctness by coordinate and partition;
paired wins/losses/ties/unavailable; each world's both-partitions correctness;
answer flips and jointly correct agreement. Unavailable coordinates remain in
denominator8 with separately reported observed comparisons and worst/best
missing-data bounds. Malformed outputs fail strict accuracy but are separately
counted. Secondary: full/lossy/direct-short/direct-expanded, extraction and
report implication, length truncation, pipeline completeness. No confidence
interval or population claim from four engineered worlds.

Costs: physical calls/tokens counted once; hypothetical full and lossy each
charged the same3 actual extraction calls plus its own parent. Ordinary gets
its3 actual children plus parent, direct only its actual call. Repeated report
delivery is not another child call. Unknown interrupted token counts remain
unknown with known subtotals. Compare summed request latency separately from
GPU/parent wall time and startup. Larger direct output ceiling is not equal
realized compute. Exact CPU oracle uses no model cost and does not establish
neural use of exact operations.

Ranked adaptive successors, proposed only until actual results:

1. If direct-short or expanded is correct on at least6/8 coordinates (ceiling
   flag fixed here), make no useful recursion-benefit claim from this pilot.
   First prepare a fresh harder48-record join with more distinct entities,
   distractors or a new conjunctive condition; keep one difficulty change and
   paired partitions. Start with8 direct/extraction screen calls, then promote
   a bounded full matrix only if it separates meaningful failure modes.
2. If malformed or length-truncated outputs materially drive failures, separate
   parsing/cap from semantics. Rank a fixed native JSON grammar or increased
   child cap comparison first, with the original retained outcomes preserved.
   Never label parse failure as omitted cross-boundary evidence. Choose one
   interface/budget manipulation at a time on fresh seeds, charge its cost.
3. If ordinary cross performance drops and full improves with complete verified
   evidence, first replicate on4 new worlds at same shape; then compare report
   length controls before learning report choice. Require raw ordinary evidence
   supporting omission, not just full-versus-lossy separation by construction.
4. If full extraction is incomplete/incorrect, prioritize4–8 controlled child
   extraction probes varying width or explicit evidence pointers. If full
   extraction is correct but synthesis fails, prioritize a small supplied-exact
   report/root reducer diagnostic with original input visible.
5. If lossy parents recover as well as full/direct, investigate actual report
   adherence separately; do not make parents blind after observing results and
   call it the same experiment. A future visibility factor needs a new design.

Every promoted successor needs fresh immutable input/seed, a discriminating
question, metric, bounded GPU cap, checkpoints and artifact directory before
launch. This method performs no GPU, queue, service or original-source edits.
