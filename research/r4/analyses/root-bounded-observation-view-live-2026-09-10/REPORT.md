# Bounded observation V2: retention becomes usable, strict accuracy remains zero

All 32 planned endpoints scored **0 strict correct**. The 4,096-byte view increased native-final availability under cumulative decoder returns from 2/8 to 5/8, and two trajectories genuinely reduced all 256 acquired labels retained across three batches. Both faithfully computed answers were wrong because the child predictions were wrong. This meets the frozen **mechanism signal**, not an end-to-end accuracy or general-effect promotion.

## Scope and authentication

Question_cards authored bounded V1/V2; bridge_audit independently reviewed this intervention and performed this outcome audit, but authored its inherited accumulation intervention and some runtime/readers. The original METHOD was frozen before launch; reviewer adoption was after launch and before any outcome read. MAIN authorized unblinding only after terminal/release. No sampled program was reexecuted, no answer was repaired, and no scientific source/output was changed. V1 never launched.

Exact science: `sidecars/root-bounded-observation-view-v1/outputs/attempt-002`, accepted READY `889b59bef1acb1e0e14d2e494182cee5b599fa40486bda816b5128893038530f`. Actual collector argv names V2 and all32. Served model directories, configs, advertised models, binding and adapter hashes authenticate fixed24 root `94022838…` and c32 child `c32de129…`. All 32 native first prefixes match frozen inputs, with root sampling .5/1, 2048 action tokens and 8192 context. All 16 within-return-arm view pairs have identical first prefixes and task-file bytes except `.observation_view.json`'s cap. This is a view/config package, not filesystem isolation.

All 440 physical artifacts are in planned directories. All 427 returned native branches have exact typed request-body/completion-token/logprob joins, parsed message/tool-call agreement and finish agreement; typed UUIDs and provider IDs remain separate. There are no unjoined returned branches. Independently recomputed host answers agree for all32. Nineteen RESULT files represent only **11 authentic finals**; missing RESULT is never rescued from wire data.

## Outcomes and pairing

| Return / view | Strict / planned | Native available | Native correct-count bounds | Faithful wrong from child labels |
|---|---:|---:|---:|---:|
| Batch / 20,000 | 0/8 | 2/8 | [0,6] | 1 |
| Batch / 4,096 | 0/8 | 2/8 | [0,6] | 1 |
| Cumulative / 20,000 | 0/8 | 2/8 | [0,6] | 1 |
| Cumulative / 4,096 | 0/8 | 5/8 | [0,3] | 4 |

Conditional correctness among available is also zero in every cell. Operational strict accuracy treats all bounded nonreturns as unsuccessful and is 0/32; the frozen native-final primary preserves 21 NULLs. Native-correct contrast bounds (4096−20000) are [−6,+6]/8 for batch and [−6,+3]/8 for cumulative, not evidence of equivalence.

There are eight blocks within **four exposed parent contexts**, not 32 independent examples. Cumulative availability has three paired gains, no losses, two both-available and three both-NULL pairs; batch has one gain, one loss, one both-available and five both-NULL. All jointly available correctness pairs are both wrong.

| Parent (two size blocks each) | B20000 | B4096 | C20000 | C4096 |
|---|---:|---:|---:|---:|
| scale-state-00 | 1 | 0 | 1 | 1 |
| scale-state-01 | 0 | 1 | 1 | 1 |
| scale-state-02 | 0 | 0 | 0 | 2 |
| scale-state-03 | 1 | 1 | 0 | 1 |

Cumulative 4096 availability is 3/4 on 128 records and 2/4 on 256, versus 2/4 and 0/4 at20000. No confidence or protected-generalization claim is made from four reused parents.

## Every available final: actual semantics

All eleven available programs and linked execution observations were manually read. Seven perform the requested operation over complete authentic predicted maps; four wrongly restrict the actual user scope. Extra nonexistent user names or `weight > 0` are extensionally harmless only where every actual user is included and every actual weight is positive; this was checked against the frozen records.

| Row | Cell | Actual result / gold | Finding |
|---|---|---:|---|
| 0 | B20000 | 87/123 | Omits actual u3. |
| 1 | C4096 | 112/123 | Faithful human-being weight sum on full128 predictions. |
| 3 | C20000 | 0/123 | Invented u00…u04 excludes all actual users. |
| 8 | B4096 | 40/26 | Faithful entity count on full128 predictions. |
| 9 | C20000 | 40/26 | Same requested entity count. |
| 11 | C4096 | 40/26 | Same requested entity count; extra nonexistent u4 harmless. |
| 17 | C4096 | 12/19 | Restricts location count to u0/u1. |
| 20 | C4096 | 151/170 | Faithful location weight sum across three retained batches. |
| 24 | B4096 | 69/89 | Omits actual u3. |
| 26 | B20000 | 84/89 | Repairs an actual NameError, then faithfully sums numeric weights from unchanged full128 map. |
| 30 | C4096 | 36/40 | Faithful numeric count across three retained batches. |

The seven faithful wrong results are not seven independent child-error sources: rows8/9/11 reuse the same input task and predicted entity count. All labels and per-record disagreements are retained in MECHANISMS.json. Scalar agreement alone was not used to infer operator fidelity.

### Two genuine retained-state witnesses

Rows20 and30 belong to parent02 and parent03 respectively. Each actually loads all256 public records, calls the child on disjoint slices `[0:100]`, `[100:200]`, `[200:256]`, and assigns each decoder return to the same live `batch_labels`. Decoder receipts show current sizes100/100/56 and returned snapshots100/200/256; every raw decoder input exactly matches a preceding authenticated child final. There are no conflicts, invented labels, literal reconstructions or missing record IDs in these witnesses.

Their final programs use `batch_labels[record['id']]` over the full original records. Independently checked earlier-batch contributions are 52+71+28=151 for row20 and 17+12+7=36 for row30. Actual tool observations print these scalars; the next authentic native finals repeat them. Thus earlier batches contribute to executed reduction, not merely to an audit-time union.

Row30/control28 is the stronger local illustration: identical first native bodies produce the same initial program, and both follow the same three acquisition programs. The high-cap final request contains the complete actually acquired256-label printed union but has 11,083 prompt tokens and receives HTTP400; no final is fabricated. However the first two child maps differ by one and five labels under identical child wire bodies, so this is not an identical-sampled-map comparison.

Row20/control22 has identical actual first root body, alias/adapter, seed and sampling, but different initial sampled programs **before clipping could act**. Control22 then asks for256 records in one child call, whose8,367-token prompt is rejected. This is unidentified sampling/runtime variation, not treatment-induced planning. Its individual availability contrast cannot be attributed to clipping.

## View exposure and missingness

Nineteen episodes preserve92 raw session tool results, all IPython. Every raw/view pair matches session/turn order, raw hash, visible hash, byte sizes and its actual tool observation. Nine harvested clipped results (two batch, seven cumulative) are consumed in later authenticated root completions; none enters a child request. Visible clipped messages are4205 or4206 bytes: 4096 retained payload plus109/110 warning-marker bytes. Observed UTF-8 boundary loss is zero; the reader separately handles nonzero boundary loss.

For rows20/30, their second/third printed maps are raw6813/8865 and6798/8686 bytes, each presented as4206 bytes. These exact views enter subsequent successful root requests before the next acquisition/reduction; the corresponding complete raw strings are absent from those requests. Native wire text also proves clipping exposure in failed row2, whose full session harvest is missing. Overall20 root request attempts contain omission markers across eight endpoints; no child request does. These wire occurrences are not incorrectly counted as20 distinct clipping events.

Thirteen attempted timeout endpoints lack session harvest, so their exact raw/view event counts and decoder-state histories remain unknown. All233 valid native tool-call arguments were inspected for file/log/state access: observed reads target `records.json`, not session logs or clipping configuration. A proposed `child_labels.json` write in row10 follows a decoder error and is not treated as executed. One malformed wrapper mentions `session_dir` but does not read it. No deliberate raw-log/config access or ledger-as-memory use is observed; existing log access remains possible, not prevented.

The21 NULLs comprise13 attempted over-context HTTP400→181–183 second episode timeouts (ten root, three child) and eight authenticated malformed/unclosed tool branches. All13 retained request prompts exceed the configured8192 tokens; raw HTTP error bodies were not retained, so their exact server prose is unknown. These are bounded policy/runtime failures, not random exogenous dropouts or unstarted slots. Rejected requests are attempts, not established GPU completions.

## Physical cost and decision

| Cell | Attempts / completions | Known input | Known output | Unknown usage requests |
|---|---:|---:|---:|---:|
| B20000 | 110/109 | 316635 | 19766 | 1 |
| B4096 | 119/116 | 342253 | 32102 | 3 |
| C20000 | 115/109 | 262549 | 15125 | 6 |
| C4096 | 96/93 | 257178 | 20317 | 3 |

Total440 attempts/427 completions: roots262/252, children178/175. Known input1,178,615, output87,310, cached-input1,028,528 tokens; each usage field is unknown on13 rejected requests. The independent full union agrees with OWNER's ledger. Legacy per-record `paid_model_call` flags are not billing evidence. Historical training/acquisition is not newly performed here; no source-map acquisition was reused as a new model call. Service preflight is a models GET, not generation. Owner elapsed939.381994s; parent939.873565s, clean exit/release, within1800outer/1770owned/1650work. MAIN's next scientific command followed release by0.49485s.

**Disposition:** the predeclared cumulative-view mechanism signal is met, with two contributing parents; strict task accuracy remains zero. Retention plus a smaller passive view is a useful experimental support configuration for subsequent large-state tests, not learned decomposition, a general accuracy improvement, or a publishable standalone effect. The next informative work should isolate the now-visible child-semantic and scope errors while retaining complete denominators; more blind view shrinking or claims that file/loader access alone solves the task are unsupported.

Artifacts: NATIVE_AUDIT.json (all32/native joins), MECHANISMS.json (all-available annotations, child provenance and views), MATCHED_WITNESSES.json (actual programs/maps and earlier-batch contributions), CLOSURE.json (files/models/costs/failure evidence), ROW_00…31.json (actual code/observations/request text), OUTCOME_PINS.json and FINAL.json. Original method, adoption and scientific artifacts remain immutable.
