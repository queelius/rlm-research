# Fixed held panel: transfer and the remaining output boundary

The original scored results stay unchanged: base 2 raw exact / 29 available / 32 planned; cp32 17 / 32 / 32. On 29 paired available episodes, cp32 has 15 wins and no losses. There are 16 contexts with two seeds each, not 32 independent contexts. This is exploratory transfer evidence on the fixed panel, with prior adaptive exposure limitations, not a confirmatory estimate.

The cp32 controller uses role/ordinal/successor procedure candidates in all 32 episodes, with 30 clean correct target observations, no dictionary/list schema errors, and no broad dumps. Held `teacher_ast_exact=0` is **not applicable**: the analyzer intentionally has no held teachers. It is not evidence of failed procedure acquisition.

## Fifteen cp32 scored failures

- Eight final action-token sequences contain exact semantic gold text, then lose trailing whitespace at the confirmed parser/root clamps.
- Four already omit gold's final two spaces in the model tokens (two contexts, two seeds each).
- One already replaces the digit `1` with `2` at Unicode offset 50 in the model tokens (`omrcr-ef800b2a29008e4717f1`, seed 2026091716), despite clean correct target stdout.
- Two are the same email-about-stores context at both seeds: lexical selector construction fails before retrieval.

All 61 saved native terminal responses (29 base, 32 cp32) were replayed through the actual installed Qwen3 parser. Typed parsed content and scored root agree, with the existing `None`/empty-string representation acknowledged explicitly. Only bare semantic spans before the first configured stop are compared to gold: 23 base and 30 cp32. Invalid tool-call wire is not an answer and is not credited. Bare-token exact diagnostics are base 2 and cp32 25; these do not replace the original 2 and 17 scored successes. The three missing base finals remain unavailable, never zero-filled as ordinary model failures.

## Why the two cp32 retrieval attempts fail

Record `omrcr-746d5e1512abdfa38878`, seeds 2026091718 and 2026091719, asks for the second **email** about stores. Actual user records at indices 3 and 9 are `write a email about stores`; the gold assistant record is index 10. Both generated first programs instead set `request_text = 'write a message about stores'`. There are zero matching user records. Saved stdout reports `ValueError: requested ordinal is unavailable`. The next programs alter ordinal bookkeeping but retain the incorrect request literal; both end with empty invalid terminals. This is an email→message lexical mapping error, not wrong document schema, role filtering, successor indexing, or whitespace trimming.

## Artifacts

The authoritative additive evidence is [HELD_DECODER_SEAM_V2.json](HELD_DECODER_SEAM_V2.json), SHA256 `ba15522db4bf25620a4fae2061d25336eae651eeeeec3d6e1302bb2e95f14dee`, generated by [held_decoder_audit.py](held_decoder_audit.py). It records all 64 coordinates, 61 parser replays, 244 source hashes, final action-token and exact text hashes, and difference operators. Long diff fragments are represented by exact offsets and fragment hashes instead of repeating full answers. V1 is preserved but superseded solely because its category name said “added” edge whitespace for four omissions; V2 labels these correctly as edge-whitespace differences. Counts and primary metrics were unchanged.

Source full readout SHA: `32b72266cf8d2484f4c21db55650d38274990afc88f60dfe2ccfeb2a16bc3a51`. No generated programs were executed, no gold entered a model prompt, no weights were loaded, and no GPU or additional model calls were used. The smallest next test is the separately approved single matched held32 `terminal-strip-disabled` arm, not further SFT or an automatic reward-policy update.
