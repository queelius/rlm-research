# Independent milestone auditor method

Written before this auditor opens any adaptive-campaign outcomes. Main's [METHOD.md](METHOD.md) is authoritative; it is post-launch/pre-outcome, not a prelaunch registration. This reviewer has read frozen scientific sources and MAIN's starting decision, and knows only MAIN's validation0 lifecycle count8/8 with43 physical attempts, not its answers.

## Independence and scope

The storage agent authored the adaptive RL implementation. This auditor authored/shared earlier SFT, root-only pilot and component harness work, and independently reviewed the new RL integration. This is independent outcome reconstruction and integration review, not an independently designed trainer or fresh audit of every inherited line. Do not read SFT outcomes again or use pending campaign outcomes to change the method or experiment.

## Milestones and one-time reads

Process a native stage only after its immutable export/MANIFEST marker and retained STATUS show completion; process a checkpoint only after state.json commits. Audit validation0, transfer0, actual updates1/4/8 and intervening update identities when they exist; validation4/8 and transfer8 supply the fixed readout curve/pairs. Any STOP or FINAL ends the bounded watcher. Missing milestones remain pending before terminal and not-run/censored afterward. A terminal stopped run has no fixedfinal8 effect estimate. Poll only these cheap existence/stat markers about once per minute; cache completed-stage calculations and sources rather than reread old tensors or source closures.

At each newly completed stage independently rebuild strict Answer:N endpoints from raw episode/root replies and authoritative host counts, with malformed/empty completed output0. Separately reproduce exclusion reasons and compare training-admitted reward to the export; recovered provider/finalization failures can retain a real endpoint while being training-null. Retain every planned coordinate and exposure/seed/context pairing. Never use missing-as-zero, label repair or validation-best selection.

Check exact planned task/seed/current-weight generation and raw slot hashes; native graph tokens, current root suffix masks, behavior log-probabilities and root/child role/typed-schema evidence where committed. Use a single stage-level request index rather than rescanning all audits per episode. Actual root and child HTTP attempts/cost include retained failed attempts, and remain distinct from committed/admitted calls. Decode embedded native response JSON usage; preserve truly missing cache/output values and unknown bounds. No recovered physical tail becomes an invented endpoint or training row.

For each new checkpoint authenticate state, adapter/config/Adam/RNG/correction/input/group identity once, inspect actual persistent Adam cursor and finite gradient/delta, and require exact previous-policy chain from efab2913/new generation0. Initial SFT Adam must not enter. A committed checkpoint may precede RESULT; report that distinction without resuming or stepping. Selection is exclusively fixedfinal8, if actually reached.

Publish small per-context endpoint/admission/selected-group metrics as soon as baseline and first update/STOP allow. Defer costly helper/coverage/semantic/manual-explanation work until a completed milestone; identify those secondary checks as pending if not yet done. Chosen examples are descriptive/posthoc, not prespecified causal evidence. All scientific outputs remain unchanged, with zero GPU/network/model/service/signal actions.

Output: additive stage/checkpoint audit JSONs, compact progress/metrics and source manifest, final readable report after terminal. The watcher ends by the recorded campaign inclusive deadline plus existing cleanup grace; it neither launches successors nor follows Qwen144.
