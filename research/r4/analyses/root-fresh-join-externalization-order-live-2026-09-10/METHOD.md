# Prospective independent fresh-order32 audit method

Scope only `sidecars/root-fresh-join-externalization-order-v1/outputs/attempt-001`. No new scientific outcome files have been opened by this auditor. MAIN disclosed generator gold sizes8,7,8,5,7,5,7,2, which are pre-model input properties, and queue status, not model outcomes. Freeze receipt records whether the attempt/row filenames exist at method seal. No GPU/service/lock/launch/queue/source changes or sampled-code execution are authorized.

I did not author this fresh implementation/generator. I authored earlier record-interface/common native/runtime plumbing and the two-backup motivating proposal. This is independent outcome rescoring and mechanism inspection, not an independent originating hypothesis. Reused producer metrics will not be the authority for final correctness. Prior exposed externalization findings motivated this experiment; they are not contemporary control rows.

## Inputs, contemporary contrast and operational closure

Verify READY canonical identity/hash and pinned scientific sources; independently reconstruct all32 coordinates, eight generated worlds, public-ID exclusions against exactly four pinned historical world manifests, seeds, random-index permutation and exact reverse, four-cell dispatch rotations, packages and native prefixes. Recompute gold as the intersection of customers purchasing each of the two requested products from all48 records. Compare supplied HOST_GOLD, preserving any discrepancies rather than repairing science. No outcome-based input replacement.

Check I/E same-order `evidence.dat` bytes and query bytes; I has full JSON both in prompt and context.txt whereas E has empty inline/context.txt. This is a representation package, not all-file-identical prompt-only ablation. Across order arms the record multiset, query, customers and gold must remain identical. No source acquisition/historical reports, gold, solution program or forced retrieval is part of the input. Novelty is only newly generated IDs/fact triples against the four explicit manifests; schema/operators/products/interface are familiar, not globally/pretraining-unseen.

At terminal or explicit failed parent exit, seal all original output hashes before rescoring. Authenticate owner identity/CLI, service descriptor, model/config/live cards, exact released cdbee/noLoRA, vLLM0.28, disabled prefix cache, source wrapper identity and service release. Missing OWNER_TERMINAL is not fabricated; explicit parent exit plus retained release receipts may close a failed attempt. No waiting indefinitely or rerunning endpoints. Retain all32 planned rows even if no raw episode exists.

## Actual native endpoint authentication and strict primary

Independently join each planned coordinate to role-audit request/result, physical wire request/response and native episode graph. Check model/adapter, role/depth/session/request identity, first system/user/tools and actual native prompt IDs against frozen prefixes. Check actual sampling2560/.6/.95/seed,8192context and exact tools. Reconstruct the actual final root branch using graph ancestry/node tokens and verify equality with physical prompt+completion token IDs; authenticate raw transport token/text/usage identity and matching root_reply with no unresolved final tool calls. Never infer native-final availability from lastHTTP200, `completed`, producer score or a code mention.

A returned authenticated final (`stop` or `length`) is observed, including empty/malformed content; strict parser accepts only a JSON array of unique known customer-ID strings in ascending order. No stripping prose/fences, sorting, reordering, repair, answer fallback or salvage. Strict valid exact-gold final1, any other authenticated final0. A length-capped authentic complete strict answer is scored normally and flagged; capped malformed final0. Missing/unverified native final, final-less loops, unresolved tools or unreturned transport remain NULL, with failure categories and operational0 sensitivity separately. Intermediate tool calls are not themselves final responses and are never executed by this audit.

Report each of32 coordinates, each of four8-endpoint cells and marginal I/E16 with available/NULL/valid/correct. Primary planned-denominator lower/upper bounds are correct/N and(correct+NULL)/N. Give conditional-on-authenticated-final accuracy only with its explicit denominator. Keep strict-final correctness shared in both representations; file use is not an admission or correctness requirement.

## Pairing, order sensitivity and mechanism

The eight worlds are the independent clustering units; all four cells/world and shared seed are repeated measures. Report eight world tables and within-world I−E contrasts separately by order and averaged across order. Report random/reverse correctness discordance and strategy change in each representation, plus descriptive representation×order contrast. NULL-paired comparisons remain interval/unknown, not automatic losses. No32-world/item independence, selective complete-pair primary or confirmatory p-value claims. All golds happen to be nonzero; report that realized support, not evidence of target-size selection.

Inspect every actual Python/tool program alongside its immediate observation and prior visible state, without execution. Separate: successful evidence.dat/context.txt/query.txt retrieval; preview-only versus complete relevant records; faithful versus corrupted manually copied literal records; acquisition/reclassification/child calls; executed grouping/intersection/other valid join; one-pass order-sensitive state overwriting or incomplete accumulation; actual computed observation versus manual final/arithmetic; final concordance with the observed result. File-read code alone is not successful retrieval, AST structure alone is not executed aggregation, and correct final alone is not a faithful mechanism. A correct computation on faithful literal data can count as a valid mechanism without file use. If data flow cannot be established from retained evidence, mark uncertain, not false or true. Summarize all32 before selecting illustrative traces; cite concrete gain/loss/order-discordant cases and representative successful paths. Across two orders, describe observed invariance only, not universal algorithmic invariance.

## Physical costs and decisions

Deduplicate actual request IDs, count physical request attempts, authenticated/returned completions, root versus optional child, pretransport failures and unreturned requests. Sum known input/output/cache/uncached tokens and retain missing usage counts. Count failed attempts in observed costs; do not infer billing or exact FLOPs from tokens. Reconcile episode/cell sums against raw ledger and owner elapsed. Source acquisition cost is exactly zero here; CPU serialization/rendering cost stays separately declared. Report copying/retrieval/call/output costs with availability, not only successful episodes.

Interpretation: retrieval/lower-copying cost without strict accuracy improvement supports a packaging/strategy result, not stronger join competence. Correctly loaded but order-sensitive errors support a reduction-algorithm follow-up. Stable strict correctness across both orders on eight new worlds provides bounded transfer evidence beyond four exposed worlds, not broad competence. Keep unresolved infrastructure separate from scientific evidence and rank follow-ups only after all planned evidence is accounted.

## Read-depth and parser status at freeze

Fully read fresh DESIGN, PLAN, study, protocol, collect, native, join_taskset, owner, service_wrapper and prepare; fully read reused root-native-partition-join protocol/native and root-example-map-visibility metrics. No output content from this fresh attempt was read. Existing frozen input manifests/READY are scientific source inputs. Parser implementation/CPU fixture seals will be timestamped separately; later parser work will not be called pre-generation if the job has already started. Method changes after outcomes, if any, require explicit additive amendment and reason.
